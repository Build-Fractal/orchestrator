# Disputes — activation-loop (capture→store→inject round-trip perspective)

*Phase 4, cooperative. Final statement after reading all three revisions. I do not re-litigate my Phase-3 concessions (live-runtime-memory-read withdrawn; the `orchestrator:note`/`:decide` net-new command name withdrawn). Both stay withdrawn.*
*Date: 2026-06-06.*

---

### Remaining Disputes

**Dispute: explicit-decision-capture-at-Quick belongs in P0, not deferred** — This is the dispute the prompt names, and I state my non-concession crisply: **it is already converged, not disputed.** Both other agents moved to my position in revision. framework-steward's New Recommendation A (`ship-G1-capture-slice-in-P0`, Priority P0) carves "always run `append-decision.sh` for explicit decisions even at Quick" into the P0 hotfix as the minimum capture loop, explicitly *conceding* the residual intensity-is-deliberate / Context-Minimization concern via the capture-cost-vs-inject-cost split: a disk row-append carries **zero inject-token cost** and "must **not** inherit FR-6's budget hedge — it ships in P0 unbudgeted because a disk row-append is not a Principle-I concern" (framework-steward Rec 7b). pipeline-reliability Rec 11 independently pulls FR-6 + the explicit-decision-capture half of FR-8 into P0. **My non-concession stance, for the record:** the explicit-capture primitive ships P0 *non-negotiably* (activation-loop revision Rec 2), and the load-bearing reason survives verified — M040's formal trigger has NOT cleanly fired (§0 meets trigger #2 but not #4; #4 needs ≥5 inbox reports, §0 supplies one — confirmed by both reviewers), so "fold into M040 provided it ships soon" defers the capture primitive *indefinitely* with no committed date. I will not concede this to any residual Context-Minimization framing because the Principle-I objection was already dissolved by the capture-cost/inject-cost distinction: capture is free, only inject is budgeted. *Because this is converged, it is not a live dispute — recording it here only to discharge the prompt's directive and to flag synthesis against re-opening it.*

**Dispute: capture is *co-primary* P0, not subordinate to the alarm** — *My claim:* the explicit-capture primitive (FR-6 + G-1) and BUG-A unification (FR-1) are **co-primary** P0 deliverables, sequenced *after* the FR-15 alarm in build order but equal in P0 membership (activation-loop New-1: "the alarm is the substrate, capture is a load-bearing tenant; both P0, alarm has no dependencies"). *Opposing position:* pipeline-reliability's framing occasionally reads the alarm as P0-primary and capture as the tenant (pipeline-reliability New Rec, and their Position Summary leads with "the alarm is the substrate that makes *every* P0 fix verifiable"). *Evidence vs advocacy:* this is an emphasis residue, not a substance fork — pipeline-reliability Rec 11 *concedes* the two capture items into P0 membership explicitly, and their Scope Discipline calls them "load-bearing tenants; both are P0, the list is unordered." *Why I will not concede:* the field-note failure (50+ decisions, two milestones, 0-MEM injects) was a *capture* failure observed *through* a missing alarm — both halves are co-primary or the hotfix re-ships the incident with a smoke detector over an empty room. *Counter-argument:* a synthesizer could read "alarm is substrate" as a priority ranking and down-rank capture to "nice-to-have within P0." *Proposed resolution path:* synthesis states P0 as an **unordered set** with an intra-P0 *build sequence* (alarm first by dependency, capture immediately after) — sequence is not priority. This is a 90%-converged item; flagging only to prevent a priority-vs-sequence mislabel.

**Dispute: the round-trip oracle (AC-1) must assert against the *observed awk field indices*, not the documented column order** — *My claim:* DQ-6 reconciles **three** disagreeing shapes — producer, consumer-*comment*, consumer-*awk* — and the round-trip oracle must assert against the consumer's *executing* awk (`scope_col=$5`, `when_col=$6` under leading-pipe `awk -F'|'`), because the consumer's own comment disagrees with its own code (activation-loop Rec 3, Rec 9: a **four**-call-site byte-alignment assertion). *Opposing position:* none in *direction* — both reviewers adopt consumer-order-wins and the four-artifact realignment (pipeline-reliability Rec 3; framework-steward byte-equality fixtures). The residual risk is that a synthesis collapses DQ-6 back to "producer vs consumer" (two shapes) and authors the fixture against the *documented* column order, which is the exact B-3 trap. *Evidence vs advocacy:* evidence — the consumer's comment (`scope-filter.sh:338/351`) and its awk (`:353-354`) are two distinct artifacts I independently re-verified; a fixture written to the comment passes review and still mis-extracts scope at runtime. *Why I will not concede:* if the acceptance oracle asserts against documented order rather than executing order, the round-trip test goes green while the bug it was built to lock is still live. *Counter-argument:* "the comment and awk should just be made to agree, so asserting against either is fine post-fix." True post-fix — but the *test* must assert against runtime behavior so it stays load-bearing if the comment drifts again. *Proposed resolution path:* synthesis records DQ-6 as reconciling **three** shapes and pins the AC-1 assertion to the *observed awk field indices*, with the four-call-site alignment (init header, append script, consumer comment, consumer awk) as the CI-run checklist item.

---

### Convergence

**Converged: BUG A (B-3 format-mismatch) is co-primary P0, named first** — *Shared position:* B-3/FR-1 producer/consumer format unification ships P0, foregrounded as co-primary with B-1, ranked by *which-store-is-unreadable* (B-3 makes the **raw store** unreadable — worst; B-1 makes only the **index** absent — recoverable via the index-free fallback). *Agreeing agents:* all three (activation-loop Rec 1; pipeline-reliability "Concession on activation-loop's central point" + Rec 11; framework-steward Position Summary "I now *foreground* it as co-primary using activation-loop's cache-vs-source-of-truth framing"). *Strength:* very high — "zero daylight" (framework-steward). *Path to convergence:* pipeline-reliability's own P-B principle (cache may be absent) logically ranks a truth-layer failure above a cache-layer failure, which they conceded explicitly.

**Converged: round-trip test is the acceptance oracle, merged with fixture-byte-equality** — *Shared position:* AC-1 runs the capture→rebuild→grep→assert round-trip over the *legacy documented* primitives on a *default-intensity (Quick)* fixture project AND asserts the injected payload **byte-for-byte** against a committed expected file — one fixture, both guarantees (coverage + byte-stability) — with the *dynamic* round-trip oracle split from the *static* byte-equality fixtures so they don't collide. *Agreeing agents:* all three (activation-loop Rec 6 + New-2; pipeline-reliability Rec 3; framework-steward New Rec C). *Strength:* high. *Path to convergence:* pipeline-reliability's `[round-trip-AC-green-on-a-store-no-alarm-watches]` resolution merged my coverage demand with their determinism demand into one AC.

**Converged: one system of record = `.orchestrator/` via graduation, NOT live-read** — *Shared position:* `.orchestrator/` is the system of record; graduation into it is the **sole** mechanism on the guarantee path; live-runtime-memory-read is cut entirely (deferred to M009-era runtime-memory-adapter work); enforcement = FR-15 0-MEM warning + doctor check flagging runtime-memory decisions absent from `.orchestrator/` (divergence loud in P0, impossible via graduation in the M040 track). *Agreeing agents:* all three (activation-loop Rec 4 — I withdrew live-read; framework-steward Rec 6 — live-read out of scope on Principle VI; pipeline-reliability `[read-runtime-memory-live-vs-index-free-determinism]`). *Strength:* very high. *Path to convergence:* I conceded live-read couples dispatch to a runtime-specific shape (latent M009 debt); the enforcement clause I authored survives intact and satisfies the requirement without any live-read.

**Converged: capture command = M040's existing `/orchestrator-capture` + `/orchestrator-promote`, pulled forward as mechanism not UX** — *Shared position:* no net-new top-level command; FR-7 extends M040's already-on-disk `/orchestrator-capture` + `/orchestrator-promote` with round-trip-confirmation + local decision-vs-knowledge classification ownership (zero GitHub/Giscus dependency); the **command UX** lands M040-track, the **underlying confirmation mechanism + the one-line explicit-capture primitive** M040 will wrap ship P0. *Agreeing agents:* all three (activation-loop Rec 5 — I withdrew the new-command name; framework-steward Rec 4 — decouple name from timing; pipeline-reliability "build the mechanism in P0, expose the command in P1"). *Strength:* very high. *Path to convergence:* framework-steward proved M040 already specifies these commands on disk; minting a new verb would fork the capture loop.

**Converged: fail-loud + 0-MEM warning + deterministic-grep floor, all budget-bounded** — *Shared position:* P-A fail-loud (visible warning + minimal provenance header in payload, full provenance to stderr/evidence-artifact); FR-15 0-MEM-on-mature-project warning in P0; deterministic grep is the *resolved* guarantee floor and the sole input to the corpus-gate evidence artifact (embeddings additive-only, never gate, never in the receipt; `LC_ALL=C`, no wall-clock, stable ordering); every index-free grep-fallback retrieval path budget-bounded through the existing M036a token-budget governor (SC-3/SC-7). *Agreeing agents:* all three (DQ-2 jointly resolved across all revisions; pipeline-reliability New Rec budget-bound + Rec 8; framework-steward Rec 2/3/7; activation-loop Rec 10). *Strength:* very high — DQ-2 moves from "open" to "jointly resolved." *Path to convergence:* my round-trip same-instant-confirmation rationale and the Principle-IX reproducibility rationale are complementary, both load-bearing under the one invariant.

---

### Final Position Statement

**Non-Negotiables**

1. **The explicit-decision-capture-at-Quick primitive ships P0**, in the same change set as FR-6's compact bounded Decisions digest (activation-loop revision Rec 2; concurred by framework-steward New Rec A, pipeline-reliability Rec 11). Why: M040's formal trigger has not cleanly fired (§0 meets #2, not #4 — verified by both reviewers), so deferral is indefinite, and FR-6 in P0 *without* the capture primitive ships a Quick Decisions slot that is empty forever — a fail-open subtler than the original bug because the surface reports healthy. The Principle-I objection is dissolved by capture-cost (free) vs inject-cost (budgeted). This is the one item I refuse to concede.

2. **The round-trip oracle asserts against the observed awk field indices** (`$5`/`$6` under leading-pipe), reconciling all four artifacts — producer, init header, consumer comment, consumer awk — in one commit (activation-loop revision Rec 3, Rec 9). Why: asserting against the *documented* column order re-creates the exact B-3 defect with a green test over it. The test must lock runtime behavior, not documentation.

**Flexibility**

1. **P0 build *sequencing* — fully flexible; P0 *membership* — preserved.** I accept alarm-first build order (FR-15 + index-free fallback have no dependencies; capture + BUG-A layer on top — activation-loop New-1). What must be preserved: capture and BUG-A remain *co-primary P0 members*, not deferred — the set is unordered even if the build sequence is not.

2. **Capture *command UX* timing — flexible (M040-track); capture *mechanism* timing — preserved (P0).** I conceded the discoverable command UX, auto-graduate-from-execution-log, and the two-store *graduation build* to the M040 track. What must be preserved: the minimum write-side primitive + the round-trip confirmation mechanism M040 will wrap ship P0 on a committed timeline.

3. **Provenance-header schema shape — flexible.** I accept the minimal-payload-header + full-schema-in-stderr split (pipeline-reliability Rec 4/8; framework-steward New Rec C) and the P1 timing for the full header byte-fixture. What must be preserved: a **resolved-id surface** the capture-confirm step can assert against — the header has two consumers (downstream M034/M038 parsers AND the synchronous capture-confirm), and the schema must serve both.
