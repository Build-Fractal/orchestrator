---
schema_version: "1.0"
type: task-summary
id: "T04"
parent: "P03"
milestone: "M004"
provides:
  - "scripts/engine/run.sh (361 lines, executable) — T04 extends T03 by integrating the context-assembly pipeline (build-context → compress-payload → select-model) into the engine task loop and wiring the DISPATCH_START event. Added: (1) pre-loop select-model.sh standard invocation reading templates/routing.yaml that populates $_selected_model and $_context_budget with a hard-coded fallback (claude-sonnet-4-6 / 150000) on any failure plus emit_event SAFETY_WARNING reason=select_model_fallback then reason=model_selected with model+budget for observability; (2) per-task context-build + compress pipeline that creates two mktemp temp files ($_payload_file, $_compressed_file), runs bash scripts/dispatch/build-context.sh .specify/orchestrator $ENGINE_MILESTONE $ENGINE_PHASE $task_id > $_payload_file and bash scripts/dispatch/compress-payload.sh --budget $_context_budget --input $_payload_file > $_compressed_file with SAFETY_WARNING + outcome=failed + continue on either failure, then swaps $_payload_file to point at the compressed variant so the downstream payload_sanity guard inspects the compressed file; (3) payload_bytes + tokens_est computation (payload_bytes/4 matching compress-payload.sh estimator); (4) emit_event DISPATCH_START with task/model/tokens_estimated/payload_bytes fields and dry_run=1 flag in dry-run mode; (5) dispatch stub block that writes dry-run or stub-dispatch output to $_output_file via mktemp and emits SAFETY_WARNING reason=dispatch_skipped_dry_run or dispatch_stub; (6) provisional TASK_COMPLETE outcome=dispatched emission with model+tokens_estimated fields preserving the one-TASK_COMPLETE-per-iteration invariant; (7) extended EXIT trap cleaning $_payload_file/$_compressed_file/$_output_file alongside $_pending_tmp; (8) relocation of the T03 guard_payload_sanity block to run AFTER the context-build pipeline (as required by the P03 phase plan risk callout) so the guard inspects a real compressed payload file in real mode instead of the unset-variable path that T03 documented as a temporary state."
requires:
  - "from:P03/T03 what:T03 guard block with literal-audit-marker printf workaround for single-word-value rendering in events.sh _orch_events_quote; from:P03/T02 what:walking skeleton with arg parsing/session lifecycle events/task loop/run_hooks PRE_DISPATCH integration and _pending_tmp cleanup pattern; from:P02/T04 what:guard_payload_sanity/guard_budget/guard_output_sanity/guard_phase_complete public API returning 0-on-pass-or-forced; from:P02/T02 what:emit_event DISPATCH_START/SAFETY_WARNING/TASK_COMPLETE canonical types from the 19-entry registry; from:P02/T03 what:orch_is_dry_run helper and ORCH_DRY_RUN export contract; from:P04/T02 what:scripts/dispatch/build-context.sh signature <orchestrator-root> <milestone-id> <phase-id> <task-id> writing payload to stdout; from:P04/T02 what:scripts/dispatch/compress-payload.sh --budget TOKENS --input FILE writing compressed payload to stdout with chars/4 tokens estimator; from:P04/T02 what:scripts/dispatch/select-model.sh <tier> --routing-config <file> writing <model-id> <context-budget> tuple to stdout; from:P04/T01 what:templates/routing.yaml standard tier model and context_budget fields"
affects:
  - "P03/T05 (verify/record/checkpoint_write pipeline replaces the provisional TASK_COMPLETE outcome=dispatched emission with a verify-gated outcome and consumes $_output_file before the current cleanup rm); P03/T06 (resume-from-checkpoint E2E runs through the now-wired dispatch pipeline); P06 (check-must-haves.sh PROJECT_ROOT detection bug and events.sh single-word-value quoting issue both flagged for cleanup — T04 preserves the T03 literal-audit-marker printf workaround unchanged); US4 AS1 (DISPATCH_START event carries model + tokens_estimated + payload_bytes fields per spec); FR-223 context envelope (engine uses build-context and compress-payload as the single source of dispatch payload); FR-226 model routing (engine resolves model + budget from routing.yaml once per session); Principle II structured events (DISPATCH_START + SAFETY_WARNING family emitted at every stage transition); Principle IX frozen timestamps (all new events use orch_now via emit_event — no inline date calls introduced)"
key_files:
  - "scripts/engine/run.sh"
key_decisions:
  - "Relocated the T03 pre-dispatch guard_payload_sanity block to execute AFTER the T04 context-build + compress pipeline per the P03 phase plan risk callout — the T03 block previously inspected an unset $_payload_file in real mode (emitting SAFETY_WARNING payload_file_unset), now it inspects the real compressed payload file the build pipeline just produced; chose the option-2 flow from the plan where the compressed file REPLACES $_payload_file (rm -f the raw, assign compressed path to _payload_file, clear _compressed_file) so the guard and the rest of the pipeline operate on the compressed variant without duplicating state; implemented the select-model.sh fallback EXACTLY per plan with a two-line if/else — success path parses stdout with two awk '{print $1}' / '{print $2}' calls and uses parameter expansion defaults (${var:=fallback}) as a belt-and-suspenders safeguard, failure path hard-codes claude-sonnet-4-6 + 150000 and emits SAFETY_WARNING reason=select_model_fallback tier=standard; the plan notes the deliberate choice to emit model_selected as SAFETY_WARNING (not a new MODEL_SELECTED event type) so we do not trigger the unknown-type companion warning in events.sh and preserved that comment verbatim; extended the EXIT trap to clean $_payload_file/$_compressed_file/$_output_file alongside $_pending_tmp per plan Step 2 so crash exits never leak temp files; preserved the T03 literal-audit-marker printf workaround on both the pre-dispatch and post-dispatch dry-run branches unchanged because the P03 phase plan explicitly defers the events.sh quoting fix to P06; provisional TASK_COMPLETE outcome=dispatched emission replaces T02/T03 skeleton outcome=dry_run/skeleton_noop so the invariant one-TASK_COMPLETE-per-iteration holds across all three paths (blocked-continue from T03 guards, failed-continue from T04 build/compress failures, and success path); dry-run mode still emits DISPATCH_START with dry_run=1 flag per plan Step 4 so observability is symmetric; no files outside scripts/engine/run.sh were modified; no changes to checkpoint.sh or any P02/P04 lib"
patterns_established:
  - "Context-assembly pipeline pattern — mktemp for raw payload, invoke build-context.sh with SAFETY_WARNING+failed+continue on error, invoke compress-payload.sh with same error handling, then swap raw→compressed on $_payload_file so the rest of the pipeline only deals with the compressed variant; select-model-once-per-session pattern — resolve model id and context budget from routing.yaml via bash scripts/dispatch/select-model.sh standard before the task loop with hard-coded fallback (claude-sonnet-4-6 / 150000) and SAFETY_WARNING reason=select_model_fallback on any failure; model-selected-as-safety-warning pattern — avoid new MODEL_SELECTED event type since the 19-entry canonical registry does not include it and the unknown-type companion warning machinery would trigger; DISPATCH_START with payload-shape fields pattern — emit task/model/tokens_estimated/payload_bytes (with dry_run=1 in dry-run mode) matching US4 AS1 spec so downstream observability gets the full dispatch envelope; dispatch-stub pattern — write a non-empty stub output file in both dry-run and real modes so the downstream output-sanity guard sees a real file while P05+ agent dispatch is still unimplemented; extended-trap-multiple-tempfiles pattern — single EXIT trap cleans all four temp files (_pending_tmp / _payload_file / _compressed_file / _output_file) with individual 2>/dev/null redirections so partial-state exits do not emit spurious errors; guard-relocation pattern — when a later task adds a step that populates a variable an earlier task's guard depends on, the later task relocates the guard (not the earlier task) so each task stays an additive edit"
drill_down_paths:
  - "scripts/engine/run.sh,scripts/dispatch/build-context.sh,scripts/dispatch/compress-payload.sh,scripts/dispatch/select-model.sh,templates/routing.yaml,.specify/orchestrator/milestones/M004/phases/P03/tasks/T04-PAYLOAD.md,.specify/orchestrator/milestones/M004/phases/P03/tasks/T03-SUMMARY.md,scripts/lib/events.sh"
duration: "15m"
verification_result: "pass"
completed_at: "2026-04-10T23:55:00Z"
---

Extended scripts/engine/run.sh from 285 to 361 lines (+76) by integrating the dispatch context-assembly pipeline into the T02/T03 task loop per the T04 dispatch payload plan. Before the task loop, added a select-model.sh standard invocation that reads templates/routing.yaml and populates $_selected_model and $_context_budget, falling back to claude-sonnet-4-6 / 150000 with a SAFETY_WARNING reason=select_model_fallback if the script exits non-zero. A second SAFETY_WARNING reason=model_selected carries the resolved model and budget for startup observability — per the plan this is deliberately a warning not a new event type because the 19-entry canonical registry does not include MODEL_SELECTED and the unknown-type companion warning would otherwise fire. Inside the task loop, inserted the context-assembly block immediately after run_hooks PRE_DISPATCH and BEFORE the T03 pre-dispatch guards: mktemp $_payload_file and $_compressed_file, invoke bash scripts/dispatch/build-context.sh .specify/orchestrator $ENGINE_MILESTONE $ENGINE_PHASE $task_id > $_payload_file with SAFETY_WARNING + TASK_COMPLETE outcome=failed reason=build_context + cleanup + continue on failure, then invoke bash scripts/dispatch/compress-payload.sh --budget $_context_budget --input $_payload_file > $_compressed_file with the same error-handling pattern for reason=compress. After both succeed the block rm -f's the raw payload and reassigns $_payload_file to point at the compressed file so the downstream guards and DISPATCH_START inspect the compressed variant. Then computed $_payload_bytes via wc -c and $_tokens_est via integer division by 4 (matching compress-payload.sh's own estimator comment). Per the phase plan risk callout and the parent task instructions, the T03 guard_payload_sanity block was RELOCATED from its original position (immediately after run_hooks PRE_DISPATCH where $_payload_file was unset in real mode producing a SAFETY_WARNING payload_file_unset notice) to run AFTER the context-build pipeline — it now inspects the real compressed payload file. The dry-run branch still emits the SAFETY_WARNING reason=dry_run_guard_skipped guard=payload_sanity companion and the literal-audit-marker printf 'EVENT:SAFETY_WARNING_AUDIT ... guard="payload_sanity" ...' workaround for the events.sh single-word-value quoting issue — this workaround is preserved unchanged per parent instructions and T03's patterns_established entry, flagged for P06 cleanup. After the guards, emit_event DISPATCH_START is emitted with task / model / tokens_estimated / payload_bytes fields (with dry_run=1 added in dry-run mode). Then the T02 dry-run/real-dispatch placeholder block was replaced with a dispatch stub block: mktemp $_output_file, write 'dry-run: M### P## T##' to it and emit SAFETY_WARNING reason=dispatch_skipped_dry_run in dry-run mode, or write 'stub-dispatch: M### P## T## (agent invocation not implemented in M004)' and emit SAFETY_WARNING reason=dispatch_stub in real mode. This ensures the downstream T03 output-sanity guard sees a non-empty file in both modes. The T03 post-dispatch output-sanity guard block was preserved (not relocated — it already ran after the placeholder); its dry-run branch still uses the literal-audit-marker printf. Finally the T02/T03 two-branch TASK_COMPLETE placeholder (outcome=dry_run or outcome=skeleton_noop) was replaced with a single provisional TASK_COMPLETE outcome=dispatched emission carrying model + tokens_estimated fields, preserving the invariant that exactly one TASK_COMPLETE fires per task iteration across all paths (blocked-continue from T03 guards, failed-continue from T04 build/compress failures, and the success path). The EXIT trap was extended to clean $_payload_file / $_compressed_file / $_output_file alongside $_pending_tmp with individual 2>/dev/null redirections so partial-state exits do not emit spurious errors. Per-iteration cleanup rm -f's $_payload_file and $_output_file at the end of each successful iteration and re-empties the variables so the next iteration starts clean. Verification: bash -n passes, all 7 plan static checks pass (syntax / build-context / compress-payload / select-model / DISPATCH_START / all three tmp vars), all 7 plan runtime must-haves pass (dry-run exit 0 / DISPATCH_START event / TASK_COMPLETE event / PHASE_COMPLETE event / RESULT ok / no inline date / no-args exit 2 with CONFIG error_kind), and all 5 phase-level invariants still hold (head-5 double-sourcing guard / all 5 P02 libs sourced / init_run_context called / all 5 lifecycle events emitted / checkpoint.sh untouched). Line count 361 (min 260 per plan). Dry-run invocation with ORCH_RUN_SEED=t04-dry ORCH_DRY_RUN=1 bash scripts/engine/run.sh --dry-run M004 P03 produced 3 DISPATCH_START events (one per pending task T04/T05/T06) each with model=claude-sonnet-4-6 / tokens_estimated=8772|8336|7267 / payload_bytes=35091|33346|29069 / dry_run=1, 3 TASK_COMPLETE outcome=dispatched events, 1 PHASE_COMPLETE completed=3 blocked=0, and final RESULT:{"status":"ok","error_kind":"","detail":"engine completed M004/P03 (dry_run=1, completed=3)"}. No files outside scripts/engine/run.sh were modified — checkpoint.sh, guards.sh, events.sh, build-context.sh, compress-payload.sh, select-model.sh, and templates/routing.yaml remain identical to pre-T04 state. Duration 15m, 0 retries.
