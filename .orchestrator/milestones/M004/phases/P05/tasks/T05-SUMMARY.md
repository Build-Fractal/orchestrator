---
schema_version: "1.0"
type: task-summary
id: "T05"
parent: "P05"
milestone: "M004"
provides:
  - ".specify/orchestrator/milestones/M004/phases/P05/fixtures/run-parity.sh — 240-line Bash 3.2 end-to-end parity and emission-audit harness runnable from repo root that exercises the full dispatch pipeline (build-context.sh -> compress-payload.sh -> select-model.sh) against the T02/T03 golden fixtures across six check groups (build-context parity after line/token/entry-count normalization; compress-payload parity at budget 2000; select-model default-mode parity on all 3 tiers; --list-fallback on all 3 tiers; --next-fallback chain walk opus->sonnet->haiku->exhausted; EVENT/RESULT emission audit on all 3 scripts) and exits 0 iff every parity check passes, treating the single known pre-existing build-context emit_event>/dev/null swallow as XFAIL so the regression surface stays visible but exit 0 is preserved; .specify/orchestrator/milestones/M004/phases/P05/fixtures/README.md — documentation of the normalization rules, regeneration recipe, check-group catalogue, known-P06 XFAIL list, and self-cleaning contract"
requires:
  - "from:M004/P05/T02 what:golden-payload-M004-P04-T04.md golden captured by build-context.sh pre-refactor baseline; from:M004/P05/T03 what:golden-compressed-budget2000.md golden captured by compress-payload.sh pre-refactor baseline fed with the build-context golden; from:M004/P05/T04 what:select-model.sh --list-fallback and --next-fallback CLI contract for check groups 4 and 5; from:M004/P02/T01 what:scripts/lib/errors.sh emit_result + 6-kind taxonomy (VERIFY for harness failure, status=ok on success); from:pre-existing what:templates/routing.yaml ships expected fallback chains (heavy=claude-sonnet-4-6,claude-haiku-4-5; standard=claude-haiku-4-5; light=empty) consumed by check groups 3/4/5; from:M004/P05/T02 subshell-scope pitfall lesson what:variables mutated inside $() do not propagate to parent — use a private mktemp -d instead of a tracking variable"
affects:
  - "P05 phase-level verification gate — T05 is the final P05 task and the harness is the mechanical parity proof the verify step will invoke; P06 engine-managed scripts refactor — harness provides a regression-detection surface that will catch any further parity drift when P06 edits build-context/compress-payload/select-model; P06 scope candidate — the XFAIL for build-context.sh emit_event>/dev/null bug is now explicitly documented and tracked so P06 can close it; no P05 production script touched (only adds two new files under fixtures/); FR-213/US10 retry primitive end-to-end coverage: check groups 4 and 5 now exercise the --list-fallback and --next-fallback paths added in T04"
key_files:
  - ".specify/orchestrator/milestones/M004/phases/P05/fixtures/run-parity.sh,.specify/orchestrator/milestones/M004/phases/P05/fixtures/README.md,.specify/orchestrator/milestones/M004/phases/P05/fixtures/golden-payload-M004-P04-T04.md,.specify/orchestrator/milestones/M004/phases/P05/fixtures/golden-compressed-budget2000.md"
key_decisions:
  - "Treat the pre-existing build-context.sh emit_event>/dev/null 2>&1 swallow as XFAIL rather than FAIL in check group 6 because the dispatch instructions explicitly forbid modifying build-context.sh (P06-deferred) and because the plan's own constraint 3 authorizes 'report as P06 scope candidate, dont patch' for known pre-existing issues; this is the only substantive deviation from the plan verbatim and preserves both the 'harness exits 0' must-have and the regression-visibility invariant. Use a single owned mktemp -d -t run-parity-XXXXXX tmpdir with trap EXIT INT TERM HUP -> rm -rf rather than a tracking-variable + rm-on-exit pattern: the classic bash pitfall where variable mutations inside $(_track_tmp) subshells do not propagate to the parent shell bit the first draft (8 temp files leaked into /var/folders); the owned-tmpdir pattern is immune to subshell scoping because every temp file lives under the directory and the directory itself is the only thing the trap needs to touch. Source scripts/lib/errors.sh ONLY (not run-context.sh or events.sh) to avoid triggering the _orch_run_nonce SIGPIPE workaround that T02/T03/T04 all carry: the harness does not need a seeded run_id, does not emit EVENT lines of its own, and must not write to the execution log; minimising sourced libraries also keeps the harness robust to the known-P06 run-context bug. Add an XFAIL level to the pass/fail taxonomy (pass_count/fail_count/xfail_count) instead of silently skipping: XFAIL lines are clearly labeled 'XFAIL: ... (P06 scope -- not counted)' and the harness summary reports 'pass=N fail=N xfail=N' so a future reviewer can tell a deliberate XFAIL from a missed assertion. Re-worded the 'no declare -A / readarray / mapfile' comment after the initial draft failed the Bash-3.2-compat must-have (which greps for those literal tokens) — the regex matcher cannot distinguish code from comments, so the comment now says 'no assoc arrays, no array-read builtins' instead of naming the forbidden syntax tokens directly."
patterns_established:
  - "Parity harness pattern for the P05 refactor trio: cd $(git rev-parse --show-toplevel) -> source errors.sh (and only errors.sh) -> allocate private mktemp -d tmpdir with trap EXIT INT TERM HUP rm -rf -> normalize() sed pipeline for volatile manifest columns (LINES, ~TOKENS, (N entries)) -> run refactored script, capture output, normalize both sides, diff -q -> emit_result ok/error VERIFY once on exit. Subshell-safe temp-file allocation: never use a tracking variable mutated inside a function called via $(), always mktemp inside a pre-allocated owned directory and rm -rf the directory on trap EXIT. XFAIL classification for known pre-existing bugs during a refactor phase: add a third pass/fail/xfail bucket so known regressions stay logged and visible without blocking a verification gate, and document each XFAIL with a one-sentence P06 scope note next to the check. Parity normalization for manifest tables: the three volatile columns are line ranges, token estimates (rounded to nearest 100), and knowledge entry counts — strip these before diff so parity captures semantic payload shape but not cosmetic churn. Minimal-library sourcing in test harnesses: only source what you need to emit RESULT — avoid run-context.sh and events.sh in harnesses that do not need a seeded run_id so the harness stays robust to library bugs being fixed in later phases."
drill_down_paths:
  - ".specify/orchestrator/milestones/M004/phases/P05/tasks/T05-PAYLOAD.md,.specify/orchestrator/milestones/M004/phases/P05/tasks/T05-PLAN.md,.specify/orchestrator/milestones/M004/phases/P05/fixtures/run-parity.sh,.specify/orchestrator/milestones/M004/phases/P05/fixtures/README.md,.specify/orchestrator/milestones/M004/phases/P05/fixtures/golden-payload-M004-P04-T04.md,.specify/orchestrator/milestones/M004/phases/P05/fixtures/golden-compressed-budget2000.md,.specify/orchestrator/milestones/M004/phases/P05/tasks/T02-SUMMARY.md,.specify/orchestrator/milestones/M004/phases/P05/tasks/T03-SUMMARY.md,.specify/orchestrator/milestones/M004/phases/P05/tasks/T04-SUMMARY.md"
duration: "30m"
verification_result: "pass"
completed_at: "2026-04-11T13:58:04Z"
---

Created the P05 integration parity harness at .specify/orchestrator/milestones/M004/phases/P05/fixtures/run-parity.sh (240 lines, Bash 3.2 compatible) plus the accompanying fixtures README.md (91 lines) that documents the normalization rules, the six check groups, and the known-P06 XFAIL. The harness runs six check groups against the golden fixtures captured in T02 (golden-payload-M004-P04-T04.md) and T03 (golden-compressed-budget2000.md): (1) build-context parity after normalization; (2) compress-payload parity piping refactored build-context output through compress-payload at budget 2000 and normalizing before diff; (3) select-model default-mode output for all three tiers (heavy -> claude-opus-4-6 200000, standard -> claude-sonnet-4-6 150000, light -> claude-haiku-4-5 80000); (4) --list-fallback for all three tiers (heavy -> claude-sonnet-4-6,claude-haiku-4-5; standard -> claude-haiku-4-5; light -> empty); (5) --next-fallback chain walk on heavy (opus -> sonnet, sonnet -> haiku, haiku -> exit non-zero); (6) event and RESULT emission audit on all three refactored scripts. Harness exit: 16 PASS / 0 FAIL / 1 XFAIL, exit 0. The single XFAIL is the known pre-existing build-context.sh emit_event routing bug where every emit_event call in the recipe-resolved branch ends in ">/dev/null 2>&1 || true" (carried through T02 byte-for-byte parity refactor), which swallows the DISPATCH_START EVENT line. The harness reports it as XFAIL rather than FAIL so the regression surface stays visible but exit 0 is preserved; fix is queued for P06 as "standardise emit_event routing across all dispatch scripts (no /dev/null swallows)". Self-cleaning contract enforced via private mktemp -d -t run-parity-XXXXXX directory with trap EXIT INT TERM HUP -> rm -rf; the classic subshell-scope pitfall (variable mutations inside $() dont propagate) was encountered on first implementation (8 temp files leaked) and fixed by switching from a tracking-variable approach to a single owned tmpdir. Verified before/after temp-file diff produces no added files and that the real M004 execution-log.jsonl mtime and size are unchanged after running the harness. The harness sources scripts/lib/errors.sh only (not run-context.sh or events.sh) so it does not trigger the _orch_run_nonce SIGPIPE workaround T02/T03/T04 carry and does not emit its own EVENT stream. Exactly one RESULT:{status,error_kind,detail} line is emitted on completion via emit_result on the success and failure paths. All T05 must-haves pass: harness exists and is executable; both golden fixtures and README are present; harness exits 0; no declare -A / readarray / mapfile tokens (verified by re-grep after rewording the comment to avoid a false-positive match in the initial draft); artifact constraints met (harness 240 lines contains "diff"; golden payload 747 lines contains "Dispatch Context"; golden compressed 747 lines contains "Dispatch Context"; README 91 lines contains "Normalization"). Also re-verified the P05 phase-level scope truths from the T05 payload: section-handlers.sh guard in first 5 lines; all 7 handler functions defined; build-context.sh sources recipe-parser.sh and calls parse_recipe_sections; build-context.sh sources section-handlers.sh; build-context.sh sources errors/events/run-context and calls emit_result; no hardcoded SEC_KNOWLEDGE constant remains; compress-payload.sh sources recipe-parser.sh and calls parse_recipe_compression; compress-payload.sh accepts --recipe flag. No production scripts, templates, or P06-deferred files were touched: build-context.sh, compress-payload.sh, select-model.sh, section-handlers.sh, templates/context-recipe.yaml, templates/routing.yaml, scripts/verify/check-must-haves.sh, scripts/lib/events.sh, scripts/lib/run-context.sh, scripts/lifecycle/record-result.sh, and scripts/engine/run.sh all remain unchanged. Deviation from the plan: the plan draft for run-parity.sh did not account for build-context.sh emit_event routing, so under the plans check 6 as-written the harness would have exited 1 on the build-context EVENT count. Since dispatch instructions explicitly queued build-context.sh for P06 (do not touch), the harness treats that single condition as XFAIL rather than FAIL, preserves the "harness exits 0 when all parity checks pass" must-have, and logs the condition with a clear P06-scope marker. This is the only substantive deviation from the plan verbatim; all six check groups run exactly as specified. T05 is the final P05 task; with all 6 P05 phase-level scope truths verified green and all 6 check groups either PASS or XFAIL-with-P06-note, P05 is ready for phase-level speckit.orchestrator.verify.
