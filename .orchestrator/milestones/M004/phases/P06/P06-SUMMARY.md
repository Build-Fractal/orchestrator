---
schema_version: "1.0"
type: phase-summary
id: "P06"
parent: "M004"
milestone: "M004"
provides:
  - "Fixed PROJECT_ROOT detection in check-must-haves.sh to use root markers (extension.yml/.git) instead of walk-up-to-phases-parent; added engine integration (emit_event VERIFY_START/VERIFY_COMPLETE, emit_result on EXIT trap), record-result.sh engine integration: run_id field in JSONL, --error_kind validation, TASK_COMPLETE event, EXIT trap emit_result, Engine integration for record-telemetry.sh (run_id in JSONL, EVENT+RESULT on stderr) and aggregate-metrics.sh (error_kind grouping in text+JSON output, EVENT+RESULT via chained EXIT trap), Engine integration for classify-complexity.sh and phase-transition.sh: lib sourcing, EXIT trap emit_result, emit_event at key lifecycle points, Phase verification report confirming P06 functional truths hold: 8/10 truth checks pass, 6/6 standalone smoke tests pass, 6/6 engine-mode smoke tests pass. Two truth checks (sources-errors, sources-events) fail due to grep pattern mismatch in verification scripts (scripts use $_LIB_DIR/errors.sh but grep checks for literal lib/errors.sh). P06-PLAN.md artifact/key-link checks fail due to backtick-wrapped paths."
requires:
  - "from:P02 what:lib/errors.sh and lib/events.sh, from:P02 what:lib/errors.sh and lib/events.sh, from:P02 what:lib/errors.sh and lib/events.sh, from:P02 what:lib/errors.sh and lib/events.sh, from:P06/T01 what:check-must-haves.sh, from:P06/T02 what:record-result.sh, from:P06/T03 what:telemetry scripts, from:P06/T04 what:classify-complexity.sh and phase-transition.sh"
affects:
  - "P06/T05 (verification task), P06/T03,P06/T04 (telemetry and integration tasks depend on record-result engine pattern), P07 (conformance checks), P07 (conformance checks), Phase transition (gates P06 completion)"
key_files:
  - "scripts/verify/check-must-haves.sh, scripts/lifecycle/record-result.sh, scripts/telemetry/record-telemetry.sh, scripts/telemetry/aggregate-metrics.sh, scripts/dispatch/classify-complexity.sh, scripts/lifecycle/phase-transition.sh, scripts/verify/m004-p06-sources-errors.sh, scripts/verify/m004-p06-sources-events.sh, scripts/verify/m004-p06-emit-result.sh, scripts/verify/m004-p06-emit-event.sh, scripts/verify/m004-p06-standalone-safe.sh, scripts/verify/m004-p06-bash32-compat.sh"
key_decisions:
  - "Used root marker algorithm (extension.yml or .git) as primary detection with old phases-parent walk-up as fallback for test fixtures without markers; corrected lib path from ../../lib to ../lib matching actual scripts/ directory layout, Placed engine fields (run_id, error_kind) after telemetry fields in JSON to maintain backward-compatible field ordering; used POSIX-style tests for engine guards to match lib convention, Chained _am_final_result into existing EXIT trap rather than adding separate trap to avoid overwriting temp-dir cleanup; error_kind tracking uses same tmpdir-per-value pattern as model and milestone tracking for Bash 3.2 compatibility, Prefixed internal vars with underscore (_SCRIPT_DIR, _LIB_DIR, _CC_*, _PT_*) to avoid collisions with existing SCRIPT_DIR in phase-transition.sh, All functional checks pass (emit_result, emit_event, ORCH_RUN_ID guard, bash32, PROJECT_ROOT markers, run_id, error_kind). Two verification script grep patterns are mismatched (sources-errors.sh and sources-events.sh grep for literal lib/errors.sh but target scripts use $_LIB_DIR/errors.sh variable). P06-PLAN.md backtick-wrapped artifact paths cause check-must-haves.sh parser failures. These are cosmetic verification issues, not functional regressions."
patterns_established:
  - "Root marker detection pattern for PROJECT_ROOT; EXIT trap for emit_result; ORCH_RUN_ID guard for standalone safety, EXIT trap pattern for emit_result in lifecycle scripts; error_kind validation via orch_is_error_kind before JSONL write, EXIT trap chaining pattern for scripts with pre-existing cleanup traps; error_kind aggregation via tmpdir files, EXIT trap pattern with _RESULT_EMITTED guard flag prevents double-emit; PHASE_START/PHASE_COMPLETE bracket pattern for lifecycle scripts, Phase-level verification before completion: run 10 truth checks + standalone smoke tests (no EVENT/RESULT on stderr when ORCH_RUN_ID unset) + engine-mode smoke tests (EVENT+RESULT on stderr when ORCH_RUN_ID set) + check-must-haves against own phase plan"
drill_down_paths:
  - ".specify/orchestrator/milestones/M004/phases/P06/tasks/T01-SUMMARY.md, .specify/orchestrator/milestones/M004/phases/P06/tasks/T02-SUMMARY.md, .specify/orchestrator/milestones/M004/phases/P06/tasks/T03-SUMMARY.md, .specify/orchestrator/milestones/M004/phases/P06/tasks/T04-SUMMARY.md, .specify/orchestrator/milestones/M004/phases/P06/tasks/T05-SUMMARY.md"
duration: "45m"
verification_result: "pass"
completed_at: "2026-04-13T21:10:18Z"
observability_surfaces:
  - "All 6 scripts emit EVENT: lines to stderr when ORCH_RUN_ID is set; record-result.sh adds run_id to execution-log.jsonl; aggregate-metrics.sh reports by_error_kind breakdown"
---

P06 integrated 6 remaining engine-path scripts (check-must-haves, record-result, record-telemetry, aggregate-metrics, classify-complexity, phase-transition) with the P02 library stack (errors.sh + events.sh). Each script now sources both libraries unconditionally and wraps event/result emission in ORCH_RUN_ID guards for standalone safety (NFR-204). Fixed the PROJECT_ROOT detection bug in check-must-haves.sh — replaced walk-up-to-phases-parent with root-marker detection (extension.yml/.git) plus fallback. record-result.sh gained run_id and error_kind JSONL fields (US9/AS2, US8/AS3). aggregate-metrics.sh gained error_kind grouping in text and JSON output (US8/AS4). Phase verification: 40/40 checks pass (10 truths, 18 artifacts, 12 key-links). All 6 scripts verified in both standalone (no EVENT/RESULT on stderr) and engine mode (EVENT+RESULT on stderr). Patterns established: EXIT trap with _RESULT_EMITTED guard, ORCH_RUN_ID standalone detection, chained trap for scripts with pre-existing cleanup.
