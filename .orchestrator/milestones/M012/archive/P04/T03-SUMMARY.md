---
schema_version: "1.0"
type: task-summary
id: "T03"
parent: "P04"
milestone: "M012"
provides:
  - "scripts/wiki/wiki-deploy.sh (161 lines, mode 0755, Bash 3.2 compliant) — chained deploy wrapper that runs four pre-deploy gates in strict order (1: scripts/diagnostics/wiki-giscus-config-check.sh --quiet; 2: mkdocs build -f wiki/mkdocs.yml; 3: scripts/diagnostics/wiki-link-check.sh --site wiki/site; 4: scripts/diagnostics/wiki-giscus-smoke.sh --site wiki/site) then runs mkdocs gh-deploy --force -f wiki/mkdocs.yml on the live path. Supports --dry-run / --help / --root DIR / --skip-smoke. Emits GATE: <name> PASS or FAIL, BUILD: ok or fail or skip, DEPLOY: pushing to gh-pages, and one DRY-RUN: or OK: or FAIL: terminator. Exit codes: 0 all-PASS, 1 any-FAIL, 2 usage-error. Aborts before gh-deploy on any non-zero gate (loud-fail with stderr FAIL line naming the gate)."
requires:
  - "from:M012/P04/T01 what:wiki/docs/index.md is the sites home page (built by gate 2); from:M012/P04/T02 what:wiki/README.md carries First-deploy checklist and Running the deploy wrapper sections that the wrappers --help points operators at by name; from:M012/P03 what:scripts/diagnostics/wiki-giscus-config-check.sh (--quiet, exit 0 or 1), scripts/diagnostics/wiki-giscus-smoke.sh (--site, exit 0 or 1 or 2); from:M012/P02 what:scripts/diagnostics/wiki-link-check.sh (--site, exit 0 or 1 or 2); from:M012/P01 what:wiki/mkdocs.yml base config that mkdocs build and mkdocs gh-deploy read"
affects:
  - "M012/P04/T04 (DEPLOY-RECORD.md — T04 operator runs this wrapper and records gate result fields from its output); M012/P04/T05 (scripts/verify/m012-p04-deploy-wrapper-contract.sh / deploy-wrapper-help.sh / deploy-wrapper-dry-run.sh / deploy-wrapper-loud-fail.sh / bash32-compat.sh will assert on this wrappers shape, output lines, exit codes, and Bash 3.2 compliance)"
key_files:
  - "scripts/wiki/wiki-deploy.sh"
key_decisions:
  - "AD-19 single-script-file Check shape (agent-facing Check invocations of the wrapper are single-invocation bash scripts/wiki/wiki-deploy.sh ...); AD-3 SSOT (wrapper composes existing diagnostics — no rewrite of .orchestrator artifacts, no duplication of gate logic); Constitution VIII Bash 3.2 (no declare -A, no mapfile or readarray, no process substitution, no ^^ or ,, case operators, no merge redirect in non-comment code); Constitution XIV (exactly four gates, four flags — no YAML driver, no plugin system); Constitution XV (T03 creates exactly one new file); MEM004 carve-out for scripts/wiki/ and scripts/diagnostics/ internals permits internal pipes and dollar-paren composition while the wrappers agent-facing invocation stays single-script-file shape; loud-fail convention (every gate FAIL emits FAIL on stderr naming the gate; no silent aborts)"
patterns_established:
  - "chained-gate deploy wrapper pattern (N sequential single-script-file gate invocations with first-non-zero-aborts semantics, per-gate GATE <name> PASS or FAIL output for deterministic gate-result capture by downstream record-keepers); bash-3.2 safe flag parser idiom (while argc gt 0 case shift done — no getopts, no shift 2, --root DIR handled by an inner argc lt 2 guard plus explicit shift); graceful-absent-tool pattern (mkdocs missing equals BUILD skip, exit 0 under --dry-run only; exit 1 on live path — dev-laptop usable without polluting deploy path); graceful-absent-artifact pattern (wiki/site missing equals link-check and giscus-smoke both emit SKIP rather than fail — covers fresh-clone --dry-run)"
drill_down_paths:
  - ".orchestrator/milestones/M012/phases/P04/tasks/T03-PLAN.md,scripts/wiki/wiki-deploy.sh"
duration: "20m"
verification_result: "pass"
completed_at: "2026-04-21T03:47:40Z"
---

Created scripts/wiki/wiki-deploy.sh verbatim from the T03-PLAN.md step-1 template (161 lines, 4794 bytes, mode 0755). The wrapper is a single shell script with set -u (no set -e — explicit per-gate exit codes are clearer than inherited trap behavior), a usage function heredoc naming all four gate basenames and all four flags, a Bash-3.2-safe flag parser that handles --dry-run, --skip-smoke, --help, --root DIR without shift 2 or getopts, an explicit root resolver that defaults to SCRIPT_DIR/../.. when --root is absent, and four sequentially-chained gate blocks with first-non-zero-aborts semantics. Gate 1 invokes scripts/diagnostics/wiki-giscus-config-check.sh --quiet — exit 1 here emits GATE giscus-config FAIL on stdout plus a stderr FAIL giscus-config diagnostic pointing at wiki/README.md First-deploy checklist, then exits 1 before mkdocs runs. Gate 2 is conditional on command -v mkdocs — if present, mkdocs build -f wiki/mkdocs.yml redirects to devnull then BUILD ok; if absent, BUILD skip is acceptable under --dry-run but exits 1 on the live path. Gates 3 and 4 are conditional on -d wiki/site — if present they invoke the link-check and giscus-smoke diagnostics with --site wiki/site; if absent they emit GATE SKIP. Gate 4 additionally honors --skip-smoke. Terminator logic: --dry-run prints DRY-RUN would deploy and exits 0; live path prints DEPLOY pushing to gh-pages then runs mkdocs gh-deploy --force -f wiki/mkdocs.yml, printing OK deployed to gh-pages exit 0 on success or FAIL mkdocs gh-deploy exited non-zero on stderr exit 1 on gh-deploy failure.

Manual verification per T03-PLAN Step 3 all PASS. Check 1: bash scripts/wiki/wiki-deploy.sh --help exits 0, usage block names all four gates and all four flags (verified stdout contains the four diagnostic script basenames and the four flag names). Check 2: GISCUS_REPO equals x plus GISCUS_REPO_ID equals x plus GISCUS_CATEGORY equals x plus GISCUS_CATEGORY_ID equals x bash scripts/wiki/wiki-deploy.sh --dry-run exits 0; stdout contains GATE giscus-config PASS, BUILD skip mkdocs not installed (mkdocs absent on this sandbox — acceptable per plan), GATE link-check SKIP no wiki site, GATE giscus-smoke SKIP no wiki site, and the DRY-RUN would deploy terminator. Check 3: GISCUS_REPO equals x plus GISCUS_CATEGORY equals x plus GISCUS_CATEGORY_ID equals x bash scripts/wiki/wiki-deploy.sh --dry-run with GISCUS_REPO_ID intentionally unset exits 1; stderr diagnostic from the underlying config-check names the missing var; wrapper emits GATE giscus-config FAIL on stdout and FAIL giscus-config diagnostic on stderr. Check 4: bash scripts/wiki/wiki-deploy.sh --unknown-flag exits 2; stderr prints ERROR unknown flag then the full usage block.

Must-Have content checks (T03-PLAN lines 223-237) all PASS: file exists, is 161 lines (>= 120), mode 0755; literal strings mkdocs gh-deploy, wiki-giscus-config-check.sh, wiki-link-check.sh, wiki-giscus-smoke.sh all present (Grep count 13 total occurrences across the four literals); usage function present; heredoc names --dry-run, --help, --root, --skip-smoke; exit-code 0, 1, 2 paths present; set -u present on line 29 (no set -e, matching the plans explicit directive). Forbidden-pattern scan (Grep for declare-A or mapfile or readarray or caret-caret or comma-comma or process-sub) returns exactly one hit — the line 27 comment noting Bash 3.2 compliant. No declare -A. No process substitution. — which is a comment mention, not executable code. Grep for literal ampersand-gt returns zero matches.

Phase-wide check-must-haves output: all four T03-owned key-links PASS (scripts/wiki/wiki-deploy.sh points at wiki-giscus-config-check.sh, wiki-link-check.sh, wiki-giscus-smoke.sh, mkdocs gh-deploy). All other FAIL lines in the run belong to later tasks (T04 DEPLOY-RECORD.md; T05 m012-p04-deploy-wrapper-contract.sh, -help.sh, -dry-run.sh, -loud-fail.sh, bash32-compat.sh, deploy-record.sh, readme-first-deploy.sh, index-finalized.sh, wiki-removable.sh, summary-walkthrough.sh, phase-suite.sh) — these are the expected incremental phase state.

Scope decisions (Constitution XV): T03-PLAN Verification section lists five Check commands pointing at scripts/verify/m012-p04-deploy-wrapper-contract.sh, -help.sh, -dry-run.sh, -loud-fail.sh and scripts/verify/m012-p04-bash32-compat.sh. T05-PLAN owns creation of every m012-p04-*.sh gate script in this phase. Per dispatch-payload guidance and Constitution XV (surgical precision), this task surfaces the missing gates as deferred to T05 rather than pre-creating them. The wrapper itself is fully contract-compliant — every must-have listed in T03-PLAN lines 223-237 has been verified manually. Out of scope and deferred: T04 first-deploy execution plus DEPLOY-RECORD.md; T05 all ten m012-p04-*.sh verify gates plus phase-suite orchestrator (including the five gates that will assert on this wrapper).
