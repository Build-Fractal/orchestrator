---
schema_version: "1.0"
type: task-summary
id: "T01"
parent: "M013/P02"
milestone: "M013"
provides:
  - "scripts/integrations/github-common.sh (12 public functions: orchestrator_id_for, emit_marker, find_marker_in_body, shasum_marker_byte_identity, sidecar_path, sidecar_get_field, sidecar_set_top_field, sidecar_upsert_item, sidecar_item_exists, gh_auth_preflight, gh_subissue_rest_preflight, gh_label_collision_preflight); tests/fixtures/m013-p02/ tree with M013-ROADMAP + P02/P03 phase plans + 2 task stubs + expected-manifest.txt + 6 gh-stub-responses files; scripts/verify/m013-p02-github-common.sh gate (13 assertions + final summary)"
requires:
  - "M013/P01/T01 sidecar-init-pending.sh; M013/P01/T06 references/github-integration.md P01 skeleton (FR-4 marker format SSOT); M016/M021 anti-pattern-lint.sh invariant"
affects:
  - "M013/P02/T02 (sources github-common.sh + fills the three gh_*_preflight stubs with real gh calls); M013/P02/T03 (consumes tests/fixtures/m013-p02/ expected-manifest.txt + gh-stub-responses/); M013/P02/T04-T07 (consume same fixture tree); M013/P03 (re-adoption branch sources github-common.sh); M013/P04 github-sync.sh (sources github-common.sh)"
key_files:
  - "scripts/integrations/github-common.sh; scripts/verify/m013-p02-github-common.sh; tests/fixtures/m013-p02/orchestrator-state/.orchestrator/milestones/M013/M013-ROADMAP.md; tests/fixtures/m013-p02/orchestrator-state/.orchestrator/milestones/M013/phases/P02/P02-PLAN.md; tests/fixtures/m013-p02/orchestrator-state/.orchestrator/milestones/M013/phases/P02/tasks/T01-PLAN.md; tests/fixtures/m013-p02/orchestrator-state/.orchestrator/milestones/M013/phases/P02/tasks/T02-PLAN.md; tests/fixtures/m013-p02/orchestrator-state/.orchestrator/milestones/M013/phases/P03/P03-PLAN.md; tests/fixtures/m013-p02/expected-manifest.txt; tests/fixtures/m013-p02/gh-stub-responses/ (6 files)"
key_decisions:
  - "none — T01 ships pre-declared contract from T01-PAYLOAD verbatim; AWK-based items.<oid> upsert chosen over jq (no hard-dep per MEM001); depth-aware brace walker inside items block isolates per-oid entries without jq"
patterns_established:
  - "AWK-based JSON object upsert with depth-aware brace walking (items.<oid> insert/replace without jq); pure-library pattern with set -u + direct-execution usage hint (BASH_SOURCE[0] == 0); echo-stub preflight pattern (gh_*_preflight return 0 at T01, T02 fills with real gh calls)"
drill_down_paths:
  - ".orchestrator/milestones/M013/phases/P02/tasks/T01-PAYLOAD.md; scripts/integrations/github-common.sh; scripts/verify/m013-p02-github-common.sh"
duration: "60"
verification_result: "pass"
completed_at: "2026-04-21T20:58:37Z"
---

T01 shipped the shared helper library for M013 GitHub integration + the P02 fixture tree. scripts/integrations/github-common.sh (449 lines, well above the >=120 minimum) implements 12 public functions across four concern groups: (1) orchestrator-id derivation (basename-from-milestone-dir with bash regex validation — exits 2 on malformed input); (2) FR-4 marker emit + search primitives (emit_marker prints the literal <!-- orchestrator-id: M###-P##[-T##] --> format; find_marker_in_body returns 0=unique, 1=zero, 2=duplicate; shasum_marker_byte_identity provides byte-identity verification per M012 marker-bounded-atomic-writes); (3) sidecar read/write (sidecar_path + sidecar_get_field + sidecar_set_top_field using grep/sed only, plus sidecar_upsert_item using an AWK depth-aware brace walker to insert/replace items.<oid> entries without jq); (4) three echo-stub gh_*_preflight helpers that return 0 unconditionally at T01 (T02 fills with real gh invocations).\n\nThe verification gate scripts/verify/m013-p02-github-common.sh exercises every public function against a sandbox directory bootstrapped from templates/github-integration-sidecar.json via the P01 sidecar-init-pending.sh helper. Gate output matches the payload's Expected Output section exactly: 13 PASS lines (library present + 3 orchestrator_id_for + 1 emit_marker + 2 find_marker_in_body + 1 sidecar_path + 1 sidecar_get_field + 1 sidecar_set_top_field + 1 sidecar_upsert_item + 1 bash -n + 1 anti-pattern-lint) followed by the final 'PASS: github-common.sh 13/13 assertions, Bash 3.2 clean.' summary, exit 0. Library is Bash 3.2 clean (confirmed via bash -n + bash-4-only pattern scan: no declare -A, mapfile, readarray, case-conversion expansion, process substitution, &>, or |&). set -u clean throughout via defensive ${var:-} defaults on every function arg.\n\nThe P02 fixture tree under tests/fixtures/m013-p02/ seeds a minimal orchestrator-state layout: M013-ROADMAP + P02 phase-plan (in-flight state, two task stubs T01/T02) + P03 phase-plan (planning state, zero tasks — exercises US-1 AS-4a lazy-projection skip). Six gh-stub-responses files cover the dry-run preflight fan-out: auth-status green + missing-scope, sub-issue REST available + unavailable, labels empty + collision. expected-manifest.txt pins the 9-record dry-run output (MILESTONE + PROJECT_V2 + 4 LABELs + 1 PHASE_ISSUE + 2 TASK_ISSUE — P03 correctly absent) that T03's gate will assert against.\n\nJudgment calls: (a) sidecar_upsert_item's AWK body tracks brace depth from the 'items:' opening brace to handle nested per-item objects without jq — tested end-to-end via probe script against a live template-bootstrapped sidecar (insert + insert + replace paths all produce valid JSON); (b) duration emitted as integer 60 (minutes) per the MEM013 + phase-transition.sh infrastructure-bug workaround flagged in P01's T07-SUMMARY; (c) gh_*_preflight echo-stubs print 'AUTH: stub-ok', 'SUBISSUE_MODE: native', and 'LABELS: no-collision' respectively — these exact tokens are referenced in the payload docstrings so T02 can diff the stub vs. live-call outputs cleanly; (d) shasum_marker_byte_identity uses the portable 'shasum' binary (macOS + linux compat) rather than sha256sum.
