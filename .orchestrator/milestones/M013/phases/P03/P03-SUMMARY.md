---
schema_version: "1.0"
type: phase-summary
id: "P03"
parent: "M013"
milestone: "M013"
provides:
  - "tests/fixtures/m013-p03/re-init-adoption/ fixture tree; gh_marker_search_remote helper in scripts/integrations/github-common.sh; scripts/verify/m013-p03-re-init-fixture.sh gate; scripts/verify/m013-p03-github-common-readopt.sh gate, re-init-adoption-branch; --re-init-flag; _is_adopted-guard; manifest_footer-adopted-suffix, graphql-call-shape-lint; fr5-three-shape-whitelist; mutation-name-awk-extractor; t03-selftest-gate, references/github-integration.md P03 extensions (Full Mapping Table, Re-init Adoption Contract FR-14, Scope Boundary P03 column, TODO P04 relabel); commands/github-init.md --re-init addendum; scripts/verify/m013-p03-reference-extensions.sh gate, P03 phase-suite orchestrator; P03 bash32-compat gate; P02 regression-guard invocation"
requires:
  - "scripts/integrations/github-common.sh from M013/P02/T01 (emit_marker helper); tests/fixtures/m013-p02/ shape precedent; M013_GH_STUB_DIR env-var stub-selector pattern from P02, T01-fixture; T01-gh_marker_search_remote; P02-github-init.sh; P02-manifest_footer, P02-github-init.sh-mutations; T02-projectsV2-query-not-flagged; scripts/integrations-scope, T02 (--re-init flag + adopted= footer settled); T03 (scripts/verify/graphql-call-shape.sh path settled); P02 github-integration.md + github-init.md authored artifacts, T01 (re-init fixture + github-common-readopt); T02 (re-init adoption + auto-mode); T03 (graphql-call-shape + selftest); T04 (reference extensions)"
affects:
  - "scripts/integrations/github-common.sh (additive: one new public function + one line in usage-hint echo list), scripts/integrations/github-init.sh; scripts/integrations/github-common.sh; scripts/verify/m013-p03-re-init-adoption.sh; scripts/verify/m013-p03-re-init-auto-mode.sh; tests/fixtures/m013-p03/re-init-adoption/orchestrator-state/.orchestrator/milestones/M013/phases/P02/P02-PLAN.md, scripts/verify/graphql-call-shape.sh; scripts/verify/m013-p03-graphql-call-shape-selftest.sh, references/github-integration.md; commands/github-init.md; scripts/verify/m013-p03-reference-extensions.sh (new); scripts/verify/m013-p02-reference-extensions.sh (relaxed assertion-3), scripts/verify/m013-p03-bash32-compat.sh; scripts/verify/m013-p03-phase-suite.sh; scripts/verify/graphql-call-shape.sh"
key_files:
  - "tests/fixtures/m013-p03/re-init-adoption/orchestrator-state/.orchestrator/milestones/M013/M013-ROADMAP.md;tests/fixtures/m013-p03/re-init-adoption/orchestrator-state/.orchestrator/milestones/M013/phases/P02/P02-PLAN.md;tests/fixtures/m013-p03/re-init-adoption/orchestrator-state/.orchestrator/milestones/M013/phases/P02/tasks/T01-PLAN.md;tests/fixtures/m013-p03/re-init-adoption/orchestrator-state/.orchestrator/milestones/M013/phases/P02/tasks/T02-PLAN.md;tests/fixtures/m013-p03/re-init-adoption/expected-readopt-manifest.txt;tests/fixtures/m013-p03/re-init-adoption/gh-stub-responses/auth-status-green.txt;tests/fixtures/m013-p03/re-init-adoption/gh-stub-responses/subissue-rest-available.json;tests/fixtures/m013-p03/re-init-adoption/gh-stub-responses/labels-no-collision.json;tests/fixtures/m013-p03/re-init-adoption/gh-stub-responses/issue-list-M013-P02.json;tests/fixtures/m013-p03/re-init-adoption/gh-stub-responses/issue-list-M013-P02-T01.json;tests/fixtures/m013-p03/re-init-adoption/gh-stub-responses/issue-list-M013-P02-T02.json;tests/fixtures/m013-p03/re-init-adoption/gh-stub-responses/project-v2-node-query.json;tests/fixtures/m013-p03/re-init-adoption/gh-stub-responses/issue-view-body-M013-P02.txt;tests/fixtures/m013-p03/re-init-adoption/gh-stub-responses/issue-view-body-M013-P02-T01.txt;tests/fixtures/m013-p03/re-init-adoption/gh-stub-responses/issue-view-body-M013-P02-T02.txt;scripts/integrations/github-common.sh;scripts/verify/m013-p03-re-init-fixture.sh;scripts/verify/m013-p03-github-common-readopt.sh, scripts/integrations/github-init.sh;scripts/integrations/github-common.sh;scripts/verify/m013-p03-re-init-adoption.sh;scripts/verify/m013-p03-re-init-auto-mode.sh;tests/fixtures/m013-p03/re-init-adoption/orchestrator-state/.orchestrator/milestones/M013/phases/P02/P02-PLAN.md, scripts/verify/graphql-call-shape.sh;scripts/verify/m013-p03-graphql-call-shape-selftest.sh, references/github-integration.md;commands/github-init.md;scripts/verify/m013-p03-reference-extensions.sh;scripts/verify/m013-p02-reference-extensions.sh, scripts/verify/m013-p03-bash32-compat.sh;scripts/verify/m013-p03-phase-suite.sh;scripts/verify/graphql-call-shape.sh"
key_decisions:
  - "Gate scripts use explicit if/then/else blocks instead of X && pass || fail short-circuit chains for set -u safety; plan-text otherwise followed verbatim, Gated implicit-detection probe off --dry-run to protect P02 fixture byte-identity (P02 has no sidecar and no M013_GH_STUB_DIR — probe would have mis-fired); amended T01 fixture P02-PLAN with state:ready so walker projects the phase (T01 omitted it); label adoption uses fallback-adopt when stub is empty-array (fixture contract); milestone-list parser is dual-shape awk + grep-sed fallback to handle both multi-line and single-line JSON, awk regex anchored on literal mutation-lparen then walks past rparen-lbrace — forward-compatible with P04 assigned-variable/heredoc shapes without a P04-era edit; scope narrowed to scripts/integrations/github-star.sh with find -maxdepth 1 to exclude scripts/diagnostics/wiki-giscus-remap.sh updateDiscussion (M012 scope, not M013 FR-5); queries using query-lparen are skipped by the mutation-lparen anchor so T02 projectsV2 query is unconstrained; pre-whitelisted updateProjectV2ItemFieldValue so P04 passes on the day it lands without editing the lint, Relaxed P02 reference-extensions gate assertion-3 to accept both pre-T04 and post-T04 mapping-table shapes rather than freezing P02 gate + duplicating hashes; kept sha256 byte-identity block on P01 sections untouched (load-bearing invariant preserved). Did NOT re-embed section sha256 hashes in new P03 gate (plan explicitly flagged this as acceptable — P02 gate is single source of byte-identity truth per AD-19)., anti-pattern-lint --fixture per-file invocation (mirrors P02/T07 — without --fixture lint scans repo and flags PAYLOAD fenced examples); comment-discipline synonym backfill on graphql-call-shape.sh (line 19 literal 'declare -A'/'mapfile' replaced with 'assoc-arrays'/'array-from-stdin builtins' per P02 convention)"
patterns_established:
  - "M013_GH_STUB_DIR env-var stub-selector pattern extended from preflights to marker-search family; awk-based JSON count + first-scalar extraction keeps integration helpers jq-optional, additive-suffix helper extension preserves upstream fixture byte-identity (manifest_footer 3->4 args); adoption pre-pass + _is_adopted guards enables one-shot re-entry on create fan-out without duplicating the create path, scoped-glob lint (find -maxdepth 1 -name prefix-star.sh) + optional dollar-1 path override for tempdir fixture tests; forward-compatible whitelist lint anticipating downstream task call shapes, Gate-evolution-on-legitimate-advancement: when a later phase legitimately completes what an earlier phase scaffolded as 'deferred', the earlier phase's presence-of-deferral assertion evolves to accept both shapes rather than fracturing the suite; byte-identity hashes on content NOT being advanced stay pinned as the load-bearing invariant., T05-shape phase-suite with P02 regression guard as final pass-counter increment; self-excluded gate-scanner (case-branch on BASH_SOURCE-path match) + comment-discipline synonyms (assoc-arrays, array-from-stdin builtins, case-conversion expansion, combined-redirect shorthand) as paired conventions keeping bash32 scanners self-clean"
drill_down_paths:
  - ".orchestrator/milestones/M013/phases/P03/tasks/T01-SUMMARY.md, .orchestrator/milestones/M013/phases/P03/tasks/T02-SUMMARY.md, .orchestrator/milestones/M013/phases/P03/tasks/T03-SUMMARY.md, .orchestrator/milestones/M013/phases/P03/tasks/T04-SUMMARY.md, .orchestrator/milestones/M013/phases/P03/tasks/T05-SUMMARY.md"
duration: "205m"
verification_result: "pass"
completed_at: "2026-04-22T01:05:50Z"
observability_surfaces:
  - "none"
---

M013/P03 (Re-init adoption path + FR-5 GraphQL call-shape lint + mapping table completion) shipped the three deferred-by-D015 scope chunks as an additive layer over P02's create path. Five tasks, 8/8 P03 phase-suite gates PASS, 8/8 P02 phase-suite gates still PASS (byte-identity preserved for P02 fixture).

T01 shipped the re-init test fixture tree at `tests/fixtures/m013-p03/re-init-adoption/` (16 files: seed orchestrator-state with P02 in-flight, `expected-readopt-manifest.txt` SSOT, 10 gh-stub responses spanning auth/sub-issue-rest/labels/issue-lists/issue-bodies/project-v2-node-query) and the `gh_marker_search_remote` public helper in `scripts/integrations/github-common.sh` (awk JSON count + first-scalar extract, jq-optional). Two T01 gates (fixture 14/14, helper-smoke 5/5).

T02 added the FR-14 re-init adoption branch to `scripts/integrations/github-init.sh`: `--re-init` flag + `REINIT=0` default + re-init trigger computation (explicit flag OR sidecar-absent + first-phase marker-hit implicit-detection probe gated off `--dry-run` to protect P02 fixture byte-identity); adoption pre-pass walks milestone → project-v2 → labels → phase Issues → task sub-Issues; `_is_adopted` guards on every create function enable one-shot re-entry without duplicating create paths; `manifest_footer` gained optional 4th arg `<adopted>` (additive — omitted call-site emits P02's 3-field shape byte-identical). Two T02 gates (adoption 6/6, auto-mode SC-7 2/2). Amended the T01 fixture P02-PLAN with `state: ready` so the walker projects the phase.

T03 shipped `scripts/verify/graphql-call-shape.sh` (FR-5 three-shape whitelist CI lint). Awk regex anchored on `mutation-lparen` + walks past `rparen-lbrace` to extract the mutation identifier; forward-compatible with P04's heredoc/assigned-variable call shapes without requiring a P04-era edit; scope narrowed to `scripts/integrations/github-*.sh` (find -maxdepth 1 -name pattern) to exclude `scripts/diagnostics/wiki-giscus-remap.sh` updateDiscussion (M012 scope, not M013). Pre-whitelisted `updateProjectV2ItemFieldValue` so P04 passes on arrival. T3's projectsV2 discovery **query** is unconstrained because the anchor only matches mutation-lparen (FR-5 governs mutations, not queries). Selftest gate 5/5 (live exit 0, both whitelisted shapes detected, injected `deleteProjectV2` fourth shape correctly flagged with non-zero exit).

T04 extended `references/github-integration.md` in place: promoted "Partial Mapping Table" → "Full Mapping Table (P02+P03)"; filled the three `_deferred to P03_` cells (spec chunk / acceptance criterion / verification status ↔ GitHub resources); relabeled three `### TODO P03:` stubs as `### TODO P04:` (sync/conversus/cost-emission are P04 content per D015 rename); authored new `### Re-init Adoption Contract (FR-14)` subsection; extended Scope Boundary table with P03 column and FR-5 lint row. Added `commands/github-init.md` Core Workflow step 8 documenting `--re-init` + `adopted=<A>` footer. T04 gate 13/13. **Relaxed** P02 `m013-p02-reference-extensions.sh` assertion-3 to accept both pre-T04 and post-T04 mapping-table shapes — preserving the load-bearing P01-sections sha256 byte-identity block untouched (**gate-evolution-on-legitimate-advancement** pattern: when a later phase legitimately completes earlier-phase scaffolding, the earlier assertion evolves to accept both shapes rather than fracturing the suite).

T05 shipped `scripts/verify/m013-p03-bash32-compat.sh` (33 PASS, self-clean via case-branch BASH_SOURCE self-exclude + comment-discipline synonyms: assoc-arrays, array-from-stdin builtins, case-conversion expansion, combined-redirect shorthand) and `scripts/verify/m013-p03-phase-suite.sh` (T05-shape: 7 P03 gates + final P02 regression guard as pass-counter increment). Backfilled `graphql-call-shape.sh` line-19 comment with synonym form (literal `declare -A`/`mapfile` tripped the scanner).

Demo criterion satisfied: re-init fixture dry-run produces `reason=adopt` for every projected resource with zero `create` calls (14-line byte-identical diff vs `expected-readopt-manifest.txt`); `graphql-call-shape.sh` exits 0 against post-P02 repo (detects both whitelisted mutations, ignores projectsV2 query); selftest exits non-zero on injected `deleteProjectV2` fourth shape; P03 phase-suite exits 0 with P02 phase-suite still passing byte-for-byte.

D014 Knowledge-Layer Boundary respected — no `SPEC-*` frontmatter changes, no `KNOWLEDGE-INDEX.md` extensions, no `scripts/knowledge/rebuild-index.sh` modifications. D015 scope split honored — three `_deferred to P03_` mapping rows filled, three `### TODO P03:` stubs relabeled `### TODO P04:` (sync/conversus/cost content intentionally deferred). FR-12 Claude-Code-only v1 preserved. SC-7 zero-`gh`-writes invariant enforced in auto-mode re-init branch via PATH-shimmed fake-gh zero-call-log assertion.

Key patterns established: (1) additive-suffix helper extension preserves upstream fixture byte-identity (manifest_footer 3→4 args); (2) adoption pre-pass + `_is_adopted` guards enables one-shot re-entry on create fan-out without duplicating create path; (3) scoped-glob lint (`find -maxdepth 1 -name prefix-star.sh`) + optional `$1` path override for tempdir fixture selftests; (4) forward-compatible whitelist lint anticipating downstream task call shapes (updateProjectV2ItemFieldValue pre-whitelisted); (5) gate-evolution-on-legitimate-advancement — earlier-phase assertions that encode "deferred" state evolve to accept both pre/post shapes while byte-identity hashes on unchanged content stay pinned as load-bearing invariant; (6) `M013_GH_STUB_DIR` env-var stub-selector pattern extended from preflights to marker-search family; (7) awk-based JSON count + first-scalar extraction keeps integration helpers jq-optional; (8) T05-shape phase-suite with P02 regression guard as final pass-counter increment; (9) comment-discipline synonyms paired with self-excluded gate-scanner keep bash32 scanners self-clean.

Verification: P03 phase suite exit 0, 8/8 PASS. P02 phase suite 8/8 PASS (byte-identity preserved). `graphql-call-shape.sh` live exit 0. Zero `gh` subprocess invocations in CI (PATH-shim enforced in auto-mode gate).

Judgment calls surfaced during execution (for operator review): (a) implicit-detection probe gated off `--dry-run` — necessary because the P02 fixture has no sidecar and no `M013_GH_STUB_DIR` env, so an unconditional live probe would have mis-fired via real `gh` and broken P02 byte-identity; (b) amended T01 fixture P02-PLAN.md with `state: ready` (T01 omitted it) so the walker projects the phase for adoption-gate exercise; (c) relaxed P02 `m013-p02-reference-extensions.sh` assertion-3 rather than freezing the P02 gate with hash-duplication in a new P03 gate — AD-19 single-source-of-byte-identity-truth pattern preserved; (d) backfilled `graphql-call-shape.sh` line-19 comment with synonym form rather than per-file special-case in the scanner; (e) T04 gate does NOT re-embed section sha256 hashes for its own added sections — planner explicitly flagged this as acceptable (P02 gate is SSOT per AD-19); (f) anti-pattern-lint invocations across P03 gates use `--fixture "$path"` per P02/T07 precedent (without `--fixture` the lint scans repo and flags PAYLOAD fenced examples); (g) gate scripts use explicit if/then/else blocks instead of `X && pass || fail` short-circuit chains for `set -u` safety; (h) `phase-transition.sh` non-numeric `duration:` bug (pre-existing P01 inheritance) worked around via integer-minutes durations throughout (P03 total: 205m).

Observability surfaces: none at P03 scope. P04 introduces `unit_close` + `conversus_gate_invocation` JSONL emitters in the M019 Tier 1 shape per roadmap.
