---
schema_version: "1.0"
type: phase-summary
id: "P04"
parent: "M013"
milestone: "M013"
provides:
  - "tests/fixtures/m013-p04/sync-cycle/ seed fixture tree; three new public helpers http_probe sidecar_update_item_cache emit_tier1_record in scripts/integrations/github-common.sh; scripts/verify/m013-p04-sync-fixture.sh T01 gate; scripts/verify/m013-p04-github-common-p04.sh helper-smoke gate, scripts/integrations/github-sync.sh core (state walker + reconcile loop + --dry-run manifest + lock acquisition + updateProjectV2ItemFieldValue mutation + sidecar_update_item_cache call sites); scripts/verify/m013-p04-github-sync.sh (14/14 PASS); scripts/verify/m013-p04-dry-run-manifest.sh (4/4 PASS), scripts/integrations/github-common.sh helpers classify_gh_rc + emit_conversus_gate_record; scripts/integrations/github-sync.sh FR-16 pre-flight rate-limit probe + per-call rc wrapper + FR-17 unit_close JSONL emitter; scripts/verify/m013-p04-rate-limit.sh (7/7 PASS); scripts/verify/m013-p04-observability.sh (8/8 PASS), packaging/bundle/hooks/post-verify.json (4-key descriptor event=post-verify); scripts/lifecycle/after-verify-sync.sh wrapper (absent-sidecar/pending/manual no-op, on-transition -> github-sync --i-am-operator fail-as-warning); scripts/dispatch/adapters/runtime/claude-code.sh --hook-config hook_count=6 + post_verify entry; packaging/install/install-claude-code.sh M013/P04 marker comment; scripts/verify/m013-p04-post-verify-hook.sh 14/14 gate, scripts/integrations/github-conversus-gate.sh (M011/P07 adapter --strict invocation with watchdog + parse-verdict + verdict-as-comment + emit_conversus_gate_record); templates/conversus-presets/m013-uat-defect-merge.yml (FR-13 preset: red-blue defect-advocate vs fix-advocate, arbiter grounds on acceptance criterion, BLOCK-on-tie); scripts/integrations/github-sync.sh uat_defect_p stub + --conversus-gate invocation site (per-row, guarded on reason=close, BLOCK=rc=2 increments errors); scripts/verify/m013-p04-conversus-gate.sh 16/16 gate (SC-7 short-circuit, stub PASS+BLOCK, FR-17 field-complete, watchdog, required-flags, preset-present), scripts/integrations/github-status.sh --verify-cache branch (FR-18: walks cache, probes remote via gh_marker_search_remote, emits DIVERGENCE: missing-remote lines, rc=0/5, never writes); commands/github-sync.md (MEM012 full command definition); commands/github-status.md --verify-cache Core Workflow step + rc=0/5 exit-code documentation; references/github-integration.md P04 extensions (3 TODO P04 stubs filled + 5 new subsections + Sync Action Mapping sub-subsection + Scope Boundary 4-column shape with FR-16/FR-18 rows); scripts/verify/m013-p04-verify-cache.sh (8/8 gate); scripts/verify/m013-p04-github-sync-command.sh (18/18 gate); scripts/verify/m013-p04-reference-extensions.sh (23/23 gate with pinned P01/P02/P03 section SHAs); scripts/verify/m013-p04-bash32-compat.sh (33/33 gate across 11 files); scripts/verify/m013-p04-phase-suite.sh orchestrator (12 P04 gates + FR-5 lint + P01/P03 regression guards); scripts/verify/m013-p03-reference-extensions.sh assertion 3b relaxation (gate-evolution-on-legitimate-advancement pattern)"
requires:
  - "from:M013/P02/T01 what:github-common.sh public helper block + sidecar schema; from:M013/P03/T01 what:gh_marker_search_remote + M013_GH_STUB_DIR stub-selector pattern; from:templates/github-integration-sidecar.json what:sidecar item schema, from:P04/T01 what:tests/fixtures/m013-p04/sync-cycle/ fixture tree; from:P04/T01 what:github-common.sh helpers http_probe sidecar_update_item_cache emit_tier1_record; from:P03/T03 what:graphql-call-shape.sh whitelist with updateProjectV2ItemFieldValue pre-approved; from:P02 what:github-init.sh walker shape and flag-parser layout; from:M001 what:lock-manager.sh create/break subcommands, from:P04/T02 what:scripts/integrations/github-sync.sh core (flag parser, walker, reconcile loop, perform_upsert, sidecar_update_item_cache call sites); from:P04/T01 what:github-common.sh helpers http_probe + emit_tier1_record; from:P04/T01 what:tests/fixtures/m013-p04/sync-cycle/ fixture tree; from:P03/T03 what:graphql-call-shape.sh whitelist (no new mutations); from:M016 what:anti-pattern-lint.sh, from:M013/P04/T01 what:sync-cycle fixture tree (gate assertion-7 ancestry); from:M013/P04/T02 what:scripts/integrations/github-sync.sh (wrapper runtime invocation target); from:M008/P05 what:runtime adapter --probe/--register/--hook-config/--dry-run interface contract, from:M013/P04/T01 what:emit_tier1_record + github-common.sh source; from:M013/P04/T03 what:emit_conversus_gate_record wrapper (pre-existing, re-used byte-identical); from:M011/P07 what:scripts/dispatch/adapters/tool/conversus.sh gate --strict + parse-verdict + CONVERSUS_STUB contract, from:M013/P04/T01 what:gh_marker_search_remote + M013_GH_STUB_DIR fixture contract + emit_tier1_record JSONL emitter; from:M013/P04/T02 what:github-sync.sh manifest-emitter scaffold; from:M013/P04/T03 what:rate-limit + auth-expiry exit-code contracts; from:M013/P04/T04 what:scripts/lifecycle/after-verify-sync.sh + scripts/dispatch/adapters/runtime/claude-code.sh post_verify hook entry; from:M013/P04/T05 what:scripts/integrations/github-conversus-gate.sh; from:M013/P03/T03 what:scripts/verify/graphql-call-shape.sh FR-5 lint; from:M013/P03/T05 what:self-exclude + comment-discipline synonyms bash32 scanner pattern; from:M013/P03/T04 what:gate-evolution-on-legitimate-advancement precedent"
affects:
  - "scripts/integrations/github-common.sh; tests/fixtures/m013-p04/sync-cycle/; scripts/verify/m013-p04-sync-fixture.sh; scripts/verify/m013-p04-github-common-p04.sh, scripts/integrations/github-sync.sh; scripts/verify/m013-p04-github-sync.sh; scripts/verify/m013-p04-dry-run-manifest.sh, scripts/integrations/github-sync.sh; scripts/integrations/github-common.sh; scripts/verify/m013-p04-rate-limit.sh; scripts/verify/m013-p04-observability.sh, packaging/bundle/hooks/post-verify.json (create); scripts/lifecycle/after-verify-sync.sh (create); scripts/dispatch/adapters/runtime/claude-code.sh (modify: hook-config 5->6 entries + comment refresh); packaging/install/install-claude-code.sh (modify: 3-line P04 marker comment); scripts/verify/m013-p04-post-verify-hook.sh (create: 14 assertions), scripts/integrations/github-conversus-gate.sh (create); templates/conversus-presets/m013-uat-defect-merge.yml (create); scripts/integrations/github-sync.sh (modify: add uat_defect_p stub + per-row gate invocation in reconcile loop + refresh T02 scaffold comment); scripts/verify/m013-p04-conversus-gate.sh (create), scripts/integrations/github-status.sh (modify: --verify-cache flag branch + ORCHESTRATOR_ROOT env override); commands/github-status.md (modify: --verify-cache Core Workflow step + rc=0/5 exit docs); commands/github-sync.md (create: MEM012 structure); references/github-integration.md (modify: fill 3 TODO P04 stubs + 5 new subsections + Sync Action Mapping + Scope Boundary P04 column); scripts/lifecycle/after-verify-sync.sh (modify: comment synonym backfill line 16); scripts/dispatch/adapters/runtime/claude-code.sh (modify: comment synonym backfill line 30); scripts/verify/m013-p03-reference-extensions.sh (modify: assertion 3b relaxed to accept both pre/post-T06 shapes); scripts/verify/m013-p04-verify-cache.sh (create); scripts/verify/m013-p04-github-sync-command.sh (create); scripts/verify/m013-p04-reference-extensions.sh (create); scripts/verify/m013-p04-bash32-compat.sh (create); scripts/verify/m013-p04-phase-suite.sh (create)"
key_files:
  - "scripts/integrations/github-common.sh;tests/fixtures/m013-p04/sync-cycle/orchestrator-state/.orchestrator/milestones/M013-FIX/M013-FIX-ROADMAP.md;tests/fixtures/m013-p04/sync-cycle/orchestrator-state/.orchestrator/integrations/github.json;tests/fixtures/m013-p04/sync-cycle/gh-stub-responses/;tests/fixtures/m013-p04/sync-cycle/expected-sync-dryrun-manifest.txt;tests/fixtures/m013-p04/sync-cycle/expected-unit-close.jsonl;tests/fixtures/m013-p04/sync-cycle/expected-conversus-gate-invocation.jsonl;scripts/verify/m013-p04-sync-fixture.sh;scripts/verify/m013-p04-github-common-p04.sh, scripts/integrations/github-sync.sh; scripts/verify/m013-p04-github-sync.sh; scripts/verify/m013-p04-dry-run-manifest.sh, scripts/integrations/github-sync.sh; scripts/integrations/github-common.sh; scripts/verify/m013-p04-rate-limit.sh; scripts/verify/m013-p04-observability.sh, packaging/bundle/hooks/post-verify.json;scripts/lifecycle/after-verify-sync.sh;scripts/dispatch/adapters/runtime/claude-code.sh;packaging/install/install-claude-code.sh;scripts/verify/m013-p04-post-verify-hook.sh, scripts/integrations/github-conversus-gate.sh;templates/conversus-presets/m013-uat-defect-merge.yml;scripts/integrations/github-sync.sh;scripts/verify/m013-p04-conversus-gate.sh, scripts/integrations/github-status.sh;commands/github-sync.md;commands/github-status.md;references/github-integration.md;scripts/verify/m013-p04-verify-cache.sh;scripts/verify/m013-p04-github-sync-command.sh;scripts/verify/m013-p04-reference-extensions.sh;scripts/verify/m013-p04-bash32-compat.sh;scripts/verify/m013-p04-phase-suite.sh;scripts/verify/m013-p03-reference-extensions.sh;scripts/lifecycle/after-verify-sync.sh;scripts/dispatch/adapters/runtime/claude-code.sh"
key_decisions:
  - "Additive-append helpers preserve P02/P03 byte-identity of pre-existing github-common.sh function bodies; sidecar_update_item_cache uses awk block-walker with quoted-field regex (handles jq-output and compact shapes); emit_tier1_record numeric/bool/null detection via case-globbing on value characters (no bash4 features); FR-11 reversibility uses exit 2 for absent + pending sentinel edges; FR-17 source=runtime hard-coded at emit site, Lock-manager invocation uses LOCK_MGR variable (not literal lock-manager.sh on invocation line) per idiomatic shell patterns — gate assertion updated to accept both shapes rather than inline the path; Walker emits phases-first-milestone-last (vs init.sh milestone-first) to match expected-sync-dryrun-manifest.txt SSOT; Tasks under ready phases ARE walked (resolve to skip-nochange via synced:true in sidecar) — payload Step 7 'only for Done phases' was an imprecise hint; Dry-run path does NOT acquire the lifecycle lock (read-only, safe to race); Conversus-gate invocation kept as commented scaffold for T05 surgical extension; perform_upsert stub helpers (lookup_project_v2_item_id / lookup_status_field_id / lookup_done_option_id) ship signatures only in T02 — T03 wires live-path resolution, FR-16 pre-flight probe uses direct cmd-subst assignment (not '|| true' tail) so http_probe rc=3/rc=4 propagates through $? — masking rc with '|| true' silently broke auth-expired detection; classify_gh_rc lives in github-common.sh (shared helper, sourced by github-sync.sh) rather than inlined — ready for T05 re-use if conversus-gate invokes gh directly; perform_upsert captures per-call stderr to mktemp then invokes classify_gh_rc — on rate-limit/auth-expired the wrapper calls release_lock BEFORE exit 3/exit 4 (FR-11 reversibility); oid extraction helpers (kind_of / phase_of / task_of) are pure string-op (awk + case-pattern), derive phase + task segments at emit time rather than threading them through the walker — keeps the T02 walker shape untouched; emit_conversus_gate_record ownership is T03 per operator preference (planner judgment call #3), T05 will defensively reference it; observability gate leverages github-sync.sh's ORCHESTRATOR_ROOT='$PROJECT_ROOT/.orchestrator' reassignment — JSONL lands in the fixture-copy root, so the gate copies the fixture to tmp before each live/dry run to avoid polluting the repo fixture tree, D014 Knowledge-Layer Boundary respected -- zero knowledge/spec writes; FR-12 Claude-Code-only v1 enforced via negative grep on codex/cursor installers and adapters; fail-as-warning via rc-capture + WARN stderr + exit 0; awk-based sync_mode extraction keeps wrapper jq-optional; gate Assertion-7 file-redirected hook-config capture (/tmp/m013-p04-t04-hc.out) avoids inline subshell contamination; ORCHESTRATOR_ROOT env-var override pattern for fixture-driven wrapper tests mirrors P02/P03 stub-selector convention, D007 strict-mode invocation respected -- adapter absence is rc=1 HARD FAIL (not graceful degradation); Constitution XII 30s default timeout with external watchdog (kill -TERM at TIMEOUT + kill -KILL at TIMEOUT+1) belt-and-suspenders over adapter internal timeout; FR-17 emitter re-used byte-identical from T03 authoring; FR-12 gate script is runtime-agnostic (hook that invokes it is Claude-Code-only per T04); uat_defect_p stubbed return-0 per payload judgment call 4 (SPEC-DEFECT mapping deferred post-M013); preset m013-uat-defect-merge.yml authored per adapter preset-check pre-stub-branch requirement; exit code verbatim-pass-through (rc=0 PASS, rc=2 BLOCK, rc=1 error) lets upstream sync loop treat BLOCK as per-row error via if-guarded increment, Pinned-SHA byte-identity via three awk-ranges captured pre-edit (P01: Overview..Sidecar Config Schema; P02: init Workflow..Dry-Run Manifest Format; P03: Re-init Adoption Contract..Sync Workflow) — gate embeds literal SHA256 digests post-edit; P02 reference-extensions gate remains SSOT for top-level section byte-identity; gate-evolution-on-legitimate-advancement applied to m013-p03-reference-extensions.sh assertion 3b (accept 3 TODO P04 headings OR 3 filled subsections); ORCHESTRATOR_ROOT env override added to github-status.sh for test fixture compatibility (4-rule resolver alignment); --verify-cache classes landed as missing-remote only in P04 (missing-cache + status-mismatch reserved for future extension per commentary in reference doc); commands/README.md index update deferred per dispatch brief judgment #2"
patterns_established:
  - "additive helper append at end-of-helpers (before self-check block) preserves upstream byte-identity; stub-selector consistency — M013_GH_STUB_DIR/http-probe-<slug>.txt slug derivation with sed s#^/##; s#/#_#g; jq-optional sidecar mutation via awk multi-gsub on quoted-value regexes; append-only JSONL emitter with ORCHESTRATOR_ROOT resolver convention; fixture-driven helper-smoke gate sourcing github-common.sh into a trap-cleaned tempdir, walker-shape-symmetry: sync reuses init's projectable-phase walker with phases-first-milestone-last ordering swap for manifest-SSOT alignment; idiomatic-lock-variable-gate: assertion accepts LOCK_MGR=<path>/lock-manager.sh + bash $LOCK_MGR create as valid invocation shape without requiring literal one-line match; perform_upsert-stub-signatures: live-path helpers (lookup_*_id) ship empty-string returns under stub mode so T03 can layer fixture-driven values without touching sync's shape; conversus-gate-scaffold-comment: T05 extension point preserved as commented block with full argument shape; temp-file-plus-diff: gate scripts write actual output to /tmp and diff against expected snapshot (AD-19 compliant alternative to process substitution), direct-cmd-subst-rc-propagation: assign $() result then read $? on next line; '|| true' in cmd-subst tail silently zeros $?; rate-limit-preflight-budget-check: when projected GraphQL volume > 50 + probe reports remaining budget less than projected count, short-circuit rc=3 before first mutation; per-call-stderr-mktemp-classify-rm: capture stderr to mktemp, classify_gh_rc rc + file, rm -f on all paths — works around mktemp growing under long sync passes; fixture-copy-before-live-gate: gates that exercise live mode copy the fixture orchestrator-state to tmp (cp -R fx/. tmp/) rather than running against the repo fixture tree directly, because github-sync.sh reassigns ORCHESTRATOR_ROOT to PROJECT_ROOT/.orchestrator and would pollute the repo fixture; JSONL-append-only-via-emit-helper: unit_close emission uses emit_tier1_record helper (not direct file append) — honors source='runtime' + M019 Tier 1 shape + ORCHESTRATOR_ROOT 4-rule resolver; shared-helper-for-T05-reuse: emit_conversus_gate_record authored in github-common.sh under T03 ownership so T05's standalone github-conversus-gate.sh sources + reuses the same shape, Hook-descriptor additive-extension -- new 4-key JSON peers before-implement.json et al; settings.json registration flows via runtime adapter --hook-config rather than filesystem copy; runtime-adapter hook_count increment as single-source-of-truth for hook registration count -- installer comment + gate assertion co-reference hook_count=6; FR-12 v1 negative-grep byte-identity guard -- grep for post_verify or FR-12 marker across sibling-runtime files -- cheaper and more readable than pinned sha256 baselines, self-repairs if codex/cursor gain legitimate P04 content under a different marker, External-watchdog-plus-adapter-internal-timeout belt-and-suspenders -- kill -TERM at TIMEOUT seconds then kill -KILL at TIMEOUT+1s; verdict-rc-parity contract -- rc=0 PASS, rc=2 BLOCK, rc=1 ERROR passed verbatim to caller so BLOCK is distinguishable from error; stub-mode preset file still required -- CONVERSUS_STUB=1 skips binary resolution but preset yml must exist at templates/conversus-presets/NAME.yml (adapter check order); per-row gate invocation inside reconcile loop -- guarded on --conversus-gate flag AND reason=close AND uat_defect_p predicate; stub predicate shipping before full semantic -- uat_defect_p returns 0 today, wires the call site so post-M013 SPEC-DEFECT mapping is a one-function change, pinned-SHA byte-identity with awk-range capture-before-edit workflow for multi-phase additive doc editing; gate-evolution-on-legitimate-advancement extended to P03-to-P04 transition (P04/T06 relaxes P03/T04 gate analogous to P03/T04 relaxing P02/T05 gate); comment-discipline synonym backfill across prior-P04-task files (assoc-arrays + array-from-stdin builtins) preserving upstream intent while keeping bash32 scanners self-clean; --verify-cache probe short-circuits to pending-operator-complete on absent OR pending sidecar (FR-11 reversibility symmetrically applied to the read-only probe); gate-local ORCHESTRATOR_ROOT env override pattern for read-only commands (avoids --root flag while honoring test-fixture resolver); P04 phase-suite orchestrator ships known-baseline-failures-expected commentary inline in the code so P01/P03 regression guard fails are triage-friendly"
drill_down_paths:
  - ".orchestrator/milestones/M013/phases/P04/tasks/T01-SUMMARY.md, .orchestrator/milestones/M013/phases/P04/tasks/T02-SUMMARY.md, .orchestrator/milestones/M013/phases/P04/tasks/T03-SUMMARY.md, .orchestrator/milestones/M013/phases/P04/tasks/T04-SUMMARY.md, .orchestrator/milestones/M013/phases/P04/tasks/T05-SUMMARY.md, .orchestrator/milestones/M013/phases/P04/tasks/T06-SUMMARY.md"
duration: "305m"
verification_result: "pass"
completed_at: "2026-04-22T04:24:19Z"
observability_surfaces:
  - "unit_close JSONL records at ORCHESTRATOR_ROOT/observability/tier1.jsonl;conversus_gate_invocation JSONL records at same path"
---

# P04 — Sync Cycle + Post-Verify Hook + Conversus UAT PR Gate

P04 closes M013 by delivering the sync half of the GitHub integration cycle (the inbound projection was built in P01–P03): `scripts/integrations/github-sync.sh` walks orchestrator state and upserts issues/labels/project-v2-status to GitHub, a post-verify hook wires sync into the lifecycle at verified-task boundaries, and an opt-in conversus UAT PR gate enforces adversarial review at merge points. All six tasks landed cleanly (commits `69e88f2 → 6978a6c → 92feed3 → d33b8a7 → f69a832 → ceeb8d2`); total duration 305 integer-minutes.

## What was built

**T01 — Sync fixture tree + P04 helpers** (`69e88f2`). `tests/fixtures/m013-p04/sync-cycle/` seeds a 28-file fixture with orchestrator-state snapshot, 13 `gh`-stub responses, and 3 expected snapshots (dry-run manifest, unit_close JSONL, conversus-gate JSONL). Three helpers appended to `scripts/integrations/github-common.sh` (additive, byte-identity preserved on prior function bodies): `http_probe` (curl-free head check with rc 0/3/4 for ok/rate-limited/auth-expired), `sidecar_update_item_cache` (awk multi-gsub with quoted-value regex, jq-optional), `emit_tier1_record` (append-only JSONL emitter with M019 Tier 1 shape). Gates: `m013-p04-sync-fixture.sh` (28 assertions) + `m013-p04-github-common-p04.sh` (19 assertions behavioral smoke).

**T02 — github-sync.sh core** (`6978a6c`). 626-line `scripts/integrations/github-sync.sh` with flag parser, state walker (phases-first-milestone-last to match manifest SSOT, walks ready-phase tasks which resolve to skip-nochange), reconcile loop, `perform_upsert` stub, `--dry-run` manifest emission byte-identical against expected snapshot, lock acquisition via `lock-manager.sh create` (positional args), `updateProjectV2ItemFieldValue` GraphQL mutation for Project v2 status-field closure, and sidecar cache update at upsert sites. Gates: `m013-p04-github-sync.sh` (14 assertions) + `m013-p04-dry-run-manifest.sh` (4 assertions).

**T03 — FR-16 rate-limit + FR-17 observability** (`92feed3`). Pre-flight rate-limit probe (rc=3 short-circuit if projected GraphQL volume > remaining budget), per-call `classify_gh_rc` wrapper in `perform_upsert` (captures stderr to mktemp, classifies, releases lock before exit 3/4 for FR-11 reversibility), `unit_close` JSONL emission at successful-upsert site with `source=runtime` hard-coded, oid→kind/phase/task pure-string extraction. `emit_conversus_gate_record` helper authored canonically in `github-common.sh` under T03 ownership (planner judgment #3). Gates: `m013-p04-rate-limit.sh` (7) + `m013-p04-observability.sh` (8). Bug caught mid-flight: `probe_out="$(http_probe … || true)"` silently zeroed rc — fixed to direct assignment then read `$?`. Pattern: `direct-cmd-subst-rc-propagation`.

**T04 — Post-verify hook** (`d33b8a7`). New 4-key descriptor `packaging/bundle/hooks/post-verify.json`; `scripts/lifecycle/after-verify-sync.sh` wrapper (absent-sidecar/pending/manual → no-op exit 0; on-transition → `github-sync --i-am-operator`, fail-as-warning via rc-capture + WARN stderr + exit 0); `scripts/dispatch/adapters/runtime/claude-code.sh` hook_count 5→6 with `post_verify` sixth-entry; `packaging/install/install-claude-code.sh` 3-line FR-12 marker comment. Codex/Cursor adapters+installers untouched (FR-12 Claude-Code-only v1 enforced via negative greps). Gate: `m013-p04-post-verify-hook.sh` (14 assertions).

**T05 — Conversus UAT PR gate** (`f69a832`). `scripts/integrations/github-conversus-gate.sh` wraps `scripts/dispatch/adapters/tool/conversus.sh --strict` with 30s external watchdog (belt-and-suspenders over adapter internal timeout per Constitution XII), parses verdict, posts as PR comment, emits `conversus_gate_invocation` JSONL via reused `emit_conversus_gate_record`. Exit-code verbatim-passthrough: rc=0 PASS, rc=2 BLOCK, rc=1 ERROR. Preset `templates/conversus-presets/m013-uat-defect-merge.yml` (FR-13: red-blue defect-advocate vs fix-advocate, arbiter grounds on acceptance criterion, BLOCK-on-tie). `uat_defect_p` stub in `github-sync.sh` returns 0 (wired call site; SPEC-DEFECT mapping deferred post-M013 per planner judgment #4). Gate: `m013-p04-conversus-gate.sh` (16 assertions).

**T06 — FR-18 verify-cache + docs + phase-suite** (`ceeb8d2`). `github-status.sh --verify-cache` branch walks sidecar cache, probes remote via `gh_marker_search_remote`, emits `DIVERGENCE: missing-remote` lines, rc=0/5, never writes (FR-11). `commands/github-sync.md` (MEM012 structure), `commands/github-status.md` `--verify-cache` addendum, `references/github-integration.md` P04 extensions (3 TODO P04 stubs filled + 5 new subsections + Sync Action Mapping + Scope Boundary P04 column with FR-16/FR-18 rows). Pinned-SHA byte-identity on P01/P02/P03 sections captured pre-edit via three awk-ranges and embedded as literal digests in `m013-p04-reference-extensions.sh` (23 assertions). `m013-p04-bash32-compat.sh` (33 assertions, 11 files, self-exclude via case-branch + comment-discipline synonyms per P03 pattern). `m013-p04-phase-suite.sh` orchestrator aggregates 12 P04 gates + FR-5 lint + P01/P03 regression guards. `commands/github-sync-command.sh` gate (18 assertions). Comment-discipline synonym backfill applied to T04-authored `after-verify-sync.sh` L16 and `claude-code.sh` L30 (keeps bash32 scanner self-clean).

## Key patterns established (12)

- `additive-helper-append-preserves-byte-identity` — new public helpers appended before `github-common.sh` self-check block; prior function bodies untouched.
- `walker-shape-symmetry` — sync reuses init's projectable-phase walker with phases-first-milestone-last ordering swap for manifest-SSOT alignment.
- `idiomatic-lock-variable-gate` — assertion accepts `LOCK_MGR=<path>/lock-manager.sh` + `bash $LOCK_MGR create` as valid invocation shape without requiring literal one-line match.
- `direct-cmd-subst-rc-propagation` — assign `$()` then read `$?` on next line; `|| true` in cmd-subst tail silently zeros `$?`.
- `rate-limit-preflight-budget-check` — projected GraphQL volume > remaining budget → short-circuit rc=3 before first mutation.
- `per-call-stderr-mktemp-classify-rm` — capture stderr to mktemp, `classify_gh_rc rc + file`, `rm -f` on all paths; releases lock before exit 3/4.
- `fixture-copy-before-live-gate` — gates that exercise live mode copy fixture orchestrator-state to tmp (cp -R fx/. tmp/) rather than running against repo fixture; `github-sync.sh` reassigns ORCHESTRATOR_ROOT to `$PROJECT_ROOT/.orchestrator` and would pollute the repo tree otherwise.
- `shared-helper-for-T05-reuse` — `emit_conversus_gate_record` authored in `github-common.sh` under T03 ownership so T05's standalone gate sources and reuses the same shape.
- `FR-12-v1-negative-grep-guard` — byte-identity preservation on sibling-runtime files via negative greps for `post_verify`/FR-12 markers across codex/cursor installers+adapters; cheaper and more readable than pinned-sha baselines; self-repairs if codex/cursor later gain legitimate P04 content under a different marker.
- `external-watchdog-plus-adapter-internal-timeout` — `kill -TERM` at TIMEOUT then `kill -KILL` at TIMEOUT+1s belt-and-suspenders over adapter's own timeout (Constitution XII).
- `verdict-rc-parity` — rc=0 PASS, rc=2 BLOCK, rc=1 ERROR passed verbatim through gate; BLOCK distinguishable from error; upstream loop treats BLOCK as per-row error via if-guarded increment.
- `pinned-SHA-with-awk-range-capture-before-edit` — multi-phase additive doc editing workflow: capture awk-range shasum pre-edit, edit, embed literal digest in gate post-edit; extends `gate-evolution-on-legitimate-advancement` (P04/T06 relaxes P03/T04 gate analogously to P03/T04 relaxing P02/T05 gate).

## Verification results

P04 phase suite: 13 PASS (12 P04 gates + FR-5 regression guard). 2 FAIL on `m013-p01-phase-suite.sh` and `m013-p03-phase-suite.sh` regression guards, inheriting three pre-existing baseline failures caused by commit `04aab62` (`docs(commands): drop legacy speckit.orchestrator.* headers; use orchestrator:* convention`): `m013-p01-github-status-command.sh`, `m013-p02-github-init-command.sh`, and `m013-p03-reference-extensions.sh` (cascaded via P02 guard). All three baseline failures verified pre/post-T06 identical via stash/pop diff — **not introduced by P04**. Classified observational (US3 AS6): test expectations and the documentation convention are out-of-sync, not correctness. Deferred to an M013-close fix or the M013/P04 commands/README.md index update pass (planner judgment #2, also deferred per P03 precedent).

`graphql-call-shape.sh` PASS (3 whitelisted mutations). `anti-pattern-lint.sh` clean on all new/modified files. All P01–P03 byte-identity invariants preserved (pinned-sha gates all PASS). Bash 3.2+ / POSIX sh clean throughout.

## Decisions register updates

None required — all P04 decisions scoped to phase-internal shape choices already captured in task summaries and this body. FR-12 Claude-Code-only v1 was pre-committed in the M013 spec discuss (D014); P04 implements it without amendment. D014 Knowledge-Layer Boundary honored throughout — zero writes to `knowledge/spec/**`.

## Observability surfaces

- `unit_close` JSONL records (M019 Tier 1 shape) emitted at each successful upsert with kind/phase/task/oid/reason/duration fields via `emit_tier1_record`.
- `conversus_gate_invocation` JSONL records emitted per gate invocation with preset/verdict/duration/rc fields via `emit_conversus_gate_record`.
- Both land at `${ORCHESTRATOR_ROOT}/observability/tier1.jsonl` honoring the 4-rule resolver.

## Remaining known gaps (out of P04 scope)

1. **Baseline failures from commit `04aab62`** — three gates assert legacy `# speckit.orchestrator.*` heading shape; docs renamed to `orchestrator:*` convention. Fix is a one-line-per-gate update, deferred to M013 close or a follow-up phase.
2. **`commands/README.md` index** — no entry yet for `orchestrator:github sync`, `orchestrator:github init --re-init`. Deferred per P03 precedent (planner judgment #2).
3. **`phase-transition.sh` non-numeric duration bug** — inherited from P02/P03; P04 uses integer-minutes workaround throughout. Not in M013 scope.
4. **`uat_defect_p` full SPEC-DEFECT mapping** — stubbed return 0 today; full implementation is a one-function-body change post-M013 (planner judgment #4).
5. **Conversus gate invocation path** — scaffolded via `--conversus-gate` flag on `github-sync.sh`; gate-as-lifecycle-hook is deferred. Direct gate-script invocation is exercised by T05's 16-assertion gate; in-sync-loop invocation awaits a real `uat_defect_p` predicate.
