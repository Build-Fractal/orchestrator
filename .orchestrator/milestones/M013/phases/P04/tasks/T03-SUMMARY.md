---
schema_version: "1.0"
type: task-summary
id: "T03"
parent: "P04"
milestone: "M013"
provides:
  - "scripts/integrations/github-common.sh helpers classify_gh_rc + emit_conversus_gate_record; scripts/integrations/github-sync.sh FR-16 pre-flight rate-limit probe + per-call rc wrapper + FR-17 unit_close JSONL emitter; scripts/verify/m013-p04-rate-limit.sh (7/7 PASS); scripts/verify/m013-p04-observability.sh (8/8 PASS)"
requires:
  - "from:P04/T02 what:scripts/integrations/github-sync.sh core (flag parser, walker, reconcile loop, perform_upsert, sidecar_update_item_cache call sites); from:P04/T01 what:github-common.sh helpers http_probe + emit_tier1_record; from:P04/T01 what:tests/fixtures/m013-p04/sync-cycle/ fixture tree; from:P03/T03 what:graphql-call-shape.sh whitelist (no new mutations); from:M016 what:anti-pattern-lint.sh"
affects:
  - "scripts/integrations/github-sync.sh; scripts/integrations/github-common.sh; scripts/verify/m013-p04-rate-limit.sh; scripts/verify/m013-p04-observability.sh"
key_files:
  - "scripts/integrations/github-sync.sh; scripts/integrations/github-common.sh; scripts/verify/m013-p04-rate-limit.sh; scripts/verify/m013-p04-observability.sh"
key_decisions:
  - "FR-16 pre-flight probe uses direct cmd-subst assignment (not '|| true' tail) so http_probe rc=3/rc=4 propagates through $? — masking rc with '|| true' silently broke auth-expired detection; classify_gh_rc lives in github-common.sh (shared helper, sourced by github-sync.sh) rather than inlined — ready for T05 re-use if conversus-gate invokes gh directly; perform_upsert captures per-call stderr to mktemp then invokes classify_gh_rc — on rate-limit/auth-expired the wrapper calls release_lock BEFORE exit 3/exit 4 (FR-11 reversibility); oid extraction helpers (kind_of / phase_of / task_of) are pure string-op (awk + case-pattern), derive phase + task segments at emit time rather than threading them through the walker — keeps the T02 walker shape untouched; emit_conversus_gate_record ownership is T03 per operator preference (planner judgment call #3), T05 will defensively reference it; observability gate leverages github-sync.sh's ORCHESTRATOR_ROOT='$PROJECT_ROOT/.orchestrator' reassignment — JSONL lands in the fixture-copy root, so the gate copies the fixture to tmp before each live/dry run to avoid polluting the repo fixture tree"
patterns_established:
  - "direct-cmd-subst-rc-propagation: assign $() result then read $? on next line; '|| true' in cmd-subst tail silently zeros $?; rate-limit-preflight-budget-check: when projected GraphQL volume > 50 + probe reports remaining budget less than projected count, short-circuit rc=3 before first mutation; per-call-stderr-mktemp-classify-rm: capture stderr to mktemp, classify_gh_rc rc + file, rm -f on all paths — works around mktemp growing under long sync passes; fixture-copy-before-live-gate: gates that exercise live mode copy the fixture orchestrator-state to tmp (cp -R fx/. tmp/) rather than running against the repo fixture tree directly, because github-sync.sh reassigns ORCHESTRATOR_ROOT to PROJECT_ROOT/.orchestrator and would pollute the repo fixture; JSONL-append-only-via-emit-helper: unit_close emission uses emit_tier1_record helper (not direct file append) — honors source='runtime' + M019 Tier 1 shape + ORCHESTRATOR_ROOT 4-rule resolver; shared-helper-for-T05-reuse: emit_conversus_gate_record authored in github-common.sh under T03 ownership so T05's standalone github-conversus-gate.sh sources + reuses the same shape"
drill_down_paths:
  - ".orchestrator/milestones/M013/phases/P04/tasks/T03-PAYLOAD.md"
duration: "45"
verification_result: "pass"
completed_at: "2026-04-22T03:46:09Z"
---

T03 layers FR-16 (rate-limit + auth-expiry detection) and FR-17 (observability emitters) on top of T02's github-sync.sh core. Additive-only — zero changes to T02's reconcile loop, walker, or dry-run path. Three deliverable deltas: (a) github-common.sh gains classify_gh_rc (maps gh rc + stderr to ok/rate-limit/auth-expired/other class + rc 0/3/4/1) and emit_conversus_gate_record (thin wrapper around emit_tier1_record for the conversus gate call site, shared between github-sync.sh and T05's github-conversus-gate.sh); (b) github-sync.sh gains oid-extraction helpers (kind_of, phase_of, task_of, lookup_desired), pre-flight rate-limit probe after lock acquisition + before reconcile loop (fires when projected GraphQL mutations > 50; exits rc=3 on 403+remaining=0 or budget-under-projected, rc=4 on 401 — release_lock before every exit), and classify_gh_rc wrapping on every live gh call inside perform_upsert (close + status-sync) with the same rc=3/rc=4/release-lock-before-exit contract; wires emit_tier1_record unit_close at the successful-upsert site (outcome=closed for close reason, status-synced for status-sync reason; never in dry-run); (c) two new gates — m013-p04-rate-limit.sh (7 assertions: classify_gh_rc defined; github-sync.sh rc=3 + rc=4 paths wired; 60-phase fixture + 403 stub → rc=3 + RATE-LIMIT diagnostic; 60-phase fixture + 401 stub → rc=4 + AUTH-EXPIRED diagnostic) and m013-p04-observability.sh (8 assertions: emit_tier1_record + emit_conversus_gate_record defined; unit_close emitter wired; dry-run writes zero JSONL; live mode writes JSONL; every line M019 Tier 1 shape; >=1 unit_close record; FR-5 lint still green). All 7+8=15 new assertions PASS. Regression: T01 gates m013-p04-sync-fixture 28/28 + m013-p04-github-common-p04 19/19 still PASS; T02 gates m013-p04-github-sync 14/14 + m013-p04-dry-run-manifest 4/4 still PASS; graphql-call-shape.sh detects 3 whitelisted mutation shapes (no new shape introduced). Key bug caught and fixed mid-flight: initial probe shape "probe_out=\$(http_probe ... || true); probe_rc=\$?" silently masked http_probe's rc=4 to 0 because '|| true' was the last element of the command-substitution pipeline — auth-expired path never fired. Removed the '|| true' so rc propagates cleanly (set -u doesn't implode on non-zero). Live JSONL output byte-matches tests/fixtures/m013-p04/sync-cycle/expected-unit-close.jsonl (modulo the FROZEN-ISO ts placeholder): 3 unit_close records (1 status-synced phase + 2 closed tasks). Judgment calls: (i) observability gate copies fixture to tmp before live run — necessary because github-sync.sh reassigns ORCHESTRATOR_ROOT unconditionally at line 119, so even with external ORCHESTRATOR_ROOT the JSONL lands in PROJECT_ROOT/.orchestrator (which is the fixture tree); (ii) classify_gh_rc authored in github-common.sh (shared, sourced) rather than inlined — ready for T05 re-use; (iii) emit_conversus_gate_record authored in T03 per planner preference (T05 defensively references). FR-5 whitelist unchanged (rate-limit probe is REST /rate_limit, outside mutation scope). SC-7 preserved: rate-limit/auth-expiry paths emit diagnostic + exit without prompt. FR-11 reversibility: release_lock before every rc=3/rc=4 exit. D014 Knowledge-Layer Boundary respected (no SPEC-* / KNOWLEDGE-INDEX.md / rebuild-index.sh writes). anti-pattern-lint.sh clean on all four touched files.
