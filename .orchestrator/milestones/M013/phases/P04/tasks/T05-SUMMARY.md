---
schema_version: "1.0"
type: task-summary
id: "T05"
parent: "P04"
milestone: "M013"
provides:
  - "scripts/integrations/github-conversus-gate.sh (M011/P07 adapter --strict invocation with watchdog + parse-verdict + verdict-as-comment + emit_conversus_gate_record); templates/conversus-presets/m013-uat-defect-merge.yml (FR-13 preset: red-blue defect-advocate vs fix-advocate, arbiter grounds on acceptance criterion, BLOCK-on-tie); scripts/integrations/github-sync.sh uat_defect_p stub + --conversus-gate invocation site (per-row, guarded on reason=close, BLOCK=rc=2 increments errors); scripts/verify/m013-p04-conversus-gate.sh 16/16 gate (SC-7 short-circuit, stub PASS+BLOCK, FR-17 field-complete, watchdog, required-flags, preset-present)"
requires:
  - "from:M013/P04/T01 what:emit_tier1_record + github-common.sh source; from:M013/P04/T03 what:emit_conversus_gate_record wrapper (pre-existing, re-used byte-identical); from:M011/P07 what:scripts/dispatch/adapters/tool/conversus.sh gate --strict + parse-verdict + CONVERSUS_STUB contract"
affects:
  - "scripts/integrations/github-conversus-gate.sh (create); templates/conversus-presets/m013-uat-defect-merge.yml (create); scripts/integrations/github-sync.sh (modify: add uat_defect_p stub + per-row gate invocation in reconcile loop + refresh T02 scaffold comment); scripts/verify/m013-p04-conversus-gate.sh (create)"
key_files:
  - "scripts/integrations/github-conversus-gate.sh;templates/conversus-presets/m013-uat-defect-merge.yml;scripts/integrations/github-sync.sh;scripts/verify/m013-p04-conversus-gate.sh"
key_decisions:
  - "D007 strict-mode invocation respected -- adapter absence is rc=1 HARD FAIL (not graceful degradation); Constitution XII 30s default timeout with external watchdog (kill -TERM at TIMEOUT + kill -KILL at TIMEOUT+1) belt-and-suspenders over adapter internal timeout; FR-17 emitter re-used byte-identical from T03 authoring; FR-12 gate script is runtime-agnostic (hook that invokes it is Claude-Code-only per T04); uat_defect_p stubbed return-0 per payload judgment call 4 (SPEC-DEFECT mapping deferred post-M013); preset m013-uat-defect-merge.yml authored per adapter preset-check pre-stub-branch requirement; exit code verbatim-pass-through (rc=0 PASS, rc=2 BLOCK, rc=1 error) lets upstream sync loop treat BLOCK as per-row error via if-guarded increment"
patterns_established:
  - "External-watchdog-plus-adapter-internal-timeout belt-and-suspenders -- kill -TERM at TIMEOUT seconds then kill -KILL at TIMEOUT+1s; verdict-rc-parity contract -- rc=0 PASS, rc=2 BLOCK, rc=1 ERROR passed verbatim to caller so BLOCK is distinguishable from error; stub-mode preset file still required -- CONVERSUS_STUB=1 skips binary resolution but preset yml must exist at templates/conversus-presets/NAME.yml (adapter check order); per-row gate invocation inside reconcile loop -- guarded on --conversus-gate flag AND reason=close AND uat_defect_p predicate; stub predicate shipping before full semantic -- uat_defect_p returns 0 today, wires the call site so post-M013 SPEC-DEFECT mapping is a one-function change"
drill_down_paths:
  - ".orchestrator/milestones/M013/phases/P04/tasks/T05-PLAN.md;.orchestrator/milestones/M013/phases/P04/tasks/T05-PAYLOAD.md"
duration: "50m"
verification_result: "pass"
completed_at: "2026-04-22T03:59:35Z"
---

T05 shipped the M013 conversus UAT PR gate as a standalone invocation site over the M011/P07 adapter. Four surfaces touched. scripts/integrations/github-conversus-gate.sh (116 lines, bash 3.2, jq-optional): argv parser for --issue-ref + --artifact + --timeout + --preset + --i-am-operator, SC-7 no-tty+no-operator short-circuit emits STATUS: gate-deferred + rc=0, adapter invocation via bash scripts/dispatch/adapters/tool/conversus.sh gate --strict PRESET ARTIFACT tmp-out, external watchdog subshell (kill -TERM at TIMEOUT seconds + kill -KILL at TIMEOUT+1s) wraps the adapter call for belt-and-suspenders enforcement, verdict parsed via adapter parse-verdict subcommand + awk extraction, FR-13 verdict-as-comment via gh issue comment on live mode (skipped when CONVERSUS_STUB or M013_GH_STUB_DIR set), FR-17 emit_conversus_gate_record call with all five fields (issue_ref, timeout_sec, verdict, rc, duration_ms), exit-code verbatim pass-through (0 PASS / 2 BLOCK / 1 error). templates/conversus-presets/m013-uat-defect-merge.yml authored per adapter preset-check requirement -- red-blue defect-advocate vs fix-advocate, arbiter grounds on defect acceptance criterion plus Constitution II Evidence Before Claims, BLOCK-on-tie semantics. scripts/integrations/github-sync.sh gained a uat_defect_p stub (returns 0 until SPEC-DEFECT mapping lands post-M013 per payload judgment call 4) and a per-row gate invocation inside the reconcile loop -- guarded on CONVERSUS_GATE equals 1 AND reason equals close AND uat_defect_p equals 1, BLOCK (rc=2) increments errors counter; T02 scaffold comment refreshed to note the wiring exists. scripts/verify/m013-p04-conversus-gate.sh 16 assertions covering: gate presence + adapter call + timeout default + emitter call (4 static); SC-7 short-circuit + zero-JSONL-on-short-circuit (2 runtime); stub-mode PASS rc=0 + JSONL append + FR-17 field completeness + verdict-plus-source correctness + stub-mode BLOCK rc=2 + BLOCK verdict field (6 runtime); watchdog within timeout+buffer (1 runtime); missing-args guidance + preset-present + adapter-present (3 static). Regression green on every P04 gate: sync-fixture 28/28, github-common-p04 19/19, github-sync 14/14, dry-run-manifest 4/4, rate-limit 7/7, observability 8/8, post-verify-hook 14/14, graphql-call-shape PASS (3 whitelisted mutation shapes), anti-pattern-lint --fixture clean on new gate script plus gate verifier plus modified github-sync.sh. D014 Knowledge-Layer Boundary respected -- zero writes to knowledge/spec, KNOWLEDGE-INDEX.md, or rebuild-index.sh. D007 strict-mode honored -- adapter absence is HARD FAIL rc=1 with no graceful degradation. Constitution XII 30s default timeout enforced, operator-overridable via --timeout. FR-12 Claude-Code-only-v1 preserved -- gate script is runtime-agnostic; the post-verify hook that invokes it (T04) is Claude-Code-only. FR-5 whitelist respected -- T05 introduces zero GraphQL mutations; gh issue comment is REST. emit_conversus_gate_record helper was not re-added defensively -- T03 authored it byte-identical at github-common.sh line 930 (per planner judgment call 3). uat_defect_p stub returns 0 today; wiring the call site means the post-M013 SPEC-DEFECT-star.md mapping ships as a single function-body change rather than a reconcile-loop retrofit. Integer-minutes duration 50m.
