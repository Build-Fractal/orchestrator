---
schema_version: "1.0"
type: phase-summary
id: "P01"
parent: "M015"
milestone: "M015"
provides:
  - "extension.yml deleted; 9 .claude/commands/speckit.*.md deleted; .specify/scripts/bash/ deleted; .specify/templates/commands/ deleted; 6 root .specify/templates/*-template.md deleted; 4 verify scripts added under scripts/verify/m015-p01-*.sh; evaluate-preflight.sh fixed to pass --project-root flag to generate-permissions.sh and write-permissions.sh, deleted scripts/verify/m002-p07-extension-registration.sh, deleted tests/fixtures/verify-pass/extension.yml, deleted tests/fixtures/verify-fail/extension.yml, new scripts/verify/m015-p01-no-extension-test-artifacts.sh, scripts/verify/m015-p01-preflight-permissions-ok.sh verify script; preflight --project-root flag fix confirmed present, new scripts/verify/m015-p01-no-stale-refs.sh; reference sweep across 30+ retained files with deleted-path references removed or updated"
requires:
  - "T01 deletions, T01 deletions context (T01 pre-applied the argument-passing fix during its edit pass), T01-T03 deletions"
affects:
  - "P02 state migration, P03 doc reframe, M015/P01 verification, future preflight runs; permissions=generated now reported instead of permissions=error, P03 doc reframe (5 P03-reserved docs + specs/ + .planning/ remain tolerated by verify script allow-list)"
key_files:
  - "scripts/verify/m015-p01-no-extension-yml.sh, scripts/verify/m015-p01-no-speckit-commands.sh, scripts/verify/m015-p01-no-specify-bash.sh, scripts/verify/m015-p01-no-specify-templates.sh, scripts/verify/m015-p01-no-extension-test-artifacts.sh, scripts/verify/m015-p01-preflight-permissions-ok.sh, scripts/verify/m015-p01-no-stale-refs.sh, scripts/lifecycle/evaluate-preflight.sh, scripts/verify/check-must-haves.sh, scripts/knowledge/lib/index-utils.sh, scripts/lifecycle/generate-permissions.sh, scripts/knowledge/write-summary.sh, scripts/AGENTS.md, scripts/verify/m007-p03-provenance-chain-traversal.sh, scripts/verify/m004-p06-check-must-haves-root.sh, references/file-formats.md, references/constitution-walkthrough.md, commands/evaluate.md, templates/orchestrator-config-default.yml, tests/test-s01-structure.sh, tests/test-s06-knowledge-lifecycle.sh, tests/test-s07-integration.sh, tests/AGENTS.md, knowledge/patterns/MEM007.md, .claude/settings.local.json"
key_decisions:
  - "FR-013 migration-adapter preservation honored (scripts/migrate/adapters/speckit.sh, scripts/state/detect-speckit.sh, scripts/dispatch/adapters/format/speckit.sh, commands/migrate.md untouched); M007 no-graceful-degradation (hard delete, no rename/shim/archive), Delete disposition for all 3 extension-shape artifacts (no standalone equivalent — skill-based discovery replaces manifest), Step 1 (1-token fix on line 74) was a no-op — T01 opportunistically applied the --project-root flag insertion while editing the same code path, updating both the generate-permissions.sh and write-permissions.sh call sites. Verified current state before editing and confirmed the buggy positional form was already replaced with the flag form. Proceeded with Steps 2-3 (create verify script + chmod) as planned., Hard-deleted 8 dead extension-registration verify scripts (m002-p05/p06/p07, m004-p07, m006-p04/p06, p06-*) whose entire purpose was asserting registration in extension.yml; deleted .specify/scripts/powershell/ mirror tree (same dead condition as deleted bash tree); deleted unreferenced root-level speckit-orchestrator-overview.md (pre-M015 historical doc, zero cross-refs); rewrote test-s01 and test-s07 to drop extension.yml manifest assertions while retaining directory-structure + command cross-ref + capability-detection + idempotency checks; replaced extension.yml project-root sentinel in get_project_root and 18 test-fixture sites with .orchestrator/ + .specify/orchestrator/ + .git markers."
patterns_established:
  - "Hard-delete cutover discipline: git rm for tracked files; no legacy/ rename; no compat shim; verify scripts use file-absence assertions, When a downstream task plan assumes a buggy state but an upstream sibling task has already fixed the bug opportunistically, verify current on-disk state before applying the documented edit; treat Step 1 as a no-op and proceed with remaining steps (verification artifact creation) that are still required., Verify-script allow-list can extend beyond spec's 5 P03 doc files when historical artifact classes (specs/, .planning/) and self-reference cases (m015-p01 verify scripts naming their own assertion patterns) legitimately match; BSD grep's --exclude-dir is basename-matched so post-filter with grep -Ev on path prefixes is required"
drill_down_paths:
  - ".specify/orchestrator/milestones/M015/phases/P01/tasks/T01-SUMMARY.md, .specify/orchestrator/milestones/M015/phases/P01/tasks/T02-SUMMARY.md, .specify/orchestrator/milestones/M015/phases/P01/tasks/T03-SUMMARY.md, .specify/orchestrator/milestones/M015/phases/P01/tasks/T04-SUMMARY.md"
duration: "78m"
verification_result: "pass"
completed_at: "2026-04-15T11:47:48Z"
observability_surfaces:
  - "none"
---

P01 Spec-Kit Host Removal is complete: the repo no longer registers itself as a spec-kit extension. `extension.yml` is deleted, the 9 dogfooded `/speckit.*` slash commands and the 5 `.specify/scripts/bash/` helpers are gone, the 6 spec-kit-style root templates and the `.specify/templates/commands/` tree are gone, and the 3 extension-shape test artifacts (`m002-p07-extension-registration.sh`, `tests/fixtures/verify-{pass,fail}/extension.yml`) are deleted. A call-site bug in `evaluate-preflight.sh` — passing `$PROJECT_ROOT` positionally to `generate-permissions.sh` / `write-permissions.sh` — was fixed to the documented `--project-root` flag form; `evaluate-preflight.sh . B` now reports `permissions=generated` instead of `permissions=error`. Seven new verify scripts (`scripts/verify/m015-p01-*.sh`) prove each removal class and the preflight fix, and all P01 must-haves PASS via `check-must-haves.sh`.

Scope expansions worth recording: (1) T01 opportunistically fixed the preflight bug while editing the same code path, making T03's Step 1 a no-op (verify script still created and run). (2) T04 swept 30+ non-exempt retained files, replacing `extension.yml` project-root sentinels with `.git`/`.orchestrator`/`.specify/orchestrator` markers, rewriting `tests/test-s01-structure.sh` and `test-s07-integration.sh` to drop manifest assertions while keeping directory + cross-ref + capability-detection checks, and hard-deleting 8 dead extension-registration verify scripts, the `.specify/scripts/powershell/` mirror tree, and the unreferenced `speckit-orchestrator-overview.md`. (3) T04 also surgically fixed a pre-existing `check-must-haves.sh` backtick-stripping bug that would have blocked P01-PLAN's new backtick-quoted artifact format.

Migration adapters (`scripts/migrate/`, `scripts/state/detect-speckit.sh`, `scripts/dispatch/adapters/format/speckit.sh`, `commands/migrate.md`) were preserved verbatim per FR-013 — they detect spec-kit shape as a migration *source*, not a runtime host. Historical artifacts under `.specify/orchestrator/`, `CHANGELOG.md`, and the 5 P03-reserved documentation files remain untouched and are tolerated by the `no-stale-refs` verify script's allow-list. Discipline followed M007 hard-delete semantics throughout — no rename, no archive, no compat shim.

Patterns established: hard-delete cutover with verify-script gating; opportunistic upstream fixes require downstream verification of current state before applying documented edits; and verify-script allow-lists must post-filter (BSD grep `--exclude-dir` is basename-matched) and legitimately extend beyond the spec's enumerated exemptions when historical artifact classes and self-referential verify scripts match the same pattern.

Verification: all 7 truth checks, all 21 artifact-bundle checks, and the key-link check PASS. Four task summaries drill down from this phase.
