P01 Spec-Kit Host Removal is complete: the repo no longer registers itself as a spec-kit extension. `extension.yml` is deleted, the 9 dogfooded `/speckit.*` slash commands and the 5 `.specify/scripts/bash/` helpers are gone, the 6 spec-kit-style root templates and the `.specify/templates/commands/` tree are gone, and the 3 extension-shape test artifacts (`m002-p07-extension-registration.sh`, `tests/fixtures/verify-{pass,fail}/extension.yml`) are deleted. A call-site bug in `evaluate-preflight.sh` — passing `$PROJECT_ROOT` positionally to `generate-permissions.sh` / `write-permissions.sh` — was fixed to the documented `--project-root` flag form; `evaluate-preflight.sh . B` now reports `permissions=generated` instead of `permissions=error`. Seven new verify scripts (`scripts/verify/m015-p01-*.sh`) prove each removal class and the preflight fix, and all P01 must-haves PASS via `check-must-haves.sh`.

Scope expansions worth recording: (1) T01 opportunistically fixed the preflight bug while editing the same code path, making T03's Step 1 a no-op (verify script still created and run). (2) T04 swept 30+ non-exempt retained files, replacing `extension.yml` project-root sentinels with `.git`/`.orchestrator`/`.specify/orchestrator` markers, rewriting `tests/test-s01-structure.sh` and `test-s07-integration.sh` to drop manifest assertions while keeping directory + cross-ref + capability-detection checks, and hard-deleting 8 dead extension-registration verify scripts, the `.specify/scripts/powershell/` mirror tree, and the unreferenced `speckit-orchestrator-overview.md`. (3) T04 also surgically fixed a pre-existing `check-must-haves.sh` backtick-stripping bug that would have blocked P01-PLAN's new backtick-quoted artifact format.

Migration adapters (`scripts/migrate/`, `scripts/state/detect-speckit.sh`, `scripts/dispatch/adapters/format/speckit.sh`, `commands/migrate.md`) were preserved verbatim per FR-013 — they detect spec-kit shape as a migration *source*, not a runtime host. Historical artifacts under `.specify/orchestrator/`, `CHANGELOG.md`, and the 5 P03-reserved documentation files remain untouched and are tolerated by the `no-stale-refs` verify script's allow-list. Discipline followed M007 hard-delete semantics throughout — no rename, no archive, no compat shim.

Patterns established: hard-delete cutover with verify-script gating; opportunistic upstream fixes require downstream verification of current state before applying documented edits; and verify-script allow-lists must post-filter (BSD grep `--exclude-dir` is basename-matched) and legitimately extend beyond the spec's enumerated exemptions when historical artifact classes and self-referential verify scripts match the same pattern.

Verification: all 7 truth checks, all 21 artifact-bundle checks, and the key-link check PASS. Four task summaries drill down from this phase.
