P03 Documentation Reframe is complete. Current-state documentation now describes the orchestrator as a standalone tool running on Claude Code, Codex CLI, or Cursor — with no runtime dependency on spec-kit. The five primary docs (`README.md`, `CLAUDE.md`, `references/architecture.md`, `references/installation.md`, `docs/getting-started.md`) were reframed end-to-end: install flow pivots from `cp -r extension.yml ./` to `packaging/install/install-<runtime>.sh`, SDD entry points move from `/speckit.*` slash commands to `orchestrator:*` skill names, canonical state location shifts to `.orchestrator/`, and the constitution reference points at `.orchestrator/memory/constitution.md`. A new M015 entry was prepended to `CHANGELOG.md` at `v0.9.0 — 2026-04-15`; the historical tail below `## [0.8.0]` is byte-identical to the pre-reframe snapshot captured in T01.

Thirteen additional reference/user-guide docs were mechanically swept for legacy path references (`references/engine.md`, `events.md`, `errors.md`, `recipes.md`, `file-formats.md` — 22 refs in that one alone — `state-machine.md`, `tier-definitions.md`, `constitution-walkthrough.md`, `verification-ladder.md`; `docs/knowledge-management.md`, `recipe-authoring.md`, `hook-development.md`; `scripts/AGENTS.md`). Tier A and verification-ladder prose shifted from "standard spec-kit commands" to runtime-neutral "host runtime's native SDD workflow" phrasing. Structural content — schemas, event names, error kinds, recipe syntax, state machine rules, tier classifications, constitution principles — was preserved.

A new `docs/migrating-from-speckit.md` (147 lines, 9 H2 sections) describes the spec-kit-to-orchestrator migration journey: what/why, spec-kit shape detection, running the migration via `orchestrator:migrate` / `scripts/migrate/migrate-state.sh`, what is preserved, post-migration layout, continuing to author spec-kit-shaped specs via `scripts/dispatch/adapters/format/speckit.sh`, and the explicit note that spec-kit is a migration *source*, not a runtime dependency (FR-012 satisfied). The migration guide was allow-listed in `scripts/verify/m015-p02-no-stale-state-refs.sh` under `ALLOW_MIGRATION` because it legitimately references `.specify/orchestrator/` as the migration source path.

T04 sealed the transitional `ALLOW_P03_DOCS` allow-list with the never-match sentinel `__P03_COMPLETE_NEVER_MATCH__` — all 17 previously-tolerated files now show zero legacy references, so any future accidental reintroduction will surface as a real FAIL rather than being silently tolerated. `ALLOW_SELF_REFERENCE` was extended to cover the P03 verify scripts and the historical-changelog snapshot (regression-guard self-reference pattern).

Historical immutability was honored: no `.orchestrator/` tree contents, no `DECISIONS.md` entries, no prior `CHANGELOG.md` entries (below `## [0.8.0]`) were rewritten. Migration adapters (`scripts/migrate/`, `scripts/state/detect-speckit.sh`, `scripts/dispatch/adapters/format/speckit.sh`, `commands/migrate.md`) were preserved verbatim per FR-013 — empty `git diff --stat` for the exempt paths.

Verification: all 6 P03 verify scripts PASS (`standalone-framing`, `no-legacy-install`, `changelog-has-m015`, `migration-doc`, `wider-docs-sweep`, `allow-list-tightened`). `m015-p02-no-stale-state-refs.sh` still PASSes (no regression). `check-must-haves.sh .orchestrator/milestones/M015/phases/P03` reports 16/16 PASS across 6 truths, 8 artifacts, and 2 key-links.

Patterns established: pre-reframe gate scaffolding (write verify scripts + historical snapshot in T01, then execute reframes against those gates); snapshot-based immutability (capture at "first ## [" pre-edit, verify starting at "second ## [" post-edit, so prepending a new top entry doesn't trip the diff); framing-verifier-friendly migration callouts (describe the relocation without literally naming the legacy path); seal-by-sentinel allow-list retirement (replace regex body with `__NAME_NEVER_MATCH__` when a transitional tolerance has served its purpose). Four task summaries drill down from this phase.
