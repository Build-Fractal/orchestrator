---
schema_version: "1.0"
type: task-summary
id: "T01"
parent: "P05"
milestone: "M018"
provides:
  - "Four additive integer fields land on dispatch_usage and unit_close JSONL records emitted by scripts/dispatch/dispatch-interface.sh:_di_emit_dispatch_usage and scripts/knowledge/write-summary.sh:_ws_emit_unit_close — filter_dropped_tokens, tier1_savings_tokens, tier2_savings_tokens, tier1_invocations. Two new internal rollup helpers _di_rollup_savings_fields (unitId-scoped) and _ws_rollup_savings_fields (granularity-aware: task=M+P+T, phase=M+P, milestone=M only) read in-flight payload_breakdown records from execution-log.jsonl via single-pass awk and emit four integers on stdout (one per line). Both emit branches in _di_emit_dispatch_usage (happy-path numeric cost AND pricing-degradation cost=null+pricing_warning) carry the new fields between pricing_version and model/pricing_warning. _ws_emit_unit_close carries the new fields between retry_count and source. CON-5 additive: pre-P05 records remain valid JSON; missing fields treated as 0 by downstream consumers."
requires:
  - "P02 payload_breakdown filter_dropped_tokens emit-time field; P03 payload_breakdown tier1_savings_tokens + tier1_invocations emit-time fields; P04 payload_breakdown tier2_savings_tokens emit-time field. The four field names match upstream P02/P03/P04 verbatim — no rename. Existing _di_emit_dispatch_usage log_file resolution at lines ~198-205 (ORCH_ROOT/phases/ fixture mode OR ORCH_ROOT/milestones/MILESTONE_ID); existing _ws_emit_unit_close log_dir/log_file resolution; existing ORCH_M019_EMIT=0 short-circuit at line 148 (BEFORE the rollup helper invocation)."
affects:
  - "T02 (metrics-rollup.sh + efficiency-footer.sh + check-anomalies.sh extensions read these dispatch_usage/unit_close fields with the absent-defaults-to-zero rule); T03 (compression-eval.sh reads from execution-log.jsonl with the same shape); T04 (verifier scripts m018-p05-dispatch-usage-additivity.sh + m018-p05-unit-close-additivity.sh exercise the production code shipped here, plus dual-write recent-changes for CLAUDE.md/AGENTS.md). M027 cost+token transparency surfaces consume these fields downstream from dispatch_usage / unit_close — the field names are the contract."
key_files:
  - "scripts/dispatch/dispatch-interface.sh,scripts/knowledge/write-summary.sh"
key_decisions:
  - "emitter-internal sed -n -E + awk extractor co-located with each rollup helper rather than sourcing scripts/util/json-field.sh — avoids a new sourcing dependency in the dispatch hot path; MEM004 carve-out applies (rollup helpers are dispatch-internal emitters, not Check: commands and not agent-facing payload bytes); rollup helpers read the IN-FLIGHT log so dispatch_usage sees the just-emitted same-unitId payload_breakdown records (build-context.sh writes payload_breakdown BEFORE dispatch-interface.sh writes dispatch_usage in the dispatch path); granularity-aware scope match for unit_close mirrors awk index() pattern from existing verification_pass_rate aggregation in _ws_emit_unit_close (lines ~395-414); defensive [-n var] || var=0 guard after each sed -n line read so a malformed helper return cannot inject a non-integer into the printf %d slot; field placement chosen to keep pricing/quality fields grouped with new savings fields then closing identity fields (source, timestamp) for readable raw JSONL."
patterns_established:
  - "MEM004 dispatch-internal carve-out extends to JSONL-record-rollup helpers — single-pass awk with index() scope filter + match()/RSTART/RLENGTH/sub() for integer field extraction with absent-defaults-to-zero arithmetic; helpers emit four integers on stdout (one per line) consumed by sed -n Np into named scalars, defensively floored to 0 (Bash 3.2 — no declare -A, parallel scalars instead); same algorithmic shape works for unitId-scoped (_di) and granularity-scoped (_ws) variants — the only difference is the awk -v variables and the early-return next conditions."
drill_down_paths:
  - ".orchestrator/milestones/M018/phases/P05/tasks/T01-schema-extensions-PAYLOAD.md,scripts/dispatch/dispatch-interface.sh,scripts/knowledge/write-summary.sh"
duration: "45m"
verification_result: "pass"
completed_at: "2026-04-28T04:19:29Z"
---

T01 lands the four additive integer fields filter_dropped_tokens / tier1_savings_tokens / tier2_savings_tokens / tier1_invocations on the dispatch_usage and unit_close JSONL records emitted by _di_emit_dispatch_usage (scripts/dispatch/dispatch-interface.sh) and _ws_emit_unit_close (scripts/knowledge/write-summary.sh) per the M018/P05/T01 plan. Two new internal rollup helpers (_di_rollup_savings_fields and _ws_rollup_savings_fields) co-locate with their emitters and run a single-pass awk over the in-flight execution-log.jsonl, emitting four integers on stdout (one per line). The dispatch_usage rollup matches on unitId; the unit_close rollup is granularity-aware (task: milestone+phase+task; phase: milestone+phase; milestone: milestone only). Records lacking a field contribute 0 (CON-5 absent-as-zero); multiple matching records sum.

Both emit branches of _di_emit_dispatch_usage (happy-path numeric cost AND pricing-degradation cost=null+pricing_warning) carry the new fields between pricing_version and model/pricing_warning. _ws_emit_unit_close carries them between retry_count and source. The ORCH_M019_EMIT=0 short-circuit at dispatch-interface.sh:148 still fences the helper (no log read, no field computation, no record append). Empty / missing log → rollup returns 0/0/0/0 → record remains valid JSON with all four fields as integer 0. Bash 3.2 compliance preserved (no declare -A, parallel scalars, awk/sed only inside MEM004-carved-out helper bodies).

Verification (Evidence Before Claims, Constitution Principle II):
- Syntax: bash -n on both files passes.
- Helper unit smoke (synthetic 4-record fixture): _di_rollup_savings_fields M018/P05/T01 returns 150/275/325/3 (sum of two matching records); _di_rollup unit=missing returns 0/0/0/0; _di_rollup empty log path returns 0/0/0/0; _ws_rollup task M018/P05/T01 returns 150/275/325/3; _ws_rollup phase M018/P05 returns 1149/1274/1324/102 (all P05 records); _ws_rollup milestone M018 returns 1156/1285/1337/105 (all M018 records, including P04/T01 record with 7/11/13/3).
- End-to-end emit smoke: _ws_emit_unit_close drives a real fixture log; emitted unit_close line carries filter_dropped_tokens:150, tier1_savings_tokens:275, tier2_savings_tokens:325, tier1_invocations:3; record_type=unit_close present; python3 json.loads validates each line.
- _di_emit_dispatch_usage end-to-end happy-path emit: dispatch_usage line carries the four fields with the same sums; JSON valid. Degradation branch (warning_override=adapter-failed): line carries the four fields PLUS estimated_cost_usd:null + pricing_warning:adapter-failed; JSON valid. ORCH_M019_EMIT=0 short-circuit honored (no append, line count stable).
- Regression sweep: m018-p02-emitter-additivity, m018-p03-emitter-additivity, m018-p04-tier2-emitter-additivity all PASS; m018-p02-disable-flag-honored, m018-p03-disable-flag-honored, m018-p04-tier2-disable-flag all PASS — pre-T01 baseline goldens remain byte-identical because T01 does not touch build-context.sh.

T04 will ship the public m018-p05-dispatch-usage-additivity.sh + m018-p05-unit-close-additivity.sh verifiers, the savings-bearing + no-savings fixtures, the fixture-staging helper, the P05-SUMMARY, and the CLAUDE.md/AGENTS.md recent-changes dual-write. T02 (cost rollup / efficiency footer / check-anomalies) and T03 (compression-eval) consume the dispatch_usage/unit_close shape that T01 now ships.
