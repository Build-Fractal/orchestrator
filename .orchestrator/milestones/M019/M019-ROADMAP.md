---
schema_version: "1.0"
type: roadmap
milestone: "M019"
feature_ref: "019-observability-metrics"
feature_spec: "specs/019-observability-metrics/spec.md"
vision: "Ship the Tier 1 emitter of M019 (Observability & Efficiency Metrics) on top of an Opus 4.7-adapted dispatch baseline so M012–M014 dogfooding produces measured, comparable data on time/tokens/$/quality across task/phase/milestone granularities — without building any Tier 2/3 surface."
tier: "C"
created_at: "2026-04-17T23:35:00Z"
updated_at: "2026-04-17T23:35:00Z"
---

## Phases

- [x] **P00**: Opus 4.7 Baseline Adaptation — "A developer builds a dispatch payload via `scripts/dispatch/build-context.sh` on a fixture task, and the rendered payload contains an explicit first-turn completeness block (intent + constraints + acceptance criteria + file paths), stable sections appearing before volatile sections wrapped in a `<dispatch-volatile>` marker, no `thinking_budget:` references anywhere in `templates/` or `scripts/engine/intensity-gate.sh`, an explicit parallel-fan-out directive for any task recipe declaring parallelizable subtasks, and `bash scripts/verify/m019-p00-payload-shape.sh` exits 0; additionally `bash scripts/lifecycle/evaluate-preflight.sh .` against a fixture `.claude/settings.json` containing the M021 hook + widened allow-list preserves both byte-identically."
  - Risk: high
  - Depends: none
  - Boundary Map:
    - Produces: `scripts/dispatch/build-context.sh` (modified — emits first-turn completeness block from existing plan artifacts; orders stable-before-volatile; emits `<dispatch-volatile>` marker around volatile sections; emits explicit parallel-fan-out directive when recipe declares parallelizable subtasks), `templates/dispatch-prompt.md` + downstream templates (modified — `thinking_budget:` removed; expressive guidance rewritten as positive examples; Constitution XV anti-pattern prohibitions remain negative by design), `scripts/engine/intensity-gate.sh` (modified — fixed thinking-budget knobs replaced with adaptive-thinking prompt nudges), `scripts/lifecycle/write-permissions.sh` (modified — additive-merge: when `_generated_by: speckit-orchestrator` marker is present AND user-added entries exist beyond the generated set, preserve hooks + extra allow-list entries; identified by a `// generated:start` / `// generated:end` sentinel pair), `.orchestrator/config/pricing.yml` (new — Anthropic Opus 4.7 / Sonnet 4.6 / Haiku 4.5 input/output rates per million tokens with `last_updated` timestamp), `scripts/verify/m019-p00-payload-shape.sh` (new — asserts L1–L5 invariants on a fixture-built payload), `scripts/verify/m019-p00-evaluate-preflight-additivity.sh` (new — asserts AD-7 fix: M021 hook + allow-list survive a fresh evaluate-preflight run), `scripts/verify/m019-p00-no-regression.sh` (new — wraps `tests/test-s01.sh`..`tests/test-s07.sh` + `scripts/verify/anti-pattern-lint.sh` + `scripts/verify/run-suite.sh m021 P04` and asserts all green against adapted templates), `scripts/verify/m019-p00-bash32-compat.sh` (new — bash-3.2 compat scan for all P00-touched/created `.sh` files), `scripts/verify/m019-p00-phase-suite.sh` (new — orchestrates the four P00 verify gates above)
    - Consumes: existing dispatch payload structure from `scripts/dispatch/build-context.sh` pre-adaptation, existing intensity-gate prompts from `scripts/engine/intensity-gate.sh`, existing template set under `templates/`, existing `tests/test-s01.sh`..`tests/test-s07.sh` regression suites, M021 `scripts/hooks/pre-bash-shape-guard.sh` + `.claude/settings.json` widened allow-list (target of preservation), M021 `scripts/verify/anti-pattern-lint.sh` (gate that must continue to PASS unchanged), `.orchestrator/scratch/articles-synthesis-2026-04-17.md` (L1–L5 source material)

- [x] **P01**: Tier 1 Emitter — "A developer dispatches a Tier-C task via `scripts/dispatch/dispatch-interface.sh` on a fixture milestone; after dispatch returns, the milestone's `execution-log.jsonl` contains exactly one new `payload_breakdown` record (record_type, unitId, payload_chars, payload_tokens_estimate, section_tokens, model, source: \"estimate\", timestamp) and exactly one new `dispatch_usage` record (input_tokens_estimate, output_tokens_estimate, estimated_cost_usd, pricing_version, source: \"estimate\"); when a task summary is then written via `scripts/knowledge/write-summary.sh task ...`, exactly one `unit_close` record is appended carrying both a cost block (estimated_cost_usd, pricing_version) and a quality block (verification_pass_rate, deviation_count, retry_count); when `.orchestrator/config/pricing.yml` is renamed away mid-run, the next dispatch still emits a record with `estimated_cost_usd: null` + `pricing_warning` and exits 0; `bash scripts/verify/m019-p01-phase-suite.sh` exits 0; `bash scripts/verify/m019-p01-no-pre-p00-emission.sh` confirms no emitter record landed in any post-M011 milestone log before P00 went green."
  - Risk: medium
  - Depends: P00
  - Boundary Map:
    - Produces: `scripts/lib/pricing.sh` (new — resolves model rates from `.orchestrator/config/pricing.yml`, honors `ORCH_PRICING_FILE` env override, returns `last_updated` age, returns null + warning when missing/stale >90 days), `scripts/dispatch/build-context.sh` (modified — appends a `payload_breakdown` JSONL record to `.orchestrator/milestones/<Mxxx>/execution-log.jsonl` after payload assembly, outside the payload; computes `payload_chars`, `payload_tokens_estimate` via char-quartile heuristic per AD-1, `section_tokens` per `context-recipe.yaml` section, includes `token_estimate_method: "char-quartile"`, `source: "estimate"`), `scripts/dispatch/dispatch-interface.sh` (modified — appends a `dispatch_usage` JSONL record after backend invocation with `input_tokens_estimate`, `output_tokens_estimate` placeholder, `estimated_cost_usd` derived via `scripts/lib/pricing.sh`, `pricing_version` pointing to `.orchestrator/config/pricing.yml`, `source: "estimate"`; degrades to null+pricing_warning per C4), `scripts/knowledge/write-summary.sh` (modified — appends one `unit_close` record per `task | phase | milestone` granularity with cost block summed from the task's `payload_breakdown` + `dispatch_usage` records and quality block computed from existing `attempt`/`outcome` fields per AD-3; rejects records missing either block via the schema validator), `scripts/verify/m019-schema.sh` (new — JSONL schema validator: enforces record_type enum, source enum {"estimate","runtime"}, granularity enum {"task","phase","milestone"} on unit_close, mandatory cost+quality on unit_close, additivity check that pre-M019 records still validate), `scripts/verify/m019-p01-emitter-presence.sh` (new — fixture-driven: asserts payload_breakdown emitted after build-context, dispatch_usage emitted after dispatch, unit_close emitted at each of the 3 boundaries), `scripts/verify/m019-p01-pricing-degradation.sh` (new — renames pricing.yml away, runs a fixture dispatch, asserts record written with estimated_cost_usd: null + pricing_warning, exit 0), `scripts/verify/m019-p01-source-enum.sh` (new — asserts schema accepts "estimate" and "runtime", rejects others), `scripts/verify/m019-p01-zero-token-growth.sh` (new — diff payload size before/after instrumentation: must be byte-identical), `scripts/verify/m019-p01-fixture-rollup.sh` (new — verification asset: parses ~10 fixture records and prints granularity-keyed totals, demonstrating SC-7 greppability without shipping a `cost` command), `scripts/verify/m019-p01-additive-compat.sh` (new — replays pre-M019 `execution-log.jsonl` fixture against modified consumers, asserts no break; SC-10), `scripts/verify/m019-p01-no-pre-p00-emission.sh` (new — asserts no emitter record exists in any post-M011 milestone's execution-log.jsonl with timestamp earlier than P00 SUMMARY's `completed_at`; SC-12 ordering enforcement), `scripts/verify/m019-p01-bash32-compat.sh` (new — bash-3.2 scan for all P01-touched/created `.sh`), `scripts/verify/m019-p01-phase-suite.sh` (new — orchestrates the eight P01 gates above)
    - Consumes: P00-adapted `scripts/dispatch/build-context.sh` (emitter hooks into the post-payload-assembly point established by P00/L1+L2), P00 `<dispatch-volatile>` marker convention (section_tokens accounting respects the boundary), P00 `scripts/lib/pricing.sh` not yet existing → P01 ships it; `.orchestrator/config/pricing.yml` from P00, P00 verify suite (m019-p00-phase-suite.sh) green status used by P01's no-pre-p00-emission gate as the epoch reference, existing `scripts/knowledge/write-summary.sh` task/phase/milestone signatures (additive change only — pre-M019 callers must keep working), existing `.orchestrator/milestones/*/execution-log.jsonl` schema (additive: new `record_type` field; pre-existing `unitId`/`duration_s`/`outcome`/`verification_result`/`attempt` semantics preserved; SC-10), existing `scripts/state/derive-phase.sh` and any other current execution-log consumers (must continue to function unchanged)

## Cross-Cutting Concerns

- **Goodhart pairing (cost + quality together)** — P01. Every `unit_close` record carries both a cost block and a quality block. Schema validator (`scripts/verify/m019-schema.sh`) rejects records missing either. Pairing happens at emission time, not at rollup time, so Tier 2 cannot accidentally optimize cost in isolation.

- **Zero-token instrumentation** — P00, P01. P01 emitter writes the `payload_breakdown` *after* payload assembly, outside the agent's context. P00 must not introduce instrumentation prose into the payload itself. SC-6 and `scripts/verify/m019-p01-zero-token-growth.sh` enforce this — payload byte size before and after instrumentation must be identical.

- **Schema additivity for Tier 2/3 forward compat** — P01. The `source: "estimate" | "runtime"` enum, `granularity: "task" | "phase" | "milestone"` enum, and `record_type` discriminator are reserved at Tier 1 so Tier 3 backend-actuals adapters land additively. No Tier 1 record-writer needs modification when Tier 3 ships its `adapters/backend/*/report-usage.sh` emitters. SC-4 + `scripts/verify/m019-p01-source-enum.sh` enforce.

- **Pre-M019 log-consumer additivity** — P01. New fields are additive only; existing fields (`duration_s`, `outcome`, `verification_result`, `attempt`, `unitId`) keep current semantics + positions. `scripts/state/derive-phase.sh` and other current consumers must not break against either pre-M019 or M019-emitted logs. SC-10 + `scripts/verify/m019-p01-additive-compat.sh`.

- **P00→P01 hard ordering (SC-12)** — P00, P01. No Tier 1 emitter record may be written to any post-M011 milestone log until P00's verify suite is green. Enforced by `scripts/verify/m019-p01-no-pre-p00-emission.sh` (P01 gate that fails if any emitter record predates P00 SUMMARY's `completed_at`). Backfilling adapted records after the fact is impossible — the dispatches already ran.

- **Bash 3.2 compatibility (Constitution VIII)** — P00, P01. No `declare -A`, `mapfile`, `${var^^}`, `<(...)`, `&>`. Each phase ships a `m019-<phase>-bash32-compat.sh` gate scanning all touched/created `.sh` files. P00 establishes the pattern for P01.

- **No compound bash in agent-facing content (Constitution XV + M016/M021 anti-pattern linter)** — P00. P00 template adaptations are agent-facing — must comply with M021 hook's 10-pattern matrix. P01 emitter code is verification-script-internal (MEM004 carve-out — pipes/`$()`/awk allowed). SC-9 + the existing `scripts/verify/anti-pattern-lint.sh` (must continue to exit 0).

- **P00 strictly dispatch-facing** — P00. P00 touches templates whose output renders into payloads, recipe ordering, and intensity-gate prompt nudges. No drift into `commands/`, `references/`, `docs/`, `README.md`, or constitution edits. Boundary map enforces by listing only dispatch-facing produces.

- **No regression of existing behavior (SC-13)** — P00. All `tests/test-s01.sh`..`tests/test-s07.sh` suites, the M021 P04 phase-suite, and `scripts/verify/anti-pattern-lint.sh` must continue to PASS unchanged against adapted templates. `scripts/verify/m019-p00-no-regression.sh` wraps the full suite as P00's load-bearing regression gate.

- **AD-7 settings.json additive-merge fix** — P00. The `evaluate-preflight.sh` overwrite that strips the M021 hook + widened allow-list on every re-run is fixed inside P00 via a `// generated:start` / `// generated:end` sentinel pair in `scripts/lifecycle/write-permissions.sh`. Without this fix, any future `evaluate` re-run breaks zero-prompt baseline; with it, re-runs are idempotent. Verified by `scripts/verify/m019-p00-evaluate-preflight-additivity.sh`.

- **Scope discipline against Tier 2/3 creep (D009)** — P00, P01. Non-Goals list is long: no `orchestrator:cost`, no rollup script, no efficiency footer, no anomaly checks, no backend-actuals adapters, no UI. Both phase boundary maps must produce zero items in those categories; verification rejects any drift.

## Dependency Graph

```
P00 ──→ P01
```

Strict linear ordering. SC-12 makes the P00→P01 edge a hard gate, not a soft preference.

## Execution Order

1. **P00 — Opus 4.7 Baseline Adaptation** (no dependencies, executes first). High risk because adaptation cuts across dispatch + autonomy + templates and the regression-safety constraint (SC-13) is unforgiving — any test suite fail on adapted templates blocks the milestone.
2. **P01 — Tier 1 Emitter** (depends on P00; cannot start until `scripts/verify/m019-p00-phase-suite.sh` is green). Medium risk because the schema decisions are well-specified and the emitter surface area is narrow (3 records × 3 boundary points), but the pre-M019 consumer additivity constraint (SC-10) and zero-token growth (SC-6) leave little room for error.

No parallelization possible — only two phases, strictly sequential per the dependency graph.

## Validation

- **No conflicting producers**: PASS. Only two phases, with non-overlapping produces sets. Both phases modify `scripts/dispatch/build-context.sh`, but at different points: P00 changes payload structure (template-driven), P01 adds an emit-record call after payload assembly. The seam is documented in P01's Consumes ("hooks into the post-payload-assembly point established by P00/L1+L2"). Same file, distinct concerns, ordered modifications — no conflict.
- **All consumed items have producers**: PASS. P01's consumes from P00 (adapted build-context.sh, `<dispatch-volatile>` marker, pricing.yml, P00 SUMMARY epoch) all match P00's produces. P00's consumes are all pre-existing repo state (build-context.sh pre-adaptation, intensity-gate.sh, templates/, M021 artifacts, articles synthesis) — no upstream phase needed. P01's consumes from existing repo state (write-summary.sh signatures, execution-log.jsonl schema, derive-phase.sh) are all real.
- **DAG is acyclic**: PASS. P00 → P01 is a single edge. No cycle possible.
- **Demo sentence coverage**: PASS. Both demos are concrete and testable: P00's demo names the exact payload assertions + the AD-7 additivity check; P01's demo names the three record types, the pricing degradation path, both phase-suite gates, and the no-pre-p00-emission ordering check. Each is observable end-to-end on a fixture.
