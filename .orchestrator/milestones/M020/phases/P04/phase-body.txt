P04 delivers FR-4 / SC-3 — the Review-Queue surface in `orchestrator:status`. Four tasks landed in dependency order T01 → T02 → T03 → T04.

**T01 (compute-staleness-review-queue)** — `scripts/knowledge/compute-staleness.sh` extended in place with `--review-queue` and `--knowledge-root <path>` flags. New mode short-circuits at the top of the argument parser; legacy `--archive-below` / `--min-hits` / `--dry-run` / no-flag invocations preserved byte-equivalent (CON-4). Walks `<knowledge-root>/**/MEM*.md`, filters via `fm_read_status` to `status: candidate`, groups via P05's `cluster_compute`, emits one structured `cluster_id=<C8hex> topic=<t> count=<N> oldest_age=<d> stale=<true|false>` line per cluster (or the literal `EMPTY`). Staleness threshold resolves: default 14 (OQ-1) → preferences override → malformed warns + falls back. Topic field whitespace-collapsed; age uses `created_at:` then `last_verified:` fallback.

**T02 (status-review-queue-section)** — `scripts/orchestrator/status.sh` extended in place with a `Review Queue:` section emitted after the per-milestone enumeration. Subprocess-invokes T01's `--review-queue` and renders header + indented `cluster=<C8hex> topic=<t> count=<N> oldest_age=<d>d[ (stale)]` lines, or `Review Queue: empty`, or `Review Queue: unavailable` on helper failure. Pre-P04 prefix preserved byte-equivalent (CON-4). FR-8/CON-1 read-only.

**T03 (truth-verifiers)** — Authored the one remaining per-truth verifier (`m020-p04-compute-staleness-stale-flag.sh`). Five other per-truth verifiers had already shipped under T01/T02 deviations (auto-loop `--step=V` requires verifiers to exist at task close, with no cross-task ordering awareness). Six per-truth verifiers under `scripts/verify/m020-p04-*.sh` cover stdout shape, stale-flag flip, status.sh rendering, the `(stale)` marker, the read-only invariant, and prefix preservation.

**T04 (integration-test)** — `tests/test-status-review-queue.sh` exercises SC-3 end-to-end across three scenarios (five-candidate / two-cluster, empty queue, stale-flag) with FR-8/CON-1 read-only invariant assertions. 9 PASS / 0 FAIL. Adopted shadow-repo + wrapper-helper pattern (T03 carry-forward) so the live `cluster.sh` + `jaccard.sh` + `frontmatter.sh` + `staleness.sh` pipeline runs end-to-end against tempdir fixtures without touching live `knowledge/`. Distinct-vocabulary fixture pattern carried forward from P05/T04.

**Verification result.** All task verifiers green; phase verify (six per-truth + integration test) green; external-mods PASS; roadmap-sync OK. Patterns established this phase: (a) plan-deviation invariant — any task whose plan names a verifier in its Verification section MUST author it inline, since auto-loop `--step=V` runs them at task close with no cross-task awareness (T01/T02 carried forward to P04 and beyond); (b) failure-tolerant subprocess invocation pattern (tempdir + RETURN trap + rc capture + four guard branches → graceful `unavailable` fallback); (c) shadow-repo + wrapper-helper pattern for integration tests that need to pin a fixture knowledge_root through a script that doesn't propagate `--knowledge-root` to its helpers; (d) two-pass parser pattern (validate every line against case-glob shape + non-negative-integer count → render) with marker injection via empty-or-fixed string variable.

**Affects.** No downstream phase consumes P04's output directly inside M020 (status surface is operator-facing). No upstream phase invalidation; pre-P04 status output preserved byte-equivalent.
