---
schema_version: "1.0"
type: task-summary
id: "T01"
parent: "P06"
milestone: "M020"
provides:
  - "scripts/knowledge/lib/preferences.sh sourceable read-only helper exposing pref_resolve <key> for the FR-6 / US-5 preferences cascade. Closed-enum key vocabulary {default_state_filter, similarity_threshold, staleness_threshold, preferred_cluster_size, operator_identifier} with built-in defaults (graduated, 0.7, 14, 8, unknown@local). project>user>built-in-default precedence per-key (THREAT-007 disposition). Per-key validators: default_state_filter closed enum {candidate,graduated,archived}; similarity_threshold float [0.0,1.0]; staleness_threshold int [1,365]; preferred_cluster_size int [1,50]; operator_identifier non-empty string with no embedded newline or surrounding whitespace. Malformed values fall back to next source per the precedence chain with a single-line stderr WARN diagnostic naming the key + offending value + selected fallback; the preferences file is NEVER mutated. Unknown keys emit FAIL: pref_resolve: unknown key '<key>' on stderr + non-zero exit + empty stdout. Path resolution honors PROJECT_ROOT and HOME env vars for fixture isolation (P01-P05 verifier convention). Double-source guard (_PREFERENCES_HELPER_SOURCED). AD-5 grep+sed YAML parsing (no yq dependency). Bash 3.2 safe (MEM001). Four contract verifiers under scripts/verify/ covering function exposure + 5 default values (helper-contract), project>user>default precedence + per-key partial-overlap (precedence), 5 malformed cases with md5 file-immutability snapshot (malformed-fallback), and unknown-key + missing-arg + 5 known-key sanity (key-vocabulary)."
requires:
  - "from:P01 what:scripts/knowledge/lib/frontmatter.sh double-source-guard convention pattern (referenced for the [ -n SOURCED ] && return 0; SOURCED=1 idiom — not actually sourced); from:P02 what:tempdir + HOME + PROJECT_ROOT fixture-isolation pattern from tests/test-knowledge-query.sh (reused verbatim across all four T01 verifiers)"
affects:
  - "P06/T02,P06/T03,P06/T04"
key_files:
  - "scripts/knowledge/lib/preferences.sh,scripts/verify/m020-p06-preferences-helper-contract.sh,scripts/verify/m020-p06-preferences-precedence.sh,scripts/verify/m020-p06-preferences-malformed-fallback.sh,scripts/verify/m020-p06-preferences-key-vocabulary.sh"
key_decisions:
  - "none-new,AD-5,FR-6,FR-8,CON-1,THREAT-007"
patterns_established:
  - "Double-source-guard convention extended to preferences.sh (matches P01 frontmatter.sh pattern). Per-key validators as a single case statement keep type+range constraints colocated and bash-3.2-safe (no associative arrays). Per-key bash-3.2 default lookup uses indirect parameter expansion (bash-3.0+ feature) — local default_var=_PREF_DEFAULT_KEY then echo via printf bang-default_var (MEM001-compatible). YAML scalar reader uses grep -E header anchor then sed strips trailing whitespace and surrounding single/double quotes — AD-5-compliant grep+sed strategy with no yq dependency. _pref_try_source isolates the read+validate+diagnose flow per source so the public pref_resolve reads as three sequential precedence steps. WARN stderr diagnostic format: WARN: pref_resolve: malformed value for KEY in FILE: RAW — falling back to LABEL — names key + file + raw + next-source-or-default in one line for grep-able operator runbooks. Verifier convention: bash -c source-LIB-then-pref_resolve-KEY for fixture isolation; HOME and PROJECT_ROOT env-var prefix on the bash -c call ensures the resolver sees the tempdir paths. md5 file-immutability snapshot uses portable hash_file helper (md5 -q on macOS, md5sum on linux) — same pattern as P02 side-effect-free verifier. Operator newline check uses case-printf-newline-subshell pattern instead of dollar-quoted-ANSI-C-newline for bash 3.2 portability."
drill_down_paths:
  - ".orchestrator/milestones/M020/phases/P06/tasks/T01-preferences-helper-PAYLOAD.md"
duration: "30m"
verification_result: "pass"
completed_at: "2026-04-25T16:28:58Z"
---

Created scripts/knowledge/lib/preferences.sh implementing the FR-6 / US-5 preferences resolution helper. Single public surface: pref_resolve <key> echoes the effective scalar on stdout, applying project>user>built-in-default precedence per-key (THREAT-007 disposition). Closed-enum key vocabulary: default_state_filter, similarity_threshold, staleness_threshold, preferred_cluster_size, operator_identifier. Built-in defaults: graduated, 0.7, 14, 8, unknown@local. Implementation follows the payload reference verbatim except for one bash-3.2 portability adjustment: the operator_identifier newline check uses case '$(printf newline)'*) instead of dollar-quoted ANSI-C $'\n' (which is bash 4+). YAML parsing uses grep + sed only per AD-5 — no yq dependency. Path resolution honors PROJECT_ROOT and HOME env vars for fixture isolation. Helper is read-only; no file writes anywhere. Double-source-guarded per the P03/P05 convention.

Four contract verifiers shipped under scripts/verify/, all PASS:
- m020-p06-preferences-helper-contract.sh (7/7): file exists + sourceable, pref_resolve is a function, all 5 default values returned with empty fixture (HOME and PROJECT_ROOT pointing at empty tempdirs).
- m020-p06-preferences-precedence.sh (14/14): project>user>default cascade across similarity_threshold (project=0.6 user=0.8 default=0.7), default_state_filter (project=candidate user=graduated default=graduated), staleness_threshold (project=7 user=21 default=14), and the THREAT-007 partial-overlap test (project declares only similarity_threshold=0.5, user declares only staleness_threshold=30 — each key resolves independently from its winning source, others fall through to default).
- m020-p06-preferences-malformed-fallback.sh (18/18): 5 malformed cases (similarity_threshold=not-a-number, similarity_threshold=1.5 out-of-range, default_state_filter=zombie outside enum, staleness_threshold=-1, project malformed + user valid → falls through to user). Each case asserts stdout=expected default, stderr matches WARN: pref_resolve: malformed value for 'KEY' pattern, exit code = 0, AND md5 of the preferences file is unchanged before and after the call (CON-1 / FR-8 read-only invariant). Case 5 also asserts exactly one WARN line (project only) when fallthrough succeeds at user.
- m020-p06-preferences-key-vocabulary.sh (13/13): unknown key (some_unknown_key) → empty stdout + FAIL: pref_resolve: unknown key 'some_unknown_key' stderr + non-zero exit. No-arg call → empty stdout + 'missing key argument' stderr + non-zero exit. Sanity: each of 5 known keys exits 0. Edge cases: trailing-space key rejected, uppercase key rejected (closed-enum is case-sensitive).

Auto-loop verify: bash scripts/lifecycle/auto-loop.sh .orchestrator/milestones/M020 --step=V --phase=P06 --task=T01-preferences-helper writes AUTO:VERIFY_PASS phase=P06 task=T01-preferences-helper checks_passed=4 to .orchestrator/milestones/M020/verify-result.txt.

CON-1 / FR-8 read-only invariant: helper itself never writes; verifier malformed-fallback enforces with md5 snapshot before/after each call. CON-4 surgical precision: T01 creates new files only (1 helper + 4 verifiers) — no in-place edits to other M020 files. AD-19 single-script-invocation shape: each verifier is a single bash <script> command with no compound chains in the orchestrator's outer Bash tool calls. MEM001 bash 3.2 compliance: no declare -A; per-key vocabulary + validators implemented as case statements; default lookup uses indirect parameter expansion (bash 3.0+, MEM001-safe). MEM002 test conventions: pass()/fail() with parallel scalars; tempdir + trap cleanup; HOME and PROJECT_ROOT tempdir fixture isolation — no live ~/.orchestrator/ or repo-root .orchestrator/ access in any verifier.

Downstream consumers (T02 query.sh integration, T03 consolidate-artifacts.sh integration, T04 docs + integration test) inherit the public pref_resolve <key> contract verbatim and the four contract verifiers as regression coverage.
