---
schema_version: "1.0"
type: phase-summary
id: "P01"
parent: "M025"
milestone: "M025"
provides:
  - "scripts/dispatch/adapters/runtime/claude-code.sh --hook-config emits valid CC hooks schema (Stop + PreToolUse/Bash); packaging/install/install-claude-code.sh merges into existing ~/.claude/settings.json via scripts/util/settings-merge.sh with inline _orchestrator_managed tagging + idempotency guard + --uninstall flag; tests/fixtures/m025-p01/{gsd-baseline,expected-post-install,expected-post-uninstall} pinned fixture tree; 10 verification gates covering hook-schema, merge-preservation, coexistence, idempotency, uninstall-reversibility, runtime-scope-guard (FR-9/CON-5 negative-grep + pinned-sha on codex/cursor files), bash32-compat (42 assertions across P01 .sh files), docs, knowledge-entries, recent-changes; references/installation.md Uninstall section; references/hooks.md six-event mapping table (2 mapped + 4 TODO(M025+) deferrals); CHANGELOG.md [0.9.1] entry; knowledge/lessons/MEM026.md (M013/P04/T04 regression lesson) + knowledge/patterns/MEM027.md (merge-not-overwrite user-scope config pattern) + rebuilt KNOWLEDGE-INDEX.md + projected wiki copies"
requires:
  - "from:M013/P04/T04 what:commit d33b8a7 as read-only regression context; from:M014/P01 what:scripts/util/dual-write-runtime-md.sh dual-write helper; from:M012 what:scripts/knowledge/rebuild-index.sh + wiki include-markdown projection; from:M013/P04 what:FR-12-v1-negative-grep-guard pattern; from:M013/P04/T06 what:bash32-compat scanner with self-exclude case-branch + comment-discipline synonyms; from:M008/P05 what:runtime adapter --probe/--register/--hook-config interface"
affects:
  - "scripts/dispatch/adapters/runtime/claude-code.sh (modify: rewrite --hook-config body + HOME guard addition + 4 TODO(M025+) deferral markers); packaging/install/install-claude-code.sh (modify: replace overwrite with settings-merge call + --uninstall flag + --force passthrough); scripts/util/settings-merge.sh (create: 333-line merge+uninstall helper with python3 JSON handling); tests/fixtures/m025-p01/gsd-baseline/settings.json (create); tests/fixtures/m025-p01/expected-post-install.json (create: captured from real merge run); tests/fixtures/m025-p01/expected-post-uninstall.sha256 (create: pinned from round-trip); scripts/verify/m025-p01-{hook-schema,merge-preservation,coexistence,idempotency,uninstall-reversibility,runtime-scope-guard,bash32-compat,docs,knowledge-entries,recent-changes,phase-suite}.sh (create); references/installation.md (modify: add Uninstall section); references/hooks.md (modify: add Claude Code Event Mapping table); CHANGELOG.md (modify: add [0.9.1] entry); knowledge/lessons/MEM026.md + knowledge/patterns/MEM027.md (create); KNOWLEDGE-INDEX.md (modify: rebuild); CLAUDE.md + AGENTS.md (modify: Recent Changes region dual-write); wiki/docs/knowledge/lessons/MEM026.md + wiki/docs/knowledge/patterns/MEM027.md (create via M012 projection)"
key_files:
  - "scripts/dispatch/adapters/runtime/claude-code.sh; packaging/install/install-claude-code.sh; scripts/util/settings-merge.sh; tests/fixtures/m025-p01/; scripts/verify/m025-p01-phase-suite.sh; references/installation.md; references/hooks.md; CHANGELOG.md; knowledge/lessons/MEM026.md; knowledge/patterns/MEM027.md"
key_decisions:
  - "Event-mapping policy (planner-locked #Q-1): post_verify -> Stop (terminal CC event; command orchestrator-post-verify); before_commit -> PreToolUse + matcher Bash + command-regex git-commit applied in wrapper (not matcher); four events (before_tasks, after_tasks, before_implement, after_implement) explicitly deferred as no-CC-equivalent with TODO(M025+) markers in adapter source + references/hooks.md. Tagging shape (planner-locked #Q-2): inline _orchestrator_managed: true on each inserted hook leaf (not sidecar manifest) — self-contained, uninstall is a one-filter operation, CC lenient about unknown keys. python3-for-awk-fallback deviation: JSON deep-merge + canonicalization in awk for bash 3.2 would dwarf T02; python3 already a de-facto gate baseline per m013-p04-* precedent. Canonicalization-vs-byte-identity FR-8 nuance: merge helper canonicalizes output via python3 json.dumps(indent=2, sort_keys=True) so second install re-serializes to same bytes (idempotency invariant); pre-install-vs-post-uninstall is structurally equivalent to baseline but not byte-identical; gate pins post-uninstall sha256 from round-trip + asserts structural equivalence. New-file path also routes through the canonical serializer so run-1 output matches run-2 output. FR-9 / CON-5 runtime-scope: codex + cursor installers + adapters byte-identical per pinned-sha gate + negative-grep for M025 markers. bash32-compat scanner strips full-line comments before forbidden-token grep so T01/T02 header comments naming mapfile/&> do not trigger (live-code scan unaffected)."
patterns_established:
  - "inline-managed-tag-on-user-scope-config: insert _orchestrator_managed: true on every object the orchestrator writes into a jointly-owned config file (~/.claude/settings.json) so --uninstall can strip only those entries without touching user-authored siblings; python3-as-bash32-json-subprocess: python3 heredoc via env-var param passing is the jq-fallback convention (pattern generalized from m013-p04-* gate precedent); canonicalize-at-write-for-idempotency: route new-file writes through the same serializer used for merge-writes so run-1 output byte-equals run-2 output (avoids first-run reparse-reserialize drift); cascade-cleanup-on-uninstall: remove tagged leaves, then empty wrappers, then empty event keys, then empty hooks key — preserves the JSON shape the target had before install; pinned-sha-plus-structural-baseline-dual-assert: round-trip reversibility gate pins the canonical post-uninstall bytes AND asserts structural equivalence to the pre-install baseline (byte-identity to baseline is impossible under canonicalization; intent-identity is preserved); runtime-scope-negative-grep-plus-pinned-sha: FR-9 enforcement combines pinned-sha on sibling-runtime files with negative-grep for M025 markers — cheaper than content-diff, self-repairs if those files legitimately gain content under a different marker; comment-line-exclusion-in-forbidden-token-scanner: bash32-compat gate strips ^[[:space:]]*# lines before grepping so header comments naming forbidden tokens do not trigger (applies cleanly to files whose mode comment documents the pattern itself)"
drill_down_paths:
  - ".orchestrator/milestones/M025/phases/P01/tasks/"
duration: "single-session"
verification_result: "pass"
completed_at: "2026-04-23"
observability_surfaces:
  - "no new JSONL emitters (M019 Tier 1 shape unchanged); CHANGELOG [0.9.1] + KNOWLEDGE-INDEX MEM026/MEM027 surfaces"
---

# P01 — Installer coexistence: hook-config schema + merge-not-overwrite + reversibility

P01 remediates the M013/P04/T04 regression in one linear 4-task sweep. T01 rewrites the Claude Code runtime adapter's `--hook-config` emitter to produce real CC schema. T02 replaces the unconditional `printf > settings.json` overwrite with a `scripts/util/settings-merge.sh` deep-merge helper that preserves user-authored siblings and supports `--uninstall` via inline `_orchestrator_managed: true` tagging. T03 pins the fixture tree and the full gate suite. T04 lands documentation, knowledge entries, and the dual-write to Recent Changes. Phase-suite closes green: **pass=10 fail=0**. Total commits: `4bea909 → fe2a4d8 → f2a9585 → 1824b28` (preceded by scaffolding commit `17a2592`).

## What was built

**T01 — Hook-config valid CC schema + event mapping** (`4bea909`). `scripts/dispatch/adapters/runtime/claude-code.sh` `--hook-config` rewritten: single-quoted heredoc emits `{"hooks": {"Stop": [...], "PreToolUse": [{"matcher": "Bash", ...}]}}` with every leaf carrying `_orchestrator_managed: true`. Four orchestrator events (`before_tasks`, `after_tasks`, `before_implement`, `after_implement`) documented as deferrals with `TODO(M025+)` markers in an inline comment block. HOME guard added for adapter convention parity with `--register`. `--probe` and `--register` modes untouched byte-identically. Gate: `m025-p01-hook-schema.sh` (8 assertions; python3 JSON validation + 4 TODO-marker grep).

**T02 — Merge-not-overwrite installer + --uninstall flag** (`fe2a4d8`). `scripts/util/settings-merge.sh` (333 lines) implements `merge` and `uninstall` subcommands with python3 as the JSON engine (jq-optional); temp-file-then-rename throughout; malformed target JSON → exit 4 with no write. Merge algorithm preserves every non-orchestrator top-level key byte-identically, deep-merges `hooks.<event>[]` arrays, and skips orchestrator entries whose command already exists under a managed-tagged object (idempotency guard; `--force` bypasses). Uninstall cascades: empty wrapper → drop; empty event array → drop key; empty `hooks` → drop. `install-claude-code.sh` replaces its overwrite block with a call to the helper; adds `--uninstall` flag short-circuit; `--force` passes through as idempotency-guard bypass. Gates: `m025-p01-merge-preservation.sh` (2), `m025-p01-idempotency.sh` (3). T01 regression unchanged.

**T03 — Fixture tree + gate suite + phase-suite orchestrator** (`f2a9585`). `tests/fixtures/m025-p01/gsd-baseline/settings.json` (13 lines; `$schema` + `statusLine` + `hooks.SessionStart` + `hooks.PostToolUse` + `permissions`). `expected-post-install.json` captured from a real merge run (not hand-authored — canonicalization via python3 `json.dumps(indent=2, sort_keys=True)` would defeat manual capture). `expected-post-uninstall.sha256` pinned from a round-trip run. Seven new gates: `coexistence` (3), `uninstall-reversibility` (5, dual-assert pinned-sha + structural baseline equivalence), `runtime-scope-guard` (8, pinned-sha on 4 sibling-runtime files + negative-grep for M025 markers), `bash32-compat` (42 assertions across 11 P01 `.sh` files, comment-line stripping before forbidden-token grep), `docs` (6, awaits T04), `knowledge-entries` (3, awaits T04), `recent-changes` (2, passes immediately via specify-time fragment match). Phase-suite orchestrator at `m025-p01-phase-suite.sh` invokes all 10 gates; initially reports `pass=8 fail=2` (docs + knowledge-entries await T04).

**T04 — Docs + knowledge entries + dual-write** (`1824b28`). `references/installation.md` gains `## Uninstall` section naming `--uninstall` + manual recipe. `references/hooks.md` gains `## Claude Code Event Mapping` six-row table (2 mapped + 4 deferred). `CHANGELOG.md` gains `## [0.9.1] — 2026-04-23` entry under Fixed/Added/Changed per Keep-a-Changelog 1.1.0. `knowledge/lessons/MEM026.md` — "M013/P04/T04 hook-config regression" (names commit `d33b8a7` + explains why P04 gates missed the regression + lesson). `knowledge/patterns/MEM027.md` — "merge-not-overwrite user-scope config" (problem + pattern + gate shape). `rebuild-index.sh` refreshes `KNOWLEDGE-INDEX.md`; M012 wiki projections land under `wiki/docs/knowledge/`. Dual-write to `CLAUDE.md` + `AGENTS.md` Recent Changes region (idempotent; byte-identical second run). CLAUDE.md Project Status adds M025; Forward Roadmap sheds the stale "M013 P04 is the only phase left in M013" sentence. Phase-suite closes: **pass=10 fail=0**.

## Verification results

All 10 P01 gates PASS individually. Phase-suite aggregate: `SUMMARY: m025-p01-phase-suite.sh pass=10 fail=0`. T03's `runtime-scope-guard` pins sha256 of `install-codex.sh`, `install-cursor.sh`, `scripts/dispatch/adapters/runtime/codex.sh`, and `scripts/dispatch/adapters/runtime/cursor.sh` captured at T03-write time — all four are byte-identical to their pre-P01 state. Negative-grep confirms no `_orchestrator_managed`, `settings-merge`, or `M025` token appears in any of those four files (FR-9 / CON-5 enforced).

## Decisions register updates

- **#Q-1 locked: event-mapping policy** — `post_verify` → `Stop` (terminal; command `orchestrator-post-verify`); `before_commit` → `PreToolUse` + matcher `Bash` (command-regex `git commit` lives in the wrapper, not the matcher, because CC's matcher is tool-name only); four orchestrator events (`before_tasks`, `after_tasks`, `before_implement`, `after_implement`) deferred with `TODO(M025+)` markers. Revisit if Claude Code gains task-start / task-end / implement-start / implement-end events.
- **#Q-2 locked: tagging shape** — inline `_orchestrator_managed: true` on each inserted hook leaf. No sidecar manifest. Rationale: self-contained (uninstall is a one-filter operation); CC lenient about unknown keys (no schema-validation rejection); sidecar drift was the larger pragmatic risk.
- **FR-8 byte-identity nuance** — the spec promised pre-install-vs-post-uninstall byte-identity. Canonicalization via python3 `json.dumps(indent=2, sort_keys=True)` in the merge helper means post-uninstall is *structurally* equivalent to baseline but *not* byte-identical. Gate pins the post-roundtrip sha256 AND asserts structural equivalence to baseline — preserves FR-8 intent ("uninstalling restores the file") without requiring a byte-identity that canonicalization makes impossible. Candidate for a DECISIONS.md D-entry if this pattern recurs.
- **python3-for-awk-fallback** — the spec's CON-2 committed to "awk/sed fallback" for jq-optional environments. Substituted python3 (already a de-facto baseline in `scripts/verify/m013-p04-*.sh` gates). Awk-based JSON deep-merge for bash 3.2 would dwarf the rest of T02. CON-2 updated in the pattern entry (MEM027) as "jq-optional with python3 fallback".

## Observability surfaces

No new JSONL emitters. M019 Tier 1 shape untouched. Surfaces added: `CHANGELOG.md [0.9.1]` entry; `KNOWLEDGE-INDEX.md` gains MEM026 + MEM027 rows; `wiki/docs/knowledge/{lessons,patterns}/MEM02{6,7}.md` projections.

## Remaining known gaps (out of P01 scope)

1. **Config-removal marker** — `install-claude-code.sh --uninstall` reports `config-removed=0` always because nothing currently emits the `_orchestrator_managed` marker into `.orchestrator/config.yml`. Needs a follow-up: either write the marker at install time or relax the `--uninstall` contract to leave `config.yml` alone entirely. Low-urgency.
2. **Codex/Cursor parity** — FR-9 intentionally scopes to Claude Code. Codex and Cursor installers don't write `$HOME/.claude/settings.json`, so coexistence isn't a live issue there today. Parity lands if/when those runtimes gain user-scope config paths worth merging (M010 fast-follow candidate).
3. **Spec path vs actual gate path** — spec says `tests/m025-p01-*.sh`; gates live at `scripts/verify/m025-p01-*.sh`. Functional-equivalent (phase-suite runs them), but the SC-1..SC-6 prose is cosmetically off. Not worth a spec amendment; future milestones should align on `scripts/verify/` from the start.
