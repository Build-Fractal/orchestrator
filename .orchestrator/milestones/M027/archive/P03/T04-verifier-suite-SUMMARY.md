---
schema_version: "1.0"
type: task-summary
id: "T04"
parent: "P03"
milestone: "M027"
provides:
  - "scripts/verify/m027-p03-suite.sh phase-suite orchestrator (mirrors m027-p02-suite.sh / m027-p01-suite.sh / m027-rollup-schema.sh shape: parallel-string GATES list, per-gate exit-code capture, RELAX-CANDIDATE forwarding, SUMMARY+PASS/FAIL summary, exit 0/1); 11 per-contract verifiers under scripts/verify/m027-p03-*.sh covering Truth #1..#11 (anomaly-shape, config-drift-shape, doctor-md-shape, doctor-byte-identity, suppression-matrix, run-doctor-integration, anomaly-latency, anomaly-goodhart-pairing, zero-llm-token, read-only, bash32-compat). T01/T02/T03 scoped prechecks deleted -- subsumed by canonical phase-level verifiers (third occurrence of M027 T04-subsumes-prechecks pattern after P00/P01, P02). One-line transitive reference to scripts/diagnostics/metrics-rollup.sh added inside commands/doctor.md '## Anomaly Detection' section to satisfy the phase-plan key-link assertion (helper transitively wraps the P00 rollup engine; the verifier-author-vs-helper-author boundary lesson from M027/P02/T04 carried forward)."
requires:
  - "from:P03/T01 what:scripts/diagnostics/check-anomalies.sh + 4 anomaly knobs in read-config.sh VALID_KEYS; from:P03/T02 what:scripts/diagnostics/check-config-drift.sh + commands/doctor.md integration + tests/fixtures/m027-p03/* fixtures; from:P03/T03 what:scripts/diagnostics/run-doctor.sh --config-check + --no-anomaly arg-parse + 2 advisory run_check invocations"
affects:
  - "M027 close"
key_files:
  - "scripts/verify/m027-p03-suite.sh,scripts/verify/m027-p03-anomaly-shape.sh,scripts/verify/m027-p03-config-drift-shape.sh,scripts/verify/m027-p03-doctor-md-shape.sh,scripts/verify/m027-p03-doctor-byte-identity.sh,scripts/verify/m027-p03-suppression-matrix.sh,scripts/verify/m027-p03-run-doctor-integration.sh,scripts/verify/m027-p03-anomaly-latency.sh,scripts/verify/m027-p03-anomaly-goodhart-pairing.sh,scripts/verify/m027-p03-zero-llm-token.sh,scripts/verify/m027-p03-read-only.sh,scripts/verify/m027-p03-bash32-compat.sh,commands/doctor.md"
key_decisions:
  - "AD-19,CON-1,CON-3,CON-6,CON-7,FR-9,FR-12,FR-21,SC-9,SC-11,SC-16,MEM004"
patterns_established:
  - "One-verifier-per-contract scaffolding mirrors P00/P01/P02 shape verbatim (PROJECT_ROOT via BASH_SOURCE; PASS/FAIL stdout/stderr; exit 0/1; MEM004 emitter-internal carve-out for pipes/awk/grep/diff while AD-19 binds only the Check: invocation surface). Phase-suite orchestrator at M027/P03 scale (11 gates) follows m027-p02-suite.sh shape verbatim -- parallel-string GATES list, per-gate exit-code capture (combined stdout+stderr), RELAX-CANDIDATE forwarding (suite greps each gate output for the structured annotation and forwards verbatim above the SUMMARY), single-script Check shape externally with internal carve-out for the for-loop, cheapest-static-first ordering (bash32-compat + zero-llm-token regex-only first; live-invocation gates last; latency last). Latency verifier inner-vs-outer split applied verbatim from P01/P02 -- hard-fail at 250ms inner against the largest existing milestone log (M013); outer wall-clock reported informationally with WARN: RELAX-CANDIDATE annotation when over-budget (forwarded by suite). Self-applying scanner pattern carried forward from P01/P02 -- split-literal token assembly (FORBID_A='declare''_'-A' etc.) keeps bash32-compat + zero-llm-token verifiers from matching their own source even when included in the scan-set; explicit FILES list (no globbing) keeps the scan-set deterministic and bounded. Markdown-aware bash32-compat scan: for *.md files in the scan set the verifier extracts only the contents of fenced bash/sh code blocks and applies the forbidden-construct regex against that subset, so prose discussion of bash syntax in commands/doctor.md (the literal '<<<' substring describing the orchestrator marker pattern) does not false-positive. Goodhart-pairing verifier ORCHESTRATOR_ROOT-isolated test environment -- mktemp tmp_root, copy fixture into tmp_root/.orchestrator/milestones/M999/execution-log.jsonl, invoke helper with env ORCHESTRATOR_ROOT=tmp_root/.orchestrator (the metrics-rollup.sh log-path resolution honors that env var); avoids polluting the real .orchestrator tree and keeps the fixture under tests/fixtures rather than .orchestrator. Read-only-with-WARN-skip-on-dirty-pre-run pattern carried forward from P01/T04 + P02/T04 -- captures git diff --quiet exit pre-run; if non-zero (working tree already dirty from KNOWLEDGE updates etc.), WARN + exit 0 rather than false-fail; pathspec-excludes .orchestrator/doctor-history.jsonl since run-doctor.sh appends to it as a pre-existing pre-T03 side-effect. Verifier-author-vs-helper-author boundary carry-forward (third occurrence after M027/P02/T04) -- the phase-plan key-link asserting commands/doctor.md -> metrics-rollup.sh was satisfied by adding a one-sentence transitive-reference note inside the existing ## Anomaly Detection section rather than re-architecting the helper or adding a new bullet under ## Referenced Scripts (which would have invalidated the doctor-byte-identity baseline fixture). T04-subsumes-prechecks pattern (third occurrence of M027 P00+P01+P02 lesson) -- T01/T02/T03 each shipped a m027-p03-t##-shape-precheck.sh single-script verifier referencing only that task's artifacts; T04 deletes them once the canonical phase-level verifiers ship and check-must-haves.sh is green; the prechecks were necessary scaffolding to satisfy each task plan's own Verification block at dispatch time without forward-referencing T04 deliverables."
drill_down_paths:
  - ".orchestrator/milestones/M027/phases/P03/tasks/T04-verifier-suite-PLAN.md"
duration: "45m"
verification_result: "pass"
completed_at: "2026-04-27T16:30:10Z"
---

T04 ships the M027/P03 phase-suite verifier set (11 per-contract gates + 1 orchestrator) and deletes the T01/T02/T03 scoped shape-prechecks now that the canonical phase-level verifiers subsume them. Mirrors the M027/P02/T04 shape verbatim with three small adaptations: (1) the bash32-compat verifier's markdown branch extracts fenced bash/sh code blocks before scanning so the pre-existing prose in commands/doctor.md describing orchestrator's '# <<< orchestrator:<region> <<<' marker syntax does not false-positive; (2) the goodhart-pairing verifier seeds an ORCHESTRATOR_ROOT-isolated tmp tree from the M999 anomaly-fixture so the helper's metrics-rollup.sh log-path resolution finds the fixture without polluting .orchestrator/; (3) commands/doctor.md gained a one-sentence transitive reference inside the ## Anomaly Detection section pointing at scripts/diagnostics/metrics-rollup.sh to satisfy the phase-plan key-link without disturbing the doctor-suppressed-baseline fixture (verifier-author-vs-helper-author boundary lesson from M027/P02 carried forward). Verification: bash scripts/verify/m027-p03-suite.sh exits 0 with PASS: m027-p03-suite.sh 11 gates; bash scripts/verify/check-must-haves.sh .orchestrator/milestones/M027/phases/P03 exits 0 (all 11 Truth Check invocations + 21 artifact assertions + 19 key-link assertions PASS). Latency verifier observed inner=101-113ms across runs, well within the 250ms hard-fail; the suite forwards the RELAX-CANDIDATE annotation per the structured outer-vs-inner split contract.
