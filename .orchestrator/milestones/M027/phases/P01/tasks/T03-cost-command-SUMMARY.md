---
schema_version: "1.0"
type: task-summary
id: "T03"
parent: "P01"
milestone: "M027"
provides:
  - "commands/cost.md — canonical user-facing definition for orchestrator:cost following MEM012 shape (frontmatter description, Title, Prerequisites/State Check, Core Workflow numbered sections, Output, Accuracy, Idempotency, Error Handling, Concurrent Safety, Referenced Scripts). Documents two surfaces: retrospective (no flags or scope flags) thin-wraps scripts/diagnostics/metrics-rollup.sh with a one-line orchestrator:cost — retrospective rollup header above engine output (SC-4); predictive (--estimate <description>) thin-wraps scripts/engine/cost-estimate.sh emitting the 3-row Quick/Standard/Full paired cost+quality table with recommended-tier marker and D027 accuracy trailer. Embeds the verbatim D027 disclaimer under ## Accuracy: Estimates use M019 char-quartile token approximation and pricing.yml rates; actual cost typically lands within +/-20%. Runtime-actuals calibration is Tier 3 (deferred). Documents mutually-exclusive flag rules (--estimate vs --milestone/--phase/--task/--granularity/--source/--since exit 2; --task + --granularity milestone|project exit 2; --estimate with empty description exit 2), graceful pricing degradation per FR-24/CON-5, read-only invariant per FR-12/CON-1, zero-LLM-token per FR-21/CON-6, concurrent-safety rationale per AD-3 copy-then-aggregate. References all four scripts: scripts/diagnostics/metrics-rollup.sh, scripts/engine/cost-estimate.sh, scripts/engine/intensity-recommend.sh, scripts/lib/pricing.sh, plus scripts/state/derive-phase.sh for active-milestone detection. 112 lines total. packaging/bundle/skills/orchestrator-cost.md — thin discovery-surface skill stub mirroring orchestrator-status.md shape: schema_version 1.0 / type skill / name orchestrator:cost / namespace orchestrator / runtime_compatibility [claude-code, codex, cursor] / command_file commands/cost.md, body redirects to canonical commands/cost.md. packaging/bundle/manifest.yml — orchestrator-cost.md inserted alphabetically in skills: list between orchestrator-consolidate.md and orchestrator-discuss.md; version unchanged at 0.3.0-dev (preserved per task plan step 7 convention)."
requires:
  - "from:scripts/diagnostics/metrics-rollup.sh what:retrospective rollup CLI accepting --granularity/--milestone/--phase/--task/--source/--log emitting paired cost+quality rows per FR-4/CON-4 (M027/P00 deliverable); from:scripts/engine/cost-estimate.sh what:predictive 3-row Quick/Standard/Full cost+quality table with header + recommended-tier marker + D027 accuracy trailer (M027/P01/T01 deliverable); from:scripts/engine/intensity-recommend.sh what:--format text|json + per-tier cost annotations (M027/P01/T02 deliverable); from:scripts/state/derive-phase.sh what:active-milestone detection for default-scope resolution; from:packaging/bundle/skills/orchestrator-status.md what:reference shape for thin discovery-surface skill stub; from:packaging/bundle/manifest.yml what:skills array to extend; from:commands/status.md what:reference shape for MEM012 canonical commands convention."
affects:
  - "P01/T04,P02,P03"
key_files:
  - "commands/cost.md,packaging/bundle/skills/orchestrator-cost.md,packaging/bundle/manifest.yml"
key_decisions:
  - "MEM012,D027,D026,FR-12,FR-20,FR-21,FR-24,CON-1,CON-4,CON-5,CON-6,CON-9,SC-4,SC-18,AD-3,AD-19"
patterns_established:
  - "Thin-wrapper command-document pattern over an existing CLI engine: the user-facing commands/cost.md adds exactly one piece of value beyond direct CLI invocation (a one-line header above retrospective rollup output for SC-4 audit-trail clarity) and otherwise streams engine stdout verbatim — keeping the command document as documentation rather than logic and preserving CON-7 (no inline shell anti-patterns in markdown). Verbatim-disclaimer load-bearing string: D027's accuracy disclaimer is treated as immutable copy under ## Accuracy and verified by grep -qF (literal substring match) — establishes the convention that downstream parser tooling can rely on the exact byte sequence. Mutually-exclusive flag-pair documentation pattern: command-document Error Handling section enumerates each forbidden pair (--estimate + scope flag, --task + --granularity milestone|project, --estimate with empty description) with the exact exit-code (2) and one-line diagnostic shape, giving the T04 verifier a static surface to assert against without re-running the command. Skill-stub-mirrors-existing-shape pattern: every new commands/*.md gets a parallel thin discovery skill at packaging/bundle/skills/ matching the YAML frontmatter shape of orchestrator-status.md (schema_version, type, name, namespace, description, runtime_compatibility, command_file) and a 4-line markdown body redirecting to the canonical command document — keeps bundle introspection and runtime-adapter auto-registration in lockstep without code changes to the adapters. Manifest update is alphabetical-insertion-only per existing convention; version pinned at task scope (bumped only at consolidate scope) per git log inspection."
drill_down_paths:
  - ".orchestrator/milestones/M027/phases/P01/tasks/T03-cost-command-PAYLOAD.md,.orchestrator/milestones/M027/phases/P01/tasks/T03-cost-command-PLAN.md"
duration: "20m"
verification_result: "pass"
completed_at: "2026-04-27T02:57:00Z"
---

Created commands/cost.md (112 lines) following MEM012 canonical command-file structure with the M027-specific ## Accuracy section inserted between Output and Idempotency. Documents both retrospective and predictive surfaces as thin wrappers over pre-existing engines (scripts/diagnostics/metrics-rollup.sh from M027/P00 and scripts/engine/cost-estimate.sh from M027/P01/T01). Retrospective surface adds exactly one byte-of-value beyond direct metrics-rollup.sh invocation — a one-line header (# orchestrator:cost — retrospective rollup) above the engine output per SC-4. Predictive surface streams cost-estimate.sh stdout verbatim (no header, no post-processing) preserving the 3-row Quick/Standard/Full paired cost+quality table with recommended-tier marker and the verbatim D027 trailer. The verbatim D027 disclaimer (Estimates use M019 char-quartile token approximation and pricing.yml rates; actual cost typically lands within +/-20%. Runtime-actuals calibration is Tier 3 (deferred).) is embedded under ## Accuracy and is verifiable via grep -qF literal-substring match per the T04 verifier shape (m027-p01-cost-command-shape.sh). Documents mutually-exclusive flag rules with explicit exit-code (2) shape, graceful pricing degradation per FR-24/CON-5 ((unavailable) cost cells, never aborts), graceful missing-execution-log behavior per CON-5 (empty rollup row, never aborts), zero-LLM-token discipline per FR-21/CON-6, read-only invariant per FR-12/CON-1, and concurrent-safety rationale per AD-3 copy-then-aggregate. References all four task-plan-required scripts (scripts/diagnostics/metrics-rollup.sh, scripts/engine/cost-estimate.sh, scripts/engine/intensity-recommend.sh, scripts/lib/pricing.sh) plus scripts/state/derive-phase.sh for active-milestone detection. Created packaging/bundle/skills/orchestrator-cost.md as a thin discovery-surface skill stub mirroring orchestrator-status.md verbatim (schema_version 1.0, type skill, name orchestrator:cost, namespace orchestrator, runtime_compatibility [claude-code, codex, cursor], command_file commands/cost.md), with a 4-line markdown body redirecting to the canonical command document. Updated packaging/bundle/manifest.yml inserting orchestrator-cost.md alphabetically between orchestrator-consolidate.md and orchestrator-discuss.md in the skills: list; version preserved at 0.3.0-dev per the task plan step 7 convention (version bumps only at consolidate scope per git log inspection). Verification: all six task-plan must-haves pass — (1) commands/cost.md exists at 112 lines, contains literal orchestrator:cost (5 occurrences); (2) verbatim D027 disclaimer matched via grep -qF (exit 0); (3) all four script paths referenced in the file body; (4) ## Accuracy heading present with both surfaces documented; (5) packaging/bundle/skills/orchestrator-cost.md exists with name: orchestrator:cost and command_file: commands/cost.md frontmatter; (6) packaging/bundle/manifest.yml lists orchestrator-cost.md in skills:. Expected Output #4 verified — bash scripts/dispatch/adapters/runtime/claude-code.sh --register --dry-run emits would_write=...orchestrator-cost.md confirming runtime-adapter contract per M015/M025. Expected Output #5 verified — no .orchestrator/milestones/M027/execution-log.jsonl touched by this task (the M-flag in git status is inherited from prior tasks T01/T02, with no new commits since f31d6d4 modifying the file). T04's m027-p01-cost-command-shape.sh ships in T04 and will formalize these gates as a single-script-file Check at the phase boundary — until then T03's verification is the manual must-have set above.
