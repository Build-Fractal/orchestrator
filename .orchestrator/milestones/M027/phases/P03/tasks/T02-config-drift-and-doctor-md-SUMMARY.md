---
schema_version: "1.0"
type: task-summary
id: "T02"
parent: "P03"
milestone: "M027"
provides:
  - "scripts/diagnostics/check-config-drift.sh sourceable bash 3.2 library + CLI for orchestrator:doctor --config-check (FR-16); audits the six M027 knobs across env/local/project/defaults precedence layers and emits one effective= line per key; null sentinel from read-config.sh and empty values both render as (unset); --no-config-check suppresses to zero stdout exit 0; read-only and zero-LLM-token,commands/doctor.md updated with two new sections (## Anomaly Detection + ## Config Drift) inserted between ## Runtime Instruction Drift and ## Usage; pre-edit canonical sections preserved in pre-edit order; documents the 5-condition suppression matrix and the baseline disclaimer; two new bullets in ## Referenced Scripts; 47->94 lines,tests/fixtures/m027-p03/doctor-suppressed-baseline.txt verbatim post-Referenced-Scripts tail of commands/doctor.md (load-bearing baseline for T04 byte-identity verifier),tests/fixtures/m027-p03/anomaly-fixture.jsonl 9 hand-crafted unit_close records under M999 sentinel milestone with T09 outlier exercising all three flagging conditions on the same row,tests/fixtures/m027-p03/README.md fixture role + update protocol documentation,scripts/verify/m027-p03-t02-shape-precheck.sh single-script T02-scoped verifier asserting the T02 must-haves plus the canonical 8-section order on doctor.md plus default-key-list audit; subsumed by T04 canonical phase-level verifiers"
requires:
  - "from:T01 what:scripts/diagnostics/check-anomalies.sh helper documented in commands/doctor.md ## Anomaly Detection section,from:T01 what:scripts/state/read-config.sh VALID_KEYS includes anomaly_cost_multiplier+anomaly_retry_threshold+anomaly_pass_rate_threshold+anomaly_check_enabled (combined with M027/P02 keys yields the six M027 knobs audited by default)"
affects:
  - "T03,T04"
key_files:
  - "scripts/diagnostics/check-config-drift.sh,commands/doctor.md,tests/fixtures/m027-p03/doctor-suppressed-baseline.txt,tests/fixtures/m027-p03/anomaly-fixture.jsonl,tests/fixtures/m027-p03/README.md,scripts/verify/m027-p03-t02-shape-precheck.sh"
key_decisions:
  - "AD-19,CON-1,CON-3,CON-5,CON-6,CON-7,FR-12,FR-16,FR-21,MEM004,MEM012,MEM015"
patterns_established:
  - "Document-shaped phase-task pattern (M027/P02/T02 carry-forward): integrate helper into runtime-rendered command document by editing commands/<cmd>.md at a stable documented attach point + parallel ## Referenced Scripts bullets only; pre-edit canonical sections preserved in pre-edit order; the new sections are the only structural additions; T04 ships byte-identity verifier diffing the live tail against tests/fixtures/<phase>/<doc>-baseline.txt fixture. Last-section-tail variant: when the attach-point precedes the document end and no further sections follow ## Referenced Scripts the tail is just the heading + bullet list and grows monotonically as new helpers are documented (mirrors M027/P02/T02 status.md pattern adapted to last-section attach).,Per-layer config resolution via env -u SPECKIT_ORCHESTRATOR_<UPPER_KEY> wrapping read-config.sh with only one of --local/--project/--defaults set per call: lets the helper isolate each precedence layer's resolved value without re-implementing read-config.sh's YAML parser; the env layer is read directly via eval-printf indirection (POSIX env -u cannot un-set the calling shell's env to read it). The  indirection is shell-injection-safe because the value comes from the VALID_KEYS enum filtered list and is normalized via tr [:lower:] [:upper:].,Null-sentinel normalization: read-config.sh emits the literal 'null' for unset-but-registered keys (no value at any layer). The helper collapses both empty stdout and the literal 'null' to (unset) at every layer + effective= line so operators can see absence at a glance; documented in commands/doctor.md ## Config Drift Invocation block and in fixture README.,Comment-hygiene-for-verifier-regex pattern carry-forward (M027/P00 + P01 + P02 + P03/T02): doc-comments use the safe phrasing 'no associative arrays / no herestring redirect' rather than spelling literal bash-4 forbidden tokens so the T04 m027-p03-bash32-compat.sh verifier grep regex stays clean against the file body. T02-scoped precheck shape carry-forward: single-script-file Check at scripts/verify/m027-p03-t02-shape-precheck.sh asserting only T02's must-haves; T04 ships canonical phase-level verifiers (m027-p03-config-drift-shape.sh + m027-p03-doctor-md-shape.sh + m027-p03-doctor-byte-identity.sh) which subsume this precheck (third-occurrence pattern from P00+P01+P02). Section-order assertion via grep -n line-position monotonic walk over the canonical 8-heading list catches any re-ordering or accidental insertion."
drill_down_paths:
  - ".orchestrator/milestones/M027/phases/P03/tasks/T02-config-drift-and-doctor-md-PLAN.md,scripts/diagnostics/check-config-drift.sh,commands/doctor.md"
duration: "25m"
verification_result: "pass"
completed_at: "2026-04-27T16:08:08Z"
---

T02 ships the M027/P03 config-drift helper and integrates the anomaly-detection + config-drift surfaces into commands/doctor.md following the document-shaped phase-task pattern P02/T02 established. All four artifacts plus the T02-scoped precheck land green on first dispatch. Helper: scripts/diagnostics/check-config-drift.sh (168 lines, sourceable + CLI). Audits the six M027 knobs (efficiency_footer + predictive_cost_surface from P02 plus anomaly_cost_multiplier + anomaly_retry_threshold + anomaly_pass_rate_threshold + anomaly_check_enabled from P03/T01) across the four precedence layers (env / local / project / defaults). Renders one block per key: env=, local=, project=, defaults=, plus a final effective= line. The literal 'null' sentinel that read-config.sh returns for unset-but-registered keys (T01 noted this on land) and the empty-string sentinel both collapse to (unset) so operators can visually distinguish absence at a glance. Per-layer resolution wraps read-config.sh with env -u SPECKIT_ORCHESTRATOR_<UPPER_KEY> so the env layer cannot win when the caller only wants the file-layer value; the env layer itself is read via eval-printf indirection. --no-config-check suppresses to zero stdout exit 0 (CON-3-equivalent; T04 verifier gates this). Read-only (FR-12 / CON-1) and zero-LLM-token (FR-21 / CON-6); bash 3.2 clean (CON-7) with comment hygiene preserved. commands/doctor.md grew 47 -> 94 lines via two new sections (## Anomaly Detection between ## Runtime Instruction Drift and ## Usage, ## Config Drift between ## Anomaly Detection and ## Usage) plus two new bullets in ## Referenced Scripts. Pre-edit canonical sections preserved in pre-edit order. The Anomaly Detection section documents the 5-condition suppression matrix (--no-anomaly + --yes + ORCHESTRATOR_AUTO=1 + ORCH_ANOMALY_CHECK_ENABLED=false / config + sample-floor) and the baseline disclaimer including the duration-fallback Goodhart-pairing carve-out. The Config Drift section documents the audited knobs and the --no-config-check suppression. tests/fixtures/m027-p03/ ships three fixtures: doctor-suppressed-baseline.txt (verbatim post-Referenced-Scripts tail; load-bearing for T04 byte-identity), anomaly-fixture.jsonl (9 unit_close records under M999 sentinel; T09 row exercises cost + retry + pass-rate flagging conditions on the same row), README.md (fixture roles + update protocol). T02-scoped precheck scripts/verify/m027-p03-t02-shape-precheck.sh asserts the helper file shape (>=80 lines, executable, title literal, render function, BASH_SOURCE guard, read-config.sh delegation), the doctor.md shape (>=60 lines, both new headings, both helper refs, all 5 suppression-matrix tokens, baseline-disclaimer literals), the canonical 8-section order via grep -n monotonic walk, the fixture shapes (doctor baseline + anomaly fixture + README size), and three behavioral assertions (--keys efficiency_footer smoke + --no-config-check zero stdout + default key list audits all six M027 knobs). All checks PASS on first run. Verification: bash scripts/verify/m027-p03-t02-shape-precheck.sh -> exit 0 PASS. T04 will ship canonical phase-level verifiers that subsume this precheck (third occurrence of the M027 T04-subsumes-prechecks pattern from P00+P01+P02).
