---
schema_version: "1.0"
type: task-summary
id: "T01"
parent: "P03"
milestone: "M028"
provides:
  - "ANTIPATTERNS.md AP-010 (cmd-sub-in-pattern; remedy grep-files.sh), AP-011 (quoted-arg-newline-hash; operator-rewrite remedy), AP-012 (multiline-quoted-script; remedy node-eval.sh), AP-013 (unquoted-brace-glob; remedy peek-files.sh; sibling AP-007), AP-014 (xargs-sh-c-compound-body; remedy peek-files.sh; CON-5 one-level body-descent; sibling AP-009); each new entry carries verbatim Cross-Refs pointing at enforcement layer (pre-bash-shape-guard.sh reject_lookup case arm), regression corpus (m021-prompt-corpus.txt IDs 21..25,27), and classifier implementation (shape-classifier.sh private detector); 122 lines additive; AP-001..AP-009 unchanged byte-for-byte (append-only invariant); file 214 -> 336 lines"
requires:
  - "from:M028/P02/T01 what:hook BASH_SOURCE-resolved scripts/hooks/pre-bash-shape-guard.sh whose reject_lookup AP-014 names; from:M028/P01 what:per-screenshot causal trace SE-02..SE-05+SE-09 cited as Evidence; from:specs/031-autonomous-hardening-v3/spec.md what:FR-8..FR-12 + CON-5 body-descent bound; from:.orchestrator/memory/constitution.md what:Principle II + IX numerals"
affects:
  - "M028/P03/T02 (classifier extension cross-refs the new private detector names); M028/P03/T03 (hook reject_lookup case-arm names match AP-010..AP-014 Cross-Refs); M028/P03/T04 (corpus IDs 21..25,27 cited as Evidence in AP-010..AP-014); M028/P03/T05 (verifier scripts/verify/m028/p03-antipatterns-entries.sh asserts the 5 new headings + sub-section presence + classifier-label cross-ref names); M028/P04 (wrappers grep-files.sh, node-eval.sh, peek-files.sh referenced as remedy targets)"
key_files:
  - "ANTIPATTERNS.md (modified -- 122 lines appended at EOF; AP-010..AP-014 entries; line 214 -> line 336; AP-001..AP-009 lines 1..214 unchanged byte-for-byte)"
key_decisions:
  - "Followed the verbatim heading text from the task plan body (e.g. AP-014 heading 'Compound Chain Hidden Inside xargs ... sh -c <body>') rather than the abbreviated shape in the phase must-haves substring assertion. Driver: the task plan Steps section is the contract for entry text; the phase must-haves substring assertion was authored as a paraphrase and is a phase-level drift not a T01 defect. Surfaced as a dogfood finding for CLAUDE.md hotfix list.,Cross-refs cite corpus IDs 21..25,27 and wrappers grep-files.sh / node-eval.sh / peek-files.sh that do not yet exist on disk -- evergreen pointer pattern; targets land in M028/P03/T04 (corpus) and M028/P04 (wrappers). The CON-7 append-only invariant means future P04 wrapper landings will not require ANTIPATTERNS.md edits.,No CON-1 (AD-19) impact: documentation-only task; no scripts authored; no shell-shape lines introduced. write-summary.sh and the commit step were the only Bash invocations in the dispatch turn,both single-script-file shape per AD-19.,Used 'git commit -F <message-file>' rather than the CLAUDE.md heredoc-with-expansion 'git commit -m' inline-cat-heredoc form per the M028/P02 dogfood finding (AP-008 hook rejects the heredoc-with-expansion shape). Commit message authored to /tmp/m028-p03-t01-commit-msg.txt via Write tool then consumed by 'git commit -F'.,Verified T01-truth-Check verifier scripts/verify/m028/p03-antipatterns-entries.sh does not yet exist (lands in T05); reported the expected RED state per the task plan acceptance criteria rather than authoring an in-task placeholder verifier (out of scope; T01 is documentation-only).,Phase-level check-must-haves.sh substring 'AP-014: xargs sh -c Compound Body' does not match the plan-body verbatim heading; T01 honored the plan-body shape per Steps 6. The mismatch is captured as a dogfood finding for CLAUDE.md hotfix list rather than papering over by rewriting the AP-014 heading to match the (non-canonical) substring."
patterns_established:
  - "Append-only invariant on documentation registers (per file header 'Entries are permanent -- they do not decay or expire'): new entries strictly appended at EOF; existing entries unchanged byte-for-byte; diff is purely additive. Verifiable via wc -l before/after + grep-pattern preservation on existing IDs.,Closed-pattern-set discipline: the M028 spec Non-Goals close the AP-ID set on the seven-screenshot evidence -- T01 ships exactly 5 entries (AP-010..AP-014); AP-015+ deferred to a future spec/milestone if new evidence surfaces. Numbering preserved (no renumbering of AP-001..AP-009).,Evergreen forward-compatibility cross-refs: each new entry cites corpus IDs and wrapper paths that do not yet exist on disk at append time. The pointers become live as their targets land in subsequent tasks/phases (T04 corpus,P04 wrappers,T02 classifier private-detector function names,T03 hook reject_lookup case arms). No back-edits needed when the targets land.,Heading-text discipline under multi-stage author/verify split: when a task plan body specifies verbatim entry text and a separate phase must-haves substring is authored as a paraphrase,follow the plan-body verbatim text; flag the must-haves drift as a dogfood finding rather than reshaping the artifact to the paraphrase. Supports CON-7 (no-M021-regression by extension to documentation registers).,Documentation-task dispatch shape: no scripts authored,no shell-shape lines introduced,no AD-19 constraint impact. Commit message via 'git commit -F <file>' (heredoc-with-expansion is AP-008-rejected). write-summary.sh long-body field passed via '--body-file=<tmp-path>' to avoid multi-line quoted CLI argument shape."
drill_down_paths:
  - ".orchestrator/milestones/M028/phases/P03/tasks/T01-antipatterns-entries-PLAN.md"
duration: "15m"
verification_result: "pass"
completed_at: "2026-04-29T16:56:11Z"
---

T01 appended five new entries (AP-010 through AP-014) to ANTIPATTERNS.md, raising the register from 9 entries (214 lines) to 14 entries (336 lines). Each new entry mirrors the canonical AP-001..AP-009 shape: level-2 heading, Observed In, Principle Violated, Related Constitution Constraint, Description, Evidence, Remedy, Cross-Refs.

The five new entries cover the M028 shape classes that the P03/T02 classifier extension and P03/T03 hook reject_lookup will enforce:

- **AP-010 cmd-sub-in-pattern** — backtick byte inside grep/sed/awk regex argument (Finding B #1, SE-02). Remedy: scripts/util/grep-files.sh wrapper.
- **AP-011 quoted-arg-newline-hash** — newline + `#` inside double-quoted CLI argument tripping path-validation heuristic (Finding B #2, SE-03). No wrapper; operator-facing remediation pointer (rewrite as separate setter calls).
- **AP-012 multiline-quoted-script** — multi-line body to `node -e` / `python -c` / `sh -c` shapes (Finding B #3, SE-04). Remedy: scripts/util/node-eval.sh wrapper.
- **AP-013 unquoted-brace-glob** — raw `{N,M,...}` outside quotes, sibling to AP-007's quoted-brace case (Finding B #4, SE-05). Remedy: scripts/util/peek-files.sh wrapper.
- **AP-014 xargs-sh-c-compound-body** — compound chain hidden inside `find | head | xargs sh -c '<body>'` shape (Finding G, SE-09). Remedy: scripts/util/peek-files.sh wrapper. AP-014 documents the CON-5 one-level-deep body-descent bound and the precedence ordering against AP-009 (more specific verdict wins).

Cross-references in each new entry point at four targets: enforcement layer (scripts/hooks/pre-bash-shape-guard.sh reject_lookup case arm), regression corpus (tests/fixtures/m021-prompt-corpus.txt entry IDs 21..25, 27), classifier implementation (scripts/verify/lib/shape-classifier.sh private detector function name), and (where applicable) sibling entries (AP-007 ↔ AP-013 quoted/unquoted brace pair; AP-009 ↔ AP-014 top-level-compound vs descended-body pair). All cross-references are evergreen pointers — the targets land in T02..T05 of P03 (and P04 for the wrappers); the pointers do not break the register if the target is authored later.

Append-only invariant preserved: AP-001..AP-009 entries unchanged byte-for-byte. The diff is purely additive at end-of-file (122 lines added, 0 lines modified).

Verification: T01's truth-Check `bash scripts/verify/m028/p03-antipatterns-entries.sh` will pass once T05 authors the verifier; today it returns "No such file" (expected per the task plan: "Verifier itself ships in T05; T01's truth-Check passes once T05 lands"). The phase-level `bash scripts/verify/check-must-haves.sh .orchestrator/milestones/M028/phases/P03` returns the artifact gate `PASS: ANTIPATTERNS.md has 336 lines (min 280)` — the line-count and existence gates pass. The `'AP-014: xargs sh -c Compound Body'` literal-substring assertion in the phase must-haves is RED because the must-haves text was authored against an abbreviated heading shape that does not match the plan-body verbatim text (`## AP-014: Compound Chain Hidden Inside `xargs … sh -c '<body>'``); T01 followed the plan-body shape per Steps 6 and the task-plan acceptance criteria. This is a phase-level must-haves vs plan-body drift item, not a T01-deliverable defect — flagged for phase-author review at consolidation time. Other RED phase-must-haves (downstream verifier scripts, classifier extensions, hook reject_lookup case arms, corpus entries) are expected per the task plan ("Other phase-level must-haves remain RED until T02..T05 land").

Patterns: documentation-only task — no scripts authored, no shell-shape constraints triggered (CON-1 AD-19 not in scope). The AP-IDs follow the closed pattern set per spec Non-Goals; AP-015 was not authored. Cross-ref forward-compatibility pattern: each new entry cites corpus IDs and wrapper paths that do not yet exist on disk; the references are evergreen and become live as their targets land in subsequent tasks/phases.

Dogfood finding for CLAUDE.md hotfix list: phase must-haves text `'AP-014: xargs sh -c Compound Body'` does not match the plan-body verbatim heading `## AP-014: Compound Chain Hidden Inside `xargs … sh -c '<body>'``. The phase-author wrote the must-haves expectation as a paraphrase rather than copying the exact heading from the task plan body. Cheapest fix: update the phase must-haves substring to either (a) the verbatim plan heading, or (b) a more permissive `'AP-014:'` token. Surfaced 2026-04-29 by M028/P03/T01 dogfooding.
