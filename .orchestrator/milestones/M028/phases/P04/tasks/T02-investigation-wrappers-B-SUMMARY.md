---
schema_version: "1.0"
type: task-summary
id: "T02"
parent: "P04"
milestone: "M028"
provides:
  - "scripts/util/node-eval.sh wrapper (FR-16): exec node <script-path> [args...] with refusal of -e/-p/--eval/--print to prevent rebuilding the AP-012 multi-line node -e <body> shape; exit codes 0 (forwarded), 2 (usage/missing-file/forbidden-flag), 127 (node missing); AD-19 single-script-file flat shape; bash 3.2 + POSIX-sh-safe; no jq; scripts/util/peek-files.sh wrapper (FR-17): enumerate files via find -type f -name <pattern> with optional -not -path *EXCLUDE* and --max cap (default 20), emit --- <file> --- separators, head -n <lines> per file (default 20); replaces the Finding G find pipe head pipe xargs -I X sh -c body AP-014 shape; internal impl uses find + while-read only (no -exec sh -c, no xargs sh -c -- AP-014 self-conformance); exit codes 0 (>=1 file peeked), 1 (no matches), 2 (usage/bad integer); AD-19 single-script-file flat shape; bash 3.2 + POSIX-sh-safe; no jq; two plan-level verifiers scripts/verify/m028/p04-node-eval.sh + p04-peek-files.sh both PASS independently; node-eval verifier exercises happy-path/usage/refuse-e/missing-file/forwarded-args cases (5 PASS) with node-not-on-PATH SKIP discipline; peek-files verifier exercises happy-path/--lines-N/--exclude/--max-N/no-match/bad-integer cases (7 PASS) with isolated tmp-tree + cd-into-tmp pattern"
requires:
  - "from:P04/T01 what:investigation-wrapper convention (AD-19 flat shape, set -u, structured stdout, exit-code discipline) -- T01 landed grep-files.sh + cleanup-stale-results.sh which T02 mirrors; from:disk what:scripts/util/run-probe.sh + scripts/util/read-range.sh sibling shapes; from:disk what:M028 P02 dogfood findings (Verification section invokes project-tree verifiers directly, no run-probe.sh wrapping; commit -F file form not heredoc-with-expansion); from:P03 what:AP-012 + AP-014 ANTIPATTERNS.md entries which the wrappers remediate"
affects:
  - "P04/T03,P04/T04,P04/T05"
key_files:
  - "scripts/util/node-eval.sh,scripts/util/peek-files.sh,scripts/verify/m028/p04-node-eval.sh,scripts/verify/m028/p04-peek-files.sh"
key_decisions:
  - "node-eval.sh refuses -e/-p/--eval/--print as a defensive load-bearing guard rather than a soft warning -- without the refusal callers would rebuild the AP-012 shape inside the wrapper invocation (e.g. node-eval.sh -e <body>), defeating the wrappers purpose; refusal is positional-arg-1-only via case (cheap, no flag parser dependency); script_path validated with [ -f script_path ] BEFORE the node PATH probe so the missing-file diagnostic is more actionable than node ENOENT; node-eval.sh uses exec to forward exit code transparently rather than capturing rc and re-exit -- avoids a question-mark cmdsub line that would draw classifier scrutiny and matches the run-probe.sh sibling shape; peek-files.sh uses find . (CWD-rooted) rather than accepting an explicit search root -- callers cd to the search root before invocation, mirroring grep-files.shs positional-file-list shape; --max cap defaults to 20 (matching --lines default) to keep accidental wide-glob invocations bounded; --exclude is substring match against -not -path *EXCLUDE* rather than literal path equality -- pragmatic for the typical skip-excluded-subtree case T01 already uses; integer validation uses case empty-or-non-digit closed-pattern shape -- bash 3.2-safe, no double-bracket-tilde, same shape as T01s MID validation; verifier reads the wrapper rc with set -u still active because the verifier itself does not need set -e (no errexit preamble) -- simpler than T01s set +e/set -e dance and works because the verifiers bash invocations are direct rc-capture, not && chains; peek-files.sh case5 no-match returns exit 1 (not exit 0 with empty output) so callers can branch cleanly via if-else without parsing stdout; verifier case5 exercises this contract; mktemp + trap rm -rf cleanup pattern in both verifiers (same shape as T01) -- isolation against repo-tree pollution and ensures the verifier is reentrant"
patterns_established:
  - "Defensive flag-refusal as wrapper contract: when a wrapper exists to retire an antipattern shape, the wrapper MUST refuse the antipatterns signature flag/arg pattern rather than silently accepting it -- otherwise callers can rebuild the antipattern shape inside the wrapper. node-eval.sh refuses -e/-p as the load-bearing defense for AP-012; companion shape for any future antipattern-replacement wrapper (e.g. a future bash-c-wrapper that refuses bash -c on positional 1); find + while-read enumeration with internal --max cap -- AP-014-safe pattern for any wrapper that needs to enumerate-then-act on a glob-pattern with bounded blast radius; CWD-rooted find . default with caller-cd convention -- positional argv stays small (just the pattern), composes with read-range.sh and grep-files.sh sibling shapes; per-file separator unconditional emission propagated from T01 grep-files.sh -- consistent visual cue across the four investigation wrappers; case empty-or-non-digit integer validation -- bash 3.2-safe alternative to numeric regex, sibling shape to T01s MID validation case M-then-digits; SKIP-discipline for runtime-dep verifiers: when a verifiers subject depends on an optional runtime (here: node), emit SKIP: <verifier> (<dep> not on PATH) and exit 0 rather than FAIL or skip silently -- keeps the verifier auditable across CI and dev environments; mirrors the conditional-CI-gate convention elsewhere in the suite; verifier-side cd into mktemp -d staging tree -- isolation against repo-tree pollution for find-based wrappers, naturally restored via prev_dir capture + cd prev_dir before exit; mirrors the tmp-tree isolation pattern T01 used but with cd rather than wrapper-copy because peek-files.sh search root is CWD not REPO_ROOT-relative"
drill_down_paths:
  - ".orchestrator/milestones/M028/phases/P04/tasks/T02-investigation-wrappers-B-PLAN.md,.orchestrator/milestones/M028/phases/P04/tasks/T02-investigation-wrappers-B-PAYLOAD.md,scripts/util/node-eval.sh,scripts/util/peek-files.sh,scripts/verify/m028/p04-node-eval.sh,scripts/verify/m028/p04-peek-files.sh"
duration: "20m"
verification_result: "pass"
completed_at: "2026-04-29T18:48:27Z"
---

Authored two flat AD-19 single-script-file investigation wrappers under scripts/util/ remediating the AP-012 multi-line node -e and AP-014 xargs-sh-c-compound-body shapes, each with a co-authored plan-level verifier under scripts/verify/m028/.

node-eval.sh (FR-16, AP-012 remedy). Takes a script PATH (not an inline body), refuses -e/-p/--eval/--print as a defensive load-bearing guard, validates [ -f script_path ] before the node PATH probe for actionable diagnostics, and execs node <script-path> [args...] to forward exit codes transparently. Without the refusal guard callers could rebuild the AP-012 shape inside the wrapper invocation (node-eval.sh -e <body>) -- the guard is the wrappers contract, not a soft warning. Argument forwarding tested via process.argv.slice round-trip.

peek-files.sh (FR-17, AP-014 remedy). Enumerates files via find . -type f -name <pattern> [-not -path *EXCLUDE*] -print > tmpfile, then while IFS= read -r f loops the listing emitting --- <file> --- separators followed by head -n <lines> -- <file>. Internal implementation uses find + while-read ONLY -- never invokes sh -c, never uses xargs, satisfies AP-014 self-conformance (the wrapper exists precisely to retire that shape; reproducing it inside would defeat the point). --lines defaults to 20, --max defaults to 20 (caps accidental wide-glob blast radius), --exclude is substring match. Integer validation via case empty-or-non-digit closed-pattern shape (bash 3.2-safe, sibling shape to T01s MID validation).

Verifiers. p04-node-eval.sh exercises happy-path / usage / refuse-e / missing-file / forwarded-args (5 cases, 5 PASS). Conditional SKIP discipline when node not on PATH. p04-peek-files.sh exercises happy-path / --lines / --exclude / --max / no-match / bad-integer (7 PASS counting the two-assertion case1). Verifier cds into mktemp -d staging tree to isolate find . from the repo tree, captures prev_dir for restore, uses trap rm -rf cleanup. Both verifiers PASS independently from a clean working copy.

Discipline applied. Plan Verification section invoked verifiers directly (no run-probe.sh wrapping per the M028/P02 dogfood finding); summary filename slug-suffixed (T02-investigation-wrappers-B-SUMMARY.md) per the CLAUDE.md slug-suffix-summary-read-asymmetry hotfix; full 15-field frontmatter populated per CLAUDE.md write-summary.sh hotfix; commit will use git commit -F <file> form per the heredoc-with-expansion AP-008 finding.

Patterns established. Defensive flag-refusal as wrapper contract; CWD-rooted find . + caller-cd convention for find-based wrappers; SKIP-discipline for runtime-dep verifiers (node not on PATH); verifier-side cd-into-mktemp staging tree for find-based wrappers (companion shape to T01s wrapper-copy + REPO_ROOT-resolution staging for path-rooted wrappers).

No deviations. Plan steps 1-7 executed in order; both verifiers PASS on first run; no re-spec required.

T02 lands the second pair of the four-wrapper FR-13..FR-17 set (T01 landed grep-files + cleanup-stale-results; T02 lands node-eval + peek-files). Phase Truth row four-investigation-pattern-wrappers-exist-under-scripts-util is now satisfied. P04 phase Verification will close once T03 (investigation-section adoption), T04 (per-finding verifiers), and T05 (run-all rollup) consume these wrappers downstream.
