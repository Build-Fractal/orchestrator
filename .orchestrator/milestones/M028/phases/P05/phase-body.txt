P05 (close-out + downstream fixture + regression gate) lands the M028 close-out axis. Four tasks executed: T01 staged the permanent in-tree consumer fixture at `tests/fixtures/downstream-project/` (CON-10 contract anchor — checked into version control, not generated at test time) plus existence + adapter-shape verifiers; T02 authored the autonomous-loop replay harness `tests/run-downstream-fixture.sh` (installer-staged isolated HOME, replays Finding A + corpus IDs 21-25, 27 + a Stop event, emits canonical `WOULD_PROMPT=0/N`) plus the wrap-the-harness clean verifier; T03 authored the close-out regression gate sequencing four sub-gates (install-roundtrip → corpus-replay-27-entry → per-finding-run-all → downstream-fixture-replay) plus thin sub-gate clean verifiers (`p05-corpus-replay-clean.sh`, `p05-run-all-clean.sh`); T04 was the close-out sweep — zero new deliverables, pure verification gate confirming all six P05 Truth-Check leaves PASS individually, the regression gate emits `M028 close-out: 4/4 sub-gates clean`, `check-must-haves.sh` reports 42/42 PASS, and `run-all.sh` confirms `M028: 7/7 findings verified (skipped: 0, failed: 0)`.

Verification results: all four task verifiers PASS on first invocation; phase-level `bash scripts/verify/check-must-haves.sh .orchestrator/milestones/M028/phases/P05` reports 42/42 PASS (6 truths + 27 artifact sub-assertions across 9 artifacts × 3 checks each + 9 key-links); `bash scripts/verify/m028/p05-regression-gate.sh` reports 4/4 sub-gates clean with rc=0 and canonical PASS tails on every sub-gate; `bash scripts/verify/m028/run-all.sh` reports 7/7 findings verified across 8 per-finding verifiers (finding-A through finding-G plus G-classifier and G-wrapper).

Patterns established for downstream / future close-out work: (1) live-adapter-emission-vs-fixture-bytes shape gate via mktemp -d isolated HOME (verifier captures `HOME=tmp/fake-home bash adapter --hook-config > tmp/adapter-emission.json` then runs shape assertions against both fixture and capture); (2) shape-pattern comparison via grep -cE on a regex matching `bash <anything>.sh` rather than literal-bytes diff (fixture uses literal HOME placeholder while adapter emits expanded HOME path; comparing shape via regex captures contract intent without tying fixture to one HOME at one execution); (3) count-equality assertions for shape contracts (managed-flag count + command-line count between fixture and adapter — additive-axis discipline catches drift in either direction); (4) installer-staged isolated HOME replay harness (harness invokes the installer with HOME=tmp_home then routes synthetic Bash hook events through the staged shape-guard hook; reusable across future end-to-end hook-event replay tests); (5) JSON event payload via printf-NOT-heredoc shape (printf JSON-template with safe-escaped command bytes avoids AP-008 heredoc-with-expansion classifier reject); (6) negative-control assertion as M021-regression catch-all (harness includes a benign single-stage `echo hello` assertion that classifies allow under both M021 and M028 baseline; over-broad classifier fails the negative control by construction); (7) wrap-the-harness Truth-Check verifier shape (~50 line bash verifier captures stdout+stderr to mktemp, asserts exit 0 + canonical summary substring + absence of `^FAIL:` lines + optional auxiliary substrings); (8) sub-gate-sequencing-with-tmp-log shape (regression gate uses `run_sub_gate` helper with per-label log files under `${TMPDIR:-/tmp}/m028-p05-regression-gate-$$/`, captures rc + tail-line for human-readable per-sub-gate report, preserves logs on FAIL, cleans up on all-PASS); (9) phase-level close-out task shape (zero new deliverables, pure verification gate, runs every Truth-Check leaf individually + the consolidated regression gate + phase-level check-must-haves + the per-finding aggregator); (10) subprocess success-message hygiene (when a wrapping verifier asserts absence of a literal substring in subprocess output, the subprocess MUST NOT emit that substring in its own success messages — surfaced by P05/T02 dogfooding when a Stop event PASS message contained `command not found` and false-failed the wrapping verifier).

Roadmap reassessment: P05 is the final phase of M028; downstream affects=none. No DECISIONS register entries required. The two-layer architecture (regression gate as consolidated CI artifact distinct from per-sub-gate clean verifiers as granular Truth-Check leaves consumed by check-must-haves.sh) is non-redundant by design — both layers consume the same underlying harnesses but serve different consumers. P05 sibling `p05-run-all-clean.sh` coexists with pre-existing P04/T05 `p04-run-all-clean.sh` by phase-scoped naming convention.

External-modification audit: phase-transition.sh flagged the four P04/T04 deliverables (T04-per-finding-verifiers-SUMMARY.md, finding-D/E/G-verifier.sh) — these were committed mid-task during P04/T04 as commit a823f06 on main; the warning is expected and the modifications are recorded.

M028 milestone is ready for `orchestrator:consolidate`.
