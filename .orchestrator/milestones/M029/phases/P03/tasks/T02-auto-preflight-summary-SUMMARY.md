---
schema_version: "1.0"
type: task-summary
id: "T02"
parent: "P03"
milestone: "M029"
provides:
  - "FR-9 preflight-summary docstring contract in commands/auto.md (Preflight Summary H2 section with four-line stderr block at Standard/Full intensity Preflight Summary/phase_count/dispatch_count_estimate/predicted_cost; Quick intensity suppresses entirely per SC-9 byte-stable invariant); AD-3 four-priority non-interactive policy documented in order (--yes -> auto_proceed: true -> non-TTY M029_PREFLIGHT_NEEDS_CONFIRMATION refusal -> TTY prompt) with TTY discrimination via detect-invocation-context.sh per AD-1 single-resolve; AD-4 oracle wrapper documented verbatim (bash predictive-surface.sh --description bash summarize-milestone.sh M### --format=keys --intensity standard) with rationale for omitting --no-predict (predictive-surface.sh:124 NO_PREDICT=1 -> SUPPRESS=1 would zero the cost block and break the byte-identity contract on cost_standard_usd=); shape verifier tools/verify/m029-p03-auto-preflight-shape.sh (AD-19 straight-line bash MEM001 bash 3.2) gating 14 invariants 12 literal presence + 2 negative CON-1 read-only assertions; pass=14 fail=0"
requires:
  - "P02,P03/T01"
affects:
  - "P03/T03,P03/T04"
key_files:
  - "commands/auto.md,tools/verify/m029-p03-auto-preflight-shape.sh"
key_decisions:
  - "AD-3 priority order is byte-stable (--yes first; auto_proceed: true second; non-TTY refusal third with M029_PREFLIGHT_NEEDS_CONFIRMATION exit-non-zero; TTY prompt fourth -- re-ordering breaks SC-8/SC-9 acceptance reasoning); AD-4 oracle invariant the documented oracle invocation MUST omit --no-predict otherwise byte-identity contract on cost_standard_usd= is unsatisfiable because output is suppressed by predictive-surface.sh:124 (load-bearing departure from pre-discuss-time roadmap text -- to be captured in spec amendment record at T05); CON-1/FR-14 read-only discipline preserved via negative-assertion verifier asserting absence of >> .orchestrator and mkdir -p .orchestrator forbidden write-tokens; CON-7/AD-8 knowledge-layer-boundary preserved -- T02 introduces NO new JSONL events NO M020 changes NO M027 surface changes the skill doc cross-references existing M027 surfaces it does not extend them; AD-1 single-resolve renderer field consumed from detect-invocation-context.sh not re-derived; #Q-G1 four-priority non-interactive policy with no silent CI auto-accept; #Q-2 cost format range form predicted_cost: est. ~$X.YY ± $Z.ZZ derived from confidence interval"
patterns_established:
  - "LLM-instruction-skill docstring contract pattern -- the preflight is documented as a contract that the LLM agent reads and emits not a Bash surface (matches design of every other commands/*.md skill: where status context); negative-assertion verifier discipline extended from P02 to P03 -- verifier code asserts absence of forbidden tokens (>> .orchestrator mkdir -p .orchestrator) in deliverable bodies while the verifier itself names those literals in its assertion strings (verifier code is not deliverable text); grep -F -q -e $needle pattern (with -e separator) is required when the needle starts with -- (such as --yes); without -e grep parses --yes as an unknown flag and errors out -- this caught a real bug in the first verifier run pass=13 fail=1 corrected to pass=14 fail=0 by adding -e separator to both _assert_present and _assert_absent helpers; oracle-wrapper-with-omit-flag-rationale pattern -- when a documented invocation deliberately omits a flag that a future planner might naively re-add the docstring records the omit-rationale (here: predictive-surface.sh:124 NO_PREDICT=1 -> SUPPRESS=1 would suppress the un-suppressed cost block the byte-identity contract operates on); inline markdown linebreak hazard -- when authoring docstring text via the Edit tool a soft linebreak inside a literal-token-bearing sentence (Quick newline intensity suppresses across two lines) breaks grep -F-q literal matching even though the sentence reads correctly to a human; rule-of-thumb keep load-bearing literal tokens on one source line and reflow surrounding prose around them"
drill_down_paths:
  - ".orchestrator/milestones/M029/phases/P03/tasks/T01-render-position-live-and-savings-marker-SUMMARY.md"
duration: "22m"
verification_result: "pass"
completed_at: "2026-05-06T03:52:52Z"
---

T02 ships the FR-9 + AD-3 + AD-4 Preflight Summary documentation contract in commands/auto.md plus its shape verifier. Documentation-only -- no executable surface lands here. The SC-8 + SC-9 acceptance scripts in T04 will exercise the documented surface end-to-end against fixture milestones; T02 is the design contract per Principle III. The Preflight Summary H2 section was prepended directly after the H1 title and intro paragraph and before the existing ## Intensity Behavior H2 (canonical insertion point matching the where/context/status skill convention). At Standard or Full intensity before entering the auto-loop the auto surface emits a four-line stderr block: Preflight Summary header + phase_count: N + dispatch_count_estimate: M + predicted_cost: est. tilde-dollar X.YY +/- dollar Z.ZZ. At Quick intensity the preflight is suppressed entirely (Quick intensity suppresses the block -- Preflight Summary does NOT appear on stderr before AUTO:READY per SC-9 byte-stable invariant). The AD-3 four-priority non-interactive policy is documented in priority order with --yes flag first auto_proceed: true second non-TTY refusal third (with byte-stable M029_PREFLIGHT_NEEDS_CONFIRMATION exit-non-zero stderr literal and explicit no silent CI auto-accept clause) and TTY prompt fourth. The AD-4 oracle wrapper is documented verbatim so future readers can reproduce the cost line by hand: bash scripts/dispatch/predictive-surface.sh --description bash scripts/diagnostics/summarize-milestone.sh M### --format=keys --intensity standard with extraction via grep -F cost_standard_usd= cut -d= -f2. The reason --no-predict is NOT in the oracle invocation is documented inline citing scripts/dispatch/predictive-surface.sh line 124 NO_PREDICT=1 -> SUPPRESS=1 -- this is a load-bearing departure from the pre-discuss-time roadmap text and is to be captured in the spec amendment record entry at T05. The shape verifier tools/verify/m029-p03-auto-preflight-shape.sh is AD-19 straight-line bash MEM001 bash 3.2 ~70 lines and gates 14 invariants: 12 literal presence assertions (Preflight Summary header FR-9 AD-3 AD-4 references M029_PREFLIGHT_NEEDS_CONFIRMATION token predictive-surface.sh oracle reference summarize-milestone.sh oracle wrapper reference auto_proceed config reference --yes flag reference Quick intensity suppresses invariant cost_standard_usd byte-identity field detect-invocation-context.sh AD-1 single-resolve reference) plus 2 negative-assertion CON-1 read-only checks (no >> .orchestrator append-write no mkdir -p .orchestrator creation). One in-flight bug surfaced and was fixed during hand-verification: the first verifier run reported pass=13 fail=1 because grep -F -q --yes parsed --yes as an unknown flag rather than as a literal pattern. Adding -e between the flags and the needle (grep -F -q -e needle) fixed the issue and brought the run to pass=14 fail=0. Captured in patterns_established for future verifier authors. Co-existing P03 verifiers re-run green (m029-p03-render-position-live-shape.sh pass=14 fail=0 + m029-p03-display-thresholds-config-shape.sh pass=14 fail=0). Verification gate: bash tools/verify/m029-p03-auto-preflight-shape.sh -> pass=14 fail=0. Commit SHA af0953e0. T02 unblocks T03 (auto chain flag) and T04 (acceptance scripts SC-7/SC-8/SC-9/SC-10 against fixture milestones).
