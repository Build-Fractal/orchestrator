---
schema_version: "1.0"
type: milestone-summary
id: "M030"
parent: "032-adaptive-model-selection"
milestone: "M030"
provides:
  - "P00 — version-controlled classifier ground-truth fixture corpus at tests/fixtures/m030-classifier-corpus/labels.yml (40 hand-labeled pre-M030 PLAN.md references; 20 mechanical / 15 standard / 5 novel) + README.md methodology + 5 corpus-shape verifiers under tools/verify/p00-*.sh; P01 — scripts/dispatch/classify-task.sh (deterministic <100ms no-network classifier emitting character + confidence) + templates/model-routing.yml SSOT (routing + per-runtime resolution + cost_rates) + references/model-routing.md operator docs + classifier-confidence stability metric (rolling variance ≤ 0.10 + N=20 minimum class-coverage); P02 — scripts/dispatch/dispatch-interface.sh shadow-mode hook (CC-only gated on M030_SHADOW_MODE=1 && CLAUDECODE=1) + 5 additive JSONL fields (model_routed,model_used,partial_flip_active,withheld_classes,classifier_confidence) + scripts/diagnostics/shadow-compare.sh (4-verdict closed-enum aggregator: ready|partially_ready|block|evidence_insufficient); P03 — operator-overrides resolution path (kill-switch → plan-frontmatter → milestone-floor → none precedence chain) + override_source closed-enum field + references/model-routing.md ## Operator Overrides; P04 — live-routing branch + verifier-fail escalation (FR-10 with CON-5 hard-cap) + FR-9 programmatic flip-gate enforcement + escalation_count/escalation_reason/shadow_gate_blocked JSONL fields + escalation_cap_hit record type; P05 — scripts/diagnostics/metrics-rollup.sh --by-model + scripts/diagnostics/efficiency-footer.sh model_mix: line + scripts/diagnostics/doctor.sh --config-check; P06 — scripts/diagnostics/check-anomalies.sh model_routing_regression check (rolling-window per-class verifier-fail-rate) + dispatch-interface character emit + .orchestrator/anomalies.jsonl emit path + references/model-routing.md ## Anomaly Records; P07 — tests/m030-acceptance/shadow-corpus-fixtures.sh idempotent synthesizer + 4 corpora (corpus-50-per-class.jsonl 150 records, corpus-zero.jsonl, corpus-2-class-only.jsonl 100 records, corpus-block.jsonl 60 records) + tests/m030-acceptance/run-acceptance-battery.sh end-to-end SC runner (BATTERY: pass=22 fail=0 across 14 SCs) + .orchestrator/milestones/M030/M030-ACCEPTANCE-EVIDENCE.md one-shot ledger + 10 P07 verifiers + p07-phase-suite.sh aggregator + M030-VALIDATED marker + this M030-SUMMARY.md"
requires:
  - "M027 cost-rollup JSONL stream (dispatch_usage, unit_close); M027 anomaly detection (check-anomalies.sh); M025 installer coexistence (.orchestrator/config.yml overlay convention); M028 autonomous-hardening v3 (hook portability for clean shadow-corpus signal in autonomous runs); A-1..A-6 spec assumptions (032-adaptive-model-selection/spec.md)"
affects:
  - "M031 (right-sized entry — Quick intensity today bypasses dispatch-interface model-routing layer; M031 must restore the routing-layer access for Quick intensity); M027 cost-observability surfaces (M030 extends additively via FR-15/FR-16/FR-17/FR-18); every future M030+ milestone (default flip is shadow-mode; live routing is operator-opt-in via .orchestrator/config.yml model_routing.live: true; FR-9 programmatic flip-gate refuses live routing if the shadow corpus is below threshold; CC-only posture preserved per FR-6 — Codex CLI + Cursor adapters resolve any symbolic tier to inherit); operator-facing cost reporting (model_mix: footer line + by-model rollup are the canonical surfaces)"
key_files:
  - "scripts/dispatch/classify-task.sh,scripts/dispatch/dispatch-interface.sh,scripts/diagnostics/shadow-compare.sh,scripts/diagnostics/metrics-rollup.sh,scripts/diagnostics/efficiency-footer.sh,scripts/diagnostics/check-anomalies.sh,scripts/diagnostics/doctor.sh,templates/model-routing.yml,references/model-routing.md,references/observability.md,tests/fixtures/m030-classifier-corpus/labels.yml,tests/fixtures/m030-classifier-corpus/README.md,tests/fixtures/m030-p02/sc3a-roundtrip-corpus.jsonl,tests/fixtures/m030-p02/shadow-corpus-ready.jsonl,tests/fixtures/m030-p02/shadow-corpus-partially-ready.jsonl,tests/fixtures/m030-p02/shadow-corpus-block.jsonl,tests/fixtures/m030-p02/shadow-corpus-evidence-insufficient.jsonl,tests/fixtures/m030-p06/regression-mechanical.jsonl,tests/fixtures/m030-p06/regression-standard.jsonl,tests/fixtures/m030-p06/regression-novel.jsonl,tests/m030-acceptance/shadow-corpus-fixtures.sh,tests/m030-acceptance/corpus-50-per-class.jsonl,tests/m030-acceptance/corpus-zero.jsonl,tests/m030-acceptance/corpus-2-class-only.jsonl,tests/m030-acceptance/corpus-block.jsonl,tests/m030-acceptance/run-acceptance-battery.sh,tools/verify/p07-phase-suite.sh,.orchestrator/milestones/M030/M030-ACCEPTANCE-EVIDENCE.md,.orchestrator/milestones/M030/phases/P00/P00-SUMMARY.md,.orchestrator/milestones/M030/phases/P01/P01-SUMMARY.md,.orchestrator/milestones/M030/phases/P02/P02-SUMMARY.md,.orchestrator/milestones/M030/phases/P03/P03-SUMMARY.md,.orchestrator/milestones/M030/phases/P04/P04-SUMMARY.md,.orchestrator/milestones/M030/phases/P05/P05-SUMMARY.md,.orchestrator/milestones/M030/phases/P06/P06-SUMMARY.md,.orchestrator/milestones/M030/phases/P07/P07-SUMMARY.md"
key_decisions:
  - "D-A1 4-verdict closed-enum (flip_recommendation ∈ {ready, partially_ready, block, evidence_insufficient}); D-A2 programmatic shadow-compare invocation as the FR-9 flip-gate (live routing refuses adapter call if corpus below threshold; emits override_source=shadow_gate_blocked); D-A3 partial-flip safety (only smart-defaulted classes may be enumerated in withheld_classes — fast/balanced classes flip wholesale or block); D-A4 mechanical independence (P00 fixture corpus committed BEFORE P01 classify-task.sh authored — independence-by-construction; verifier graduates post-P01 to git-log ordering check); D-A5 kill-switch precedence over min_tier (model_routing_enabled: false short-circuits before flip-gate or escalation logic; stderr names the bypassed min_tier); D-A6 cost_rates SSOT in templates/model-routing.yml (single source of truth for rollup + counterfactual computation); D-A7 / SC-3a write-path correctness (every shadow record's model_routed agrees with an independent classifier run on the same plan; classifier_confidence emitted end-to-end in P02 not deferred to P03); D-A8 escalation cap CON-5 (3 attempts max: fast → balanced → smart → escalation_cap_hit; no fourth record); D-A9 anomaly-record JSONL append-only convention (separate file .orchestrator/anomalies.jsonl preserves CON-6 dispatch-stream invariant on execution-log.jsonl); P01 classifier-confidence stability metric definition (rolling variance ≤ 0.10 + N=20 minimum class-coverage; consumed end-to-end by shadow-compare.sh); P02 4-verdict closed-enum + shadow-corpus path-resolution priority (env var M030_SHADOW_COMPARE_CORPUS → --corpus flag → default); P03 override-source closed-enum closure + kill-switch precedence over min_tier (compound config tested via SC-7a); P04 escalation-cap programmatic enforcement + flip-gate refusal of below-threshold corpus; P05 cost_rates-absent fallback (warning + zero-savings line) + ORCHESTRATOR_ROOT carve-out for footer fixtures; P06 #Q-4 threshold default (pass_rate 0.50 + min_class_sample 10) + env-only JSONL emit path (M030_ANOMALIES_JSONL_PATH default .orchestrator/anomalies.jsonl) instead of CLI flag; P07 4-task split (corpus + battery + ledger + close-ceremony) + structurally-distinct T04 ceremony fusing phase + milestone close into one atomic commit"
patterns_established:
  - "pre-implementation fixture corpus + version-controlled labels (P00 — closed-milestone-only sourcing avoids in-flight bias; rubric application per-plan with judgment, no automated keyword pre-pass; rationale field as audit trail); classifier-as-pure-bash + heuristic-table-as-SSOT (P01 — deterministic <100ms no-network with no jq/python3 hard dependency; bash 3.2 compatible); 4-verdict closed-enum shadow-compare (P02 — flip_recommendation always ∈ {ready, partially_ready, block, evidence_insufficient}, no string interpolation); additive-jsonl-schema CON-2/FR-19 preserved across 6 schema extensions (P02 added 4 fields, P03 added 1, P04 added 3, P06 added 1 — all additive, all preserve byte-equality on shadow-off branch); pre-amendment golden-baseline + delegate-and-pass-through cross-phase wrappers (P05/T01 inverted P04/T01 pattern: capture HEAD output BEFORE amendment, delegate post-amendment SC-11 verifier to upstream P02 wrapper); ORCHESTRATOR_ROOT carve-out for fixture-routing without modifying the surface's resolver (P05/T01); additive-field-on-shadow-on-emit lineage (P02/T02 + P04/T03 + P06/T02 — single field appended to printf format string + arg list on shadow-on branch only; shadow-off branch byte-untouched; SC-11 contract via P02 delegate-and-pass-through wrapper); env-var-seam-for-{shadow-mode,corpus-path,routing-table-path,anomaly-jsonl-path,allow-dirty-mark} convention (M030_SHADOW_MODE / M030_SHADOW_COMPARE_CORPUS / M030_ROUTING_TABLE_PATH / M030_ANOMALIES_JSONL_PATH / ORCHESTRATOR_ALLOW_DIRTY_MARK); phase-suite straight-line aggregator pattern (P02–P07 all share the no-loops-no-eval-no-compound-chains-AD-19-clean shape; per-gate parallel scalars + bash <path> + rc=$? + if-block accumulation; single SUMMARY: line); plan-amendment-not-task-reopen pattern applied at every phase close for artifact-grep predicate divergences (line-count relaxation + key-link source inversion to forward-direction reachable file with parenthetical reason); structurally-distinct-T04 ceremony fusing phase + milestone close (P07 only — when final phase IS milestone-close gate); idempotent corpus synthesizer + sha256-equality verifier (P05/T01 + P06/T01 + P07/T01 lineage); acceptance-battery as straight-line SC delegator (P07/T02); one-shot evidence ledger discipline (P07/T03 — committed snapshot at milestone close, never regenerated dynamically; operators re-run the contract for fresh stdout); milestone-grain unit_close convention (P07/T04 — first-of-kind record introduced; granularity=milestone + phase_count=N); pre-amendment-tolerant verifier pattern (P03/T01 — same verifier flips from tolerant to strict as deliverable lands); CON-3 closure verifier (P02/P03/P04 — runtime awk-extraction of resolution table from templates/model-routing.yml prevents hardcoded model IDs; HEAD-vs-WT per-pattern grep counts mechanically re-checkable); append-only invariant on JSONL via inode preservation + first-N-lines bit-identity + line-count delta = +1 (P02/P04/P06)"
drill_down_paths:
  - ".orchestrator/milestones/M030/phases/P00/P00-SUMMARY.md, .orchestrator/milestones/M030/phases/P01/P01-SUMMARY.md, .orchestrator/milestones/M030/phases/P02/P02-SUMMARY.md, .orchestrator/milestones/M030/phases/P03/P03-SUMMARY.md, .orchestrator/milestones/M030/phases/P04/P04-SUMMARY.md, .orchestrator/milestones/M030/phases/P05/P05-SUMMARY.md, .orchestrator/milestones/M030/phases/P06/P06-SUMMARY.md, .orchestrator/milestones/M030/phases/P07/P07-SUMMARY.md, .orchestrator/milestones/M030/M030-ACCEPTANCE-EVIDENCE.md"
duration: "1526m"
verification_result: "pass"
completed_at: "2026-05-01T03:26:36Z"
observability_surfaces:
  - "scripts/diagnostics/metrics-rollup.sh --by-model (per-tier dispatch counts + aggregated_cost_usd + counterfactual_all_smart_cost_usd lines); scripts/diagnostics/efficiency-footer.sh model_mix: line; scripts/diagnostics/doctor.sh --config-check (routing-table syntax validation with file:line diagnostics); scripts/diagnostics/check-anomalies.sh model_routing_regression text + JSONL record (rolling-window per-class verifier-fail-rate); scripts/diagnostics/shadow-compare.sh 4-verdict flip_recommendation= line; .orchestrator/milestones/M030/M030-ACCEPTANCE-EVIDENCE.md one-shot ledger; tests/m030-acceptance/run-acceptance-battery.sh BATTERY: pass=N fail=N canonical line; classifier-confidence stability metric in shadow-compare per-class evidence lines; .orchestrator/anomalies.jsonl append-only stream"
---

M030 (adaptive model selection) closed: a task-character classifier routes each dispatch to the cheapest single model that can do the job correctly, with a two-layer safety story — pre-flip classifier-calibration evidence (FR-7/FR-8 shadow corpus + 4-verdict shadow-compare) and post-flip regression-detection mesh (FR-10 verifier-fail escalation + FR-18 per-class anomaly detection + CON-4 operator kill switch). Eight phases (P00–P07) closed end-to-end across 1526 minutes; 14 success criteria (SC-1 through SC-11 inclusive of SC-2a/SC-3a/SC-7a) verified via the M030 acceptance battery (`tests/m030-acceptance/run-acceptance-battery.sh` → `BATTERY: pass=22 fail=0`); evidence captured at `.orchestrator/milestones/M030/M030-ACCEPTANCE-EVIDENCE.md`.

P00 (Fixture corpus + ground-truth labels, 180m) shipped a 40-entry hand-labeled classifier ground-truth corpus at `tests/fixtures/m030-classifier-corpus/labels.yml` (20 mechanical / 15 standard / 5 novel) drawn from this repo's closed-milestone history, methodology + D-A4 compliance README, and 5 verifiers under `tools/verify/p00-*` aggregated by `p00-phase-suite.sh`. The labeling preceded `classify-task.sh` on disk (D-A4 mechanical-independence-by-construction). Patterns: project-owned verifier path (`tools/verify/`) for AD-19 install-clobber containment; SELECTION-NOTES → README graduation at phase close; absence-check-as-load-bearing-D-A4-proxy.

P01 (Classifier + routing table + cost_rates, 240m) shipped `scripts/dispatch/classify-task.sh` (deterministic <100ms no-network bash classifier emitting `character=` + `confidence=` lines), `templates/model-routing.yml` SSOT (routing + per-runtime resolution + `cost_rates:` section per D-A6/CON-3), `references/model-routing.md` operator docs (with aggressive overlay), and the classifier-confidence stability metric definition (rolling variance ≤ 0.10 + N=20 minimum class-coverage) consumed by P02's shadow-compare. Closes spec #Q-3 (stability-metric definition). Patterns: classifier-as-pure-bash (no jq/python3 hard dependency, bash 3.2 compatible); awk section-walker for routing+resolution extraction at dispatch time.

P02 (Dispatch integration + shadow-mode JSONL + shadow-compare, 245m) amended `scripts/dispatch/dispatch-interface.sh` with a CC-only shadow-mode hook (gated on `M030_SHADOW_MODE=1 && CLAUDECODE=1`) emitting 5 additive JSONL fields (`model_routed`, `model_used`, `partial_flip_active`, `withheld_classes`, `classifier_confidence`); shipped `scripts/diagnostics/shadow-compare.sh` 4-verdict closed-enum aggregator emitting exactly one `flip_recommendation=` line drawn from `{ready, partially_ready, block, evidence_insufficient}` (D-A1); pinned partial-flip safety (D-A3: only smart-defaulted classes may be enumerated in `withheld_classes`); shipped 9-gate `p02-phase-suite.sh`. Patterns: dual-format-string emit branches preserve byte-equality across additive schema changes; CON-3 closure verifier compares HEAD-vs-WT per-pattern grep counts; append-only JSONL via inode preservation + first-N-lines bit-identity + line-count delta = +1; classifier-confidence end-to-end-in-P02-not-deferred-to-P03.

P03 (Operator overrides + kill switch, 238m) amended `dispatch-interface.sh` with the kill-switch → plan-frontmatter → milestone-floor → none precedence chain (FR-4/FR-11/FR-12/FR-13/FR-14); added `override_source` closed-enum field (`plan_frontmatter|milestone_floor|disabled|shadow_gate_blocked`); pinned D-A5 kill-switch precedence over `min_tier` (compound config tested via SC-7a fixture); landed `references/model-routing.md ## Operator Overrides` section. Patterns: pre-amendment-tolerant verifier pattern (zero-tokens-PASS + exactly-one-with-enum-valid-value-PASS — same verifier flips from tolerant to strict as the deliverable lands); override-resolution-before-routing-extraction shape; runtime-extraction of expected-literal from SSOT via awk section-walker.

P04 (Live routing + verifier-fail escalation + flip-gate enforcement, 257m) amended `dispatch-interface.sh` with the live-routing branch (FR-9 with programmatic shadow-compare invocation per D-A2; per-class partial-flip authorization per D-A3) + escalation logic (FR-10 with CON-5 hard-cap of 3 attempts: fast → balanced → smart → `escalation_cap_hit`); added 3 JSONL fields (`escalation_count`, `escalation_reason`, `shadow_gate_blocked` verdict) + the `escalation_cap_hit` record type. Patterns: live-routing branch short-circuits at kill-switch check before flip-gate or escalation logic (CON-4 amended); FR-9 programmatic flip-gate enforces shadow-corpus threshold mechanically; escalation produces NEW records with new timestamps (CON-6 append-only invariant preserved).

P05 (M027 surface integration, 147m) amended `scripts/diagnostics/metrics-rollup.sh` with `--by-model` flag (per-tier dispatch counts + aggregated_cost_usd + counterfactual_all_smart_cost_usd lines when `cost_rates:` defined; "cost rates not configured" warning + zero-savings line when absent), `scripts/diagnostics/efficiency-footer.sh` with `model_mix:` line, `scripts/diagnostics/doctor.sh` with `--config-check` flag (routing-table syntax with file:line diagnostics on malformed input). Patterns: pre-amendment golden-baseline + delegate-and-pass-through cross-phase wrappers (inverted P04/T01 pattern); ORCHESTRATOR_ROOT carve-out for fixture-routing without modifying the surface's resolver; cost_rates-absent fallback gracefully degrades (warning + zero-savings line, exit 0).

P06 (Anomaly-driven regression detection, 49m) amended `scripts/diagnostics/check-anomalies.sh` with a `_ca_model_routing_regression_check` function (rolling-window per-class verifier-fail-rate; emits `FLAGGED model_routing_regression class=<X> class_pass_rate=<R> sample=<N> threshold=<T>` text + JSONL record only when `class_sample >= min_class_sample` AND `class_pass_rate < pass_rate_threshold`); added `character` additive field to dispatch-interface shadow-on emit branch (extending the additive-field-on-shadow-on-emit lineage from P02/T02 + P04/T03; SC-11 byte-equality preserved); env-only JSONL emit path (`M030_ANOMALIES_JSONL_PATH` default `.orchestrator/anomalies.jsonl`) keeps surface narrow. Pre-P06 records (no `character` field) silently skipped — preserving SC-11 byte-equality on every existing pre-amendment fixture. Patterns: append-only anomalies.jsonl emit via `>>` redirect with `mkdir -p` guard (CON-6 invariant extended via separate-file boundary); rolling-window-per-class-verifier-fail-rate-check shape.

P07 (E2E shadow-corpus + flip-gate validation — milestone close gate, 170m) shipped `tests/m030-acceptance/shadow-corpus-fixtures.sh` idempotent synthesizer + 4 corpora (`corpus-50-per-class.jsonl` 150 records / 50 per class; `corpus-zero.jsonl` 0 records; `corpus-2-class-only.jsonl` 100 records: 50 mechanical + 50 standard + 0 novel; `corpus-block.jsonl` 60 records below per-class threshold), `tests/m030-acceptance/run-acceptance-battery.sh` end-to-end SC runner emitting `BATTERY: pass=22 fail=0` across all 14 SCs (SC-1 through SC-11 inclusive of SC-2a/SC-3a/SC-7a; SC-2 / SC-8 / SC-11 carry multiple sub-gates), `.orchestrator/milestones/M030/M030-ACCEPTANCE-EVIDENCE.md` one-shot evidence ledger frozen at the green-run state (`recorded_at: 2026-05-01T02:57:14Z`, runner SHA `c628bc4`, synthesizer SHA `a3cf00a`), 10 P07 verifiers under `tools/verify/p07-*.sh`, the 9-gate `p07-phase-suite.sh` aggregator, and the milestone-close ceremony (P07-SUMMARY.md + phase-grain `unit_close` + `M030-VALIDATED` marker via `mark-complete.sh` + this `M030-SUMMARY.md` + milestone-grain `unit_close` + recent-changes dual-write + close commit + final `validate-milestone.sh` clean pass). T04 is structurally distinct from P02–P06 closes — it fuses phase + milestone close into one atomic commit because the M030-VALIDATED contract requires every `P0[0-7]-SUMMARY.md` present simultaneously, and splitting creates a fragile half-closed state. Patterns: structurally-distinct-T04 fusing phase + milestone close; one-shot-evidence-ledger discipline; acceptance battery as straight-line SC delegator; milestone-grain unit_close convention introduced (granularity=milestone + phase_count=N — first-of-kind on disk).

Verification: `bash tests/m030-acceptance/run-acceptance-battery.sh` reports `BATTERY: pass=22 fail=0`, exit 0. `bash tools/verify/p07-phase-suite.sh` reports `SUMMARY: p07-phase-suite.sh pass=9 fail=0`, exit 0. `bash scripts/verify/validate-milestone.sh .orchestrator/milestones/M030` reports `VALIDATE: PASS — N/N checks passed`, exit 0. Per-phase suites green at every phase (P00 5/5, P02 9/9, P03 8/8, P04 12/12, P05 7/7, P06 8/8, P07 9/9). M030-ACCEPTANCE-EVIDENCE.md cited 24 distinct verifier paths, all on-disk-resolved.

Cross-cutting outcomes:

- **Adaptive routing live, shadow-mode default**: `scripts/dispatch/dispatch-interface.sh` consults `scripts/dispatch/classify-task.sh` for every dispatch, emits the additive shadow-mode JSONL fields, and (when `model_routing.live: true` is operator-set) routes through the FR-9 programmatic flip-gate before calling any adapter. Default posture is shadow-mode — live routing is operator-opt-in.
- **Operator-override surface complete**: kill-switch (`model_routing_enabled: false`) → plan-frontmatter (`model_override:`) → milestone-floor (`min_tier:`) precedence chain implemented end-to-end with `override_source` JSONL field for audit. CON-4 amended kill-switch wins over `min_tier` per D-A5.
- **Two-layer safety story locked**: pre-flip evidence via shadow-compare 4-verdict aggregator + per-class classifier-confidence stability metric; post-flip regression detection via verifier-fail escalation (FR-10 with CON-5 hard-cap), per-class anomaly detection (FR-18 with `model_routing_regression` record), and operator kill switch (CON-4).
- **Cost observability surfaces extend M027 additively**: `metrics-rollup --by-model` + `efficiency-footer model_mix:` + `doctor --config-check` ship without breaking byte-equality on pre-M030 JSONL fixtures (SC-11 contract preserved at every phase via delegate-and-pass-through wrappers).
- **CON-3 symbolic-tier closure preserved end-to-end**: zero new provider model-ID literals introduced across all 8 phases; every concrete model identifier resolves through `templates/model-routing.yml` at the adapter boundary.
- **CC-only launch posture preserved per FR-6**: Codex CLI + Cursor adapters resolve any symbolic tier to `inherit`; multi-runtime parity is M009 post-launch.
- **Acceptance battery green at acceptance scale**: 14 SCs verified via 22 verifier invocations against 4 synthetic corpora (550 total records); the FR-9 flip-gate refuses below-threshold corpora mechanically; the cross-surface coherence gate confirms metrics-rollup + footer + check-anomalies stay coherent across 150 dispatches.

Reusable patterns (carried across phases):

- **Phase-suite straight-line aggregator** (P02–P07 share the no-loops-no-eval-no-compound-chains-AD-19-clean shape — per-gate parallel scalars + `bash <path>` + `rc=$?` + if-block accumulation + single `SUMMARY:` line).
- **Additive-field-on-shadow-on-emit lineage** (P02/T02 + P04/T03 + P06/T02 — single field appended to printf format string + arg list on shadow-on branch only; shadow-off branch byte-untouched; SC-11 contract via P02 delegate-and-pass-through wrapper).
- **Pre-amendment golden-baseline + delegate-and-pass-through cross-phase wrappers** (P05/T01 + P06/T01 — capture HEAD output BEFORE amendment, delegate post-amendment SC-11 verifier to upstream wrapper for byte-equality re-check).
- **ORCHESTRATOR_ROOT carve-out for fixture-routing** (P05/T01 + P02/T01 — exploit env-var resolver for staged fixture without modifying the surface's path resolution).
- **Plan-amendment-not-task-reopen** (every phase close — artifact-grep predicate divergences resolved via plan amendments with parenthetical reasons, not by re-opening completed tasks).
- **Idempotent corpus synthesizer + sha256-equality verifier** (P05/T01 + P06/T01 + P07/T01 lineage).
- **One-shot evidence ledger discipline** (P07/T03 — committed snapshot at milestone close, never regenerated dynamically; operators re-run the contract for fresh stdout).
- **Structurally-distinct-T04 fusing phase + milestone close** (P07 only — when final phase IS milestone-close gate; reusable convention for any future milestone whose final-phase-task IS the milestone-close gate).

Real-app smoke test pending (Plan-Time Discipline rule 5): M030 ships against synthetic acceptance corpora — `tests/m030-acceptance/corpus-50-per-class.jsonl` is hand-crafted with 50 records per class, not drawn from real-world dispatch traces. Real shadow-mode dispatches against pre-launch milestones (M031–M035) are the n=1 in-the-wild validator. Operators activating live routing should run >=50 shadow-mode dispatches per class against their own milestone history before flipping `model_routing.live: true` in `.orchestrator/config.yml`. The FR-9 programmatic flip-gate enforces this mechanically: insufficient corpus = `shadow_gate_blocked` JSONL record, no adapter call. The acceptance battery verifies the gate logic at acceptance scale; live activation is a downstream operator decision and the milestone close does NOT activate live routing in any operator's project.

Constitution Principle II (Evidence Before Claims) honored: M030-SUMMARY.md provenance points to `.orchestrator/milestones/M030/M030-ACCEPTANCE-EVIDENCE.md` + the green-run `BATTERY: pass=22 fail=0` line as the empirical basis for the closure claim. The shadow-mode-default + FR-9 flip-gate posture honors Principle II's "no claims that cannot be mechanically verified" requirement — M030 doesn't claim cross-model equivalence pre-flip; it claims classifier-confidence calibration pre-flip + post-flip regression detection (FR-10/FR-18/CON-4).

Roadmap impact: M030 ships shadow-mode by default. Operators activate live routing via `.orchestrator/config.yml model_routing.live: true`; the FR-9 programmatic flip-gate refuses live routing if the shadow corpus is below threshold. The flip is per-project + reversible. Pre-launch CC-only posture preserved — Codex CLI + Cursor adapters resolve any symbolic tier to `inherit` per FR-6. Multi-runtime parity is M009 post-launch.

What's next: M031 (right-sized entry) restores knowledge-graph + compression access for Quick intensity (today `commands/dispatch.md:21` skips `build-context.sh` — load-bearing leak that bypasses the M030 routing layer for Quick-intensity tasks). M031 + M030 compose as the thrift-and-ergonomics pair. M032 (wiki distribution + init integration) and M033 (project onboarding) follow. The remaining pre-launch sequence is **M031 → M032 → M033 → M029 → M035 → launch**.
