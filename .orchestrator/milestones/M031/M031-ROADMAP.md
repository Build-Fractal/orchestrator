---
schema_version: "1.0"
type: roadmap
milestone: "M031"
feature_ref: "034-right-sized-entry"
feature_spec: "specs/034-right-sized-entry/spec.md"
vision: "The orchestrator's small-task path becomes knowledge-rich and ergonomic — Quick dispatches inject knowledge unconditionally with traversal-aggressiveness as the dial (1-hop / 2-hop / full provenance), a Tier A+ middle flow chains research → plan → build for medium tasks, and a universal `orchestrator:do <task>` entry auto-routes via the M024 classifier with confidence-gated approval."
tier: "C"
created_at: "2026-05-01"
updated_at: "2026-05-01"
---

## Phases

- [x] **P00**: Empirical baseline + foundation — "Running `bash tests/m031-acceptance/empirical-baseline.sh` against the 20-task fixture corpus emits a JSONL record per task with both pre-M031 and post-M031 Quick total task tokens + verifier pass rate; the harness exits 0 if the median post-M031 run uses fewer total task tokens AND verifier pass rate is equal-or-higher (CON-5 hypothesis confirmed); `tests/m031-acceptance/fixtures/empirical-baseline/CORPUS-MANIFEST.md` declares the AD-15 stratification (5 historical 2H/2M/1L + 5 synthetic edge-case + 10 spread across 3 categories); the pre-M031 stub is preserved at `fixtures/empirical-baseline/pre-m031-stub.sh`; `references/RUNTIME-ASSUMPTIONS.md` records the M018 tier-1 threshold value; spec.md is amended in-place to fold AD-1..AD-20 (with pinned phase IDs); config defaults (`quick_knowledge_token_budget`, `entry_routing_confidence_floor`, `tier_a_plus_prompt_summary_lines`) are pinned; SC-13 Option A vs B is selected per observed CI git-history availability."
  - Risk: medium
  - Depends: none
  - Boundary Map:
    - Produces: `tests/m031-acceptance/fixtures/empirical-baseline/` (20-task fixture corpus, CORPUS-MANIFEST.md per AD-15, pre-m031-stub.sh per AD-14); `tests/m031-acceptance/empirical-baseline.sh` (FR-18 harness, JSONL emitter); `tests/m031-acceptance/verify-baseline-ordering.sh` (SC-13 Option B git-history check per AD-12, with Option A protocol-note fallback); pinned defaults for `quick_knowledge_token_budget` (proposal 800, AD-5 may revise), `entry_routing_confidence_floor` (proposal 0.7, OQ-4), `tier_a_plus_prompt_summary_lines` (AD-20, default 8); `references/RUNTIME-ASSUMPTIONS.md` documenting M018 tier-1 threshold value (AD-17 P00 precondition); amended `specs/034-right-sized-entry/spec.md` with AD-1..AD-20 folded in and SC-* renumbered to N≥15 (operator-deferred to P00 first task per CONTEXT.md); empirical-baseline JSONL ledger committed under `tests/m031-acceptance/fixtures/empirical-baseline/<runtime>-baseline.jsonl` (one-shot capture of pre-M031 and post-M031 records simultaneously while both code paths are live, per AD-14)
    - Consumes: nothing (foundational; the corpus must commit + capture pre-M031 records BEFORE P01's FR-4 merge collapses the skip branch — there is no second window per AD-14)

- [x] **P01**: Knowledge-unconditional Quick + `--meta-out` sidecar — "Running `bash scripts/dispatch/build-context.sh --profile=quick --task-plan <fixture> --out /tmp/payload.md --meta-out /tmp/meta.json` emits a Quick payload (touched-files-only scope, 1-hop traversal, no Decisions section, glossary slice over touched terms only) and a JSON sidecar with `{mem_count, total_tokens, profile, compression_applied, snip_applied}`; M018 tier-1 + tier-2 records appear in JSONL when the payload exceeds thresholds; the median `knowledge_section_tokens` across the P00 corpus is ≤ `quick_knowledge_token_budget`; `commands/dispatch.md` no longer contains `Skip payload assembly` for Quick; `bash tests/m031-acceptance/test-quick-injects-knowledge.sh` and `test-compression-applies-to-quick.sh` exit 0."
  - Risk: high
  - Depends: P00
  - Boundary Map:
    - Produces: amended `scripts/dispatch/build-context.sh` (`--profile=quick|standard|full` flag per FR-2 + per-profile scope/traversal/decisions/glossary policy + `--meta-out <file>` JSON sidecar per AD-11); amended `commands/dispatch.md:21` (FR-4 — replaces "Skip payload assembly" branch with Quick-profile-with-knowledge); amended `templates/orchestrator-config-default.yml` adding `quick_knowledge_token_budget` (FR-5); JSONL `payload_breakdown` schema additions for `profile`, `knowledge_section_tokens`, compression-tier counters (CON-1 invariant — every dispatch path emits a payload_breakdown record); `tests/m031-acceptance/test-quick-injects-knowledge.sh` (SC-1); `tests/m031-acceptance/test-compression-applies-to-quick.sh` (SC-3, prescriptive per AD-17 — fixture constructs payload exceeding tier-1 threshold); `tests/m031-acceptance/test-build-context-profile.sh` (SC-2 amended per AD-13 — gates on tier-2 snip record + Knowledge section ≤ budget, no ±20% clause); `tests/m031-acceptance/test-quick-budget-median.sh` (SC-15 per AD-18 — median absolute compliance independent of pre-M031 baseline)
    - Consumes: P00 fixture corpus (for SC-15 median compliance + SC-2/SC-3 fixture inputs); P00 RUNTIME-ASSUMPTIONS.md M018 tier-1 threshold value (SC-3 fixture precondition); P00 pinned `quick_knowledge_token_budget` default; P00 pre-M031 stub at `fixtures/empirical-baseline/pre-m031-stub.sh` (preserved post-FR-4 for the SC-11 stored-record comparison)

- [x] **P02**: Tier A+ middle flow — "Running `bash scripts/intake/shape-detect.sh < tests/m031-acceptance/fixtures/tier-a-plus-input.txt` emits `tier_a_plus`; running `bash tests/m031-acceptance/test-tier-a-plus-flow.sh` produces exactly three sequential dispatches with role payloads `research` / `plan` / `build`, deterministic output paths at `.orchestrator/tier-a-plus/<task-slug>/research.md` and `.../plan.md` (slug = first 40 chars lower-cased, hyphenated, alphanumerics-only, 4-char SHA-1 collision suffix per AD-10), one approval prompt before plan dispatch (zero under `--yes`), no `.orchestrator/milestones/M###/` scaffolding created, no `orchestrator:auto` lock acquired; the prompt UX integration test `test-tier-a-plus-prompt-ux.sh` (SC-16 per AD-20) verifies plain-language framing, inline 8-line research summary, three named options (`y`/`n`/`c`), `(N more lines at <path>)` ellipsis when research exceeds the inline budget, and `research: <path>` audit line on stderr under `--yes`; `tests/m031-acceptance/fixtures/FIXTURE-PROVENANCE.md` documents the historical JSONL `unit_close` record the Tier A+ classifier fixture is grounded in (AD-16)."
  - Risk: high
  - Depends: P01
  - Boundary Map:
    - Produces: amended `scripts/intake/paragraph-classify.sh` and `scripts/intake/shape-detect.sh` (FR-6 — `tier_a_plus` verdict surface, additive to M024 verdict set per AD-2 / CON-3); amended `scripts/intake/route-to-dispatch.sh` (FR-7 — chains three dispatches sequentially, no auto/roadmap/consolidate per CON-4 / DC-4); `templates/dispatch-role-research.md`, `templates/dispatch-role-plan.md`, `templates/dispatch-role-build.md` (FR-8 — three prescriptive role templates with explicit output shapes); Tier A+ approval prompt implementation honoring AD-7 + AD-20 (plain-language framing, inline research summary truncated to `tier_a_plus_prompt_summary_lines`, three-option response, `--yes` skip with stderr audit line, resume-vs-rerun visibility against existing `research.md`); deterministic task-slug derivation library (AD-10); `tests/m031-acceptance/fixtures/FIXTURE-PROVENANCE.md` (AD-16 normative — historical JSONL grounding); `tests/m031-acceptance/test-tier-a-plus-flow.sh` (SC-6); `tests/m031-acceptance/test-tier-a-plus-classifier.sh` (SC-5 with provenance check per AD-16); `tests/m031-acceptance/test-tier-a-plus-prompt-ux.sh` (SC-16 per AD-20); JSONL `unit_close` schema additions for `tier_a_plus_role` (`research|plan|build`) and `aborted` flag
    - Consumes: P01 `build-context.sh --profile` flag (each Tier A+ dispatch injects per-profile knowledge per US1); P01 `--meta-out` sidecar (research/plan/build dispatches each emit one); P01 amended `commands/dispatch.md` Quick branch (Tier A+ research and build dispatches default to Quick profile unless task plan declares otherwise); P00 pinned `tier_a_plus_prompt_summary_lines` default

- [x] **P03**: Universal entry `orchestrator:do <task>` — "Running `orchestrator:do \"fix typo in foo.md\"` against a high-confidence Tier A degenerate verdict produces zero approval prompts, exactly one dispatch, one stderr summary line of shape `doing: <task> — knowledge: N MEMs / X tokens` (N and X read from the P01 `--meta-out` sidecar), and a final diff; running against a low-confidence verdict (below `entry_routing_confidence_floor`) produces an explicit Tier A vs Tier B prompt and records the chosen shape in the `unit_close` record; running against a Tier A+ verdict hands off to P02's three-dispatch flow with one approval prompt before plan; running against a Tier B/C verdict routes to today's `orchestrator:specify` flow unchanged; `bash tests/m031-acceptance/test-universal-entry-trivial.sh` and `test-universal-entry-lowconf.sh` exit 0."
  - Risk: medium
  - Depends: P01, P02
  - Boundary Map:
    - Produces: `commands/do.md` (FR-10 — new universal-entry skill registered as `orchestrator:do <task>` per AD-6 OQ-1 resolution; verbless `orchestrator <task>` reserved for post-M009 runtime parity); FR-11 confidence-gate logic reading `entry_routing_confidence_floor` from config; FR-12 fast-path stderr summary line implementation (consumes P01 `--meta-out` sidecar); FR-13 Tier B/C passthrough to existing `orchestrator:evaluate` / `orchestrator:specify` flow; amended `templates/orchestrator-config-default.yml` adding `entry_routing_confidence_floor` knob; M024 classifier confidence-emission check (A-2 — extends M024 to emit confidence in the verdict record if not already present); `tests/m031-acceptance/test-universal-entry-trivial.sh` (SC-7); `tests/m031-acceptance/test-universal-entry-lowconf.sh` (SC-8)
    - Consumes: P01 `--meta-out` JSON sidecar (FR-12 reads `mem_count` as N and `total_tokens` as X); P01 Quick-profile knowledge inject (universal entry's fast-path dispatches use Quick); P02 Tier A+ router (FR-7 chain) — universal entry hands off `tier_a_plus` verdicts to the P02 router; P02 task-slug derivation; P00 pinned `entry_routing_confidence_floor` default

- [x] **P04**: Drift fix + observability + comms + acceptance battery aggregator — "Running `bash tests/m031-acceptance/doc-drift-verifier.sh` exits 0 (zero matches for 'no orchestrator overhead' / 'Do NOT create any orchestrator directory' in `commands/evaluate.md`; canonical Tier A description present in both `commands/evaluate.md` and `references/tier-definitions.md`); running `bash tests/m031-acceptance/scope-guard.sh` exits 0 (M031's diff touches no path under `knowledge/**` schema, `scripts/cost/`, `scripts/dispatch/adapters/router/`, `scripts/auto/loop/`); `grep auto_proceed templates/orchestrator-config-default.yml` shows `auto_proceed: true`; running `orchestrator:doctor` against a project whose `.orchestrator/config.yml` predates M031 emits a one-time compound-change message naming both the auto-proceed flip AND unconditional Quick injection (AD-9); a 7-dispatch fixture with rolling median Quick injection > budget × 1.1 produces a `QUICK_BUDGET_DRIFT` warning record in the M027 efficiency-footer JSONL stream (AD-19); CHANGELOG.md entry for M031 names the compound behavioral change; running `bash tests/m031-acceptance/run-acceptance-battery.sh` outputs `BATTERY: pass=N fail=0` with N≥15 (SC-1..SC-13 per AD-12 selection + SC-15 + SC-16; SC-14 is the aggregator itself)."
  - Risk: medium
  - Depends: P01, P02, P03
  - Boundary Map:
    - Produces: amended `commands/evaluate.md` (FR-14 — drops pre-M024 "no orchestrator overhead" / "Do NOT create any orchestrator directory" section; canonical Tier A description = "single dispatch with knowledge + compression via the Quick profile"); amended `references/tier-definitions.md` (FR-15 — matches evaluate.md post-fix; explicitly states `.orchestrator/` always present, only `.orchestrator/milestones/M###/` scaffolding is conditional); amended `templates/orchestrator-config-default.yml` flipping `auto_proceed` default to `true` (FR-16 / AD-8); amended `scripts/lifecycle/doctor.sh` emitting one-time M031 compound-change message (AD-9 — names auto-proceed flip + unconditional Quick injection + recovery path; detects pre-M031 config by absence of `quick_knowledge_token_budget`); amended `scripts/diagnostics/efficiency-footer.sh` (or equivalent M027 surface) emitting `QUICK_BUDGET_DRIFT` warning when rolling median across 7 consecutive Quick dispatches > budget × 1.1 (AD-19, non-blocking); `tests/m031-acceptance/doc-drift-verifier.sh` (FR-17 / SC-9, POSIX-bash per CON-6 / DC-7); `tests/m031-acceptance/scope-guard.sh` (SC-12 — strict allow-list verifier); `tests/m031-acceptance/test-auto-proceed-default.sh` (SC-10); `tests/m031-acceptance/test-doctor-compound-change.sh` (AD-9 verification); `tests/m031-acceptance/test-budget-drift-warning.sh` (AD-19 verification); `tests/m031-acceptance/run-acceptance-battery.sh` (SC-14 aggregator emitting `BATTERY: pass=N fail=0`); `CHANGELOG.md` M031 entry naming the compound behavioral change (AD-9 normative requirement); `.orchestrator/milestones/M031/M031-ACCEPTANCE-EVIDENCE.md` (one-shot evidence ledger of the green run, parallels M030 convention)
    - Consumes: P01 `payload_breakdown` JSONL records (efficiency-footer reads `knowledge_section_tokens` for AD-19 rolling-median check); P01 amended `commands/dispatch.md` (FR-14 evaluate.md must align with FR-4's Quick-with-knowledge framing); P01 `quick_knowledge_token_budget` config knob (AD-19 reads it); P02 Tier A+ test outputs (battery aggregator runs SC-5/SC-6/SC-16); P03 universal-entry test outputs (battery aggregator runs SC-7/SC-8); every prior phase's acceptance script (the aggregator chains them all)

## Cross-Cutting Concerns

- **CON-1 invariant — every dispatch path emits a `payload_breakdown` JSONL record** — affects P01, P02, P03. P01 establishes the invariant via `build-context.sh` always running; P02's three Tier A+ dispatches each emit one; P03's universal-entry dispatch emits one. There is no "skip context" exit anywhere; only "scope it tighter" via `--profile`. Plan-phase for P02 + P03 MUST verify the invariant against fixture-replay JSONL (no missing records).
- **`build-context.sh --meta-out` sidecar interface (AD-11 / DC-9)** — affects P01, P02, P03. P01 ships the sidecar with the minimum schema `{mem_count, total_tokens, profile, compression_applied, snip_applied}`; P02's Tier A+ research/plan/build dispatches each emit one; P03's universal entry stderr summary line reads from it. Schema changes are breaking; plan-phase for P02 + P03 MUST treat additions as additive and reserve breaking changes for M029 / M036 negotiation.
- **Knowledge-unconditional invariant (CON-1 / DC-3 / Principle VII)** — affects P01, P02, P03. Every dispatch produced by M031 — Quick / Standard / Full / Tier A+ research / plan / build / universal-entry fast-path — runs through `build-context.sh` and injects knowledge at the appropriate profile. Plan-phase for each consuming phase MUST grep the diff for any "skip-context" or "no-knowledge" branch as a pre-merge check.
- **Symbolic-tier closure / no parallel routing (CON-3 / AD-2)** — affects P02, P03. P02 extends M024's verdict surface additively (`tier_a_plus`); P03's universal entry consumes M024 verdicts directly. Neither phase introduces a parallel routing implementation. Plan-phase for P02 + P03 MUST grep the diff for any new classifier or routing logic outside `scripts/intake/`.
- **No new state machines / lock files (CON-4 / DC-4 / Principle XIV)** — affects P02. Tier A+ is three sequential `dispatch` calls recorded as JSONL `unit_close` records. Plan-phase for P02 MUST verify no new lock-file path, no new state-derivation rule, no new milestone-scaffolding write.
- **CON-5 empirical gate (AD-4 / DC-2 / Principle II)** — affects P00, P01. P00's empirical baseline gates P01's FR-1/2/3 merge. Plan-phase for P01 MUST confirm `verify-baseline-ordering.sh` (or AD-12 Option A protocol note) passes BEFORE P01's first `commands/dispatch.md` amendment commit lands. The pre-M031 + post-M031 record capture window closes the moment P01's FR-4 merges; AD-14 is the single-window discipline.
- **Scope guardrails (SC-12 / DC-5 / Principle XV)** — affects every phase. The Boundary write-sites table at `specs/034-right-sized-entry/spec.md:172–186` is the normative source. P04 ships `scope-guard.sh`; every phase's plan-phase MUST run it as a pre-merge check against the in-flight diff.
- **CC-only launch posture (CON-6 / DC-7)** — affects every phase. Acceptance battery scripts MUST be POSIX-bash so M009 can extend without rewrite. Plan-phase MUST NOT introduce Codex CLI / Cursor adapter logic; runtime gaps note in `references/RUNTIME-ASSUMPTIONS.md`.
- **D020 todo-token hygiene (CON-7 / DC-8)** — affects every phase's authored prose. Plan-phase + dispatch payloads MUST NOT embed the literal scaffold-placeholder open-bracket-TODO-colon byte pattern inside backticked inline code; paraphrase as "scaffold-placeholder marker" or escape the bracket. The conversus.sh gate adapter pre-flight refuses artifacts containing the pattern.
- **Spec-body fold-in (CONTEXT.md operator deferral)** — affects P00 only. AD-1..AD-20 fold into `specs/034-right-sized-entry/spec.md` AS THE FIRST P00 TASK so that downstream phases reference SC numbers (SC-15, SC-16) and AD numbers that exist on disk. Doing this post-roadmap (rather than pre-roadmap) avoids a second amendment pass once phase IDs are pinned.

## Dependency Graph

```
P00 ──→ P01 ──┬──→ P02 ──→ P03 ──┐
              │                  │
              └──────────────────┴──→ P04
```

Edges:
- `P00 → P01` — empirical baseline gates Quick-with-knowledge merge (CON-5 / AD-4 / AD-14 single-window capture)
- `P01 → P02` — Tier A+ dispatches inject knowledge via P01's `build-context.sh --profile` and consume the `--meta-out` sidecar
- `P01 → P03` — universal entry's stderr summary line (FR-12) reads N and X from P01's `--meta-out` sidecar
- `P02 → P03` — universal entry hands `tier_a_plus` verdicts to P02's three-dispatch router (FR-7)
- `P01 → P04` — P04's drift-fix language must align with P01's FR-4 Quick-with-knowledge framing; P04's AD-19 budget-drift warning reads P01's `payload_breakdown` JSONL records
- `P02 → P04` — battery aggregator (SC-14) runs P02's SC-5 / SC-6 / SC-16 acceptance scripts
- `P03 → P04` — battery aggregator runs P03's SC-7 / SC-8 acceptance scripts; P04's `commands/evaluate.md` post-fix must remain consistent with P03's universal-entry handoff to `orchestrator:specify`

## Execution Order

1. **P00** — foundation, no dependencies. Hard prerequisite by CON-5 + AD-14 single-window capture (pre-M031 + post-M031 records must be captured simultaneously while both code paths are live; the window closes the moment P01's FR-4 merges).
2. **P01** — knowledge-unconditional Quick + `--meta-out` sidecar. High risk; the load-bearing constitutional fix (Principle I + VII) and the SSOT for the cross-milestone sidecar interface (DC-9). Cannot start until P00's empirical baseline confirms the Quick-with-knowledge-wins hypothesis (or until the AD-12 Option A fallback is selected).
3. **P02** — Tier A+ middle flow. High risk because the AD-20 prompt UX is the only operator interaction in the chain. Cannot start until P01 ships `--profile` + `--meta-out`.
4. **P03** — universal entry. Medium risk; depends on P01 (sidecar) AND P02 (Tier A+ router as one routing destination).
5. **P04** — drift fix + observability + comms + acceptance battery. Medium risk; depends on P01 (drift-fix alignment, AD-19 reads JSONL) plus P02 + P03 (battery aggregator runs every phase's acceptance scripts). Milestone-close gate.

For `orchestrator:auto`: phases run strictly sequentially. There is no parallelism opportunity — every phase has at least one upstream dependency that gates start. (P02 and P03 are the only candidates for parallelism in principle, but P03 depends on P02's `tier_a_plus` router as a handoff target, so P03 cannot start until P02 closes.)

## Validation

- **No conflicting producers**: PASS. Each phase produces a disjoint set of files. `scripts/dispatch/build-context.sh` is amended only by P01. `commands/dispatch.md` is amended only by P01 (line 21 Quick branch). `commands/evaluate.md` and `references/tier-definitions.md` are amended only by P04. `scripts/intake/*.sh` are amended only by P02. `templates/orchestrator-config-default.yml` is amended additively across P01 (FR-5 `quick_knowledge_token_budget`) / P03 (FR-11 `entry_routing_confidence_floor`) / P04 (FR-16 `auto_proceed: true`); each phase's plan-phase MUST mark its amendment as additive (no overlapping keys). Three new `templates/dispatch-role-*.md` files are produced only by P02. `commands/do.md` is produced only by P03. `tests/m031-acceptance/` is populated additively across all phases — no two phases produce a file at the same path.
- **All consumed items have producers**: PASS. Every Consumes entry resolves to a Produces entry in an upstream phase. P00 has no Consumes (foundation). P01 consumes P00. P02 consumes P01. P03 consumes P01 + P02. P04 consumes P01 + P02 + P03. No unresolved-dependency entries.
- **DAG is acyclic**: PASS. The graph is a strict partial order rooted at P00 and terminating at P04. No back-edges. The single non-linear edge — `P01 → P04` skipping over P02 / P03 — does not introduce a cycle; it short-circuits the linear chain because P04's drift-fix and AD-19 budget-drift surfaces depend only on P01, not on P02 or P03.
- **Demo sentence coverage**: PASS. Each phase's demo sentence names specific script invocations, fixture paths, JSONL field assertions, or grep checks an operator can mechanically verify — not vague descriptions.
