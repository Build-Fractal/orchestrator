---
schema_version: "1.0"
type: task-summary
id: "T02"
parent: "P03"
milestone: "M031"
provides:
  - "tests/m031-acceptance/test-universal-entry-trivial.sh (120 lines bash 3.2 SC-7 acceptance test exercising do-entry.sh tier_a_degenerate fast-path via --dispatch-stub seam),tests/m031-acceptance/test-universal-entry-lowconf.sh (171 lines bash 3.2 SC-8 acceptance test exercising do-entry.sh low-confidence-prompt branch under --no-prompt-mode A and B sub-cases),tests/m031-acceptance/fixtures/do-entry-stub.sh (canned dispatch stub mirroring P02 SC-6 stub shape; positional argv branch+task+payload+sidecar; logs to ORCH_DO_ENTRY_STUB_LOG),tests/m031-acceptance/fixtures/do-entry-trivial-input.txt (4-word idea/high fixture),tests/m031-acceptance/fixtures/do-entry-lowconf-input.txt (9-word idea/low boundary-band fixture; empirically derived via shape-detect.sh first-candidate hit),tools/verify/m031-p03-test-universal-entry-trivial-shape.sh (AD-19 single-script Truth Check; pass=12 fail=0),tools/verify/m031-p03-test-universal-entry-lowconf-shape.sh (AD-19 single-script Truth Check; pass=13 fail=0)"
requires:
  - "from:P03/T01 what:scripts/intake/do-entry.sh four-branch routing driver + --dispatch-stub seam + --scratch-root + --no-prompt-mode + ORCH_DO_ENTRY_LOG env override + ORCH_DO_ENTRY_STUB_LOG support + FR-12 stderr summary line shape,from:P02 what:scripts/intake/shape-detect.sh tier_a_plus + idea/low boundary classification (8-10 word band),from:P02 what:tests/m031-acceptance/test-tier-a-plus-flow.sh stub shape reference (positional argv pattern)"
affects:
  - "T03"
key_files:
  - "tests/m031-acceptance/test-universal-entry-trivial.sh,tests/m031-acceptance/test-universal-entry-lowconf.sh,tests/m031-acceptance/fixtures/do-entry-stub.sh,tests/m031-acceptance/fixtures/do-entry-trivial-input.txt,tests/m031-acceptance/fixtures/do-entry-lowconf-input.txt,tools/verify/m031-p03-test-universal-entry-trivial-shape.sh,tools/verify/m031-p03-test-universal-entry-lowconf-shape.sh"
key_decisions:
  - "empirical low-confidence fixture chosen as 9-word idea/low boundary input (shape-detect.sh emits low at 8-10 word band) rather than 30-32 or 78-80 tier_a_plus boundary -- simpler deterministic hit; first candidate succeeded so no alternative needed,SC-7 stderr also contains the build-context.sh RESULT JSON line as a side-effect; the SC-7 doing-line assertion uses anchored grep -cE returning exactly 1 to filter past the side-channel line,SC-8 sub-case A and sub-case B use independent TMPDIR scratch roots + independent JSONL log paths so they do not race; trap rm -rf EXIT cleans both,verifier check-helpers use grep -F -q -e needle for flag-token literal matches (T01 deviation note: BSD grep on macOS rejects bare --task / --no-prompt-mode etc as flags),CON-4 invariant gated via verifier absent-check on three orchestration command names (orchestrator-auto / orchestrator-roadmap / orchestrator-consolidate),CON-7 scaffold-placeholder byte pattern detection via printf 0133 octal escape (mirrors the M031 P02 source-side discipline),no T01 deliverable byte changed in T02 (commands/do.md + scripts/intake/do-entry.sh + four T01 shape verifiers all byte-frozen post-T01 per plan-time discipline)"
patterns_established:
  - "acceptance-test-via-stub-seam pattern: drive end-to-end via --dispatch-stub seam against canned stub that logs invocations to ORCH-STUB-LOG env; assertion is grep -c on the log file (mirrors P02 SC-6 stub shape one milestone earlier),independent-tmp-roots-per-sub-case discipline: SC-8 sub-case A and B each get their own mktemp -d work-a / work-b + ORCH_DO_ENTRY_LOG override so the assertions read only this run records,empirical-fixture-derivation as a planning step (run shape-detect.sh against candidate inputs then commit the first hit) -- documented in fixture filename and reasoning lives in the Description block of the task plan + reproduced in this SUMMARY,build-context.sh-stderr-side-channel awareness in test assertions: anchored grep -cE returning 1 filters past the RESULT JSON line build-context.sh emits on stderr without polluting the FR-12 doing-line check,SC-7 + SC-8 envelope conventions match M031 P01/P02: RESULT SC-N pass / RESULT SC-N fail diagnostic on the test scripts; SUMMARY verifier pass=N fail=M on the shape verifiers,test scripts use trap rm -rf work-dirs EXIT for hermetic cleanup -- no /tmp residue between test runs"
drill_down_paths:
  - ".orchestrator/milestones/M031/phases/P03/tasks/T02-acceptance-tests-PAYLOAD.md,.orchestrator/milestones/M031/phases/P03/tasks/T02-acceptance-tests-PLAN.md"
duration: "45m"
verification_result: "pass"
completed_at: "2026-05-01T20:13:22Z"
---

T02 ships the two M031 P03 acceptance tests that exercise scripts/intake/do-entry.sh end-to-end via the --dispatch-stub seam, plus the three fixtures the tests consume, plus two AD-19 single-script Truth Check shape verifiers under tools/verify/. SC-7 (trivial-path) drives the entry against a 4-word idea/high input ("fix typo in foo.md") and asserts: rc=0, exactly one FR-12 stderr line matching ^doing: .* — knowledge: [0-9]+ MEMs / [0-9]+ tokens$ (em-dash U+2014), zero approval-prompt residue ((y) substring count zero), and exactly one stub invocation with branch=tier_a_degenerate. SC-8 (low-confidence-prompt) drives the entry against a 9-word idea/low boundary input ("update default config flag to enable feature X today") under --no-prompt-mode A and B; sub-case A asserts the stderr renders the explicit Tier A vs Tier B prompt with a ? token, the JSONL unit_close record at ORCH_DO_ENTRY_LOG carries chosen_shape:"A", and the stub fired exactly once with branch=tier_a_degenerate (prompt collapsed to Tier A degenerate fast-path); sub-case B asserts route=tier_bc + passthrough=orchestrator:specify on stderr, JSONL chosen_shape:"B", and zero stub invocations (Tier B/C passthrough fires no dispatch). Empirical fixture derivation: shape-detect.sh confirmed the 9-word "update default config flag to enable feature X today" string emits idea/low at the 8-10 word boundary band per shape-detect.sh:108-114; alternative candidates were not needed since the first candidate hit the low-confidence band cleanly. Both shape verifiers (m031-p03-test-universal-entry-trivial-shape.sh pass=12 fail=0 / m031-p03-test-universal-entry-lowconf-shape.sh pass=13 fail=0) gate: target file exists + executable + >= 60 lines, required literal substrings (SC-7|SC-8 + flow-specific tokens), absence of forbidden orchestration command names (orchestrator:auto / orchestrator:roadmap / orchestrator:consolidate -- CON-4), absence of CON-7 scaffold-placeholder bracket-TODO byte pattern (D020 hygiene). T01's four shape verifiers still exit 0 with fail=0 (no T01 deliverable byte changed in T02 -- plan-time discipline rule). Tests use independent mktemp -d scratch roots + per-sub-case JSONL log paths via ORCH_DO_ENTRY_LOG override + per-test stub log paths via ORCH_DO_ENTRY_STUB_LOG so they do not race; cleanup via trap EXIT. Real-app smoke test pending callout (plan-time discipline rule 5): SC-7/SC-8 gate the contract surface via the --dispatch-stub seam; production handoff to the agent runtime (MEM018 -- agent IS the adapter) is integration-tested under M033 onboarding flows.
