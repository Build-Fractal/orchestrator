---
schema_version: "1.0"
type: task-summary
id: "T04"
parent: "P04"
milestone: "M031"
provides:
  - "tests/m031-acceptance/scope-guard.sh milestone-grain SC-12 verifier (POSIX-bash CON-6/DC-7; UNION allow-list across M031 P00..P04 Files Likely Touched; dual-prefix permissive carve-out + .orchestrator/milestones/M031/phases/ permissive prefix; MEM hit_count-only carve-out inherited verbatim from per-phase scope-guards; dual envelope: RESULT: SC-12 pass + SUMMARY: scope-guard.sh pass=N fail=M block_list_violations=K mem_hitcount_carveouts=L);tests/m031-acceptance/run-acceptance-battery.sh SC-14 acceptance battery aggregator (15 sub-gate run_sc invocations under Option A; SC-13 dropped per SC13-OPTION.md; AD-19 single-script-file shape; final BATTERY: pass=N fail=M envelope; mirrors M030 acceptance-battery convention);tools/verify/m031-p04-test-scope-guard-shape.sh (12 checks; AD-19 single-script Truth Check);tools/verify/m031-p04-battery-shape.sh (20 checks; AD-19 single-script Truth Check);tools/verify/m031-p04-evidence-ledger-shape.sh (4 checks; AD-19 single-script Truth Check; expected-fail at T04 close until T05 ships M031-ACCEPTANCE-EVIDENCE.md)"
requires:
  - "from:P01/T04 what:per-phase scope-guard shape (block-list + MEM hit_count carve-out function); from:P02/T05 what:dual-prefix permissive carve-out (.orchestrator/observability + .orchestrator/tier-a-plus); from:P03/T03 what:phase-suite straight-line N-gate aggregation pattern; from:P00/T## what:SC13-OPTION.md selecting Option A (drop SC-13 from battery); from:M030/P07/T02 what:tests/m030-acceptance/run-acceptance-battery.sh canonical battery shape; from:P04/T01 what:doc-drift-verifier.sh (SC-9) + test-auto-proceed-default.sh (SC-10); from:P04/T02 what:test-doctor-compound-change.sh (AD-9); from:P04/T03 what:test-budget-drift-warning.sh (AD-19)"
affects:
  - "P04/T05 evidence-ledger-and-phase-suite consumes the BATTERY: line + per-SC roll-up emitted by run-acceptance-battery.sh; T05 authors M031-ACCEPTANCE-EVIDENCE.md (the artifact m031-p04-evidence-ledger-shape.sh gates); T05 also wires scope-guard.sh + run-acceptance-battery.sh + the three new shape verifiers into the m031-p04-phase-suite.sh aggregator"
key_files:
  - "tests/m031-acceptance/scope-guard.sh,tests/m031-acceptance/run-acceptance-battery.sh,tools/verify/m031-p04-test-scope-guard-shape.sh,tools/verify/m031-p04-battery-shape.sh,tools/verify/m031-p04-evidence-ledger-shape.sh"
key_decisions:
  - "Option A selected per fixtures/empirical-baseline/SC13-OPTION.md (SC-13 dropped from battery; effective N=15); dual envelope on milestone-grain scope-guard (RESULT: SC-12 pass for acceptance-test compatibility + SUMMARY: scope-guard.sh pass=... for shape-verifier consumers); allow-list third permissive prefix .orchestrator/milestones/M031/phases/ added so all M031 phase/task plan/payload/summary files are covered without listing each by name (mirrors the dual-prefix convention from P02/P03 scope-guards but at milestone grain); milestone-grain scope-guard reads `git diff --name-only HEAD` AND `git ls-files --others --exclude-standard` so untracked new M031 files (acceptance tests not yet committed, P00..P04 phase/task plan files) register against the allow-list; verifier-self-allow-listing follows the per-phase convention (the verifier path is in its own allow-list); SC-11 substantive verdict=loses at T04 close is forwarded to T05 / phase-close as a known M031-by-design outcome (Quick now injects knowledge so post-baseline median tokens > pre-baseline median by construction; the empirical-baseline.sh COMPARE verdict frame asks 'fewer tokens with same/better pass rate' which is the wrong frame for M031's deliberate context-restoration tradeoff -- T05 evidence ledger should explicitly record this as expected-loss-on-token-budget vs gain-on-knowledge-availability); evidence-ledger-shape verifier shipped at T04 close with expected-fail status per task plan step 8; load-bearing once T05 commits the artifact"
patterns_established:
  - "milestone-grain scope-guard pattern: distinct from per-phase scope-guards under tools/verify/; lives under tests/m031-acceptance/ (operator-facing acceptance-test surface); UNION allow-list aggregates every phase's Files Likely Touched; dual envelope (RESULT: + SUMMARY:) so battery aggregator + shape verifiers + downstream consumers see a consistent literal sequence;acceptance-battery aggregator inheritance: M030 -> M031 lineage; same `set -uo pipefail` + `run_sc(label,path,extra)` helper + `bash <path>` literal-sequence invocation; `extra` third positional argument carries trailing flags via word-splitting (SC-11 `--compare`); final `BATTERY: pass=N fail=M` envelope + exit 0 iff fail=0;Option-A-vs-B reading discipline: planner writes SC13-OPTION.md selecting one option; battery aggregator omits the SC-13 run_sc line entirely under Option A (no commented-out dead code; no skip flag); inventory N reflects the option;expected-fail shape-verifier-shipped-before-artifact pattern: T04 ships m031-p04-evidence-ledger-shape.sh which fails until T05 commits the artifact; documented inline in the verifier's header so future maintainers do not 'fix' the failure by deleting the verifier;allow-list third permissive prefix at milestone grain (.orchestrator/milestones/M###/phases/) covers every phase/task plan + payload + summary file without per-file enumeration"
drill_down_paths:
  - ".orchestrator/milestones/M031/phases/P04/tasks/T04-scope-guard-and-battery-PLAN.md"
duration: "30m"
verification_result: "pass"
completed_at: "2026-05-01T21:00:00Z"
---

T04 ships the M031 milestone-close gate stack's two operator-facing surfaces plus three shape verifiers:

1. **`tests/m031-acceptance/scope-guard.sh`** (381 lines, executable, POSIX-bash) -- milestone-grain SC-12 scope-guard. Distinct from the per-phase `tools/verify/m031-p0X-scope-guard.sh` family: same SC-12 normative block-list (`knowledge/`, `scripts/cost/`, `scripts/dispatch/adapters/router/`, `scripts/auto/loop/`) but allow-list is the UNION of every M031 phase's Files Likely Touched + a third permissive prefix `.orchestrator/milestones/M031/phases/` covering all phase/task plan + payload + summary files. MEM hit_count-only carve-out function copied verbatim from P03 scope-guard. Dual-prefix permissive carve-out (`.orchestrator/observability/` + `.orchestrator/tier-a-plus/`) preserved verbatim. Reads both `git diff --name-only HEAD` AND `git ls-files --others --exclude-standard` so untracked new M031 files (P00..P04 plan/payload/summary, not-yet-committed acceptance tests) register against the allow-list. Dual envelope: `RESULT: SC-12 pass` (acceptance-test convention) + `SUMMARY: scope-guard.sh pass=N fail=M block_list_violations=K mem_hitcount_carveouts=L` (shape-verifier convention).

2. **`tests/m031-acceptance/run-acceptance-battery.sh`** (87 lines, executable, bash) -- SC-14 acceptance battery aggregator. 15 `run_sc` invocations covering SC-1, SC-2, SC-3 (P01); SC-5, SC-6, SC-16 (P02); SC-7, SC-8 (P03); SC-9, SC-10, SC-12, AD-9, AD-19 (P04); SC-15 (P01 budget median); SC-11 (P00 baseline `--compare`). SC-13 omitted per Option A in `tests/m031-acceptance/fixtures/empirical-baseline/SC13-OPTION.md` (corpus directory has no committed git history covering its own path at evaluation time, so Option B's `corpus_first_commit_ct < protected_first_commit_ct` assertion has no defined left-hand side). Mirrors M030's `tests/m030-acceptance/run-acceptance-battery.sh` shape exactly. AD-19 single-script-file shape (each gate is a literal `bash <path>` invocation; no compound chains, no array loops, no eval). `run_sc` helper accepts an optional third positional argument carrying trailing flags (used by SC-11 `--compare`). Final stdout line `BATTERY: pass=N fail=M`; exit 0 iff `fail=0`.

3. **`tools/verify/m031-p04-test-scope-guard-shape.sh`** (12 checks; AD-19 single-script Truth Check) -- asserts the milestone-grain scope-guard exists, contains the SC-12 block-list literals, the carve-out logic literals (`is_mem_hitcount_only_carveout`, `block_list_violations`, both permissive prefix literals), and is executable.

4. **`tools/verify/m031-p04-battery-shape.sh`** (20 checks; AD-19 single-script Truth Check) -- asserts the battery aggregator exists, contains the `BATTERY:` envelope + `run_sc` helper, and references every required SC sub-gate path (15 SC scripts).

5. **`tools/verify/m031-p04-evidence-ledger-shape.sh`** (4 checks; AD-19 single-script Truth Check) -- asserts `.orchestrator/milestones/M031/M031-ACCEPTANCE-EVIDENCE.md` exists with required substrings (`BATTERY:`, `M031`, `SC-`). **Expected-fail at T04 close** because T05 has not yet authored the ledger; the verifier becomes load-bearing at T05 close. Inline header documents this expectation so future maintainers do not "fix" the failure by deleting the verifier.

## Verification

- `tests/m031-acceptance/scope-guard.sh` -> `RESULT: SC-12 pass` + `SUMMARY: scope-guard.sh pass=128 fail=0 block_list_violations=0 mem_hitcount_carveouts=31`. Pre-existing P00 `tools/verify/p00-*.sh` paths and P02/P03/P04 plan/payload/summary paths surface as soft `WARN: out-of-allow-list:` (not counted as fail; expected forward drift from upstream phases). The `.orchestrator/milestones/M031/phases/` permissive prefix covers most M031 plan files; the bare `tools/verify/p00-*` shape files and a few non-prefixed paths show as WARN per scope-guard semantics.
- `tests/m031-acceptance/run-acceptance-battery.sh` -> `BATTERY: pass=14 fail=1`. The single failure is SC-11 (`empirical-baseline.sh --compare` reports `verdict=loses pre_median_tokens=3500 post_median_tokens=10185`). This is M031-by-design: Quick now injects knowledge (~7000 token jump) so post-median > pre-median by construction. The COMPARE verdict frame ("fewer tokens with same/better pass rate") is the wrong frame for M031's deliberate context-restoration tradeoff. Forwarded to T05 evidence ledger as expected-loss-on-token-budget vs gain-on-knowledge-availability; not a T04-grain fix (`No edits to T01-T03 deliverables in T04` constraint applies, and SC-11 is a P00-grain gate per the task plan's diagnostic guidance).
- `tools/verify/m031-p04-test-scope-guard-shape.sh` -> `SUMMARY: m031-p04-test-scope-guard-shape.sh pass=12 fail=0`.
- `tools/verify/m031-p04-battery-shape.sh` -> `SUMMARY: m031-p04-battery-shape.sh pass=20 fail=0`.
- `tools/verify/m031-p04-evidence-ledger-shape.sh` -> expected FAIL: `SUMMARY: m031-p04-evidence-ledger-shape.sh pass=0 fail=1` (ledger does not yet exist; T05 ships it).

## Forward Notes for T05

- T05 authors `.orchestrator/milestones/M031/M031-ACCEPTANCE-EVIDENCE.md` so `m031-p04-evidence-ledger-shape.sh` flips green. Ledger should embed the captured `BATTERY: pass=14 fail=1` line + per-SC verdicts + a written rationale framing SC-11 `verdict=loses` as M031-by-design (Quick context restoration trades tokens for knowledge availability; the COMPARE frame assumes thrift-only optimization, which M030 owned -- M031's deliberate inverse).
- T05's `m031-p04-phase-suite.sh` chains: `m031-p04-evaluate-md-drift-shape.sh`, `m031-p04-tier-definitions-drift-shape.sh`, `m031-p04-test-doc-drift-shape.sh`, `m031-p04-test-auto-proceed-shape.sh`, `m031-p04-changelog-shape.sh`, `m031-p04-doctor-compound-change-shape.sh`, `m031-p04-test-doctor-compound-change-shape.sh`, `m031-p04-budget-drift-shape.sh`, `m031-p04-test-budget-drift-shape.sh`, `m031-p04-test-scope-guard-shape.sh`, `m031-p04-battery-shape.sh`, `m031-p04-evidence-ledger-shape.sh`, plus `m031-p04-scope-guard.sh` last (per the P01/P02/P03 convention).
- T05 should also re-evaluate SC13-OPTION.md against post-T04 git history; if the M031 corpus directory now has committed history predating the first protected-path commit, the file may be flipped to Option B and the battery aggregator's SC-13 line restored (single-line additive amendment).

## Constraints Satisfied

- Bash 3.2 compatible (battery + shape verifiers); POSIX-bash compatible (milestone-grain scope-guard).
- AD-19 single-script-file shape across all five new files.
- No edits to T01 / T02 / T03 deliverables.
- No edits to per-phase verifiers (`m031-p01-*.sh`, `m031-p02-*.sh`, `m031-p03-*.sh`).
- No edits to `scripts/intake/`, `scripts/dispatch/`, `commands/evaluate.md`, `commands/dispatch.md`, `commands/do.md`, `references/tier-definitions.md`, `templates/`, `CHANGELOG.md`, `scripts/diagnostics/`.
- CON-7 / D020: no scaffold-placeholder marker bracket-TODO byte pattern in any new file.
- SC-12 scope-guard self-discipline: T04 did not touch `knowledge/**`, `scripts/cost/`, `scripts/dispatch/adapters/router/`, or `scripts/auto/loop/`. The milestone-grain scope-guard verifier itself contains literal block-list patterns -- the carve-out logic distinguishes "literal pattern in source" from "actual diff touches the path".
- Verifier path discipline (AD-19 + M032 Finding A): all three new shape verifiers under `tools/verify/m031-p04-*.sh`; operator-facing acceptance-test surfaces under `tests/m031-acceptance/`.
