---
schema_version: "1.0"
type: roadmap
milestone: "M033"
feature_ref: "036-project-onboarding-experience"
feature_spec: "specs/036-project-onboarding-experience/spec.md"
vision: "M033 turns project onboarding into a warm conversational front door — `orchestrator:start` detects which of four starting states a user is in (greenfield-empty / greenfield-with-materials / existing-codebase / migrating), routes them through a per-branch sub-flow, authors an orchestrator-native constitution with zero spec-kit dependencies, seeds the knowledge graph from materials or codebase, populates `CLAUDE.md`'s custom block, and gates milestone close on a friendly-tester pass that validates the cold-start UX no synthetic fixture can simulate."
tier: "C"
created_at: "2026-05-03"
updated_at: "2026-05-03"
paired_with: "M032"
---

## Phases

- [x] **P01**: Start-command skeleton + branch detection + friendly-tester protocol artifacts + PBJ fixture — "Running `bash scripts/lifecycle/start.sh --project-dir <fixture> --yes` against four fixture projects (one per branch shape: greenfield-empty / greenfield-with-materials / existing-codebase / migrating) detects each fixture's intended branch deterministically per the FR-2 ordered rules (rule 1: prior-tooling artifacts → migrating; rule 2: ≥3 PBJ-shape `.md` files AND no `src/` → greenfield-with-materials; rule 3: `src/` OR ≥10 root source files OR `.git/` with ≥1 commit → existing-codebase; rule 4: otherwise → greenfield-empty), invokes `init-project.sh` exactly once (idempotent re-run skipped), and prints the per-branch sub-flow stub's would-execute diagnostic; running against a fifth fixture with ambiguous signals (`package.json` + `.gsd/`) without `--yes` fires the disambiguation question per US-1 AS-5; running against a sixth fixture where `git init` has been run on an otherwise-empty project (≤9 source files, no prior-tooling artifacts) fires the FR-2 / MIT-006 / RISK-006 disambiguation question offering `greenfield-empty` as the recommended branch (converts the silent routing failure into a visible disambiguation moment per AD-4 arbiter ruling); `references/branch-detection.md` is the SSOT for the regex/glob patterns; `tests/m033-acceptance/friendly-tester-pass/protocol.md` exists with explicit pre-conditions + 30-minute walkthrough script + friction-capture template per branch and a tester-eligibility checklist excluding orchestrator maintainers (per Edge Case `Friendly-tester pass run by a tester who is too close`); `tests/m033-acceptance/friendly-tester-pass/report-template.md` exists with `friction_blockers:` frontmatter field defined; `bash tests/m033-acceptance/friendly-tester-pass/validate-report.sh <filled-report.md>` exits 0 on `friction_blockers: 0` AND `eligible_testers >= 1` and exits non-zero on `friction_blockers > 0` (mechanical, not advisory); `tests/fixtures/m033-pbj-materials-fixture/` exists with synthetic documents containing exactly 5 curated inconsistencies covering at minimum one instance per CON-4 detection category (ID misalignment, scheme contradictions, orphan references) and `tests/fixtures/m033-pbj-materials-fixture/README.md` enumerates all 5 by name + category + affected document pair (the ground-truth oracle SC-4 reads against); `bash tests/m033-acceptance/p01-start-branch-routing.sh` and `tests/m033-acceptance/p07-friendly-tester-protocol.sh` exit 0."
  - Risk: high
  - Depends: none
  - Boundary Map:
    - Produces: `commands/start.md` (new command surface — universal-entry warm front door per FR-1 / MIT-004); `scripts/lifecycle/start.sh` (FR-1 — accepts `--project-dir`, `--yes`, `--branch`, `--stack` (with stack-detection fallback question per MIT-004), `--dry-run`; probes filesystem for branch signals; invokes `init-project.sh` exactly once; routes to per-branch sub-flow stubs); branch-detection logic implementing FR-2's deterministic ordered rules with the MIT-006 / RISK-006 disambiguation-question implementation extension (per AD-4 arbiter ruling — converts silent `existing-codebase` routing of `git init`-only projects with ≤9 source files into a visible disambiguation moment offering `greenfield-empty` as recommended); `references/branch-detection.md` (SSOT for regex/glob patterns); `tests/m033-acceptance/friendly-tester-pass/protocol.md` (FR-19 — tester-eligibility checklist excluding orchestrator maintainers; per-branch pre-conditions; 30-minute walkthrough scripts; friction-capture template per AD-3); `tests/m033-acceptance/friendly-tester-pass/report-template.md` (FR-19 — `friction_blockers:` frontmatter field defined per US-8 AS-3); `tests/m033-acceptance/friendly-tester-pass/validate-report.sh` (FR-19 — exits non-zero on `friction_blockers > 0`, the SC-15 mechanical gate); `tests/fixtures/m033-pbj-materials-fixture/` (FR-23 / MIT-003 / AD-5 — synthetic documents with exactly 5 curated inconsistencies covering at minimum one per CON-4 category, deterministic across platforms and operator identities); `tests/fixtures/m033-pbj-materials-fixture/README.md` (FR-23 — names all 5 inconsistency types by name, category, affected document pair; this README is SC-4's ground-truth oracle); `tests/m033-acceptance/p01-start-branch-routing.sh` (SC-1 — verifies FR-1 + FR-2 across 4+1+1 fixtures, including the disambiguation cases); `tests/m033-acceptance/p07-friendly-tester-protocol.sh` (SC-8 — asserts protocol artifact + report template + validator script exist with correct shape; does NOT assert the pass has been run — that's SC-15 in P05)
    - Consumes: nothing (foundation phase per AD-8 — no P00 empirical-baseline phase; the empirical signal for the 4 branches is the 2026-04-28 PBJ bootstrap session evidence + 2026-04-29 dogfood addendum already on disk per Assumption A-5); orchestrator's existing `init-project.sh` (M001/M015 closed); orchestrator's existing fixture-naming + `tests/fixtures/` convention

- [x] **P02**: Grilling-shell module + glossary inline-update writer + dual-write + observability + partial-state markers — "Running `source scripts/lifecycle/grilling-shell.sh && ask_one 'What stack?' 'web-saas' '/tmp/partial.yml'` invokes the uniform `ask_one <question> <recommendation> [<context-file>]` API (FR-17), presenting the question sequentially (never batched per CON-5), waiting for an answer, and accepting the recommendation default with one keystroke; the calling protocol is *recommendation-not-interrogation* (the recommendation is named first, then the question asks for confirm-or-correct); when the optional `[<context-file>]` parameter points at a `partial-answers.yml` accumulator, the shell scans it for contradictions against the new question's resolved answer BEFORE returning success — the per-MIT-007 wiring required for live contradiction surfacing during normal sessions, not only on resume-from-interruption (proves the AD-6 architectural invariant: P03/P04/P05's calling commands inherit the contradiction-detection wiring from the module, not by re-invention); when the operator's answer resolves a domain term, the shell writes `<term>: <one-line definition>` (with at most a two-line elaboration, alphabetized-insert) immediately to `<fixture>/wiki/glossary.md` per FR-18 (NOT batched to end-of-session); the shell records every load-bearing event to `.orchestrator/execution-log.jsonl` per FR-22 with schema version 1.0 and the documented event types (`start_branch_detected`, `start_init_invoked`, `constitution_authored`, `ingest_codebase_completed`, `materials_intake_completed`, `ideation_completed`, `migrate_routed`, `customblock_drafted`, `wiki_init_invoked`, `github_init_invoked`, `friendly_tester_report_validated`); each sub-flow writes its own `.orchestrator/start-state/<sub-flow-name>.complete` marker file at completion per FR-20 so that re-invoked `start` resumes from the last completed sub-flow rather than re-running US-1 from scratch (per Edge Case `User aborts mid-flow`); the dual-write Recent Changes contract from M014 is inherited via `scripts/util/dual-write-runtime-md.sh` (FR-21 — every M033 command appends a one-line fragment to the `# >>> orchestrator:recent-changes >>>` regions in both `CLAUDE.md` and `AGENTS.md`, skip `AGENTS.md` if `dual_write_agents: false` in config); `bash tests/m033-acceptance/p07-grilling-shell.sh`, `tests/m033-acceptance/p07-resume-on-partial-state.sh`, and `tests/m033-acceptance/p07-observability-records.sh` exit 0."
  - Risk: high
  - Depends: P01
  - Blocked by: M032 spec 035 FR-15 (`wiki/glossary.md` path convention + `--include-glossary` scanner toggle) for full FR-18 inline-update integration testing; in M033/P02..P04 (before M032/P02 closes), FR-18 writes against a fixture-local glossary path per the same paired-stub-mode discipline SC-9 uses for FR-15 (Seam-C exercise mode = stub; real-mode integration testing happens in M033/P05). Plan-phase for P02 MUST capture this stub-mode escape valve.
  - Boundary Map:
    - Produces: `scripts/lifecycle/grilling-shell.sh` (FR-17 — uniform `ask_one <question> <recommendation> [<context-file>]` API; sequential-never-batched invariant per CON-5; recommendation-not-interrogation framing; code-first speculation cap reading project state before asking what reading would resolve; per-MIT-007 contradiction-detection wiring activated whenever `[<context-file>]` is passed, NOT only on resume-from-interruption — this is the AD-6 architectural invariant calling commands inherit); `<fixture>/wiki/glossary.md` inline-update writer (FR-18 — alphabetized-insert per existing entries; one-line definition + at most two-line elaboration; immediate-not-batched per CON-5 + spec 035 CON-6 / US-6 format invariant; M033 is the *primary writer* per Knowledge-Layer Boundary, M032 is the *path/adapter owner*); `scripts/util/dual-write-runtime-md.sh` invocations from M033 commands (FR-21 — Recent Changes dual-write inheritance from M014/spec 035; honors `dual_write_agents: false` config flag); JSONL emitter library writing `.orchestrator/execution-log.jsonl` records per FR-22 with schema version 1.0 and the 11 documented event types (`start_branch_detected`, `start_init_invoked`, `constitution_authored`, `ingest_codebase_completed`, `materials_intake_completed`, `ideation_completed`, `migrate_routed`, `customblock_drafted`, `wiki_init_invoked` (with downstream exit code), `github_init_invoked` (with downstream exit code), `friendly_tester_report_validated` (with verdict)); `.orchestrator/start-state/<sub-flow-name>.complete` marker file convention per FR-20 — each sub-flow writes its marker at completion so re-invoked `start` detects partial state and resumes from the last completed sub-flow (per CON-6 resume-on-partial-state-required); `tests/m033-acceptance/p07-grilling-shell.sh` (SC-11 — verifies FR-17 + FR-18: uniform API exposed; all 4 calling commands (FR-3 / FR-9 / FR-10 / FR-13 — invocations land in P03/P04/P05 but the test uses module-level fixture invocations; the calling-command integration is asserted by SC-2/SC-4/SC-5/SC-7); glossary inline-update writes alphabetized entries to `wiki/glossary.md` immediately, not batched); `tests/m033-acceptance/p07-resume-on-partial-state.sh` (SC-12 — verifies FR-20: marker files written by each sub-flow; re-invoked `start` resumes from last completed sub-flow rather than re-running US-1); `tests/m033-acceptance/p07-observability-records.sh` (SC-13 — verifies FR-22: every load-bearing event produces a JSONL record with documented event-type and schema version 1.0)
    - Consumes: P01 `commands/start.md` + `scripts/lifecycle/start.sh` (start skeleton harness — grilling-shell is invoked from sub-flow stubs that P03/P04/P05 fill in); P01 `references/branch-detection.md` (FR-22's `start_branch_detected` event references the branch identified by P01's detection logic); P01 sub-flow stub framework (FR-20's marker-file convention layers onto the per-branch sub-flow harness P01 establishes)

- [x] **P03**: Constitution authoring + codebase ingestion + standalone-gate compliance test — "Running `bash scripts/lifecycle/constitution-author.sh --stack web-saas --project-dir <fixture> --yes` (with `EDITOR=cat` to bypass the interactive editor pass) against three stack fixtures (one per `--stack` value: `web-saas`, `cli-tool`, `library`) loads the corresponding `templates/constitution-starters/<stack>.md`, runs the 5–8-question grilling-protocol-shaped flow consuming P02's `ask_one` API, substitutes `{{...}}` placeholders, hands the draft to `$EDITOR`, runs `bash scripts/verify/constitution-shape-lint.sh <output>` as a post-step verifier (exit 0 on pass; non-zero with the missing/malformed item named on fail), and writes the reviewed file to `<fixture>/.orchestrator/memory/constitution.md` containing the 6–8 baseline principles + 2–3 stack-specific principles named in the starter (web-saas: `Idempotent Deploys`, `User Data Privacy`; cli-tool: `Composable Default Exit Codes`, `POSIX Convention Adherence`; library: `Stable API Surface`, `Semantic Versioning Discipline`); `grep -i 'speckit' <fixture>/.orchestrator/memory/constitution.md` AND `bash scripts/verify/standalone-gate.sh constitution` (FR-6) return zero matches across the entire M033-shipped surface (the Principle XVI content-authoring compliance test); re-running without `--force` preserves the existing file byte-identical with `no changes` diagnostic; re-running with `--force` regenerates with stderr warning naming discarded edits; passing an unknown `--stack` value exits non-zero with the v1 list and points at #Q-2 expansion path; running `bash scripts/lifecycle/ingest-codebase.sh --project-dir <fixture>` against three stack fixtures (TypeScript SaaS / Python CLI / Rust library) produces 5–15 seed MEMs each across `knowledge/{architecture,conventions,decisions}/` with stable IDs derivable from source paths; re-running on the same fixture produces zero new MEMs and emits a `re-ingest: <N> existing entries detected, no changes` diagnostic (idempotent); against a fixture with `<fixture>/.orchestrator/DECISIONS.md` containing `DR-` entries, the rich-context import path fires per FR-8 / MIT-005 and emits a thin context file at `.orchestrator/milestones/<current-milestone>/<current-milestone>-CONTEXT.md` (or at `.orchestrator/milestones/_imported-context/_imported-context.md` when no active milestone is configured) with `context_source: imported-from-existing` referencing the source files, AND the `DR-` entries are surfaced as `knowledge/decisions/MEM-DR-*` MEMs (cross-reference, not duplicate authoring); `bash scripts/dispatch/build-context.sh --profile=quick --task 'fix flaky <test-name>' --project-dir <fixture>` against the post-ingest fixture loads ≥3 of the seeded MEMs into the payload (proves the seeded graph is consumable by M031's universal-entry path); `bash tests/m033-acceptance/p02-constitution-author.sh` and `tests/m033-acceptance/p03-ingest-codebase.sh` exit 0."
  - Risk: high
  - Depends: P01, P02
  - Boundary Map:
    - Produces: `commands/constitution.md` (new command surface per FR-3); `scripts/lifecycle/constitution-author.sh` (FR-3 — accepts `--stack <stack>` (validated against `web-saas|cli-tool|library`), `--project-dir`, `--force`, `--yes`; loads `templates/constitution-starters/<stack>.md`; runs 5–8-question grilling-protocol-shaped flow via P02's `ask_one`; substitutes `{{...}}` placeholders; hands draft to `$EDITOR`; verifies via `constitution-shape-lint.sh`; writes to `.orchestrator/memory/constitution.md`); `templates/constitution-starters/web-saas.md` (FR-4 — 6–8 baseline principles + 2–3 web-saas-specific: `Idempotent Deploys`, `User Data Privacy`, etc.); `templates/constitution-starters/cli-tool.md` (FR-4 — `Composable Default Exit Codes`, `POSIX Convention Adherence`, etc.); `templates/constitution-starters/library.md` (FR-4 — `Stable API Surface`, `Semantic Versioning Discipline`, etc.); `references/constitution-starter-format.md` (FR-4 — file-format documented for community-contributed additions); `scripts/verify/constitution-shape-lint.sh` (FR-5 — asserts required principle headers (Constitution Check section, `### Principle <roman-numeral>` sub-headers, non-empty body) and absence of literal `{{` placeholder strings; exit 0 on pass, non-zero with named missing/malformed item on fail); `scripts/verify/standalone-gate.sh` `constitution` invocation handler (FR-6 — Principle XVI's first content-authoring compliance test: zero `speckit.*` references in any code path, output, or template across the M033-shipped surface; mechanical enforcement of CON-3); `commands/ingest-codebase.md` (new command surface per FR-7); `scripts/lifecycle/ingest-codebase.sh` (FR-7 — scans top-level docs, package manifests, directory structure capped at top 2 levels per Edge Case `Codebase scan against a 100k-LOC monorepo`, test directory shape, recent git log (last 50 commits), prior-tooling artifacts; produces 5–15 seed MEMs under `knowledge/{architecture,conventions,decisions}/` with stable IDs derivable from source paths; deterministic structural extraction NOT LLM-augmented per Constitution XIV / NG-8; idempotent re-runs detect existing seed MEMs and emit `re-ingest` diagnostic); FR-8 / MIT-005 rich-context import path (`ingest-codebase` detects `.orchestrator/DECISIONS.md` with `DR-` entries / `MILESTONE-AUDIT.md` / populated `CLAUDE.md` custom blocks; emits a thin context file with `context_source: imported-from-existing`; path resolution: emit to `.orchestrator/milestones/<current-milestone>/<current-milestone>-CONTEXT.md` when an active milestone is configured, OR to `.orchestrator/milestones/_imported-context/_imported-context.md` with the `_imported-context/` sentinel for pre-milestone-configured projects per #Q-11; `DR-` entries cross-referenced as `knowledge/decisions/MEM-DR-*` MEMs, provenance-preserving, not duplicate authoring); `_imported-context/` sentinel-convention documentation in `references/` per #Q-11 (downstream traversers `validate-milestone.sh`, `build-context.sh`, `run-doctor.sh` MUST treat any `_*` prefix as special non-milestone class); `tests/m033-acceptance/p02-constitution-author.sh` (SC-2 — verifies FR-3 + FR-4 + FR-5 + FR-6 across three stacks; idempotency without `--force`; `--force` with discarded-edits warning; standalone-gate zero `speckit.*` references); `tests/m033-acceptance/p03-ingest-codebase.sh` (SC-3 amended per MIT-005 — verifies FR-7 + FR-8 across three stack fixtures; rich-context import-path path resolution at milestone-configured-or-`_imported-context/` location; `build-context.sh --profile=quick` loads ≥3 seeded MEMs)
    - Consumes: P01 `start.sh` sub-flow harness (constitution-author + ingest-codebase are routed to from the per-branch sub-flow stubs P01 produced); P01 `references/branch-detection.md` (ingest-codebase routing trigger comes from the existing-codebase branch detected per FR-2); P02 `scripts/lifecycle/grilling-shell.sh` `ask_one` API (FR-3 consumes the uniform module per AD-6 — each calling-command implementation is the wiring, not the module re-invention); P02 partial-state marker convention (FR-3 + FR-7 each write their own `.orchestrator/start-state/<sub-flow>.complete` marker on completion per FR-20); P02 JSONL emitter library (FR-3 emits `constitution_authored` event; FR-7 emits `ingest_codebase_completed` event); M020 knowledge-graph kinds (FR-7 writes into existing `pattern | convention | lesson | decision` schema; no new kinds per Knowledge-Layer Boundary); M031 traversal contract (SC-3 verifies seeded MEMs are consumable via `build-context.sh --profile=quick` without M033 extending or modifying the profile contract); M030 adaptive model routing (FR-3's grilling flow inherits routing for surgical-character tasks per AD-10 / Assumption A-4)

- [x] **P04**: Materials intake + ideation + migrate-routing — "Running `bash scripts/lifecycle/materials-intake.sh --project-dir <fixture> --yes` against the PBJ-shape fixture from P01 (with `--yes` auto-accepting labeling defaults) enumerates the detected materials (`.md`, `.pdf` via `textutil` on darwin / `pdftotext` on linux, `.json`, `.txt`), runs the labeling loop (operator labels each as `primary | supplementary | decision-history | out-of-scope`), runs the deterministic drift detector against the PBJ fixture's 5 known inconsistencies (CON-4 — no LLM-magic merge: ID misalignment, scheme contradictions, orphan references), surfaces all 5 conflicts as a checklist (no false positives, no false negatives — verified against the fixture README ground truth), accepts terminal-interactive answers via P02's grilling-shell for the 5 conflicts (≤`M033_CONFLICT_FILE_THRESHOLD` default 5 per #Q-6), emits a byte-deterministic reconciled pre-spec to `<fixture>/.orchestrator/intake/<timestamp>/reconciled-pre-spec.md` (same fixture + same answers → same output across platforms and operator identities), and `orchestrator:specify --description \"$(cat <reconciled-pre-spec.md>)\" --yes` runs cleanly against the output; against a synthetic `>5 conflicts` fixture (~8 conflicts), the loop hits the boundary and writes a markdown checklist to `<fixture>/.orchestrator/intake/<timestamp>/conflicts.md` exiting 0 with `edit then re-invoke with --resolve <conflicts.md>` diagnostic; running `bash scripts/lifecycle/ideation.sh --project-dir <empty-fixture> --yes < <answers-file>` runs the 7-question grilling-protocol-shaped flow (Problem / Target User / MVP / User Stories / Success Metric / Risks / Non-Goals), persists partial answers to `<fixture>/.orchestrator/intake/<timestamp>/partial-answers.yml` after each question, and emits the structured pre-spec to `ideation-pre-spec.md`; running `--with-conversus-stress-test` invokes the conversus adapter and appends adversarial findings as `## Adversarial Findings (deferred to specify)` (default off per #Q-7 — opt-in only); scripting a session with two contradicting ideation answers (e.g., target-user claims `enterprise B2B` in question 2 but problem-statement implies consumer in question 1) surfaces the contradiction via grilling-shell's `[<context-file>]` wiring per MIT-007 BEFORE question 3 fires (proves the partial-answers.yml live-contradiction wiring is active during normal sessions, not only on resume-from-interruption); running `bash scripts/lifecycle/start.sh --project-dir <migrating-fixture> --yes` against three fixtures (one per `--from` value: `.gsd/v1-roadmap.yml`, `.gsd2/state.yml`, `.specify/specs/`) prints the proposed `orchestrator:migrate --from <kind>` command line with the correct `<kind>`, auto-accepts under `--yes`, invokes the migration exactly once, and (when the fixture also has `src/` files per FR-12) optionally invokes `ingest-codebase` as a follow-on producing zero duplicate MEMs across the migrate-output + ingest-output paths (stable-ID match + `skip-duplicate-from-migrate` diagnostic); against an unsupported tooling shape (e.g., hypothetical `.aider/`), the operator is shown the detected artifacts and routed to a `no orchestrator:migrate adapter for this tooling — please file a request` diagnostic (the migrating branch does not silently fall through to greenfield-empty per US-6 AS-5); `bash tests/m033-acceptance/p04-materials-intake.sh`, `tests/m033-acceptance/p04-ideation.sh`, and `tests/m033-acceptance/p05-migrate-routing.sh` exit 0."
  - Risk: medium
  - Depends: P01, P02, P03
  - Boundary Map:
    - Produces: `commands/materials-intake.md` (new command surface per FR-9); `scripts/lifecycle/materials-intake.sh` (FR-9 — enumerates `.md` / `.pdf` (via `textutil` on darwin / `pdftotext` on linux per Assumption A-3) / `.json` / `.txt` materials; runs labeling loop (`primary | supplementary | decision-history | out-of-scope`); runs **deterministic** drift detection per CON-4 (ID misalignment, scheme contradictions, orphan references — explicitly NO LLM-magic merge); reconciles via terminal-interactive UX for ≤`M033_CONFLICT_FILE_THRESHOLD` (default 5 per #Q-6) conflicts and file-based UX above the threshold; emits reconciled pre-spec to `.orchestrator/intake/<timestamp>/reconciled-pre-spec.md` byte-deterministically — same fixture + same answers → same output across platforms); `commands/ideation.md` (new command surface per FR-10); `scripts/lifecycle/ideation.sh` (FR-10 — 7-question grilling-protocol-shaped flow consuming P02's `ask_one`; persists partial answers to `<fixture>/.orchestrator/intake/<timestamp>/partial-answers.yml` after EACH question for resume-on-interruption per CON-6 + Edge Case `Ideation conversation interrupted mid-question`; passes `partial-answers.yml` as the `[<context-file>]` argument to grilling-shell on EVERY `ask_one` invocation per MIT-007 — live contradiction detection during normal sessions, not only on resume; emits structured pre-spec to `.orchestrator/intake/<timestamp>/ideation-pre-spec.md`; optional `--with-conversus-stress-test` flag composes with conversus adapter, default OFF per #Q-7); FR-11 migrate-routing glue in `start.sh` (detects prior-tooling shape — `.gsd/v1-roadmap.yml` → `--from gsd-v1`, `.gsd2/state.yml` → `--from gsd-v2`, `.specify/specs/` → `--from spec-kit`; prints proposed `orchestrator:migrate --from <kind>` command line with one-keystroke accept, auto-accept under `--yes`; unsupported tooling shapes trigger US-6 AS-5 `no adapter` diagnostic — does not silently fall through to greenfield-empty); FR-12 migrate-then-ingest ordering (post-migration sub-flow optionally invokes `ingest-codebase` from P03 to fill gaps; duplicate-MEM prevention via stable-ID match + `skip-duplicate-from-migrate` diagnostic per US-6 AS-3 / brief #Q-10; resolved at clarify against M015 + M020 closures per #Q-4); `tests/m033-acceptance/p04-materials-intake.sh` (SC-4 — verifies FR-9 against the PBJ fixture from P01: 5 inconsistencies surface as checklist no-false-positives-no-false-negatives, terminal-interactive resolution for ≤5, file-based UX above threshold, reconciled pre-spec is byte-deterministic, downstream `orchestrator:specify` consumes cleanly); `tests/m033-acceptance/p04-ideation.sh` (SC-5 amended per MIT-007 — verifies FR-10 7-question flow, opt-in `--with-conversus-stress-test` invocation count via JSONL, partial-answer persistence + resume-on-interrupt, AND scripted-contradiction-detection via `[<context-file>]` wiring during normal session not only on resume); `tests/m033-acceptance/p05-migrate-routing.sh` (SC-6 — verifies FR-11 + FR-12 across three `--from` values, migrate-then-ingest path produces zero duplicate MEMs, `no adapter` diagnostic fires for unsupported tooling)
    - Consumes: P01 `tests/fixtures/m033-pbj-materials-fixture/` (FR-23 fixture is the SC-4 oracle; AD-5 places fixture in P01 specifically because P04 cannot pass without it); P01 branch detection (FR-11 migrate-routing fires from US-1 AS-4 `migrating` branch detection); P02 `grilling-shell.sh` `ask_one` API (FR-9, FR-10 invoke it per AD-6 architectural invariant; MIT-007's `[<context-file>]` wiring is the load-bearing seam between FR-10 and FR-17 — P02 ships the wiring, P04 verifies it via SC-5); P02 partial-state marker convention (each sub-flow writes its `.orchestrator/start-state/<sub-flow>.complete` marker on completion); P02 JSONL emitter library (FR-9 emits `materials_intake_completed`; FR-10 emits `ideation_completed`; FR-11 emits `migrate_routed`); P03 `ingest-codebase.sh` (FR-12 invokes ingest-codebase as the migrate-then-ingest follow-on; duplicate-MEM prevention requires P03's stable-ID derivation logic to be the same as P04's invocation path); M015 `orchestrator:migrate --from <kind>` (closed; FR-11 routes to it); Conversus adapter (M011/P07, shipped — FR-10's optional stress-test consumes it); M030 adaptive model routing (FR-9, FR-10 inherit routing for surgical-character tasks per AD-10 / Assumption A-4)

- [x] **P05**: Customblock drafter + paired-launch integration + friendly-tester pass + milestone close — "Running `bash scripts/lifecycle/customblock-draft.sh --project-dir <fixture> --yes` (with `EDITOR=cat` to bypass interactive review) against a fixture that has completed US-1 + US-2 + US-3 (existing-codebase branch with constitution authored + ingest-codebase produced) reads the upstream sub-flow outputs (constitution from FR-3, codebase scan from FR-7, materials intake from FR-9, ideation from FR-10), drafts the custom block in the prescribed 5-section structure (`## Project`, `## Stack`, `## Source-Docs` or `## Entry Points`, `## Conventions`, `## Decisions`) per FR-14, hands the draft to `$EDITOR` for review, and writes the reviewed content into the marker-delimited `<!-- BEGIN CUSTOM --> <!-- END CUSTOM -->` region of `<fixture>/CLAUDE.md`; content is strictly aggregated from upstream outputs (no LLM-invented facts); operator additions beyond the 5 sections (e.g., `## Notes`) are preserved verbatim per US-7 AS-2 (the prescriptive structure is a *floor*, not a *ceiling*); re-running without `--force` preserves operator edits with `no changes` diagnostic; re-running with `--force` after upstream outputs change regenerates with stderr warning naming discarded edits; running against a fixture missing US-2 (constitution not authored) exits non-zero with `constitution not present — run \"orchestrator:constitution\" first` diagnostic; running `bash scripts/lifecycle/start.sh --with-wiki --project-dir <fresh-fixture>` after M032/P02 closure invokes spec 035's `wiki-init.sh` for real (not stub) and the `wiki_init_invoked` observability record appears in `.orchestrator/execution-log.jsonl`; on `wiki-init.sh` non-zero exit per spec 035 FR-11 sequential-atomicity model (`init-complete, wiki-pending` partial state), `start --with-wiki` surfaces the failure as a wiki-init failure (NOT a `start` failure), preserves all outputs from US-1..US-7 sub-flows, emits `wiki-init failed; re-run \"orchestrator:wiki-init\" independently to complete; all other onboarding outputs preserved` diagnostic, AND the exit code of `start --with-wiki` equals the exit code of the underlying `wiki-init.sh` invocation (FR-15 / spec 035 CON-3 paired-launch contract); running `bash scripts/lifecycle/start.sh --with-github --project-dir <fresh-fixture>` invokes M013's `orchestrator:github-init` with the same failure-propagation contract per FR-16; the friendly-tester pass artifacts from P01 are exercised by ≥1 outsider tester per SC-15 (the artifact existed in P01; the PASS itself runs in parallel with P02..P05 implementation per AD-3 and gates close after P05); `bash tests/m033-acceptance/run-acceptance-battery.sh` enumerates and runs all 13 named acceptance scripts (`p01-*` + `p02-*` + `p03-*` + `p04-materials-intake.sh` + `p04-ideation.sh` + `p05-*` + `p06-*` + `p07-friendly-tester-protocol.sh` + `p07-grilling-shell.sh` + `p07-resume-on-partial-state.sh` + `p07-observability-records.sh` + `p08-with-wiki-passthrough.sh` + `p08-with-github-passthrough.sh`) per MIT-002 explicit-enumeration-not-phase-prefix-grouping discovery model and prints `BATTERY: pass=13 skip=0 fail=0` (skip>0 acceptable only with signed attestation in `M033-SUMMARY.md` per MIT-001); `bash tests/m033-acceptance/friendly-tester-pass/validate-report.sh tests/m033-acceptance/friendly-tester-pass/report-<latest>.md` exits 0 with `friction_blockers: 0` AND `eligible_testers >= 1`, OR `M033_SKIP_FRIENDLY_TESTER_PASS=1` is declared in `M033-SUMMARY.md` with signed attestation per US-8 AS-5 + #Q-1 fallback by 2026-05-12; `bash scripts/verify/validate-milestone.sh M033` reports `M033: NNN/NNN PASS` with NNN ≥ 15 (the count of named acceptance test scripts in SC-1..SC-15) per MIT-004 NNN-floor-non-negotiable; the `M033-VALIDATED` marker file is present at `.orchestrator/milestones/M033/`, `M033-SUMMARY.md` is present and references SC-1..SC-16 verdicts (with signed-attestation block(s) if SC-14 closed at `skip>0` or SC-15 closed via `M033_SKIP_FRIENDLY_TESTER_PASS=1`), and one `unit_close` record at milestone-grain is appended to `.orchestrator/execution-log.jsonl`."
  - Risk: high
  - Depends: P02, P03, P04
  - Blocked by: M032/P02 closure (real-mode SC-9 testing of FR-15 `--with-wiki` passthrough requires M032/P02's `--with-wiki` flag chain shipped; #Q-C / spec 035 #Q-C / launch sequencing amendment binds M032/P02 to close ≥ 1 week before M033/P05 starts to absorb integration-bug discovery — P05 currently targets the 2026-05-15 PBJ pilot deadline per Assumption A-3); friendly-tester recruiting (#Q-1 / launch sequencing amendment Q1) — outreach to begin no later than 2026-05-04; recruiting target 2026-05-08; fallback to `M033_SKIP_FRIENDLY_TESTER_PASS=1` signed attestation by 2026-05-12.
  - Boundary Map:
    - Produces: `commands/customblock-draft.md` (new command surface per FR-13); `scripts/lifecycle/customblock-draft.sh` (FR-13 — reads upstream sub-flow outputs from FR-3 / FR-7 / FR-9 / FR-10; drafts custom block in prescribed 5-section structure; hands to `$EDITOR`; writes to marker-delimited `<!-- BEGIN CUSTOM --> <!-- END CUSTOM -->` region of `<fixture>/CLAUDE.md`; idempotent without `--force`; regenerates with `--force` and stderr discarded-edits warning; strictly aggregates upstream outputs — no LLM-invented facts per Constitution XV); `references/customblock-format.md` (FR-14 — prescriptive 5-section format documented for future tooling parsing per #Q-8; operator additions preserved verbatim per US-7 AS-2 — structure is a floor not a ceiling); FR-15 `start --with-wiki` real-mode passthrough wiring (consumes spec 035 FR-11's `init-project.sh --with-wiki [--with-giscus] [--deploy]` flag chain after M032/P02 closes; sequential-atomicity model preserves all US-1..US-7 sub-flow outputs on `wiki-init.sh` non-zero exit per spec 035 CON-3; exit code propagation matches spec 035 FR-11; FR-22 emits `wiki_init_invoked` record with downstream exit code); FR-16 `start --with-github` passthrough wiring (consumes M013's `orchestrator:github-init`; same failure-propagation contract as FR-15; FR-22 emits `github_init_invoked` record with downstream exit code); friendly-tester pass execution + filed report (US-8 part 2 — the artifact existed in P01 / FR-19; the PASS itself runs in parallel with P02..P05 implementation per AD-3 with reports filed at `tests/m033-acceptance/friendly-tester-pass/report-<date>.md`); `tests/m033-acceptance/p06-customblock-draft.sh` (SC-7 — verifies FR-13 + FR-14: 5 prescribed sections present, content derived strictly from upstream outputs no LLM invention, idempotency without `--force`, `--force` regenerates with warning, structurally-downstream-of-US-2 check fires); `tests/m033-acceptance/p08-with-wiki-passthrough.sh` (SC-9 amended per MIT-001 — two-mode test contract: in M033/P01..P04 runs under `M033_FR15_STUB=1` asserting stub invocation correctness + synthetic-failure exit-code propagation + upstream-output preservation + diagnostic emission; in M033/P05 (after M032/P02 closure) runs against real `wiki-init.sh` surface and additionally asserts real invocation via `wiki_init_invoked` JSONL record; SC-14's `skip=0` requirement applies throughout — the stub-mode test in P01..P04 MUST produce pass not skip); `tests/m033-acceptance/p08-with-github-passthrough.sh` (SC-10 — verifies FR-16 same failure-propagation contract as FR-15); `tests/m033-acceptance/run-acceptance-battery.sh` (SC-14 amended per MIT-002 — explicit-enumeration discovery model NOT phase-prefix-grouping; runs all 13 named acceptance scripts; emits `BATTERY: pass=13 skip=0 fail=0`; propagates `M033_FR15_STUB=1` env var when present; modeled on `tests/m030-acceptance/run-acceptance-battery.sh` and `tests/m031-acceptance/run-acceptance-battery.sh`); per-script-assertion-group derivation list summing to NNN (SC-16 per MIT-004 — the audit-trace `validate-milestone.sh M033` reports against; planner-task-breakdown deliverable, NNN ≥ 15 floor non-negotiable; **planner setting NNN below 15 is in violation of SC-16 regardless of the planning document**); `M033-VALIDATED` marker file at `.orchestrator/milestones/M033/` (SC-17 — creation conditioned on AD-7 three-part gate: SC-14 `skip=0` AND SC-15 friendly-tester verdict AND SC-16 NNN ≥ 15; signed-attestation escalation paths exist for SC-14 (skip>0 with attestation) and SC-15 (`M033_SKIP_FRIENDLY_TESTER_PASS=1` with attestation), but NOT for SC-16 (NNN floor non-negotiable per spec text)); `M033-SUMMARY.md` referencing SC-1..SC-16 verdicts including signed-attestation block(s) when applicable (SC-17 / AD-2 milestone-close discipline mirroring M030/M031/M032 precedent); milestone-grain `unit_close` JSONL record appended to `.orchestrator/execution-log.jsonl` (SC-17)
    - Consumes: P02 `grilling-shell.sh` `ask_one` API (FR-13 customblock-draft consumes the uniform module per AD-6 architectural invariant); P02 JSONL emitter library (FR-13 emits `customblock_drafted`; FR-15 emits `wiki_init_invoked`; FR-16 emits `github_init_invoked`; FR-22 / SC-13 records); P02 partial-state marker convention (FR-13 writes its `customblock-draft.complete` marker); P03 constitution authoring outputs (`<fixture>/.orchestrator/memory/constitution.md` is one of FR-13's upstream sources for the `## Conventions` section); P03 ingest-codebase outputs (`knowledge/{architecture,conventions,decisions}/MEM-*` MEMs are FR-13's upstream sources for `## Stack`, `## Entry Points`, `## Decisions` sections); P04 materials-intake outputs (`reconciled-pre-spec.md` is FR-13's upstream source for `## Source-Docs` section); P04 ideation outputs (`ideation-pre-spec.md` is FR-13's upstream source for greenfield-empty `## Project` and `## Source-Docs` sections); P01 friendly-tester protocol artifacts + validate-report.sh (SC-15 mechanical gate consumes FR-19 artifacts); M032 spec 035 FR-11 `--with-wiki [--with-giscus] [--deploy]` flag chain (Seam-B real-mode invocation after M032/P02 closes); M032 spec 035 FR-15 `wiki/glossary.md` path convention (Seam-C — P02's FR-18 inline-update writes feed M032's scanner integration); M013 `orchestrator:github-init` (FR-16 routes to it; Assumption A-2's `--project-dir` flag compatibility)

## Cross-Cutting Concerns

- **CON-1 paired-launch seams (Seam-A / Seam-B / Seam-C with M032)** — affects every phase. Seam-A (shared install-bundle surface): P01 ships M033's 7 new commands + 6 new scripts via M032's `project_assets:` schema; Seam-B (`--with-wiki` failure-propagation contract): MIT-001 two-mode SC-9 test runs under `M033_FR15_STUB=1` in P01..P04 and against real `wiki-init.sh` in P05 — the stub-mode test in P01..P04 MUST produce pass not skip per SC-14's `skip=0` requirement; Seam-C (`wiki/glossary.md` format): P02's FR-18 inline-update writer is the *primary writer* M033 ships, M032 owns the path convention + scanner integration. Plan-phase for every phase MUST verify the seam-test scripts at `tests/paired-m032-m033/seam-{A,B,C}.sh` (shipped by M032/P02 per #Q-B) pass against the in-flight diff. Failure on any seam is a paired-launch failure that blocks both milestones per AD-1..AD-2 and the launch sequencing amendment.
- **CON-2 friendly-tester gate (SC-15 + AD-3) — load-bearing per launch sequencing amendment** — affects P01 (artifacts ship) + P05 (pass executes + gate closes). The protocol artifact + report template + `validate-report.sh` ship in P01/FR-19 so recruiting can begin in parallel with P02..P04 implementation. The friendly-tester PASS itself runs in parallel with P02..P05 — outreach by 2026-05-04, recruiting by 2026-05-08, fallback to `M033_SKIP_FRIENDLY_TESTER_PASS=1` signed attestation by 2026-05-12 per #Q-1. Outsiders ONLY — orchestrator maintainers excluded per FR-19 tester-eligibility checklist. Synthetic test fixtures are explicitly weak signal for cold-start UX per the launch sequencing amendment; warm bodies are the only adequate validation. Plan-phase for P01 MUST list recruiting outreach as a calendar gate, not just a logical dependency.
- **CON-3 standalone-posture content-authoring (Principle XVI's first content-authoring compliance test)** — affects every M033-shipped command, but mechanical enforcement is centralized in P03 (FR-6 `bash scripts/verify/standalone-gate.sh constitution`). Zero `speckit.*` references in any code path, output, or template across the M033-shipped surface. Plan-phase for every phase MUST run the standalone-gate verifier as a pre-merge check — not only on P03's constitution path but on the full M033 diff. Violation = standalone-launch story breaks; CC-only launch posture compromised.
- **CON-4 deterministic-not-LLM drift detection** — affects P04 only (FR-9 implementation). No LLM-magic merge. ID misalignment, scheme contradictions, orphan references are detected via deterministic scanning. The PBJ fixture from P01 (FR-23) is the ground-truth oracle: same fixture + same answers → same output across platforms and operator identities. Plan-phase for P04 MUST grep the FR-9 diff for any conversus / LLM / model invocation outside the explicit `--with-conversus-stress-test` opt-in (which is FR-10 ideation, not FR-9 materials-intake).
- **CON-5 grilling-protocol non-batchable (FR-17)** — affects P02 (writes the invariant) + P03 (FR-3 inherits) + P04 (FR-9, FR-10 inherit) + P05 (FR-13 inherits). Sequential never batched is a hard architectural invariant; calling commands MAY NOT bypass the sequential constraint by invoking `ask_one` in a loop without awaiting answers. Plan-phase for P03/P04/P05 MUST grep the calling-command diff for any parallel question-emission path or batched-prompt construction. Recommendation-not-interrogation framing applies uniformly: each `ask_one` invocation names the recommendation first, then asks for confirm-or-correct.
- **CON-6 resume-on-partial-state required (FR-20)** — affects P02 (writes the marker convention) + every P03..P05 sub-flow (each writes its own marker file at `.orchestrator/start-state/<sub-flow-name>.complete`). Mid-flow Ctrl+C MUST NOT cascade — interruption preserves outputs from completed sub-flows; re-invocation of `start` resumes from the last completed sub-flow rather than re-running US-1 from scratch. Plan-phase for every phase shipping a sub-flow MUST verify the marker-write happens at sub-flow completion, NOT at start.
- **AD-6 grilling-shell-before-callers** — affects P02 (ships first) + P03 / P04 / P05 (consumers). The uniform `ask_one <question> <recommendation> [<context-file>]` API is consumed by FR-3 (constitution-author), FR-9 (materials-intake), FR-10 (ideation), FR-13 (customblock-draft). Shipping the module first means each calling command's implementation is the wiring, not the module re-invention. Per MIT-007, the `[<context-file>]` parameter MUST be wired for live contradiction detection during normal sessions (not only resume) — this is a P02 architectural invariant the calling commands inherit. Plan-phase for P03/P04/P05 MUST verify each calling command passes `partial-answers.yml` (or equivalent accumulator) on EVERY `ask_one` invocation, not only on resume-from-interruption.
- **AD-7 three-part M033-VALIDATED close gate** — affects P05 only. SC-14 `skip=0` AND SC-15 friendly-tester verdict AND SC-16 NNN ≥ 15 floor. All three mechanical (acceptance battery exit code; `validate-report.sh` exit code; `validate-milestone.sh` count). Signed-attestation escalation paths exist for SC-14 (skip>0 with signed attestation) and SC-15 (`M033_SKIP_FRIENDLY_TESTER_PASS=1` with signed attestation), but NOT for SC-16 (NNN floor is non-negotiable per spec text per MIT-004). Plan-phase for P05 MUST list all three gates as named milestone-close deliverables — none is implicit.
- **MIT-001 two-mode SC-9 contract** — affects P02 (FR-15 stub-mode wiring foundation) + P03 (FR-3 + FR-7 may reference `--with-wiki` documentation) + P04 (calling commands may declare `--with-wiki` composable) + P05 (real-mode integration testing after M032/P02 closes). The stub-mode test under `M033_FR15_STUB=1` in P01..P04 MUST produce pass not skip — SC-14's `skip=0` requirement applies throughout. Plan-phase for the SC-9 implementation phase (P05) MUST design the test runner with both modes wired, controlled by env var; the battery runner MUST propagate the env var when present.
- **MIT-002 battery discovery model (explicit enumeration, not phase-prefix grouping)** — affects P05 only (SC-14 implementation). The battery runs all 13 named scripts via explicit enumeration; multiple scripts sharing a phase prefix (`p04-materials-intake.sh` and `p04-ideation.sh`; `p07-*` ×4; `p08-*` ×2) are intentional — each represents a distinct concern within that development phase. Plan-phase for P05 MUST explicitly cite this discipline against the battery aggregator implementation.
- **MIT-003 PBJ fixture in P01 (not P04)** — affects P01 (ships) + P04 (consumes for SC-4). AD-5 places fixture creation as a P01 deliverable specifically because SC-4 cannot pass without it; SC-14's `pass=13 skip=0 fail=0` requires SC-4 to pass; missing fixture fails the battery. Fixture construction is curatorial work (~1 day) requiring identification of 5 inconsistency types from the 2026-04-28 PBJ session evidence + synthesis of fixture documents — front-load, do not defer.
- **MIT-005 `_imported-context/` sentinel convention (FR-8)** — affects P03 (writes the convention) + downstream traversers (`validate-milestone.sh`, `build-context.sh`, `run-doctor.sh`). Document the convention in `references/` alongside the milestone directory schema; downstream traversers treat any `_*` prefix as a special non-milestone class. Plan-phase for P03 MUST resolve #Q-11 before P03 lock — the sentinel-convention documentation is part of P03's scope.
- **MIT-006 RISK-006 disambiguation-question (FR-2 git-init handling)** — affects P01 only. Per arbiter ruling per AD-4, the disambiguation-question implementation extension (not just the spec text addition) is the v1 fix; converts silent `existing-codebase` routing of `git init`-only projects into a visible disambiguation moment offering `greenfield-empty` as recommended. Plan-phase for P01 MUST list both the spec text addition AND the implementation extension as named tasks.
- **MIT-007 `partial-answers.yml` live wiring (FR-10)** — affects P02 (grilling-shell wires the `[<context-file>]` parameter for live contradiction detection during normal sessions) + P04 (FR-10 ideation is the test surface for this wiring per SC-5). The wiring is the load-bearing seam between FR-10 and FR-17 — calling commands inherit it from the module, do not re-implement. Plan-phase for P02 MUST verify the `[<context-file>]` scan-and-detect path fires whether or not the session is a resume; plan-phase for P04 MUST script a contradicting-answers test that proves contradiction surfaces before the next question fires.
- **CC-only launch posture (NG-equivalent)** — affects every phase. M033's interactive flows route to M030's adaptive model selection for surgical-character tasks per AD-10 / Assumption A-4. Plan-phase MUST NOT introduce Codex CLI / Cursor adapter logic; runtime gaps note in `references/RUNTIME-ASSUMPTIONS.md` if surfaced. M030 closure is satisfied (2026-05-01).
- **Knowledge-Layer Boundary (M033 vs. M020 vs. M031 vs. M032)** — affects P03 (FR-7 + FR-8 write into M020's existing kinds, no new kinds; M033 SHALL NOT add a new knowledge-graph kind) + P02 (FR-18 writes inline into M032's `wiki/glossary.md`; M033 is the *primary writer*, M032 is the *path/adapter owner*; format invariant — alphabetized term entries with one-line definitions and at most two-line elaborations per spec 035 US-6 — is shared) + every consuming phase (FR-7's seeded MEMs MUST be discoverable via M031's existing traversal contract per SC-3; M033 does not extend or modify the profile contract). Plan-phase for P02 + P03 MUST grep the FR-7 / FR-8 / FR-18 diff for any write to a new kind under `knowledge/` or any modification to M031's traversal logic as a pre-merge check.

## Dependency Graph

```
P01 ──┬──→ P02 ──→ P03 ──┬──→ P05
      │          │       │
      │          └─→ P04 ┘
      │              ↑
      └──────────────┘
```

Edges (declared producer/consumer pairs):
- `P01 → P02` — `start.sh` sub-flow harness (P01) gates the grilling-shell module (P02 — invoked from sub-flow stubs that P03/P04/P05 fill in); P01's branch detection (FR-2) triggers P02's FR-22 `start_branch_detected` event emission
- `P01 → P04` — PBJ fixture from P01 (FR-23 / MIT-003) is the SC-4 oracle for P04's materials-intake test; AD-5 places fixture in P01 specifically because P04 cannot pass without it
- `P01 → P05` — friendly-tester protocol artifacts + `validate-report.sh` from P01 (FR-19) are the SC-15 mechanical gate P05 consumes at milestone close
- `P02 → P03` — grilling-shell `ask_one` API (P02) is consumed by FR-3 constitution-author per AD-6; partial-state marker convention is consumed by FR-3 + FR-7
- `P02 → P04` — grilling-shell `ask_one` API (P02) is consumed by FR-9 materials-intake + FR-10 ideation per AD-6; MIT-007 `[<context-file>]` wiring is the load-bearing seam between FR-10 and FR-17 — P02 ships, P04 verifies via SC-5
- `P02 → P05` — grilling-shell is consumed by FR-13 customblock-draft per AD-6; JSONL emitter library is consumed by FR-15 + FR-16 passthrough wiring
- `P03 → P04` — FR-12 migrate-then-ingest path requires P03's `ingest-codebase` stable-ID derivation to be available for duplicate-MEM prevention (P04's FR-12 calls into P03's surface)
- `P03 → P05` — FR-13 customblock-draft strictly aggregates P03's constitution-author outputs (`## Conventions` section) + ingest-codebase MEMs (`## Stack`, `## Entry Points`, `## Decisions` sections)
- `P04 → P05` — FR-13 customblock-draft strictly aggregates P04's materials-intake outputs (`## Source-Docs` section) + ideation outputs (greenfield-empty `## Project` + `## Source-Docs`)

## Execution Order

1. **P01** — foundation, no dependencies. High risk because (a) US-1 routing is the most dogfooded surface in the milestone, (b) PBJ fixture creation is curatorial work that frontloads ~1 day before any downstream test can run, (c) friendly-tester protocol + recruiting outreach (#Q-1, by 2026-05-04) starts the calendar clock for P05's close gate. Strict prerequisite to all sub-flow phases — every sub-flow routes through `start.sh`'s harness.
2. **P02** — grilling-shell + glossary inline-update writer + dual-write + observability + partial-state markers. **High risk because (a) AD-6 architectural invariant — every P03/P04/P05 calling command depends on this module; (b) MIT-007 `[<context-file>]` live-contradiction wiring is the load-bearing seam between FR-10 and FR-17, ship right or break SC-5; (c) FR-18 inline-glossary writer binds M032's wiki/glossary.md path convention (Seam-C) — paired-launch seam discipline applies even before M032/P02 closes (stub-mode escape valve documented).** Cannot start until P01 ships start-skeleton harness.
3. **P03** — constitution authoring + codebase ingestion + standalone-gate compliance test. High risk because (a) Principle XVI's first content-authoring compliance test (FR-6 standalone-gate, zero `speckit.*` references); (b) MIT-005 `_imported-context/` sentinel convention (FR-8) requires #Q-11 resolution before lock + downstream-traverser handling. Cannot start until P02 ships grilling-shell + JSONL emitter + partial-state markers.
4. **P04** — materials intake + ideation + migrate-routing. Medium risk; consumes well-established grilling-shell from P02 + PBJ fixture from P01 + ingest-codebase from P03. Main novelty is CON-4 deterministic drift detection (no LLM-magic merge) and MIT-007 live-contradiction wiring verification via SC-5. Cannot start until P03 ships ingest-codebase (FR-12 migrate-then-ingest ordering requires it).
5. **P05** — customblock drafter + paired-launch integration + friendly-tester pass + milestone close. **High risk because (a) integration phase — FR-15 real-mode `--with-wiki` invocation gates on M032/P02 closure (#Q-C calendar gate, target 2026-05-06ish for M033/P05 to start by 2026-05-13ish); (b) friendly-tester pass close is non-negotiable per CON-2 / SC-15 (or signed-attestation fallback by 2026-05-12); (c) AD-7 three-part close gate is mechanical with one non-escalable arm (SC-16 NNN ≥ 15 floor); (d) SC-9 mode-flip from stub to real exposes any M033/P02 stub assumption that diverged from M032 reality.** Milestone-close gate. Cannot start until P02, P03, P04 all close AND M032/P02 closes ≥ 1 week prior.

For `orchestrator:auto`: phases run strictly sequentially with no parallelism opportunity at the phase level — every downstream phase has at least one upstream dependency that gates start. The only parallelism happens *within* the milestone's overall calendar: friendly-tester recruiting (#Q-1) runs in parallel with P02..P04 implementation per AD-3, and the friendly-tester PASS itself runs in parallel with P02..P05 implementation per AD-3 — but neither is a phase-level parallelism (they are calendar-level parallel workstreams gated at P05 close).

## Validation

- **No conflicting producers**: PASS. Each phase produces a disjoint set of files with the following declared additive overlaps explicitly marked at plan-phase pre-merge time:
  - `commands/start.md` + `scripts/lifecycle/start.sh` — created by P01 (FR-1 skeleton + FR-2 branch detection); amended additively by P04 (FR-11 migrate-routing glue; FR-12 migrate-then-ingest ordering); amended additively by P05 (FR-15 `--with-wiki` real-mode passthrough; FR-16 `--with-github` passthrough). Each phase's plan-phase MUST mark its amendment as additive (no overlapping flag handlers; FR-1's `--branch`/`--stack`/`--yes`/`--dry-run` flags are P01's; FR-15's `--with-wiki [--with-giscus] [--deploy]` and FR-16's `--with-github` are P05's).
  - `scripts/lifecycle/grilling-shell.sh` — created only by P02 (FR-17 + FR-18 inline-glossary writer + MIT-007 `[<context-file>]` contradiction wiring). No other phase amends it.
  - `commands/constitution.md` + `scripts/lifecycle/constitution-author.sh` — created only by P03 (FR-3). No other phase amends.
  - `commands/ingest-codebase.md` + `scripts/lifecycle/ingest-codebase.sh` — created only by P03 (FR-7 + FR-8). FR-12 migrate-then-ingest in P04 *invokes* the ingest surface but does not amend the script.
  - `commands/materials-intake.md` + `scripts/lifecycle/materials-intake.sh` — created only by P04 (FR-9). No other phase amends.
  - `commands/ideation.md` + `scripts/lifecycle/ideation.sh` — created only by P04 (FR-10). No other phase amends.
  - `commands/customblock-draft.md` + `scripts/lifecycle/customblock-draft.sh` — created only by P05 (FR-13 + FR-14). No other phase amends.
  - `templates/constitution-starters/{web-saas,cli-tool,library}.md` — created only by P03 (FR-4). Disjoint files per stack.
  - `references/branch-detection.md` — created only by P01.
  - `references/constitution-starter-format.md` — created only by P03.
  - `references/customblock-format.md` — created only by P05.
  - `references/_imported-context-sentinel.md` (or equivalent — exact filename TBD at plan-phase) — created only by P03 (#Q-11 resolution).
  - `scripts/verify/constitution-shape-lint.sh` — created only by P03 (FR-5).
  - `scripts/verify/standalone-gate.sh` `constitution` invocation handler — added only by P03 (FR-6).
  - `scripts/util/dual-write-runtime-md.sh` invocations — added by P02 (FR-21) into the M033 commands' shared invocation library; consumed by P03/P04/P05 calling commands as the JSONL emitter foundation.
  - `tests/m033-acceptance/` — populated additively across all phases. No two phases produce a file at the same path.
  - `tests/m033-acceptance/friendly-tester-pass/` — created only by P01 (FR-19). P05 files reports under this directory but does not modify the protocol/template/validator artifacts.
  - `tests/fixtures/m033-pbj-materials-fixture/` — created only by P01 (FR-23 / MIT-003 / AD-5). P04 reads but does not modify.
- **All consumed items have producers**: PASS. P01 has no consumes (foundation). P02 consumes P01's start-skeleton harness, branch detection (FR-22's `start_branch_detected` event references it), and sub-flow stub framework (FR-20's marker convention layers onto it) — all P01 produces. P03 consumes P01's start-skeleton harness, branch detection, P02's grilling-shell `ask_one` API, partial-state marker convention, and JSONL emitter library — all P01 + P02 produces. P04 consumes P01's PBJ fixture, P02's grilling-shell + MIT-007 wiring + JSONL emitter, and P03's ingest-codebase stable-ID derivation — all P01 + P02 + P03 produces. P05 consumes P02's grilling-shell + JSONL emitter + partial-state markers, P03's constitution + ingest outputs, P04's materials + ideation outputs, and P01's friendly-tester artifacts — all P01 + P02 + P03 + P04 produces. External upstream consumes (M032 spec 035 FR-11, FR-15; M013 `orchestrator:github-init`; M015 `orchestrator:migrate`; Conversus adapter; M030 routing) are declared in the spec's Dependencies section and are out-of-scope-for-validation here (they are paired/closed-milestone surfaces, not P##-internal). No unresolved-dependency entries.
- **DAG is acyclic**: PASS. Strict partial order rooted at P01 and terminating at P05. No back-edges. Non-linear edges (`P01 → P04` PBJ fixture, `P01 → P05` friendly-tester artifacts, `P02 → P04` grilling-shell, `P02 → P05` grilling-shell, `P03 → P05` constitution+ingest, `P04 → P05` materials+ideation) all point forward in the linear P01..P05 ordering — none cycle backwards.
- **Demo sentence coverage**: PASS. Each phase's demo sentence names specific script invocations (`bash scripts/lifecycle/start.sh ...`, `bash tests/m033-acceptance/p01-start-branch-routing.sh`, etc.), HTTP-or-equivalent assertions (`exit 0`, `--yes auto-accepts`, `byte-deterministic across platforms`), JSONL field assertions (`wiki_init_invoked` record present in `.orchestrator/execution-log.jsonl`, `BATTERY: pass=13 skip=0 fail=0`), file-system assertions (`M033-VALIDATED marker present`, `tests/fixtures/m033-pbj-materials-fixture/ exists with 5 curated inconsistencies`), three-part-gate verifications (SC-14 + SC-15 + SC-16 mechanical), or grep checks an operator can mechanically verify — not vague descriptions.
