---
schema_version: "1.0"
type: phase-plan
phase: "P05"
milestone: "M033"
goal: "Land M033's customblock-drafter (FR-13/FR-14), the FR-15 `--with-wiki` and FR-16 `--with-github` paired-launch passthroughs in `scripts/lifecycle/start.sh`, the SC-7/SC-9/SC-10 acceptance scripts, the SC-14 `tests/m033-acceptance/run-acceptance-battery.sh` enumerator, and the AD-7 three-part milestone-close gate (`M033-VALIDATED` marker + `M033-SUMMARY.md` + milestone-grain `unit_close` JSONL record), respecting (a) CON-1 paired-launch contract — FR-15 ships in two-mode shape per MIT-001 (`M033_FR15_STUB=1` produces pass-not-skip in stub mode, real `wiki-init.sh` invocation in real mode after M032/P02 closes), (b) CON-2 friendly-tester gate — SC-15 mechanical via `validate-report.sh` exits 0 on `friction_blockers: 0` AND `eligible_testers >= 1` OR `M033_SKIP_FRIENDLY_TESTER_PASS=1` signed attestation per US-8 AS-5, (c) AD-7 three-part close gate — SC-14 `skip=0` AND SC-15 friendly-tester verdict AND SC-16 NNN ≥ 15 floor (non-escalable per MIT-004), (d) MIT-002 explicit-enumeration battery discovery model (NOT phase-prefix grouping), and (e) AD-15 cross-phase regression discipline — every prior P01..P04 phase-suite still exits 0 against the post-P05 working tree."
demo_sentence: "An operator runs `EDITOR=cat bash scripts/lifecycle/customblock-draft.sh --project-dir /tmp/m033p05-existing --yes` against a fixture that has completed US-1 + US-2 (constitution authored at `<fixture>/.orchestrator/memory/constitution.md`) + US-3 (≥5 ingest-codebase MEMs at `<fixture>/.orchestrator/knowledge/{architecture,conventions,decisions}/`) and observes (a) the marker-delimited `<!-- BEGIN CUSTOM --> <!-- END CUSTOM -->` region in `<fixture>/CLAUDE.md` populated with all 5 prescribed sections (`## Project`, `## Stack`, `## Source-Docs` or `## Entry Points`, `## Conventions`, `## Decisions`) per FR-14, (b) every section's content traceable to an upstream sub-flow output (no LLM-invented facts per Constitution XV — `## Stack` lines verbatim from architecture-MEMs, `## Conventions` lines verbatim from convention-MEMs, `## Decisions` lines verbatim from decision-MEMs and `MEM-DR-*` cross-references, `## Source-Docs` derived from materials-intake reconciled-pre-spec or ideation-pre-spec when present, falling back to ingest-codebase entry-point detection for existing-codebase branches, `## Project` summarizing detected stack + branch), (c) a `customblock-drafted` JSONL record appended to `<fixture>/.orchestrator/execution-log.jsonl`, (d) a `<fixture>/.orchestrator/start-state/customblock-draft.complete` marker written, (e) re-running without `--force` exits 0 with `no changes` diagnostic and the file byte-identical, (f) re-running with `--force` after the constitution is re-authored regenerates the block with stderr `--force discards prior operator edits` warning, (g) running against a fixture missing US-2 exits non-zero with `constitution not present — run \"orchestrator:constitution\" first` diagnostic. Then runs `M033_FR15_STUB=1 bash scripts/lifecycle/start.sh --with-wiki --project-dir /tmp/m033p05-fresh --yes` and observes (a) the stub stand-in is invoked at the post-onboarding call site, (b) the stub-emitted synthetic non-zero exit code is propagated to `start.sh`'s exit code verbatim, (c) US-1..US-7 sub-flow markers under `<fresh>/.orchestrator/start-state/` are preserved, (d) a `wiki_init_invoked` JSONL record with `exit_code: <non-zero>` appears, (e) the stdout diagnostic `wiki-init failed; re-run \"orchestrator:wiki-init\" independently to complete; all other onboarding outputs preserved` is emitted; in real mode (no `M033_FR15_STUB`), the same surface invokes `bash scripts/lifecycle/wiki-init.sh --project-dir <fresh>` from M032/P02 with the same propagation contract. Then runs `M033_GHINIT_STUB=1 bash scripts/lifecycle/start.sh --with-github --project-dir /tmp/m033p05-gh --yes` and observes the same propagation contract for `orchestrator:github-init` per FR-16, with a `github_init_invoked` JSONL record. Then runs `bash tests/m033-acceptance/p06-customblock-draft.sh`, `bash tests/m033-acceptance/p08-with-wiki-passthrough.sh`, and `bash tests/m033-acceptance/p08-with-github-passthrough.sh` and observes exit 0 from each. Then runs `M033_FR15_STUB=1 bash tests/m033-acceptance/run-acceptance-battery.sh` and observes the final line `BATTERY: pass=13 fail=0` with each of the 13 named scripts (`p01-start-branch-routing.sh`, `p02-constitution-author.sh`, `p03-ingest-codebase.sh`, `p04-materials-intake.sh`, `p04-ideation.sh`, `p05-migrate-routing.sh`, `p06-customblock-draft.sh`, `p07-friendly-tester-protocol.sh`, `p07-grilling-shell.sh`, `p07-resume-on-partial-state.sh`, `p07-observability-records.sh`, `p08-with-wiki-passthrough.sh`, `p08-with-github-passthrough.sh`) emitting a `BATTERY-PASS:` line. Then runs `bash tools/verify/m033-p05-phase-suite.sh` and observes `SUMMARY: m033-p05-phase-suite.sh pass=N fail=0`, runs `bash tools/verify/m033-p05-cross-phase-regression.sh` and observes the four prior-phase suites (P01/P02/P03/P04) each exiting 0, runs `bash tools/verify/m033-p05-scope-guard.sh` and observes the bidirectional check pass (no out-of-scope leakage; all P05 deliverables present). Finally, with the friendly-tester report at `tests/m033-acceptance/friendly-tester-pass/report-2026-05-DD.md` carrying `friction_blockers: 0` AND `eligible_testers: 1` (or with `M033_SKIP_FRIENDLY_TESTER_PASS=1` declared in `.orchestrator/milestones/M033/M033-SUMMARY.md` with signed attestation per US-8 AS-5), runs `bash scripts/verify/validate-milestone.sh M033` and observes `M033: NNN/NNN PASS` with NNN ≥ 15, observes `.orchestrator/milestones/M033/M033-VALIDATED` exists, observes `.orchestrator/milestones/M033/M033-SUMMARY.md` exists referencing SC-1..SC-16 verdicts (with signed-attestation block(s) when applicable), and observes a single milestone-grain `{\"event_type\":\"unit_close\",\"unit_id\":\"M033\",...}` record appended to `.orchestrator/execution-log.jsonl`."
risk: "high"
depends_on: ["P02", "P03", "P04"]
---

## Must-Haves

<!-- All Check commands use single-script-file shape per AD-19.
     Project-owned slug-bearing verifiers live under tools/verify/ with the
     m033-p05-* prefix to avoid collision with M030/M031/M032 and with
     M033/P01..P04 namespaces. Artifact bullets use the
     `Label: path (constraints) — create|modify` shape (NOT bare-backtick
     bullets — auto-loop --step=V eval's bare-backtick bullets as commands). -->

### Truths

- `commands/customblock-draft.md` exists as a canonical command-doc per MEM012 (YAML frontmatter `description:` field; `# orchestrator:customblock-draft` title; Prerequisites / State Check section; Core Workflow numbered sections; Output / Idempotency / Error Handling / Referenced Scripts sections). The body documents the FR-13/FR-14 contract: invokes `bash scripts/lifecycle/customblock-draft.sh --project-dir <path> [--yes] [--force]`, names the prescribed 5-section structure (`## Project`, `## Stack`, `## Source-Docs`, `## Entry Points`, `## Conventions`, `## Decisions` — `## Source-Docs` and `## Entry Points` are alternative, branch-dependent), names the marker-delimited `<!-- BEGIN CUSTOM -->` / `<!-- END CUSTOM -->` write region per FR-13, names the load-bearing tokens `customblock_drafted` (JSONL event) and `customblock-draft.complete` (start-state marker), names the upstream-output sources (`<project>/.orchestrator/memory/constitution.md` from P03/FR-3, `<project>/.orchestrator/knowledge/{architecture,conventions,decisions}/MEM-*.md` from P03/FR-7, `<project>/.orchestrator/intake/<timestamp>/reconciled-pre-spec.md` from P04/FR-9, `<project>/.orchestrator/intake/<timestamp>/ideation-pre-spec.md` from P04/FR-10), states the strict-aggregation invariant (no LLM-invented facts per Constitution XV), states the structurally-downstream-of-US-2 precondition (constitution-not-present → exit non-zero), and links to `references/customblock-format.md` as the FR-14 SSOT.
  - Check: `bash tools/verify/m033-p05-customblock-draft-md-shape.sh`

- `scripts/lifecycle/customblock-draft.sh` exists, is executable, and implements the FR-13/FR-14 contract per the spec. The driver: (a) accepts `--project-dir <path>` (defaults to `pwd`), `--yes` (auto-accepts editor-pass via `EDITOR=cat`-like passthrough), `--force` (regenerate in place); (b) verifies upstream prerequisites — if `<project-dir>/.orchestrator/memory/constitution.md` does NOT exist, exits non-zero with `constitution not present — run \"orchestrator:constitution\" first` diagnostic on stderr per US-7 AS-5 (structurally-downstream-of-US-2 gate); (c) detects which upstream outputs are present and chooses the section variant — `## Source-Docs` if `<project-dir>/.orchestrator/intake/<timestamp>/reconciled-pre-spec.md` or `ideation-pre-spec.md` exists; `## Entry Points` if neither intake artifact exists but ingest-codebase MEMs exist (existing-codebase branch); (d) drafts the custom block content by **strict aggregation** of upstream outputs — `## Project` line summarizing detected stack + branch (read from existing config or constitution preamble); `## Stack` lines verbatim from `knowledge/architecture/MEM-*.md` content fields (one bullet per MEM); `## Source-Docs` lines verbatim from the intake pre-spec H2 section headers OR `## Entry Points` lines from ingest-codebase architecture MEMs; `## Conventions` lines verbatim from `knowledge/conventions/MEM-*.md` content fields; `## Decisions` lines verbatim from `knowledge/decisions/MEM-*.md` content fields including any `MEM-DR-*` cross-references from FR-8 rich-context import — explicitly NO LLM invocation, NO conversus invocation, NO model-routing in the draft path (Constitution XV); (e) handles re-run idempotency: when the existing `CLAUDE.md` between `<!-- BEGIN CUSTOM -->` and `<!-- END CUSTOM -->` is non-empty AND `--force` was NOT passed, exits 0 with `no changes` diagnostic and the file byte-identical (preserves operator edits per US-7 AS-3); when `--force` is passed and the existing block is non-empty, regenerates with stderr warning `--force discards prior operator edits` per US-7 AS-4; (f) hands the draft to the operator's editor (`$EDITOR`, default `vi`) when `--yes` is NOT set; under `--yes`, skips the editor pass and writes the draft directly; (g) writes the reviewed content into the marker-delimited region of `<project-dir>/CLAUDE.md` — operator additions beyond the 5 prescribed sections (e.g., `## Notes`) MUST be preserved verbatim per US-7 AS-2 (the prescriptive structure is a *floor*, not a *ceiling*); (h) writes `<project-dir>/.orchestrator/start-state/customblock-draft.complete` marker per FR-20 via `bash scripts/util/start-state-markers.sh write customblock-draft <project-dir>`; (i) emits `customblock_drafted` JSONL event per FR-22 via `bash scripts/util/jsonl-event-emitter.sh emit customblock_drafted <payload>` (payload includes `project_dir`, `sections_drafted: [...]`, `force: true|false`); (j) appends a one-line FR-21 dual-write Recent Changes fragment via `bash scripts/util/dual-write-runtime-md.sh --root <project-dir> --marker recent-changes --append-entry '<fragment>'` per the P03/T05-harmonized API. Bash 3.2 compatible per MEM001 (no `declare -A`, no process substitution, no command-substitution-with-pipes). Zero `speckit.*` references in any code path or output (CON-3 / Principle XVI).
  - Check: `bash tools/verify/m033-p05-customblock-draft-sh-shape.sh`

- `references/customblock-format.md` exists as the FR-14 SSOT documenting the prescriptive 5-section custom-block format (per #Q-8). The doc names each section header verbatim (`## Project`, `## Stack`, `## Source-Docs`, `## Entry Points`, `## Conventions`, `## Decisions`), documents the `## Source-Docs` vs `## Entry Points` branch-dependent variant rule, documents the marker-delimited write region (`<!-- BEGIN CUSTOM -->` / `<!-- END CUSTOM -->`), states the *floor-not-ceiling* discipline (operator additions beyond the 5 sections preserved verbatim per US-7 AS-2), states the strict-aggregation invariant (no LLM-invented facts per Constitution XV), and names the upstream-output source map (each section ↔ its upstream sub-flow output path). Includes a worked example showing the rendered custom block for a fixture that completed US-1 + US-2 + US-3.
  - Check: `bash tools/verify/m033-p05-customblock-format-ref-shape.sh`

- `scripts/lifecycle/start.sh` is extended additively for FR-15 `--with-wiki` real-mode passthrough per spec 035 FR-11 (paired-launch contract, CON-1 / MIT-001 two-mode test contract). The extension: (a) adds `--with-wiki`, `--with-giscus`, `--deploy` flags to the argument parser (additive — no overlap with existing P01/P04 flags `--project-dir`, `--yes`, `--branch`, `--stack`, `--dry-run`, `--no-resume`); (b) gates the wiki-init invocation post-onboarding (after all sub-flow stubs / real implementations have completed and their `start-state/<sub-flow>.complete` markers are written) — wiki-init MUST NOT fire before US-1..US-7 sub-flows have produced their outputs (sequential-atomicity model per spec 035 CON-3); (c) under stub mode (`M033_FR15_STUB=1`), invokes a stub stand-in helper (an inline bash function or `scripts/util/wiki-init-stub.sh` — implementation choice T02) that emits `STUB: wiki-init invoked --project-dir=<path> [--with-giscus] [--deploy]` to stdout and returns the exit code from `M033_FR15_STUB_EXIT_CODE` env var (default 0; tests set non-zero to exercise failure-propagation); (d) under real mode (no `M033_FR15_STUB`), invokes `bash scripts/lifecycle/wiki-init.sh --project-dir <project-dir> [--with-giscus] [--deploy]` (the M032/P02-shipped surface; not yet on disk in M033/P05 working tree, so the call site is conditioned on `[ -f scripts/lifecycle/wiki-init.sh ]` — when absent AND not stubbed, emits diagnostic `wiki-init.sh not found — M032/P02 must close before --with-wiki real-mode can fire`); (e) on non-zero wiki-init exit code, emits stdout diagnostic `wiki-init failed; re-run "orchestrator:wiki-init" independently to complete; all other onboarding outputs preserved` and exits with the SAME exit code as the underlying wiki-init invocation (FR-15 / spec 035 CON-3 contract); (f) US-1..US-7 sub-flow markers under `.orchestrator/start-state/` are NOT cleared on wiki-init failure (sequential-atomicity preservation); (g) emits `wiki_init_invoked` JSONL event per FR-22 with payload `{"project_dir": ..., "exit_code": <N>, "stub_mode": true|false, "with_giscus": true|false, "deploy": true|false}`. P01/P02/P03/P04 behavior fully preserved for invocations without `--with-wiki` (cross-phase regression gated by `tools/verify/m033-p05-cross-phase-regression.sh`).
  - Check: `bash tools/verify/m033-p05-with-wiki-passthrough-shape.sh`

- `scripts/lifecycle/start.sh` is extended additively for FR-16 `--with-github` passthrough per the same failure-propagation contract as FR-15. The extension: (a) adds `--with-github` flag to the argument parser (additive, no overlap with existing flags); (b) gates the github-init invocation post-onboarding (and post-wiki-init when both flags are present — github-init MUST NOT fire before wiki-init when `--with-wiki --with-github` are combined); (c) under stub mode (`M033_GHINIT_STUB=1`), invokes a stub stand-in that emits `STUB: github-init invoked --project-dir=<path>` to stdout and returns the exit code from `M033_GHINIT_STUB_EXIT_CODE` env var (default 0); (d) under real mode, invokes `bash scripts/lifecycle/github-init.sh --project-dir <project-dir>` (M013-shipped surface; per Assumption A-2 a one-line wrapper may be added if M013's existing entry point does not accept `--project-dir`) — when the script is absent AND not stubbed, emits diagnostic `github-init.sh not found — M013 must be installed before --with-github real-mode can fire`; (e) on non-zero github-init exit code, emits stdout diagnostic `github-init failed; re-run "orchestrator:github-init" independently to complete; all other onboarding outputs preserved` and exits with the SAME exit code as the underlying github-init invocation per FR-16; (f) US-1..US-7 sub-flow markers (and wiki-init marker if present) are NOT cleared on github-init failure; (g) emits `github_init_invoked` JSONL event per FR-22 with payload `{"project_dir": ..., "exit_code": <N>, "stub_mode": true|false}`. P01/P02/P03/P04 behavior fully preserved for invocations without `--with-github`.
  - Check: `bash tools/verify/m033-p05-with-github-passthrough-shape.sh`

- `tests/m033-acceptance/p06-customblock-draft.sh` exists, is executable, and exits 0 (SC-7). The script: (a) creates a staging project under `mktemp -d` carrying a pre-baked existing-codebase fixture state — `<staging>/CLAUDE.md` containing only the empty marker-delimited region, `<staging>/.orchestrator/memory/constitution.md` (US-2 satisfied), `<staging>/.orchestrator/knowledge/architecture/MEM-A1.md`, `<staging>/.orchestrator/knowledge/conventions/MEM-C1.md`, `<staging>/.orchestrator/knowledge/decisions/MEM-D1.md` (US-3 satisfied — minimum 1 MEM per category for section-population coverage); (b) runs `EDITOR=cat bash scripts/lifecycle/customblock-draft.sh --project-dir <staging> --yes` and asserts: the marker-delimited region in `<staging>/CLAUDE.md` is non-empty (was empty before), all 5 prescribed section headers (`## Project`, `## Stack`, `## Entry Points`, `## Conventions`, `## Decisions` — `## Entry Points` since no intake artifact exists in this fixture) are present, each section's body content matches a verbatim substring of the corresponding upstream MEM (no LLM-invented strings — assert via `grep -F` for known content from each MEM), `<staging>/.orchestrator/execution-log.jsonl` contains a `customblock_drafted` JSONL record, `<staging>/.orchestrator/start-state/customblock-draft.complete` marker exists; (c) re-runs the same command without `--force` and asserts the file is byte-identical (`diff` exits 0) AND `no changes` diagnostic appears on stdout; (d) re-runs with `--force` and asserts the file is regenerated AND the stderr warning `--force discards prior operator edits` appears; (e) creates a second staging project mirroring the first BUT adds an operator-edit `## Notes` section (with content `Test note line 1`) inside the marker region after the first draft; runs `customblock-draft --force` and asserts the `## Notes` section is preserved verbatim per US-7 AS-2 (floor-not-ceiling); (f) creates a third staging project missing `<staging>/.orchestrator/memory/constitution.md` (US-2 not satisfied), runs the command, and asserts non-zero exit AND `constitution not present` stderr diagnostic per US-7 AS-5; (g) creates a fourth staging project carrying an intake artifact at `<staging>/.orchestrator/intake/20260504T000000Z/ideation-pre-spec.md`, runs the command, and asserts the rendered block carries `## Source-Docs` (NOT `## Entry Points`) per the branch-dependent variant rule. Cleanup mandatory.
  - Check: `bash tools/verify/m033-p05-acceptance-shape-sc7.sh`

- `tests/m033-acceptance/p08-with-wiki-passthrough.sh` exists, is executable, and exits 0 (SC-9 amended per MIT-001 two-mode test contract). The script: (a) creates a staging project under `mktemp -d` and runs the full `start --yes` baseline to populate US-1..US-7 sub-flow markers (or directly seeds the markers under `<staging>/.orchestrator/start-state/` to keep the test fast — implementation choice T04); (b) runs `M033_FR15_STUB=1 M033_FR15_STUB_EXIT_CODE=0 bash scripts/lifecycle/start.sh --with-wiki --project-dir <staging> --yes` and asserts the stub stand-in fires (`STUB: wiki-init invoked` token on stdout), the start.sh exit code is 0, the `wiki_init_invoked` JSONL record contains `"exit_code": 0` AND `"stub_mode": true`, all US-1..US-7 sub-flow markers are still present (preservation invariant); (c) runs `M033_FR15_STUB=1 M033_FR15_STUB_EXIT_CODE=42 bash scripts/lifecycle/start.sh --with-wiki --project-dir <staging2> --yes` (fresh staging, fresh seed), asserts start.sh exit code is **42** (exit-code propagation per FR-15 / spec 035 CON-3), the stdout diagnostic `wiki-init failed; re-run "orchestrator:wiki-init" independently to complete; all other onboarding outputs preserved` appears, all US-1..US-7 markers preserved, the `wiki_init_invoked` JSONL record contains `"exit_code": 42`; (d) runs without `M033_FR15_STUB` AND with `--with-wiki` against a staging where `scripts/lifecycle/wiki-init.sh` is absent (M032/P02 not closed in the M033 working tree at SC-9 authorship time), asserts the diagnostic `wiki-init.sh not found — M032/P02 must close before --with-wiki real-mode can fire` AND the start.sh exit code is non-zero. The script's body MUST NOT reference any `SKIP:` or `EXIT 77` token — SC-14's `skip=0` invariant requires this test produces pass not skip in stub mode per CON-1 / MIT-001. Cleanup mandatory.
  - Check: `bash tools/verify/m033-p05-acceptance-shape-sc9.sh`

- `tests/m033-acceptance/p08-with-github-passthrough.sh` exists, is executable, and exits 0 (SC-10). Mirrors SC-9's two-mode contract but for FR-16 `--with-github`. The script: (a) creates a staging project under `mktemp -d` with US-1..US-7 markers seeded; (b) runs `M033_GHINIT_STUB=1 M033_GHINIT_STUB_EXIT_CODE=0 bash scripts/lifecycle/start.sh --with-github --project-dir <staging> --yes` and asserts the stub fires (`STUB: github-init invoked` token), exit code 0, `github_init_invoked` JSONL record present with `"exit_code": 0` AND `"stub_mode": true`, sub-flow markers preserved; (c) runs `M033_GHINIT_STUB=1 M033_GHINIT_STUB_EXIT_CODE=17 bash scripts/lifecycle/start.sh --with-github --project-dir <staging2> --yes` and asserts start.sh exit code is **17** (propagation), the diagnostic `github-init failed; re-run "orchestrator:github-init" independently to complete; all other onboarding outputs preserved` appears, sub-flow markers preserved; (d) runs `M033_FR15_STUB=1 M033_GHINIT_STUB=1 bash scripts/lifecycle/start.sh --with-wiki --with-github --project-dir <staging3> --yes` and asserts wiki-init fires BEFORE github-init (token-ordering check on stdout — `STUB: wiki-init invoked` appears before `STUB: github-init invoked`) per FR-16's "github-init MUST NOT fire before wiki-init when both flags are present" ordering rule. Cleanup mandatory.
  - Check: `bash tools/verify/m033-p05-acceptance-shape-sc10.sh`

- `tests/m033-acceptance/run-acceptance-battery.sh` exists, is executable, and prints final stdout line `BATTERY: pass=13 fail=0` when all 13 named SC scripts pass (SC-14 amended per MIT-002 explicit-enumeration discovery model). The runner: (a) is modeled byte-for-byte on `tests/m030-acceptance/run-acceptance-battery.sh` and `tests/m031-acceptance/run-acceptance-battery.sh` — same `run_sc()` helper shape, same `BATTERY-PASS:` / `BATTERY-FAIL:` line format, same final aggregation line `BATTERY: pass=N fail=M`, exits 0 iff fail=0; (b) enumerates the 13 named acceptance scripts via explicit `run_sc` calls in literal sequence (MIT-002 — NOT phase-prefix grouping; multiple `p04-*` / `p07-*` / `p08-*` siblings are intentional, each represents a distinct concern within that development phase): `p01-start-branch-routing.sh` (SC-1), `p02-constitution-author.sh` (SC-2), `p03-ingest-codebase.sh` (SC-3), `p04-materials-intake.sh` (SC-4), `p04-ideation.sh` (SC-5), `p05-migrate-routing.sh` (SC-6), `p06-customblock-draft.sh` (SC-7), `p07-friendly-tester-protocol.sh` (SC-8), `p07-grilling-shell.sh` (SC-11), `p07-resume-on-partial-state.sh` (SC-12), `p07-observability-records.sh` (SC-13), `p08-with-wiki-passthrough.sh` (SC-9), `p08-with-github-passthrough.sh` (SC-10); (c) propagates `M033_FR15_STUB=1` AND `M033_GHINIT_STUB=1` env vars to child invocations when set in the runner's environment (so the battery can run pre-M032/P02 closure with stub-mode tests producing pass not skip — CON-1 / MIT-001 invariant); (d) MUST NOT introduce any `EXIT 77` SKIP path — SC-14's `skip=0` invariant means the runner has no skip mechanism (skip handling is reserved for a hypothetical post-launch extension; today every named SC must produce pass or fail); (e) writes the final summary to stdout in the canonical `BATTERY: pass=%d fail=%d\n` format; if fail>0, exits 1.
  - Check: `bash tools/verify/m033-p05-acceptance-battery-shape.sh`

- `tools/verify/m033-p05-phase-suite.sh` exists, is executable, invokes every P05 verifier in dependency order, exits 0 iff every sub-gate passes, and emits a single line `SUMMARY: m033-p05-phase-suite.sh pass=N fail=M` before exit. The suite chains, in order: `m033-p05-customblock-draft-md-shape.sh`, `m033-p05-customblock-draft-sh-shape.sh`, `m033-p05-customblock-format-ref-shape.sh`, `m033-p05-with-wiki-passthrough-shape.sh`, `m033-p05-with-github-passthrough-shape.sh`, `m033-p05-acceptance-shape-sc7.sh`, `m033-p05-acceptance-shape-sc9.sh`, `m033-p05-acceptance-shape-sc10.sh`, `m033-p05-acceptance-battery-shape.sh`. Nine sub-gates plus the suite line.
  - Check: `bash tools/verify/m033-p05-phase-suite.sh`

- The P01 + P02 + P03 + P04 cross-phase regression boundary holds — every prior-phase phase-suite still exits 0 against the M033 working tree after P05 land. Per the AD-15 cross-phase regression precedent baked into P02/T02 / P03/T05 / P04/T05, P05 deliverables MUST NOT regress prior surfaces. The cross-phase regression verifier re-runs `tools/verify/m033-p01-phase-suite.sh`, `tools/verify/m033-p02-phase-suite.sh`, `tools/verify/m033-p03-phase-suite.sh`, `tools/verify/m033-p04-phase-suite.sh` and asserts each exits 0; additionally re-runs `bash scripts/verify/standalone-gate.sh constitution` and asserts it still exits 0 with `pass=N skip=0` (Principle XVI / CON-3 invariant unchanged after the customblock-draft surface lands).
  - Check: `bash tools/verify/m033-p05-cross-phase-regression.sh`

- The bidirectional scope-guard invariant holds for the P05 diff: P05 modifies/creates only files declared in this phase's "Files Likely Touched" list. Forbidden-presence whitelist asserts the M032 paired-launch internals, M013 internals, M015 internals, and M020 knowledge schema are NOT touched (one-way READ contract per Knowledge-Layer Boundary). Allowed-presence whitelist asserts every P05 deliverable is on disk. Modifications to `scripts/lifecycle/start.sh` (FR-15 + FR-16 additive extensions) are behavior-preserving — re-running P01/P02/P03/P04 phase-suites against the post-P05 tree MUST still exit 0. The bidirectional scope-guard catches both overflow (forbidden) and underflow (missing-deliverable).
  - Check: `bash tools/verify/m033-p05-scope-guard.sh`

- The `M033-VALIDATED` marker file at `.orchestrator/milestones/M033/M033-VALIDATED` exists with non-empty content (timestamp + signed acknowledgement of the AD-7 three-part close gate). Per AD-7, the marker is gated on (1) SC-14 `skip=0` (acceptance battery exits 0 with `BATTERY: pass=13 fail=0`); (2) SC-15 friendly-tester verdict (`bash tests/m033-acceptance/friendly-tester-pass/validate-report.sh tests/m033-acceptance/friendly-tester-pass/report-<latest>.md` exits 0 with `friction_blockers: 0` AND `eligible_testers >= 1` per CON-2, OR `M033_SKIP_FRIENDLY_TESTER_PASS=1` is declared in `M033-SUMMARY.md` with signed attestation per US-8 AS-5 / launch sequencing amendment #Q-1 fallback by 2026-05-12); (3) SC-16 NNN ≥ 15 floor (non-escalable per MIT-004 — `bash scripts/verify/validate-milestone.sh M033` reports `M033: NNN/NNN PASS` with NNN ≥ 15). The marker creation MUST NOT fire if any of the three gates fail.
  - Check: `bash tools/verify/m033-p05-validated-marker-shape.sh`

- `.orchestrator/milestones/M033/M033-SUMMARY.md` exists with the canonical M030/M031/M032 milestone-summary shape: YAML frontmatter (`schema_version: "1.0"`, `type: milestone-summary`, `id: "M033"`, `provides: ...`, `key_files: ...`, `key_decisions: ...`, `patterns_established: ...`, `drill_down_paths: ...`, `verification_result: pass|fail`, `completed_at: ...`); body sections enumerate each of SC-1..SC-16 with the verifier-script path and verdict (PASS/FAIL or SIGNED-ATTESTATION when applicable); the body explicitly references the friendly-tester report path used (or the `M033_SKIP_FRIENDLY_TESTER_PASS=1` signed attestation block when the fallback path was used per US-8 AS-5); the body names every P01..P05 phase-summary as a drill-down path. The summary includes a CON-3 standalone-posture verdict (`bash scripts/verify/standalone-gate.sh constitution` exit 0 with `skip=0`) and an AD-15 cross-phase regression verdict (every phase-suite exits 0 in the final close-state).
  - Check: `bash tools/verify/m033-p05-summary-md-shape.sh`

- A single milestone-grain `unit_close` JSONL record is appended to `.orchestrator/execution-log.jsonl` at milestone close. The record has `event_type: unit_close`, `unit_id: M033`, `unit_grain: milestone`, `verification_result: pass`, `completed_at: <ISO 8601 UTC>`, and a `gates_passed: [SC-14, SC-15, SC-16]` array (or `gates_passed: [SC-14, SC-16]` + `gates_attested: [SC-15]` when the `M033_SKIP_FRIENDLY_TESTER_PASS=1` fallback path was used). Modeled on the M030/M031 milestone-grain `unit_close` precedents.
  - Check: `bash tools/verify/m033-p05-unit-close-jsonl-shape.sh`

### Artifacts

- Command doc: `commands/customblock-draft.md` (min 60 lines, contains "orchestrator:customblock-draft", contains "FR-13", contains "FR-14", contains "customblock-draft.sh", contains "BEGIN CUSTOM", contains "END CUSTOM", contains "## Project", contains "## Stack", contains "## Source-Docs", contains "## Entry Points", contains "## Conventions", contains "## Decisions", contains "customblock_drafted", contains "customblock-draft.complete", contains "constitution not present", contains "no LLM", contains "Constitution XV", contains "customblock-format.md") — create
- Driver: `scripts/lifecycle/customblock-draft.sh` (min 250 lines, contains "--project-dir", contains "--yes", contains "--force", contains "BEGIN CUSTOM", contains "END CUSTOM", contains "## Project", contains "## Stack", contains "## Source-Docs", contains "## Entry Points", contains "## Conventions", contains "## Decisions", contains "constitution not present", contains "knowledge/architecture", contains "knowledge/conventions", contains "knowledge/decisions", contains "intake/", contains "reconciled-pre-spec.md", contains "ideation-pre-spec.md", contains "customblock_drafted", contains "customblock-draft.complete", contains "dual-write-runtime-md.sh", contains "discards prior operator edits", contains "no changes", bash 3.2 compatible) — create
- Reference: `references/customblock-format.md` (min 60 lines, contains "FR-14", contains "## Project", contains "## Stack", contains "## Source-Docs", contains "## Entry Points", contains "## Conventions", contains "## Decisions", contains "BEGIN CUSTOM", contains "END CUSTOM", contains "floor", contains "ceiling", contains "no LLM", contains "Constitution XV", contains "US-7 AS-2") — create
- Driver extension: `scripts/lifecycle/start.sh` (modify, min net +80 lines added; existing P01+P02+P04 contract preserved; contains "--with-wiki", contains "--with-giscus", contains "--deploy", contains "--with-github", contains "M033_FR15_STUB", contains "M033_GHINIT_STUB", contains "wiki_init_invoked", contains "github_init_invoked", contains "wiki-init failed", contains "github-init failed", contains "wiki-init.sh", contains "github-init.sh", contains "all other onboarding outputs preserved") — modify
- Acceptance: `tests/m033-acceptance/p06-customblock-draft.sh` (min 130 lines, contains "SC-7", contains "FR-13", contains "FR-14", contains "customblock-draft.sh", contains "BEGIN CUSTOM", contains "END CUSTOM", contains "## Project", contains "## Stack", contains "## Conventions", contains "## Decisions", contains "constitution not present", contains "no changes", contains "discards prior operator edits", contains "## Notes", contains "## Source-Docs", contains "customblock_drafted") — create
- Acceptance: `tests/m033-acceptance/p08-with-wiki-passthrough.sh` (min 120 lines, contains "SC-9", contains "FR-15", contains "MIT-001", contains "M033_FR15_STUB", contains "M033_FR15_STUB_EXIT_CODE", contains "wiki_init_invoked", contains "wiki-init failed", contains "all other onboarding outputs preserved", contains "stub_mode", contains "exit_code", contains "wiki-init.sh not found") — create
- Acceptance: `tests/m033-acceptance/p08-with-github-passthrough.sh` (min 110 lines, contains "SC-10", contains "FR-16", contains "M033_GHINIT_STUB", contains "M033_GHINIT_STUB_EXIT_CODE", contains "github_init_invoked", contains "github-init failed", contains "all other onboarding outputs preserved", contains "stub_mode", contains "exit_code") — create
- Acceptance battery: `tests/m033-acceptance/run-acceptance-battery.sh` (min 80 lines, contains "BATTERY:", contains "BATTERY-PASS:", contains "BATTERY-FAIL:", contains "MIT-002", contains "p01-start-branch-routing.sh", contains "p02-constitution-author.sh", contains "p03-ingest-codebase.sh", contains "p04-materials-intake.sh", contains "p04-ideation.sh", contains "p05-migrate-routing.sh", contains "p06-customblock-draft.sh", contains "p07-friendly-tester-protocol.sh", contains "p07-grilling-shell.sh", contains "p07-resume-on-partial-state.sh", contains "p07-observability-records.sh", contains "p08-with-wiki-passthrough.sh", contains "p08-with-github-passthrough.sh", contains "M033_FR15_STUB", contains "M033_GHINIT_STUB") — create
- Verifier: `tools/verify/m033-p05-customblock-draft-md-shape.sh` (min 30 lines, contains "commands/customblock-draft.md", contains "FR-13", contains "FR-14", contains "BEGIN CUSTOM") — create
- Verifier: `tools/verify/m033-p05-customblock-draft-sh-shape.sh` (min 35 lines, contains "scripts/lifecycle/customblock-draft.sh", contains "knowledge/architecture", contains "knowledge/conventions", contains "knowledge/decisions", contains "BEGIN CUSTOM", contains "constitution not present", contains "customblock_drafted") — create
- Verifier: `tools/verify/m033-p05-customblock-format-ref-shape.sh` (min 25 lines, contains "references/customblock-format.md", contains "FR-14", contains "## Project", contains "## Stack", contains "## Decisions") — create
- Verifier: `tools/verify/m033-p05-with-wiki-passthrough-shape.sh` (min 30 lines, contains "scripts/lifecycle/start.sh", contains "--with-wiki", contains "--with-giscus", contains "--deploy", contains "M033_FR15_STUB", contains "wiki_init_invoked", contains "wiki-init failed") — create
- Verifier: `tools/verify/m033-p05-with-github-passthrough-shape.sh` (min 30 lines, contains "scripts/lifecycle/start.sh", contains "--with-github", contains "M033_GHINIT_STUB", contains "github_init_invoked", contains "github-init failed") — create
- Verifier: `tools/verify/m033-p05-acceptance-shape-sc7.sh` (min 25 lines, contains "p06-customblock-draft.sh", contains "SC-7") — create
- Verifier: `tools/verify/m033-p05-acceptance-shape-sc9.sh` (min 25 lines, contains "p08-with-wiki-passthrough.sh", contains "SC-9", contains "MIT-001") — create
- Verifier: `tools/verify/m033-p05-acceptance-shape-sc10.sh` (min 25 lines, contains "p08-with-github-passthrough.sh", contains "SC-10") — create
- Verifier: `tools/verify/m033-p05-acceptance-battery-shape.sh` (min 30 lines, contains "run-acceptance-battery.sh", contains "BATTERY:", contains "MIT-002", contains "p01-start-branch-routing.sh", contains "p08-with-wiki-passthrough.sh", contains "p08-with-github-passthrough.sh") — create
- Verifier: `tools/verify/m033-p05-phase-suite.sh` (min 60 lines, contains "SUMMARY:", contains "m033-p05-customblock-draft-md-shape", contains "m033-p05-customblock-draft-sh-shape", contains "m033-p05-customblock-format-ref-shape", contains "m033-p05-with-wiki-passthrough-shape", contains "m033-p05-with-github-passthrough-shape", contains "m033-p05-acceptance-shape-sc7", contains "m033-p05-acceptance-shape-sc9", contains "m033-p05-acceptance-shape-sc10", contains "m033-p05-acceptance-battery-shape", contains "m033-p05-phase-suite") — create
- Verifier: `tools/verify/m033-p05-cross-phase-regression.sh` (min 30 lines, contains "m033-p01-phase-suite.sh", contains "m033-p02-phase-suite.sh", contains "m033-p03-phase-suite.sh", contains "m033-p04-phase-suite.sh", contains "standalone-gate.sh", contains "AD-15") — create
- Verifier: `tools/verify/m033-p05-scope-guard.sh` (min 60 lines, contains "scripts/lifecycle/customblock-draft.sh", contains "commands/customblock-draft.md", contains "references/customblock-format.md", contains "scripts/lifecycle/start.sh", contains "p06-customblock-draft.sh", contains "p08-with-wiki-passthrough.sh", contains "p08-with-github-passthrough.sh", contains "run-acceptance-battery.sh") — create
- Verifier: `tools/verify/m033-p05-validated-marker-shape.sh` (min 25 lines, contains "M033-VALIDATED", contains "AD-7", contains "SC-14", contains "SC-15", contains "SC-16") — create
- Verifier: `tools/verify/m033-p05-summary-md-shape.sh` (min 30 lines, contains "M033-SUMMARY.md", contains "schema_version", contains "type: milestone-summary", contains "SC-1", contains "SC-16", contains "standalone-gate") — create
- Verifier: `tools/verify/m033-p05-unit-close-jsonl-shape.sh` (min 25 lines, contains "unit_close", contains "M033", contains "milestone", contains "execution-log.jsonl", contains "gates_passed") — create
- Marker: `.orchestrator/milestones/M033/M033-VALIDATED` (min 5 lines, contains "M033", contains "VALIDATED", contains "AD-7", contains "SC-14", contains "SC-15", contains "SC-16") — create
- Summary: `.orchestrator/milestones/M033/M033-SUMMARY.md` (min 100 lines, contains "schema_version", contains "type: milestone-summary", contains "M033", contains "SC-1", contains "SC-2", contains "SC-3", contains "SC-4", contains "SC-5", contains "SC-6", contains "SC-7", contains "SC-8", contains "SC-9", contains "SC-10", contains "SC-11", contains "SC-12", contains "SC-13", contains "SC-14", contains "SC-15", contains "SC-16", contains "P01", contains "P02", contains "P03", contains "P04", contains "P05", contains "standalone-gate", contains "verification_result") — create

### Key Links

- `commands/customblock-draft.md` → `scripts/lifecycle/customblock-draft.sh` (command doc references the driver per MEM012 Referenced Scripts convention)
- `commands/customblock-draft.md` → `references/customblock-format.md` (command doc cites the FR-14 SSOT)
- `scripts/lifecycle/customblock-draft.sh` → `scripts/util/jsonl-event-emitter.sh` (driver emits `customblock_drafted`)
- `scripts/lifecycle/customblock-draft.sh` → `scripts/util/start-state-markers.sh` (driver writes `customblock-draft.complete` marker)
- `scripts/lifecycle/customblock-draft.sh` → `scripts/util/dual-write-runtime-md.sh` (driver appends FR-21 dual-write Recent Changes fragment)
- `references/customblock-format.md` → `commands/customblock-draft.md` (SSOT references the consuming command)
- `scripts/lifecycle/start.sh` → `scripts/util/jsonl-event-emitter.sh` (FR-15 emits `wiki_init_invoked`; FR-16 emits `github_init_invoked`)
- `scripts/lifecycle/start.sh` → `scripts/lifecycle/wiki-init.sh` (FR-15 real-mode passthrough — M032/P02 surface; conditional invocation per Assumption A-1)
- `scripts/lifecycle/start.sh` → `scripts/lifecycle/github-init.sh` (FR-16 real-mode passthrough — M013 surface; conditional invocation per Assumption A-2)
- `tests/m033-acceptance/p06-customblock-draft.sh` → `scripts/lifecycle/customblock-draft.sh` (SC-7 exercises the driver)
- `tests/m033-acceptance/p08-with-wiki-passthrough.sh` → `scripts/lifecycle/start.sh` (SC-9 exercises the FR-15 passthrough)
- `tests/m033-acceptance/p08-with-github-passthrough.sh` → `scripts/lifecycle/start.sh` (SC-10 exercises the FR-16 passthrough)
- `tests/m033-acceptance/run-acceptance-battery.sh` → `tests/m033-acceptance/p06-customblock-draft.sh` (SC-14 enumerator runs SC-7)
- `tests/m033-acceptance/run-acceptance-battery.sh` → `tests/m033-acceptance/p08-with-wiki-passthrough.sh` (SC-14 enumerator runs SC-9)
- `tests/m033-acceptance/run-acceptance-battery.sh` → `tests/m033-acceptance/p08-with-github-passthrough.sh` (SC-14 enumerator runs SC-10)
- `tools/verify/m033-p05-phase-suite.sh` → `tools/verify/m033-p05-acceptance-shape-sc7.sh` (suite chains the SC-7 wrapper)
- `tools/verify/m033-p05-phase-suite.sh` → `tools/verify/m033-p05-acceptance-shape-sc9.sh` (suite chains the SC-9 wrapper)
- `tools/verify/m033-p05-phase-suite.sh` → `tools/verify/m033-p05-acceptance-shape-sc10.sh` (suite chains the SC-10 wrapper)
- `tools/verify/m033-p05-phase-suite.sh` → `tools/verify/m033-p05-acceptance-battery-shape.sh` (suite chains the battery shape verifier)
- `tools/verify/m033-p05-cross-phase-regression.sh` → `tools/verify/m033-p04-phase-suite.sh` (cross-phase re-runs P04 suite per AD-15)
- `.orchestrator/milestones/M033/M033-SUMMARY.md` → `.orchestrator/milestones/M033/M033-VALIDATED` (summary references the validated marker file)
- `.orchestrator/milestones/M033/M033-SUMMARY.md` → `tests/m033-acceptance/run-acceptance-battery.sh` (summary cites the SC-14 battery)
- `.orchestrator/milestones/M033/M033-SUMMARY.md` → `tests/m033-acceptance/friendly-tester-pass/validate-report.sh` (summary cites the SC-15 mechanical gate)

## Tasks

### T01: `commands/customblock-draft.md` + `scripts/lifecycle/customblock-draft.sh` + `references/customblock-format.md` (FR-13 / FR-14)

See `tasks/T01-customblock-draft-PLAN.md`.

### T02: FR-15 `--with-wiki` paired-launch passthrough additive extension to `scripts/lifecycle/start.sh` (CON-1 / MIT-001 two-mode test contract)

See `tasks/T02-with-wiki-passthrough-PLAN.md`.

### T03: FR-16 `--with-github` passthrough additive extension to `scripts/lifecycle/start.sh`

See `tasks/T03-with-github-passthrough-PLAN.md`.

### T04: SC-7 + SC-9 + SC-10 acceptance scripts + SC-14 `run-acceptance-battery.sh` (MIT-002 explicit enumeration)

See `tasks/T04-acceptance-and-battery-PLAN.md`.

### T05: M033 milestone close — phase-suite + cross-phase regression + scope-guard + `M033-VALIDATED` marker + `M033-SUMMARY.md` + milestone-grain `unit_close` JSONL (AD-7 three-part close gate)

See `tasks/T05-milestone-close-PLAN.md`.

## Task Dependencies

```
T01 ────────────┐
T02 ──► T03 ────┤
                ├──► T04 ──► T05
```

T01 (`customblock-draft.md` + `customblock-draft.sh` + `customblock-format.md`) has no intra-phase prerequisites — it consumes only P02 surfaces (`grilling-shell.sh`, `jsonl-event-emitter.sh`, `start-state-markers.sh`, `dual-write-runtime-md.sh`) and reads P03/P04 sub-flow outputs at run time (no compile-time dependency on those phases inside T01's deliverables). T02 (`start.sh` FR-15 additive extension) has no intra-phase prerequisites — extends a P01/P04 surface. T03 (`start.sh` FR-16 additive extension) depends on T02 because both edit `scripts/lifecycle/start.sh` and T02 establishes the stub-mode + post-onboarding-gate scaffolding T03 inherits (a single-file serial-edit dependency, not a behavioral dependency). T04 (acceptance scripts + battery runner) depends on T01 (SC-7 exercises `customblock-draft.sh`) + T02 (SC-9 exercises `--with-wiki`) + T03 (SC-10 exercises `--with-github`). T05 (milestone close) depends on T01 + T02 + T03 + T04 (the `M033-VALIDATED` gate consumes the SC-14 battery exit code; the `M033-SUMMARY.md` references every SC-1..SC-16 verdict).

## Files Likely Touched

- `commands/customblock-draft.md` (create, T01)
- `scripts/lifecycle/customblock-draft.sh` (create, T01)
- `references/customblock-format.md` (create, T01)
- `scripts/lifecycle/start.sh` (modify, T02 — adds `--with-wiki`/`--with-giscus`/`--deploy` flags + post-onboarding wiki-init invocation gate + stub-mode dispatch + `wiki_init_invoked` JSONL emit; preserves P01/P02/P04 behavior for invocations without `--with-wiki`)
- `scripts/lifecycle/start.sh` (modify, T03 — adds `--with-github` flag + post-onboarding-and-post-wiki-init github-init invocation gate + stub-mode dispatch + `github_init_invoked` JSONL emit; preserves P01/P02/P04/T02 behavior)
- `tests/m033-acceptance/p06-customblock-draft.sh` (create, T04)
- `tests/m033-acceptance/p08-with-wiki-passthrough.sh` (create, T04)
- `tests/m033-acceptance/p08-with-github-passthrough.sh` (create, T04)
- `tests/m033-acceptance/run-acceptance-battery.sh` (create, T04)
- `tools/verify/m033-p05-customblock-draft-md-shape.sh` (create, T01)
- `tools/verify/m033-p05-customblock-draft-sh-shape.sh` (create, T01)
- `tools/verify/m033-p05-customblock-format-ref-shape.sh` (create, T01)
- `tools/verify/m033-p05-with-wiki-passthrough-shape.sh` (create, T02)
- `tools/verify/m033-p05-with-github-passthrough-shape.sh` (create, T03)
- `tools/verify/m033-p05-acceptance-shape-sc7.sh` (create, T04)
- `tools/verify/m033-p05-acceptance-shape-sc9.sh` (create, T04)
- `tools/verify/m033-p05-acceptance-shape-sc10.sh` (create, T04)
- `tools/verify/m033-p05-acceptance-battery-shape.sh` (create, T04)
- `tools/verify/m033-p05-phase-suite.sh` (create, T05)
- `tools/verify/m033-p05-cross-phase-regression.sh` (create, T05)
- `tools/verify/m033-p05-scope-guard.sh` (create, T05)
- `tools/verify/m033-p05-validated-marker-shape.sh` (create, T05)
- `tools/verify/m033-p05-summary-md-shape.sh` (create, T05)
- `tools/verify/m033-p05-unit-close-jsonl-shape.sh` (create, T05)
- `.orchestrator/milestones/M033/M033-VALIDATED` (create, T05 — gated on AD-7 three-part close gate)
- `.orchestrator/milestones/M033/M033-SUMMARY.md` (create, T05 — references SC-1..SC-16 verdicts)
- `.orchestrator/execution-log.jsonl` (modify, T05 — append a single milestone-grain `unit_close` record)

## Notes

### Per-script assertion-group derivation list summing to NNN (SC-16 / MIT-004)

SC-16 requires `bash scripts/verify/validate-milestone.sh M033` reports `M033: NNN/NNN PASS` with NNN ≥ 15 (the count of named acceptance test scripts in SC-1..SC-15) per MIT-004 NNN-floor-non-negotiable. The 15 named scripts and their per-script assertion-group counts (the floor — actual implementations may exceed):

1. `tests/m033-acceptance/p01-start-branch-routing.sh` (SC-1) — ≥1 assertion group (4-branch routing + ambiguous-signal disambiguation + git-init disambiguation)
2. `tests/m033-acceptance/p02-constitution-author.sh` (SC-2) — ≥1 assertion group (3-stack starters + idempotency + `--force` + standalone-gate)
3. `tests/m033-acceptance/p03-ingest-codebase.sh` (SC-3) — ≥1 assertion group (3-stack fixtures + 5–15 MEMs + idempotency + rich-context import + build-context payload load)
4. `tests/m033-acceptance/p04-materials-intake.sh` (SC-4) — ≥1 assertion group (PBJ 5 conflicts + terminal-interactive + file-based threshold + byte-determinism + AS-5 fallback)
5. `tests/m033-acceptance/p04-ideation.sh` (SC-5) — ≥1 assertion group (7-question flow + partial-answers persistence + opt-in conversus + MIT-007 contradiction)
6. `tests/m033-acceptance/p05-migrate-routing.sh` (SC-6) — ≥1 assertion group (3 `--from` variants + dup-prevention + no-adapter diagnostic)
7. `tests/m033-acceptance/p06-customblock-draft.sh` (SC-7) — ≥1 assertion group (5 sections + strict aggregation + idempotency + `--force` + structurally-downstream gate + variant rule)
8. `tests/m033-acceptance/p07-friendly-tester-protocol.sh` (SC-8) — ≥1 assertion group (protocol + report template + validator script existence)
9. `tests/m033-acceptance/p08-with-wiki-passthrough.sh` (SC-9) — ≥1 assertion group (stub mode + exit-code propagation + diagnostic + sub-flow preservation + missing-script diagnostic)
10. `tests/m033-acceptance/p08-with-github-passthrough.sh` (SC-10) — ≥1 assertion group (stub mode + exit-code propagation + ordering when both flags + sub-flow preservation)
11. `tests/m033-acceptance/p07-grilling-shell.sh` (SC-11) — ≥1 assertion group (uniform `ask_one` API + glossary writer)
12. `tests/m033-acceptance/p07-resume-on-partial-state.sh` (SC-12) — ≥1 assertion group (marker write + resume-from-last-completed)
13. `tests/m033-acceptance/p07-observability-records.sh` (SC-13) — ≥1 assertion group (every load-bearing event emits a JSONL record with documented event-type + schema 1.0)
14. `tests/m033-acceptance/run-acceptance-battery.sh` (SC-14) — ≥1 assertion group (13 named scripts run via explicit enumeration; final `BATTERY: pass=13 fail=0` line)
15. `tests/m033-acceptance/friendly-tester-pass/validate-report.sh` (SC-15) — ≥1 assertion group (`friction_blockers: 0` AND `eligible_testers >= 1` OR `M033_SKIP_FRIENDLY_TESTER_PASS=1` signed attestation)

NNN ≥ 15 is the floor; T05's `M033-SUMMARY.md` enumerates the actual NNN from the close-state `validate-milestone.sh M033` output. Per MIT-004, **a planner who sets NNN below 15 is in violation of SC-16 regardless of the planning document** — this list IS the planner's audit trace.

### Two-mode SC-9 (CON-1 / MIT-001) — stub-mode produces pass-not-skip

SC-9's two-mode contract per MIT-001 means the test MUST produce **pass** (not skip) under `M033_FR15_STUB=1` in M033/P01..P04 and against the real `wiki-init.sh` surface in M033/P05 (after M032/P02 closes). SC-14's `skip=0` invariant requires this — a `SKIP:` outcome would fail the battery's `BATTERY: pass=13 fail=0` aggregation. T04's SC-9 implementation MUST NOT emit any `SKIP:` token, MUST NOT exit `EXIT 77` (POSIX skip), MUST treat `wiki-init.sh not found` as a real failure when no stub mode is active (this is the M032/P02-not-yet-closed case, which is genuinely a fail in the closed-M032/P02 future). The runner (T04's `run-acceptance-battery.sh`) propagates `M033_FR15_STUB=1` AND `M033_GHINIT_STUB=1` env vars to child invocations when set in the runner's environment so the maintainer can run the battery pre-M032/P02 closure with stub-mode tests producing pass.

### `customblock-draft.sh` strict-aggregation discipline (Constitution XV)

Per FR-13 + FR-14 + Constitution XV, `customblock-draft.sh` MUST NOT invoke conversus, MUST NOT call any LLM/model-routing path, MUST NOT use `scripts/dispatch/*` for any code path. Every line emitted into the custom block traces verbatim to a file under `<project-dir>/.orchestrator/{memory,knowledge,intake}/`. The shape verifier `m033-p05-customblock-draft-sh-shape.sh` asserts negative-grep for `conversus|model_routing|dispatch.*-c |claude-code.*--task` to lock this invariant.

### `## Source-Docs` vs `## Entry Points` branch-dependent variant rule

Per FR-13, the rendered custom block contains EITHER `## Source-Docs` (when an intake artifact at `<project-dir>/.orchestrator/intake/<timestamp>/{reconciled-pre-spec.md,ideation-pre-spec.md}` exists — greenfield-with-materials or greenfield-empty branches) OR `## Entry Points` (when no intake artifact exists but ingest-codebase MEMs exist — existing-codebase or migrating branches), not both. This is the branch-dependent variant rule per US-7. The customblock-format.md SSOT documents this; the shape verifier asserts the driver implements it via the `if [ -f intake-pre-spec ]; then ...` branch shape.

### Friendly-tester pass timeline (CON-2 / launch sequencing amendment #Q-1)

Per CON-2 + the launch sequencing amendment, the friendly-tester pass on the four init branches is the load-bearing change between published and amended sequences. Recruiting outreach SHOULD begin no later than 2026-05-04; recruiting target 2026-05-08; fallback to `M033_SKIP_FRIENDLY_TESTER_PASS=1` signed attestation by 2026-05-12. The artifact (FR-19 — protocol.md, report-template.md, validate-report.sh, validate-report fixtures) ALREADY shipped in M033/P01; T05's role at milestone close is to consume the filed report (`tests/m033-acceptance/friendly-tester-pass/report-<latest>.md`) via `validate-report.sh` as part of the AD-7 SC-15 gate. T05 does NOT recruit testers and does NOT modify the FR-19 artifacts — those are P01 deliverables. T05 DOES support the `M033_SKIP_FRIENDLY_TESTER_PASS=1` fallback path by emitting the signed-attestation block in `M033-SUMMARY.md` when that env var is declared in the close-state environment (per US-8 AS-5).

### `start.sh` additive-extension discipline (T02 + T03 share a single file)

Both T02 and T03 modify `scripts/lifecycle/start.sh`. Per the AD-15 cross-phase regression precedent: each task's modification is **additive** (no overlapping flag handlers, no overlap with P01 `--branch`/`--stack`/`--yes`/`--dry-run` flags, no overlap with P02 `--no-resume` flag, no overlap with P04 migrate-routing internals). T02 adds the `--with-wiki` / `--with-giscus` / `--deploy` flag handlers and the post-onboarding wiki-init invocation gate. T03 adds the `--with-github` flag handler and the post-onboarding-and-post-wiki-init github-init invocation gate. The serial-edit ordering (T02 before T03) is a single-file convention so T03 inherits T02's stub-mode dispatch scaffolding. The cross-phase regression verifier (T05) re-runs `m033-p01-phase-suite.sh`, `m033-p02-phase-suite.sh`, `m033-p03-phase-suite.sh`, `m033-p04-phase-suite.sh` and asserts each exits 0 against the post-T02+T03 tree.

### M032/P02 closure as soft prerequisite for SC-9 real-mode

Per Assumption A-1 + CON-1, M032/P02's `--with-wiki` flag chain (spec 035 FR-11) MUST close before SC-9 real-mode integration testing fires. In the M033/P05 working tree at planning time, `scripts/lifecycle/wiki-init.sh` is NOT on disk (M032/P02 has not closed). T02's implementation MUST condition the real-mode invocation on `[ -f scripts/lifecycle/wiki-init.sh ]` AND emit the diagnostic `wiki-init.sh not found — M032/P02 must close before --with-wiki real-mode can fire` when the file is absent AND no stub mode is active. T04's SC-9 acceptance script asserts this diagnostic appears in the no-stub-no-real-script case. Once M032/P02 closes and `scripts/lifecycle/wiki-init.sh` lands, the real-mode path activates automatically with no T02 source change required.

### Path-collision check (Plan-Time Discipline rule 6)

Every artifact path declared as `create` in `## Files Likely Touched` was checked at plan-authoring time via `ls`. NONE exist on disk at planning time:

- `commands/customblock-draft.md` — absent
- `scripts/lifecycle/customblock-draft.sh` — absent
- `references/customblock-format.md` — absent
- `tests/m033-acceptance/p06-customblock-draft.sh` — absent
- `tests/m033-acceptance/p08-with-wiki-passthrough.sh` — absent
- `tests/m033-acceptance/p08-with-github-passthrough.sh` — absent
- `tests/m033-acceptance/run-acceptance-battery.sh` — absent
- `.orchestrator/milestones/M033/M033-VALIDATED` — absent
- `.orchestrator/milestones/M033/M033-SUMMARY.md` — absent
- All P05 `tools/verify/m033-p05-*` verifier paths — absent (M033/P05 namespace is fresh; no collision with M030/M031/M032 or M033/P01..P04)

`scripts/lifecycle/start.sh` IS present (P01/T01 deliverable, modified additively in P02/T02 + P04/T04); T02 and T03 declare it as `modify`, not `create`, so no collision.

### Verifier-availability cross-check (Plan-Time Discipline rule 2)

Every `## Verification` command in T01..T05 task plans resolves to a verifier scheduled inside this phase (T01 verifiers co-authored in T01, etc.). The phase-suite, cross-phase regression, scope-guard, and milestone-close verifiers are all T05 deliverables. The acceptance scripts (T04 deliverables) are referenced by T04's wrapper verifiers (also T04 deliverables) and by T05's phase-suite (which chains the wrappers). No cross-task verifier dependency goes upstream-of-its-author.
