---
schema_version: "1.0"
type: task-summary
id: "T04"
parent: "P02"
milestone: "M035"
provides:
  - ".github/workflows/release.yml GitHub Actions release pipeline skeleton (160 lines, valid YAML, ubuntu-latest D001, two jobs: pr-validate runs on PR + main-push without secrets exercising T01/T02/T03 verifiers + cross-channel byte-equivalence (npm-channel arm) + CON-6 negative-assertion runtime step that fails on NPM_TOKEN env-var leak; npm-publish runs only on v* tag-push events on canonical repo with NODE_AUTH_TOKEN populated from secrets.NPM_TOKEN, runs npm publish --access public, includes tag-vs-package.json version-gate step + pre-publish gates invoking all T01-T03 verifiers + post-publish npm view confirmation); tools/verify/m035-p02-release-workflow-shape.sh task-grain verifier (75 lines, executable, AD-19 single-script Check shape, BATTERY: pass=10 fail=0 -- 10 grep-contract surfaces covering job presence + runner + CON-6 secrets-scope + SC-14 negative-assertion step presence + T03 test invocation + T01 verifier invocation + scoped-package --access public flag); /tmp/m035-p02-t04-yaml-validate.sh staged probe (Python yaml.safe_load shape-guard against indentation breakage in future workflow edits, invoked via scripts/util/run-probe.sh approved-roots wrapper)"
requires:
  - "from:T01 what:package.json existing at repo root with name @build-fractal/orchestrator + scripts.postinstall + version field (npm-publish job runs npm publish against this manifest; tag-version-gate step compares stripped GITHUB_REF to package.json version); from:T02 what:packaging/npm/postinstall.sh exists (lives inside published tarball, not directly invoked by workflow); from:T03 what:tests/m035-acceptance/cross-channel-byte-equivalence.sh exists at documented path emitting NPM_HASH= and BATTERY pass/fail/skip line shape (invoked by both pr-validate and npm-publish jobs as gating step); from:T01-T03 what:tools/verify/m035-p02-package-json-shape.sh + m035-p02-bin-entry.sh + m035-p02-postinstall-shape.sh + m035-p02-installation-doc-exclusion-list.sh + m035-p02-byte-equivalence-skeleton.sh exist (invoked by workflow as gating steps); from:disk what:.github/ directory existing with only ISSUE_TEMPLATE/ subdir (T04 creates .github/workflows/ fresh per Plan-Time Discipline Rule 6 path-collision check); from:disk what:.orchestrator/DECISIONS.md D001 row recording CI runner = ubuntu-latest; from:disk what:scripts/util/run-probe.sh staged-probe wrapper (invokes /tmp/m035-p02-t04-yaml-validate.sh under approved-roots contract); from:executor what:python3 on PATH (yaml.safe_load YAML-validity check); from:GitHub-Actions-runtime what:github.ref + github.event_name + secrets.NPM_TOKEN context vars + actions/checkout@v4 + actions/setup-node@v4 actions"
affects:
  - "P02/T05 (bundle-hygiene-filter + npm-pack-contents verifiers will land at tools/verify/m035-p02-bundle-hygiene-filter.sh + m035-p02-npm-pack-contents.sh; release.yml already wires conditional invocation via if test -x then bash else echo SKIP shape so T05 lands additive without workflow re-edit); P02/close (acceptance battery invokes m035-p02-release-workflow-shape.sh as part of phase-suite); P03 (homebrew-bottle build job extends release.yml with macOS-runner matrix arm; T04 explicitly leaves macOS deferred per D001 ubuntu-latest decision; P03 plan must add matrix or sibling job); P04 (curl-pipe-bash artifact composition + signed install.sh upload + GH release-note generation extend release.yml with additional steps inside npm-publish or new sibling job; T04 CON-6 secrets-scoping discipline is the contract P04 must preserve); P05 (signing + checksums + orchestrator:update first-class command extend release.yml with sigstore/cosign steps; CON-6 secrets-scope expands to additional signing keys -- same job-condition pattern); first-PR-after-T04-lands (real-app smoke per Plan-Time Discipline Rule 5 analog -- pr-validate job runs end-to-end on GitHub Actions infrastructure for the first time); first-v*-tag-push (publish job runs end-to-end for first time; operator-side action gated by secrets.NPM_TOKEN configuration outside this plan authoring scope)"
key_files:
  - ".github/workflows/release.yml,tools/verify/m035-p02-release-workflow-shape.sh"
key_decisions:
  - "D001 (CI runner = ubuntu-latest); CON-6 (secrets-scoped to v* tag-push -- secrets.NPM_TOKEN appears ONLY inside npm-publish job body; pr-validate carries runtime negative-assertion step that fails on NPM_TOKEN env-var leak; static shape verifier asserts both surfaces); SC-14 (PR-build job-condition negative assertion encoded as runtime step -- Static analysis of workflow YAML at acceptance-battery time + runtime check at PR-build time = two complementary surfaces); D-RN-1 (npm scope @build-fractal/orchestrator drives publish target name; --access public flag required for scoped packages); CON-3/AP-009 (workflow YAML is single declarative document; inline run blocks use YAML pipe block scalar = single-script-shape not heredoc; no compound chains); AD-19 (verifier ships single-script Check shape with BATTERY-line output convention); CON-7 (pre-launch reversibility: T04 does not push tags or configure NPM_TOKEN; npm unpublish 72h policy window is operator-side); D002 (DRY_RUN=1 postinstall contract preserved by T03 surface invoked from workflow)"
patterns_established:
  - "CON-6-defense-in-depth (workflow uses TWO layers to enforce secrets-scoping: (a) npm-publish job if-condition startsWith(github.ref refs/tags/v) AND github.event_name == push -- structural gate; (b) pr-validate runtime step CON-6 -- assert no NPM_TOKEN access -- actively fails build if env var leaks; either layer alone load-bearing; both together survive single misconfig like org-level secret leak); tag-vs-manifest-version-gate (npm-publish first non-checkout step strips v from GITHUB_REF and compares to package.json version via grep + head + sed -E pipeline inside YAML pipe block scalar single-script-shape; failed gate exits non-zero before npm publish call preventing accidental publish of mismatched tag/version); job-condition-AND-clause-canonical-repo-canonical-event (publish-job condition combines startsWith(github.ref refs/tags/v) with github.event_name == push; event-name guard prevents fork-PR shenanigans -- fork PR that somehow tagged a commit cannot trigger publish since fork PRs are pull_request not push); verifier-asserts-shape-not-runtime-semantics (workflow-shape verifier checks CON-6 negative-assertion step EXISTS in workflow not that NPM_TOKEN is absent at runtime -- that is SC-14 runtime check inside actual GHA-rendered job; static analysis of YAML at acceptance-battery time + runtime check at PR-build time = two complementary surfaces); skeleton-with-extension-points-for-P03-P04-P05 (workflow comment header explicitly enumerates deferred scope: homebrew-bottle build P03; signed install.sh upload P05; GH release-notes P04; future plan-phase authors have clear contract); pending-T05-step-conditional-shape (if test -x verifier then bash verifier else echo SKIP fi -- canonical extension pattern for verifiers shipping in later tasks of same phase; lets release.yml land at T04 close without re-edit when T05 lands); POSIX-character-class-over-backslash-s-in-grep-E (BSD grep -E on macOS does not honor backslash-s reliably; substituted POSIX space class which is portable across BSD and GNU grep -E; mirrors P01.5 T04 BSD-grep-boundary-anchor-replacement pattern -- functionally identical both match space+tab+ff+vt; verifier runs locally on macOS during dispatch and on ubuntu-latest in CI so portability load-bearing); workflow-YAML-block-scalar-as-single-script-shape (multi-line shell uses YAML pipe block scalar which is single-script-shape per AP-009 -- not heredoc-with-expansion; satisfies CON-3 compound-chain guard while allowing multi-statement shell logic inside one declarative step)"
drill_down_paths:
  - ".orchestrator/milestones/M035/phases/P02/tasks/T04-release-workflow-PAYLOAD.md,.orchestrator/milestones/M035/phases/P02/tasks/T04-release-workflow-PLAN.md"
duration: "14m"
verification_result: "pass"
completed_at: "2026-05-08T20:21:06Z"
---

T04 authored the GitHub Actions release pipeline skeleton — `.github/workflows/release.yml` (160 lines, valid YAML) and the workflow-shape verifier `tools/verify/m035-p02-release-workflow-shape.sh` (75 lines, executable). The workflow is the **CON-6 secrets-scoping discipline surface at v1**: PR builds and main-branch pushes run the `pr-validate` job which never sees `NPM_TOKEN`; only `v*` tag-push events on the canonical repo run the `npm-publish` job that reads `secrets.NPM_TOKEN` into `NODE_AUTH_TOKEN` and runs `npm publish --access public`. Both jobs run on `ubuntu-latest` per D001; macOS-runner matrix expansion is deferred to P03 (homebrew arm needs `brew`).

The `pr-validate` job carries the load-bearing **SC-14 negative-assertion step** (`CON-6 — assert no NPM_TOKEN access`) — a runtime check that fails with `FAIL: NPM_TOKEN visible to pr-validate job — CON-6 violated` if the env var leaks into the PR-build context, ensuring secrets-scoping is enforced not just by job-condition but by an in-job runtime gate. The `npm-publish` job carries a tag-vs-package.json version-gate step that strips the `v` prefix from `GITHUB_REF` and asserts equality with `package.json`'s `version` field — preventing accidental publish of a tag that doesn't match the manifest. Pre-publish gates run all four T01-T03 verifiers (`m035-p02-package-json-shape.sh`, `m035-p02-bin-entry.sh`, `m035-p02-postinstall-shape.sh`) plus the cross-channel byte-equivalence test (T03 surface) before `npm publish`.

The workflow-shape verifier `tools/verify/m035-p02-release-workflow-shape.sh` ships the AD-19 single-script Check shape with the BATTERY-line output convention: 10 grep-contract assertions covering job presence (`pr-validate:`, `npm-publish:`), runner (`ubuntu-latest`), CON-6 secrets-scope (`startsWith(github.ref, 'refs/tags/v')` job condition + `secrets.NPM_TOKEN` reference inside `npm-publish`), SC-14 negative-assertion step presence (`NPM_TOKEN visible to pr-validate` literal), T03 test invocation (`tests/m035-acceptance/cross-channel-byte-equivalence.sh`), T01 verifier invocation (`m035-p02-package-json-shape.sh`), and the scoped-package publish flag (`npm publish --access public`). Output: `BATTERY: pass=10 fail=0`. The verifier exits non-zero on any FAIL via `[ "$fail" -eq 0 ]` final guard.

Verification: both gates PASS. `bash tools/verify/m035-p02-release-workflow-shape.sh` emits `BATTERY: pass=10 fail=0` (10 PASS lines covering file existence + 9 grep-contract surfaces). `bash scripts/util/run-probe.sh /tmp/m035-p02-t04-yaml-validate.sh` emits `PASS: release.yml is valid YAML` (Python's `yaml.safe_load` parses without error — guards against indentation-breakage in future edits).

One payload deviation documented: **POSIX `[[:space:]]` over `\s` in the verifier's grep -E patterns**. The payload's verbatim verifier body uses `\s` (e.g., `'^\s*pr-validate:'`, `'runs-on:\s*ubuntu-latest'`, `"startsWith\(github\.ref,\s*'refs/tags/v'\)"`). BSD `grep -E` on macOS does not honor `\s` reliably (it matches a literal `s` or fails to match the whitespace at all, depending on flavor) — this is the exact lesson captured by P01.5 T04's BSD-grep-boundary-anchor-replacement pattern. Substituted POSIX character class `[[:space:]]` which is portable across BSD and GNU grep -E. Functionally identical (both match `[ \t\f\v]`); the verifier runs locally on macOS during dispatch and on `ubuntu-latest` in CI, so portability is load-bearing. All 10 PASS lines emit cleanly. Captured here as deviation rather than silent fix because the payload's verifier body was specified verbatim.

Patterns established: (1) **CON-6 defense-in-depth** — the workflow uses TWO layers to enforce secrets-scoping: (a) the `npm-publish` job's `if:` condition (`startsWith(github.ref, 'refs/tags/v') && github.event_name == 'push'`) ensures the job only runs on tag-push events, and (b) the `pr-validate` job's `CON-6 — assert no NPM_TOKEN access` runtime step actively fails the PR build if the env var leaks. Either layer alone would be load-bearing; both together survive a single misconfiguration (e.g. an org-level secret leak setting). (2) **Tag-vs-manifest version gate** — the `npm-publish` job's first non-checkout step strips `v` from `GITHUB_REF` and compares to `package.json` version via `grep | head | sed -E '...'` (single-script-shape per AP-009; the YAML `run: |` block scalar is single-script-shape, not heredoc). Failed gate exits non-zero before any `npm publish` call, preventing accidental publish of mismatched tag/version. (3) **Job-condition AND-clause for canonical-repo + canonical-event** — the publish-job condition combines `startsWith(github.ref, 'refs/tags/v')` with `github.event_name == 'push'`. The event-name guard prevents fork-PR shenanigans (a PR from a fork that somehow tagged a commit can't trigger publish since fork PRs are `pull_request` events not `push`). (4) **Verifier asserts shape, not runtime semantics** — the workflow-shape verifier checks the CON-6 negative-assertion *step exists* in the workflow, not that NPM_TOKEN is absent at runtime (that's SC-14's runtime check inside the actual GHA-rendered job). Static analysis of the YAML at acceptance-battery time, runtime check at PR-build time — two complementary surfaces. (5) **Skeleton-with-extension-points-for-P03/P04/P05** — the workflow comment header explicitly enumerates what's deferred (homebrew bottle build → P03; signed `install.sh` upload → P05; GH release notes → P04) so future plan-phase authors extending the workflow have a clear contract. The `pending T05` step-conditional shape (`if [ -x ... ]; then bash ...; else echo SKIP; fi`) is the canonical extension pattern for verifiers that ship in later tasks of the same phase. (6) **POSIX `[[:space:]]` over `\s` in grep -E patterns** — captured by P01.5 T04's BSD-grep-boundary-anchor-replacement; reapplied here. macOS BSD grep -E does not honor `\s` reliably; POSIX char classes are portable.

Reversibility: removing `.github/workflows/release.yml` (the workflow file) and `tools/verify/m035-p02-release-workflow-shape.sh` (the verifier) plus the staged `/tmp/m035-p02-t04-yaml-validate.sh` probe unwinds the task. The `.github/workflows/` directory is created fresh in T04 (verified pre-condition: `.github/` previously held only `ISSUE_TEMPLATE/`); removing the directory is safe. T04 does NOT push tags, does NOT configure `secrets.NPM_TOKEN`, and does NOT trigger any real publish — those are operator-side actions outside this plan's authoring scope. Per CON-7, pre-launch reversal of an accidentally-published npm package is operator-side via `npm unpublish @build-fractal/orchestrator` within the 72h npm policy window; T04 does not exercise this surface.

Plan-Time Discipline Rule 5 analog: the workflow itself is exercised on GitHub Actions infrastructure at PR-build time and tag-push time — T04's local smoke is the YAML-validity check (step 5) and the static-shape verifier (step 4). The first PR after T04 lands runs the `pr-validate` job; flag this as the real-app smoke and confirm at phase-close. No real `npm publish` exercised at T04 time; the publish path is exercised only when the operator pushes a `v*` tag with `secrets.NPM_TOKEN` configured.
