---
schema_version: "1.0"
type: task-summary
id: "T02"
parent: "P03"
milestone: "M035"
provides:
  - ".github/workflows/release.yml extended additively with the homebrew-publish job (tag-push-only, needs: npm-publish, runs-on: ubuntu-latest, permissions: contents: read; 6-step chain: Checkout (canonical repo) + Resolve version + tarball URL + Download SHA256SUMS from release + Extract tarball SHA-256 (awk-based digest lookup with both './'-prefix and bare-filename forms) + Render formula (calls T01's render-formula.sh) + Clone tap repo (HTTPS x-access-token PAT clone of Build-Fractal/homebrew-orchestrator with operator-friendly empty-secret guard) + Write formula and push (no-op idempotent for re-runs)); pr-validate extended with one new step CON-6 — assert no HOMEBREW_TAP_TOKEN access immediately after the existing CON-6 NPM_TOKEN negative-assertion (em-dash U+2014 preserved); .orchestrator/DECISIONS.md extended with D008 (tap-push mechanism: secrets.HOMEBREW_TAP_TOKEN PAT scoped to Build-Fractal/homebrew-orchestrator:contents:write only, with NPM_TOKEN-symmetry + CON-6 job-condition gating + GitHub App migration as fast-follow rationale legs); tools/verify/m035-p03-release-workflow-homebrew-job.sh (8 required-pattern needles + file-exists guard, BATTERY: pass=9 fail=0); tools/verify/m035-p03-release-workflow-con6-homebrew.sh (negative-assertion step name needle + FAIL-message needle + file-exists guard, BATTERY: pass=3 fail=0)"
requires:
  - "from:P03/T01 what:packaging/homebrew/render-formula.sh CLI surface (--version/--url/--sha256 flags, 64-hex sha256 shape validation, sed token substitution, stdout-emits-rendered-formula contract); from:P03/T01 what:packaging/homebrew/orchestrator.rb.tmpl template (consumed by render-formula.sh); from:P02/P05 what:.github/workflows/release.yml pre-existing pr-validate + npm-publish jobs (CON-6 NPM_TOKEN negative-assertion as the insertion anchor; npm-publish's Confirm publish succeeded as the final step before homebrew-publish appends); from:P05 what:SHA256SUMS published to GitHub release by npm-publish job's signing pass (homebrew-publish reads via gh release download --pattern SHA256SUMS); from:operator MOS-1 what:Build-Fractal/homebrew-orchestrator public repo (default branch main, stub README); from:operator MOS-2 what:HOMEBREW_TAP_TOKEN Actions secret loaded on Build-Fractal/spec-kit-orchestrator (fine-grained PAT, contents:write only, scoped to Build-Fractal/homebrew-orchestrator); from:disk what:.orchestrator/DECISIONS.md D001-D007 prior rows preserved"
affects:
  - "P03/T03 (cross-channel byte-equivalence test for homebrew tarball will assert the formula's url + sha256 match the npm tarball published by P02 — D007 + D008 enforcement; the homebrew-publish job authored here is the live producer of the formula sha256 the test consumes); P03/T04 (operator runbook references HOMEBREW_TAP_TOKEN secret name + the tap-push job-condition predicate); P04 (SC-14 dry-run-tag CI invocation exercises this job's end-to-end behavior including the live tap-push to Build-Fractal/homebrew-orchestrator); P05 (signing pipeline is consumed read-only — homebrew-publish reads SHA256SUMS from the GitHub release rather than re-signing); pr-validate (carries one additional CON-6 negative-assertion step; no other steps modified, no other jobs modified)"
key_files:
  - ".github/workflows/release.yml,tools/verify/m035-p03-release-workflow-homebrew-job.sh,tools/verify/m035-p03-release-workflow-con6-homebrew.sh,.orchestrator/DECISIONS.md"
key_decisions:
  - "D008 (tap-push mechanism: secrets.HOMEBREW_TAP_TOKEN PAT, contents:write only, scoped to Build-Fractal/homebrew-orchestrator); CON-6 (secret-scope discipline — homebrew-publish job-condition predicate identical to npm-publish: startsWith(github.ref, 'refs/tags/v') && github.event_name == 'push'); CON-6 negative-assertion shape (pr-validate carries explicit `[ -n \"${HOMEBREW_TAP_TOKEN:-}\" ] && exit 1` step asserting PAT not visible in PR context — symmetric with NPM_TOKEN); D007 binding (homebrew-publish reads SHA256SUMS from the GitHub release rather than re-signing — single signing chain preserved); FR-9 (job has no formula-specific install logic surface — calls render-formula.sh + writes Formula/orchestrator.rb only); idempotent-no-op-on-re-run (git diff --staged --quiet branch allows re-running the job for the same tag without erroring on no-op commit)"
patterns_established:
  - "tap-push-via-x-access-token-PAT (HTTPS git clone embeds the PAT as `https://x-access-token:${TOKEN}@github.com/...` — GitHub's documented PAT-as-password convention, no SSH key surface, no GitHub App ownership semantics; pairs with the empty-secret guard that surfaces operator MOS-2 as the FAIL-message remediation pointer); awk-digest-extraction-from-SHA256SUMS (awk -v t=\"$TAR\" '$2 == \"./\" t || $2 == t { print $1 }' handles both `./<filename>` and bare `<filename>` forms — defense against shasum -a 256's `./` prefix on `find . -maxdepth 1 -type f`-fed input as P05's SHA256SUMS generation does); idempotent-no-op-job-shape (`if git diff --staged --quiet; then exit 0; fi` before commit — re-running the job for the same tag is a no-op rather than an error, supports operator workflows like `gh workflow run` against an already-published tag); CON-6-negative-assertion-symmetry (every secret added to a CON-6-scoped publishing job MUST also gain a paired negative-assertion step in pr-validate — NPM_TOKEN sets the precedent, HOMEBREW_TAP_TOKEN follows; future channel additions extend the same pattern); job-level-permissions-default-read (homebrew-publish does NOT need contents:write on the canonical repo because the tap-push targets a separate repo via PAT — workflow-level `permissions: contents: read` inherited unchanged, no job-level override needed; only the npm-publish job needs the contents:write override for `gh release create`)"
drill_down_paths:
  - ".orchestrator/milestones/M035/phases/P03/tasks/T02-release-workflow-homebrew-job-PLAN.md"
duration: "8m"
verification_result: "pass"
completed_at: "2026-05-09T00:00:00Z"
---

T02 extended `.github/workflows/release.yml` with the `homebrew-publish` job and an additional CON-6 negative-assertion step in `pr-validate`, appended D008 to `.orchestrator/DECISIONS.md`, and authored the two task-grain shape verifiers under `tools/verify/m035-p03-release-workflow-*.sh`.

Sanity-check (plan step 1) confirmed the existing workflow shape: `npm-publish` is the exact job name, its final step is `Confirm publish succeeded`, and `pr-validate` carries `CON-6 — assert no NPM_TOKEN access` as its final step (the insertion anchor for the new HOMEBREW_TAP_TOKEN negative-assertion). No drift, no improvisation needed — the plan's prescribed insertion points held byte-for-byte.

The new `homebrew-publish` job sits at sibling-job indentation under `jobs:` (2 spaces, matching `pr-validate` and `npm-publish`). Job-condition predicate `startsWith(github.ref, 'refs/tags/v') && github.event_name == 'push'` is identical to `npm-publish` per CON-6 — diverging predicates would introduce a per-channel attack surface. `needs: npm-publish` ensures the tarball + SHA256SUMS are uploaded to the GitHub release before homebrew reads from them. The 6-step chain: Checkout (canonical repo) → Resolve version + tarball URL (computes the GitHub release download URL + tarball filename + version from `GITHUB_REF`) → Download SHA256SUMS from release (via `gh release download --pattern SHA256SUMS`) → Extract tarball SHA-256 (awk lookup tolerating both `./<filename>` and bare `<filename>` forms) → Render formula (calls T01's `render-formula.sh` with --version/--url/--sha256 flags) → Clone tap repo (HTTPS `x-access-token:${HOMEBREW_TAP_TOKEN}` clone with empty-secret guard pointing at MOS-2 in `references/installation.md`) → Write formula and push (idempotent: `git diff --staged --quiet` branch makes re-runs a no-op rather than an error). `permissions: contents: read` at the job level — the tap-push targets a separate repo via PAT, so the canonical repo's contents:write override is not needed.

The CON-6 negative-assertion step inserted into `pr-validate` (immediately after the existing NPM_TOKEN assertion) carries the em-dash `—` (U+2014) in its name verbatim per plan: `CON-6 — assert no HOMEBREW_TAP_TOKEN access`. The verifier matches via `grep -qF` so the em-dash must survive the paste — confirmed via `grep -qF` running green. The step shape is symmetric with the NPM_TOKEN precedent: `[ -n "${HOMEBREW_TAP_TOKEN:-}" ] && echo FAIL && exit 1`, asserting the PAT is empty in PR context.

D008 records the tap-push mechanism decision: `secrets.HOMEBREW_TAP_TOKEN` PAT scoped to `Build-Fractal/homebrew-orchestrator:contents:write` only, with three rationale legs — (1) symmetry with the `secrets.NPM_TOKEN` precedent reduces operator review-cadence load; (2) CON-6 job-condition gating identical to npm closes the PR-build exfiltration vector + `pr-validate` carries the negative-assertion step; (3) GitHub App migration is a clean fast-follow if rotation friction surfaces — `homebrew-orchestrator` is the only repo the PAT writes to, so swapping the auth principal is a one-secret rotation with no formula changes. Bound to FR-9 / CON-6 / SC-14 / MOS-2.

Verification: `bash tools/verify/m035-p03-release-workflow-homebrew-job.sh` → `BATTERY: pass=9 fail=0` (file-exists + 8 required-pattern needles: `homebrew-publish:`, `needs: npm-publish`, `startsWith(github.ref, 'refs/tags/v')`, `secrets.HOMEBREW_TAP_TOKEN`, `Build-Fractal/homebrew-orchestrator`, `render-formula.sh`, `Formula/orchestrator.rb`, `formula: bump to v`). `bash tools/verify/m035-p03-release-workflow-con6-homebrew.sh` → `BATTERY: pass=3 fail=0` (file-exists + step-name needle + FAIL-message needle). `grep -qE '^### D008' .orchestrator/DECISIONS.md` returns exit 0 silently — D008 heading present at column 0 in canonical `### DXXX — <decision>` shape (no `{ #dr-code-NNN }` anchor). `python3 -c 'import yaml,sys; yaml.safe_load(open(".github/workflows/release.yml").read())'` parses without error — workflow YAML is structurally valid.

Line-count floors satisfied: `release.yml` 351 lines (was 257; ≥320 expected per plan); both verifiers ≥30 lines (37 + 30). Both verifiers `chmod +x` confirmed. Branch posture preserved: stayed on `main` the entire run, no `git checkout` / `git switch` / detached HEAD.

Reversibility: removing the two new verifier files + reverting the homebrew-publish job + the CON-6 HOMEBREW_TAP_TOKEN step + the D008 block in `.orchestrator/DECISIONS.md` unwinds T02 cleanly. The live tap-push only fires on a real `v*` tag-push against the canonical repo (CON-6) — T02 is pure authoring; P04's SC-14 dry-run-tag CI invocation is what actually exercises the end-to-end tap-push.
