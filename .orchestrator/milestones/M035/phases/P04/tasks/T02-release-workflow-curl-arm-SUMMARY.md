---
schema_version: "1.0"
type: task-summary
id: "T02"
parent: "P04"
milestone: "M035"
provides:
  - ".github/workflows/release.yml extended with three additive edits: (1) `Stage release artifacts` step (npm-publish job) replaces the `# P04 hook: install.sh would land here.` comment with `cp packaging/install/install.sh release-artifacts/` so the file lands alongside the npm tarball — the existing P05 cosign-keyless loop signs install.sh automatically (`for artifact in *`), the SHA256SUMS step computes its digest automatically, and `gh release create release-artifacts/*` uploads it as a release asset automatically (no edits to those steps required); (2) `npm-publish` job header carries `timeout-minutes: 20` (CON-8 / D010, 6× headroom over nominal ~3min wall-clock); (3) `homebrew-publish` job header carries `timeout-minutes: 20` (same shape, per-job timeout means a hung homebrew-publish doesn't block npm-publish's success signal). .orchestrator/DECISIONS.md extended with D010 (CON-8 release-workflow timeout = 20 minutes on ubuntu-latest with escalation clause for >15min observed wall-clock). tools/verify/m035-p04-release-workflow-curl-arm.sh authored as 6-needle shape verifier (release.yml exists + install.sh staging line + timeout-minutes: 20 occurs >=2 times via `^[[:space:]]+timeout-minutes:[[:space:]]+20$` regex count + CON-8 reference present + no curl-channel-specific secret regression-guard via `secrets\\.(CURL|INSTALL_SH|PIPE_BASH)` negative-grep + D010 row in DECISIONS.md). BATTERY: pass=6 fail=0."
requires:
  - "from:P04/T01 what:packaging/install/install.sh executable on disk (release.yml `cp` step requires the file to exist before staging); from:disk what:.github/workflows/release.yml at P02+P03+P05 cumulative shape (three jobs pr-validate + npm-publish + homebrew-publish, P05 cosign-keyless loop, P05 SHA256SUMS step, P05 `gh release create release-artifacts/*` glob); from:P04/T01 what:.orchestrator/DECISIONS.md with D009 latest (T02 appends D010 after the D009 block)"
affects:
  - "P04/T03 (cross-channel byte-equivalence test relies on the `release-artifacts/`-layout contract that T02 binds: `build-fractal-orchestrator-<TAG>.tgz` + `install.sh` + `SHA256SUMS` co-located, which T03's `M035_P04_LOCAL_TARBALL` test-mode reads from indirectly via the npm pack output); P04/T04 (installation.md § Releasing via curl-pipe-bash documents the D010 / CON-8 20-minute timeout binding for operator-facing release rehearsal); MOS-5 (synthetic v0.0.0-test tag exercise validates the 20-minute timeout against real CI wall-clock); SC-14 (synthetic-tag-push CI completes within documented timeout — D010 makes the value explicit)"
key_files:
  - ".github/workflows/release.yml,tools/verify/m035-p04-release-workflow-curl-arm.sh,.orchestrator/DECISIONS.md"
key_decisions:
  - "D010 (CON-8 release-workflow CI timeout = 20 minutes on ubuntu-latest, job-level not workflow-level, with escalation clause if wall-clock consistently >15min across three synthetic-tag runs); CON-8 (escalation clause as load-bearing safety net, declarative-not-automated at v1, post-launch fast-follow could add CI-side measurement-and-alert step); CON-6 (no new secrets — install.sh requires no curl-channel-specific secret, existing NPM_TOKEN + HOMEBREW_TAP_TOKEN + GITHUB_TOKEN set is sufficient, regression-guard verifier needle catches future drift); D007 (single-source-of-truth tarball reused for install.sh staging — no separate curl-tarball, install.sh is just one more file in release-artifacts/); D009 (install.sh URL host = GitHub release asset URL — release.yml's `gh release create release-artifacts/*` glob makes this work automatically without per-channel logic)"
patterns_established:
  - "additive-glob-driven-release-artifact-extension (new release artifacts land in release-artifacts/ via `cp`/`mv`; downstream cosign-loop + SHA256SUMS + `gh release create release-artifacts/*` pick up new files automatically without per-channel logic — extending the release surface is one `cp` line + zero edits to the signing/uploading steps); job-level-timeout-minutes-with-CON-comment-cross-reference (`timeout-minutes: 20` annotated with `# CON-8 (M035 P04 T02 / D010): ...` so future plan-phase authors reading release.yml see the rationale inline; per-job timeout is GitHub-Actions native — workflow-level timeout doesn't exist); regression-guard-via-negative-grep (verifier needle 5: `grep -qE 'secrets\\.(CURL|INSTALL_SH|PIPE_BASH)'` — failing if matched, passing if absent — catches a future plan-phase author accidentally introducing a curl-channel secret); upstream-verifier-regression-discipline (T02's edits are additive but step 9 re-runs all three pre-existing release.yml verifiers `m035-p02-release-workflow-shape.sh` + `m035-p03-release-workflow-homebrew-job.sh` + `m035-p05-release-workflow-signing-shape.sh` to confirm no contract regression — all green post-edit)"
drill_down_paths:
  - ".orchestrator/milestones/M035/phases/P04/tasks/T02-release-workflow-curl-arm-PLAN.md"
duration: "8m"
verification_result: "pass"
completed_at: "2026-05-09T00:00:00Z"
---

T02 extended `.github/workflows/release.yml` with three additive edits + recorded D010 in DECISIONS.md + authored the task-grain shape verifier. Single atomic commit `b81118d3` on `main` adds 120 insertions / 1 deletion across three files; bookkeeping commit `c11430a8` follows the convention of separate task-summary + execution-log commit per P03 T01 / P01 T04 / M030 P01 T02 precedent.

Edit 1 (release-artifacts staging): the `Stage release artifacts` step in the `npm-publish` job replaces the placeholder `# P04 hook: install.sh would land here.` comment with `cp packaging/install/install.sh release-artifacts/` (and a five-line comment block explaining why no further step edits are required). The downstream P05 cosign-keyless loop iterates `for artifact in *` and skips `*.sig`/`*.pem`/`SHA256SUMS`, so it signs install.sh automatically. The SHA256SUMS step runs `shasum -a 256 *` and computes install.sh's digest automatically. The `gh release create release-artifacts/*` glob uploads install.sh as a release asset automatically. This is the additive-glob-driven release-artifact-extension pattern: extending the release surface is one `cp` line + zero edits to the signing/uploading steps, which is why the homebrew bottle (P03) + install.sh (P04) + future channels can ship without re-litigating the release-asset distribution model.

Edit 2 + 3 (job-level timeouts): both `npm-publish` (line 106) and `homebrew-publish` (line 274) job headers now carry `timeout-minutes: 20` immediately after `runs-on: ubuntu-latest`, each annotated with a CON-8 comment cross-referencing D010. Per-job timeout (not workflow-level — GitHub Actions doesn't support workflow-level timeout-minutes) means a hung homebrew-publish doesn't block npm-publish's success signal, which matters because npm-publish runs FIRST in the release-workflow DAG and its success is the gate for the homebrew tarball reference. 20 minutes provides 6× headroom over the nominal ~3-minute wall-clock (npm publish ~30s + npm pack ~5s + cosign ~30s + SHA256SUMS ~1s + gh release create ~10s + homebrew-publish ~45s). CON-8's escalation clause makes the contract explicit: if wall-clock consistently >15min across three synthetic-tag runs, plan-phase author splits the workflow into parallel jobs or documents a revised timeout.

D010 records the CI timeout decision: `### D010 — Release-workflow CI timeout: 20 minutes on ubuntu-latest (CON-8)` with four rationale legs (spec recommendation honored, headroom over typical run, CON-8 escalation as safety net, job-level not workflow-level). D010 binds to SC-14 / CON-8 / FR-10 and cross-references release.yml + `references/installation.md § Releasing via curl-pipe-bash` (T04's documentation deliverable).

Verification: `bash tools/verify/m035-p04-release-workflow-curl-arm.sh` → `BATTERY: pass=6 fail=0`. The 6 grep needles cover (1) workflow file existence, (2) install.sh staging line via `grep -F`, (3) `timeout-minutes: 20` count >=2 (one per publishing job, asserted via `^[[:space:]]+timeout-minutes:[[:space:]]+20$` regex match count), (4) CON-8 reference in workflow (cross-link to DECISIONS), (5) no curl-channel-specific secret introduced (regression guard via negative `grep -qE 'secrets\\.(CURL|INSTALL_SH|PIPE_BASH)'`), (6) D010 row in DECISIONS.md.

Upstream regression discipline: re-ran all three pre-existing release.yml verifiers post-edit:
- `bash tools/verify/m035-p02-release-workflow-shape.sh` → `BATTERY: pass=10 fail=0` (P02 npm-publish shape preserved)
- `bash tools/verify/m035-p03-release-workflow-homebrew-job.sh` → `BATTERY: pass=9 fail=0` (P03 homebrew-publish shape preserved)
- `bash tools/verify/m035-p05-release-workflow-signing-shape.sh` → `BATTERY: pass=10 fail=0` (P05 cosign + SHA256SUMS + gh release create shape preserved)

YAML structural shape-guard via `bash scripts/util/run-probe.sh /tmp/m035-p04-t02-yaml-validate.sh` → `PASS: release.yml YAML parses` (python yaml.safe_load on the modified file). No indentation drift, no structural breakage from the three Edit invocations.

Branch posture preserved: stayed on `main` the entire run, no detached HEAD, no branch switching. Single atomic commit per RENAME-PLAN convention § 5. Working tree clean post-commit (modulo the bookkeeping pair `c11430a8` for T01-SUMMARY + execution-log).

Reversibility: `git revert b81118d3` undoes the workflow + DECISIONS + verifier in one step. The release pipeline doesn't fire until a `v*` tag is pushed (CON-6 binding), so T02 has no external side effects pre-tag-push. MOS-5 synthetic v0.0.0-test tag exercise (operator-only, deferred to launch rehearsal) is the first end-to-end exercise of the install.sh staging + 20-minute-timeout binding.
