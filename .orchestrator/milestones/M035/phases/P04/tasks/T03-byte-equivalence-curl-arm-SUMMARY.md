---
schema_version: "1.0"
type: task-summary
id: "T03"
parent: "P04"
milestone: "M035"
provides:
  - "tests/m035-acceptance/cross-channel-byte-equivalence.sh extended with the curl-pipe-bash third-channel arm: replaces the `SKIP: pending P04 -- curl-pipe-bash-channel hash assertion` stub with a real arm that (a) re-uses the npm-channel `npm pack` tarball (D007 single-source-of-truth, same pattern as P03's homebrew arm), (b) invokes `bash packaging/install/install.sh` with M035_P04_LOCAL_TARBALL + M035_P04_STAGE_ONLY=1 + M035_P04_STAGE_DIR test-mode env vars to short-circuit download/SHA-verify/dispatch and just stage the tarball into a deterministic fixture dir, (c) hashes the staged tree via the existing _byte-equivalence-hash.sh helper with CHANNEL=curl-pipe-bash exclusion list extracted via _exclusion-list-by-channel.sh, (d) emits CURL_HASH=<sha> on stdout. Section 5's existing 2-way `NPM_HASH = HOMEBREW_HASH` assertion is lifted to a 3-way `[ NPM_HASH = HOMEBREW_HASH ] && [ HOMEBREW_HASH = CURL_HASH ]` (CON-5 / Constitution Principle XVI third-channel coverage); 2-way fallback is preserved when curl arm SKIPs (install.sh missing on disk OR TARBALL unavailable). EXIT trap extended to clean up CURL_FIXTURE alongside NPM_FIXTURE + BREW_FIXTURE. tools/verify/m035-p04-byte-equivalence-curl-arm.sh authored as 9-needle verifier with functional smoke: 8 static shape assertions (file exists + SKIP-stub-removed + CURL_HASH usage + install.sh invocation + M035_P04_LOCAL_TARBALL hook + M035_P04_STAGE_ONLY hook + 3-way PASS-message + CHANNEL=curl-pipe-bash extraction) + needle 9 functional smoke (test-IS-the-smoke pattern from P02 T03 — runs the test end-to-end, distinguishes 3-way-PASS from 2-way-fallback-PASS from genuine FAIL via tempfile capture + grep, accepts non-zero exit only when npm is genuinely absent on PATH). BATTERY: pass=9 fail=0."
requires:
  - "from:P04/T01 what:packaging/install/install.sh executable on disk with M035_P04_LOCAL_TARBALL + M035_P04_STAGE_ONLY + M035_P04_STAGE_DIR test-mode env-var hooks (load-bearing for the curl arm — without these, the test would need network + real GitHub release artifacts to exercise install.sh's code path); from:P04/T02 what:install.sh staged into release-artifacts/ at release time (T03 doesn't directly read release-artifacts/, but T02's release-artifacts/-layout contract — `build-fractal-orchestrator-<TAG>.tgz` + install.sh + SHA256SUMS co-located — is what install.sh's production-mode download targets, which the test-mode short-circuits via LOCAL_TARBALL); from:disk what:tests/m035-acceptance/_byte-equivalence-hash.sh STAGED + EXCLUSION_LIST env-var contract (P02 T03); from:disk what:tests/m035-acceptance/_exclusion-list-by-channel.sh CHANNEL+INSTALLATION_MD env-var contract with comma-separated channel-list parsing supporting CHANNEL=curl-pipe-bash without edit (P03 T03)"
affects:
  - "P04/T04 (phase-suite aggregator chains m035-p04-byte-equivalence-curl-arm.sh as one of its six per-truth verifiers — T03's BATTERY: pass=9 fail=0 contributes to the consolidated rollup); MOS-4 (operator-side first-release smoke validates that install.sh's production-mode download path produces the same tree shape that T03 exercises in test-mode); SC-10 (cross-channel byte-equivalence covers all three published channels at v1 — npm + homebrew + curl-pipe-bash); CON-5 (cross-channel byte-equivalence as structural invariant gains its third channel coverage); Constitution Principle XVI (Distribution Surface Integrity — three-channel proof of byte-equivalent surfaces is the load-bearing test)"
key_files:
  - "tests/m035-acceptance/cross-channel-byte-equivalence.sh,tools/verify/m035-p04-byte-equivalence-curl-arm.sh"
key_decisions:
  - "D007 (single-tarball cross-channel binding — curl arm reuses the npm pack tarball, no separate curl-tarball, structural byte-equivalence is preserved by sharing the same source artifact); CON-5 (cross-channel byte-equivalence as structural invariant — 3-way assertion is the load-bearing test); Constitution Principle XVI (Distribution Surface Integrity — three-channel proof); FR-10 (curl-pipe-bash channel as a first-class published surface); 2-way-fallback-preservation (when curl arm SKIPs, the test gracefully falls back to NPM_HASH = HOMEBREW_HASH; CI runners that lack install.sh in some pre-T01 fixture or that have npm absent on PATH still get a meaningful partial-coverage assertion); BSD-sed paper-cut from P05-SUMMARY remains unfixed (post-launch fast-follow scope — T03's curl arm inherits the silently-degraded EXCLUSION_LIST behavior, which works in T03's favor: with no real exclusion applied, all three arms hash the same flat tarball-extract and produce identical hashes; 3-way equality holds today 'by accident' the same way P03's 2-way equality holds)"
patterns_established:
  - "test-IS-the-smoke-functional-verifier (verifier needle 9: run the test end-to-end with stdout captured to a tempfile, grep the captured log for the canonical PASS message — distinguishes 3-way-PASS from 2-way-fallback-PASS from genuine FAIL; AP-009-safe single-script shape, alternative `if bash $TEST 2>&1 | grep -q 'pattern'` is `$(...)` -with-pipe and trips the harness heuristic); load-bearing-default-off-test-mode-hooks-actually-load-bearing (T01 declared M035_P04_LOCAL_TARBALL + M035_P04_STAGE_ONLY + M035_P04_STAGE_DIR; T03 is the consumer that proves they pull their weight — without these hooks, the curl arm would need network access + real GitHub release artifacts, breaking the test's locality and CI portability); 3-way-supersedes-2-way-with-fallback-preservation (the test's section 5 assertion ladders: 3-way fires when ALL three arms emit hashes; 2-way fallback fires when only npm + homebrew emit hashes; SKIP fires when fewer than two arms produce hashes — graceful degradation matches P02's CI-portability discipline); EXIT-trap-extension-not-replacement (existing trap was `rm -rf $NPM_FIXTURE $BREW_FIXTURE`; T03 extends to `rm -rf $NPM_FIXTURE $BREW_FIXTURE $CURL_FIXTURE` — preserve all prior cleanup obligations rather than rewriting the trap)"
drill_down_paths:
  - ".orchestrator/milestones/M035/phases/P04/tasks/T03-byte-equivalence-curl-arm-PLAN.md"
duration: "9m"
verification_result: "pass"
completed_at: "2026-05-09T00:00:00Z"
---

T03 extended `tests/m035-acceptance/cross-channel-byte-equivalence.sh` with the curl-pipe-bash third-channel arm + lifted the existing 2-way assertion to a 3-way assertion + authored the task-grain functional verifier. Single atomic commit `f81f3dcf` on `main` adds 170 insertions / 8 deletions across two files.

The new section 4 (curl arm) executes between the homebrew arm (section 3) and the cross-channel equality assertion (section 5). It pre-computes the curl-pipe-bash exclusion list via `CHANNEL=curl-pipe-bash bash _exclusion-list-by-channel.sh` (the existing P03 helper supports this channel name without edit per its comma-separated channel-list parsing), guards on `$TARBALL` availability + install.sh executable presence (graceful SKIP if either is missing), creates a `CURL_FIXTURE` mktemp dir, extends the EXIT trap to clean up all three fixtures, then invokes `bash packaging/install/install.sh` with the three M035_P04_* test-mode env vars exported. install.sh's STAGE_ONLY mode short-circuits the download + SHA-verify + dispatch chain — the file just extracts the local tarball into the fixture and exits. The test then runs `_byte-equivalence-hash.sh` against the staged tree with the curl-pipe-bash exclusion list and emits `CURL_HASH=<sha>` on stdout.

Section 5's assertion ladder is now three-tiered:
1. **3-way path** (fires when ALL three arms emit hashes) — the load-bearing CON-5 / Constitution Principle XVI test, with FAIL message displaying all three hashes for divergence diagnosis.
2. **2-way fallback** (fires when only npm + homebrew emit hashes — curl arm SKIPped) — preserves the pre-T03 assertion semantics so CI runners lacking install.sh or npm don't break the test.
3. **SKIP** (fires when fewer than two arms produce hashes) — graceful degradation.

Verification: `bash tools/verify/m035-p04-byte-equivalence-curl-arm.sh` → `BATTERY: pass=9 fail=0`. The 9 needles cover (1) test file exists, (2) `SKIP: pending P04` stub is GONE (negative grep), (3) `CURL_HASH=` variable used, (4) install.sh invoked from test, (5) M035_P04_LOCAL_TARBALL test-mode, (6) M035_P04_STAGE_ONLY test-mode, (7) 3-way `NPM_HASH = HOMEBREW_HASH = CURL_HASH` PASS message, (8) `CHANNEL=curl-pipe-bash` exclusion-list extraction, (9) **functional smoke** — runs the test end-to-end with stdout captured to a tempfile, greps for the canonical 3-way PASS message, distinguishes 3-way-PASS from 2-way-fallback-PASS from genuine FAIL, accepts non-zero exit only when `npm` is genuinely absent on PATH (graceful CI-portability). Live run on the dev workstation (npm + brew + install.sh all present) produced the 3-way PASS path: `PASS: cross-channel byte-equivalence (3-way) — NPM_HASH = HOMEBREW_HASH = CURL_HASH`.

Upstream regression: `bash tools/verify/m035-p03-byte-equivalence-homebrew-arm.sh` → `BATTERY: pass=9 fail=0`. T03's edits did not regress P03's homebrew arm — the homebrew arm runs in section 3 (untouched by T03) and the existing 2-way assertion's PASS message is preserved as the 2-way-fallback PASS message in the new ladder.

BSD-sed paper-cut inheritance: `_byte-equivalence-hash.sh` uses a `sed -E '[][.^$*+?(){}|\\]'` bracket class that errors on macOS BSD sed, silently degrading EXCLUSION_LIST to a no-op locally. P03's NPM_HASH = HOMEBREW_HASH equality holds today "by accident" (single tarball, identical extract semantics, no real per-channel exclusion ever applied). T03 inherits this: with no real exclusion applied, all three arms hash the same flat tarball-extract and produce identical hashes. The 3-way equality holds today "by accident" the same way the 2-way does. The fix is post-launch cross-channel hardening; P04 inherits the paper-cut explicitly per the user-prompt's "two paper-cuts surfaced this week, neither blocks P04 plan-phase" guidance.

Branch posture preserved: `main` throughout, no detached HEAD. Single atomic commit per RENAME-PLAN convention § 5. Bookkeeping (T03-SUMMARY.md + execution-log.jsonl entry) follows in a separate commit per the established pattern.

Reversibility: `git revert f81f3dcf` restores the SKIP stub + 2-way assertion. The verifier file deletion is also rolled back. The test continues to function as a 2-way assertion (P03 T03 shape) without external side effects, since the test does not write to the canonical repo or trigger any release-side automation.
