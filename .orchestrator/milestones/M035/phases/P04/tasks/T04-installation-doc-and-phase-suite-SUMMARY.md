---
schema_version: "1.0"
type: task-summary
id: "T04"
parent: "P04"
milestone: "M035"
provides:
  - "references/installation.md extended with two new H2 sections appended after `## Releasing via Homebrew`: (1) `## Installing via curl-pipe-bash` consumer-facing recipe with `curl -sSL .../install.sh | bash` (latest), `ORCHESTRATOR_VERSION=v1.0.0 curl ...` (pinned), `orchestrator --version` smoke, per-project `/orchestrator-init` setup, `bash packaging/install/install-claude-code.sh --uninstall` recipe, CC-only v1 runtime support note + M009 deferral pointer, optional integrity-verification cross-link to `## Verifying integrity`; (2) `## Releasing via curl-pipe-bash` operator-facing procedure with D009 (URL host) + D010/CON-8 (20-minute timeout + escalation clause) + D011 (manual stable releases pre-1.0 cadence) inline rationale + MOS-4 (one-time `curl … | bash` smoke against first published release) + MOS-5 (synthetic v0.0.0-test tag push against fork) operator-only validation procedures. commands/update.md `## Update sources` H2 extended with a fourth bullet `update_source: curl-pipe-bash` documenting the curl-pipe-bash channel re-runs install.sh from the latest release URL on update; consumer-facing recipe + operator-facing release procedure cross-links into installation.md; P06 wires the actual dispatch via scripts/lifecycle/run-update.sh, P04 records the surface symmetric with the homebrew row above. .orchestrator/DECISIONS.md extended with D011 (release cadence: manual stable releases pre-1.0, operator-policy not code surface, no CI cron / no PR-merge auto-tagging / no conventional-commits parsing at v1, reversible to automatic post-1.0 cadence as a fast-follow). Three new task-grain verifiers: (a) tools/verify/m035-p04-installation-doc-curl.sh with 13 grep needles (consumer-facing + operator-facing checks; `--uninstall` needle uses `grep -qF -- '--uninstall'` BSD-grep-safe form discovered during T04 execution); (b) tools/verify/m035-p04-update-skill-doc-curl.sh with 4 grep needles; (c) tools/verify/m035-p04-phase-suite.sh aggregator chaining all five P04 children + self-reference (P03's chain-the-children form, NOT P05's sum-the-counters form). Phase-suite emits BATTERY: pass=6 fail=0."
requires:
  - "from:P04/T01 what:packaging/install/install.sh + D009 binding (operator-facing § Releasing via curl-pipe-bash references D009 inline); from:P04/T02 what:.github/workflows/release.yml + D010/CON-8 binding (operator-facing prose references D010/CON-8 inline); from:P04/T03 what:tests/m035-acceptance/cross-channel-byte-equivalence.sh curl arm (referenced indirectly via FR-14/SC-10 cross-channel-byte-equivalence contract); from:disk what:references/installation.md with ## Installing via Homebrew + ## Releasing via Homebrew + ## Verifying integrity sections present (P03 T04 + P05 T04); from:disk what:commands/update.md with ## Update sources H2 enumerating git/npm/homebrew (P03 T04); from:disk what:.orchestrator/DECISIONS.md with D009 (T01) + D010 (T02) recorded — D011 appends after the D010 block"
affects:
  - "P04/M035 phase closure (T04 is the last task in P04; phase-suite aggregator is the on-disk evidence for P04 closure under the validate-milestone shape contract); P06 (orchestrator:update first-class command — P06 wires update_source: curl-pipe-bash dispatch in scripts/lifecycle/run-update.sh consuming the surface T04 records here); MOS-4/MOS-5 (operator-side first-release smoke + synthetic v0.0.0-test tag exercise — installation.md § Releasing via curl-pipe-bash is the canonical operator-facing playbook); SC-14 (synthetic-tag-push CI exercises the URL surface — operator-validated at MOS-5 per the documented procedure); SC-10 (cross-channel byte-equivalence triple coverage — installation.md cross-links to ## Verifying integrity and the ## Channel-specific metadata files table)"
key_files:
  - "references/installation.md,commands/update.md,.orchestrator/DECISIONS.md,tools/verify/m035-p04-installation-doc-curl.sh,tools/verify/m035-p04-update-skill-doc-curl.sh,tools/verify/m035-p04-phase-suite.sh"
key_decisions:
  - "D011 (release cadence: manual stable releases pre-1.0, operator-policy not code surface — release-workflow already fires on v* tag push, no CI cron / no PR-merge auto-tagging / no conventional-commits parsing at v1, post-1.0 automatic cadence is post-launch fast-follow scope, symmetric with operator-driven first-release MOS-4/MOS-5); operator-facing-prose-duplicates-DECISIONS-rationale (operators reading installation.md don't need to context-switch to .orchestrator/DECISIONS.md to understand release procedure — DECISIONS.md is the canonical record, installation.md prose is the operator-facing exposition; verifier greps for the literal D-numbers + CON-8 + MOS-numbers to enforce the linkage structurally); BSD-grep-safe-flag-pattern (grep -qF -- '--uninstall' discovered as required form during T04 execution — BSD grep treats `--uninstall` as a flag without the `--` separator, GNU grep is more permissive; pattern-after-`--` is portable across both); chain-the-children-aggregator-form (mirrors P03's pattern: pass = number of green children + 1 self-reference; P05's sum-the-counters form would double-count via the aggregator's own pass count when children have heterogeneous BATTERY counts — T01 pass=13, T02 pass=6, T03 pass=9, T04 children pass=4 + pass=13)"
patterns_established:
  - "two-H2-section-append-at-EOF (consumer-facing § Installing via X + operator-facing § Releasing via X — symmetric with the existing § Installing via Homebrew + § Releasing via Homebrew pair from P03 T04; preserves operator readability via parallel structure rather than splitting an existing section); inline-D-number-rationale-blocks-in-operator-prose (operator-facing § Releasing via curl-pipe-bash duplicates the D009/D010/D011 rationale inline rather than just cross-linking to DECISIONS.md — ergonomic for operators reading the playbook end-to-end at release time, structurally enforced by verifier grepping for literal D-numbers); MOS-numbered-operator-procedures (MOS-4 first-release smoke + MOS-5 synthetic-tag-push procedures live in installation.md as named callouts — symmetric with P03 T04's MOS-1/MOS-2/MOS-3 pattern, gives operators a stable reference to invoke at launch-rehearsal time); phase-suite-aggregator-chain-children-form (verifier-unit counting: pass = number of green children + 1 self-reference; aggregator's `if [ ! -x \"$v\" ]` guard catches missing/non-executable verifiers as FAILs without crashing; tempfile-capture-and-cat-on-failure for surfacing child verifier diagnostics on demand)"
drill_down_paths:
  - ".orchestrator/milestones/M035/phases/P04/tasks/T04-installation-doc-and-phase-suite-PLAN.md"
duration: "12m"
verification_result: "pass"
completed_at: "2026-05-09T00:00:00Z"
---

T04 closed P04 with the documentation surface, the D011 release-cadence decision, and the phase-suite aggregator. Single atomic commit `f11f4182` on `main` adds 324 insertions across six files; bookkeeping (T04-SUMMARY + execution-log) follows in a separate commit per the established convention.

Documentation: `references/installation.md` extends with two new H2 sections appended after `## Releasing via Homebrew`. `## Installing via curl-pipe-bash` is the consumer-facing recipe — latest-release one-liner, ORCHESTRATOR_VERSION pinning, `orchestrator --version` smoke, per-project `/orchestrator-init` setup, uninstall via `bash packaging/install/install-claude-code.sh --uninstall`, CC-only v1 runtime advisory + M009 deferral pointer, integrity-verification cross-link to `## Verifying integrity`. `## Releasing via curl-pipe-bash` is the operator-facing procedure — three load-bearing decisions (D009 URL host + D010/CON-8 timeout with escalation clause + D011 manual cadence) inline-documented with rationale, plus MOS-4 (first-release `curl … | bash` smoke) and MOS-5 (synthetic v0.0.0-test tag push against fork) operator-only validation procedures. `commands/update.md ## Update sources` H2 extends with a fourth bullet documenting `update_source: curl-pipe-bash` symmetric with the homebrew row from P03 T04.

D011 records the release-cadence decision: pre-1.0 cadence is manual operator-driven tag push (operator authors CHANGELOG.md, bumps version in package.json per CON-4, commits, pushes a `v*` tag — release-workflow fires automatically). Post-1.0 conventional-commits-driven cadence with PR-merge auto-tagging is post-launch fast-follow scope. Four rationale legs: spec recommendation honored, no code surface at v1, reversible (a future `.github/workflows/auto-tag.yml` is purely additive), symmetric with operator-driven MOS-4/MOS-5. D011 binds to US-8/FR-10 + MOS-1/MOS-2/MOS-3/MOS-4/MOS-5 precedent.

Three task-grain verifiers landed: `tools/verify/m035-p04-installation-doc-curl.sh` with 13 grep needles (5 consumer-facing + 7 operator-facing + file-exists; the `--uninstall` needle required `grep -qF -- '--uninstall'` discovered during T04 execution because BSD grep treats `--uninstall` as a flag without the `--` separator); `tools/verify/m035-p04-update-skill-doc-curl.sh` with 4 grep needles; `tools/verify/m035-p04-phase-suite.sh` aggregator chaining all five P04 children verifiers in T01→T04 order plus a self-reference. Aggregator uses chain-the-children verifier-unit counting form (pass = number of green children + 1 self) per P03's pattern, NOT P05's sum-the-counters form — children have heterogeneous BATTERY counts (T01 pass=13, T02 pass=6, T03 pass=9, T04 children pass=4 + pass=13), so summing them would double-count via the aggregator's own pass count.

Verification battery (run on dev workstation post-edit):
- `bash tools/verify/m035-p04-installation-doc-curl.sh` → `BATTERY: pass=13 fail=0`
- `bash tools/verify/m035-p04-update-skill-doc-curl.sh` → `BATTERY: pass=4 fail=0`
- `bash tools/verify/m035-p04-phase-suite.sh` → `BATTERY: pass=6 fail=0` (5 children all green + self-reference)

Upstream regression discipline: re-ran all three pre-existing doc verifiers post-edit:
- `bash tools/verify/m035-p03-installation-doc-homebrew.sh` → `BATTERY: pass=11 fail=0` (P03 homebrew sections preserved)
- `bash tools/verify/m035-p03-update-skill-doc-homebrew.sh` → `BATTERY: pass=3 fail=0` (P03 update.md homebrew row preserved)
- `bash tools/verify/m035-p05-installation-doc-verifying-integrity.sh` → `BATTERY: pass=9 fail=0` (P05 § Verifying integrity preserved)

BSD-grep paper-cut surfaced and resolved at execution time: the plan's `if grep -qF '--uninstall' "$DOC"` form fails on macOS BSD grep ("unrecognized option `--uninstall`") because `-F` doesn't terminate option processing the way `-e` would. The fix `grep -qF -- '--uninstall' "$DOC"` is portable across BSD + GNU grep. This is a small enough fix to ship in T04's commit (rather than a follow-up paper-cut sweep) — the verifier is still less than 60 lines, the change is one character per problematic needle, and the BSD-grep-safe form is the "obviously correct" portable shape going forward.

Branch posture preserved: `main` throughout, no detached HEAD. Single atomic commit per RENAME-PLAN convention § 5. Working tree clean post-commit (modulo the bookkeeping pair for T04-SUMMARY + execution-log). Phase closure ready: `validate-milestone.sh M035` should pick up the P04 task summaries + phase-suite verifier on next invocation.

Reversibility: `git revert f11f4182` undoes installation.md + update.md + DECISIONS.md (D011) + the three new verifiers in a single step. The release pipeline doesn't fire until a `v*` tag is pushed; T04 has no external side effects pre-tag-push. MOS-4/MOS-5 operator procedures are documented but operator-deferred to launch-rehearsal time per the launch-sequencing-amendment.

P04 phase closure (post-T04): four task plans + four task summaries + P04-PLAN.md on disk. The phase-grain unit_close + P04-SUMMARY.md authoring is operator-side or a follow-up dispatch. Validate-milestone re-run picks up the phase-suite aggregator pass=6 as evidence for the P04 truth set.
