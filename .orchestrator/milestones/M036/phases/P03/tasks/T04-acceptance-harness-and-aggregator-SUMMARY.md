---
schema_version: "1.0"
type: task-summary
id: "T04"
parent: "P03"
milestone: "M036"
provides:
  - "tests/fixtures/m036-p03-tier-2/canned-structured.md (~17-line PASS-stub fixture preserving sample.md section structure - consumed by EXTRACT_TIER_2_DISPATCH=stub:pass); tests/fixtures/m036-p03-tier-2/canned-structured-low-fidelity.md (~12-line BLOCK-stub fixture with Section 1 dropped - consumed by EXTRACT_TIER_2_DISPATCH=stub:block; conversus stub returns BLOCK regardless because CONVERSUS_STUB_VERDICT=BLOCK is set in the test - low-fidelity content makes the artifact-level decision auditable); tests/test-tier-2-extraction-with-gate.sh (SC-11+SC-12 acceptance harness - mocked LLM via EXTRACT_TIER_2_DISPATCH=stub:pass|stub:block + mocked conversus via CONVERSUS_STUB=1 with CONVERSUS_STUB_VERDICT=PASS|BLOCK per CON-3; drives PASS leg + BLOCK leg sequentially in two separate per-leg mktemp -d workspaces; per-leg ORCHESTRATOR_ROOT isolates execution-log writes; PASS leg asserts 8 properties (driver rc=0+EXTRACTED stdout+verdict=PASS stdout+.structured.md present in chunk-store+pass.md present in _extraction-log+unit_close JSONL with task_type=extraction+cost_usd field+model field); BLOCK leg asserts 4 properties (driver rc=0+BLOCKED stdout+block.md present+.structured.md NOT in chunk-store=FR-18 invariant); 4 fixture-presence sanity assertions; emits BATTERY: pass=N fail=N skip=N as last stdout line; exit 0 iff fail=0; AD-19 single-script-file shape; Bash 3.2); tools/verify/m036-p03-test-harness.sh (SC-11 harness-shape verifier - asserts harness exists+executable+ran-to-completion rc<=1 permissive+well-formed BATTERY line; rc=1 still emits BATTERY in fail mode so permissive shape captures contract without coupling to per-leg pass count); tools/verify/m036-p03-acceptance-harness-passes.sh (SC-11 strict pass-rate gate - asserts harness exits 0; complement to permissive shape verifier above); tools/verify/m036-p03-phase-suite.sh (M036 P03 14-gate phase-suite aggregator wiring all P03 sub-gates: T01:conversus-preset-shape+m030-task-type-extraction+fixture-corpus-shape; T02:driver-tier-2-shape+tier-2-llm-helper-shape+unit-close-extraction-shape; T03:gate-helper-shape+tier-2-deferred-error-removed+tier-2-pass-end-to-end+tier-2-block-retention+p02-regression-pass; T04:fixture-canned-structured-shape+test-harness+acceptance-harness-passes; patterned after m036-p02-phase-suite.sh - run helper inspects exit code only; SUMMARY: m036-p03-phase-suite.sh pass=14 fail=0 on clean run); modified tools/verify/m036-p03-tier-2-pass-end-to-end.sh (mid-task correction - added gate-result-pass.md and gate-result-block.md staging into per-leg workspace; conversus stub adapter resolves _REPO_ROOT via 4-dirs-up-from-script-location which equals the workspace when scripts are copied there; without staging the stub fails with stub-fixture-not-found and PASS leg cannot complete); modified tools/verify/m036-p03-tier-2-block-retention.sh (same mid-task correction - gate-result fixture staging)"
requires:
  - "from:T01 what:templates/conversus-presets/tier-2-fidelity.yml+templates/model-routing.yml task_type:extraction row+tests/fixtures/m036-p03-tier-2/extract-manifest.yaml+sample.md+fixture-corpus-shape verifier (closed PASS); from:T02 what:scripts/knowledge/lib/extract-tier-2-llm.sh exposing extract_tier_2_dispatch with EXTRACT_TIER_2_DISPATCH=stub:pass|stub:block honouring + extract_tier_2_emit_unit_close + 3 verifiers (closed PASS); from:T03 what:scripts/knowledge/lib/extract-tier-2-gate.sh exposing extract_tier_2_invoke_gate+extract_tier_2_promote_or_retain + driver auto-branch + 6 verifiers including 4 observational-red awaiting T04 fixtures (closed done_with_concerns); from:disk what:scripts/dispatch/adapters/tool/conversus.sh stub-mode contract CONVERSUS_STUB=1 with CONVERSUS_STUB_VERDICT=PASS|BLOCK + tests/fixtures/gate-result-pass.md and tests/fixtures/gate-result-block.md stub-mode-canned-verdict fixtures already on disk from M026 work + tools/verify/m036-p02-phase-suite.sh structural template"
affects:
  - "P04 (ingest layer consumes the SC-11 acceptance harness as the integration smoke test for the Tier 2 extraction pipeline before chunk-classifier wiring); P05 (knowledge graph ingestion consumes the structured-md+pass.md+unit_close JSONL contract that SC-11 acceptance harness now asserts); M036 phase close (M036/P03 closes PASS via 14-gate phase-suite aggregator + SC-11+SC-12 BATTERY=16/0/0; the four T03-deferred observational verifiers all flipped green retroactively at T04 close per the cross-task ordering pattern documented at T03)"
key_files:
  - "tests/fixtures/m036-p03-tier-2/canned-structured.md,tests/fixtures/m036-p03-tier-2/canned-structured-low-fidelity.md,tests/test-tier-2-extraction-with-gate.sh,tools/verify/m036-p03-test-harness.sh,tools/verify/m036-p03-acceptance-harness-passes.sh,tools/verify/m036-p03-phase-suite.sh,tools/verify/m036-p03-tier-2-pass-end-to-end.sh,tools/verify/m036-p03-tier-2-block-retention.sh"
key_decisions:
  - "none"
patterns_established:
  - "Mid-task correction: gate-result fixture staging in per-leg workspace - conversus stub adapter resolves _REPO_ROOT via 4-dirs-up-from-its-own-script-location (scripts/dispatch/adapters/tool/conversus.sh -> workspace), so when scripts/ is copied into a mktemp workspace the conversus stub will look for tests/fixtures/gate-result-pass.md and tests/fixtures/gate-result-block.md inside that workspace not the original repo; the T03-authored tier-2-pass-end-to-end and tier-2-block-retention verifiers + the T04 SC-11 harness all needed an additional cp of tests/fixtures/gate-result-pass.md and tests/fixtures/gate-result-block.md into the workspace tests/fixtures/ to bring the stub-mode lookup back into the staged tree; not caught at T03 close because verifiers were observational-red on the prior canned-structured-fixture-missing assertion - the conversus-stub-fixture-missing failure was masked behind it; recorded for any future test that copies scripts/ + invokes conversus.sh under stub-mode (always also stage tests/fixtures/gate-result-pass.md and gate-result-block.md alongside any other workspace fixtures - or alternately propagate _REPO_ROOT via env var override if the conversus adapter ever grows one); Two-leg acceptance harness pattern with per-leg mktemp workspace: PASS-leg + BLOCK-leg run sequentially in the same harness rather than as two test files - single BATTERY: line summarises full SC-11+SC-12 acceptance + cross-leg invariants (no leakage of PASS-leg artifacts into BLOCK-leg workspace) verifiable in one process + phase-suite aggregator consumes one acceptance-harness-passes.sh gate not two; per-leg ORCHESTRATOR_ROOT isolates the per-leg execution-log JSONL writes so the repo's real execution-log is never touched + every assertion against _extraction-log and execution-log.jsonl starts from zero state; mirrors the M036/P02 SC-10 harness pattern; Permissive vs strict harness-gate split pattern: shape verifier (test-harness.sh) is permissive on rc<=1 since rc=1 is fail-mode-but-still-emitted-BATTERY whereas rc=2+ would be syntax/abort - captures shape contract without coupling to per-doc pass count; companion strict gate (acceptance-harness-passes.sh) asserts rc=0 specifically as the pass-rate gate; pattern reusable: any acceptance harness emitting BATTERY: lines should have a (shape-rc<=1, strict-rc=0) verifier pair so phase-suite can independently fail on (a) harness machinery broken vs (b) harness ran but found regressions; 14-gate phase-suite aggregator pattern reuse from M036/P02: same set -eu + run helper redirecting both stdout+stderr to /dev/null + SUMMARY: line format; sub-gate exit code is the only signal so SKIP-internal verifiers exit 0 informationally and report PASS at aggregator level; carried verbatim - including filename milestone-prefixed slug m036-p03-phase-suite.sh per the post-M031 plan-phase contract; Cross-task ordering green-flip pattern (M036 standard form): T03 authored 4 observational-red verifiers (tier-2-deferred-error-removed already passing because it ran against real ROOT not workspace-copied scripts; tier-2-pass-end-to-end + tier-2-block-retention failed because canned-structured fixtures absent + gate-result fixtures unstaged in workspace; fixture-canned-structured-shape failed on both fixture absences); all 4 went green at T04 close after canned-structured fixtures landed AND gate-result staging fix landed in the two T03 verifiers + SC-11 harness; auto-loop DONE_WITH_CONCERNS handling per US3 AS6 carried the loop forward without manual intervention; this is the third instance of the pattern in M036 (M036/P02 size-cap-external-pointer.sh, M036/P03 driver-tier-2-shape T02->T03 green-flip, M036/P03 four T03 deferred -> T04 green-flip)"
drill_down_paths:
  - ".orchestrator/milestones/M036/phases/P03/tasks/T04-acceptance-harness-and-aggregator-PAYLOAD.md"
duration: "15m"
verification_result: "pass"
completed_at: "2026-05-02T17:30:00Z"
---

T04 lands the SC-11+SC-12 acceptance harness, the two canned-structured fixtures consumed by the stub LLM dispatch, and the M036/P03 14-gate phase-suite aggregator. All four T03-deferred observational verifiers flipped green retroactively at T04-close per the cross-task ordering pattern. Phase-suite aggregator reports SUMMARY: m036-p03-phase-suite.sh pass=14 fail=0; SC-11+SC-12 acceptance battery reports BATTERY: pass=16 fail=0 skip=0.

What was built:

- tests/fixtures/m036-p03-tier-2/canned-structured.md (~17 lines, PASS stub preserving sample.md section structure) and canned-structured-low-fidelity.md (~12 lines, BLOCK stub with Section 1 dropped). Authored verbatim from the plan body.

- tests/test-tier-2-extraction-with-gate.sh (SC-11+SC-12 acceptance harness, ~120 lines, executable). Drives PASS leg + BLOCK leg sequentially in two per-leg mktemp -d workspaces, copying scripts/ + templates/ + per-leg fixture corpus + the gate-result-pass.md and gate-result-block.md fixtures so the conversus stub adapter's _REPO_ROOT-relative fixture lookup resolves inside the workspace. PASS leg asserts: driver rc=0; stdout EXTRACTED+verdict=PASS; .structured.md in chunk-store; pass.md in _extraction-log; unit_close JSONL with task_type=extraction+cost_usd+model. BLOCK leg asserts: driver rc=0; stdout BLOCKED; block.md in _extraction-log; .structured.md NOT in chunk-store (FR-18). Plus 4 fixture-presence sanity assertions. Emits BATTERY: pass=N fail=N skip=N as last stdout line; exit 0 iff fail=0.

- tools/verify/m036-p03-test-harness.sh (permissive shape verifier; rc<=1) + tools/verify/m036-p03-acceptance-harness-passes.sh (strict pass-rate gate; rc=0) + tools/verify/m036-p03-phase-suite.sh (14-gate aggregator wiring all 11 T01-T03 sub-gates + 3 T04 sub-gates).

Mid-task correction:

- Plan body for the SC-11 harness omitted the gate-result-pass.md and gate-result-block.md staging into the per-leg workspace tests/fixtures/. The conversus stub adapter (scripts/dispatch/adapters/tool/conversus.sh:354,357) resolves _REPO_ROOT via 4-dirs-up-from-script-location, which equals the workspace when scripts/ is copied there - so the stub looks for the gate-result fixtures inside the workspace, not the original repo. Without staging, the harness PASS leg's first conversus stub call fails with 'stub fixture not found' and the rest of the leg cascades to FAIL. Fixed in the SC-11 harness AND in the two T03-authored deferred verifiers (tier-2-pass-end-to-end and tier-2-block-retention) that have the same workspace-copy shape; tier-2-deferred-error-removed was unaffected because it runs against the real ROOT not a workspace copy. All four deferred verifiers now PASS. Recorded under patterns_established for any future tests that copy scripts/ and invoke conversus.sh under stub mode.

- Plan Expected Output line said 'PASS leg asserts 9 PASS lines' but the harness body in the plan only emits 8 PASS-leg assertions (driver rc=0, stdout EXTRACTED, stdout verdict=PASS, .structured.md, pass.md, unit_close extraction record, cost_usd, model). 8+4+4=16, so BATTERY: pass=16 fail=0 skip=0 (plan docstring said pass=17). Descriptive defect only - no functional impact; the BATTERY contract (pass=N fail=0) is what gates phase close.

Verification at T04-close:

- Phase Truth Checks (7 of 7 from must-haves frontmatter): all PASS.
- T04 task verifiers (4 of 4 from Verification block): all PASS - fixture-canned-structured-shape, test-harness, acceptance-harness-passes, phase-suite (14/14 sub-gates green).
- T03-deferred observational verifiers (4 of 4 - tier-2-deferred-error-removed, tier-2-pass-end-to-end, tier-2-block-retention, fixture-canned-structured-shape): all flipped green retroactively at T04-close.
- SC-11+SC-12 acceptance battery: BATTERY: pass=16 fail=0 skip=0.
- M036/P03 phase-suite aggregator: SUMMARY: m036-p03-phase-suite.sh pass=14 fail=0.

Forward notes:

- M036/P03 phase close ready: 14-gate phase-suite green + SC-11+SC-12 acceptance battery green + all P03 task summaries on disk with full task-summary frontmatter contract.
- P04 (ingest layer) consumes the structured-md+pass.md+unit_close-JSONL artifact contract that SC-11 asserts as the integration smoke test before chunk-classifier wiring.
- The mid-task gate-result staging fix is now documented as a M036 reusable pattern: any test that copies scripts/ into a mktemp workspace AND invokes conversus.sh under stub mode must also stage tests/fixtures/gate-result-pass.md and tests/fixtures/gate-result-block.md into the workspace tests/fixtures/.
