---
schema_version: "1.0"
type: task-summary
id: "T02"
parent: "P04"
milestone: "M036"
provides:
  - "scripts/knowledge/ingest-reference.sh (~150-line driver; Bash 3.2 / POSIX-sh per CON-2; sources scripts/knowledge/classify-reference.sh; parses --reference-root <path> + --no-index-rebuild flags; walks <root>/<category>/REF-*.md across the four taxonomy categories cms-rule|training-material|glossary|regulatory-doc; per-file delegates FR-1+FR-2 validation to classify_reference_file; emits CREATED:/SKIPPED:/REJECTED:/BLOCKED:/SUMMARY: structured stdout per MEM001; idempotency gate via content_hash frontmatter vs body sha256 with shasum/sha256sum probe-and-fallback; FR-18 BLOCK gate emits BLOCKED: + asserts absence of .structured.md sibling + does NOT emit CREATED for blocked chunks; partial-success ingest — per-chunk REJECTED does not abort the pass; invokes scripts/knowledge/rebuild-index.sh at end unless --no-index-rebuild passed; backwards-compat green path returns SUMMARY: ... created=0 skipped=0 rejected=0 blocked=0 (no reference corpus configured) when REF_ROOT directory does not exist; CON-1 preservation); scripts/knowledge/rebuild-index.sh (modified — additive amendment to basename-filter case block: extended MEM*|SPEC-* → MEM*|SPEC-*|REF-* AND added *.text|*.structured exclusion branch above to skip extraction-artifact siblings; all other behavior — file-discovery glob at line 66 + fm_field reads + edge-insertion paths for cites/derived_from/applies_to_field at lines 145+ — byte-identical pre/post per CON-5); 3 single-script-file shape verifiers under tools/verify/m036-p04-* (m036-p04-driver-shape.sh — 12 checks: existence+executable + 10 token-presence on flags --reference-root/--no-index-rebuild + stdout-protocol prefixes CREATED:/SKIPPED:/REJECTED:/BLOCKED:/SUMMARY: ingest-reference.sh + sourced-helper-name classify-reference.sh + invoked-helper-name rebuild-index.sh + idempotency-field content_hash; m036-p04-rebuild-index-recognizes-ref.sh — 2 checks asserting MEM*|SPEC-*|REF-* widened pattern + *.text|*.structured exclusion both present in rebuild-index.sh; m036-p04-idempotency.sh — CON-4 contract verifier: stages 4 taxonomy fixture-corpus categories into mktemp -d workspace + snapshots tree before run 1 + drives driver twice with --no-index-rebuild + snapshots tree after run 2 + diff -q snapshots assert byte-identical; trap rm -rf cleanup on EXIT; AD-19 single-script-file shape; Bash 3.2; structured PASS:/FAIL:/SUMMARY: stdout)"
requires:
  - "from:T01 what:scripts/knowledge/classify-reference.sh exposing classify_reference_required_fields + classify_reference_file functions (sourced by driver); from:T01 what:tests/fixtures/m036-p04-reference-corpus/ (6 valid REF chunks across 4 taxonomy categories + 3 negative-path chunks under _negative/ — driver walks only the 4 taxonomy categories so _negative/ is silently ignored; idempotency verifier stages the 4 valid categories into mktemp workspace); from:disk what:scripts/knowledge/rebuild-index.sh (M011/M020 deliverable extended by P05 with edge-insertion paths for cites/derived_from/applies_to_field; T02 amends basename-filter case block additively); from:disk what:tools/verify/lib/p00-validate-chunk-frontmatter.sh (P00 T03 deliverable — taxonomy + tier validator the classifier delegates to)"
affects:
  - "T03 (m036-p04-tier-2-block-not-promoted.sh verifier exercises the FR-18 BLOCK gating branch already implemented in T02 driver — verifier authored in T03 against driver behavior landed here; classifier-rejects-unknown.sh + classifier-rejects-missing-required.sh verifiers also consume the partial-success ingest semantics established here); T04 (acceptance harness tests/test-reference-ingest-fixture.sh drives this driver against the full T01 fixture corpus end-to-end; phase-suite aggregator m036-p04-phase-suite.sh wires this T02 three verifiers among others)"
key_files:
  - "scripts/knowledge/ingest-reference.sh,scripts/knowledge/rebuild-index.sh,tools/verify/m036-p04-driver-shape.sh,tools/verify/m036-p04-rebuild-index-recognizes-ref.sh,tools/verify/m036-p04-idempotency.sh"
key_decisions:
  - "none"
patterns_established:
  - "P04 inherits the M036 verifier conventions intact: milestone-prefixed slug (m036-p04-*), AD-19 single-script-file shape, grep -qF -e token-loop body for leading-dash safety (avoids BSD-grep flag-misinterpretation when patterns start with --), structured PASS:/FAIL:/SUMMARY: stdout per MEM001, set -eu strict, ROOT resolution via ORCHESTRATOR_ROOT-or-pwd; pure-lib MEM004 carried into ingest surface — driver sources scripts/knowledge/classify-reference.sh + invokes classify_reference_file as the single FR-1+FR-2 gate; per-chunk partial-success ingest pattern (per-chunk REJECTED does not abort the pass — MEM011 validation-as-task semantics applied to ingest path; matches the spec-chunk classifier tolerance per commands/ingest.md:99-100); shasum/sha256sum probe-and-fallback for cross-platform sha256 carried from M036/P02 binary preservation; awk frontmatter-skip recipe for body-content extraction — second --- line marks frontmatter close, everything after is body content fed to sha256; additive case-block amendment pattern for rebuild-index.sh — old MEM*|SPEC-*) widened to MEM*|SPEC-*|REF-*) with new *.text|*.structured) exclusion branch added above (preserves CON-5 spec-chunk-untouched invariant; superstring grep-positive-check is sufficient — no negative check needed since old pattern is fully consumed by new pattern); FR-18 BLOCK gate at ingest-time as defense-in-depth — extract-time gating in P03 already withholds .structured.md promotion; ingest-time BLOCK detection asserts the absence of unexpected .structured.md sibling and emits a stderr WARNING if found (audit trail for any P03 retention-contract violation); backwards-compat green path for missing-reference-root — exit 0 with informational SUMMARY: ... (no reference corpus configured) suffix preserves CON-1 (operators without a reference corpus see no spurious failures); idempotency-via-tree-snapshot pattern in m036-p04-idempotency.sh — find -type f + per-file sha256 + sort + diff -q snapshots gives byte-identical-tree assertion that is the hard CON-4 contract regardless of whether per-line SKIPPED reporting fires (per-line SKIPPED depends on whether content_hash frontmatter equals body_sha256, which is mostly false for extract-produced chunks because extract writes the source-binary hash not the body hash; tree-snapshot is the load-bearing assertion)"
drill_down_paths:
  - ".orchestrator/milestones/M036/phases/P04/tasks/T02-driver-and-rebuild-index-PAYLOAD.md"
duration: "15m"
verification_result: "pass"
completed_at: "2026-05-02T17:44:25Z"
---

T02 lands the M036 P04 ingest-reference driver + the additive amendment to rebuild-index.sh basename filter that admits REF-* chunks into the index rebuild + three single-script-file shape verifiers gating the artifact contracts. Closes the three task-level must-haves T02 owns (driver exists/executable/parses --reference-root/walks taxonomy directories; rebuild-index.sh basename-filter widened additively; CON-4 idempotency invariant — re-running on unchanged inputs produces zero modifications).

What was built:

- scripts/knowledge/ingest-reference.sh (~150 lines, Bash 3.2 / POSIX-sh, set -eu strict). Sources scripts/knowledge/classify-reference.sh from T01. Parses --reference-root <path> + --no-index-rebuild flags. Walks <root>/<category>/REF-*.md across cms-rule|training-material|glossary|regulatory-doc — anything outside this taxonomy (e.g. _negative/) is silently skipped. Per-file: extracts chunk_id (frontmatter chunk_id, then id, then basename fallback); delegates FR-1 + FR-2 to classify_reference_file (per-chunk REJECTED on failure, partial-success ingest); checks tier_2_verdict frontmatter (FR-18 BLOCK branch — emits BLOCKED: stdout + asserts no unexpected .structured.md sibling + bumps blocked counter + does NOT emit CREATED); checks idempotency via content_hash frontmatter vs body sha256 (probe-and-fallback shasum -a 256 / sha256sum); emits CREATED:/SKIPPED:/REJECTED:/BLOCKED: per-chunk + SUMMARY: ingest-reference.sh created=N skipped=N rejected=N blocked=N at end; invokes scripts/knowledge/rebuild-index.sh unless --no-index-rebuild passed (rebuild failure non-fatal — chunks are on disk, index may be stale). Backwards-compat green path: REF_ROOT missing → exit 0 with informational SUMMARY (CON-1).

- scripts/knowledge/rebuild-index.sh modified additively. The basename-filter case block at lines 81-87 was extended from MEM*|SPEC-*) to MEM*|SPEC-*|REF-*) AND a new *.text|*.structured) exclusion branch was added above to skip extraction-artifact siblings (REF-foo.text.md and REF-foo.structured.md basenames after .md strip become REF-foo.text and REF-foo.structured — without the exclusion they would match the widened MEM*|SPEC-*|REF-* glob). All other rebuild-index.sh behavior — file-discovery glob at line 66, fm_field reads, P05 edge-insertion paths for cites/derived_from/applies_to_field — byte-identical pre/post per CON-5.

- 3 verifiers under tools/verify/m036-p04-*:
  - m036-p04-driver-shape.sh — 12 checks: existence + executable + 10 token-presence (--reference-root, --no-index-rebuild, CREATED:, SKIPPED:, REJECTED:, BLOCKED:, SUMMARY: ingest-reference.sh, classify-reference.sh, rebuild-index.sh, content_hash). Uses grep -qF -e form for leading-dash safety.
  - m036-p04-rebuild-index-recognizes-ref.sh — 2 checks: MEM*|SPEC-*|REF-* widened pattern present + *.text|*.structured exclusion branch present in rebuild-index.sh. Positive-only checks; no negative check because the old pattern is a substring of the new pattern.
  - m036-p04-idempotency.sh — CON-4 contract verifier. Stages the 4 taxonomy fixture-corpus categories from tests/fixtures/m036-p04-reference-corpus/ into a mktemp -d workspace; snapshots tree (find -type f + per-file sha256 + sort) before run 1; drives driver twice with --no-index-rebuild; snapshots tree after run 2; diff -q snapshots asserts byte-identical. The diff-of-snapshots is the load-bearing CON-4 assertion regardless of whether per-line SKIPPED fires (per-line SKIPPED depends on content_hash matching body_sha256, which is mostly false for extract-produced chunks because extract writes source-binary-hash not body-hash; T04 acceptance harness covers the extracted-and-then-re-ingested case where content_hash IS valid).

Verification (all three Verification block commands exit 0 with fail=0):

- bash tools/verify/m036-p04-driver-shape.sh → SUMMARY: m036-p04-driver-shape.sh fail=0 (12 PASS lines).
- bash tools/verify/m036-p04-rebuild-index-recognizes-ref.sh → SUMMARY: m036-p04-rebuild-index-recognizes-ref.sh fail=0 (2 PASS lines).
- bash tools/verify/m036-p04-idempotency.sh → SUMMARY: m036-p04-idempotency.sh fail=0 (1 PASS — tree byte-identical across two runs).

Mid-task corrections: none. All three T02-owned verifiers PASSed first try.

Forward notes:

- T03 authors m036-p04-tier-2-block-not-promoted.sh which exercises the FR-18 BLOCK gating branch ALREADY landed in T02 driver — the verifier is T03 deliverable but the driver behavior under test is T02. T03 also authors m036-p04-classifier-rejects-unknown.sh + m036-p04-classifier-rejects-missing-required.sh which exercise the partial-success ingest semantics established here.
- T04 acceptance harness tests/test-reference-ingest-fixture.sh drives this driver against the full T01 fixture corpus end-to-end. T04 phase-suite aggregator m036-p04-phase-suite.sh wires this T02 three verifiers among others.
- The body_sha256-vs-content_hash idempotency gate is best-effort for extract-produced chunks (their content_hash is the source-binary hash, not the body hash) — every run will emit CREATED rather than SKIPPED for those chunks. This is acceptable because the CON-4 invariant is byte-identical-tree-across-runs (the driver does not modify chunk files — read-only), not per-line SKIPPED reporting. A future amendment could add a per-ingest body_content_hash frontmatter field to enable true per-line SKIPPED idempotency on re-ingest; deferred.

Cross-task ordering note: this task three verifiers green at T02 close — no cross-task green-flip pattern needed at T02. The cross-task ordering pattern (verifier authored in this task exercising a property that requires a downstream task deliverable) surfaces in T03 + T04 per the M036/P02 + M036/P03 precedent.
