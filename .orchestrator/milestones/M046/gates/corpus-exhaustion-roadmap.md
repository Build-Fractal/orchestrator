---
schema_version: "1.0"
type: corpus-exhaustion
verdict: "PASS"
checkpoint: "roadmap"
generated_at: "2026-07-12T16:15:00Z"
manifest: "/Users/brettkellgren/Sites/orchestrator/templates/corpus-store-manifest.yml"
questions_total: 9
questions_surviving: 9
clean: 0
hits_pending: 0
dropped: 0
kept: 9
caveat: 0
---

# Corpus-Exhaustion Artifact — roadmap

**Verdict:** PASS — every question is clean or dispositioned; 9 question(s) survive to the human.

## Per-question evidence

### Q1 — KEPT

- **question**: - #Q-1 (hook-install-portability): Can the default-DENY PreToolUse hook (FR-9) be installed via the M028 path on every supported CC install shape (npm / homebrew / curl / symlink) and survive the M021 shape-guard?
- **disposition**: kept (spec-annotated not corpus-answerable (genuinely new hook surface); same kept verdict at discuss gate 2026-07-02; resolved by P01 spike + SC-5 non-stubbed proof)
- **terms** (12): Q-1, FR-9, hook, install, portability, default, deny, pretooluse, installed, m028, path, every
- **hits**:
  - constitution :: .orchestrator/memory/constitution.md:10:  evidence per Decision A Pathway 1; Items 1 + 2 also ship in this
  - constitution :: .orchestrator/memory/constitution.md:105:Every architectural decision MUST optimize for minimizing the context
  - constitution :: .orchestrator/memory/constitution.md:121:- When this ratio degrades, the system is failing. Every design
  - constitution :: .orchestrator/memory/constitution.md:161:Every piece of work MUST go through an explicit design step, no
  - constitution :: .orchestrator/memory/constitution.md:182:- Document everything: exact file paths, complete code, exact
  - constitution :: .orchestrator/memory/constitution.md:225:Every phase of work MUST produce structured, discoverable
  - constitution :: .orchestrator/memory/constitution.md:295:routing config, hooks config), not inferred by scripts at runtime.
  - constitution :: .orchestrator/memory/constitution.md:303:- Hook lifecycle points and behavior: declared in `hooks.yaml`.
  - constitution :: .orchestrator/memory/constitution.md:316:  (task > phase > milestone > default).
  - constitution :: .orchestrator/memory/constitution.md:32:      templates/orchestrator-config-default.yml (41 leaves, baseline 0
  - constitution :: .orchestrator/memory/constitution.md:337:### XII. Hook Isolation
  - constitution :: .orchestrator/memory/constitution.md:339:Hook scripts operate in a sandbox: they receive a read-only state
  - constitution :: .orchestrator/memory/constitution.md:344:- State snapshots are `chmod 444` temp files deleted after hook
  - constitution :: .orchestrator/memory/constitution.md:348:- Hook timeout is enforced (default 30s). Hooks that exceed timeout
  - constitution :: .orchestrator/memory/constitution.md:5:  a new "Install gates" bullet per follow-on amendment Item 3 of
  - constitution :: .orchestrator/memory/constitution.md:50:      script for installer-channel version SST. Reassigned from
  - constitution :: .orchestrator/memory/constitution.md:52:      (Decision A Pathway 1 ship-commit batch).
  - constitution :: .orchestrator/memory/constitution.md:55:    - Quality Gates — at v2.3.0 gains a new "Install gates" bullet
  - constitution :: .orchestrator/memory/constitution.md:57:      install testing) from XXII's invariant body to release-time
  - constitution :: .orchestrator/memory/constitution.md:58:      evidence per follow-on amendment Item 3 (Decision A Pathway 1).
  - constitution :: .orchestrator/memory/constitution.md:75:    - Operator-routed Path 1 (accept headline PASS, defer blind's
  - constitution :: .orchestrator/memory/constitution.md:8:  Invariant 3 (end-to-end install testing) relocates from XXII's
  - constitution :: .orchestrator/memory/constitution.md:92:      declarations ratified (2026-05-11, Path 1)
  - constitution :: (capped at 5 hits — more may exist)
  - decisions :: .orchestrator/DECISIONS.md:113:- **Choice**: Revised sequence: **M011 → M012 → M013 → M014 → M019 → M018 → M009 → M010** (M019 inserted by D009). M018 adopts caveman-style (github.com/JuliusBrussee/caveman) token compression as a first-class pipeline stage aligned with Constitution Principle I (Context Minimization). Proposed phase outline (subject to detailed planning closer to kickoff): P01 compression grammar (what's safe to compress per artifact class; preserve frontmatter, code fences, paths, MEM IDs, commands, URLs), P02 dispatch compressor (optional filter in `scripts/dispatch/build-context.sh`, emits `payload.compressed.md` beside original), P03 intensity mapping (Quick→ultra, Standard→full, Full→lite/off via `scripts/engine/intensity-gate.sh`), P04 memory compression (opt-in `CLAUDE.compressed.md` / `KNOWLEDGE.compressed.md` with `.original` siblings), P05 agent output profile (inject terseness directive into payload prefix), P06 eval harness (3-arm port of caveman's evals against orchestrator payloads, gate on accuracy parity), P07 multi-runtime parity (Claude Code / Codex / Cursor). Differentiation vs. installing caveman as-is: roundtrip-safe by policy (Principle VI — originals authoritative, compress only at dispatch/load boundaries), grammar-aware preservation of orchestrator artifact structure, intensity-bound (not always-on), explicit opt-out surfaces (constitution, spec.md, PR descriptions, user-visible docs), and telemetry integrated with existing cost tracking so savings are measurable per dispatch.
  - decisions :: .orchestrator/DECISIONS.md:128:- **Revisable**: Runtime cost of tracking is effectively zero (JSONL append + reuse of existing `estimate_tokens` function; no added LLM tokens — agent does NOT self-report, runtime hooks supply actuals). Build cost is the real decision, hence the tiered framing so M019's final scope is set by what the Tier 1 data reveals rather than up-front speculation. M019 before M018 because compression needs a measured baseline. M019 before M009 because launch narrative differentiates on published receipts ("M011 shipped in N phases, M tasks, $X spend, Y% first-try verification pass") — no other orchestrator tool publishes this. Also surfaces Principle V's fresh-context cost quantitatively, making the Context Minimization claim auditable.
  - decisions :: .orchestrator/DECISIONS.md:141:- **Choice**: Replace the D008 phase outline's "P02 dispatch compressor (optional filter...)" framing with a tier-ladder model informed by Article 3 (Rohit's Claude Code harness teardown) and logged in `.orchestrator/scratch/articles-synthesis-2026-04-17.md`. Every dispatch runs through tiers in order, cheapest first: **Tier 1 microcompact** (reuse cached tool-call results that haven't changed + persist oversized tool results to disk with `file_path + preview` references — absorbs L6 tool-result budgeting; zero LLM cost); **Tier 2 snip** (head-drop with protected tail; no LLM call); **Tier 3 auto-compact** (summarize via LLM call; existing caveman-style territory); **Tier 4 context collapse** (staged multi-phase compression of tool results → reasoning → sections; feature-flagged). Intensity maps to tier ceiling: Quick→T1–T2 only, Standard→through T3, Full→all tiers available. Revised phase outline: P01 tier grammar + per-tier safety boundaries (what's preservable per tier), P02 Tier 1 microcompact + tool-result budgeting, P03 Tier 2 snip with protected-tail semantics, P04 Tier 3 auto-compact (summarization, intensity-gated), P05 Tier 4 collapse (staged; feature-flagged, off by default), P06 eval harness (3-arm parity tests per tier), P07 multi-runtime parity. D008's original scope envelope (roundtrip-safe by policy, grammar-aware preservation, intensity-bound, explicit opt-out surfaces, telemetry-integrated) carries forward unchanged — only the internal staging model is reframed. Fresh `orchestrator:discuss` at M018 kickoff addresses detailed tier design once M019 Tier 1 data reveals which artifacts dominate token spend. **Conversus integration (locked in per D007 reuse pattern)**: M018/P01 tier-grammar + per-tier-safety-boundary work invokes `scripts/dispatch/adapters/tool/conversus.sh` via `role/red-team` + `domain/security` presets at plan-review time. Rationale: grammar decisions carry parse-regression risk across downstream scripts that grep prose (the risk D008 originally flagged), which is exactly the subjective-quality territory the conversus fidelity-gate adapter earns its cost on. A new preset drops under `templates/conversus-presets/` without adapter modification (same pattern M013/M014 use). Gate is intensity-bound by default (Quick=off, Standard=advisory, Full=blocking); `--no-review` escape available.
  - decisions :: .orchestrator/DECISIONS.md:169:- **Choice**: Revised sequence: **M011 (closed) → M021 (active) → M019 Tier 1 emitter → M012 → M013 → M014 → M019 Tier 2+3 → M018 → M009 → M010**. M021 closes the residual Class B prompt triggers (12 shape patterns surviving M016's Class A hardening) identified in 20 M011/P05–P07 auto-mode screenshots via three layers: a three-wrapper catalog under `scripts/util/`, linter v2 (AP-005..AP-009 detectors + scope widening to task-PAYLOADs), and a PreToolUse shape-guard hook enforcing a closed 10-pattern rewrite/reject matrix. Ordered before M019 so observability metrics (time/tokens/$/quality) dogfood on a **zero-prompt baseline** rather than mixing cost measurements with interruption overhead — cleaner Tier 1 data from M012–M014 dogfooding, more defensible launch narrative. (ID note: this entry was anticipated as D010 in M021-ROADMAP.md and M021-CONTEXT.md on 2026-04-17; D010/D011 landed first, so this reorder decision is recorded as D012.)
  - decisions :: .orchestrator/DECISIONS.md:170:- **Revisable**: No — once the hook is live and the replay corpus is in CI, rolling back is a permission-only change in `.claude/settings.json` (disable the hook entry) but the corpus and AP-005..AP-009 entries stay as knowledge (constitution VII).
  - decisions :: .orchestrator/DECISIONS.md:172:Evidence-grounded: 20 M011/P05–P07 screenshots define the closed pattern matrix (AD-2, AD-5); no speculative additions (constitution XIV). M021 itself dogfoods via its own `orchestrator:auto` execution through P01–P04 (AD-8) — SC-7 attestation is produced by `scripts/verify/m021-p04-dogfood-attestation.sh` and the permanent replay corpus at `tests/fixtures/m021-prompt-corpus.txt`. Adding M021 ahead of M019 costs ~1 milestone of sequencing but eliminates the noise floor that would otherwise contaminate every subsequent metrics measurement — principle I (Context Minimization) applied to the measurement apparatus itself.
  - decisions :: .orchestrator/DECISIONS.md:186:M012/P02 D011-EVALUATION.md (.orchestrator/milestones/M012/phases/P02/D011-EVALUATION.md) records the mechanical outcome: 1 of 3 criteria shipped (cross-refs to knowledge/**/MEM*.md entries via shared ID namespace via the include-markdown pipeline; review/unreviewed state model NOT shipped; query/search surface callable from dispatches NOT shipped). D011's decision rule: <=1 of 3 -> promote as committed milestone, positioned between M014 and M019 Tier 2+3 so A5 on_failure flagging can draw on its data. M012's cross-ref affordance (pathname-keyed wiki stubs with include-markdown) is necessary but not sufficient for the full A1/A2/A3/A6 knowledge-layer workflow; M020 ships the remaining review-state + query-surface + clustering surface. Minimum-viable subset (L8 explored state + A2 minimal graduate.sh guard + A4 session-start manifest) still ships independently before M020 per D011 framing; full A2 workflow and review queue UI wait for M020 kickoff.
  - decisions :: .orchestrator/DECISIONS.md:197:- **Choice**: Narrow M013 scope per `specs/023-github-native-integration/conversus/` run: (1) FR-12 handler installation is Claude-Code-only for v1 (CV-3 / RD-2 binding ruling) — Codex CLI + Cursor fall back to `manual` sync at init, non-Claude-Code runtime-adapter work deferred to a future milestone; (2) Minimal Slice (CV-1 / RD-3 binding ruling) pins Phase 1 load-bearing scope to full US-3 + minimal US-1 scaffolding (sidecar config + UAT template install) + US-2 idempotency applied to UAT entries only; (3) M020 sequencing unchanged (RD-1 reclassified) — M013 -> M014 -> M020 per D013; schema authority bounded by MIT-2 narrowing FR-9 to additive-emit-only with chunk-ID pinned to existing `SPEC-*` frontmatter + new Knowledge-Layer Boundary subsection explicitly forbidding review-state / query-surface / clustering; (4) 13 unmitigated P5 risks landed as MIT-1..MIT-13 spec edits (sub-issue fallback Constraint, Constitution Check section, FR-5 three-shape rewrite, FR-6 per-item cache schema, FR-7 sync lock, FR-9 narrowing, FR-12 prose correction + scope, FR-13 `--strict` provenance + 30s timeout + drop intensity-engine coupling, FR-14 re-init via marker search + label-collision preflight, FR-15 dry-run generalized to init+sync, FR-16 rate-limit + auth-expiry detection, FR-17 cost emission, FR-18 cache reconciliation, plus SC-8/SC-9 reconciliation and new SC-12/SC-13 scope + overhead caps); Open Questions #1/#3/#6/#7 closed; new OQ on PAT-vs-App default for M009. Spec status promoted Draft -> Ready-for-discuss.
  - decisions :: .orchestrator/DECISIONS.md:198:- **Revisable**: Yes — if Phase 1 discovery shows FR-9 flat-list constraint cannot deliver US-3 autocomplete, that triggers re-sequencing consideration with evidence (not a pre-plan blocker, per RD-1 settlement). If a current consumer for non-Claude-Code `on-transition` emerges before milestone close, FR-12 scope can widen with a new Decision row; otherwise Codex/Cursor registration stays deferred.
  - decisions :: .orchestrator/DECISIONS.md:200:Red-blue deliberation with constitution-arbiter binding on three disputes. Spec was pinning scope intentions that had not been converted into Constitution-XIV/XV-bounded commitments; deliberation surfaced 13 concrete unmitigated risks + 3 arbitrated disputes. Arbiter ruled on XIV ("current demonstrable need" governs FR-12 and Minimal Slice) and on XV (narrow FR-9 to minimum schema authority — no sequence re-open). Landing edits pre-discuss means `orchestrator:discuss` operates on the narrowed spec and cannot silently re-expand scope. See `specs/023-github-native-integration/conversus/summary/final.md` + `conversus/arbitration/resolution.md` for full register + rulings.
  - decisions :: .orchestrator/DECISIONS.md:204:### P02 scope split post-P01: narrow P02 to create path; insert P03 for re-init adoption + GraphQL lint { #dr-code-015 }
  - decisions :: .orchestrator/DECISIONS.md:211:- **Choice**: Original P02 shipped 7 produces (init + common helpers + marker invariant + GraphQL lint + re-adoption + sidecar extensions + doc extensions) in a single risk=high phase. Post-P01 retrospective showed P02 was the highest risk concentration in M013. Split cuts P02 to create-path-only (reuses all primitives from P01 + common helpers authored in P02), leaves a medium-risk P03 for the harder idempotency cases (re-adoption + GraphQL lint), and pushes the sync phase to P04.
  - decisions :: .orchestrator/DECISIONS.md:225:- **Choice**: Revised sequence: **M013 (P04 remaining) → M014 (extended) → M020 → M024 → M019 Tier 2+3 → M018 → M023 → M009 (extended) → M010 (adjusted)**. (1) **M014 scope extension**: port spec-kit's `specify` shape as native `orchestrator:specify` (CC-first, written portably, treating spec-kit template as the I/O contract rather than a verbatim port); add conversus-suggestion logic so complex/controversial specs auto-propose red-blue pressure-test with optional spec-decomposition-before-conversus for very large specs; dual-write `AGENTS.md` alongside `CLAUDE.md` so Codex stays fed without a separate milestone. (2) **M024 Universal Intake** (new): extend `orchestrator:evaluate` to input-agnostic (idea, paragraph, fragment, full spec, or empty + Q&A) and emit a reviewable proposal artifact at `.orchestrator/intake/<id>/proposal.md` covering six axes (input shape, scope tier, decomposition, design gate, conversus gate, intensity); degenerate fast-path auto-proceeds if Tier A + Quick + no conversus + no design, approval-gated otherwise. Positioned after M020 so we dogfood it through remaining milestones. Design gate degrades gracefully pre-M023 ("design walkthrough lands in M023; author DESIGN.md manually or skip"). (3) **M023 Design Layer** (new): `orchestrator:design` command — conversus spawns N design-personality agents in parallel, each produces a DESIGN.md draft + working coded prototype in user's stack; side-by-side comparison; user picks/arbitrates; canonical DESIGN.md lands at repo root and auto-injects into payloads for ui-tagged phases. Renderer adapter interface shaped as MCP clients (runtime-agnostic; Stitch/v0/Figma/etc. consumed as they expose MCP). Stage-1-only scope — high-fidelity stakeholder-mockup expansion (Stitch/Claude-design rendering) deferred until user demand earns it. Positioned pre-M009 because orchestrator has no internal UI to dogfood against — landing earlier buys zero learning. (4) **M009 scope extension**: add runtime-parity audit as pre-launch gate — dogfood every `orchestrator:*` command end-to-end on CC + Codex + Cursor, consuming the `RUNTIME-ASSUMPTIONS.md` registry accumulated during M013–M018, fix parity bugs before external eyes. (5) **M010 posture adjustment**: ship with Managed Agents primary adapter + Codex Cloud **stubbed** adapter (proves abstraction holds without full feature parity); full Codex Cloud as demand-driven fast-follow rather than launch-critical. (6) **Ultraplan/ultrareview stance**: parked, not integrated — Claude-Code-only (requires CC on web + GitHub repo + Anthropic cloud; not available on Bedrock/Vertex/Foundry), forks multi-runtime UX. M013 GitHub integration delivers the same plan-review value universally via PR comments. Re-evaluate only if real CC-only user demand emerges. (7) **`RUNTIME-ASSUMPTIONS.md` discipline** (no milestone cost): a registry file tracking CC-specific shape assumptions as written (`settings.json` hook formats, CLAUDE.md-only writes, `claude` CLI invocation patterns). M009's parity audit consumes it as a punch-list rather than an open-ended investigation. Optional: fold a single Codex smoke-probe telemetry point into M019 T2+3's emitter for drift detection.
  - decisions :: .orchestrator/DECISIONS.md:228:User is dogfooding CC-exclusively right now; Codex parity is a launch concern, not a dogfood concern. Back-loading Codex validation to M009 is constitutionally cleaner (XIV — don't build Codex plumbing speculatively before CC usage teaches us what needs to be portable) and keeps the current dogfooding loop fast. M023/M024 are the adoption layer — M024 ships where its internal dogfood signal is cheapest (post-M020, exercised on every remaining milestone), M023 ships where its external signal is cheapest (pre-launch, since this repo has no UI to dogfood design against). MCP-as-adapter for design renderers means M023 is runtime-agnostic by construction (CC + Codex + Cursor all support MCP). Extending `evaluate` rather than adding a new `orchestrator:start` command keeps the surface small (XIV). Proposal-as-artifact gates the router on Principle III (Design Before Code). The `RUNTIME-ASSUMPTIONS.md` discipline is a lightweight hygiene practice — a few lines of markdown per observation — preventing "CC-first" from silently becoming "CC-only." Supersedes the forward-sequence framing from D004/D005/D006/D008/D013 where M013→M014→M020→M019 T2+3→M018→M009→M010 was the last committed order.
  - decisions :: .orchestrator/DECISIONS.md:239:- **Choice**: M014 pressure-tested pre-discuss via cooperative conversus deliberation (Tier 1; 2 agents — `spec-author` + `devils-advocate`; 1 round; no arbiter). 14 MITs applied pre-discuss covering: (P1) Phase Sequencing table as spec-layer subsection after Minimal Slice; SC-2 tightened to mechanical stdout/exit-code/stderr-grep assertions; FR-2b template SSOT (`templates/spec-template.md`) + FR-18 byte-compatibility fixture test (`tests/test-specify-shape.sh`) + SC-17; US-5 promoted to independent P2 user story with scope-boundary sentence ("US-5 is the only path that edits `specs/<NNN>-<slug>/spec.md` from a comment"); Constitution XV retrofit at SC-5 + Constitution Check section → III (primary) + XIV (supporting), with Constitution Check XV paragraph split into "applies to / does NOT apply to" sub-contexts (FR-12 + FR-14 byte-preservation retained; spec-amendment human-gate retargeted); (P2) SC-4 rewritten as two-branch construction (default: measurement + re-planning trigger; upgrade branch: pinned-shape gate with failure-posture clause); FR-14 three-case semantics (all-placeholder / partial / fully-authored) + SC-14 byte-preservation invariant; SC-6a outside-markers `shasum` invariant + `tests/test-dual-write-outside-invariant.sh`; FR-19 `--dry-run` JSONL manifest format pinned M014-local only (M013 retrofit deferred); SC-15 `RUNTIME-ASSUMPTIONS.md` close-out deliverable; SC-16 dogfood-data sizing for FR-9 classifier-shape decision; (P3) FR-7 interim→M024 migration handshake committed as one-line rule; Open Question #C-11 exhaustive CLAUDE.md write-site enumeration at planning time. Spec frontmatter promoted `Status: Draft` → `Status: Ready-for-discuss`; `Last Revised: 2026-04-22` line added matching M013 precedent. P3 Dependencies prose rewrite skipped as subsumed by Phase Sequencing table (P1 #1). Two Phase 4 disputes (SC-4 default branch; Phase Sequencing table layer) resolved via synthesizer-recommended compromises: SC-4 adopts measurement default with upgrade-branch failure-posture; table lands spec-layer.
  - decisions :: .orchestrator/DECISIONS.md:240:- **Revisable**: Yes — if planning-phase discovery shows FR-9 classifier shape cannot be pinned mechanically (dogfood data too sparse, or no regex/heuristic floor earns its keep), SC-4 stays on the default branch and the upgrade branch never activates. If M024 planning reshapes the `.orchestrator/intake/` manifest schema, FR-7's migration-handshake commitment may need amendment via a new D-row.
  - decisions :: .orchestrator/DECISIONS.md:270:Dogfooding M014/P01 on M020 (spec 025) surfaced a silent dead zone: the shipped `orchestrator:specify` scaffolds TODOs but does not author, and the pressure-test gate runs probe-and-prompt against whatever body exists. On a freshly scaffolded spec, the probe reads below-threshold (no FRs, no user stories, just TODOs) and the gate is skipped — so the gate's adversarial value is never exercised on the first draft where it matters most. The pressure-test only earns its cost against authored content; running it against placeholders produces nonsense findings, running it too late means authoring defects propagate into evaluate/discuss/roadmap before anyone catches them. Intensity-scaling the three passes respects the "right tool for the right job" ethos already pervading the orchestrator (Quick trusts the agent + skips the gate for low-risk specs; Standard adds a non-blocking safety net; Full invokes human judgment + strict gate where architecturally load-bearing work earns the cost). Smell-test-only-escalates honors operator intent (user setting Quick isn't silently upgraded to Full unless CLI override forces it) while protecting against accidental under-classification (an architecturally heavy spec at project-default Quick gets recognized and bumped). The gate TODO pre-flight is a universal invariant rather than a specify-only check so every future gate consumer (M013 UAT gate, M023 design gate, M024 routing gate) gets the protection without each one reimplementing it. Spec 025 also surfaced two upstream conversus bugs (claude-code false-fail; anthropic parallel-429) captured separately in `specs/025-knowledge-layer-maturation/conversus/PRESSURE-TEST-FINDINGS.md` + handoff in `CONVERSUS-PR-HANDOFF.md` — orthogonal to D019's contract work.
  - decisions :: .orchestrator/DECISIONS.md:29:- **Choice**: P06 scope expanded by five additional items discovered during P05 dispatch refactors: (1) scripts/lib/run-context.sh _orch_run_nonce SIGPIPE under set -o pipefail — causes scripts/engine/run.sh --dry-run to exit 141 when ORCH_RUN_SEED is unset; worked around in T02/T03/T04 with set +o pipefail wrapper around init_run_context (3 replicated call sites to drop after P06 fix). (2) scripts/dispatch/lib/section-handlers.sh handle_phase_summaries has a Bash 3.2 while-read bug with unterminated dep-list temp file — worked around by local _bc_handle_phase_summaries_fixed shim in build-context.sh (to drop after P06 fix). (3) templates/context-recipe.yaml default section order/priority values don't match pre-refactor dispatch-prompt parity — worked around by _bc_display_order/_bc_display_name/_bc_display_priority shim in build-context.sh (to drop after P06 recipe re-authoring). (4) scripts/verify/check-must-haves.sh doesn't discover tasks/TNN-PLAN.md files — all P05 tasks had to verify must-haves manually from payloads rather than via the canonical entrypoint. (5) scripts/dispatch/build-context.sh all emit_event calls are >/dev/null 2>&1
  - decisions :: .orchestrator/DECISIONS.md:295:- **Choice**: (1) **Installer staging** (`feat(install)`): `packaging/install/install-{claude-code,codex,cursor}.sh` now stage `scripts/`, `templates/`, and `references/` into the target project root at install time, record every placed file in `.orchestrator/installed-files.txt`, and honor `--uninstall` by replaying the manifest (emits `runtime_removed=N` in the UNINSTALLED summary). `references/installation.md` + `docs/getting-started.md` rewritten to drop the stale "skill bundle" claim and document the real install → project layout including Upgrading and Uninstall sections. `tests/test-installer-stages-runtime.sh` added (24 assertions, green) covering install-exits-0, stage-to-disk, manifest-written, re-install-idempotent, CLAUDE.md-untouched, and uninstall-via-manifest. (2) **State tier fix** (`fix(state)`): `scripts/state/find-active-milestone.sh` prefers `<mid>-EVALUATION.md` as the tier authority (per `commands/evaluate.md`) and falls back to `<mid>-ROADMAP.md`. Previously read tier from roadmap only, which returned `tier=none` for evaluated-but-unroadmapped milestones and caused `orchestrator:auto` to bail with "no active milestone" mid-flow on Tier C projects. Verified against bbt-companion M001 — now returns `M001 planning C`. (3) **Ingest FR-slug fix** (`fix(knowledge)`): `scripts/knowledge/ingest-spec.sh` accepts both `**FR-N**` and `**FR-N (slug-or-anything)**` marker forms (extract numeric ID, treat optional parenthetical as decoration), and aborts loudly with `ERROR:` on any line that looks like an FR marker but yields no numeric ID. Previous behavior silently dropped 36 chunks (19 requirements / 8 constraints / 9 non-goals) from bbt-companion's spec because the slugged markers fell through the narrow regex. Loud-failure turns silent data loss into actionable spec-authoring feedback. (4) **Reinit sentinel-preservation fix** (`fix(lifecycle)`): `scripts/lifecycle/reinit-handler.sh` canonicalizes `--project-dir` via `cd && pwd` so basename extraction is correct for relative paths; snapshots every `# >>> orchestrator:NAME >>>` sentinel block (not just `project-identity`) before the regenerate pass and re-injects each one via `dual-write-runtime-md.sh` post-write; reads `runtime_confidence` from `config.yml` first then from the rendered instruction-file line, and refuses to downgrade a previously-established `high`/`medium`/`low` to `unknown`. (5) **Known limitations parked for follow-up** (four items, do NOT auto-promote to milestone scope): (a) reinit template-regenerate model still loses free-form CLAUDE.md prose outside sentinel + `<!-- BEGIN CUSTOM -->` blocks — redesign needed to either switch to targeted-edit or expand sentinel surface area; (b) installer stage count of 840 files includes `scripts/verify/m*-p*-*` fixtures and `tests/fixtures/**` that a consumer project does not need — a stage-filter could drop to ~300 files (low priority); (c) `scripts/state/check-settings-state.sh` returned `state=ok` while the regen pipeline silently failed during bbt-companion install (observed, not root-caused); (d) manifest does not track upstream deletions — a file removed from spec-kit-orchestrator between installer runs leaves a stale copy in the consumer project (accepted trade-off in handoff §6, worth revisiting).
  - decisions :: .orchestrator/DECISIONS.md:296:- **Revisable**: Yes — (a) If reinit free-form-prose loss bites a second user, promote 5a to a dedicated milestone or M014-extended task. (b) Stage bloat 5b can be addressed inside the installer without a D-row (filter in `install-claude-code.sh` Stage 4.5 `find` expression). (c) If `check-settings-state.sh` silent regen failure reproduces, open a spec and a bugfix commit. (d) Upstream-removal 5d can be fixed via manifest-diff in a future install pass. (e) If the four dogfood-bugfix commits should have been a single "fix(dogfood)" squashed commit rather than a 4-commit split, revisit commit-message-style for future hotfix batches — current split prioritizes archaeology readability.
  - decisions :: .orchestrator/DECISIONS.md:298:bbt-companion is the first external project the orchestrator has been installed into at scale, and the install surfaced a class of bugs the `test-s*` suite cannot catch because every `test-s*` runs against the development-tree layout where scripts sit beside their callers. Bundle-infrastructure gap was the root (installer wasn't staging the files `commands/*.md` reference via project-relative paths); three downstream bugs (#2, #3, #4) compounded on top because they'd never been exercised against a genuinely-fresh `.orchestrator/` state tree with no prior chunks or milestones. Fixing as out-of-band D-rows (not a new milestone) because each fix is <50 lines of code, scoped to a single script, and orthogonal to M026. Recording as D021 rather than folding into M026 keeps the conversus-OSS migration audit-trail clean. The four parked limitations (5a-d) get a D-row line instead of immediate follow-up work because (a) reinit redesign is architecturally non-trivial (targeted-edit vs sentinel expansion is a real decision); (b) installer stage bloat is cosmetic; (c) check-settings-state is speculative without reproduction; (d) upstream-removal is acknowledged-accepted. Bringing them up explicitly here means they don't get lost to context-expiry between now and when the operator decides to tackle them.
  - decisions :: .orchestrator/DECISIONS.md:310:- **Revisable**: The decision is the consolidation point for three load-bearing M026 commitments that downstream milestones (M013, M014, M018, M023, M024) need a single auditable reference for. Rather than scattering the rationale across three MEM entries and the M026-SUMMARY (not yet authored), the D-row anchors the cross-references in the existing DECISIONS audit trail. The two-tier detection pattern is reusable beyond conversus (any pip/pipx-installed Python tool with multi-channel publishing); naming the convention now means the next adapter migration can adopt the shape without re-deliberating env-var naming. The `edition_required:` preset-frontmatter contract was deferred to OQ-3 at spec-027 discuss-finalize and chose the minimum-viable shape (single optional field, no auto-detect of CLI-flag-based paid-only signals per NG-6) — D022 commits the decision in the audit trail so future scope-expansion conversations have the rationale in hand. The `FAIL:`-vs-`ERROR:` prefix uniformity rationale lives here because it is a one-line decision (preserve adapter convention; SC-7 regex accommodates) that does not warrant its own MEM entry.
  - decisions :: .orchestrator/DECISIONS.md:368:Closes Q-14. Aligns with M019 char-quartile token-estimate library and pricing.sh degradation contract (FR-24). Keeps existing key=value text contract byte-stable per CON-3 — verifier (FR-15 / SC-17) gates regression. JSON shape mirrors retrospective rollup row structure so M018 / Tier 3 consumers can parse one shape across surfaces. Tier keys (quick/standard/full) match recipes/intensity tier names.
  - decisions :: .orchestrator/DECISIONS.md:382:Closes Q-15. The +/-20% figure carries from M019/P01 token-estimate library design notes (char-quartile tokens differ from BPE tokens by roughly that margin under English-prose recipe templates). Spelling out the disclaimer in commands/cost.md makes it operator-visible without polluting machine-parseable output. Output trailer is one short line so machine consumers (M018, Tier 3) can strip it deterministically.
  - decisions :: .orchestrator/DECISIONS.md:498:| D002 | M035/P02 | convention | Test-fixture strategy for postinstall + bin entry validation (#Q-10) | npm pack to local tarball + install into fixture-local prefix | Avoids polluting public registry and adopter home dirs. npm pack assembles the same tarball npm publish would; npm install -g --prefix=<fixture>/.npm-prefix ./orchestrator-*.tgz exercises postinstall under DRY_RUN=1 by default. Live postinstall is exercised in CI under a containerized fixture only on tag-push events. Avoids the verdaccio service dependency. | No |
  - decisions :: .orchestrator/DECISIONS.md:500:| D004 | M035/P05 | signing-strategy | Release-artifact signing for the npm publishing pipeline (FR-11 / SC-11 / #Q-3 binding) | sigstore (cosign keyless) primary + SHA-256 checksum fallback | Keyless cosign signing in `.github/workflows/release.yml` (`npm-publish` job) binds each release artifact's signature to the workflow's GitHub OIDC identity (canonical-repo `v*` tag-push) and records it in the public Rekor transparency log — eliminates the GPG private-key blast radius (no long-lived signing key to rotate or compromise). `SHA256SUMS` published alongside the signed artifacts gives operators without `cosign` installed a tooling-free verification path (`shasum -a 256 -c SHA256SUMS`). The signing job uses a job-level `permissions: id-token: write` override; the workflow-level `permissions: contents: read` stays unchanged so other jobs (pr-validate) inherit least-privilege. CON-6 secret-scope discipline preserved (no new long-lived secrets; `secrets.GITHUB_TOKEN` is the default token, scoped by GHA). | No |
  - decisions :: .orchestrator/DECISIONS.md:501:| D005 | M035/P05 | schema | Rollback-marker schema for `.orchestrator/.previous-version` (FR-12 / SC-12 / #Q-G8 binding) | Five-field structured `key=value` sidecar (`prior_version`, `prior_commit_sha`, `prior_manifest_path`, `prior_install_mode`, `rolled_at`) + byte-for-byte snapshot of prior `installed-files.txt` to `.orchestrator/.rollback/manifest-<prior-version>.txt` | Snapshot-at-upgrade-time decouples rollback from source-repo reachability — the rollback path must succeed even when the source repo is unreachable (e.g. `update_source: npm` upgrades against a published tarball with no local clone). `prior_install_mode` field captures `copy` / `symlink` / `mixed` / `unknown`; T02's `--rollback` driver consults this field to refuse the rollback per #Q-G8 when symlink-mode is anywhere in the prior tree (the more restrictive interpretation: any symlink in the runtime tree makes byte-equivalent revert undefined). T01 records; T02 enforces. | No |
  - decisions :: .orchestrator/DECISIONS.md:99:- **Choice**: (1) Remove M017 (Conversus Deliberation Gate) from the forward milestone sequence. Revised sequence: **M011 → M012 → M013 → M014 → M009 → M010**. (2) Add M011/P07 (Format-Agnostic Intake & Conversus Fidelity Gate) which lands both the LLM-driven normalizer (any markdown → spec-kit shape) and the reusable `scripts/dispatch/adapters/tool/conversus.sh` adapter. (3) The adapter becomes shared infrastructure: later milestones that want deliberation at specific checkpoints (M013 UAT PR gate, M014 ambiguous-comment classification, roadmap phase-decomposition at Full intensity) invoke it from within their own scope, rather than waiting on a dedicated milestone. (4) Intensity engine owns the policy (when gates fire by default); users own opt-in/out via `--review` / `--no-review`.
  - decisions :: (capped at 5 hits — more may exist)
  - knowledge-entries :: (capped at 5 hits — more may exist)
  - knowledge-entries :: knowledge/conventions/MEM012.md:32:Commands reference scripts by relative path in "Referenced Scripts" sections. Integration tests verify all cross-references resolve to existing, executable files.
  - knowledge-entries :: knowledge/conventions/MEM013.md:21:Summary frontmatter: 15-field base schema for tasks (`schema_version`, `type`, `id`, `parent`, `milestone`, `provides`, `requires`, `affects`, `key_files`, `key_decisions`, `patterns_established`, `drill_down_paths`, `duration`, `verification_result`, `completed_at`); phase/milestone summaries add `observability_surfaces` for 16 fields.
  - knowledge-entries :: knowledge/conventions/MEM014.md:19:**Scripts -> Commands**: Commands reference scripts by relative path in "Referenced Scripts" sections. Integration tests verify all cross-references resolve to existing, executable files.
  - knowledge-entries :: knowledge/conventions/MEM017.md:19:`scripts/lib/verdicts.sh` provides `emit_verdict`, `parse_verdict`, `orch_is_verdict` with four constants: PASS, BLOCK, WARN, NEEDS_REVIEW. `hooks.sh` captures hook stdout, parses VERDICT lines, resolves multiple verdicts to most severe, and maps to block/warn/continue behavior. Backward compatible when no VERDICT present. Provider convention documented in `references/provider-convention.md`.
  - knowledge-entries :: knowledge/conventions/MEM030.md:17:# MEM030: `<TOOL>_EDITION=<value>` env-var convention for OSS-default escape hatches
  - knowledge-entries :: knowledge/conventions/MEM030.md:21:When an orchestrator integration flips its default from a paid build to an OSS build, the operator still needs a discoverable, undestructive way to reach the paid build for one-off invocations (debugging a paid-only feature, reproducing a paid-build regression, running a preset that depends on paid-only upstream plumbing). Three anti-patterns to avoid:
  - knowledge-entries :: knowledge/conventions/MEM030.md:23:1. **Path-only escape** — requiring the operator to set `<TOOL>_HOME=/explicit/path/to/paid/build` per invocation. Undiscoverable, easy to forget, and brittle across machines with different install paths.
  - knowledge-entries :: knowledge/conventions/MEM030.md:25:3. **No escape** — routing all paid-only access through a separate command or wrapper. Forces the orchestrator to maintain two parallel invocation paths with the same surface, doubling test burden.
  - knowledge-entries :: knowledge/conventions/MEM030.md:37:5. **Telemetry shape**: every JSONL record emitted by the integration adapter MUST include an `edition` field alongside the existing identifying fields (e.g., `adapter_version`, `gate_id`). Place adjacent to the version field for readability and for adjacency-invariant tests (M026/P02/T02 pattern).
  - knowledge-entries :: knowledge/conventions/MEM030.md:43:The M026 migration is the first OSS-default escape-hatch landing in this repo. Future migrations (e.g., when M010 ships and the orchestrator starts integrating with multiple LLM-provider editions, or when M023 ships and design-renderer adapters need to distinguish freemium tier vs paid tier) will face the same shape. Naming it as a convention now means the next migration can copy the pattern verbatim instead of re-deliberating the env-var name in each milestone.
  - knowledge-entries :: knowledge/conventions/MEM030.md:48:- **JSONL `edition` field presence**: every `*_invocation` record from an edition-aware adapter MUST contain an `edition` field. Verify with `scripts/verify/m026-p02-jsonl-edition-field.sh` (existing) — extend the pattern when adding a second edition-aware adapter.
  - knowledge-entries :: knowledge/conventions/MEM031.md:21:Every `knowledge/**/MEM*.md` and `knowledge/spec/**` entry carries a
  - knowledge-entries :: knowledge/conventions/MEM031.md:27:| `graduated` | Reviewed and accepted; visible to the default query surface (FR-2).     |
  - knowledge-entries :: knowledge/conventions/MEM031.md:32:in P02 (`scripts/verify/knowledge-schema-lint.sh`, FR-9 + SC-8). Adding a
  - knowledge-entries :: knowledge/conventions/MEM031.md:36:## Default semantics for pre-M020 entries
  - knowledge-entries :: knowledge/conventions/MEM031.md:64:M020 holds exclusive schema authority over these fields per FR-9. Consuming
  - knowledge-entries :: knowledge/conventions/MEM031.md:80:- `scripts/verify/knowledge-schema-lint.sh` (lands in P02 per FR-9 + SC-8)
  - knowledge-entries :: knowledge/lessons/MEM026.md:17:# MEM026: M013/P04/T04 hook-config regression
  - knowledge-entries :: knowledge/lessons/MEM026.md:21:Commit `d33b8a7` (M013/P04/T04) shipped a `scripts/dispatch/adapters/runtime/claude-code.sh --hook-config` emitter whose root object carried wrapper metadata (`runtime`, `hook_count`, `target_file`) alongside orchestrator-internal event names (`before_tasks`, `after_tasks`, `before_implement`, `after_implement`, `before_commit`, `post_verify`) that Claude Code does not recognize. The companion installer `packaging/install/install-claude-code.sh` wrote that invalid document unconditionally to `$HOME/.claude/settings.json`, overwriting any sibling tool's configuration (notably GSD-authored `statusLine`, `SessionStart`, `PostToolUse`, and `permissions` keys) with no merge path and no reversibility.
  - knowledge-entries :: knowledge/lessons/MEM026.md:25:Every P04 verification script — including `scripts/verify/m013-p04-post-verify-hook.sh` — ran against an empty `$HOME/.claude/` fixture. No gate seeded a pre-existing non-orchestrator `settings.json`, so the overwrite path was never exercised. The schema-validity failure was invisible because CC itself was not invoked against the emitted document in any P04 gate; the gates only asserted presence of the wrapper JSON's internal keys, not conformance to the CC `hooks` schema. The regression required a real user on a real multi-tool system to observe.
  - knowledge-entries :: knowledge/lessons/MEM026.md:29:Every user-scope config write — `~/.claude/settings.json`, `~/.codex/*.toml`, `~/.cursor/*.json`, shell rc files, git hooks paths, and anything else jointly owned with sibling tools — requires a coexistence gate driven by a pre-seeded non-orchestrator fixture (pattern realized by M025 at `tests/fixtures/m025-p01/gsd-baseline/`). Empty-home fixtures are insufficient: they cannot surface overwrite damage or schema-shape issues that only manifest when the host tool actually parses the file. Pair the coexistence gate with (a) a round-trip reversibility gate (install → uninstall → sha256 byte-match) and (b) an idempotency gate (double-install → sha256 byte-match) to lock the invariant triad.
  - knowledge-entries :: knowledge/lessons/MEM028.md:17:# MEM028: Bash arithmetic silently interprets zero-padded numerics as octal
  - knowledge-entries :: knowledge/lessons/MEM028.md:2:id: MEM028
  - knowledge-entries :: knowledge/lessons/MEM028.md:23:Historical damage: M025's spec landed at `specs/021-github-installer-coexistence/` instead of `specs/025-*` (bug pre-dated this fix; renaming deferred since all M025 artifacts reference the `021-` path consistently and the milestone is closed). A smoke-test fixture `specs/021-yn-test/` was also cleaned up as leaked state from a pre-hermetic verifier run.
  - knowledge-entries :: knowledge/patterns/MEM027.md:21:User-scope config files (`~/.claude/settings.json`, `~/.codex/*.toml`, `~/.cursor/*.json`, shell rc files, git `core.hooksPath`) are jointly owned by multiple tools — the host runtime itself, sibling workflow tools (GSD, spec-kit, orchestrator), user hand-edits, and corporate MDM policy. Any installer that writes these files with `printf > "$file"` or `cp` silently destroys sibling configurations and user edits. The regression surfaced at M013/P04/T04 (see MEM026) made this concrete: the orchestrator clobbered GSD's `statusLine`, `SessionStart`, and `PostToolUse` hooks with no warning and no rollback.
  - knowledge-entries :: knowledge/patterns/MEM029.md:21:When an installed package has multiple distributable builds (OSS vs paid, dev vs prod, community vs enterprise) that share a single PyPI/npm/etc. package name and venv install path, path-based detection is infeasible — both editions install to the same canonical path under pip/pipx/npm. A consumer that needs to know which edition is currently active (for routing decisions, edition-aware diagnostics, or telemetry) cannot rely on the binary path alone.
  - knowledge-entries :: knowledge/patterns/MEM029.md:29:2. **Fallback — runtime metadata probe**. When the env var is unset, query the package's installed metadata (`pip show <pkg>` for Python; `npm ls <pkg> --json` for Node; equivalent registry probes for other ecosystems). Parse a stable identifying field (`Home-page:` for Python pip; `repository.url` for Node) and key on a canonical substring (e.g., `*-oss` in the URL). The probe is read-only, side-effect-free, and runs under the consumer's existing subprocess discipline. On probe failure (subprocess fails, field absent, value unrecognized), emit `edition=unknown reason=metadata-probe-failed` rather than guessing.
  - knowledge-entries :: knowledge/patterns/MEM029.md:45:- Distinguishing editions of any pip/pipx-installed Python tool that publishes under one package name across multiple build channels.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:134:- **[milestone:M005]** [2026-04-12] Gate verdict protocol — scripts/lib/verdicts.sh provides emit_verdict, parse_verdict, orch_is_verdict with four constants: PASS, BLOCK, WARN, NEEDS_REVIEW. hooks.sh captures hook stdout, parses VERDICT lines, resolves multiple verdicts to most severe, and maps to block/warn/continue behavior. Backward compatible when no VERDICT present. Provider convention documented in references/provider-convention.md. Established in P05 per AD-3.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:135:- **[milestone:M005]** [2026-04-12] Autonomy permission pipeline — generate-permissions.sh introspects project toolchain (package.json, Makefile, extension.yml, config files, agent host markers) and emits canonical JSON. write-permissions.sh translates to .claude/settings.json with additive merge for user-authored files. check-permissions.sh detects permission drift. Policy is declarative in autonomy-defaults.yaml read via recipe-parser.sh. AD-19 script-file verification shape: task plan Check: commands must use single-script invocations, not inline compound bash. Established in P07.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:139:- **[milestone:M004]** [2026-04-13] P06 backtick-in-plan-artifacts lesson: check-must-haves.sh includes backtick characters literally when parsing Artifact and Key Links sections from phase plans. Paths in these sections must NOT be wrapped in markdown backticks or the artifact/key-link checks will fail with 'not found' errors pointing to paths like `path/to/file` instead of path/to/file. Truth Check: commands are unaffected because the parser strips the outer backticks from the fenced command.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:142:- **[milestone:M006]** [2026-04-14] Documentation-as-verification (verify-as-you-write) surfaces real bugs: M006 found and fixed a stale routing fallback value in references/file-formats.md. The mechanical discipline of running every documented command catches drift that tests miss.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:143:- **[milestone:M006]** [2026-04-14] Cross-link validation scripts (checking that doc A references doc B) catch missing references that manual review misses. Every P01-P06 phase required cross-link fixes during the verification step. Pattern: always create cross-link verification scripts during planning.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:145:- **[milestone:M008]** [2026-04-14] Hermetic-first testing for runtime/HOME-touching scripts: all installer, runtime-adapter, and init tests use HOME=$(mktemp -d) or --project-dir $(mktemp -d). HOME guards in adapter scripts refuse HOME="" or HOME=/ to prevent root-directory writes. A static gate (m008-p07-hermetic-only.sh) mechanically enforces this pattern across P07 verifiers. Never invoke --register or install-*.sh against real developer HOME during automated tests.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:147:- **[milestone:M008]** [2026-04-14] User-edit preservation via comment-delimited blocks: templates include <!-- BEGIN CUSTOM --> / <!-- END CUSTOM --> markers on their own lines. Regeneration scripts (reinit-handler.sh --mode update) extract the custom block via awk line matching and re-inject it into the regenerated file. Pair with field-level awk surgery for YAML/config (regenerate auto-detected top-level keys, preserve user-added keys). Default regeneration mode should be update (preserving), not reset.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:148:- **[milestone:M008]** [2026-04-14] Thin delegation pattern: top-level orchestration scripts (init-project.sh, installers) should delegate to existing single-responsibility scripts rather than duplicate logic. init-project.sh delegates to detect-runtime (P05), detect-capabilities (P01), detect-project (P07), resolve-root (P04), and runtime-specific installer (P06). Installers delegate --probe/--register/--hook-config to P05 runtime adapters. This keeps each script testable in isolation and prevents behavior drift across layers.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:150:- **[milestone:M003]** [2026-04-15] Three-tier root resolution (flag → env var → resolve-root.sh fallback) is the standard CLI contract for orchestrator-root-consuming scripts. Makes scripts work under both ORCHESTRATOR_ROOT env and explicit --root, with config-driven default. Reference: scripts/orchestrator/status.sh.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:154:- **[milestone:M016]** [2026-04-16] write-summary.sh --completed_at is optional. Omit it to default to current UTC timestamp. Pass --completed_at=now as a sentinel. This eliminates the #1 source of Claude Code safety prompts (command substitution in timestamp generation). See AP-004 in ANTIPATTERNS.md.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:157:- **[milestone:M021]** [2026-04-17] Shape-classifier as single source of truth: a classifier library (scripts/verify/lib/shape-classifier.sh) consumed by both the enforcement layer (pre-Bash hook) and the regression gate (replay-prompt-corpus.sh) guarantees byte-identical decisions and catches classifier drift the moment corpus expectations diverge.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:158:- **[milestone:M021]** [2026-04-17] Permanent fixture corpus with locked entry count: the 20-entry M011/P05-P07 prompt corpus is the authoritative regression surface — every entry traces to a specific screenshot (ID+SCREENSHOT+INPUT+EXPECTED_OUTCOME). Future hook triggers earn a new milestone, not a fixture append (constitution XIV; AD-5).
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:159:- **[milestone:M021]** [2026-04-17] Dogfood attestation from state on disk: milestone closure gate reads auto-loop-result.txt + execution-log.jsonl + P*-SUMMARY.md and greps for user_prompt/safety_prompt/hook_reject_unexpected events. hook_reject_recovered is deliberately OMITTED from needles because recovery events are the success signal (AD-6). Gate is self-referential-safe — passes during its own phase by skipping in-flight summaries.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:164:- **[milestone:M019]** [2026-04-18] Goodhart pairing enforced at emission time: every unit_close record carries both a cost block (estimated_cost_usd, pricing_version) and a quality block (verification_pass_rate, deviation_count, retry_count derived from existing attempt/outcome/verification_result per AD-3). Schema validator rejects records missing either. Prevents Tier 2 from optimizing cost in isolation.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:169:- **[milestone:M019]** [2026-04-18] Opus 4.7 dispatch payload structure (M019/P00 L1-L5): every TASK-branch payload emits a First-Turn Completeness block (Intent/Constraints/Acceptance Criteria/Files To Touch, re-surfacing existing content, no new prose); stable sections before <dispatch-volatile> marker, volatile sections after, </dispatch-volatile> close; conditional Parallel Fan-Out directive only when recipe or task plan declares parallelizable work; intensity-gate.sh documents adaptive-thinking contract as a comment block (no fixed thinking_budget literals); expressive guidance rewritten positive with a narrow dotfile whitelist for Constitution XV retained-negatives.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:18:- **Cross-reference validation**: Integration tests extract script/template paths from command files via `grep -oE` regex, then verify existence/executability. Self-maintaining as commands evolve.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:182:- **[milestone:M013]** [2026-04-22] FR-12-v1 negative-grep guard for sibling-runtime byte-identity: when one runtime adopts a feature that other runtimes legitimately don't yet support (FR-12 Claude-Code-only v1 for the post-verify hook), guard sibling-runtime files (codex/cursor adapters + installers) with a negative-grep gate for the feature marker (e.g., 'post_verify' / 'FR-12') rather than pinned-sha digests. Cheaper to author and READ, and self-repairs if codex/cursor later gain legitimate content under a DIFFERENT marker. Trade-off: weaker than pinned-sha against accidental drift, but the constraint here is intent-shape not exact bytes.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:19:- **Self-diagnostic pattern**: Test files verify their own `fail()` messages include actionable file paths or contract identifiers.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:202:- **[milestone:M028]** [2026-04-29] Helper-function carve-out for AD-19: function bodies are NOT scanned by the AP-009 inline-command-shape classifier in production. assert_reject(), classify_one(), compute_sha(), count_managed_flags(), extract_corpus_input() and similar multi-step helpers can be hoisted into bash function bodies at top-of-script and called inline; the surrounding script remains AD-19-conformant even when helper bodies contain compound substitutions or multi-stage pipelines. Codified at top-of-file as a comment-block convention starting M028/P02/T05; load-bearing across every M028 verifier authoring extraction or shape-assertion helpers.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:203:- **[milestone:M028]** [2026-04-29] Plan-time classifier-shape pre-validation as authoring discipline: every shape-bearing line introduced by a task plan or inside a hook/script body MUST be empirically run through scripts/verify/lib/shape-classifier.sh::classify_command at plan-authoring time, with the verdict recorded in plan prose. Closes the planner-vs-classifier confabulation gap surfaced repeatedly across M028 (T01 reshaped resolution block; T05 swapped test command from sh-c-body to bare compound chain; P03 verifiers used pinned-INPUT alignment between corpus and verifier). Without the trace, the verdict claim is text not contract.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:204:- **[milestone:M028]** [2026-04-29] Per-task verifier co-located with deliverable: cross-task verifier dependency is rejected at plan-authoring time. Every task plan's ## Verification command MUST resolve to an existing-on-disk script at plan-authoring time. If a verifier doesn't exist yet, the plan must either (a) schedule its authorship inside the same task's ## Steps (co-authored with the deliverable), or (b) use a stub-tolerant inline shape-check. Auto-loop --step=V is mechanical — cross-task verifier dependency causes uniform false FAIL no matter how correctly deliverables land. Surfaced in M028/P03 (T01-T04 plans referenced T05-deliverable verifiers; required mid-execution patching). Symmetric on the verification side to plan-time prerequisite-existence verification.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:205:- **[milestone:M028]** [2026-04-29] Snapshot-ladder shape for byte-equality gates: pre-state -> action-1 -> action-2 -> reverse-action; SHA-256 at each step; assert (action-1 == action-2) for idempotency AND (pre == reverse) for reversibility in a single isolated-HOME exercise. Cheaper than separate idempotency + reversibility verifiers; the four snapshots fit in one tmp dir and one set -u execution context. Codified by M028/P02/T05 install-roundtrip.sh; pinned post-install SHA d47ab7f32ef6bda277e4d3b6ab04ed70ddd92a3388250f13badc79d85389f32a captured in comment block as informational drift signal (hard gate stays byte-equality; pin is human audit trail).
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:206:- **[milestone:M028]** [2026-04-29] Defensive flag-refusal as wrapper contract: when a wrapper exists to retire an antipattern shape, the wrapper MUST refuse the antipattern's signature flag/arg pattern rather than silently accepting it — otherwise callers can rebuild the antipattern shape inside the wrapper invocation. node-eval.sh refuses -e/-p/--eval/--print as the load-bearing defense for AP-012 multiline-quoted-script. Companion shape for any future antipattern-replacement wrapper (e.g. a future bash-c-wrapper that refuses bash -c on positional 1). Surfaced and codified in M028/P04.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:38:- Summary frontmatter: 15-field base schema for tasks (`schema_version`, `type`, `id`, `parent`, `milestone`, `provides`, `requires`, `affects`, `key_files`, `key_decisions`, `patterns_established`, `drill_down_paths`, `duration`, `verification_result`, `completed_at`); phase/milestone summaries add `observability_surfaces` for 16 fields
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:66:- Knowledge entries without scope tags included by default (project-level)
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:86:Commands reference scripts by relative path in "Referenced Scripts" sections. Integration tests verify all cross-references resolve to existing, executable files.
  - knowledge-md :: (capped at 5 hits — more may exist)
  - milestone-summaries :: .orchestrator/milestones/M002/M002-SUMMARY.md:19:drill_down_paths:
  - milestone-summaries :: .orchestrator/milestones/M003/M003-SUMMARY.md:12:  - "M009 (recipe packaging can assume .orchestrator/ default target); future milestone-management work"
  - milestone-summaries :: .orchestrator/milestones/M003/M003-SUMMARY.md:19:drill_down_paths:
  - milestone-summaries :: .orchestrator/milestones/M003/M003-SUMMARY.md:28:M003 Migration Tool milestone complete. Initial build (P01-P06, commit ad3da8a 2026-04-09) delivered the end-to-end migration pipeline: adapter interface with pluggable source formats (GSD2/GSD v1/spec-kit), per-artifact transforms (knowledge, decisions, requirements, milestones with tiering), and CLI with idempotency + reporting. After M007 (knowledge graph) and M008 (standalone multi-runtime + 5-rule state resolver) landed, a delta audit surfaced three drift points: hardcoded .specify/orchestrator/ paths, unpopulated graph database, single-layout idempotency probe. P07 refit wired migrate.sh to resolve-root.sh --absolute, dropped hardcoded paths in three transforms, added dual-root idempotency probing, wired rebuild-index.sh as final pipeline stage, and documented AD-13/14/15 in commands/migrate.md. P08 validated end-to-end via deterministic synthetic GSD2 fixture (primary) and live lakeledger (secondary, skip-gracefully), new scripts/orchestrator/status.sh CLI wrapper making the roadmap demo sentence literally executable, 8 AD-19-safe verify scripts, and a two-pass integration test — all green. Surfaced and fixed one latent P02 bug (missing content_hash frontmatter) during refit. Retroactive phase summaries added for P01-P06 (originally delivered before phase-summary machinery) based on roadmap Revision Note + commit ad3da8a. Migration pipeline now produces resolver-wired output, populates the M007 graph database, and participates in graph traversal — ready for M009 recipe packaging and M010 cloud dispatch.
  - milestone-summaries :: .orchestrator/milestones/M004/M004-SUMMARY.md:12:  - "Conversus integration (hook lifecycle points), future milestones (structured events, recipe-driven assembly)"
  - milestone-summaries :: .orchestrator/milestones/M004/M004-SUMMARY.md:14:  - "scripts/lib/errors.sh, scripts/lib/events.sh, scripts/lib/run-context.sh, scripts/lib/guards.sh, scripts/lib/hooks.sh, scripts/engine/run.sh, scripts/engine/checkpoint.sh, scripts/lib/recipe-parser.sh, templates/context-recipe.yaml, templates/hooks.yaml, scripts/dispatch/build-context.sh, scripts/dispatch/compress-payload.sh, scripts/dispatch/select-model.sh, scripts/diagnostics/check-recipe.sh, .specify/memory/constitution.md"
  - milestone-summaries :: .orchestrator/milestones/M004/M004-SUMMARY.md:18:  - "Double-sourcing guard in first 5 lines, emit_result on every exit path, emit_event at lifecycle boundaries, ORCH_RUN_ID standalone detection, recipe-driven context assembly, hook sandbox with chmod 444 frozen snapshots, chained EXIT traps for scripts with pre-existing cleanup"
  - milestone-summaries :: .orchestrator/milestones/M004/M004-SUMMARY.md:19:drill_down_paths:
  - milestone-summaries :: .orchestrator/milestones/M004/M004-SUMMARY.md:25:  - "All engine-path scripts emit EVENT: lines to stderr when ORCH_RUN_ID is set; RESULT: lines on every exit; record-result.sh adds run_id to execution-log.jsonl; aggregate-metrics.sh reports by_error_kind; check-recipe.sh emits DOCTOR:RECIPE"
  - milestone-summaries :: .orchestrator/milestones/M004/M004-SUMMARY.md:28:M004 replaced implicit agent-driven coordination with a mechanical engine layer. P01 evolved the constitution to v2.0 (13 principles + ANTIPATTERNS.md). P02 delivered 5 shared libraries (errors, events, run-context, guards, hooks) totaling 647 lines. P03 built the engine core (run.sh pipeline coordinator + checkpoint.sh crash recovery). P04 designed the YAML recipe system (context-recipe.yaml, hooks.yaml, extended routing.yaml + recipe-parser.sh). P05 refactored 3 dispatch scripts to be recipe-driven while preserving identical output. P06 integrated 6 additional scripts with the library stack, fixing the PROJECT_ROOT detection bug and adding run_id/error_kind to JSONL. P07 added recipe conformance diagnostics. 38 dispatches across 7 phases, 0 task-level retries for P06+P07. All scripts maintain standalone compatibility via ORCH_RUN_ID guards (NFR-204).
  - milestone-summaries :: .orchestrator/milestones/M004/M004-SUMMARY.md:8:  - "Engine architecture foundation: 5 shared libraries (errors.sh, events.sh, run-context.sh, guards.sh, hooks.sh), engine core (run.sh + checkpoint.sh), YAML recipe system (context-recipe.yaml, hooks.yaml, routing.yaml, recipe-parser.sh), recipe-driven refactored dispatch scripts (build-context.sh, compress-payload.sh, select-model.sh + section-handlers.sh), engine integration for 6 additional scripts (check-must-haves.sh, record-result.sh, record-telemetry.sh, aggregate-metrics.sh, classify-complexity.sh, phase-transition.sh), recipe conformance diagnostic (check-recipe.sh), constitution v2.0 (13 principles + ANTIPATTERNS.md)"
  - milestone-summaries :: .orchestrator/milestones/M008/M008-SUMMARY.md:14:  - "scripts/engine/intensity-analyze.sh,scripts/engine/intensity-recommend.sh,scripts/engine/intensity-gate.sh,scripts/engine/intensity-override.sh,scripts/engine/context-pressure.sh,scripts/dispatch/detect-capabilities.sh,scripts/dispatch/detect-runtime.sh,scripts/dispatch/dispatch-interface.sh,scripts/dispatch/backend-registry.sh,scripts/dispatch/adapters/backend/,scripts/dispatch/adapters/runtime/,scripts/dispatch/adapters/format/,scripts/state/resolve-root.sh,scripts/state/detect-speckit.sh,scripts/state/config-system.sh,scripts/state/namespace-aliases.sh,scripts/migrate/migrate-state.sh,scripts/knowledge/intensity-knowledge.sh,scripts/lifecycle/init-project.sh,scripts/lifecycle/detect-project.sh,scripts/lifecycle/reinit-handler.sh,scripts/lifecycle/check-update.sh,scripts/packaging/generate-skills.sh,scripts/packaging/build-bundle.sh,packaging/SKILL.md,packaging/skills/,packaging/bundle/,packaging/install/,templates/intensity-metadata.md,templates/dispatch-result.md,templates/dispatch-error.md,templates/project-instruction.md,commands/init.md"
  - milestone-summaries :: .orchestrator/milestones/M008/M008-SUMMARY.md:16:  - "Centralized stage×intensity matrix in one script (intensity-gate.sh) to avoid drift across 5 command docs; filename-based adapter routing (zero backend-specific code in core) per SC-003; coordination-boundary adapter pattern for in-process tools (MEM018); uniform-interface fallback (adapter always emits dispatch-result even on unavailability); HOME guard mandatory on runtime adapters; one-directional foreign format adapter (speckit.sh rejects --write with exit 4); hard migration per project constitution (no dual code paths); surgical documentation-only refactor of derive-phase.sh per Constitution XV; additive-only command doc refactor preserving MEM012; comment-aware Bash 3.2 compat scanner; open-standard SKILL.md format with YAML frontmatter; thin installer pattern delegating to P05 runtime adapters; offline-safe version check with graceful degradation; reinit default mode is update (preserve existing user context)."
  - milestone-summaries :: .orchestrator/milestones/M008/M008-SUMMARY.md:25:  - "Per-phase observability surfaces rolled up: intensity recommendation stdout (Quick/Standard/Full + confidence + reasoning); dispatch-interface.sh stdout (dispatch-result) + stderr (dispatch-error with distinct exit codes 2-6); intensity-gate.sh stdout (execute_substeps + skip_substeps); detect-runtime.sh stdout (runtime + confidence); runtime adapter --probe/--hook-config/--dry-run stdout; check-update.sh stdout (installed_version + latest_version + update_available + update_instructions); init-project.sh SUMMARY: line with installed skills count + paths"
  - milestone-summaries :: .orchestrator/milestones/M008/M008-SUMMARY.md:28:M008 transformed the orchestrator from a spec-kit extension into a standalone multi-runtime extension with adaptive process intensity. Across 7 phases (P01-P07), delivered: (1) Adaptive Intensity Engine — analyzes task descriptions along scope/risk/complexity axes and recommends Quick/Standard/Full intensity with environment capability awareness. (2) Backend-Agnostic Dispatch Interface — uniform entry point with filename-based adapter routing, two local backends (Claude Code Agent tool + Codex CLI SDK), structured result/error schemas with distinct exit codes. The explicit seam for M010's cloud backends. (3) Intensity-Aware Pipeline Scaling — central 7×3 stage×intensity matrix wires intensity awareness into every pipeline stage (discuss, research, plan-phase, dispatch, verify, knowledge, auto) with atomic mid-workflow override. (4) State & Namespace Independence — 5-rule root resolver, unified config, hard migration tool, surgical derive-phase refactor, namespace aliases. Full workflow runs standalone without spec-kit (SC-004 validated). (5) Runtime & Format Adapters — auto-detection + 3 runtime adapters (claude-code/codex/cursor) with HOME/project-dir guards + 2 format adapters (native round-trip, spec-kit one-directional read). (6) Multi-Runtime Packaging — open-standard SKILL.md format, 12 skills generated from commands, installable bundle, 3 runtime installers delegating to adapters, offline-safe version checker. (7) Init, Onboarding & Spec-Kit Bridge — orchestrator:init detects project (10 languages + 9 frameworks + 6 CI systems + 9 tools), generates config + runtime-appropriate instruction file (CLAUDE.md/AGENTS.md/.cursor/rules), installs skills. Reinit preserves user-edited custom blocks via comment markers. Full onboarding completes in ~1 second wall-clock (SC-005 validated with 2 orders of magnitude margin vs 5min target). Cross-cutting patterns: hermetic-first testing (mktemp fixtures, never touch real HOME), comment-aware Bash 3.2 compat, filename-based auto-discovery mirrored from P02 through P05/P06, thin delegation pattern, user-edit preservation via comment-delimited blocks. M008 is the foundation for M009 (debug protocols) and M010 (cloud dispatch via Managed Agents).
  - milestone-summaries :: .orchestrator/milestones/M008/M008-SUMMARY.md:8:  - "Standalone multi-runtime orchestrator with adaptive intensity: intensity engine (scope/risk/complexity analyzer + recommendation engine + metadata schema + context-pressure evaluator), uniform backend-agnostic dispatch interface (filename-based adapter routing with local-agent + local-codex backends), intensity-aware pipeline scaling (stage-level gate + override + knowledge wrapper across 5 command docs), state/namespace independence (resolve-root + detect-speckit + unified config + migration tool + namespace aliases), runtime adapters (Claude Code + Codex + Cursor with HOME/project-dir guards), format adapters (native round-trip + spec-kit one-directional read), multi-runtime packaging (SKILL.md spec + 12 skills + bundle + 3 installers + offline-safe version check), and onboarding (detect-project + project-instruction template + init-project + reinit-handler with user-edit preservation)."
  - milestone-summaries :: .orchestrator/milestones/M011/M011-SUMMARY.md:30:Cross-cutting verification: 45+ `m011-*.sh` verify scripts created across the seven phases, plus preserve-references regressions that guard the growing doc surface phase-over-phase. Final milestone validator passes 121/121. P04 regression stays green across all downstream phase additions. Bash 3.2 compat is scanned in every phase. E2E timings stay well under contract (<60s ingest, <120s foreign PRD pipeline in stub mode).
  - milestone-summaries :: .orchestrator/milestones/M012/M012-SUMMARY.md:119:- **M013 (GitHub Issues/Projects/Milestones sync)** inherits the chained-gate deploy wrapper pattern, the loud-fail-over-silent-empty convention for missing external config, and the `scripts/diagnostics/` idempotent-helper convention (dry-run-default, `DRY-RUN`/`OK`/`NOOP`/`FAIL` fixed-verb output).
  - milestone-summaries :: .orchestrator/milestones/M012/M012-SUMMARY.md:16:  - "AD-1 D011 ship cross-refs only (defer review-state + query surface to M020); AD-2 deploy to same repo gh-pages + same Discussions; AD-3 SSOT via include-markdown plugin (no copy, no symlinks); AD-4 archive included labeled with no date cutoff; AD-5 Giscus mapping=pathname + documented remap script; AD-6 all phases/tasks included with collapsible per-milestone nav; AD-7 MkDocs + Material + include-markdown-plugin + Giscus + GitHub Pages framework stack locked; AD-19 single-script-file Check shape with MEM004 carve-out for scanner/diagnostic/wrapper internals; D011 mechanical evaluation rule (1 of 3 criteria -> M020 promoted); Constitution VI state-on-disk-is-truth preserved via include-only projection; Constitution XIV no-speculative-complexity (no flags, no plugin drivers, no YAML-driven wrappers); Constitution XV surgical precision (each phase touches disjoint file surfaces; cross-phase additions via marker-bounded blocks + EOF-append); MEM001 Bash 3.2 discipline across all shipped scripts; loud-fail-over-silent-empty for ENV empty defaults; SC-10 self-contained wiki/ + sibling-gate allow-list extension policy; SC-12 pending-sentinel deploy record for operator-gated outcomes"
  - milestone-summaries :: .orchestrator/milestones/M012/M012-SUMMARY.md:30:M012 ships the dogfood spec→wiki loop: a deployable MkDocs Material site that projects every in-scope `.orchestrator/**.md` artifact plus the `knowledge/**/MEM*.md` corpus through a single-source-of-truth include pipeline, overlays per-page Giscus threads, and deploys via a chained wrapper that runs four pre-deploy gates before `mkdocs gh-deploy`. The milestone's vision was "a dogfood-only MkDocs + Giscus site that renders `.orchestrator/` artifacts as a navigable, commentable wiki with one-command deploy" — and that is exactly what landed across four phases (P01 → P02 → P03 → P04), 37 verification gates in total, and a D011 mechanical evaluation that promoted **M020** to a committed milestone.
  - milestone-summaries :: .orchestrator/milestones/M012/M012-SUMMARY.md:57:- **Giscus injection surface** — `wiki/overrides/partials/comments.html` (Material comments-partial override) emits the Giscus `<script>` block populated from `config.extra.giscus.*`. `wiki/mkdocs.yml` gains `theme.custom_dir: overrides` + a marker-bounded `extra.giscus` block (`repo`, `repo_id`, `category`, `category_id` via `!ENV NAME,''` with empty defaults; `mapping: pathname` literal).
  - milestone-summaries :: .orchestrator/milestones/M013/M013-SUMMARY.md:16:  - "D014 — Knowledge-Layer Boundary: M020 retains chunk-ID authority; M013 cross-links the M020 schema and never redefines/extends it (zero writes to knowledge/spec/** from any P01–P04 task); FR-9 additive-only emit pinned to existing SPEC-* frontmatter; D015 — P02/P03 scope split applied as load-bearing markers (_deferred to P03_ cells fillable in place); FR-12 Claude-Code-only v1 binding via Constitution X+XII (Codex/Cursor parity demand-driven post-launch via M010); projection-not-peer grounding via XIV+M007+D007 (sidecar operator-owned + gitignored; in-repo template); FR-11 reversibility (all writes have absent/pending exit paths; lock released before exit 3/4); FR-16 rate-limit pre-flight + auth-expiry exit-code contract; FR-17 cost emission in M019 Tier 1 shape with source=runtime hard-coded; FR-18 status --verify-cache as read-only divergence probe; AD-19 single-script-file Check shape preserved across all verify gates with comment-discipline synonyms keeping bash32 scanners self-clean; conversus adapter --strict mode added as reusable invariant for callers requiring adapter presence (M011/P07 graceful-degradation flipped off via --strict / CONVERSUS_STRICT=1)"
  - milestone-summaries :: .orchestrator/milestones/M013/M013-SUMMARY.md:32:The milestone's binding pre-discuss commitments (D014, applied via conversus red-blue pressure test of `specs/023-github-native-integration/spec.md`) all landed: **FR-12 Claude-Code-only v1** (Codex/Cursor parity demand-driven post-launch); **Minimal Slice subsection** (P01 ships an end-to-end demo before any sync surface); **FR-9 narrowed to additive-only emit** with chunk-IDs pinned to existing `SPEC-*` frontmatter (Knowledge-Layer Boundary preserves M020 chunk-ID authority); **FR-16** rate-limit + auth-expiry exit-code contracts; **FR-17** cost emission via M019 Tier 1 shape; **FR-18** `status --verify-cache` read-only divergence probe.
  - milestone-summaries :: .orchestrator/milestones/M013/M013-SUMMARY.md:43:- **D014** — Knowledge-Layer Boundary: M020 retains chunk-ID authority. M013 cross-links the M020 schema; never redefines or extends it. Zero writes to `knowledge/spec/**` from any P01–P04 task. FR-9 `## Spec Chunks` emit is additive-only (`emit_spec_chunks_section` appends after `write_full_index`; pre-existing pipe-table body remains byte-identical).
  - milestone-summaries :: .orchestrator/milestones/M013/M013-SUMMARY.md:8:  - "opt-in GitHub native integration projecting orchestrator state to Issues + Project v2 + Milestones with FR-4 byte-identity markers; operator-owned sidecar at .orchestrator/integrations/github.json (gitignored) with templates/github-integration-sidecar.json schema (sync_mode + sub_issue_mode + per-item cache); tri-state orchestrator:github status command (absent / pending-operator-complete / configured) + status --verify-cache FR-18 read-only divergence probe; orchestrator:github init --re-init FR-14 adoption branch sourcing existing repo state via marker search; orchestrator:github sync 626-line walker + reconcile loop with --dry-run manifest + lock acquisition + updateProjectV2ItemFieldValue Project v2 status closure + FR-16 pre-flight rate-limit probe + per-call classify_gh_rc wrapper releasing lock before exit 3/4 (FR-11 reversibility); UAT bug ingestion loop (.github/ISSUE_TEMPLATE/uat-bug.yml + scripts/integrations/uat-ingest.sh + knowledge/spec/defect/SPEC-DEFECT-NNN schema with chunk-lookup-failed sentinel); FR-9 additive ## Spec Chunks emit in scripts/knowledge/rebuild-index.sh + scripts/knowledge/lib/index-utils.sh emit_spec_chunks_section helper; opt-in conversus UAT PR gate scripts/integrations/github-conversus-gate.sh wrapping M011/P07 adapter --strict with 30s external watchdog (Constitution XII) + FR-13 preset templates/conversus-presets/m013-uat-defect-merge.yml (red-blue defect-advocate vs fix-advocate, BLOCK-on-tie); post-verify lifecycle hook packaging/bundle/hooks/post-verify.json + scripts/lifecycle/after-verify-sync.sh wrapper + claude-code.sh runtime adapter hook_count 5->6 + FR-12 v1 Claude-Code-only enforcement via negative-grep guard on codex/cursor adapters+installers; scripts/integrations/github-common.sh shared lib (12 P02 helpers + 1 P03 helper + 5 P04 helpers — orchestrator-id derivation + FR-4 marker primitives with shasum byte-identity + AWK depth-aware sidecar item upserts + http_probe + sidecar_update_item_cache + emit_tier1_record + classify_gh_rc + emit_conversus_gate_record + gh_marker_search_remote + auth/sub-issue/label-collision preflights); scripts/verify/graphql-call-shape.sh FR-5 three-shape whitelist CI lint; references/github-integration.md full P01..P04 reference doc with Auth Modes / Sub-Issue Representation Modes / Full Mapping Table / Re-init Adoption Contract / Sync Workflow / Sync Action Mapping / Scope Boundary table; commands/github-init.md + commands/github-sync.md + commands/github-status.md (MEM012 structure); FR-17 unit_close + conversus_gate_invocation JSONL emitters at ${ORCHESTRATOR_ROOT}/observability/tier1.jsonl in M019 Tier 1 shape; scripts/integrations/sidecar-init-pending.sh pending-sentinel bootstrap with clobber-refusal exit 2; ~30 verification gates across 4 phase-suite orchestrators (P01 9 + P02 8 + P03 8 + P04 12 + FR-5 lint regression guard)"
  - milestone-summaries :: .orchestrator/milestones/M014/M014-SUMMARY.md:10:  - "M011 (spec management primitives — ingest-spec.sh, detect-spec-shape.sh, normalize-spec.sh, rebuild-index.sh, chunk-source-line metadata); M011/P07 conversus adapter scripts/dispatch/adapters/tool/conversus.sh (shipped, not modified, D007 discipline); M012 (wiki Giscus threading + wiki-giscus-remap.sh pathname-keyed thread mapping — preflight resolved 2026-04-23 per D023); M013 (GitHub comment surfaces + UAT ingestion path + orchestrator-id marker convention); M019 Tier 1 emitter (execution-log.jsonl append path); M016/M021 zero-prompt baseline (SC-7 gate); M026 (conversus-OSS migration — edition-aware adapter shipped mid-M014, inherited without modification); gh CLI operator-installed + authenticated for FR-8 comment fetch"
  - milestone-summaries :: .orchestrator/milestones/M014/M014-SUMMARY.md:36:**P03 (170m, 5 tasks, 14-gate suite)** closed the original M014 mission — wiki Giscus + GitHub Issue/PR comments classify into uat-bug / decision-append / spec-amendment / ambiguous; two trivial classes auto-apply above threshold (uat-bug via M013/FR-10 UAT ingestion; decision-append via templated DECISIONS.md row append); spec-amendment ALWAYS queues for human sign-off (CON-5/SC-5 two-tier verification); ambiguous routes through conversus adapter under `--strict` with adapter-missing graceful-degradation to human triage bucket. FR-9 classifier shape pinned to regex/heuristic v1 baseline per D023 with explicit retune trigger (≥30 actioned comments OR ≥20 percent calibration divergence). D023 also retired the synthetic P99 dependency placeholder from the roadmap by replacing it with an explicit Preflight note covering the M012 wiki deploy (resolved 2026-04-23) and the SC-16 inbox-dogfood data capture (relaxed to best-available signal).
  - milestone-summaries :: .orchestrator/milestones/M018/M018-SUMMARY.md:18:  - "Sourceable lib pattern under scripts/lib/ for cross-tier reuse (preservation-check shared by T1/T2/T3); preservation-check failure-passthrough (restore pre-tier capture byte-identical + emit tier_preservation_violation); SHA-256 keyed disposable cache (FR-16 / FR-17) with cache-reuse short-circuit-on-write (mtime preserved); intensity-gate consumed exclusively in build-context.sh via INTENSITY_METADATA_FILE; in-band tier-marker grammar (CON-4) — uniform <!-- compressed:tierN <kvpairs> --> shape; additive emitter schema discipline (CON-5) with rollup column-index contract pinned over time; MEM004 emitter-internal carve-out for awk pipes inside dispatch / diagnostic / rollup helpers; LLM-call shim isolates runtime-portability surface (operator-binary | backend-default | exit-1 ladder); RUNTIME-ASSUMPTIONS.md registry pattern for M009 launch-gate audit consumption; hermetic-fixture-with-knowledge-snapshot parity-runner pattern; awk-driven entry-boundary detection in bash 3.2 stream filters; documented-divergence carve-out (verifier asserts documentation discipline rather than suppressing observations)"
  - milestone-summaries :: .orchestrator/milestones/M018/M018-SUMMARY.md:201:  `claude-code-claude` → `exit 1`) means without an operator-installed
  - milestone-summaries :: .orchestrator/milestones/M020/M020-SUMMARY.md:16:  - "D024,AD-1,AD-2,AD-3,AD-4,AD-5,AD-6,AD-7,AD-8,FR-1,FR-2,FR-3,FR-4,FR-5,FR-6,FR-7,FR-8,FR-9,FR-10,CON-1,CON-2,CON-3,CON-4,CON-5,THREAT-006,THREAT-007,SC-1,SC-2,SC-3,SC-4,SC-5,SC-7,SC-8,US-1,US-2,US-3,US-4,US-5,OQ-1,OQ-2,OQ-3,OQ-4"
  - milestone-summaries :: .orchestrator/milestones/M020/M020-SUMMARY.md:36:P04 (review queue) extended `scripts/knowledge/compute-staleness.sh` in place with `--review-queue` + `--knowledge-root <path>` flags (legacy invocations preserved byte-equivalent), and extended `scripts/orchestrator/status.sh` with a `Review Queue:` section emitted after the per-milestone enumeration. The renderer subprocess-invokes the helper and is failure-tolerant via tempdir + RETURN trap + four guard branches (rc!=0, EMPTY, empty-output, malformed-line) each emitting `Review Queue: unavailable` + one-line stderr diagnostic, with `status.sh` exit always 0. Stale flag resolves at default 14 days (OQ-1) with preferences override; topic field whitespace-collapsed; age uses `created_at:` then `last_verified:` fallback. The integration test adopts the shadow-repo + wrapper-helper pattern so the live cluster.sh + jaccard.sh + frontmatter.sh + staleness.sh pipeline runs end-to-end against tempdir fixtures while leaving live `knowledge/` and `.orchestrator/execution-log.jsonl` untouched.
  - milestone-summaries :: .orchestrator/milestones/M021/M021-SUMMARY.md:36:**P03 — Pre-Bash Hook + Permission Widening**. `scripts/hooks/pre-bash-shape-guard.sh` implements a 10-pattern rewrite/reject matrix (6 deterministic rewrites, 4 hard rejects) via a shared classifier library `scripts/verify/lib/shape-classifier.sh`. `.claude/settings.json` registers the hook as PreToolUse and widens the allow-list for safe read-only shapes (`/var/folders/**`, `bash /tmp/*.sh`, project-relative `tmp/**`, `sed -n`, `head`, `tail`, `stat`). Dispatch payload gained an "Allowed invocation shapes" section via `scripts/dispatch/lib/section-handlers.sh`.
  - milestone-summaries :: .orchestrator/milestones/M025/M025-SUMMARY.md:17:  - "Tier B (user-directed override; confirmed by metrics — 4 US / 11 AS / 10 FR / 1 SDD flow); single-phase milestone (P01). #Q-1 event-mapping policy locked at plan-phase: post_verify -> Stop; before_commit -> PreToolUse + Bash matcher; 4 deferred (before_tasks, after_tasks, before_implement, after_implement) with TODO(M025+) markers. #Q-2 tagging shape locked: inline _orchestrator_managed: true (not sidecar manifest) — self-contained uninstall; CC lenient about unknown keys. FR-9 runtime-scope: Claude Code only; codex + cursor installers + adapters byte-identical (pinned-sha + negative-grep gate). python3-for-awk-fallback deviation from CON-2 spec prose — documented in MEM027 as the pattern's canonical shape. FR-8 byte-identity nuance (canonicalization vs raw baseline): round-trip reversibility gate dual-asserts pinned-sha + structural-equivalence to preserve intent. New-file write path canonicalizes through the same serializer as the merge path so idempotency holds on first+second install."
  - milestone-summaries :: .orchestrator/milestones/M025/M025-SUMMARY.md:37:- **Valid Claude Code hooks schema** — `claude-code.sh --hook-config` emits a real CC `hooks` object with only mapped events (`Stop` for `post_verify`, `PreToolUse` + matcher `Bash` for `before_commit`). Four orchestrator lifecycle events without CC equivalents (`before_tasks`, `after_tasks`, `before_implement`, `after_implement`) are explicit deferrals with `TODO(M025+)` markers. Every inserted hook leaf carries `_orchestrator_managed: true`.
  - milestone-summaries :: .orchestrator/milestones/M025/M025-SUMMARY.md:50:- **Tier B** — User-directed override confirmed by metrics (4 US / 11 AS / 10 FR / 1 SDD flow / bounded scope / no cross-phase coordination). Auto mode deemed overkill; Tier B manual per-phase dispatch kept the human in the loop on `#Q-1` and `#Q-2`.
  - milestone-summaries :: .orchestrator/milestones/M025/M025-SUMMARY.md:51:- **#Q-1 event-mapping policy** (locked at plan-phase) — `post_verify` → `Stop`; `before_commit` → `PreToolUse` + matcher `Bash`; four orchestrator events deferred with `TODO(M025+)` markers. Candidate revisit if Claude Code gains task-start / task-end / implement-start / implement-end events.
  - milestone-summaries :: .orchestrator/milestones/M025/M025-SUMMARY.md:9:  - "install-claude-code.sh merges into existing ~/.claude/settings.json via scripts/util/settings-merge.sh (python3 JSON engine, jq-optional, temp-file-then-rename, inline _orchestrator_managed tagging with cascade-cleanup on --uninstall); claude-code.sh runtime adapter --hook-config emits valid CC hooks schema (Stop + PreToolUse/Bash mapping, 4 TODO(M025+) deferrals); tests/fixtures/m025-p01/ pinned fixture tree; 10-gate P01 phase-suite (hook-schema + merge-preservation + coexistence + idempotency + uninstall-reversibility + runtime-scope-guard + bash32-compat + docs + knowledge-entries + recent-changes); references/installation.md Uninstall section + references/hooks.md Claude Code Event Mapping table; CHANGELOG.md [0.9.1] entry; knowledge/lessons/MEM026.md (M013/P04/T04 regression lesson) + knowledge/patterns/MEM027.md (merge-not-overwrite user-scope config pattern)"
  - milestone-summaries :: .orchestrator/milestones/M026/M026-SUMMARY.md:10:  - "conversus binary installed at ~/Sites/conversus-oss (OSS) and/or ~/Sites/conversus (paid); pipx venv at ~/.local/pipx/venvs/conversus/ for metadata-probe fallback; dual-write helper scripts/util/dual-write-runtime-md.sh (M014/P01); M011/P07 reusable adapter shape"
  - milestone-summaries :: .orchestrator/milestones/M028/M028-SUMMARY.md:10:  - "M021 shape-classifier surface (scripts/verify/lib/shape-classifier.sh git SHA 12fcd98) + M021 21-entry prompt corpus + M021 pre-bash-shape-guard.sh hook + M021 ANTIPATTERNS.md AP-001..AP-009 baseline; M025 _orchestrator_managed:true uninstall-cascade invariant + M025 install/uninstall reversibility contract; M008 lifecycle hook enrollment (after-verify-sync + before-commit); Claude Code runtime adapter --hook-config emission contract; M018-close operator settings.json snapshot (sanitized to tests/fixtures/m028-pre-repair-snapshot.json); auto-loop --step=V mechanical verification resolver"
  - milestone-summaries :: .orchestrator/milestones/M028/M028-SUMMARY.md:12:  - "M030 (adaptive model selection — next pre-launch milestone unblocked by M028 hook stability); every consumer project running orchestrator:auto in any non-orchestrator-repo CLAUDE_PROJECT_DIR context (Finding A blast radius now closed end-to-end); future shape-class additions (AP-015+ — closed pattern-set discipline preserves the M028 evidence floor; new evidence required for new AP-IDs); future investigation-pattern wrappers (defensive flag-refusal + self-relative-wrapper fixture + three-surface documentation discipline are reusable patterns); future close-out regression gates (sub-gate-sequencing-with-tmp-log + non-short-circuit semantics + per-sub-gate clean verifier shape are reusable)"
  - milestone-summaries :: .orchestrator/milestones/M028/M028-SUMMARY.md:16:  - "collapse-decision pinned to full-5-phase via per-screenshot causal trace (M=0 of N=7 threshold not met) — hook portability alone resolves zero events because the four B-family screenshots all carry existing-classifier verdict allow; AP-014 ordered BEFORE the existing AP-009 _sc_count_top_level_stages > 2 check (load-bearing for SC-6); AP-010..AP-013 ordered AFTER the four M021 reject checks (more-general rules fire first); CON-5 one-level body-descent via OPAQUE placeholder substitution (avoids unbounded recursion; bash 3.2 flat); CON-7 strict-superset preserved by case-extension discipline (new arms between existing M021 baseline arms and catch-all default); M025 baseline assertions 5+6 (orchestrator-post-verify / orchestrator-before-commit bare names) updated to absolute-path contract in lockstep with adapter rewrite (verifier-as-contract-test, not immutable artifact); per-leaf tuple-keyed dedup with single-walk target-key collection (empty post-dedup wrappers skipped to keep settings.json byte-stable); wrapper-OR-leaf managed-detection for repair path (preserves both pre-T02 wrapper-level and post-T02 leaf-level flagging shapes); strict-tuple match with extra-key guard for orphan removal (preserves user-authored entries that happen to share a tuple but add extra fields); pinned-SHA-in-comment as informational drift signal (hard gate stays byte-equality; pin is human audit trail); test-command stability across classifier evolution (bare-form compound chains over sh-c-body-wrapped chains); permanent in-tree consumer fixture (CON-10 — checked into version control NOT generated at test time); shape-pattern comparison via grep -cE regex over literal-bytes diff (HOME-agnostic shape contract); summary-contract preservation under additive verifier expansion (clamp pass_count at total=7 over bumping to 8); defensive flag-refusal as wrapper contract (node-eval.sh refuses -e/-p as load-bearing defense for AP-012); AP-014 wrapper choice for AP-011 (read-range.sh as diagnostic anchor; actual remedy is operator-command-shape change documented in ANTIPATTERNS.md); helper-function carve-out codified at top-of-file as canonical AD-19 verifier-authoring shape (function bodies are NOT scanned by AP-009 inline-shape classifier in production)"
  - milestone-summaries :: .orchestrator/milestones/M028/M028-SUMMARY.md:28:M028 (autonomous hardening v3) closed the gap between M021's shape-guard infrastructure and the actual autonomous-run experience in any consumer project. Seven findings (A hook portability, B four new shape classes, C investigation-compound preservation, D missing cleanup wrapper, E missing investigation wrappers, F lifecycle-script PATH lookup, G xargs-sh-c-compound-body body descent) closed end-to-end across five phases. The pre-launch slice that lets `orchestrator:auto` run cleanly inside any downstream project, not just the orchestrator repo.
  - milestone-summaries :: .orchestrator/milestones/M028/M028-SUMMARY.md:32:P02 (Findings A + F — hook portability + adapter+installer dedup, 300m) retired `CLAUDE_PROJECT_DIR` from the shape-guard hook in favor of `BASH_SOURCE`-derived two-step fallback (install-side sibling + in-tree dev) with `pwd -P` symlink resolution; rewrote the Claude Code adapter `--hook-config` to emit absolute `bash <abs-path>/<name>.sh` commands for three leaves with `_orchestrator_managed: true` per leaf; added installer stage 2.5 staging a 5-file payload (4 scripts + MANIFEST) under `${HOME}/.claude/orchestrator-hooks/` with MANIFEST-driven uninstall; promoted settings-merge dedup key from M025's command-only to a `(event, matcher, command)` tuple via new `collect_managed_keys()` (closes the operator's M018-close 5-Stop-dupes / 7-PreToolUse-dupes regression); added installer `--repair` mode with `--repair --dry-run` and companion `settings-merge.sh repair` walking `~/.claude/settings.json` to remove flag-less M025 orphans against a hard-coded `ORPHAN_TUPLES` table; extended M025 reversibility via the four-snapshot pinned-SHA `install-roundtrip.sh` ladder (sha0 pre-install, sha1+sha2 post-install pair, sha3 post-uninstall; sha1==sha2 idempotency; sha0==sha3 reversibility extended); authored permissive no-op `scripts/lifecycle/before-commit.sh` shim closing a pre-existing M008 packaging gap. Patterns: BASH_SOURCE self-location with two-step fallback, shape-classifier-aware resolution-block authoring (every introduced line empirically validated against `classify_command` before commit), per-leaf tuple-keyed dedup with single-walk target-key collection, wrapper-OR-leaf managed-detection helper, strict-tuple match with extra-key guard, idempotency-by-construction verifier, helper-function carve-out for AD-19 multi-step computations (function bodies are NOT scanned by AP-009 inline-shape classifier — codified at top-of-file), reversibility-normalization at the installer layer (file-absent for logically-empty JSON), pinned-SHA-in-comment as informational drift signal, snapshot-ladder shape for byte-equality gates.
  - milestone-summaries :: .orchestrator/milestones/M028/M028-SUMMARY.md:4:id: "M028"
  - milestone-summaries :: .orchestrator/milestones/M028/M028-SUMMARY.md:6:milestone: "M028"
  - milestone-summaries :: .orchestrator/milestones/M028/M028-SUMMARY.md:8:  - "hook portability across consumer projects (Finding A — BASH_SOURCE self-location + adapter absolute-path emission + installer-staged ~/.claude/orchestrator-hooks/ runtime-stable payload); lifecycle-script PATH lookup closure (Finding F — bare-name hooks retired in favor of absolute bash <abs-path>.sh emission); five new shape classes AP-010 through AP-014 (cmd-sub-in-pattern; quoted-arg-newline-hash; multiline-quoted-script; unquoted-brace-glob; xargs-sh-c-compound-body) with CON-5 one-level body-descent; four investigation-pattern wrappers under scripts/util/ (grep-files.sh; cleanup-stale-results.sh; node-eval.sh; peek-files.sh) retiring the screenshot investigation shapes; install-side dedup via (event,matcher,command) tuple closing the M018-close 5-Stop / 7-PreToolUse-dupes regression; install-roundtrip pinned-SHA gate extending M025 reversibility; permanent in-tree consumer fixture at tests/fixtures/downstream-project/ as CON-10 contract anchor for adapter --hook-config emission shape; M028 close-out regression gate sequencing 4 sub-gates (install-roundtrip + corpus-replay-27-entry + per-finding-run-all + downstream-fixture-replay); 8 per-finding verifiers (finding-A through finding-G plus G-classifier and G-wrapper) rolling up to canonical 'M028: 7/7 findings verified (skipped: 0,failed: 0)' summary line; 27-entry replay harness tests/run-prompt-corpus-replay.sh with WOULD_PROMPT=0/27 canonical line; CON-7 strict-superset preserved (every M021 baseline verdict on every M021 corpus entry unchanged); three-surface investigation-pattern documentation (commands/dispatch.md table + templates/dispatch-prompt.md bullets + ANTIPATTERNS.md catalog cross-ref)"
  - milestone-summaries :: .orchestrator/milestones/M032/M032-SUMMARY.md:18:  - "Managed project-asset distribution (project_assets manifest schema + read-project-assets.sh shared reader + install-asset-mode.sh per-mode handler + install-collision-check.sh dual-oracle hierarchy with MIT-006 bootstrapping carve-out + .orchestrator/installed-files.txt tab-delimited column-1 invariant); cross-installer parity invariant (project-asset staging block byte-identical across install-{claude-code,codex,cursor}.sh; differences confined to surrounding runtime-specific context); fixture-staging via mktemp -d + cp -R + git init + git remote add origin (committed fixture stays immutable; runtime .git/ materialized at test time); self-application detection (REPO_ROOT == PROJECT_DIR) skips bundle staging in dogfooding loops; field-line rewrite (^key:.* -> key: "<value>") idempotent against both placeholder and resolved starting states; sequential-atomicity dispatch (writes-first-then-secondary; secondary failure preserves primary outputs and propagates exit code with diagnostic); reader-only knowledge-adapter boundary (M020 retains schema-authority over on-disk knowledge/<category>/MEM*.md; M032 synthesizes records on-the-fly for build-context.sh consumption); --kind=<glossary|mem|reference> extensible argument-parsing seam at the adapter front; safe-default-no-terms fallback fires BEFORE any I/O on the budget-conscious Quick path (MIT-010); paired-milestone seam-script convention at tests/paired-m032-m033/seam-{A,B,C}.sh shared by both M032 and M033 verifier suites; <TOOL>_<HELPER>_STUB=<1|fail> failure-injection envelope per M026/MEM030; verifier-contract-over-verifier-skeleton course-correction (when plan-stated expectation conflicts with on-disk reality, ship the contract by manually preparing inputs the contract expects or adopting the surrounding-phase pattern); four-step ordered remote-state mutation with read-before-write guard + structured JSONL audit-trail BEFORE side-effect-print (Constitution VI applied to remote-state mutations); per-step parallel-scalar mutation flags as bash 3.2 substitute for declare -A object arrays; gate-0 cwd-vs-canonical-source sanity gate as Finding J counter-pattern; two-region marker split (auto-* regenerated wholly + custom-* preserved verbatim) with one-time legacy-migration branch (silent migration is precisely the failure mode MIT-005 was authored to prevent); count_between_markers helper (skips blanks+comments) + extract_between_markers helper (preserves all bytes); AS-3 self-healing (regenerator re-creates deleted marker pair at standard slot); three-category exit semantics for live-network acceptance (0=pass / 77=POSIX-skip per MIT-001 / non-zero=fail) -- distinguishes test-environment-not-ready from test-failed; trap-EXIT cleanup pattern + explicit-cleanup-then-trap-disarm pattern; throwaway-fixture protocol (timestamp-prefix naming + gh repo create --private --add-readme + trap-EXIT teardown + four no-orphan-state invariants); progressive-opt-in --with-<feature> convention codified at references/installation.md as project-wide spec (default-off + independently composable + reversibility invariants + future-flag commitments); deferred-cleanup-recovery escape hatch documented in throwaway-fixture-protocol.md; first-run-captures-HEAD-as-baseline + regex-allowlist + regex-denylist twin-check scope-guard pattern (committed-history-only diff per the P01/P02/P03 patterns-established lesson); thin-aggregator phase-suite chains existing verifiers without adding new logic per AD-19 single-script-file shape (literal sequence NOT a for-loop); FR/SC/AD tag-prefix in gate names preserved for grep-able diagnostics; three-source-family scanner extension (proposals:* + extra:<dirname> + knowledge-flat) with default-on/default-off opt-in flags; YAML frontmatter stage-badge derivation with unknown fallback per US-7 AS-1 no-exclusion invariant; single-script-file FR-20 build-time decorator stub with regex-pattern multi-class match + glossary lookup + first-occurrence-titled subsequent-link-only rendering + missing-glossary fallback; three-orthogonal-invariant single-acceptance-script pattern (Constitution VIII + MIT-002 + MIT-008 stitched via three say_pass/say_fail assertion groups under one trap-EXIT envelope); three-category battery aggregator over MIT-001 wrapped around the M030/M031 run_sc() helper; <MILESTONE>_ACCEPTANCE_BATTERY_DRY=1 test-only dry-mode escape hatch in acceptance battery runners; shape verifier with embedded runtime check (beyond grep-for-token assertions); fixture-completeness precondition pattern (SC-5 emits exit 77 SKIP_REASON when fixture lacks scripts/wiki/wiki-deploy.sh -- environmental precondition not test failure); milestone-close evidence-ledger convention (mirrors M030/M031); conditional M032-VALIDATED marker per MIT-001 + SC-14 (marker MUST NOT be created at skip=1 unless signed-attestation block); in-flight repair convention extended across phases (P02/T02 P01-verifier relaxation + P03/T05 verifier-contract-over-verifier-skeleton + P04/T05 milestone-close ceremony in-flight repairs to sibling-phase verifier shape drift surfaced by SC-12 battery)"
  - milestone-summaries :: .orchestrator/milestones/M032/M032-SUMMARY.md:46:- **First-run-captures-HEAD-as-baseline + regex-allowlist + regex-denylist twin-check scope-guard pattern** — Single-pass `git diff --name-only baseline_ref HEAD` iteration; each diff path checked against `ALLOWED_RE` for in-scope-membership and against `DENIED_RE` for SC-13 violation surface. Committed-history-only diff per the P01/P02/P03 patterns-established lesson — working-tree noise is the failure mode that produces sibling-phase false-positives in dogfood loops.
  - milestone-summaries :: .orchestrator/milestones/M032/M032-SUMMARY.md:59:- **Per-phase scope-guard**: P01 PASS, P02 PASS (3/0 in_scope=13), P03 PASS (4/4 denylist_hits=0), P04 captured baseline + denylist_hits=0 at close.
  - milestone-summaries :: .orchestrator/milestones/M032/M032-SUMMARY.md:8:  - "Project-asset distribution (P01) — packaging/bundle/manifest.yml project_assets schema with read-project-assets.sh shared reader + install-asset-mode.sh per-mode handler (copy + symlink + windows fail-closed) + install-collision-check.sh FR-22 dual-oracle hierarchy (tracking-file + MIT-006 bootstrapping + operator-owned); install-{claude-code,codex,cursor}.sh migrated to project_assets-driven staging with --asset-mode-override flag and cross-installer parity; .orchestrator/installed-files.txt per-asset mode tracking (FR-4 + tab-delimited column-1 invariant); SC-1/SC-2/SC-10 acceptance scripts. Wiki distribution + init integration (P02) — orchestrator:wiki-init command + scripts/lifecycle/wiki-init.sh (FR-5 default-scope canonical implementation) + wiki/mkdocs.yml four-field placeholder amendment + FR-6 self-application loop closed against orchestrator repo + sequential-atomicity dispatch of wiki-init.sh per FR-11/MIT-011; commands/init.md --with-wiki/--with-giscus/--deploy documentation + init-project.sh composition validation + passthrough; wiki/glossary.md US-6 path convention with three populated entries; scripts/wiki/wiki-scan-sources.sh --include-glossary additive flag + scripts/wiki/wiki-generate-nav.sh + scripts/wiki/wiki-generate-stubs.sh top:glossary routing; scripts/knowledge/lookup-mems.sh --kind=glossary READER adapter (M020 schema-authority preserved + M031 Quick/Standard/Full profile contract honored + MIT-010 safe-default-no-terms fallback); SC-3/SC-7 acceptance scripts; tests/paired-m032-m033/seam-{A,B,C}.sh paired-launch contracts shared with M033. Wiki composition layer (P03) — FR-7 Giscus partial templating (wiki/overrides/partials/comments.html dual-template interpolation) + --with-giscus scope on wiki-init.sh; FR-9/MIT-007/MIT-008 --deploy scope on wiki-init.sh (four-step ordered remote-state mutation: gh api PATCH discussions=true -> wiki-deploy.sh -> MIT-007 read-before-write Pages guard -> gh api PUT /pages); FR-10 cwd-vs-repo_url sanity gate-0 on wiki-deploy.sh (Finding J counter-pattern); structured wiki-deploy-mutation NDJSON audit-trail BEFORE side-effect-print (Constitution VI applied to remote-state mutations); M032_DEPLOY_GH_API_STUB[_DIR] + M032_WIKI_DEPLOY_BYPASS_CWD_GATE test-only stub envelopes per M026/MEM030; FR-14 region split on wiki-generate-nav.sh (auto-nav regenerated wholly + custom-nav preserved verbatim + legacy M012-P01 markers recognized only for one-time migration); FR-13 progressive-opt-in --with-<feature> flag pattern documented at references/installation.md (default-off + independently composable + reversibility); AD-7/CON-5 throwaway-fixture-protocol document with timestamp-prefix naming + gh repo create --private --add-readme creation contract + trap-EXIT teardown + four no-orphan-state invariants + recovery-on-partial-failure runbook; SC-4/SC-5/SC-6 acceptance scripts (SC-5 with three-category exit semantics 0/77/non-zero per MIT-001). Scanner extensions + decorator + acceptance battery + milestone close (P04) — FR-17/18/19 scanner extensions on scripts/wiki/wiki-scan-sources.sh (proposals:* records with [stage] badge derivation; extra:<dirname> records driven by wiki.extra_dirs config; knowledge-flat records on .orchestrator/knowledge/*.md flat files); FR-17/18/19 nav rendering in scripts/wiki/wiki-generate-nav.sh under # >>> auto-nav region; FR-20 build-time code-shorthand decorator stub scripts/wiki/wiki-decorate-codes.sh (US-8 P3 surface); SC-8/SC-9/SC-11 acceptance scripts; SC-12 milestone-grain acceptance battery tests/m032-acceptance/run-acceptance-battery.sh (MIT-001 three-category exit semantics); --with-wiki no-op in-flight repair on scripts/lifecycle/wiki-init.sh (FR-11 passthrough symmetry); milestone-close ceremony surfaces (M032-SUMMARY.md + conditional M032-VALIDATED marker per MIT-001 + SC-14 + milestone-grain unit_close JSONL record + M032-ACCEPTANCE-EVIDENCE.md per the M030/M031 evidence-ledger convention); twenty-eight tools/verify/m032-p0[1-4]-* shape verifiers + four phase-suite aggregators + four scope-guards + four baseline-ref fixtures."
  - milestone-summaries :: (capped at 5 hits — more may exist)

### Q2 — KEPT

- **question**: - #Q-2 (yes-broadening-resolution): Does `--yes` broadening warrant a distinct flag alias / longer deprecation window vs an explicit notice?
- **disposition**: kept (operator decision at plan-phase per spec; kept at discuss gate 2026-07-02)
- **terms** (12): Q-2, broadening, resolution, warrant, distinct, flag, alias, longer, deprecation, window, explicit, notice
- **hits**:
  - constitution :: .orchestrator/memory/constitution.md:12:  enforcement; Invariant 2 X-distinctness sentence recorded.
  - constitution :: .orchestrator/memory/constitution.md:161:Every piece of work MUST go through an explicit design step, no
  - constitution :: .orchestrator/memory/constitution.md:169:  is a red flag, not a valid exemption.
  - constitution :: .orchestrator/memory/constitution.md:198:  dispatch. This payload is explicitly defined, not implicitly
  - constitution :: .orchestrator/memory/constitution.md:271:scope boundary is declared explicitly in
  - constitution :: .orchestrator/memory/constitution.md:315:- Configuration: `orchestrator-config.yml` with specificity resolution
  - constitution :: .orchestrator/memory/constitution.md:398:- If you notice unrelated issues: record them in the task summary
  - constitution :: .orchestrator/memory/constitution.md:415:- Every task MUST fit in one context window. This is the "iron rule."
  - constitution :: .orchestrator/memory/constitution.md:459:    explicitly in the smoke test fixture's documented preconditions
  - constitution :: .orchestrator/memory/constitution.md:484:  distinctness) are not re-stated here — amendment authors follow
  - constitution :: .orchestrator/memory/constitution.md:491:  1. Explicit version bump following semantic versioning:
  - constitution :: .orchestrator/memory/constitution.md:76:      substantive distinctness findings to follow-on amendment).
  - constitution :: (capped at 5 hits — more may exist)
  - decisions :: .orchestrator/DECISIONS.md:113:- **Choice**: Revised sequence: **M011 → M012 → M013 → M014 → M019 → M018 → M009 → M010** (M019 inserted by D009). M018 adopts caveman-style (github.com/JuliusBrussee/caveman) token compression as a first-class pipeline stage aligned with Constitution Principle I (Context Minimization). Proposed phase outline (subject to detailed planning closer to kickoff): P01 compression grammar (what's safe to compress per artifact class; preserve frontmatter, code fences, paths, MEM IDs, commands, URLs), P02 dispatch compressor (optional filter in `scripts/dispatch/build-context.sh`, emits `payload.compressed.md` beside original), P03 intensity mapping (Quick→ultra, Standard→full, Full→lite/off via `scripts/engine/intensity-gate.sh`), P04 memory compression (opt-in `CLAUDE.compressed.md` / `KNOWLEDGE.compressed.md` with `.original` siblings), P05 agent output profile (inject terseness directive into payload prefix), P06 eval harness (3-arm port of caveman's evals against orchestrator payloads, gate on accuracy parity), P07 multi-runtime parity (Claude Code / Codex / Cursor). Differentiation vs. installing caveman as-is: roundtrip-safe by policy (Principle VI — originals authoritative, compress only at dispatch/load boundaries), grammar-aware preservation of orchestrator artifact structure, intensity-bound (not always-on), explicit opt-out surfaces (constitution, spec.md, PR descriptions, user-visible docs), and telemetry integrated with existing cost tracking so savings are measurable per dispatch.
  - decisions :: .orchestrator/DECISIONS.md:141:- **Choice**: Replace the D008 phase outline's "P02 dispatch compressor (optional filter...)" framing with a tier-ladder model informed by Article 3 (Rohit's Claude Code harness teardown) and logged in `.orchestrator/scratch/articles-synthesis-2026-04-17.md`. Every dispatch runs through tiers in order, cheapest first: **Tier 1 microcompact** (reuse cached tool-call results that haven't changed + persist oversized tool results to disk with `file_path + preview` references — absorbs L6 tool-result budgeting; zero LLM cost); **Tier 2 snip** (head-drop with protected tail; no LLM call); **Tier 3 auto-compact** (summarize via LLM call; existing caveman-style territory); **Tier 4 context collapse** (staged multi-phase compression of tool results → reasoning → sections; feature-flagged). Intensity maps to tier ceiling: Quick→T1–T2 only, Standard→through T3, Full→all tiers available. Revised phase outline: P01 tier grammar + per-tier safety boundaries (what's preservable per tier), P02 Tier 1 microcompact + tool-result budgeting, P03 Tier 2 snip with protected-tail semantics, P04 Tier 3 auto-compact (summarization, intensity-gated), P05 Tier 4 collapse (staged; feature-flagged, off by default), P06 eval harness (3-arm parity tests per tier), P07 multi-runtime parity. D008's original scope envelope (roundtrip-safe by policy, grammar-aware preservation, intensity-bound, explicit opt-out surfaces, telemetry-integrated) carries forward unchanged — only the internal staging model is reframed. Fresh `orchestrator:discuss` at M018 kickoff addresses detailed tier design once M019 Tier 1 data reveals which artifacts dominate token spend. **Conversus integration (locked in per D007 reuse pattern)**: M018/P01 tier-grammar + per-tier-safety-boundary work invokes `scripts/dispatch/adapters/tool/conversus.sh` via `role/red-team` + `domain/security` presets at plan-review time. Rationale: grammar decisions carry parse-regression risk across downstream scripts that grep prose (the risk D008 originally flagged), which is exactly the subjective-quality territory the conversus fidelity-gate adapter earns its cost on. A new preset drops under `templates/conversus-presets/` without adapter modification (same pattern M013/M014 use). Gate is intensity-bound by default (Quick=off, Standard=advisory, Full=blocking); `--no-review` escape available.
  - decisions :: .orchestrator/DECISIONS.md:155:- **Choice**: Context: `.orchestrator/scratch/articles-synthesis-2026-04-17.md` identified a coherent knowledge-layer theme bundling L7 (wikilinks), L8 (explored state), A1 (preferences layer), A2 (candidate→graduate with required rationale), A3 (review queue in status), A4 (AGENTS.md read-order map), A6 (Jaccard clustering in consolidate). Without a logged trigger, the M020 decision drifts. Evaluation rule at M012/P02 close: assess whether M012's spec-wiki implementation ships **(a)** clickable cross-refs from spec pages to `knowledge/**/MEM*.md` entries via shared ID namespace, **(b)** a review/unreviewed state model on generated pages, **(c)** a query/search surface callable from orchestrator dispatches (wiki-query analog). **Decision rule**: ≥2 of 3 land → M020 dissolves into knowledge-tooling maintenance PRs with no milestone commitment (M012 patterns cover enough of the surface); ≤1 of 3 land → promote M020 as a committed milestone positioned between M014 and M018 (before M019 Tier 2/3, so A5 on_failure flagging can draw on its data). **Minimum-viable subset ships regardless of the M020 decision**: L8 (explored state on auto-generated MEM entries) + A2 minimal guard (`scripts/knowledge/graduate.sh --rationale "..."` flips `status: candidate` → `status: graduated`) bundled into a standalone maintenance PR. Rationale: these are small (~30 lines), address the rubber-stamping failure mode directly, and compound M019 Tier 1's measurement value. The *full* A2 workflow (clustering, staging layer, review queue UI, decision history) waits for the M020 decision. **A4 also ships standalone**, before M019/P00, as an independent concern (session-start file-loading manifest for non-Claude-Code runtimes) — not bundled with P00 because P00's scope discipline is "dispatch-facing payload adaptation only."
  - decisions :: .orchestrator/DECISIONS.md:156:- **Revisable**: Yes — criteria can be adjusted if M012 scope shifts significantly before P02. If M013 or M014 scope starts absorbing knowledge-layer work (e.g., comment-automation's classification surface overlaps with clustering), promote the trigger to that milestone's close. If a 4th distinct affordance emerges during M012 planning (e.g., MkDocs + Giscus establishes a graduation workflow), add it to the criteria set.
  - decisions :: .orchestrator/DECISIONS.md:186:M012/P02 D011-EVALUATION.md (.orchestrator/milestones/M012/phases/P02/D011-EVALUATION.md) records the mechanical outcome: 1 of 3 criteria shipped (cross-refs to knowledge/**/MEM*.md entries via shared ID namespace via the include-markdown pipeline; review/unreviewed state model NOT shipped; query/search surface callable from dispatches NOT shipped). D011's decision rule: <=1 of 3 -> promote as committed milestone, positioned between M014 and M019 Tier 2+3 so A5 on_failure flagging can draw on its data. M012's cross-ref affordance (pathname-keyed wiki stubs with include-markdown) is necessary but not sufficient for the full A1/A2/A3/A6 knowledge-layer workflow; M020 ships the remaining review-state + query-surface + clustering surface. Minimum-viable subset (L8 explored state + A2 minimal graduate.sh guard + A4 session-start manifest) still ships independently before M020 per D011 framing; full A2 workflow and review queue UI wait for M020 kickoff.
  - decisions :: .orchestrator/DECISIONS.md:197:- **Choice**: Narrow M013 scope per `specs/023-github-native-integration/conversus/` run: (1) FR-12 handler installation is Claude-Code-only for v1 (CV-3 / RD-2 binding ruling) — Codex CLI + Cursor fall back to `manual` sync at init, non-Claude-Code runtime-adapter work deferred to a future milestone; (2) Minimal Slice (CV-1 / RD-3 binding ruling) pins Phase 1 load-bearing scope to full US-3 + minimal US-1 scaffolding (sidecar config + UAT template install) + US-2 idempotency applied to UAT entries only; (3) M020 sequencing unchanged (RD-1 reclassified) — M013 -> M014 -> M020 per D013; schema authority bounded by MIT-2 narrowing FR-9 to additive-emit-only with chunk-ID pinned to existing `SPEC-*` frontmatter + new Knowledge-Layer Boundary subsection explicitly forbidding review-state / query-surface / clustering; (4) 13 unmitigated P5 risks landed as MIT-1..MIT-13 spec edits (sub-issue fallback Constraint, Constitution Check section, FR-5 three-shape rewrite, FR-6 per-item cache schema, FR-7 sync lock, FR-9 narrowing, FR-12 prose correction + scope, FR-13 `--strict` provenance + 30s timeout + drop intensity-engine coupling, FR-14 re-init via marker search + label-collision preflight, FR-15 dry-run generalized to init+sync, FR-16 rate-limit + auth-expiry detection, FR-17 cost emission, FR-18 cache reconciliation, plus SC-8/SC-9 reconciliation and new SC-12/SC-13 scope + overhead caps); Open Questions #1/#3/#6/#7 closed; new OQ on PAT-vs-App default for M009. Spec status promoted Draft -> Ready-for-discuss.
  - decisions :: .orchestrator/DECISIONS.md:200:Red-blue deliberation with constitution-arbiter binding on three disputes. Spec was pinning scope intentions that had not been converted into Constitution-XIV/XV-bounded commitments; deliberation surfaced 13 concrete unmitigated risks + 3 arbitrated disputes. Arbiter ruled on XIV ("current demonstrable need" governs FR-12 and Minimal Slice) and on XV (narrow FR-9 to minimum schema authority — no sequence re-open). Landing edits pre-discuss means `orchestrator:discuss` operates on the narrowed spec and cannot silently re-expand scope. See `specs/023-github-native-integration/conversus/summary/final.md` + `conversus/arbitration/resolution.md` for full register + rulings.
  - decisions :: .orchestrator/DECISIONS.md:242:Cooperative-mode precedent (distinct from D014's red-blue precedent on M013). Cooperative mode produced bilateral convergence: both agents' largest structural recommendations (milestone split / US-5 collapse) were withdrawn by their own authors on the merits during revision; 5 explicit bilateral convergences; 2 narrow-compromise disputes. Pressure-testing pre-discuss — as with M013 per D014 — means `orchestrator:discuss` operates on the narrowed spec and cannot silently re-expand scope. Synthesis at `specs/024-spec-management-extended/conversus/summary/final.md` is the audit-trail artifact; the deliberation record is untracked/gitignored per conversus convention.
  - decisions :: .orchestrator/DECISIONS.md:256:Roadmap explicitly predicts P02+P04 closing before P03; preflight is operator-gated and cannot be resolved autonomously
  - decisions :: .orchestrator/DECISIONS.md:267:- **Choice**: (1) **Three-pass contract** pinned in `commands/specify.md`: Pass 1 (scaffold) always runs — creates file, populates frontmatter + Input, writes Section Contract `<TODO:` placeholders. Pass 2 (author) drafts the body from Input + DECISIONS.md + constitution + neighboring CLAUDE.md/AGENTS.md Recent Changes; Quick = agent-only no review, Standard = agent + self-review checklist warnings, Full = agent + `speckit.clarify` loop (≤5 load-bearing ambiguity questions, operator-answered, agent-revised). Pass 3 (gate) adversarially reviews authored body via `scripts/dispatch/adapters/tool/conversus.sh gate spec-pressure-test`; Quick = skip, Standard = advisory (findings folded into Open Questions, no BLOCK), Full = strict (BLOCK halts with dispute list + "revise then --amend" message). On Full+PASS, spec status promoted `Draft` → `Ready-for-discuss` + `Last Revised:` line added per M013/M014 precedent. (2) **Intensity resolution order**: project default from `.orchestrator/config.yml` → smell-test escalation via `scripts/engine/intensity-analyze.sh --description <prose>` (can only escalate, never de-escalate) → CLI `--intensity` override (trumps both). One `specify_intensity_resolution` JSONL record emitted at run start. (3) **Gate TODO pre-flight invariant** (universal): `scripts/dispatch/adapters/tool/conversus.sh gate` refuses any artifact containing ≥ `CONVERSUS_GATE_TODO_THRESHOLD` (default 1) `<TODO:` markers, with actionable error pointing at the author pass. Bypass: `CONVERSUS_GATE_SKIP_TODO_CHECK=1` (reserved for tests + intentional-stub preset authoring). Protects M013/M014/M023/M024 + any future gate consumer, not just `orchestrator:specify`. Applied to adapter in `scripts/dispatch/adapters/tool/conversus.sh:150-169` + regression test in `tests/test-conversus-adapter-shim.sh` section 1b. (4) **Reuse over rebuild**: all intensity machinery already exists (`intensity-analyze.sh`, `intensity-override.sh`, `intensity-gate.sh`, `spec-complexity-probe.sh`) — D019 wires them into the specify flow rather than adding parallel infrastructure. (5) **Implementation posture**: command contract is the SSOT (landed 2026-04-23 in `commands/specify.md`); shell implementation in `scripts/specify/specify.sh` lands in the next M014 extended phase. Until then, agents invoking the command execute passes 2 and 3 manually per the doc.
  - decisions :: .orchestrator/DECISIONS.md:268:- **Revisable**: Yes — (a) if Full-intensity dogfooding reveals clarify's ≤5 question cap is too tight or too loose, adjust in a follow-up D-row without re-opening the three-pass contract. (b) If smell-test false-escalation rate is high, tune `intensity-analyze.sh` keyword weights independently — D019 commits to the resolution order, not the keyword set. (c) `CONVERSUS_GATE_TODO_THRESHOLD` default can be raised (threshold-2 to allow one placeholder) if authoring pass legitimately leaves sections marked as deferred-to-plan. (d) If Full+BLOCK exit-code-2 semantics conflict with `orchestrator:auto` loop handling, harmonize in an auto-specific D-row. (e) Implementation shell in `scripts/specify/specify.sh` can adopt the contract incrementally (Quick intensity first, then Standard, then Full's clarify wiring) without invalidating the contract itself.
  - decisions :: .orchestrator/DECISIONS.md:281:- **Choice**: (1) **spec-metrics.sh spec-scoping bug**: `scripts/state/spec-metrics.sh <orch-root>` reads `spec_chunks_present` globally and, if true, returns the first-ingested specs counts regardless of which spec the caller is evaluating. During Task C of this session (evaluate on spec 025), it returned 3/0/10 (story/req/acceptance) from spec 001s chunks rather than the authored spec 025 counts. Workaround applied: fall back to `metrics_source: raw_spec` counts when the target spec has no chunks. Remediation: either (a) accept a `<spec-path>` argument and filter chunks to that specs ID, OR (b) emit a `chunks_cover_spec=<bool>` flag that `orchestrator:evaluate` reads before trusting `spec_chunks_present`. Landed as a plan-phase task in spec 026 M014 shell work or a dedicated M011/M014 follow-up (operator chooses). (2) **`<TODO:` in backticked code gotcha**: when authoring a spec body that refers to the scaffold-placeholder token by name (e.g. "the TODO pre-flight refuses any artifact containing `<TODO:` markers"), the literal `<TODO:` string inside inline code ticks matches the conversus.sh gate pre-flight pattern and causes a false TODO-filled refusal. This session encountered it 6 times in spec 026 before rewording to "scaffold-placeholder markers". Remediation: add a one-line Gotchas entry to `commands/specify.md` naming the escape practice ("when referring to the scaffold-placeholder token in authored prose, use `scaffold-placeholder marker` or escape the angle bracket so the literal `<TODO:` pattern does not appear"). The Pass 2 author prompt template landed under spec 026 FR-3 should carry the same guidance so automated authoring does not reintroduce the footgun. (3) **Scope bound**: D020 captures friction; it does NOT commit to a specific fix location. Fix #1 lives most naturally in `scripts/state/spec-metrics.sh` + `commands/evaluate.md` (evaluate is the sole caller today); fix #2 lives in `commands/specify.md` Gotchas + the spec-026 author prompt template. Neither warrants its own spec — both are bite-sized cleanups executable inside M014 extended or as consolidate-time follow-ups.
  - decisions :: .orchestrator/DECISIONS.md:282:- **Revisable**: Yes — (a) if `orchestrator:ingest` becomes mandatory before `orchestrator:evaluate` as a workflow invariant, fix #1 becomes moot (spec chunks always cover the current spec). (b) if `CONVERSUS_GATE_TODO_THRESHOLD` is raised to 2+ to allow legitimate deferred-to-plan placeholders, fix #2s urgency drops (one accidental match no longer trips refusal). (c) if spec-026s Pass 2 prompt template evolves to post-process authored output for stray TODO patterns, fix #2 becomes a validation-layer concern not an authoring-hygiene concern.
  - decisions :: .orchestrator/DECISIONS.md:302:### Edition-resolution two-tier detection committed as canonical pattern for runtime edition identification { #dr-code-022 }
  - decisions :: .orchestrator/DECISIONS.md:310:- **Revisable**: The decision is the consolidation point for three load-bearing M026 commitments that downstream milestones (M013, M014, M018, M023, M024) need a single auditable reference for. Rather than scattering the rationale across three MEM entries and the M026-SUMMARY (not yet authored), the D-row anchors the cross-references in the existing DECISIONS audit trail. The two-tier detection pattern is reusable beyond conversus (any pip/pipx-installed Python tool with multi-channel publishing); naming the convention now means the next adapter migration can adopt the shape without re-deliberating env-var naming. The `edition_required:` preset-frontmatter contract was deferred to OQ-3 at spec-027 discuss-finalize and chose the minimum-viable shape (single optional field, no auto-detect of CLI-flag-based paid-only signals per NG-6) — D022 commits the decision in the audit trail so future scope-expansion conversations have the rationale in hand. The `FAIL:`-vs-`ERROR:` prefix uniformity rationale lives here because it is a one-line decision (preserve adapter convention; SC-7 regex accommodates) that does not warrant its own MEM entry.
  - decisions :: .orchestrator/DECISIONS.md:312:paid` closed enum (stderr warning + fall-through on bad values), metadata-probe fallback via `pip show conversus` `Home-page:` parsing, short-circuit `edition=unknown reason=stub` under stub mode. Output contract: `edition=` + `reason=` lines on stdout in stable order, warnings on stderr (DC-5). (2) `<TOOL>_EDITION=<value>` env-var name is reserved as the convention for any future OSS-default-with-paid-escape-hatch tool integration in this repo (graduated as MEM030, `knowledge/conventions/MEM030.md`). The paired edition-resolution two-tier-detection pattern is graduated as MEM029 (`knowledge/patterns/MEM029.md`). (3) Preset frontmatter `edition_required: paid` (M026/P03/T01, `scripts/dispatch/adapters/tool/conversus.sh` `_read_preset_edition_required`) refuses paid-only presets on OSS-resolved binaries before any `conversus run` invocation; diagnostic on stderr matches case-insensitive regex `paid-only.*CONVERSUS_EDITION=paid` (SC-7). Diagnostic uses `FAIL:` prefix per the adapter's `_emit_fail` convention rather than the FR-11 literal `ERROR:` opener — body content satisfies the SC-7 regex regardless. (4) CHANGELOG.md records the migration under v0.9.1 heading. (5) Cross-cuts: M013/P04 observability shape unchanged (additive `edition` field per AD-4 adjacency rule); spec-026 M014 shell-impl Pass 3 wiring is the next consumer that may exercise the new resolver under a fresh-install code path.
  - decisions :: .orchestrator/DECISIONS.md:338:- **Revisable**: Yes — (a) if a fourth state proves necessary (e.g. deprecated distinct from archived), open a follow-up D-row extending the closed enum and updating MEM031. (b) If decision_history: length becomes unwieldy (more than 50 records on one entry), compact via a follow-up D-row defining compaction rules (NG-6 currently defers). (c) If consuming milestones discover a needed field, the handshake is: open M020 D-row -> M020 lands schema change -> consuming milestone uses the field. Never bypass this gate.
  - decisions :: .orchestrator/DECISIONS.md:352:- **Revisable**: M020 holds schema authority over knowledge entries (MEM031 / D024); the intake-proposal frontmatter is M024-owned but uses the same closed-enum discipline. This D-row is the M024 schema-evolution record for the intake proposal and does NOT require an M020 D-row update — intake-proposal frontmatter is not a knowledge entry, it is a transient routing artifact under M024's authority. The flag is tracked separately from `design_authored_manually` because the latter is the terminal/persistent state ("operator authored DESIGN.md, ready for approval") while the transient flag tracks the in-between halt window where the operator might or might not return. Encoding the in-between explicitly avoids a stale-state ambiguity where `design_authored_manually: false` could mean either "never started" or "started and abandoned."
  - decisions :: .orchestrator/DECISIONS.md:394:- **Revisable**: Yes — (a) if P00 acceptance fails (emitter coverage parity < 95% across the 20-dispatch sample OR section-level probe reveals < 10% achievable savings per spec acceptance scenario 4), the milestone re-routes through `orchestrator:discuss` for re-scope before P01 starts, at which point the gate is re-run against the re-scoped spec; (b) if a new risk surfaces during P00 implementation that was not in the gate's RISK-1..7 register, open a follow-up D-row and re-gate before proceeding past P01; (c) if MIT-3's US-7 P2 promotion proves insufficient (e.g. eval harness ships but quality regressions still slip through during the Tier 3 deployment window), elevate to a runtime quality gate via follow-up D-row before any further Tier 3 dispatches.
  - decisions :: .orchestrator/DECISIONS.md:435:- **Choice**: All operator-facing commands use the `orchestrator:<cmd>` cohort prefix (already canonical in `CLAUDE.md` and `commands/*.md`). T06 finishes the 4 remaining operational template surfaces that still carry the legacy `speckit:<cmd>` form. Legacy-form references are preserved verbatim in 5 allowlisted files (`commands/migrate.md`, `docs/migrating-from-speckit.md`, `references/RENAME-PLAN.md`, `scripts/verify/m015-p03-helpers/changelog-historical-snapshot.txt`, `scripts/state/namespace-aliases.sh`) as documented historical reference.
  - decisions :: .orchestrator/DECISIONS.md:499:| D003 | M035/P02 | convention | Windows postinstall guard binding (MIT-9 / #Q-G9) | package.json engines + os fields + postinstall uname check | package.json declares engines.node >=14 and os: [darwin, linux] (npm refuses install on win32 with EBADPLATFORM). Belt-and-suspenders: postinstall script also exits non-zero with a clear stderr message on uname -s = MINGW*/CYGWIN*/MSYS*/Windows_NT before invoking install-claude-code.sh. Aligned with #Q-8 deferral: Windows symlink-mode is post-launch M009 territory; until then, Windows is fail-closed at install time. | No |
  - decisions :: (capped at 5 hits — more may exist)
  - knowledge-entries :: (capped at 5 hits — more may exist)
  - knowledge-entries :: knowledge/conventions/MEM020.md:19:Dispatched agents must write `T##-SUMMARY.md` files using `write-summary.sh` — without the summary file, the auto-loop cannot advance to the next task. Include explicit write-summary.sh instructions in dispatch prompts.
  - knowledge-entries :: knowledge/conventions/MEM030.md:23:1. **Path-only escape** — requiring the operator to set `<TOOL>_HOME=/explicit/path/to/paid/build` per invocation. Undiscoverable, easy to forget, and brittle across machines with different install paths.
  - knowledge-entries :: knowledge/conventions/MEM030.md:24:2. **Magic-value escape** — using a generic feature flag like `USE_PAID=1` or `LEGACY_MODE=1`. Doesn't express edition intent, doesn't compose with other build-channel distinctions, and is inconsistent across tools.
  - knowledge-entries :: knowledge/conventions/MEM030.md:31:2. **Values**: closed enum `oss|paid` (or analogous closed enum for non-OSS-vs-paid distinctions like `community|enterprise` or `free|pro`). Enforce the closed enum with a stderr warning on unrecognized values; fall through to the metadata probe (see MEM029) rather than silently accepting.
  - knowledge-entries :: knowledge/conventions/MEM030.md:33:3. **Precedence**: env-var declaration is **primary**. Metadata-probe fallback is secondary. Operator-supplied absolute overrides (`<TOOL>_HOME`) trump both — they're an explicit "use exactly this binary" instruction. Resolver order from highest to lowest precedence: STUB (test-only) → PATH (`command -v`) → `<TOOL>_HOME` → `<TOOL>_EDITION`-aware user-local probe.
  - knowledge-entries :: knowledge/conventions/MEM030.md:35:4. **Diagnostic surface**: the `check` subcommand of the integration's adapter MUST emit `edition=<value> reason=<resolution-tag>` on stdout so the resolved edition is visible to the operator and to telemetry without a separate probe call.
  - knowledge-entries :: knowledge/conventions/MEM030.md:47:- **Env-var-name lint** (advisory): a future `scripts/diagnostics/check-edition-conventions.sh` could grep adapter-tree env-var references and flag any non-`<TOOL>_EDITION`-shaped escape-hatch names.
  - knowledge-entries :: knowledge/lessons/MEM024.md:17:# MEM024: Lib Path Resolution in Task Plans
  - knowledge-entries :: knowledge/lessons/MEM028.md:37:# Correct — explicit base-10
  - knowledge-entries :: knowledge/patterns/MEM001.md:25:Dual argument style: scripts accept both positional subcommands and `--flag` style for human and programmatic use.
  - knowledge-entries :: knowledge/patterns/MEM003.md:23:Empty milestone directory (no `M###-*` files) -> `pre-planning` (distinct from scaffolded).
  - knowledge-entries :: knowledge/patterns/MEM029.md:17:# MEM029: Edition-resolution two-tier detection (env-var primary, metadata-probe fallback)
  - knowledge-entries :: knowledge/patterns/MEM029.md:27:1. **Primary signal — operator declaration via env var**. A `<TOOL>_EDITION=<value>` env var is the operator's declarative signal. The consumer trusts the declaration without further probing. This makes the active edition explicit, audit-trail-visible (env-var is logged with the dispatch), and portable across host-OS and install-method differences. Bad values (typos) emit a single-line stderr warning and fall through to tier 2 — never silently accept a bad declaration.
  - knowledge-entries :: knowledge/patterns/MEM029.md:35:The consumer's edition resolver emits two structured stdout lines per resolution: `edition=<oss|paid|unknown>` and `reason=<env-override|metadata-probe|metadata-probe-failed|stub|home|command-v|fallback>`. Line ordering is the verifiable contract. Warnings (e.g., bad env-var value) go to stderr. The same stdout shape is consumed downstream by JSONL emitters (M026/P02/T02 pattern) and by edition-aware-diagnostic refusal blocks (M026/P03/T01 pattern).
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:11:- **Dual argument style**: Scripts accept both positional subcommands and `--flag` style for human and programmatic use.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:138:- **[milestone:M002]** [2026-04-13] Dispatched agents must write T##-SUMMARY.md files using write-summary.sh — without the summary file, the auto-loop cannot advance to the next task. Include explicit write-summary.sh instructions in dispatch prompts.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:150:- **[milestone:M003]** [2026-04-15] Three-tier root resolution (flag → env var → resolve-root.sh fallback) is the standard CLI contract for orchestrator-root-consuming scripts. Makes scripts work under both ORCHESTRATOR_ROOT env and explicit --root, with config-driven default. Reference: scripts/orchestrator/status.sh.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:160:- **[milestone:M021]** [2026-04-17] MEM004 gate-internals carve-out: AD-19 single-script-file invocation shape constrains only agent-facing tool invocations. Verification-script INTERNALS may use pipes, command substitution, awk, etc. freely. The anti-pattern linter's scope rules (commands/, templates/, scripts/dispatch/lib/, tasks/*-PAYLOAD.md) enforce the distinction.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:162:- **[milestone:M021]** [2026-04-17] Self-recursion guard via env flag: a phase-suite script discovered by its own run-suite.sh glob must set an env flag (e.g. M021_P04_SUITE_ACTIVE=1) and short-circuit nested invocations as no-op PASS. Outer invocation remains authoritative.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:183:- **[milestone:M026]** [2026-04-24] M026: Edition-resolution two-tier detection (env-var primary + metadata-probe fallback) for runtime edition identification under single-venv reality. CONVERSUS_EDITION=oss|paid env var declares active edition; pip show conversus Home-page probe is fallback; stub-mode short-circuit edition=unknown. See knowledge/patterns/MEM029.md and DECISIONS D022.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:186:- **[milestone:M020]** [2026-04-25] Deferred-resolution sentinel pattern for CLI-vs-preferences precedence in in-place script extensions: replace eager default initialization with empty-string sentinel + seen-on-cli flag (set to 1 inside the CLI arm); after the argparse loop and BEFORE existing closed-enum validation, run pref_resolve <key> only when the seen-on-cli flag is 0; net precedence becomes CLI > project preferences > user preferences > built-in default. Existing validation block runs against the resolved value as belt-and-suspenders defence (catches future helper bugs or vocabulary drift without modifying the validator). Stderr from pref_resolve must NOT be redirected to /dev/null because malformed-value WARN diagnostics are an operator-actionable signal.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:187:- **[milestone:M020]** [2026-04-25] Capture-validate-restore for byte-equivalent preservation of upstream-phase deliverables: when a downstream task must regenerate an artifact whose canonical path is owned by an upstream phase (e.g. P05 widening the CON-5 feature vector and re-running validate against a P01-owned report path), copy the original to /tmp BEFORE invoking the regenerator, write the new artifact to its downstream path, then restore the upstream original via /bin/cp (NOT cp — many shells alias cp to cp -i which prompts and breaks the restore). Preserves CON-4 byte-equivalence on the upstream deliverable while letting the downstream phase own a divergent copy.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:188:- **[milestone:M020]** [2026-04-25] Distinct-vocabulary fixture rule for clustering integration tests: when a clustering integration test asserts singleton clusters, every per-entry body must use a wholly disjoint single-token vocabulary (zero token intersection with siblings). Boilerplate scaffolding words (distinct, fixture, body, unique, for, the) leak into the CON-5 v2 feature-vector token set and push pairwise Jaccard above the 0.7 default threshold, causing should-be-singletons to co-cluster spuriously. Repeat each disjoint single token 6+ times per body so the union is large enough to dilute any incidental token overlap from frontmatter fields. Validated by P05/T04 + P04/T04 carry-forward.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:200:- **[milestone:M018]** [2026-04-28] Hermetic-runtime-parity-via-operator-binary-stub: when proving multi-runtime parity for a feature whose surface depends on an external binary (LLM provider, formatter, etc.), ship a deterministic stub at tests/<feature>/_stubs/<name>.sh and inject it via the highest-precedence env var in the production provider-resolution ladder (here: ORCH_TIER3_LLM_BIN). Stub MUST honor the same flag contract the production binary uses so swapping back is a drop-in replacement. Failure-mode stubs (--stub-fail) must record their invocation (append a line to the invocations log) BEFORE returning non-zero, so the parity runner can disambiguate 'never reached' from 'reached and failed'. Pair with knowledge-snapshot + restore between runtime runs when the helper has side-effects on shared project state (KNOWLEDGE-INDEX.md hit_count bumps, etc.) so byte-equality assertions hold without requiring source plumbing changes (Constitution VI: canonical files byte-identical at end of test).
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:210:- **[milestone:M028]** [2026-04-29] Self-conformance verifier scope-narrowing precedent: when a hook's body contains M021-immutable surface that legitimately violates the inline-shape classifier (because the AD-19 helper-function carve-out applies in production), the self-conformance verifier scopes ONLY to lines the current task explicitly introduces. M028/P02/T01 established this with resolution-block-only scoping (lines between '# Locate classifier' and '# Read stdin' markers). M028/P03/T05's finding-G-self-conformance.sh extended to (resolution + reject_lookup case-arm printf bodies). Dispatch case branches stay out of scope per CON-7 even though plan text proposed including them — empirical line-by-line classifier scan informs the scope decision, not plan prose. Bash-parameter-expansion case-arm body extraction (body=${line#*)}; body=${body%;;}) — no sed/awk/external-process needed.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:213:- **[milestone:M031]** [2026-05-01] Per-profile dispatch shape: Quick / Standard / Full each have distinct scope (touched-files-only / module / full) + traversal hops (1 / 2 / unbounded) + Decisions section inclusion (no / no / yes) + glossary slice (touched terms only / touched + adjacent / full) + token budget. Implemented via build-context.sh --profile=quick|standard|full flag (M031/P01) with --meta-out JSON sidecar emitted on every dispatch path (CON-1 invariant — sidecar carries mem_count + total_tokens + profile + compression_applied + snip_applied for downstream consumers like P03 FR-12 stderr summary + P04 AD-19 budget-drift surface).
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:43:- Empty milestone directory (no `M###-*` files) → `pre-planning` (distinct from scaffolded)
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:58:| D007 | convention | `scope-filter.sh` uses scope tags in KNOWLEDGE.md, `When` column in DECISIONS.md, `--depends` flag for dependency resolution | FR-062/FR-063 scoping rules; caller resolves deps from roadmap to avoid duplicating roadmap parsing |
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:67:- Pause detection: file-based flag (`.specify/orchestrator/pause-requested`), not signals
  - knowledge-md :: (capped at 5 hits — more may exist)
  - milestone-summaries :: .orchestrator/milestones/M002/M002-SUMMARY.md:8:  - "Three-temperature knowledge architecture (hot/warm/cold) with KNOWLEDGE-INDEX.md pipe-delimited index, individual detail files at knowledge/{category}/{entry-id}.md, 7 CRUD scripts (create, update, supersede, archive, promote, rebuild-index, scope-filter), 3 shared libraries (staleness.sh, index-utils.sh, detail-utils.sh), lifecycle management (staleness decay, overlap detection, hit count tracking, confidence adjustment), graph traversal (traverse-graph.sh with 1-hop BFS, cycle-safe, max 5 cap), detail resolution (resolve-entries.sh), pre-inlined dispatch with manifest header (FR-111/FR-112/FR-113), execution telemetry pipeline (record-telemetry.sh, aggregate-metrics.sh), model routing configuration (classify-complexity.sh, select-model.sh, routing.yaml), diagnostics command (run-doctor.sh with 4 check scripts)"
  - milestone-summaries :: .orchestrator/milestones/M003/M003-SUMMARY.md:18:  - "adapter-interface extract() contract for source-format pluggability; tiered milestone layout (active/recent/historical/archived); three-tier root resolution (flag→env→resolver) as standard CLI contract; dual-layout state probe for idempotency; warn-but-continue post-pipeline rebuild; progressive-disclosure ADR documentation in command docs; Tier-1 static+behavioral verify-per-truth with single-file AD-19 invocations; skip-gracefully integration-test pattern; strict/warn split for synthetic-vs-live validation; deterministic SQLite fixtures under 50KB (PRAGMA page_size=1024 + hard-coded ISO-8601); .gitignore negation for test fixtures sharing ignored names"
  - milestone-summaries :: .orchestrator/milestones/M008/M008-SUMMARY.md:12:  - "M003 (provides standalone foundation for M003 context management), M009 (future debug/fix protocols inherit adaptive intensity + backend-agnostic dispatch), M010 (dispatch interface is the explicit seam for Managed Agents cloud backend)"
  - milestone-summaries :: .orchestrator/milestones/M008/M008-SUMMARY.md:14:  - "scripts/engine/intensity-analyze.sh,scripts/engine/intensity-recommend.sh,scripts/engine/intensity-gate.sh,scripts/engine/intensity-override.sh,scripts/engine/context-pressure.sh,scripts/dispatch/detect-capabilities.sh,scripts/dispatch/detect-runtime.sh,scripts/dispatch/dispatch-interface.sh,scripts/dispatch/backend-registry.sh,scripts/dispatch/adapters/backend/,scripts/dispatch/adapters/runtime/,scripts/dispatch/adapters/format/,scripts/state/resolve-root.sh,scripts/state/detect-speckit.sh,scripts/state/config-system.sh,scripts/state/namespace-aliases.sh,scripts/migrate/migrate-state.sh,scripts/knowledge/intensity-knowledge.sh,scripts/lifecycle/init-project.sh,scripts/lifecycle/detect-project.sh,scripts/lifecycle/reinit-handler.sh,scripts/lifecycle/check-update.sh,scripts/packaging/generate-skills.sh,scripts/packaging/build-bundle.sh,packaging/SKILL.md,packaging/skills/,packaging/bundle/,packaging/install/,templates/intensity-metadata.md,templates/dispatch-result.md,templates/dispatch-error.md,templates/project-instruction.md,commands/init.md"
  - milestone-summaries :: .orchestrator/milestones/M008/M008-SUMMARY.md:18:  - "Natural-language scope/risk/complexity pattern matching via keyword tables; pipeline composition via --flag file-or-text inputs for testability; intensity-aware threshold adjustment (Quick tightens, Full loosens); structured dispatch result/error schemas; filename-based adapter auto-discovery; atomic metadata rewrite via awk+mktemp+mv with cksum-sentinel verification; thin wrapper pattern with --dry-run mode; pure resolver pattern (read-only, zero side effects); hermetic standalone e2e via mktemp fixtures (never touch real HOME); runtime adapter HOME guard + project-dir guard; skill generation via single-source-of-truth transform with --check drift detection; project introspection via marker-file presence; user-edit preservation via comment-delimited blocks; top-level pipeline delegation (init orchestrates existing scripts); reinit-with-preservation via awk field-level surgery; wall-clock budget enforcement + hermetic-only static gate."
  - milestone-summaries :: .orchestrator/milestones/M008/M008-SUMMARY.md:25:  - "Per-phase observability surfaces rolled up: intensity recommendation stdout (Quick/Standard/Full + confidence + reasoning); dispatch-interface.sh stdout (dispatch-result) + stderr (dispatch-error with distinct exit codes 2-6); intensity-gate.sh stdout (execute_substeps + skip_substeps); detect-runtime.sh stdout (runtime + confidence); runtime adapter --probe/--hook-config/--dry-run stdout; check-update.sh stdout (installed_version + latest_version + update_available + update_instructions); init-project.sh SUMMARY: line with installed skills count + paths"
  - milestone-summaries :: .orchestrator/milestones/M008/M008-SUMMARY.md:28:M008 transformed the orchestrator from a spec-kit extension into a standalone multi-runtime extension with adaptive process intensity. Across 7 phases (P01-P07), delivered: (1) Adaptive Intensity Engine — analyzes task descriptions along scope/risk/complexity axes and recommends Quick/Standard/Full intensity with environment capability awareness. (2) Backend-Agnostic Dispatch Interface — uniform entry point with filename-based adapter routing, two local backends (Claude Code Agent tool + Codex CLI SDK), structured result/error schemas with distinct exit codes. The explicit seam for M010's cloud backends. (3) Intensity-Aware Pipeline Scaling — central 7×3 stage×intensity matrix wires intensity awareness into every pipeline stage (discuss, research, plan-phase, dispatch, verify, knowledge, auto) with atomic mid-workflow override. (4) State & Namespace Independence — 5-rule root resolver, unified config, hard migration tool, surgical derive-phase refactor, namespace aliases. Full workflow runs standalone without spec-kit (SC-004 validated). (5) Runtime & Format Adapters — auto-detection + 3 runtime adapters (claude-code/codex/cursor) with HOME/project-dir guards + 2 format adapters (native round-trip, spec-kit one-directional read). (6) Multi-Runtime Packaging — open-standard SKILL.md format, 12 skills generated from commands, installable bundle, 3 runtime installers delegating to adapters, offline-safe version checker. (7) Init, Onboarding & Spec-Kit Bridge — orchestrator:init detects project (10 languages + 9 frameworks + 6 CI systems + 9 tools), generates config + runtime-appropriate instruction file (CLAUDE.md/AGENTS.md/.cursor/rules), installs skills. Reinit preserves user-edited custom blocks via comment markers. Full onboarding completes in ~1 second wall-clock (SC-005 validated with 2 orders of magnitude margin vs 5min target). Cross-cutting patterns: hermetic-first testing (mktemp fixtures, never touch real HOME), comment-aware Bash 3.2 compat, filename-based auto-discovery mirrored from P02 through P05/P06, thin delegation pattern, user-edit preservation via comment-delimited blocks. M008 is the foundation for M009 (debug protocols) and M010 (cloud dispatch via Managed Agents).
  - milestone-summaries :: .orchestrator/milestones/M008/M008-SUMMARY.md:8:  - "Standalone multi-runtime orchestrator with adaptive intensity: intensity engine (scope/risk/complexity analyzer + recommendation engine + metadata schema + context-pressure evaluator), uniform backend-agnostic dispatch interface (filename-based adapter routing with local-agent + local-codex backends), intensity-aware pipeline scaling (stage-level gate + override + knowledge wrapper across 5 command docs), state/namespace independence (resolve-root + detect-speckit + unified config + migration tool + namespace aliases), runtime adapters (Claude Code + Codex + Cursor with HOME/project-dir guards), format adapters (native round-trip + spec-kit one-directional read), multi-runtime packaging (SKILL.md spec + 12 skills + bundle + 3 installers + offline-safe version check), and onboarding (detect-project + project-instruction template + init-project + reinit-handler with user-edit preservation)."
  - milestone-summaries :: .orchestrator/milestones/M011/M011-SUMMARY.md:12:  - "commands/evaluate.md chunks-first wiring, commands/roadmap.md intensity-aware wiring, commands/ingest.md 6-step pre-chunker pipeline, scripts/engine/intensity-gate.sh new roadmap + ingest stages, scripts/dispatch/scope-filter.sh spec/* resolution, scripts/state/spec-metrics.sh + spec-story-graph.sh helpers, M013/M014 Conversus gate reuse via adapter, all downstream dispatch payloads benefit from scope-filtered spec chunks"
  - milestone-summaries :: .orchestrator/milestones/M011/M011-SUMMARY.md:28:M011 (Spec Management) delivers the "paste a spec, get a roadmap" value proposition end-to-end. Seven phases took the orchestrator from an opaque-spec-file consumer to a full ingest→chunk→scope-filter→normalize→fidelity-gate pipeline that treats specs as structured knowledge on equal footing with the existing three-temperature knowledge model (M002/M007). P01–P03 built the deterministic chunker (spec directory tree with six subcategories, SPEC-prefixed natural IDs, nested-directory indexing, create-entry.sh SPEC- namespace support, full markdown→chunk classifier with classify+dispatch decision layer, versioned-ID supersession chains, REMOVED detection, phase-impact REVIEW emission). P04 wired scope-filter.sh to resolve `spec/*` scope tags (graph-mode + spec-scope-tags mode), letting build-context.sh deliver scope-filtered spec chunks in dispatch payloads per Constitution Principle I. P05 wired chunks-first evaluation and intensity-aware roadmap generation into commands/evaluate.md and commands/roadmap.md (spec-metrics.sh counts non-superseded chunks; spec-story-graph.sh emits directional story-to-story depends_on edges via bidirectional-traverse + directional-frontmatter-filter composition; intensity-gate.sh roadmap stage with Quick/Standard/Full modes). P06 delivered the user-facing commands/ingest.md, four doc-shape verify scripts, a full sandbox E2E gate script, a timing harness (<60s on representative specs), and dogfood evidence against specs/016-autonomous-hardening/spec.md (20 chunks, 7s elapsed). P07 closed with format-agnostic intake and a reusable Conversus fidelity gate: detect-spec-shape.sh probe classifier, normalize-spec.sh thin wrapper dispatching through dispatch-interface.sh (no hardcoded LLM calls), templates/spec-normalizer-prompt.md, a reusable Conversus adapter (scripts/dispatch/adapters/tool/conversus.sh with check/gate/parse-verdict subcommands, graceful degradation, CONVERSUS_STUB escape, three-value exit contract 0/1/2), commands/conversus-gate.md designed for M013/M014 reuse with different presets, templates/conversus-presets/normalize-fidelity.yml, templates/gate-result.md, and integration into commands/ingest.md as a pre-chunker 6-step pipeline (detect-shape → normalize → resolve-policy → fidelity-gate → chunker → rebuild) gated by a new `ingest` intensity stage (Quick=normalize; Standard/Full=normalize+fidelity-gate). A 222-line user guide (docs/ingesting-arbitrary-specs.md) covers flag semantics, BLOCK resolution, stub modes, and preset extension.
  - milestone-summaries :: .orchestrator/milestones/M011/M011-SUMMARY.md:32:Cross-cutting patterns established: decision-layer wrapper around creation primitives (P03); chain-walking next-version supersession (P03); intensity-gate stage registration via minimal case-row insertion (P05, P07); chunks-first with raw-spec fallback in command docs (P05); bidirectional-traverse + directional-frontmatter-filter for typed edges over generic graphs (P05); `grep -Fq -- "$tok"` end-of-options idiom for --flag-shaped tokens under BSD grep (P06); sandboxed PROJECT_ROOT=$(mktemp -d) + EXIT-trap for pipeline tests (P06, P07); evidence-present token gate (file-exists + token-contains) as standard dogfood verification shape (P06, P07); per-phase preserve-references regression contract (P06, P07); `_STUB=1` env-var escape hatches as first-class deterministic-testing contracts (P07 NORMALIZER_STUB + CONVERSUS_STUB); three-value exit contract (0/1/2) for gate-shaped commands where BLOCK is distinct from error (P07); dispatch-interface.sh as uniform LLM-agent-dispatch boundary (no bash HTTP calls, P07).
  - milestone-summaries :: .orchestrator/milestones/M011/M011-SUMMARY.md:8:  - "End-to-end spec management: knowledge/spec/ six-subcategory tree, SPEC-prefixed natural IDs, nested-directory indexing, deterministic markdown chunker with classify+dispatch decision layer, versioned-ID supersession chains, REMOVED detection pass, phase-impact REVIEW emission, scope-filter.sh spec/* scope tag resolution, spec-metrics.sh non-superseded chunk counts, spec-story-graph.sh directional depends_on edges, intensity-gate.sh roadmap + ingest stages, commands/evaluate.md + commands/roadmap.md + commands/ingest.md + commands/conversus-gate.md user-facing docs, detect-spec-shape.sh probe classifier, normalize-spec.sh wrapper (via dispatch-interface.sh), templates/spec-normalizer-prompt.md, reusable Conversus adapter (scripts/dispatch/adapters/tool/conversus.sh check/gate/parse-verdict with graceful degradation + STUB escape), templates/conversus-presets/normalize-fidelity.yml, templates/gate-result.md, docs/ingesting-arbitrary-specs.md user guide, 45+ m011-*.sh verify scripts, dogfood evidence sets under phases P06/P07/evidence/"
  - milestone-summaries :: .orchestrator/milestones/M012/M012-SUMMARY.md:109:- **D011 consequence — M020 promotion** — per `.orchestrator/DECISIONS.md` D011 trigger rule, the 1-of-3-criteria outcome at P02 close promotes **M020** as a committed milestone. Roadmap update positions M020 between M014 and M019 Tier 2/3 per D011's framing. (Review-state per page and dispatch-callable query surface — D011 criteria b/c — remain explicitly out of scope for M012 and land in M020.)
  - milestone-summaries :: .orchestrator/milestones/M012/M012-SUMMARY.md:16:  - "AD-1 D011 ship cross-refs only (defer review-state + query surface to M020); AD-2 deploy to same repo gh-pages + same Discussions; AD-3 SSOT via include-markdown plugin (no copy, no symlinks); AD-4 archive included labeled with no date cutoff; AD-5 Giscus mapping=pathname + documented remap script; AD-6 all phases/tasks included with collapsible per-milestone nav; AD-7 MkDocs + Material + include-markdown-plugin + Giscus + GitHub Pages framework stack locked; AD-19 single-script-file Check shape with MEM004 carve-out for scanner/diagnostic/wrapper internals; D011 mechanical evaluation rule (1 of 3 criteria -> M020 promoted); Constitution VI state-on-disk-is-truth preserved via include-only projection; Constitution XIV no-speculative-complexity (no flags, no plugin drivers, no YAML-driven wrappers); Constitution XV surgical precision (each phase touches disjoint file surfaces; cross-phase additions via marker-bounded blocks + EOF-append); MEM001 Bash 3.2 discipline across all shipped scripts; loud-fail-over-silent-empty for ENV empty defaults; SC-10 self-contained wiki/ + sibling-gate allow-list extension policy; SC-12 pending-sentinel deploy record for operator-gated outcomes"
  - milestone-summaries :: .orchestrator/milestones/M012/M012-SUMMARY.md:18:  - "scanner-to-stubs-to-nav three-stage pipeline with additive append-only emission blocks keeping downstream generators stable across phases; marker-bounded atomic writes to shared config files with shasum byte-identity verification across edits; include-only SSOT with spec-quote exclusion for task-plan and payload files; pure-string Bash 3.2 primitives (PID-suffixed /tmp list files as associative-array replacement; IFS-slash positional-parameter normalize_path with no realpath dep; parallel indexed-array pattern registries; mktemp list-file + while-IFS-read as find-pipe-while replacement); SKIP-as-PASS at gate boundaries for optional toolchain (mkdocs / built site / phase-close summary); phase-suite orchestrator parallel-indexed-variable pattern with eval indirection and stderr-indent-on-FAIL; cross-phase allow-list extension as sibling-gate maintenance; self-scan carve-out via assignment-line regex skip in compat gates; MEM004 carve-out for scanner and diagnostic and wrapper internals with single-script-file Check-layer shape; loud-fail-over-silent-empty convention for missing external config; chained-gate deploy wrapper with first-non-zero-aborts semantics and fixed-verb per-gate output (GATE/BUILD/DEPLOY/DRY-RUN/OK/FAIL); graceful-absent-tool and graceful-absent-artifact patterns with dry-run-exit-0 and live-path-exit-1 split; pending-sentinel artifact for operator-gated outcomes; D011 mechanical-evaluation record as first-class phase artifact; EOF-append pattern for multi-author operator guides preserving prior section byte-identity; heading-as-section-boundary convention for README (distinct from mkdocs.yml marker-comment convention); theme-override as AD-3-compliant injection surface (append-at-render vs body-rewrite); scanner-presence-gated subtree emission (Archive / Knowledge Entries disappear cleanly when scanner sees zero records); stderr-noise-stdout-success diagnostic convention for machine-readable capture"
  - milestone-summaries :: .orchestrator/milestones/M013/M013-SUMMARY.md:90:M013 is the next-up gate before **M014 (extended)** which inherits the github-common.sh helper API + sidecar schema + sync wrapper for spec management surfaces. **M020** Knowledge-Layer authority is now firmly established (D014); M013 explicitly does not extend `knowledge/spec/**`. **M009** (launch-readiness audit) consumes M013's `RUNTIME-ASSUMPTIONS.md` entries (FR-12 v1 stance). **Codex/Cursor parity** for the post-verify hook is fast-follow demand-driven (M009 audit will surface whether parity is needed pre-launch). The conversus adapter `--strict` mode added under P04 is a reusable invariant for any future caller that needs the M011/P07 adapter to be present rather than gracefully skipped.
  - milestone-summaries :: .orchestrator/milestones/M014/M014-SUMMARY.md:16:  - "D016 (M014 extended scope — native orchestrator:specify + AGENTS.md dual-write + comment classifier); D017 (14 MITs applied pre-discuss via cooperative conversus deliberation); D018 (P03 deferred pending M012 wiki deploy + inbox dogfood; retired by D023); D019 (three-pass scaffold→author→gate flow intensity-scaled per existing intensity engine + universal TODO pre-flight guard on conversus gate adapter); D020 (spec-metrics chunks-first scoping bug + scaffold-placeholder-in-backticks gate false-positive); D023 (M014/P03 unblock — synthetic P99 placeholder retired in favor of explicit Preflight note + SC-16 relaxed to best-available signal with regex/heuristic v1 classifier pin and retune trigger ≥30 actioned comments OR ≥20 percent calibration divergence); byte-identical dual-write (not transform-based) between CLAUDE.md and AGENTS.md across all P02 write-sites; loud-error on slug collision (not silent overwrite); stub-to-full transition contract (P01 stubs emit stable structured fields with zero values so downstream phases replace body without changing caller contract — used for spec-complexity-probe, commands/specify subcommands, RUNTIME-ASSUMPTIONS entries, references/spec-management sections); append-only registry discipline for RUNTIME-ASSUMPTIONS (multiple phases append entries, heading byte-identical across body refreshes); hardening-spec exception keyed on fr_count==0 AND user_story_count>=5 (M016/M021 pattern); OR-semantics across five complexity axes with boolean hardening override; D007 reuse discipline honored throughout — conversus adapter byte-identical pre/post M014 despite two new presets landing; two-tier SC-5 invariant verification (source-level grep in scripts/comments excluding apply.sh + runtime-event assertion in pipeline verifier); patch -N forward-only for stale-diff probe (vanilla --dry-run auto-reverses silently); env-overridable sub-script paths (COMMENTS_FETCH, COMMENTS_CLASSIFY, COMMENTS_ADAPTER, COMMENTS_UAT_INGEST, COMMENTS_APPLY, COMMENTS_REJECT, COMMENTS_TRIAGE) for hermetic testability without D007 violation; deterministic content-addressed queue/triage filenames (shasum-256 first 8 hex of comment URL); adapter-missing as routing verdict not failure (M013/FR-13 graceful-degradation at comments seam)"
  - milestone-summaries :: .orchestrator/milestones/M014/M014-SUMMARY.md:18:  - "Template SSOT with ground-truth heading fixture; marker-bounded atomic splice with byte-preserved outside region (SC-6a); subcommand surface with deferred body (diagnostic-stub exits distinct codes; full semantics land in later phase without command-file re-authoring); hermetic scratch project pattern for end-to-end command testing; verifier self-exemption for rule-embedding gates; awk-field extraction for structured corpus parse; write-site enumeration discipline with scanner gate; allow-list pattern for orthogonal writes (runtime-native full-file renders); hermetic-root via dirname of ORCH_ROOT (decouples dual-write target from repo root); advisory-first drift severity model; one-time migration script pattern with --dry-run/--apply/--force + idempotent already-migrated short-circuit; CC-first with defensive fallback (runtime gate + empty-response bailout — never fails caller); STATE_ROOT vs PROJECT_ROOT separation for mutation sinks vs dependency lookups; CONVERSUS_STUB hermetic testing via fixture-copy path in adapter; dispatch-interface mock via backup-and-swap; section-split awk + per-section classifier; append-with-body-refresh for registry entries (heading byte-identical body refreshed); bash32 scanner comment-strip to avoid false-positive on comment prose; hermetic-test stub via env-var pointing at JSONL fixture (GH_API_STUB, GH_GRAPHQL_STUB); per-surface source_surface filter on shared stub fixture; FR-19 dry-run JSONL action records as no-disk-write counterpart; grep -F literal-string match for URL dedup; stdout-verdict + stderr-INFO split; single-agent cooperative preset for refinement-only LLM calls; patch -N forward-only stale-diff probe; env-overridable sub-script-path hermetic-test pattern (COMMENTS_*); non-executable adapter as routing verdict (graceful-degradation test pattern); deterministic content-addressed filenames for idempotent queue/triage; two-tier must-not-happen invariant verification (source grep + runtime event assertion); bash32+lint omnibus with dynamic enumeration + basename self-exemption; zero-prompts hermetic scratch + fixture replay under --yes; outside-marker shasum invariant pre/post dual-write; phase-suite orchestrator with IFS-newline iteration + per-gate rc capture + FAILED_GATES accumulator"
  - milestone-summaries :: .orchestrator/milestones/M019/M019-SUMMARY.md:53:- **`scripts/lib/pricing.sh`** — sourceable bash 3.2 lib (9 functions: `pricing_file_path` / `pricing_last_updated` / `pricing_stale_days` / `pricing_is_stale` / `pricing_resolve_alias` / `pricing_lookup_rates` / `pricing_estimate_cost_usd` / `pricing_warning_reason` / `chars_to_tokens_quartile`); honors `ORCH_PRICING_FILE`; never aborts caller — returns empty + module-scoped `_PRICING_WARNING_REASON` on missing / stale / no-rate.
  - milestone-summaries :: .orchestrator/milestones/M020/M020-SUMMARY.md:16:  - "D024,AD-1,AD-2,AD-3,AD-4,AD-5,AD-6,AD-7,AD-8,FR-1,FR-2,FR-3,FR-4,FR-5,FR-6,FR-7,FR-8,FR-9,FR-10,CON-1,CON-2,CON-3,CON-4,CON-5,THREAT-006,THREAT-007,SC-1,SC-2,SC-3,SC-4,SC-5,SC-7,SC-8,US-1,US-2,US-3,US-4,US-5,OQ-1,OQ-2,OQ-3,OQ-4"
  - milestone-summaries :: .orchestrator/milestones/M020/M020-SUMMARY.md:18:  - "schema-authority gate via D-row + conventions MEM before code lands; closed-enum discipline with companion-field cohesion (status:/decision_history:/archived_into: documented together); atomic frontmatter mutation via tempfile+rename(2) with closed-enum guard before file I/O,read-only-during-dispatch invariant verified via md5+mtime byte-equivalence across multi-invocation batteries (strictly stronger than git status); side-effect-free query surface; tempdir+PROJECT_ROOT+ORCH_ROOT+HOME env-override fixture isolation pattern reused across all phases,additive-in-place script extension via new-flag-gating preserves CON-4 byte-equivalence (legacy invocations pay zero cost); deferred-resolution sentinel pattern for CLI-vs-preferences precedence (empty-string sentinel + seen-on-cli flag set in CLI arm + post-argparse pref_resolve cascade); belt-and-suspenders closed-enum validation runs against the resolved value,cluster-aware mutation script pattern: pre-flight read at graduate-time (not cluster-time per THREAT-006) -> abort with structured diagnostic + zero mutations on drift -> deterministic write loop with shared per-cluster scalars -> JSONL emission after all writes succeed; drift-gate-as-CON-4-preserver,plan-deviation invariant for verifiers: any task whose plan names a verifier in its Verification section MUST author it inline because auto-loop --step=V parses Verification section per task with no cross-task ordering awareness,capture-validate-restore for byte-equivalent preservation of upstream-phase deliverables (cp original to /tmp before in-place rewrite; restore via /bin/cp because cp may be aliased),distinct-vocabulary fixture rule for clustering integration tests: boilerplate scaffolding words (distinct/fixture/body/unique) must be removed from per-entry bodies; otherwise v2 feature-vector token overlap on common scaffolding pushes pairwise similarity above the threshold and supposed-singletons co-cluster spuriously,subprocess invocation of jaccard.sh from helpers (NOT sourced) because jaccard.sh's file-scope set -euo pipefail + bottom-of-file empty-subcommand exit makes sourcing hostile; AD-19 safe because shape-guard inspects directly-invoked Bash tool calls not script internals,failure-tolerant subprocess invocation pattern (tempdir + RETURN trap + rc capture + N guard branches each emitting structured-unavailable + stderr diagnostic + early-return 0); two-pass parser pattern (validate every line against case-glob shape -> render),adaptive-threshold-recommendation in validate subcommands (data-driven branching on observed top similarity); status-count-as-bulk-migration proxy (^status: line counts with percentage tolerance robust against unrelated frontmatter churn); pure-helper composition with file-level tempfile+rename atomicity composing into cluster atomicity,bash 3.2 pure-function pairwise primitive: tokenize -> sort -u -> comm -12 for intersection / cat | sort -u for union / awk for floating-point division (no bc dependency); first-paragraph awk extraction must defer blank-line termination until at least one content line printed,JSONL emission via dh_emit_jsonl helper (one JSON object per call with conservative backslash + double-quote escaping; no jq dependency); double-source guard sentinel (_<HELPER>_SOURCED=1) lets multiple sourceable helpers coexist; portable md5 fallback (md5 -q on macOS / md5sum on linux / shasum -a 1 fallback); per-key validators as single case statement keep type+range constraints colocated and bash-3.2-safe; stderr-preservation as load-bearing operator surface (do NOT redirect WARN diagnostics to /dev/null); plan-implementation-reality drift detected at integration time when unit-level verifiers miss silenced stderr,grep -c safe-counter (grep -c returns rc=1 + prints 0 on no-match; the common '|| echo 0' fallback DOUBLES the count line; wrap in count_event helper with '|| true' and default-empty-to-zero); shadow-repo + wrapper-helper pattern for integration tests that need to pin a fixture knowledge_root through a script that doesn't propagate the flag to its helpers"
  - milestone-summaries :: .orchestrator/milestones/M020/M020-SUMMARY.md:34:P03 (graduate workflow + schema-authority lint) extended `graduate.sh` in place with `--cluster <id>` + `--reject` + multi-entry positional shape. The cluster-aware mutation script pattern is the load-bearing P03 contribution: pre-flight `fm_read_status` on every member at graduate-time (not at cluster-time per THREAT-006 disposition), abort with structured `cluster-membership-drift` diagnostic + zero file mutations on any non-`candidate` member, then deterministic write loop with shared per-cluster scalars (operator, rationale_hash, canonical), then JSONL emission after all writes succeed. Drift-gate-as-CON-4-preserver: gating the new pre-flight on the new flag means legacy single-entry invocations pay no cost. `scripts/knowledge/lib/decision-history.sh` exposes `dh_resolve_operator` (git config user.email -> preferences.yml:operator_identifier -> unknown@local fallthrough per OQ-2) and `dh_emit_jsonl` (one JSON object per call, conservative escape, no jq dependency). `scripts/verify/knowledge-schema-lint.sh` enforces FR-9 + SC-8 with three failure shapes (unauthorized-field, vocabulary-drift, malformed-frontmatter); live tree scan: 31 entries / 0 violations.
  - milestone-summaries :: .orchestrator/milestones/M025/M025-SUMMARY.md:17:  - "Tier B (user-directed override; confirmed by metrics — 4 US / 11 AS / 10 FR / 1 SDD flow); single-phase milestone (P01). #Q-1 event-mapping policy locked at plan-phase: post_verify -> Stop; before_commit -> PreToolUse + Bash matcher; 4 deferred (before_tasks, after_tasks, before_implement, after_implement) with TODO(M025+) markers. #Q-2 tagging shape locked: inline _orchestrator_managed: true (not sidecar manifest) — self-contained uninstall; CC lenient about unknown keys. FR-9 runtime-scope: Claude Code only; codex + cursor installers + adapters byte-identical (pinned-sha + negative-grep gate). python3-for-awk-fallback deviation from CON-2 spec prose — documented in MEM027 as the pattern's canonical shape. FR-8 byte-identity nuance (canonicalization vs raw baseline): round-trip reversibility gate dual-asserts pinned-sha + structural-equivalence to preserve intent. New-file write path canonicalizes through the same serializer as the merge path so idempotency holds on first+second install."
  - milestone-summaries :: .orchestrator/milestones/M025/M025-SUMMARY.md:50:- **Tier B** — User-directed override confirmed by metrics (4 US / 11 AS / 10 FR / 1 SDD flow / bounded scope / no cross-phase coordination). Auto mode deemed overkill; Tier B manual per-phase dispatch kept the human in the loop on `#Q-1` and `#Q-2`.
  - milestone-summaries :: .orchestrator/milestones/M025/M025-SUMMARY.md:52:- **#Q-2 tagging shape** (locked at plan-phase) — inline `_orchestrator_managed: true` on each orchestrator-inserted hook leaf. No sidecar manifest. Rationale: self-contained uninstall; CC lenient about unknown keys; sidecar drift the larger pragmatic risk.
  - milestone-summaries :: .orchestrator/milestones/M030/M030-SUMMARY.md:25:  - "scripts/diagnostics/metrics-rollup.sh --by-model (per-tier dispatch counts + aggregated_cost_usd + counterfactual_all_smart_cost_usd lines); scripts/diagnostics/efficiency-footer.sh model_mix: line; scripts/diagnostics/doctor.sh --config-check (routing-table syntax validation with file:line diagnostics); scripts/diagnostics/check-anomalies.sh model_routing_regression text + JSONL record (rolling-window per-class verifier-fail-rate); scripts/diagnostics/shadow-compare.sh 4-verdict flip_recommendation= line; .orchestrator/milestones/M030/M030-ACCEPTANCE-EVIDENCE.md one-shot ledger; tests/m030-acceptance/run-acceptance-battery.sh BATTERY: pass=N fail=N canonical line; classifier-confidence stability metric in shadow-compare per-class evidence lines; .orchestrator/anomalies.jsonl append-only stream"
  - milestone-summaries :: .orchestrator/milestones/M030/M030-SUMMARY.md:42:P06 (Anomaly-driven regression detection, 49m) amended `scripts/diagnostics/check-anomalies.sh` with a `_ca_model_routing_regression_check` function (rolling-window per-class verifier-fail-rate; emits `FLAGGED model_routing_regression class=<X> class_pass_rate=<R> sample=<N> threshold=<T>` text + JSONL record only when `class_sample >= min_class_sample` AND `class_pass_rate < pass_rate_threshold`); added `character` additive field to dispatch-interface shadow-on emit branch (extending the additive-field-on-shadow-on-emit lineage from P02/T02 + P04/T03; SC-11 byte-equality preserved); env-only JSONL emit path (`M030_ANOMALIES_JSONL_PATH` default `.orchestrator/anomalies.jsonl`) keeps surface narrow. Pre-P06 records (no `character` field) silently skipped — preserving SC-11 byte-equality on every existing pre-amendment fixture. Patterns: append-only anomalies.jsonl emit via `>>` redirect with `mkdir -p` guard (CON-6 invariant extended via separate-file boundary); rolling-window-per-class-verifier-fail-rate-check shape.
  - milestone-summaries :: .orchestrator/milestones/M030/M030-SUMMARY.md:8:  - "P00 — version-controlled classifier ground-truth fixture corpus at tests/fixtures/m030-classifier-corpus/labels.yml (40 hand-labeled pre-M030 PLAN.md references; 20 mechanical / 15 standard / 5 novel) + README.md methodology + 5 corpus-shape verifiers under tools/verify/p00-*.sh; P01 — scripts/dispatch/classify-task.sh (deterministic <100ms no-network classifier emitting character + confidence) + templates/model-routing.yml SSOT (routing + per-runtime resolution + cost_rates) + references/model-routing.md operator docs + classifier-confidence stability metric (rolling variance ≤ 0.10 + N=20 minimum class-coverage); P02 — scripts/dispatch/dispatch-interface.sh shadow-mode hook (CC-only gated on M030_SHADOW_MODE=1 && CLAUDECODE=1) + 5 additive JSONL fields (model_routed,model_used,partial_flip_active,withheld_classes,classifier_confidence) + scripts/diagnostics/shadow-compare.sh (4-verdict closed-enum aggregator: ready|partially_ready|block|evidence_insufficient); P03 — operator-overrides resolution path (kill-switch → plan-frontmatter → milestone-floor → none precedence chain) + override_source closed-enum field + references/model-routing.md ## Operator Overrides; P04 — live-routing branch + verifier-fail escalation (FR-10 with CON-5 hard-cap) + FR-9 programmatic flip-gate enforcement + escalation_count/escalation_reason/shadow_gate_blocked JSONL fields + escalation_cap_hit record type; P05 — scripts/diagnostics/metrics-rollup.sh --by-model + scripts/diagnostics/efficiency-footer.sh model_mix: line + scripts/diagnostics/doctor.sh --config-check; P06 — scripts/diagnostics/check-anomalies.sh model_routing_regression check (rolling-window per-class verifier-fail-rate) + dispatch-interface character emit + .orchestrator/anomalies.jsonl emit path + references/model-routing.md ## Anomaly Records; P07 — tests/m030-acceptance/shadow-corpus-fixtures.sh idempotent synthesizer + 4 corpora (corpus-50-per-class.jsonl 150 records, corpus-zero.jsonl, corpus-2-class-only.jsonl 100 records, corpus-block.jsonl 60 records) + tests/m030-acceptance/run-acceptance-battery.sh end-to-end SC runner (BATTERY: pass=22 fail=0 across 14 SCs) + .orchestrator/milestones/M030/M030-ACCEPTANCE-EVIDENCE.md one-shot ledger + 10 P07 verifiers + p07-phase-suite.sh aggregator + M030-VALIDATED marker + this M030-SUMMARY.md"
  - milestone-summaries :: .orchestrator/milestones/M031/M031-SUMMARY.md:16:  - "AD-1..AD-20 folded in-place into specs/034-right-sized-entry/spec.md (P00); SC-13 Option A active per fixtures/empirical-baseline/SC13-OPTION.md (corpus has no committed git history at evaluation time; battery N=15 not 16); FR-4 collapse of commands/dispatch.md:21 Skip-payload-assembly branch is the load-bearing change that makes Quick knowledge-unconditional; Quick profile traversal scope = touched-files-only + 1-hop + no Decisions section + glossary slice over touched terms (per-profile scope/traversal/decisions/glossary policy); --meta-out JSON sidecar emitted by every Quick dispatch with mem_count + total_tokens + profile + compression_applied + snip_applied (CON-1 invariant); deterministic task-slug derivation = first 40 chars lower-cased + hyphenated + alphanumerics-only + 4-char SHA-1 collision suffix (AD-10); ORCH_<SUBSURFACE>_<NOUN> env-override seam naming as the universal test-seam idiom; passive-observation-surface discipline (m031_compound_change_check returns 0 unconditionally — never increments checks_passed/total/advisory_warnings); detection-by-knob-absence pattern (no version field, no migration timestamp — knob absence IS the migration trigger); window=7 + threshold-multiplier=1.1 + median-not-mean for QUICK_BUDGET_DRIFT (operator-meaningful summary, absorbs single-task variance); QUIET=0 gate as byte-identity preservation discipline (additive emission surfaces never break --quiet zero-stdout contract); P04/T04 follow-up commit 6979afa: empirical-baseline.sh COMPARE verdict frame inverted from 'fewer tokens' to 'post pass-rate >= pre pass-rate' (M031's restoration thesis vs the pre-M031 thrift thesis) — pass-rate parity is the load-bearing CON-5 + Principle II claim, token-budget discipline is owned separately by SC-15; auto_proceed: true default flip framed via single CHANGELOG bullet using 'compound' keyword (AD-9); milestone-grain SC-12 scope-guard distinct from per-phase scope-guards (operator-facing acceptance-test surface vs internal phase-suite gate); doc-comment-only Key-links remediation pattern at phase-close (single comment block in cross-referenced script naming the literal target so check-must-haves grep resolves) — established P01 → P02 → P03 → P04, now canonical; verbless 'orchestrator <task>' entry reserved for post-M009 runtime parity (M031 ships orchestrator:do <task> only)"
  - milestone-summaries :: .orchestrator/milestones/M031/M031-SUMMARY.md:8:  - "Knowledge-unconditional Quick intensity (build-context.sh --profile=quick|standard|full + --meta-out JSON sidecar) restoring knowledge graph + compression access to small-task dispatches; FR-4 collapse of dispatch.md Skip-payload-assembly branch (every Quick dispatch now emits payload_breakdown JSONL with profile + knowledge_section_tokens + compression-tier counters); Tier A+ middle flow (research → plan → build chained role dispatches via route-to-dispatch.sh, deterministic task-slug paths under .orchestrator/tier-a-plus/<slug>/, plain-language y/n/c approval prompt with inline 8-line research summary and --yes audit trail, no auto/roadmap/consolidate, no milestones/M###/ scaffolding); commands/do.md universal-entry skill + scripts/intake/do-entry.sh driver routing through M024 classifier with confidence-gated approval (entry_routing_confidence_floor) + FR-12 fast-path stderr summary line + FR-13 Tier B/C passthrough; post-M024 doc-drift correction across commands/evaluate.md + references/tier-definitions.md (Tier A always-present .orchestrator/ contract, single-dispatch + Quick-profile descriptor); auto_proceed: true default flip + CHANGELOG compound-change framing (AD-9); run-doctor.sh AD-9 compound-change banner via m031_compound_change_check (knob-absence detection); efficiency-footer.sh QUICK_BUDGET_DRIFT informational warning (windowed-median over 7 records, threshold 1.1× quick_knowledge_token_budget, AD-19); milestone-grain SC-12 scope-guard (tests/m031-acceptance/scope-guard.sh) + SC-14 acceptance-battery aggregator (tests/m031-acceptance/run-acceptance-battery.sh, BATTERY: pass=15 fail=0 Option A); M031-ACCEPTANCE-EVIDENCE.md evidence ledger; pinned config defaults (quick_knowledge_token_budget=800 + entry_routing_confidence_floor=0.7 + tier_a_plus_prompt_summary_lines=8); 20-task fixture corpus + JSONL ledger + pre-M031 stub preservation under tests/m031-acceptance/fixtures/empirical-baseline/"
  - milestone-summaries :: .orchestrator/milestones/M036/M036-SUMMARY.md:60:- **Stale P09 dispatch artifact on disk** — `phases/P09/P09-PLANNING-PAYLOAD.md` from an interrupted plan-phase invocation. Harmless (validate-milestone no longer enumerates P09 after the roadmap edit). Operator follow-up: clean up the directory at next milestone consolidation pass.
  - milestone-summaries :: .orchestrator/milestones/M037/M037-SUMMARY.md:12:  - "M035 (packaging & distribution — pre-launch wiki polish no longer contaminates launch validation loop); post-launch wiki-ux-deep + external-tool-adapters proposal (FR-12..FR-17 absorbed via M037-fr-12-17-deferred-scope.md); post-launch yaml-merge list-element-preservation fix (round-5 surfaced gap, deferred)"
  - milestone-summaries :: (capped at 5 hits — more may exist)

### Q3 — KEPT

- **question**: - #Q-3 (do-removal-runway): How long is the `orchestrator:do` deprecation runway before shim removal, and what release gates it?
- **disposition**: kept (operator + roadmap decision at plan-phase per spec; kept at discuss gate 2026-07-02)
- **terms** (9): Q-3, removal, runway, long, orchestrator, deprecation, shim, release, gates
- **hits**:
  - constitution :: .orchestrator/memory/constitution.md:14:  land in the new Quality Gates bullet body — Decision B resolved
  - constitution :: .orchestrator/memory/constitution.md:27:      conversus; orchestrator-side evidence under scripts/verify/*.sh
  - constitution :: .orchestrator/memory/constitution.md:30:      conversus; orchestrator-side evidence via
  - constitution :: .orchestrator/memory/constitution.md:32:      templates/orchestrator-config-default.yml (41 leaves, baseline 0
  - constitution :: .orchestrator/memory/constitution.md:4:  Version change: 2.2.1 → 2.3.0 (MINOR — Quality Gates section gains
  - constitution :: .orchestrator/memory/constitution.md:442:  structural home as release-time evidence rather than as an
  - constitution :: .orchestrator/memory/constitution.md:444:  the GH release automation workflow introduced in M035 runs each
  - constitution :: .orchestrator/memory/constitution.md:456:    before and destroyed immediately after each release gate run.
  - constitution :: .orchestrator/memory/constitution.md:492:     - MAJOR: Principle removal or backward-incompatible redefinition.
  - constitution :: .orchestrator/memory/constitution.md:5:  a new "Install gates" bullet per follow-on amendment Item 3 of
  - constitution :: .orchestrator/memory/constitution.md:55:    - Quality Gates — at v2.3.0 gains a new "Install gates" bullet
  - constitution :: .orchestrator/memory/constitution.md:57:      install testing) from XXII's invariant body to release-time
  - constitution :: .orchestrator/memory/constitution.md:6:  .orchestrator/proposals/M0XX-tier-2-xxii-xii-substantive-followups.md;
  - constitution :: .orchestrator/memory/constitution.md:66:      inheritance basis); orchestrator I-XV grandfathered.
  - constitution :: .orchestrator/memory/constitution.md:9:  invariant body to constitution Quality Gates as release-time
  - constitution :: (capped at 5 hits — more may exist)
  - decisions :: .orchestrator/DECISIONS.md:102:Inspection of the conversus source (`~/Sites/conversus`) showed that `/conversus gate <phase> <artifact>` already provides the exact CI-shaped primitive orchestrator needs — structured `PASS \
  - decisions :: .orchestrator/DECISIONS.md:127:- **Choice**: Revised sequence: **M011 → [M019/Tier 1 emitter] → M012 → M013 → M014 → [M019/Tier 2+3] → M018 → M009 → M010**. Tier 1 ships as a small standalone unit once M011 is closed — avoids mid-milestone scope insertion (Constitution XV — Surgical Precision), gives a clean boundary for instrumentation to start, and still captures M012+M013+M014 (~3 full milestones of dogfooding) before M018 planning needs a baseline. Cost of not backfilling M011 dispatches: ~15–25 unlogged records from M011/P04–P07; acceptable tradeoff. M019 records time, token cost, estimated dollar cost, retries, deviations, verification pass rate, and domain-unit cost (per ingested spec / merged PR / auto-applied comment) at task/phase/milestone/project/all-time granularities. Three-tier framing: **Tier 1 — Just emit** (~1 day: append `payload_breakdown`, `dispatch_usage`, `unit_close` JSONL records to existing `execution-log.jsonl`; no UI; ships first thing after M011 close so M012–M014 dogfooding produces real data); **Tier 2 — Rollup + `orchestrator:cost` command** (~3 days: `scripts/diagnostics/metrics-rollup.sh`, `.orchestrator/metrics/*.jsonl` aggregates, `orchestrator:status` efficiency footer; worth it if we launch); **Tier 3 — Full polished surface** (~7 days: backend-specific `adapters/backend/*/report-usage.sh`, auto-generated `MNNN-METRICS.md` on consolidate, `orchestrator:doctor` anomaly checks, parity evals across runtimes; only if efficiency becomes M009 launch headline). Full M019 phase outline mirrors the tiers: P01 schema + pricing table, P02 dispatch-time emitter (Tier 1 boundary), P03 backend usage adapters, P04 rollup + `orchestrator:cost` command (Tier 2 boundary), P05 milestone close report, P06 status + doctor integration, P07 evals + multi-runtime parity (Tier 3 boundary). Schema must pair cost metrics with **quality metrics** (verification pass rate, deviation rate) from day one to avoid Goodhart failure mode — optimizing cheap over correct. All records labeled `source: "estimate" \
  - decisions :: .orchestrator/DECISIONS.md:15:- **Choice**: P06 scope expanded by three engine-layer workarounds discovered during P03 implementation: (1) literal-audit-marker printf pattern added after SAFETY_WARNING emissions in scripts/engine/run.sh (T03/T04) because events.sh _orch_events_quote does not quote single-word values, breaking must-have greps that expect quoted form; (2) cd $REPO_ROOT subshell wrapper around check-must-haves.sh and record-result.sh calls in scripts/engine/run.sh (T05) working around check-must-haves.sh PROJECT_ROOT walk-up bug that computes PROJECT_ROOT as .specify/orchestrator/milestones/<m> instead of repo root; (3) execution-log.jsonl entries from engine dispatch do not thread ORCH_RUN_ID because record-result.sh does not currently accept/emit run_id. P06 cleanup should: fix check-must-haves.sh PROJECT_ROOT detection, extend events.sh _orch_events_quote to always quote values, extend record-result.sh to thread run_id from env, then remove all three workarounds from run.sh.
  - decisions :: .orchestrator/DECISIONS.md:225:- **Choice**: Revised sequence: **M013 (P04 remaining) → M014 (extended) → M020 → M024 → M019 Tier 2+3 → M018 → M023 → M009 (extended) → M010 (adjusted)**. (1) **M014 scope extension**: port spec-kit's `specify` shape as native `orchestrator:specify` (CC-first, written portably, treating spec-kit template as the I/O contract rather than a verbatim port); add conversus-suggestion logic so complex/controversial specs auto-propose red-blue pressure-test with optional spec-decomposition-before-conversus for very large specs; dual-write `AGENTS.md` alongside `CLAUDE.md` so Codex stays fed without a separate milestone. (2) **M024 Universal Intake** (new): extend `orchestrator:evaluate` to input-agnostic (idea, paragraph, fragment, full spec, or empty + Q&A) and emit a reviewable proposal artifact at `.orchestrator/intake/<id>/proposal.md` covering six axes (input shape, scope tier, decomposition, design gate, conversus gate, intensity); degenerate fast-path auto-proceeds if Tier A + Quick + no conversus + no design, approval-gated otherwise. Positioned after M020 so we dogfood it through remaining milestones. Design gate degrades gracefully pre-M023 ("design walkthrough lands in M023; author DESIGN.md manually or skip"). (3) **M023 Design Layer** (new): `orchestrator:design` command — conversus spawns N design-personality agents in parallel, each produces a DESIGN.md draft + working coded prototype in user's stack; side-by-side comparison; user picks/arbitrates; canonical DESIGN.md lands at repo root and auto-injects into payloads for ui-tagged phases. Renderer adapter interface shaped as MCP clients (runtime-agnostic; Stitch/v0/Figma/etc. consumed as they expose MCP). Stage-1-only scope — high-fidelity stakeholder-mockup expansion (Stitch/Claude-design rendering) deferred until user demand earns it. Positioned pre-M009 because orchestrator has no internal UI to dogfood against — landing earlier buys zero learning. (4) **M009 scope extension**: add runtime-parity audit as pre-launch gate — dogfood every `orchestrator:*` command end-to-end on CC + Codex + Cursor, consuming the `RUNTIME-ASSUMPTIONS.md` registry accumulated during M013–M018, fix parity bugs before external eyes. (5) **M010 posture adjustment**: ship with Managed Agents primary adapter + Codex Cloud **stubbed** adapter (proves abstraction holds without full feature parity); full Codex Cloud as demand-driven fast-follow rather than launch-critical. (6) **Ultraplan/ultrareview stance**: parked, not integrated — Claude-Code-only (requires CC on web + GitHub repo + Anthropic cloud; not available on Bedrock/Vertex/Foundry), forks multi-runtime UX. M013 GitHub integration delivers the same plan-review value universally via PR comments. Re-evaluate only if real CC-only user demand emerges. (7) **`RUNTIME-ASSUMPTIONS.md` discipline** (no milestone cost): a registry file tracking CC-specific shape assumptions as written (`settings.json` hook formats, CLAUDE.md-only writes, `claude` CLI invocation patterns). M009's parity audit consumes it as a punch-list rather than an open-ended investigation. Optional: fold a single Codex smoke-probe telemetry point into M019 T2+3's emitter for drift detection.
  - decisions :: .orchestrator/DECISIONS.md:228:User is dogfooding CC-exclusively right now; Codex parity is a launch concern, not a dogfood concern. Back-loading Codex validation to M009 is constitutionally cleaner (XIV — don't build Codex plumbing speculatively before CC usage teaches us what needs to be portable) and keeps the current dogfooding loop fast. M023/M024 are the adoption layer — M024 ships where its internal dogfood signal is cheapest (post-M020, exercised on every remaining milestone), M023 ships where its external signal is cheapest (pre-launch, since this repo has no UI to dogfood design against). MCP-as-adapter for design renderers means M023 is runtime-agnostic by construction (CC + Codex + Cursor all support MCP). Extending `evaluate` rather than adding a new `orchestrator:start` command keeps the surface small (XIV). Proposal-as-artifact gates the router on Principle III (Design Before Code). The `RUNTIME-ASSUMPTIONS.md` discipline is a lightweight hygiene practice — a few lines of markdown per observation — preventing "CC-first" from silently becoming "CC-only." Supersedes the forward-sequence framing from D004/D005/D006/D008/D013 where M013→M014→M020→M019 T2+3→M018→M009→M010 was the last committed order.
  - decisions :: .orchestrator/DECISIONS.md:267:- **Choice**: (1) **Three-pass contract** pinned in `commands/specify.md`: Pass 1 (scaffold) always runs — creates file, populates frontmatter + Input, writes Section Contract `<TODO:` placeholders. Pass 2 (author) drafts the body from Input + DECISIONS.md + constitution + neighboring CLAUDE.md/AGENTS.md Recent Changes; Quick = agent-only no review, Standard = agent + self-review checklist warnings, Full = agent + `speckit.clarify` loop (≤5 load-bearing ambiguity questions, operator-answered, agent-revised). Pass 3 (gate) adversarially reviews authored body via `scripts/dispatch/adapters/tool/conversus.sh gate spec-pressure-test`; Quick = skip, Standard = advisory (findings folded into Open Questions, no BLOCK), Full = strict (BLOCK halts with dispute list + "revise then --amend" message). On Full+PASS, spec status promoted `Draft` → `Ready-for-discuss` + `Last Revised:` line added per M013/M014 precedent. (2) **Intensity resolution order**: project default from `.orchestrator/config.yml` → smell-test escalation via `scripts/engine/intensity-analyze.sh --description <prose>` (can only escalate, never de-escalate) → CLI `--intensity` override (trumps both). One `specify_intensity_resolution` JSONL record emitted at run start. (3) **Gate TODO pre-flight invariant** (universal): `scripts/dispatch/adapters/tool/conversus.sh gate` refuses any artifact containing ≥ `CONVERSUS_GATE_TODO_THRESHOLD` (default 1) `<TODO:` markers, with actionable error pointing at the author pass. Bypass: `CONVERSUS_GATE_SKIP_TODO_CHECK=1` (reserved for tests + intentional-stub preset authoring). Protects M013/M014/M023/M024 + any future gate consumer, not just `orchestrator:specify`. Applied to adapter in `scripts/dispatch/adapters/tool/conversus.sh:150-169` + regression test in `tests/test-conversus-adapter-shim.sh` section 1b. (4) **Reuse over rebuild**: all intensity machinery already exists (`intensity-analyze.sh`, `intensity-override.sh`, `intensity-gate.sh`, `spec-complexity-probe.sh`) — D019 wires them into the specify flow rather than adding parallel infrastructure. (5) **Implementation posture**: command contract is the SSOT (landed 2026-04-23 in `commands/specify.md`); shell implementation in `scripts/specify/specify.sh` lands in the next M014 extended phase. Until then, agents invoking the command execute passes 2 and 3 manually per the doc.
  - decisions :: .orchestrator/DECISIONS.md:282:- **Revisable**: Yes — (a) if `orchestrator:ingest` becomes mandatory before `orchestrator:evaluate` as a workflow invariant, fix #1 becomes moot (spec chunks always cover the current spec). (b) if `CONVERSUS_GATE_TODO_THRESHOLD` is raised to 2+ to allow legitimate deferred-to-plan placeholders, fix #2s urgency drops (one accidental match no longer trips refusal). (c) if spec-026s Pass 2 prompt template evolves to post-process authored output for stray TODO patterns, fix #2 becomes a validation-layer concern not an authoring-hygiene concern.
  - decisions :: .orchestrator/DECISIONS.md:29:- **Choice**: P06 scope expanded by five additional items discovered during P05 dispatch refactors: (1) scripts/lib/run-context.sh _orch_run_nonce SIGPIPE under set -o pipefail — causes scripts/engine/run.sh --dry-run to exit 141 when ORCH_RUN_SEED is unset; worked around in T02/T03/T04 with set +o pipefail wrapper around init_run_context (3 replicated call sites to drop after P06 fix). (2) scripts/dispatch/lib/section-handlers.sh handle_phase_summaries has a Bash 3.2 while-read bug with unterminated dep-list temp file — worked around by local _bc_handle_phase_summaries_fixed shim in build-context.sh (to drop after P06 fix). (3) templates/context-recipe.yaml default section order/priority values don't match pre-refactor dispatch-prompt parity — worked around by _bc_display_order/_bc_display_name/_bc_display_priority shim in build-context.sh (to drop after P06 recipe re-authoring). (4) scripts/verify/check-must-haves.sh doesn't discover tasks/TNN-PLAN.md files — all P05 tasks had to verify must-haves manually from payloads rather than via the canonical entrypoint. (5) scripts/dispatch/build-context.sh all emit_event calls are >/dev/null 2>&1
  - decisions :: .orchestrator/DECISIONS.md:296:- **Revisable**: Yes — (a) If reinit free-form-prose loss bites a second user, promote 5a to a dedicated milestone or M014-extended task. (b) Stage bloat 5b can be addressed inside the installer without a D-row (filter in `install-claude-code.sh` Stage 4.5 `find` expression). (c) If `check-settings-state.sh` silent regen failure reproduces, open a spec and a bugfix commit. (d) Upstream-removal 5d can be fixed via manifest-diff in a future install pass. (e) If the four dogfood-bugfix commits should have been a single "fix(dogfood)" squashed commit rather than a 4-commit split, revisit commit-message-style for future hotfix batches — current split prioritizes archaeology readability.
  - decisions :: .orchestrator/DECISIONS.md:298:bbt-companion is the first external project the orchestrator has been installed into at scale, and the install surfaced a class of bugs the `test-s*` suite cannot catch because every `test-s*` runs against the development-tree layout where scripts sit beside their callers. Bundle-infrastructure gap was the root (installer wasn't staging the files `commands/*.md` reference via project-relative paths); three downstream bugs (#2, #3, #4) compounded on top because they'd never been exercised against a genuinely-fresh `.orchestrator/` state tree with no prior chunks or milestones. Fixing as out-of-band D-rows (not a new milestone) because each fix is <50 lines of code, scoped to a single script, and orthogonal to M026. Recording as D021 rather than folding into M026 keeps the conversus-OSS migration audit-trail clean. The four parked limitations (5a-d) get a D-row line instead of immediate follow-up work because (a) reinit redesign is architecturally non-trivial (targeted-edit vs sentinel expansion is a real decision); (b) installer stage bloat is cosmetic; (c) check-settings-state is speculative without reproduction; (d) upstream-removal is acknowledged-accepted. Bringing them up explicitly here means they don't get lost to context-expiry between now and when the operator decides to tackle them.
  - decisions :: .orchestrator/DECISIONS.md:3:Decision register for the spec-kit-orchestrator project. Each entry is permalink-stable
  - decisions :: .orchestrator/DECISIONS.md:310:- **Revisable**: The decision is the consolidation point for three load-bearing M026 commitments that downstream milestones (M013, M014, M018, M023, M024) need a single auditable reference for. Rather than scattering the rationale across three MEM entries and the M026-SUMMARY (not yet authored), the D-row anchors the cross-references in the existing DECISIONS audit trail. The two-tier detection pattern is reusable beyond conversus (any pip/pipx-installed Python tool with multi-channel publishing); naming the convention now means the next adapter migration can adopt the shape without re-deliberating env-var naming. The `edition_required:` preset-frontmatter contract was deferred to OQ-3 at spec-027 discuss-finalize and chose the minimum-viable shape (single optional field, no auto-detect of CLI-flag-based paid-only signals per NG-6) — D022 commits the decision in the audit trail so future scope-expansion conversations have the rationale in hand. The `FAIL:`-vs-`ERROR:` prefix uniformity rationale lives here because it is a one-line decision (preserve adapter convention; SC-7 regex accommodates) that does not warrant its own MEM entry.
  - decisions :: .orchestrator/DECISIONS.md:323:- **Choice**: (1) **Roadmap edit**: `Depends: P01, P99` → `Depends: P01` plus a `Preflight (external, operator-gated — see D023)` block enumerating both external gates with current resolution status. P03 phase-entry gate (CONTEXT OQ-4) at dispatcher level remains the load-bearing enforcement for the wiki preflight; encoding it as a fake phase ID was always belt-and-suspenders and caused a parse-time hard-fail under `read-roadmap.sh`'s P##-token guard (introduced M026/P02 commit 316411e). (2) **Wiki preflight: RESOLVED 2026-04-23** — `M012/P04/DEPLOY-RECORD.md` shows live deploy against `Build-Fractal/spec-kit-orchestrator`, all four gates (giscus-config, mkdocs-build, link-check, giscus-smoke) PASS, site live at `https://Build-Fractal.github.io/spec-kit-orchestrator/`. SC-5 redeploy-persistence is the only remaining sub-check and is non-blocking (validated partially via `mkdocs serve`; full validation lands at the next M012 redeploy event). (3) **SC-16 inbox-dogfood preflight: RELAXED** — original spec called for ≥1 week M012/M013 inbox data captured into `specs/024-spec-management-extended/planning-inputs/inbox-dogfood.md` before plan-phase pins FR-9 classifier shape. Wiki was deployed only 1 day before this decision; waiting ≥6 calendar days delays the M014 close past usable cadence. Plan-phase will pin FR-9 shape on **regex/heuristic v1 baseline** (the lowest-cost option from OQ #C-1) with explicit retune commitment after meaningful comment volume accumulates organically. **Retune trigger**: when `actioned.jsonl` shows ≥30 fetched comments OR when classifier confidence calibration on observed comments diverges from regex/heuristic predictions in ≥20% of samples, open a follow-up D-row that re-pins FR-9 shape (likely escalating to LLM-per-comment or two-pass hybrid per OQ #C-1) and either lands the change inside M014 extended scope or as a dedicated M011/M014 follow-up.
  - decisions :: .orchestrator/DECISIONS.md:36:### Remove state_root: from .orchestrator/config.yml in T04 { #dr-code-003 }
  - decisions :: .orchestrator/DECISIONS.md:368:Closes Q-14. Aligns with M019 char-quartile token-estimate library and pricing.sh degradation contract (FR-24). Keeps existing key=value text contract byte-stable per CON-3 — verifier (FR-15 / SC-17) gates regression. JSON shape mirrors retrospective rollup row structure so M018 / Tier 3 consumers can parse one shape across surfaces. Tier keys (quick/standard/full) match recipes/intensity tier names.
  - decisions :: .orchestrator/DECISIONS.md:393:- **Choice**: **Skip re-gate; proceed to plan P00.** All three required mitigations (P0 MIT-1 emitter coverage, P0 MIT-2 SC-9 empirical calibration, P1 MIT-3 eval-harness timing) were absorbed structurally into the spec via `--amend` before the gate's unit_close: (a) MIT-1 → US-0 / FR-0a / FR-0b as P00 hard prerequisite gating all tier code (spec line 296, A-3); (b) MIT-2 → SC-9 threshold deferred to P00 section-level distribution probe with `--amend` commit before P01 closes (spec lines 241, 295); (c) MIT-3 → US-7 promoted P3→P2, must ship before/alongside Tier 3 (spec lines 162, 222). Section-level probe input data (`Knowledge=45.9%`, `Task Plan=24.0%`, `mean=16,797 tok` over 169 dispatches) already on disk at `.orchestrator/scratch/m018-telemetry-probe-report.txt` — MIT-2's empirical foundation is captured. P00 carries the proof: its mechanical acceptance criteria (≥95% `dispatch_usage` parity over 20-dispatch sample; probe-derived SC-9 threshold landed via `--amend`) demonstrate the mitigations operationally rather than through another advocate exchange.
  - decisions :: .orchestrator/DECISIONS.md:43:- **Choice**: Spec Truth #4 requires resolver to report source=existing:.orchestrator, but T02 preserved the redundant state_root: declaration in config, causing Rule 2 to win over Rule 3. Removing the declaration aligns config semantics: config is override mechanism, directory existence is canonical.
  - decisions :: .orchestrator/DECISIONS.md:450:- **Revisable**: Yes — converting a single-formula tap to multi-formula is mechanical (move `orchestrator.rb` under `Formula/` already, add new formulae alongside); shipping single-formula does not foreclose later expansion.
  - decisions :: .orchestrator/DECISIONS.md:470:### D-RN-6 — Migrate Claude memory dir alongside path rename { #dr-code-034 }
  - decisions :: .orchestrator/DECISIONS.md:491:- **Choice**: Author a local-only git tag `v0.9.X-final-spec-kit-name` at HEAD immediately before the rename branch lands, where `X` is the current `CHANGELOG.md` top-line patch number (resolved via `awk '/^## \[[0-9]/{print; exit}' CHANGELOG.md` per CON-4 — skips `## [Unreleased]`). Captured at T01 execution time as `v0.9.2-final-spec-kit-name`. Tag gives post-rename archaeology a clean cutover marker without polluting the SemVer release stream (no `v` release tag is published — this is not a release).
  - decisions :: .orchestrator/DECISIONS.md:500:| D004 | M035/P05 | signing-strategy | Release-artifact signing for the npm publishing pipeline (FR-11 / SC-11 / #Q-3 binding) | sigstore (cosign keyless) primary + SHA-256 checksum fallback | Keyless cosign signing in `.github/workflows/release.yml` (`npm-publish` job) binds each release artifact's signature to the workflow's GitHub OIDC identity (canonical-repo `v*` tag-push) and records it in the public Rekor transparency log — eliminates the GPG private-key blast radius (no long-lived signing key to rotate or compromise). `SHA256SUMS` published alongside the signed artifacts gives operators without `cosign` installed a tooling-free verification path (`shasum -a 256 -c SHA256SUMS`). The signing job uses a job-level `permissions: id-token: write` override; the workflow-level `permissions: contents: read` stays unchanged so other jobs (pr-validate) inherit least-privilege. CON-6 secret-scope discipline preserved (no new long-lived secrets; `secrets.GITHUB_TOKEN` is the default token, scoped by GHA). | No |
  - decisions :: .orchestrator/DECISIONS.md:511:  the GitHub release by P02's `npm-publish` job + P05's signing
  - decisions :: .orchestrator/DECISIONS.md:532:  `.github/workflows/release.yml` writes to
  - decisions :: .orchestrator/DECISIONS.md:556:### D009 — Curl-pipe-bash install.sh URL host: GitHub release asset URL
  - decisions :: .orchestrator/DECISIONS.md:99:- **Choice**: (1) Remove M017 (Conversus Deliberation Gate) from the forward milestone sequence. Revised sequence: **M011 → M012 → M013 → M014 → M009 → M010**. (2) Add M011/P07 (Format-Agnostic Intake & Conversus Fidelity Gate) which lands both the LLM-driven normalizer (any markdown → spec-kit shape) and the reusable `scripts/dispatch/adapters/tool/conversus.sh` adapter. (3) The adapter becomes shared infrastructure: later milestones that want deliberation at specific checkpoints (M013 UAT PR gate, M014 ambiguous-comment classification, roadmap phase-decomposition at Full intensity) invoke it from within their own scope, rather than waiting on a dedicated milestone. (4) Intensity engine owns the policy (when gates fire by default); users own opt-in/out via `--review` / `--no-review`.
  - decisions :: (capped at 5 hits — more may exist)
  - knowledge-entries :: (capped at 5 hits — more may exist)
  - knowledge-entries :: knowledge/conventions/MEM018.md:22:- **await-completion / collect-result**: The agent runtime handles task execution and writes artifacts to disk. The orchestrator detects completion via file presence (task summary exists = done).
  - knowledge-entries :: knowledge/conventions/MEM030.md:21:When an orchestrator integration flips its default from a paid build to an OSS build, the operator still needs a discoverable, undestructive way to reach the paid build for one-off invocations (debugging a paid-only feature, reproducing a paid-build regression, running a preset that depends on paid-only upstream plumbing). Three anti-patterns to avoid:
  - knowledge-entries :: knowledge/conventions/MEM030.md:25:3. **No escape** — routing all paid-only access through a separate command or wrapper. Forces the orchestrator to maintain two parallel invocation paths with the same surface, doubling test burden.
  - knowledge-entries :: knowledge/conventions/MEM030.md:37:5. **Telemetry shape**: every JSONL record emitted by the integration adapter MUST include an `edition` field alongside the existing identifying fields (e.g., `adapter_version`, `gate_id`). Place adjacent to the version field for readability and for adjacency-invariant tests (M026/P02/T02 pattern).
  - knowledge-entries :: knowledge/conventions/MEM030.md:43:The M026 migration is the first OSS-default escape-hatch landing in this repo. Future migrations (e.g., when M010 ships and the orchestrator starts integrating with multiple LLM-provider editions, or when M023 ships and design-renderer adapters need to distinguish freemium tier vs paid tier) will face the same shape. Naming it as a convention now means the next migration can copy the pattern verbatim instead of re-deliberating the env-var name in each milestone.
  - knowledge-entries :: knowledge/conventions/MEM031.md:72:`.orchestrator/DECISIONS.md` D024 (2026-04-25).
  - knowledge-entries :: knowledge/lessons/MEM026.md:21:Commit `d33b8a7` (M013/P04/T04) shipped a `scripts/dispatch/adapters/runtime/claude-code.sh --hook-config` emitter whose root object carried wrapper metadata (`runtime`, `hook_count`, `target_file`) alongside orchestrator-internal event names (`before_tasks`, `after_tasks`, `before_implement`, `after_implement`, `before_commit`, `post_verify`) that Claude Code does not recognize. The companion installer `packaging/install/install-claude-code.sh` wrote that invalid document unconditionally to `$HOME/.claude/settings.json`, overwriting any sibling tool's configuration (notably GSD-authored `statusLine`, `SessionStart`, `PostToolUse`, and `permissions` keys) with no merge path and no reversibility.
  - knowledge-entries :: knowledge/lessons/MEM026.md:23:## Why P04 gates missed it
  - knowledge-entries :: knowledge/lessons/MEM026.md:25:Every P04 verification script — including `scripts/verify/m013-p04-post-verify-hook.sh` — ran against an empty `$HOME/.claude/` fixture. No gate seeded a pre-existing non-orchestrator `settings.json`, so the overwrite path was never exercised. The schema-validity failure was invisible because CC itself was not invoked against the emitted document in any P04 gate; the gates only asserted presence of the wrapper JSON's internal keys, not conformance to the CC `hooks` schema. The regression required a real user on a real multi-tool system to observe.
  - knowledge-entries :: knowledge/lessons/MEM028.md:43:Regression coverage: `tests/test-specify-number-allocator.sh` pre-seeds a scratch `specs/` with `008/009/024` sentinels and asserts the expected `009/010/025` outputs. Any future allocator that consumes zero-padded input belongs in that test's fixture matrix.
  - knowledge-entries :: knowledge/patterns/MEM006.md:19:`run-doctor.sh` aggregates 12 checks (4 legacy + 8 DOCTOR:) into `Checks passed: N/M` with HEALTHY/NEEDS_ATTENTION status. Legacy checks use exit-code pass/fail; new checks parse DOCTOR: status lines. Advisory checks (check-plans.sh) counted separately. History appended to `doctor-history.jsonl` per run.
  - knowledge-entries :: knowledge/patterns/MEM027.md:34:The pattern is enforced by a three-gate triad — any user-scope config write without all three gates fails review:
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:1:# Orchestrator Knowledge Base
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:130:- **[milestone:M005]** [2026-04-12] Scored health reporting — run-doctor.sh aggregates 12 checks (4 legacy + 8 DOCTOR:) into Checks passed: N/M with HEALTHY/NEEDS_ATTENTION status. Legacy checks use exit-code pass/fail; new checks parse DOCTOR: status lines. Advisory checks (check-plans.sh) counted separately. History appended to doctor-history.jsonl per run.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:148:- **[milestone:M008]** [2026-04-14] Thin delegation pattern: top-level orchestration scripts (init-project.sh, installers) should delegate to existing single-responsibility scripts rather than duplicate logic. init-project.sh delegates to detect-runtime (P05), detect-capabilities (P01), detect-project (P07), resolve-root (P04), and runtime-specific installer (P06). Installers delegate --probe/--register/--hook-config to P05 runtime adapters. This keeps each script testable in isolation and prevents behavior drift across layers.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:174:- **[milestone:M013]** [2026-04-22] Additive-helper-append preserves byte-identity for cross-phase shared libs: scripts/integrations/github-common.sh extended P02 (12 helpers) → P03 (gh_marker_search_remote + manifest_footer 4th-arg additive-suffix) → P04 (http_probe + sidecar_update_item_cache + emit_tier1_record + classify_gh_rc + emit_conversus_gate_record). Every extension lands at end-of-helpers before the self-check block; prior function bodies stay byte-identical so earlier-phase byte-identity gates continue to pass. Optional-arg additive-suffix on existing helpers (manifest_footer 3→4 args) preserves existing call-site behavior.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:175:- **[milestone:M013]** [2026-04-22] Gate-evolution-on-legitimate-advancement: when a later phase legitimately fills earlier-phase scaffolding (D015-style _deferred to PNN_ markers), the earlier-phase assertion evolves to accept BOTH pre/post shapes rather than fracturing the suite by duplicating hashes in a new gate. Byte-identity hashes on content NOT being advanced stay pinned as load-bearing invariants. P02→P03 (mapping table cells) and P03→P04 (reference doc TODO stubs) both applied this. The single source of byte-identity truth (the original gate) stays authoritative; later gates do not re-embed pinned hashes.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:178:- **[milestone:M013]** [2026-04-22] PATH-shimmed fake-gh enforces zero-subprocess-call invariant in CI: SC-7 auto-mode safety requires that 'github-init.sh' / 'github-sync.sh' make ZERO 'gh' calls when running without TTY + --i-am-operator. The verify gate prepends a tempdir containing a fake 'gh' executable to PATH that logs every invocation to a file, runs the command, then asserts the call log is empty. Catches accidental gh calls from new code paths immediately. Pattern extends to any shell-script invariant of the form 'must NOT invoke <external-binary>'.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:179:- **[milestone:M013]** [2026-04-22] Fixture-copy-before-live-gate: when a script reassigns ORCHESTRATOR_ROOT (e.g., github-sync.sh sets it to $PROJECT_ROOT/.orchestrator), live-mode gates MUST 'cp -R fx/. tmp/' the fixture orchestrator-state to a tempdir before invoking — running against the repo fixture tree directly pollutes it with sidecar updates and observability JSONL records. Pair with M013_GH_STUB_DIR pointing at the fixture's gh-stub-responses subdir so the script never touches real network.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:3:Consolidated from M001 implementation. Read before any orchestrator development.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:52:| D001 | arch | tasks.md phases map 1:1 to orchestrator slices | Boundary maps and must-haves align naturally; no transformation needed |
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:64:- `build-context.sh` supports both orchestrator-root and fixture-mode layouts
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:67:- Pause detection: file-based flag (`.specify/orchestrator/pause-requested`), not signals
  - knowledge-md :: (capped at 5 hits — more may exist)
  - milestone-summaries :: .orchestrator/milestones/M002/M002-SUMMARY.md:10:  - "spec-kit >=0.1.0, Bash 3.2+, existing orchestrator infrastructure from M001"
  - milestone-summaries :: .orchestrator/milestones/M002/M002-SUMMARY.md:20:  - ".specify/orchestrator/milestones/M002/phases/P01/P01-SUMMARY.md, .specify/orchestrator/milestones/M002/phases/P02/P02-SUMMARY.md, .specify/orchestrator/milestones/M002/phases/P03/P03-SUMMARY.md, .specify/orchestrator/milestones/M002/phases/P04/P04-SUMMARY.md, .specify/orchestrator/milestones/M002/phases/P05/P05-SUMMARY.md, .specify/orchestrator/milestones/M002/phases/P06/P06-SUMMARY.md, .specify/orchestrator/milestones/M002/phases/P07/P07-SUMMARY.md"
  - milestone-summaries :: .orchestrator/milestones/M003/M003-SUMMARY.md:12:  - "M009 (recipe packaging can assume .orchestrator/ default target); future milestone-management work"
  - milestone-summaries :: .orchestrator/milestones/M003/M003-SUMMARY.md:14:  - "scripts/migrate/migrate.sh,scripts/migrate/adapter-interface.sh,scripts/migrate/adapters/gsd2.sh,scripts/migrate/adapters/gsd1.sh,scripts/migrate/adapters/speckit.sh,scripts/migrate/transform/knowledge.sh,scripts/migrate/transform/knowledge-index.sh,scripts/migrate/transform/decisions.sh,scripts/migrate/transform/requirements.sh,scripts/migrate/transform/milestone-tiering.sh,scripts/migrate/transform/active-milestone.sh,scripts/migrate/transform/milestone-rollup.sh,scripts/migrate/transform/telemetry-aggregator.sh,scripts/migrate/transform/report.sh,scripts/migrate/lib/idempotency.sh,scripts/migrate/lib/sqlite-reader.sh,scripts/migrate/lib/json-fallback.sh,scripts/migrate/lib/category-mapper.sh,scripts/migrate/lib/supersession-chain.sh,scripts/migrate/lib/scope-tag.sh,scripts/migrate/lib/decision-numbering.sh,scripts/migrate/lib/category-inferrer.sh,scripts/migrate/lib/error-handler.sh,scripts/orchestrator/status.sh,commands/migrate.md,tests/fixtures/m003-p08-gsd-minimal/,tests/integration/test-m003-e2e-migration.sh"
  - milestone-summaries :: .orchestrator/milestones/M003/M003-SUMMARY.md:8:  - "Migration tool for GSD2/GSD v1/spec-kit → orchestrator format: adapter interface (P01), knowledge/decisions/requirements/milestone transforms (P02-P04), GSD v1 + spec-kit adapters (P05), end-to-end CLI with idempotency + reporting (P06). Refit (P07-P08) aligns migration output with M007 knowledge graph and M008 5-rule state resolver: resolver-wired target root, dual-root idempotency, post-migration graph rebuild, AD-13/14/15 documented, synthetic + live E2E validation with 8 AD-19-safe verify scripts + integration test (all green)."
  - milestone-summaries :: .orchestrator/milestones/M008/M008-SUMMARY.md:28:M008 transformed the orchestrator from a spec-kit extension into a standalone multi-runtime extension with adaptive process intensity. Across 7 phases (P01-P07), delivered: (1) Adaptive Intensity Engine — analyzes task descriptions along scope/risk/complexity axes and recommends Quick/Standard/Full intensity with environment capability awareness. (2) Backend-Agnostic Dispatch Interface — uniform entry point with filename-based adapter routing, two local backends (Claude Code Agent tool + Codex CLI SDK), structured result/error schemas with distinct exit codes. The explicit seam for M010's cloud backends. (3) Intensity-Aware Pipeline Scaling — central 7×3 stage×intensity matrix wires intensity awareness into every pipeline stage (discuss, research, plan-phase, dispatch, verify, knowledge, auto) with atomic mid-workflow override. (4) State & Namespace Independence — 5-rule root resolver, unified config, hard migration tool, surgical derive-phase refactor, namespace aliases. Full workflow runs standalone without spec-kit (SC-004 validated). (5) Runtime & Format Adapters — auto-detection + 3 runtime adapters (claude-code/codex/cursor) with HOME/project-dir guards + 2 format adapters (native round-trip, spec-kit one-directional read). (6) Multi-Runtime Packaging — open-standard SKILL.md format, 12 skills generated from commands, installable bundle, 3 runtime installers delegating to adapters, offline-safe version checker. (7) Init, Onboarding & Spec-Kit Bridge — orchestrator:init detects project (10 languages + 9 frameworks + 6 CI systems + 9 tools), generates config + runtime-appropriate instruction file (CLAUDE.md/AGENTS.md/.cursor/rules), installs skills. Reinit preserves user-edited custom blocks via comment markers. Full onboarding completes in ~1 second wall-clock (SC-005 validated with 2 orders of magnitude margin vs 5min target). Cross-cutting patterns: hermetic-first testing (mktemp fixtures, never touch real HOME), comment-aware Bash 3.2 compat, filename-based auto-discovery mirrored from P02 through P05/P06, thin delegation pattern, user-edit preservation via comment-delimited blocks. M008 is the foundation for M009 (debug protocols) and M010 (cloud dispatch via Managed Agents).
  - milestone-summaries :: .orchestrator/milestones/M012/M012-SUMMARY.md:10:  - "populated .orchestrator/ tree (constitution, DECISIONS.md, KNOWLEDGE.md, milestone-summary.md, milestones/, archive/); populated knowledge/patterns+conventions+lessons/MEM corpus; mkdocs + mkdocs-material + mkdocs-include-markdown-plugin + pymdown-extensions Python toolchain (version-pinned in wiki/requirements.txt); GitHub Pages + GitHub Discussions enabled on the repo for the operator-gated first deploy; gh CLI for wiki-giscus-remap.sh live path; M011 spec-ingest pipeline produced the 022-spec-wiki feature spec; M021 anti-pattern-lint + pre-bash-shape-guard + widened allow-list gates"
  - milestone-summaries :: .orchestrator/milestones/M012/M012-SUMMARY.md:18:  - "scanner-to-stubs-to-nav three-stage pipeline with additive append-only emission blocks keeping downstream generators stable across phases; marker-bounded atomic writes to shared config files with shasum byte-identity verification across edits; include-only SSOT with spec-quote exclusion for task-plan and payload files; pure-string Bash 3.2 primitives (PID-suffixed /tmp list files as associative-array replacement; IFS-slash positional-parameter normalize_path with no realpath dep; parallel indexed-array pattern registries; mktemp list-file + while-IFS-read as find-pipe-while replacement); SKIP-as-PASS at gate boundaries for optional toolchain (mkdocs / built site / phase-close summary); phase-suite orchestrator parallel-indexed-variable pattern with eval indirection and stderr-indent-on-FAIL; cross-phase allow-list extension as sibling-gate maintenance; self-scan carve-out via assignment-line regex skip in compat gates; MEM004 carve-out for scanner and diagnostic and wrapper internals with single-script-file Check-layer shape; loud-fail-over-silent-empty convention for missing external config; chained-gate deploy wrapper with first-non-zero-aborts semantics and fixed-verb per-gate output (GATE/BUILD/DEPLOY/DRY-RUN/OK/FAIL); graceful-absent-tool and graceful-absent-artifact patterns with dry-run-exit-0 and live-path-exit-1 split; pending-sentinel artifact for operator-gated outcomes; D011 mechanical-evaluation record as first-class phase artifact; EOF-append pattern for multi-author operator guides preserving prior section byte-identity; heading-as-section-boundary convention for README (distinct from mkdocs.yml marker-comment convention); theme-override as AD-3-compliant injection surface (append-at-render vs body-rewrite); scanner-presence-gated subtree emission (Archive / Knowledge Entries disappear cleanly when scanner sees zero records); stderr-noise-stdout-success diagnostic convention for machine-readable capture"
  - milestone-summaries :: .orchestrator/milestones/M012/M012-SUMMARY.md:30:M012 ships the dogfood spec→wiki loop: a deployable MkDocs Material site that projects every in-scope `.orchestrator/**.md` artifact plus the `knowledge/**/MEM*.md` corpus through a single-source-of-truth include pipeline, overlays per-page Giscus threads, and deploys via a chained wrapper that runs four pre-deploy gates before `mkdocs gh-deploy`. The milestone's vision was "a dogfood-only MkDocs + Giscus site that renders `.orchestrator/` artifacts as a navigable, commentable wiki with one-command deploy" — and that is exactly what landed across four phases (P01 → P02 → P03 → P04), 37 verification gates in total, and a D011 mechanical evaluation that promoted **M020** to a committed milestone.
  - milestone-summaries :: .orchestrator/milestones/M012/M012-SUMMARY.md:40:- **P01 verify suite** — 9 gates (`wiki-self-contained`, `requirements-pinned`, `include-plugin`, `ssot`, `exclusion-policy`, `nav-structure`, `serve-smoke`, `index-placeholder`, `bash32-compat`) + `m012-p01-phase-suite.sh` orchestrator. `serve-smoke` SKIPs gracefully when `mkdocs` is absent.
  - milestone-summaries :: .orchestrator/milestones/M012/M012-SUMMARY.md:8:  - "deployable MkDocs Material wiki projecting .orchestrator/**.md + knowledge/**/MEM*.md via include-markdown SSOT pipeline; scripts/wiki/ three-stage pipeline (wiki-scan-sources.sh + wiki-generate-stubs.sh + wiki-generate-nav.sh) with additive-extension invariant + marker-bounded atomic nav/extra splices in wiki/mkdocs.yml; wiki/overrides/partials/comments.html Material theme override for per-page Giscus threads keyed by pathname (AD-5); scripts/diagnostics/ Bash 3.2 diagnostic trio (wiki-link-check.sh built-site walker, wiki-giscus-config-check.sh loud-fail pre-build gate, wiki-giscus-smoke.sh post-build walker, wiki-giscus-remap.sh idempotent thread remap); scripts/wiki/wiki-deploy.sh chained deploy wrapper (4 pre-deploy gates + mkdocs gh-deploy, --dry-run/--help/--root/--skip-smoke, exit 0/1/2, GATE/BUILD/DEPLOY output lines); wiki/docs/index.md finalized 65-line orientation home page with SSOT paragraph-overlap guard; wiki/README.md consolidated 420-line operator guide (install/preview/regenerate/scope/link resolution/Giscus mapping/remap/first-deploy checklist/deploy wrapper); .orchestrator/milestones/M012/phases/P02/D011-EVALUATION.md structured 1-of-3-criteria record promoting M020; .orchestrator/milestones/M012/phases/P04/DEPLOY-RECORD.md first-deploy record contract with pending-sentinel path for operator-gated completion; 37 verification gates across 4 phase-suite orchestrators (P01 9 + P02 9 + P03 8 + P04 11)"
  - milestone-summaries :: .orchestrator/milestones/M012/M012-SUMMARY.md:94:- **P01 phase-suite** — `scripts/verify/m012-p01-phase-suite.sh` — **9/9 PASS** (at P02/P03 close; P04 close shows 2/9 due to pre-existing nav regen + placeholder removal — phase-close consolidation concerns, not P04 regressions).
  - milestone-summaries :: .orchestrator/milestones/M013/M013-SUMMARY.md:16:  - "D014 — Knowledge-Layer Boundary: M020 retains chunk-ID authority; M013 cross-links the M020 schema and never redefines/extends it (zero writes to knowledge/spec/** from any P01–P04 task); FR-9 additive-only emit pinned to existing SPEC-* frontmatter; D015 — P02/P03 scope split applied as load-bearing markers (_deferred to P03_ cells fillable in place); FR-12 Claude-Code-only v1 binding via Constitution X+XII (Codex/Cursor parity demand-driven post-launch via M010); projection-not-peer grounding via XIV+M007+D007 (sidecar operator-owned + gitignored; in-repo template); FR-11 reversibility (all writes have absent/pending exit paths; lock released before exit 3/4); FR-16 rate-limit pre-flight + auth-expiry exit-code contract; FR-17 cost emission in M019 Tier 1 shape with source=runtime hard-coded; FR-18 status --verify-cache as read-only divergence probe; AD-19 single-script-file Check shape preserved across all verify gates with comment-discipline synonyms keeping bash32 scanners self-clean; conversus adapter --strict mode added as reusable invariant for callers requiring adapter presence (M011/P07 graceful-degradation flipped off via --strict / CONVERSUS_STRICT=1)"
  - milestone-summaries :: .orchestrator/milestones/M013/M013-SUMMARY.md:18:  - "additive-helper-append-preserves-byte-identity (P02 12 + P03 1 + P04 5 helpers appended at end-of-helpers; prior bodies untouched); gate-evolution-on-legitimate-advancement (P02→P03 mapping table; P03→P04 reference extensions — earlier-phase assertions evolve to accept both pre/post shapes; byte-identity hashes on unchanged content stay pinned); pinned-SHA-with-awk-range-capture-before-edit (multi-phase additive doc editing with three pinned ranges in references/github-integration.md); FR-12-v1-negative-grep-guard (sibling-runtime byte-identity via negative greps for post_verify/FR-12 markers — cheaper than pinned-sha and self-repairs if codex/cursor gain legitimate content under different markers); AWK-based JSON upsert + count + first-scalar extraction (no jq hard-dep across all 4 phases); M013_GH_STUB_DIR fixture-driven preflight + marker-search stub-selector (env-var-driven stub paths exercise every branch deterministically without gh calls); PATH-shimmed fake-gh for zero-subprocess-call invariant enforcement (auto-mode SC-7 enforcement logs every gh call + asserts log empty); per-call-stderr-mktemp-classify-rm (capture stderr to mktemp + classify_gh_rc rc + file + rm -f on all paths; releases lock before exit 3/4); direct-cmd-subst-rc-propagation (assign $() then read $? on next line; '|| true' in cmd-subst tail silently zeros $? — caught mid-T03); external-watchdog-plus-adapter-internal-timeout (Constitution XII belt-and-suspenders kill -TERM at TIMEOUT then kill -KILL at TIMEOUT+1s); verdict-rc-parity (rc=0 PASS / rc=2 BLOCK / rc=1 ERROR passed verbatim so BLOCK is distinguishable from error); pending-sentinel bootstrap with clobber-refusal exit 2 (extends M012/P04 DEPLOY-RECORD convention); fixture-copy-before-live-gate (cp -R fx/. tmp/ before invoking github-sync.sh because it reassigns ORCHESTRATOR_ROOT and would pollute repo fixture tree); stdout-record + stderr-summary split (fixture diffs stay byte-identical while operator still sees per-run summary); buffered-manifest-body via mktemp tempfile during walker iteration (counts finalized before header emission); FORMAT STABILITY CONTRACT doc-block at helper definition site (pins shape for downstream P03/P04 consumers); section-content byte-identity gate via awk-extract by literal-prefix heading match + shasum compare against embedded hashes (robust against insertion-shifts); load-bearing _deferred to P0N_ cells functioning as scope-boundary markers a later phase replaces in place (D015 split); comment-discipline synonyms in bash32 scanners (assoc-arrays / array-from-stdin builtins / case-conversion expansion / combined-redirect shorthand)"
  - milestone-summaries :: .orchestrator/milestones/M013/M013-SUMMARY.md:47:- **FR-11 reversibility** — All writes have absent/pending exit paths: `status --verify-cache` short-circuits to pending-operator-complete on absent OR pending sidecar; `perform_upsert` releases the lock before rate-limit/auth-expiry exits; post-verify hook fail-as-warning never blocks the dispatch loop.
  - milestone-summaries :: .orchestrator/milestones/M013/M013-SUMMARY.md:49:- **`write-summary.sh --field-file=`** — added to honor AD-19 shape safety for long milestone/phase bodies (no multiline quoted CLI args).
  - milestone-summaries :: .orchestrator/milestones/M013/M013-SUMMARY.md:59:- **PATH-shimmed fake-gh** — auto-mode SC-7 zero-`gh`-write invariant enforcement: tests log every `gh` invocation and assert the log is empty.
  - milestone-summaries :: .orchestrator/milestones/M014/M014-SUMMARY.md:40:**Side-fixes bundled along the way**: `auto-loop.sh --step=V` parser scope fix (M026/P02 bbt-companion dogfood — was extracting informational backticks in Inputs/Constraints sections as commands); `read-roadmap.sh` Risk-field truncation (M026/P02 — multi-word Risk values like "Risk: low — prose" now parse cleanly); `auto-loop.sh` slug-bearing `T##-<slug>-PLAN.md` support (M014/P03/T01 dispatch — the one consumer that used literal-concat path rather than `T*-PLAN.md` glob).
  - milestone-summaries :: .orchestrator/milestones/M015/M015-SUMMARY.md:10:  - "from:M008 what:5-rule state-root resolver, standalone runtime adapters, multi-runtime packaging, orchestrator:init onboarding — the substrate the cutover operates on; from:M003 what:migration tool + spec-kit adapter + orchestrator-auto-in-Claude-Code-native validation (P07/P08) — source of the migration adapter kept after cutover and source of the auto-behavior evidence the cutover relies on per spec assumption; from:M007 what:no-graceful-degradation discipline — governs the hard-cutover approach; no dual code paths, no feature flags, no compat shims"
  - milestone-summaries :: .orchestrator/milestones/M015/M015-SUMMARY.md:16:  - "Hard-delete discipline (M007 governance): git rm for tracked files; no legacy/ rename; no compat shim; verify scripts use file-absence assertions — applied uniformly across P01 deletions and P02 resolver bridge removal; FR-013 migration-adapter preservation treated as orthogonal to host removal: scripts/migrate/, scripts/state/detect-speckit.sh, scripts/dispatch/adapters/format/speckit.sh, commands/migrate.md untouched — verified via empty git diff --stat at P01 and P02 close; Resolver reduced from 5 rules to 4 by outright deletion of the .specify/orchestrator/ bridge (Rule 4), not feature-flagged; former rule 5 renumbered; D003 recorded the removal of the redundant state_root: line from .orchestrator/config.yml (conflicted with Rule 3 directory existence); P01 opportunistic preflight fix: evaluate-preflight.sh argument-passing bug (missing --project-root flag to generate-permissions.sh / write-permissions.sh) patched in the same edit pass; P03 ALLOW_P03_DOCS allow-list sealed with never-match sentinel __P03_COMPLETE_NEVER_MATCH__ after docs graduated — converts transitional tolerance into permanent regression guard; P04 clean-clone simulation uses working-tree tar (rather than git archive HEAD) because deletions live only in the working tree pre-commit; fixture paths excluded from the extension-host artifact probe; Stream 3 migration-adapter invocation adapted inline: migrate.sh exposes --path/--output (not --source/--target); constitution last-mile copy materialized in-transcript since adapter extracts to intermediate .dat format"
  - milestone-summaries :: .orchestrator/milestones/M015/M015-SUMMARY.md:18:  - "Pre-change gate scaffolding (validation-as-task MEM011): write verify scripts before the changes they assert, so every downstream task has an immediate pass/fail signal — applied in every M015 phase (P01 removal gates, P02 migration gates, P03 reframe gates, P04 validation gates); Snapshot-based immutability check: capture at 'first ## [' pre-edit and verify starting at 'second ## [' post-edit — handles prepend-only append patterns (new CHANGELOG entry) without re-flagging the historical tail as modified; Seal-by-sentinel allow-list retirement: replace regex body with __NAME_NEVER_MATCH__ when a transitional tolerance has served its purpose; keeps negated-grep syntax valid while tolerating nothing; Hard-delete cutover discipline: git rm for tracked files; no legacy/ rename; no compat shim; verify scripts use file-absence assertions to gate the cutover; Framing-verifier-friendly migration callouts: describe the relocation without literally naming the legacy path so the no-legacy-install grep stays clean while the narrative stays informative; Config file is override, not canonical-location declaration: Rule 3 directory existence is authoritative; state_root: line in config.yml conflicts with Rule 3 — preferred pattern is an empty or commented-example config; Four-stream evidence capture for validation phases: test-suite + doctor + migration-adapter-against-fixture + clean-clone-shape, each with a named marker line (ALL_SUITES_PASS / DOCTOR_CLEAN / MIGRATION_SUCCESS / CLEAN_CLONE_OK) the gate verifier keys on; Transcript-inline adjustment documentation: when a plan's speculative invocation shape diverges from actual CLI shape, document the divergence IN the evidence transcript so post-hoc reviewers can audit the call; Allow-list with documented reason for regression-guard scripts: verify scripts whose purpose is to PROVE absence of legacy-path regression must reference those paths in their assertions — explicitly allow-list with a comment naming the regression they guard"
  - milestone-summaries :: .orchestrator/milestones/M015/M015-SUMMARY.md:44:### P01 — Spec-Kit Host Removal (78m)
  - milestone-summaries :: .orchestrator/milestones/M015/M015-SUMMARY.md:46:The repo stopped registering itself as a spec-kit extension. `extension.yml`, the 9 dogfooded `/speckit.*` slash commands, the 5 `.specify/scripts/bash/` helpers, the 6 spec-kit-style root templates, and the `.specify/templates/commands/` tree were all hard-deleted (`git rm`, no rename, no compat shim). The 3 extension-shape test artifacts (`m002-p07-extension-registration.sh`, `tests/fixtures/verify-{pass,fail}/extension.yml`) were deleted. A latent bug in `evaluate-preflight.sh` (positional args instead of `--project-root` flag to `generate-permissions.sh` / `write-permissions.sh`) was fixed opportunistically — preflight now reports `permissions=generated` instead of `permissions=error`. Seven verify scripts (`scripts/verify/m015-p01-*.sh`) prove each removal class + the preflight fix. T04 swept 30+ non-exempt retained files replacing `extension.yml` project-root sentinels with `.git` / `.orchestrator` / `.specify/orchestrator` markers, rewrote `tests/test-s01-structure.sh` and `test-s07-integration.sh` to drop manifest assertions while keeping directory + cross-ref + capability-detection checks, and hard-deleted 8 dead extension-registration verify scripts plus the `.specify/scripts/powershell/` mirror tree.
  - milestone-summaries :: .orchestrator/milestones/M015/M015-SUMMARY.md:58:Seven P04 verify scripts were authored (all-tests-pass, doctor-clean, speckit-migration-works, clean-clone-shape, verification-complete, milestone-summary-present, evidence-captured) along with a deterministic spec-kit migration fixture under `tests/fixtures/m015-p04-speckit-migration/`. Four validation streams executed: (1) all 8 `tests/test-s*.sh` suites run and pass with no skips, transcript at `.orchestrator/milestones/M015/phases/P04/evidence/test-suite-transcript.txt` (marker `ALL_SUITES_PASS`); (2) `scripts/diagnostics/run-doctor.sh` exits 0 with no FAIL lines, report at `doctor-report.txt` (marker `DOCTOR_CLEAN`); (3) the spec-kit migration adapter produces a valid `.orchestrator/` from a spec-kit-shaped fixture, transcript at `migration-adapter-transcript.txt` (marker `MIGRATION_SUCCESS`, with inline documentation of the `--path`/`--output` CLI shape adaptation and the constitution last-mile copy); (4) a working-tree tar extraction confirms zero extension-host artifacts, transcript at `clean-clone-shape.txt` (marker `CLEAN_CLONE_OK`). T03 authored `M015-VERIFICATION.md` scoring FR-001..FR-019 all PASS with cited evidence pointers and one non-material deviation (FR-016 spec text says "7 test suites" but 8 exist; intent — no-skip — is satisfied). T04 (this task) authored `M015-SUMMARY.md`.
  - milestone-summaries :: .orchestrator/milestones/M020/M020-SUMMARY.md:16:  - "D024,AD-1,AD-2,AD-3,AD-4,AD-5,AD-6,AD-7,AD-8,FR-1,FR-2,FR-3,FR-4,FR-5,FR-6,FR-7,FR-8,FR-9,FR-10,CON-1,CON-2,CON-3,CON-4,CON-5,THREAT-006,THREAT-007,SC-1,SC-2,SC-3,SC-4,SC-5,SC-7,SC-8,US-1,US-2,US-3,US-4,US-5,OQ-1,OQ-2,OQ-3,OQ-4"
  - milestone-summaries :: .orchestrator/milestones/M029/M029-SUMMARY.md:16:  - "AD-1 single-resolve invocation context (TTY/CI/runtime/non_interactive resolved once via detect-invocation-context.sh three-line env block); AD-2 unconditional ANSI strip in JSON sections (render-status-json.sh single ANSI-strip site); AD-3 four-priority non-interactive policy (auto_proceed:true default + --no-confirm override + standard/full intensity gate + leave-marker-absent failure semantics); AD-4 SC-8 oracle wrapper amendment (predictive-surface.sh → summarize-milestone.sh per CON-3 knowledge-layer-boundary; M029 owns its own helper; Spec Amendment Record entry on specs/037-roadmap-visibility-cli-ux/spec.md); AD-5 display_thresholds.compression_savings_pct:5.0 heuristic-default + FR-8 review trigger (re-evaluate post-10-milestone median savings ≥ 3%); AD-6 cross-milestone milestones: frontmatter list + reverse-lookup advisory; AD-7 schema_version:1.0 from day 1; AD-8 knowledge-layer-boundary discipline (no M020 schema change; unit_close consumes existing M019 emitter); AD-9 sentinel-file mechanism for SC-14; scope tightening 2026-05-05 (cut FR-11 GitHub fold-in + FR-12 --refresh-github; defer to demand-driven post-launch external-tool-adapters); #Q-1 full-re-render; #Q-3 leave-marker-absent failure semantics; #Q-5 collapsed-by-default + --expand-all + active-always-expanded; #Q-G8 canonical compact-form ▽ saved Nk invariant; #Q-G9 p95 ≤ 1.0s live-tail latency methodology; CON-1/FR-14 read-only with documented closure-ceremony write sites; CON-3 silent FR-6 cost-column suppression; CON-4/FR-11 no-GitHub-API anti-coupling; CON-7/AD-8 read-only-consumer discipline; AD-19 straight-line bash; MEM001 Bash 3.2 compatibility; MEM004 carve-out for awk/sed/grep pipes inside renderer body"
  - milestone-summaries :: .orchestrator/milestones/M029/M029-SUMMARY.md:38:- **P03 (live + preflight + auto-chain)** — `--live` branch on `render-position.sh` (FR-7, #Q-1 full-re-render on every appended `dispatch_usage` record, #Q-G9 `p95 ≤ 1.0s` measurement methodology via `measure-live-tail-latency.sh` with three-tier nanosecond-clock portability shim); canonical compact-form `▽ saved Nk` marker (FR-8, #Q-G8 — verbose-suffix forms forbidden) gated by AD-5 `display_thresholds.compression_savings_pct: 5.0` config knob (templates/orchestrator-config-default.yml + scripts/state/read-config.sh VALID_KEYS extension + references/file-formats.md docs + FR-8 review trigger); `commands/auto.md ## Preflight Summary` section (FR-9, AD-3 four-priority non-interactive policy, AD-4 oracle wrapper); `commands/start.md --auto-chain` flag + `scripts/lifecycle/start.sh` chain-driver passthrough (FR-10, #Q-3 leave-marker-absent failure semantics, AUTO_CHAIN_STAGE_STUB fixture-only escape hatch); `tests/m029-acceptance/measure-live-tail-latency.sh` harness; SC-7 / SC-8 / SC-9 / SC-10 fixtures + acceptance scripts; AD-4 Spec Amendment Record entry on `specs/037-roadmap-visibility-cli-ux/spec.md`; SC-11 milestone-grain `run-acceptance-battery.sh` chaining all three per-phase batteries + emitting `BATTERY: pass=14 fail=0`; SC-12 `m029-p03-validate-milestone-pass.sh` hook asserting `validate-milestone.sh M029 VALIDATE: PASS` exit 0; closure ceremony (this `M029-SUMMARY.md` + `M029-VALIDATED` marker + milestone-grain `unit_close` JSONL record auto-emitted by `write-summary.sh milestone`). Six tasks, thirteen-gate phase suite PASS at close-time.
  - milestone-summaries :: .orchestrator/milestones/M029/M029-SUMMARY.md:56:- **Verifier-contract-over-verifier-skeleton + in-flight-repair** (M032 lineage extended) — When plan-stated expectation conflicts with on-disk reality, ship the contract by manually preparing inputs the contract expects or by adopting the actual-emitter shape. T06 closure: write-summary.sh emits `record_type:"unit_close"` not the plan-stated `event:"unit_close"`; closure verifier asserts on actual emitter shape. P01-SUMMARY directory references repaired (trailing-slash convention) in T06 alongside the closure ceremony.
  - milestone-summaries :: .orchestrator/milestones/M029/M029-SUMMARY.md:8:  - "M029 (roadmap visibility & CLI UX) closes the orchestrator's invocation-context + tree-rendering + auto-chain onboarding surfaces. P01 foundation: scripts/state/detect-invocation-context.sh AD-1 single-resolve env block + references/status-headline-shape.md FR-2 + references/status-json-schema.md FR-3 + commands/status.md headline block + scripts/diagnostics/render-status-json.sh AD-2 unconditional ANSI strip + commands/context.md FR-4 read-only single-screen runtime profile skill. P02 at-rest tree: scripts/diagnostics/render-position.sh ~470-line tree renderer engine FR-5 four-glyph alphabet + commands/where.md skill + references/cross-milestone-feature-shape.md AD-6 contract + scripts/diagnostics/summarize-milestone.sh AD-4 SC-8 oracle wrapper + SC-5/6/13/14 fixtures + AD-9 sentinel-file mechanism + FR-6 silent pre-M019 cost-column suppression. P03 live + preflight + auto-chain: --live branch on render-position.sh FR-7 + #Q-G9 p95 ≤ 1.0s methodology via measure-live-tail-latency.sh three-tier nanosecond-clock portability shim + ▽ saved Nk canonical compact-form FR-8 #Q-G8 gated by AD-5 display_thresholds.compression_savings_pct config knob + commands/auto.md ## Preflight Summary FR-9 AD-3 four-priority non-interactive policy AD-4 oracle wrapper + commands/start.md --auto-chain flag + scripts/lifecycle/start.sh chain-driver passthrough FR-10 #Q-3 leave-marker-absent failure semantics AUTO_CHAIN_STAGE_STUB fixture-only escape hatch + SC-7/8/9/10 fixtures + AD-4 Spec Amendment Record on specs/037-roadmap-visibility-cli-ux/spec.md + SC-11 milestone-grain run-acceptance-battery.sh BATTERY: pass=14 fail=0 + SC-12 validate-milestone-pass.sh VALIDATE: PASS hook + closure ceremony M029-VALIDATED + M029-SUMMARY.md + milestone-grain unit_close JSONL record. Three phases all green; acceptance battery pass=14 fail=0; validate-milestone.sh M029 reports VALIDATE: PASS — 101/101 checks passed."
  - milestone-summaries :: .orchestrator/milestones/M029/M029-SUMMARY.md:84:- **AD-3 four-priority non-interactive policy** — `auto_proceed: true` default + `--no-confirm` override + standard/full intensity gate + #Q-3 leave-marker-absent failure semantics.
  - milestone-summaries :: .orchestrator/milestones/M029/M029-SUMMARY.md:99:- **M035 P00+P01 next**: dev-ergonomic prep before launch (`--mode=symlink` install + `orchestrator:status` version-drift warning) + multi-consumer-project freshness today; M035 P02–P06 IS the launch event (npm + homebrew + curl-pipe-bash publishing pipelines, GH release automation, install-script integrity, `orchestrator:update` first-class command). Operator note: invoke as `orchestrator:auto milestone=M035` to bypass the M036b deferred-state paper-cut.
  - milestone-summaries :: .orchestrator/milestones/M035/M035-SUMMARY.md:8:  - "Pre-launch dev-ergonomics layer (P00) — installer hardening across the three claude-code/codex/cursor installers (bash 3.2 process-substitution exit-status capture replacement; managed .gitignore block primitive; wiki-stubs-fresh diagnostic + pages.yml pre-build gate; M032 SC-5 fixture-completeness fallback in wiki-init.sh --deploy; D-RN-1 npm-name collision-check evidence). Symlink-mode + drift-line surface (P01) — user-facing --mode=copy|symlink flag promoted from M032's TEST-ONLY --asset-mode-override; symlink branch retargeted directly at $REPO_ROOT/<src> (US-1 dogfood-velocity contract — git pull updates every consumer instantly); install-meta.txt extended with always-present commit_sha= + version= fields; mode-aware uninstall (-L test for symlink-mode, -f test for copy-mode); read-only check-orchestrator-drift.sh helper + status-line drift surface in both TUI + JSON. Spec-kit-orchestrator → orchestrator namespace rename (P01.5) — D-RN-1..D-RN-7 decision block (anchors dr-code-029..dr-code-035); reversible pre-rename git tag v0.9.2-final-spec-kit-name; specs/001-speckit-orchestrator → specs/001-orchestrator git-mv with full history preservation; C1/C2/C3/C4/C5/C6 sweep cohort across 158+ files / ~308 occurrences. npm publishing channel (P02) — package.json + bin/orchestrator with @build-fractal/orchestrator (D-RN-1); engines.node>=14 + os: [darwin,linux] (D003 / MIT-9 Windows fail-closed); packaging/npm/postinstall.sh thin delegate (INIT_CWD-aware + Claude Code soft-fail + DRY_RUN=1); Constitution Principle XVI cross-channel byte-equivalence test (CON-5, single-channel reflexive at P02 close); .github/workflows/release.yml two-job split with CON-6 defense-in-depth (structural job-condition + runtime negative-assertion step); per-subdir .npmignore as load-bearing dogfood-exclusion layer when files: whitelist is present. Homebrew tap publishing channel (P03) — packaging/homebrew/orchestrator.rb.tmpl + render-formula.sh; release.yml homebrew-publish job needs:npm-publish (D007 single-source-of-truth tarball — no separate brew-tarball); HOMEBREW_TAP_TOKEN fine-grained PAT scoped to Build-Fractal/homebrew-orchestrator:contents:write only (D008); cross-channel byte-equivalence lifted to load-bearing 2-way (NPM_HASH = HOMEBREW_HASH). Curl-pipe-bash channel + 3-way byte-equivalence (P04) — packaging/install/install.sh bash 3.2 / POSIX-sh-safe runtime-detect + tarball-fetch + dispatch wrapper hosted at the GitHub release asset URL (D009); job-level timeout-minutes: 20 with CON-8 escalation clause (D010); D011 manual stable releases pre-1.0 cadence; cross-channel byte-equivalence lifted to load-bearing 3-way (NPM_HASH = HOMEBREW_HASH = CURL_HASH). Install-script integrity (P05) — sigstore keyless signing + SHA-256 fallback in npm-publish job (D004 resolves spec #Q-3); job-level permissions:id-token:write override; rollback marker contract (D005 — five-field key=value sidecar plus snapshotted manifest under .orchestrator/.rollback/); orchestrator:update --rollback dispatch with #Q-G8 verbatim symlink-mode refusal advisory; references/installation.md ## Verifying integrity operator-facing section (cosign verify-blob + shasum -c paths). Multi-source dispatch + milestone-grain acceptance battery (P06) — update_source: git|npm|homebrew|none config schema (D012); resolve_update_source + persist_update_source helpers + four-arm dispatch in run-update.sh (D014 AD-5 detect-by-install-method-first); emit_update_run_event + --no-emit-jsonl flag + 5-condition suppression matrix (D013 verbatim from M027); commands/update.md ## Update sources H2 with 4 H3 subsections; tests/m035-acceptance/run-acceptance-battery.sh milestone-grain acceptance battery (SC-15 self-reference); fixture quartet for SC-13; m035-p06-phase-suite.sh aggregator (summing-counters form); m035-p06-milestone-close-shape.sh + M035-VALIDATED + M035-SUMMARY.md + milestone-grain unit_close JSONL (SC-16 milestone-close oracle)"
  - milestone-summaries :: (capped at 5 hits — more may exist)

### Q4 — KEPT

- **question**: - #Q-4 (m019-cost-cadence): Does M019 Tier-1 JSONL emit at per-segment granularity in time for pre-spawn lease reads, or is `--output-format json` `total_cost_usd` the sole cost source?
- **disposition**: kept (empirical timing question — corpus has no cadence measurement; SC-3 precondition resolved by P01 spike; kept at discuss gate 2026-07-02)
- **terms** (12): Q-4, m019, cost, cadence, tier, jsonl, emit, segment, granularity, time, spawn, lease
- **hits**:
  - constitution :: .orchestrator/memory/constitution.md:120:  exploration-recovery costs, per the clarification below.)*
  - constitution :: .orchestrator/memory/constitution.md:139:- "Should work" is NOT evidence. "Tests passed last time" is NOT
  - constitution :: .orchestrator/memory/constitution.md:153:- Engine-managed scripts MUST emit structured events (`emit_event`)
  - constitution :: .orchestrator/memory/constitution.md:154:  and a final result (`emit_result`). A script that runs to
  - constitution :: .orchestrator/memory/constitution.md:155:  completion without emitting a RESULT line is treated as a silent
  - constitution :: .orchestrator/memory/constitution.md:21:  Previous: 2.1.0 → 2.2.0 (MINOR — Tier 2 inheritance declarations,
  - constitution :: .orchestrator/memory/constitution.md:24:  Added (Tier 2 inheritance, recorded in CONFORMANCE.md not as new
  - constitution :: .orchestrator/memory/constitution.md:26:    - Tier 2 XXII (Distribution Surface Integrity) — inherited from
  - constitution :: .orchestrator/memory/constitution.md:284:  timestamps.
  - constitution :: .orchestrator/memory/constitution.md:295:routing config, hooks config), not inferred by scripts at runtime.
  - constitution :: .orchestrator/memory/constitution.md:442:  structural home as release-time evidence rather than as an
  - constitution :: .orchestrator/memory/constitution.md:444:  the GH release automation workflow introduced in M035 runs each
  - constitution :: .orchestrator/memory/constitution.md:448:  Principle II's mechanical-gate requirement; the gate emits a
  - constitution :: .orchestrator/memory/constitution.md:456:    before and destroyed immediately after each release gate run.
  - constitution :: .orchestrator/memory/constitution.md:465:    resulting project's status command exits with code 0 and emits
  - constitution :: .orchestrator/memory/constitution.md:57:      install testing) from XXII's invariant body to release-time
  - constitution :: .orchestrator/memory/constitution.md:6:  .orchestrator/proposals/M0XX-tier-2-xxii-xii-substantive-followups.md;
  - constitution :: .orchestrator/memory/constitution.md:7:  Candidate A / Tier 2 XXII inheritance shape changes materially —
  - constitution :: .orchestrator/memory/constitution.md:9:  invariant body to constitution Quality Gates as release-time
  - constitution :: (capped at 5 hits — more may exist)
  - decisions :: .orchestrator/DECISIONS.md:113:- **Choice**: Revised sequence: **M011 → M012 → M013 → M014 → M019 → M018 → M009 → M010** (M019 inserted by D009). M018 adopts caveman-style (github.com/JuliusBrussee/caveman) token compression as a first-class pipeline stage aligned with Constitution Principle I (Context Minimization). Proposed phase outline (subject to detailed planning closer to kickoff): P01 compression grammar (what's safe to compress per artifact class; preserve frontmatter, code fences, paths, MEM IDs, commands, URLs), P02 dispatch compressor (optional filter in `scripts/dispatch/build-context.sh`, emits `payload.compressed.md` beside original), P03 intensity mapping (Quick→ultra, Standard→full, Full→lite/off via `scripts/engine/intensity-gate.sh`), P04 memory compression (opt-in `CLAUDE.compressed.md` / `KNOWLEDGE.compressed.md` with `.original` siblings), P05 agent output profile (inject terseness directive into payload prefix), P06 eval harness (3-arm port of caveman's evals against orchestrator payloads, gate on accuracy parity), P07 multi-runtime parity (Claude Code / Codex / Cursor). Differentiation vs. installing caveman as-is: roundtrip-safe by policy (Principle VI — originals authoritative, compress only at dispatch/load boundaries), grammar-aware preservation of orchestrator artifact structure, intensity-bound (not always-on), explicit opt-out surfaces (constitution, spec.md, PR descriptions, user-visible docs), and telemetry integrated with existing cost tracking so savings are measurable per dispatch.
  - decisions :: .orchestrator/DECISIONS.md:120:### Insert M019 (Observability & Efficiency Metrics) between M014 and M018 as a tiered sketch { #dr-code-009 }
  - decisions :: .orchestrator/DECISIONS.md:127:- **Choice**: Revised sequence: **M011 → [M019/Tier 1 emitter] → M012 → M013 → M014 → [M019/Tier 2+3] → M018 → M009 → M010**. Tier 1 ships as a small standalone unit once M011 is closed — avoids mid-milestone scope insertion (Constitution XV — Surgical Precision), gives a clean boundary for instrumentation to start, and still captures M012+M013+M014 (~3 full milestones of dogfooding) before M018 planning needs a baseline. Cost of not backfilling M011 dispatches: ~15–25 unlogged records from M011/P04–P07; acceptable tradeoff. M019 records time, token cost, estimated dollar cost, retries, deviations, verification pass rate, and domain-unit cost (per ingested spec / merged PR / auto-applied comment) at task/phase/milestone/project/all-time granularities. Three-tier framing: **Tier 1 — Just emit** (~1 day: append `payload_breakdown`, `dispatch_usage`, `unit_close` JSONL records to existing `execution-log.jsonl`; no UI; ships first thing after M011 close so M012–M014 dogfooding produces real data); **Tier 2 — Rollup + `orchestrator:cost` command** (~3 days: `scripts/diagnostics/metrics-rollup.sh`, `.orchestrator/metrics/*.jsonl` aggregates, `orchestrator:status` efficiency footer; worth it if we launch); **Tier 3 — Full polished surface** (~7 days: backend-specific `adapters/backend/*/report-usage.sh`, auto-generated `MNNN-METRICS.md` on consolidate, `orchestrator:doctor` anomaly checks, parity evals across runtimes; only if efficiency becomes M009 launch headline). Full M019 phase outline mirrors the tiers: P01 schema + pricing table, P02 dispatch-time emitter (Tier 1 boundary), P03 backend usage adapters, P04 rollup + `orchestrator:cost` command (Tier 2 boundary), P05 milestone close report, P06 status + doctor integration, P07 evals + multi-runtime parity (Tier 3 boundary). Schema must pair cost metrics with **quality metrics** (verification pass rate, deviation rate) from day one to avoid Goodhart failure mode — optimizing cheap over correct. All records labeled `source: "estimate" \
  - decisions :: .orchestrator/DECISIONS.md:128:- **Revisable**: Runtime cost of tracking is effectively zero (JSONL append + reuse of existing `estimate_tokens` function; no added LLM tokens — agent does NOT self-report, runtime hooks supply actuals). Build cost is the real decision, hence the tiered framing so M019's final scope is set by what the Tier 1 data reveals rather than up-front speculation. M019 before M018 because compression needs a measured baseline. M019 before M009 because launch narrative differentiates on published receipts ("M011 shipped in N phases, M tasks, $X spend, Y% first-try verification pass") — no other orchestrator tool publishes this. Also surfaces Principle V's fresh-context cost quantitatively, making the Context Minimization claim auditable.
  - decisions :: .orchestrator/DECISIONS.md:130:"runtime"` so estimates and ground-truth usage (from Claude Code `SessionEnd`, Codex/Cursor where available) don't silently mix. Pricing table lives at `config/pricing.yml`; stale rates degrade gracefully with last-updated date surfaced in reports.
  - decisions :: .orchestrator/DECISIONS.md:134:### Reframe M018 core approach from single compression filter to four-tier compaction ladder { #dr-code-010 }
  - decisions :: .orchestrator/DECISIONS.md:141:- **Choice**: Replace the D008 phase outline's "P02 dispatch compressor (optional filter...)" framing with a tier-ladder model informed by Article 3 (Rohit's Claude Code harness teardown) and logged in `.orchestrator/scratch/articles-synthesis-2026-04-17.md`. Every dispatch runs through tiers in order, cheapest first: **Tier 1 microcompact** (reuse cached tool-call results that haven't changed + persist oversized tool results to disk with `file_path + preview` references — absorbs L6 tool-result budgeting; zero LLM cost); **Tier 2 snip** (head-drop with protected tail; no LLM call); **Tier 3 auto-compact** (summarize via LLM call; existing caveman-style territory); **Tier 4 context collapse** (staged multi-phase compression of tool results → reasoning → sections; feature-flagged). Intensity maps to tier ceiling: Quick→T1–T2 only, Standard→through T3, Full→all tiers available. Revised phase outline: P01 tier grammar + per-tier safety boundaries (what's preservable per tier), P02 Tier 1 microcompact + tool-result budgeting, P03 Tier 2 snip with protected-tail semantics, P04 Tier 3 auto-compact (summarization, intensity-gated), P05 Tier 4 collapse (staged; feature-flagged, off by default), P06 eval harness (3-arm parity tests per tier), P07 multi-runtime parity. D008's original scope envelope (roundtrip-safe by policy, grammar-aware preservation, intensity-bound, explicit opt-out surfaces, telemetry-integrated) carries forward unchanged — only the internal staging model is reframed. Fresh `orchestrator:discuss` at M018 kickoff addresses detailed tier design once M019 Tier 1 data reveals which artifacts dominate token spend. **Conversus integration (locked in per D007 reuse pattern)**: M018/P01 tier-grammar + per-tier-safety-boundary work invokes `scripts/dispatch/adapters/tool/conversus.sh` via `role/red-team` + `domain/security` presets at plan-review time. Rationale: grammar decisions carry parse-regression risk across downstream scripts that grep prose (the risk D008 originally flagged), which is exactly the subjective-quality territory the conversus fidelity-gate adapter earns its cost on. A new preset drops under `templates/conversus-presets/` without adapter modification (same pattern M013/M014 use). Gate is intensity-bound by default (Quick=off, Standard=advisory, Full=blocking); `--no-review` escape available.
  - decisions :: .orchestrator/DECISIONS.md:15:- **Choice**: P06 scope expanded by three engine-layer workarounds discovered during P03 implementation: (1) literal-audit-marker printf pattern added after SAFETY_WARNING emissions in scripts/engine/run.sh (T03/T04) because events.sh _orch_events_quote does not quote single-word values, breaking must-have greps that expect quoted form; (2) cd $REPO_ROOT subshell wrapper around check-must-haves.sh and record-result.sh calls in scripts/engine/run.sh (T05) working around check-must-haves.sh PROJECT_ROOT walk-up bug that computes PROJECT_ROOT as .specify/orchestrator/milestones/<m> instead of repo root; (3) execution-log.jsonl entries from engine dispatch do not thread ORCH_RUN_ID because record-result.sh does not currently accept/emit run_id. P06 cleanup should: fix check-must-haves.sh PROJECT_ROOT detection, extend events.sh _orch_events_quote to always quote values, extend record-result.sh to thread run_id from env, then remove all three workarounds from run.sh.
  - decisions :: .orchestrator/DECISIONS.md:162:### Insert M021 (Autonomous Hardening v2) before M019 and position M019 Tier 1 emitter only after M021 closes { #dr-code-012 }
  - decisions :: .orchestrator/DECISIONS.md:225:- **Choice**: Revised sequence: **M013 (P04 remaining) → M014 (extended) → M020 → M024 → M019 Tier 2+3 → M018 → M023 → M009 (extended) → M010 (adjusted)**. (1) **M014 scope extension**: port spec-kit's `specify` shape as native `orchestrator:specify` (CC-first, written portably, treating spec-kit template as the I/O contract rather than a verbatim port); add conversus-suggestion logic so complex/controversial specs auto-propose red-blue pressure-test with optional spec-decomposition-before-conversus for very large specs; dual-write `AGENTS.md` alongside `CLAUDE.md` so Codex stays fed without a separate milestone. (2) **M024 Universal Intake** (new): extend `orchestrator:evaluate` to input-agnostic (idea, paragraph, fragment, full spec, or empty + Q&A) and emit a reviewable proposal artifact at `.orchestrator/intake/<id>/proposal.md` covering six axes (input shape, scope tier, decomposition, design gate, conversus gate, intensity); degenerate fast-path auto-proceeds if Tier A + Quick + no conversus + no design, approval-gated otherwise. Positioned after M020 so we dogfood it through remaining milestones. Design gate degrades gracefully pre-M023 ("design walkthrough lands in M023; author DESIGN.md manually or skip"). (3) **M023 Design Layer** (new): `orchestrator:design` command — conversus spawns N design-personality agents in parallel, each produces a DESIGN.md draft + working coded prototype in user's stack; side-by-side comparison; user picks/arbitrates; canonical DESIGN.md lands at repo root and auto-injects into payloads for ui-tagged phases. Renderer adapter interface shaped as MCP clients (runtime-agnostic; Stitch/v0/Figma/etc. consumed as they expose MCP). Stage-1-only scope — high-fidelity stakeholder-mockup expansion (Stitch/Claude-design rendering) deferred until user demand earns it. Positioned pre-M009 because orchestrator has no internal UI to dogfood against — landing earlier buys zero learning. (4) **M009 scope extension**: add runtime-parity audit as pre-launch gate — dogfood every `orchestrator:*` command end-to-end on CC + Codex + Cursor, consuming the `RUNTIME-ASSUMPTIONS.md` registry accumulated during M013–M018, fix parity bugs before external eyes. (5) **M010 posture adjustment**: ship with Managed Agents primary adapter + Codex Cloud **stubbed** adapter (proves abstraction holds without full feature parity); full Codex Cloud as demand-driven fast-follow rather than launch-critical. (6) **Ultraplan/ultrareview stance**: parked, not integrated — Claude-Code-only (requires CC on web + GitHub repo + Anthropic cloud; not available on Bedrock/Vertex/Foundry), forks multi-runtime UX. M013 GitHub integration delivers the same plan-review value universally via PR comments. Re-evaluate only if real CC-only user demand emerges. (7) **`RUNTIME-ASSUMPTIONS.md` discipline** (no milestone cost): a registry file tracking CC-specific shape assumptions as written (`settings.json` hook formats, CLAUDE.md-only writes, `claude` CLI invocation patterns). M009's parity audit consumes it as a punch-list rather than an open-ended investigation. Optional: fold a single Codex smoke-probe telemetry point into M019 T2+3's emitter for drift detection.
  - decisions :: .orchestrator/DECISIONS.md:239:- **Choice**: M014 pressure-tested pre-discuss via cooperative conversus deliberation (Tier 1; 2 agents — `spec-author` + `devils-advocate`; 1 round; no arbiter). 14 MITs applied pre-discuss covering: (P1) Phase Sequencing table as spec-layer subsection after Minimal Slice; SC-2 tightened to mechanical stdout/exit-code/stderr-grep assertions; FR-2b template SSOT (`templates/spec-template.md`) + FR-18 byte-compatibility fixture test (`tests/test-specify-shape.sh`) + SC-17; US-5 promoted to independent P2 user story with scope-boundary sentence ("US-5 is the only path that edits `specs/<NNN>-<slug>/spec.md` from a comment"); Constitution XV retrofit at SC-5 + Constitution Check section → III (primary) + XIV (supporting), with Constitution Check XV paragraph split into "applies to / does NOT apply to" sub-contexts (FR-12 + FR-14 byte-preservation retained; spec-amendment human-gate retargeted); (P2) SC-4 rewritten as two-branch construction (default: measurement + re-planning trigger; upgrade branch: pinned-shape gate with failure-posture clause); FR-14 three-case semantics (all-placeholder / partial / fully-authored) + SC-14 byte-preservation invariant; SC-6a outside-markers `shasum` invariant + `tests/test-dual-write-outside-invariant.sh`; FR-19 `--dry-run` JSONL manifest format pinned M014-local only (M013 retrofit deferred); SC-15 `RUNTIME-ASSUMPTIONS.md` close-out deliverable; SC-16 dogfood-data sizing for FR-9 classifier-shape decision; (P3) FR-7 interim→M024 migration handshake committed as one-line rule; Open Question #C-11 exhaustive CLAUDE.md write-site enumeration at planning time. Spec frontmatter promoted `Status: Draft` → `Status: Ready-for-discuss`; `Last Revised: 2026-04-22` line added matching M013 precedent. P3 Dependencies prose rewrite skipped as subsumed by Phase Sequencing table (P1 #1). Two Phase 4 disputes (SC-4 default branch; Phase Sequencing table layer) resolved via synthesizer-recommended compromises: SC-4 adopts measurement default with upgrade-branch failure-posture; table lands spec-layer.
  - decisions :: .orchestrator/DECISIONS.md:267:- **Choice**: (1) **Three-pass contract** pinned in `commands/specify.md`: Pass 1 (scaffold) always runs — creates file, populates frontmatter + Input, writes Section Contract `<TODO:` placeholders. Pass 2 (author) drafts the body from Input + DECISIONS.md + constitution + neighboring CLAUDE.md/AGENTS.md Recent Changes; Quick = agent-only no review, Standard = agent + self-review checklist warnings, Full = agent + `speckit.clarify` loop (≤5 load-bearing ambiguity questions, operator-answered, agent-revised). Pass 3 (gate) adversarially reviews authored body via `scripts/dispatch/adapters/tool/conversus.sh gate spec-pressure-test`; Quick = skip, Standard = advisory (findings folded into Open Questions, no BLOCK), Full = strict (BLOCK halts with dispute list + "revise then --amend" message). On Full+PASS, spec status promoted `Draft` → `Ready-for-discuss` + `Last Revised:` line added per M013/M014 precedent. (2) **Intensity resolution order**: project default from `.orchestrator/config.yml` → smell-test escalation via `scripts/engine/intensity-analyze.sh --description <prose>` (can only escalate, never de-escalate) → CLI `--intensity` override (trumps both). One `specify_intensity_resolution` JSONL record emitted at run start. (3) **Gate TODO pre-flight invariant** (universal): `scripts/dispatch/adapters/tool/conversus.sh gate` refuses any artifact containing ≥ `CONVERSUS_GATE_TODO_THRESHOLD` (default 1) `<TODO:` markers, with actionable error pointing at the author pass. Bypass: `CONVERSUS_GATE_SKIP_TODO_CHECK=1` (reserved for tests + intentional-stub preset authoring). Protects M013/M014/M023/M024 + any future gate consumer, not just `orchestrator:specify`. Applied to adapter in `scripts/dispatch/adapters/tool/conversus.sh:150-169` + regression test in `tests/test-conversus-adapter-shim.sh` section 1b. (4) **Reuse over rebuild**: all intensity machinery already exists (`intensity-analyze.sh`, `intensity-override.sh`, `intensity-gate.sh`, `spec-complexity-probe.sh`) — D019 wires them into the specify flow rather than adding parallel infrastructure. (5) **Implementation posture**: command contract is the SSOT (landed 2026-04-23 in `commands/specify.md`); shell implementation in `scripts/specify/specify.sh` lands in the next M014 extended phase. Until then, agents invoking the command execute passes 2 and 3 manually per the doc.
  - decisions :: .orchestrator/DECISIONS.md:29:- **Choice**: P06 scope expanded by five additional items discovered during P05 dispatch refactors: (1) scripts/lib/run-context.sh _orch_run_nonce SIGPIPE under set -o pipefail — causes scripts/engine/run.sh --dry-run to exit 141 when ORCH_RUN_SEED is unset; worked around in T02/T03/T04 with set +o pipefail wrapper around init_run_context (3 replicated call sites to drop after P06 fix). (2) scripts/dispatch/lib/section-handlers.sh handle_phase_summaries has a Bash 3.2 while-read bug with unterminated dep-list temp file — worked around by local _bc_handle_phase_summaries_fixed shim in build-context.sh (to drop after P06 fix). (3) templates/context-recipe.yaml default section order/priority values don't match pre-refactor dispatch-prompt parity — worked around by _bc_display_order/_bc_display_name/_bc_display_priority shim in build-context.sh (to drop after P06 recipe re-authoring). (4) scripts/verify/check-must-haves.sh doesn't discover tasks/TNN-PLAN.md files — all P05 tasks had to verify must-haves manually from payloads rather than via the canonical entrypoint. (5) scripts/dispatch/build-context.sh all emit_event calls are >/dev/null 2>&1
  - decisions :: .orchestrator/DECISIONS.md:323:- **Choice**: (1) **Roadmap edit**: `Depends: P01, P99` → `Depends: P01` plus a `Preflight (external, operator-gated — see D023)` block enumerating both external gates with current resolution status. P03 phase-entry gate (CONTEXT OQ-4) at dispatcher level remains the load-bearing enforcement for the wiki preflight; encoding it as a fake phase ID was always belt-and-suspenders and caused a parse-time hard-fail under `read-roadmap.sh`'s P##-token guard (introduced M026/P02 commit 316411e). (2) **Wiki preflight: RESOLVED 2026-04-23** — `M012/P04/DEPLOY-RECORD.md` shows live deploy against `Build-Fractal/spec-kit-orchestrator`, all four gates (giscus-config, mkdocs-build, link-check, giscus-smoke) PASS, site live at `https://Build-Fractal.github.io/spec-kit-orchestrator/`. SC-5 redeploy-persistence is the only remaining sub-check and is non-blocking (validated partially via `mkdocs serve`; full validation lands at the next M012 redeploy event). (3) **SC-16 inbox-dogfood preflight: RELAXED** — original spec called for ≥1 week M012/M013 inbox data captured into `specs/024-spec-management-extended/planning-inputs/inbox-dogfood.md` before plan-phase pins FR-9 classifier shape. Wiki was deployed only 1 day before this decision; waiting ≥6 calendar days delays the M014 close past usable cadence. Plan-phase will pin FR-9 shape on **regex/heuristic v1 baseline** (the lowest-cost option from OQ #C-1) with explicit retune commitment after meaningful comment volume accumulates organically. **Retune trigger**: when `actioned.jsonl` shows ≥30 fetched comments OR when classifier confidence calibration on observed comments diverges from regex/heuristic predictions in ≥20% of samples, open a follow-up D-row that re-pins FR-9 shape (likely escalating to LLM-per-comment or two-pass hybrid per OQ #C-1) and either lands the change inside M014 extended scope or as a dedicated M011/M014 follow-up.
  - decisions :: .orchestrator/DECISIONS.md:326:(a) The user explicitly chose this trade-off after being given options (wait-for-data vs proceed-with-relax vs alternate read on P99): "im open to coming back and retuning as needed once weve used the comments more." (b) D018 created P99 as an audit-trail device, not a dispatcher contract — the dispatcher already enforces preflight via `Phase-entry gate (CONTEXT OQ-4)` at task dispatch time per the M014 spec boundary map. Keeping the artifact parseable while preserving the gate at the load-bearing layer is the cleaner separation. (c) FR-9 classifier shape is the only plan-phase decision genuinely sensitive to dogfood data; pinning regex/heuristic v1 is the conservative baseline (no LLM cost, no calibration burden) and matches the spec's documented v1 floor. Retune-via-follow-up-D-row is consistent with D019(a)/(b)/(c) precedent (commit to the contract, tune the parameters in follow-up rows). (d) The roadmap edit removes the parser-hostile `P99` token without losing audit trail (this D-row + the new Preflight note carry the same information in machine-parseable form).
  - decisions :: .orchestrator/DECISIONS.md:491:- **Choice**: Author a local-only git tag `v0.9.X-final-spec-kit-name` at HEAD immediately before the rename branch lands, where `X` is the current `CHANGELOG.md` top-line patch number (resolved via `awk '/^## \[[0-9]/{print; exit}' CHANGELOG.md` per CON-4 — skips `## [Unreleased]`). Captured at T01 execution time as `v0.9.2-final-spec-kit-name`. Tag gives post-rename archaeology a clean cutover marker without polluting the SemVer release stream (no `v` release tag is published — this is not a release).
  - decisions :: .orchestrator/DECISIONS.md:500:| D004 | M035/P05 | signing-strategy | Release-artifact signing for the npm publishing pipeline (FR-11 / SC-11 / #Q-3 binding) | sigstore (cosign keyless) primary + SHA-256 checksum fallback | Keyless cosign signing in `.github/workflows/release.yml` (`npm-publish` job) binds each release artifact's signature to the workflow's GitHub OIDC identity (canonical-repo `v*` tag-push) and records it in the public Rekor transparency log — eliminates the GPG private-key blast radius (no long-lived signing key to rotate or compromise). `SHA256SUMS` published alongside the signed artifacts gives operators without `cosign` installed a tooling-free verification path (`shasum -a 256 -c SHA256SUMS`). The signing job uses a job-level `permissions: id-token: write` override; the workflow-level `permissions: contents: read` stays unchanged so other jobs (pr-validate) inherit least-privilege. CON-6 secret-scope discipline preserved (no new long-lived secrets; `secrets.GITHUB_TOKEN` is the default token, scoped by GHA). | No |
  - decisions :: .orchestrator/DECISIONS.md:511:  the GitHub release by P02's `npm-publish` job + P05's signing
  - decisions :: .orchestrator/DECISIONS.md:532:  `.github/workflows/release.yml` writes to
  - decisions :: .orchestrator/DECISIONS.md:541:     adding one more under the same review cadence is lower
  - decisions :: .orchestrator/DECISIONS.md:556:### D009 — Curl-pipe-bash install.sh URL host: GitHub release asset URL
  - decisions :: .orchestrator/DECISIONS.md:638:### D011 — Release cadence: manual stable releases pre-1.0 (operator-policy)
  - decisions :: .orchestrator/DECISIONS.md:644:Pre-1.0 release cadence is **manual operator-driven tag push**. The
  - decisions :: .orchestrator/DECISIONS.md:667:   cadence inherits the same shape for consistency.
  - decisions :: .orchestrator/DECISIONS.md:74:M016 is prerequisite for credible launch narrative — autonomous mode must actually work autonomously. M017 adds multi-agent deliberation as an opt-in quality gate, informed by the autoreason paper's generation-evaluation gap findings. Positioned before M011-M014 because spec management milestones involve subjective quality judgments where Conversus provides the most value. M017 is opt-in with no hard dependency from M011, so it can be deferred without rework cost.
  - decisions :: .orchestrator/DECISIONS.md:822:   spawn cost. Git fallback persistence is intentionally suppressed
  - decisions :: (capped at 5 hits — more may exist)
  - knowledge-entries :: (capped at 5 hits — more may exist)
  - knowledge-entries :: knowledge/conventions/MEM015.md:19:All diagnostic scripts emit a single structured line for machine parsing by run-doctor.sh:
  - knowledge-entries :: knowledge/conventions/MEM016.md:17:# MEM016: Cost Source Closed Enum
  - knowledge-entries :: knowledge/conventions/MEM016.md:19:Telemetry entries carry `cost_source` field (estimated/reported/unknown) with closed enum validation. `aggregate-metrics.sh` groups by cost_source, distinguishes null cost (unknown) from zero cost (free). Legacy entries classified by presence of cost data.
  - knowledge-entries :: knowledge/conventions/MEM017.md:19:`scripts/lib/verdicts.sh` provides `emit_verdict`, `parse_verdict`, `orch_is_verdict` with four constants: PASS, BLOCK, WARN, NEEDS_REVIEW. `hooks.sh` captures hook stdout, parses VERDICT lines, resolves multiple verdicts to most severe, and maps to block/warn/continue behavior. Backward compatible when no VERDICT present. Provider convention documented in `references/provider-convention.md`.
  - knowledge-entries :: knowledge/conventions/MEM018.md:17:# MEM018: Runtime Adapter Interface
  - knowledge-entries :: knowledge/conventions/MEM018.md:22:- **await-completion / collect-result**: The agent runtime handles task execution and writes artifacts to disk. The orchestrator detects completion via file presence (task summary exists = done).
  - knowledge-entries :: knowledge/conventions/MEM018.md:23:- **signal-failure**: Verification scripts (`check-must-haves.sh`, `run-commands.sh`) detect failure by checking artifacts against must-haves. Failures are recorded in `execution-log.jsonl`.
  - knowledge-entries :: knowledge/conventions/MEM019.md:17:# MEM019: Three-Temperature Knowledge Architecture
  - knowledge-entries :: knowledge/conventions/MEM019.md:2:id: MEM019
  - knowledge-entries :: knowledge/conventions/MEM020.md:13:relates_to: [MEM019]
  - knowledge-entries :: knowledge/conventions/MEM030.md:29:1. **Naming**: `<TOOL>_EDITION` (uppercase tool name + literal `_EDITION` suffix). Examples: `CONVERSUS_EDITION`, `<NEWTOOL>_EDITION`. Reads naturally in shell history, in JSONL telemetry, and in operator-facing error messages.
  - knowledge-entries :: knowledge/conventions/MEM030.md:35:4. **Diagnostic surface**: the `check` subcommand of the integration's adapter MUST emit `edition=<value> reason=<resolution-tag>` on stdout so the resolved edition is visible to the operator and to telemetry without a separate probe call.
  - knowledge-entries :: knowledge/conventions/MEM030.md:37:5. **Telemetry shape**: every JSONL record emitted by the integration adapter MUST include an `edition` field alongside the existing identifying fields (e.g., `adapter_version`, `gate_id`). Place adjacent to the version field for readability and for adjacency-invariant tests (M026/P02/T02 pattern).
  - knowledge-entries :: knowledge/conventions/MEM030.md:43:The M026 migration is the first OSS-default escape-hatch landing in this repo. Future migrations (e.g., when M010 ships and the orchestrator starts integrating with multiple LLM-provider editions, or when M023 ships and design-renderer adapters need to distinguish freemium tier vs paid tier) will face the same shape. Naming it as a convention now means the next migration can copy the pattern verbatim instead of re-deliberating the env-var name in each milestone.
  - knowledge-entries :: knowledge/conventions/MEM030.md:48:- **JSONL `edition` field presence**: every `*_invocation` record from an edition-aware adapter MUST contain an `edition` field. Verify with `scripts/verify/m026-p02-jsonl-edition-field.sh` (existing) — extend the pattern when adding a second edition-aware adapter.
  - knowledge-entries :: knowledge/conventions/MEM030.md:51:See: `scripts/dispatch/adapters/tool/conversus.sh` for the canonical implementation; MEM029 for the paired two-tier-detection pattern.
  - knowledge-entries :: knowledge/conventions/MEM031.md:50:  YAML map containing `rationale: <text>`, `timestamp: <ISO 8601 UTC>`,
  - knowledge-entries :: knowledge/conventions/MEM031.md:65:milestones (M024 universal intake, M019 Tier 2+3 observability) MAY READ
  - knowledge-entries :: knowledge/conventions/MEM031.md:81:  enforces the schema-authority boundary at lint time.
  - knowledge-entries :: knowledge/lessons/MEM026.md:21:Commit `d33b8a7` (M013/P04/T04) shipped a `scripts/dispatch/adapters/runtime/claude-code.sh --hook-config` emitter whose root object carried wrapper metadata (`runtime`, `hook_count`, `target_file`) alongside orchestrator-internal event names (`before_tasks`, `after_tasks`, `before_implement`, `after_implement`, `before_commit`, `post_verify`) that Claude Code does not recognize. The companion installer `packaging/install/install-claude-code.sh` wrote that invalid document unconditionally to `$HOME/.claude/settings.json`, overwriting any sibling tool's configuration (notably GSD-authored `statusLine`, `SessionStart`, `PostToolUse`, and `permissions` keys) with no merge path and no reversibility.
  - knowledge-entries :: knowledge/patterns/MEM005.md:13:relates_to: [MEM019]
  - knowledge-entries :: knowledge/patterns/MEM006.md:19:`run-doctor.sh` aggregates 12 checks (4 legacy + 8 DOCTOR:) into `Checks passed: N/M` with HEALTHY/NEEDS_ATTENTION status. Legacy checks use exit-code pass/fail; new checks parse DOCTOR: status lines. Advisory checks (check-plans.sh) counted separately. History appended to `doctor-history.jsonl` per run.
  - knowledge-entries :: knowledge/patterns/MEM029.md:17:# MEM029: Edition-resolution two-tier detection (env-var primary, metadata-probe fallback)
  - knowledge-entries :: knowledge/patterns/MEM029.md:25:## Pattern: two-tier detection
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:10:- **Structured output**: All scripts emit prefixed lines to stdout (`PASS:`, `FAIL:`, `LOCK:`, `STUCK:`, `BUDGET:`, `SUMMARY:`, `DECISION:`, `KNOWLEDGE:`, `ROLLBACK:`, `VALIDATE:`, `CONSOLIDATE:`). Errors to stderr. Exit 0 on success, 1 on failure.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:106:- All timestamps use `date -u +%Y-%m-%dT%H:%M:%SZ` (UTC, ISO 8601). Rollback-phase.sh was using `date +%Y%m%dT%H%M%S` — fixed for consistency with lock-manager and recovery-briefing.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:119:### Runtime Adapter Interface (FR-067/FR-068/FR-069)
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:122:  - **await-completion / collect-result**: The agent runtime handles task execution and writes artifacts to disk. The orchestrator detects completion via file presence (task summary exists = done).
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:123:  - **signal-failure**: Verification scripts (`check-must-haves.sh`, `run-commands.sh`) detect failure by checking artifacts against must-haves. Failures are recorded in `execution-log.jsonl`.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:129:- **[milestone:M005]** [2026-04-12] DOCTOR: structured output protocol — all diagnostic scripts emit a single structured line (DOCTOR:<CHECK> status=<ok|warn|skip|drift|missing> key=value ...) for machine parsing by run-doctor.sh. Advisory checks (like check-plans.sh) always exit 0; non-advisory checks exit 0 on ok, 1 on warn. Established in P04, extended through P05-P07.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:130:- **[milestone:M005]** [2026-04-12] Scored health reporting — run-doctor.sh aggregates 12 checks (4 legacy + 8 DOCTOR:) into Checks passed: N/M with HEALTHY/NEEDS_ATTENTION status. Legacy checks use exit-code pass/fail; new checks parse DOCTOR: status lines. Advisory checks (check-plans.sh) counted separately. History appended to doctor-history.jsonl per run.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:133:- **[milestone:M005]** [2026-04-12] Cost_source closed enum — telemetry entries carry cost_source field (estimated/reported/unknown) with closed enum validation. aggregate-metrics.sh groups by cost_source, distinguishes null cost (unknown) from zero cost (free). Legacy entries classified by presence of cost data. Established in P02 per AD-2.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:134:- **[milestone:M005]** [2026-04-12] Gate verdict protocol — scripts/lib/verdicts.sh provides emit_verdict, parse_verdict, orch_is_verdict with four constants: PASS, BLOCK, WARN, NEEDS_REVIEW. hooks.sh captures hook stdout, parses VERDICT lines, resolves multiple verdicts to most severe, and maps to block/warn/continue behavior. Backward compatible when no VERDICT present. Provider convention documented in references/provider-convention.md. Established in P05 per AD-3.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:135:- **[milestone:M005]** [2026-04-12] Autonomy permission pipeline — generate-permissions.sh introspects project toolchain (package.json, Makefile, extension.yml, config files, agent host markers) and emits canonical JSON. write-permissions.sh translates to .claude/settings.json with additive merge for user-authored files. check-permissions.sh detects permission drift. Policy is declarative in autonomy-defaults.yaml read via recipe-parser.sh. AD-19 script-file verification shape: task plan Check: commands must use single-script invocations, not inline compound bash. Established in P07.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:144:- **[milestone:M008]** [2026-04-14] Filename-based adapter auto-discovery: drop any script into scripts/dispatch/adapters/<type>/*.sh to register it. No central registry file. Mirror applied across P02 backends, P05 runtime adapters, and P05 format adapters. Dispatch core uses /.sh filename lookup — zero type-specific branching. New backends/runtimes/formats can be added with zero core-orchestrator edits (satisfies SC-003).
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:150:- **[milestone:M003]** [2026-04-15] Three-tier root resolution (flag → env var → resolve-root.sh fallback) is the standard CLI contract for orchestrator-root-consuming scripts. Makes scripts work under both ORCHESTRATOR_ROOT env and explicit --root, with config-driven default. Reference: scripts/orchestrator/status.sh.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:153:- **[milestone:M003]** [2026-04-15] Tier-1 verify-per-truth pattern: each phase truth gets one single-file AD-19-safe scripts/verify/<milestone>-<phase>-<name>.sh emitting PASS/SKIP/FAIL, exit 0/non-zero. Static grep + behavioral checks per truth keeps verification auditable and refactor-tolerant.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:159:- **[milestone:M021]** [2026-04-17] Dogfood attestation from state on disk: milestone closure gate reads auto-loop-result.txt + execution-log.jsonl + P*-SUMMARY.md and greps for user_prompt/safety_prompt/hook_reject_unexpected events. hook_reject_recovered is deliberately OMITTED from needles because recovery events are the success signal (AD-6). Gate is self-referential-safe — passes during its own phase by skipping in-flight summaries.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:163:- **[milestone:M019]** [2026-04-18] Zero-token instrumentation via stdout capture-and-replay: PAYLOAD_CAPTURE tempfile then cat then emitter. Emitter runs strictly after stdout and contributes zero bytes. SC-6 byte-identical stdout enforced this way in scripts/dispatch/build-context.sh.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:164:- **[milestone:M019]** [2026-04-18] Goodhart pairing enforced at emission time: every unit_close record carries both a cost block (estimated_cost_usd, pricing_version) and a quality block (verification_pass_rate, deviation_count, retry_count derived from existing attempt/outcome/verification_result per AD-3). Schema validator rejects records missing either. Prevents Tier 2 from optimizing cost in isolation.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:165:- **[milestone:M019]** [2026-04-18] Sentinel-scoped JSON span overwrite in bash 3.2 (AD-7 fix): line-oriented replacement inside _generated_start / _generated_end JSON-key sentinel pair with tail-comma auto-insertion when trailing keys are present, no jq dependency. User-authored content outside the span survives evaluate-preflight re-runs byte-identical; canonical content for widened allow-lists goes through templates/autonomy-defaults.yaml.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:166:- **[milestone:M019]** [2026-04-18] BSD awk in-keyword pitfall: awk variables starting with 'in_' silently tokenize-zero on BSD awk (default on macOS). Use ival/oval or prefer index() over 'in'. Caused a silent zero of input_per_million_usd in T01 and was reflagged as a watch-for in T02/T03/T04.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:167:- **[milestone:M019]** [2026-04-18] Module-scoped globals and subshell loss: pricing lib sets _PRICING_WARNING_REASON in whatever shell the estimator runs in. Capturing estimator stdout via $(...) loses the warning. Workaround: route estimator stdout to a mktemp file so the estimator runs in the caller's shell and the warning survives. Applied in scripts/dispatch/dispatch-interface.sh _di_emit_dispatch_usage helper.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:168:- **[milestone:M019]** [2026-04-18] Tier 1 metrics emitter schema (AD-4): reserve three enums at record emission time so Tier 2 rollup and Tier 3 backend-actuals land additively without rewriting emitter code. record_type: payload_breakdown | dispatch_usage | unit_close. source: estimate | runtime | aggregate. granularity: task | phase | milestone. Validator scripts/verify/m019-schema.sh enforces.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:179:- **[milestone:M013]** [2026-04-22] Fixture-copy-before-live-gate: when a script reassigns ORCHESTRATOR_ROOT (e.g., github-sync.sh sets it to $PROJECT_ROOT/.orchestrator), live-mode gates MUST 'cp -R fx/. tmp/' the fixture orchestrator-state to a tempdir before invoking — running against the repo fixture tree directly pollutes it with sidecar updates and observability JSONL records. Pair with M013_GH_STUB_DIR pointing at the fixture's gh-stub-responses subdir so the script never touches real network.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:193:- **[milestone:M027]** [2026-04-27] 5-condition suppression matrix as load-bearing reusable contract (M027/P02 + P03): operator-controlled surfaces should ship --yes / ORCHESTRATOR_AUTO / --no-<feature> / <knob>: false / structural-carve-out — all 5 paths short-circuit to zero stdout exit 0 before any I/O. Gate mechanically with a per-phase suppression-matrix verifier exercising all 5 paths. Quick-tier carve-out (P02) and sample-floor carve-out (P03) are the canonical structural-carve-out shapes. Future cost/quality/anomaly surfaces should follow this contract.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:197:- **[milestone:M018]** [2026-04-28] Additive emitter schema (CON-5) with pinned rollup column-index contract: when extending JSONL records (payload_breakdown / dispatch_usage / unit_close), append new fields ONLY at end-of-printf and append new rollup columns at absolute indices (not NF-relative). Pre-extension records remain valid JSON; downstream rollups treat absent fields as zero. M018 pinned cols 1-12 (M027 baseline), 13-16 (P05 filter+tier1+tier2 fields), 17-18 (P06 tier3 fields). Verifier shape rule: anchor column reads to absolute positions ($13..$18) not offset-from-end ($(NF-3)..$NF) — the column-index contract IS the back-compat invariant; NF-relative reads are fragile and will silently shift when the next tier extends the schema.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:57:| D006 | convention | Phase plan must-haves: `### Truths` / `### Artifacts` / `### Key Links` with parseable modifiers | Must be human-readable AND machine-parseable; `(min N lines)`, `(contains "pattern")`, `Check: \`command\`` enable Tier 1/2 checks |
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:63:- Truths without `Check:` sub-items logged as descriptive (Tier 3), not mechanically checked
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:73:- Rollback archives use `YYYYMMDDTHHMMSS` timestamp prefix
  - knowledge-md :: (capped at 5 hits — more may exist)
  - milestone-summaries :: .orchestrator/milestones/M002/M002-SUMMARY.md:25:  - "doctor-history.jsonl for diagnostics trend tracking, execution-log.jsonl telemetry entries for cost/performance monitoring"
  - milestone-summaries :: .orchestrator/milestones/M003/M003-SUMMARY.md:14:  - "scripts/migrate/migrate.sh,scripts/migrate/adapter-interface.sh,scripts/migrate/adapters/gsd2.sh,scripts/migrate/adapters/gsd1.sh,scripts/migrate/adapters/speckit.sh,scripts/migrate/transform/knowledge.sh,scripts/migrate/transform/knowledge-index.sh,scripts/migrate/transform/decisions.sh,scripts/migrate/transform/requirements.sh,scripts/migrate/transform/milestone-tiering.sh,scripts/migrate/transform/active-milestone.sh,scripts/migrate/transform/milestone-rollup.sh,scripts/migrate/transform/telemetry-aggregator.sh,scripts/migrate/transform/report.sh,scripts/migrate/lib/idempotency.sh,scripts/migrate/lib/sqlite-reader.sh,scripts/migrate/lib/json-fallback.sh,scripts/migrate/lib/category-mapper.sh,scripts/migrate/lib/supersession-chain.sh,scripts/migrate/lib/scope-tag.sh,scripts/migrate/lib/decision-numbering.sh,scripts/migrate/lib/category-inferrer.sh,scripts/migrate/lib/error-handler.sh,scripts/orchestrator/status.sh,commands/migrate.md,tests/fixtures/m003-p08-gsd-minimal/,tests/integration/test-m003-e2e-migration.sh"
  - milestone-summaries :: .orchestrator/milestones/M003/M003-SUMMARY.md:18:  - "adapter-interface extract() contract for source-format pluggability; tiered milestone layout (active/recent/historical/archived); three-tier root resolution (flag→env→resolver) as standard CLI contract; dual-layout state probe for idempotency; warn-but-continue post-pipeline rebuild; progressive-disclosure ADR documentation in command docs; Tier-1 static+behavioral verify-per-truth with single-file AD-19 invocations; skip-gracefully integration-test pattern; strict/warn split for synthetic-vs-live validation; deterministic SQLite fixtures under 50KB (PRAGMA page_size=1024 + hard-coded ISO-8601); .gitignore negation for test fixtures sharing ignored names"
  - milestone-summaries :: .orchestrator/milestones/M003/M003-SUMMARY.md:28:M003 Migration Tool milestone complete. Initial build (P01-P06, commit ad3da8a 2026-04-09) delivered the end-to-end migration pipeline: adapter interface with pluggable source formats (GSD2/GSD v1/spec-kit), per-artifact transforms (knowledge, decisions, requirements, milestones with tiering), and CLI with idempotency + reporting. After M007 (knowledge graph) and M008 (standalone multi-runtime + 5-rule state resolver) landed, a delta audit surfaced three drift points: hardcoded .specify/orchestrator/ paths, unpopulated graph database, single-layout idempotency probe. P07 refit wired migrate.sh to resolve-root.sh --absolute, dropped hardcoded paths in three transforms, added dual-root idempotency probing, wired rebuild-index.sh as final pipeline stage, and documented AD-13/14/15 in commands/migrate.md. P08 validated end-to-end via deterministic synthetic GSD2 fixture (primary) and live lakeledger (secondary, skip-gracefully), new scripts/orchestrator/status.sh CLI wrapper making the roadmap demo sentence literally executable, 8 AD-19-safe verify scripts, and a two-pass integration test — all green. Surfaced and fixed one latent P02 bug (missing content_hash frontmatter) during refit. Retroactive phase summaries added for P01-P06 (originally delivered before phase-summary machinery) based on roadmap Revision Note + commit ad3da8a. Migration pipeline now produces resolver-wired output, populates the M007 graph database, and participates in graph traversal — ready for M009 recipe packaging and M010 cloud dispatch.
  - milestone-summaries :: .orchestrator/milestones/M004/M004-SUMMARY.md:16:  - "Closed 6-kind error taxonomy (CONFIG/STATE/DISPATCH/VERIFY/BUDGET/IO), 19-entry canonical event registry, YAML recipes parsed with grep/sed/awk (no jq), standalone detection via ORCH_RUN_ID guard, root-marker PROJECT_ROOT detection, EXIT trap with _RESULT_EMITTED guard pattern"
  - milestone-summaries :: .orchestrator/milestones/M004/M004-SUMMARY.md:18:  - "Double-sourcing guard in first 5 lines, emit_result on every exit path, emit_event at lifecycle boundaries, ORCH_RUN_ID standalone detection, recipe-driven context assembly, hook sandbox with chmod 444 frozen snapshots, chained EXIT traps for scripts with pre-existing cleanup"
  - milestone-summaries :: .orchestrator/milestones/M004/M004-SUMMARY.md:25:  - "All engine-path scripts emit EVENT: lines to stderr when ORCH_RUN_ID is set; RESULT: lines on every exit; record-result.sh adds run_id to execution-log.jsonl; aggregate-metrics.sh reports by_error_kind; check-recipe.sh emits DOCTOR:RECIPE"
  - milestone-summaries :: .orchestrator/milestones/M004/M004-SUMMARY.md:28:M004 replaced implicit agent-driven coordination with a mechanical engine layer. P01 evolved the constitution to v2.0 (13 principles + ANTIPATTERNS.md). P02 delivered 5 shared libraries (errors, events, run-context, guards, hooks) totaling 647 lines. P03 built the engine core (run.sh pipeline coordinator + checkpoint.sh crash recovery). P04 designed the YAML recipe system (context-recipe.yaml, hooks.yaml, extended routing.yaml + recipe-parser.sh). P05 refactored 3 dispatch scripts to be recipe-driven while preserving identical output. P06 integrated 6 additional scripts with the library stack, fixing the PROJECT_ROOT detection bug and adding run_id/error_kind to JSONL. P07 added recipe conformance diagnostics. 38 dispatches across 7 phases, 0 task-level retries for P06+P07. All scripts maintain standalone compatibility via ORCH_RUN_ID guards (NFR-204).
  - milestone-summaries :: .orchestrator/milestones/M005/M005-SUMMARY.md:16:  - "AD-1 sha256 format, AD-2 cost_source enum, AD-3 verdict protocol, AD-4 instruction schema, AD-5 pure functions, AD-6 provider convention, AD-19 AD-19 plan shape"
  - milestone-summaries :: .orchestrator/milestones/M005/M005-SUMMARY.md:18:  - "content-hash idempotency, cost_source closed enum, pure lib functions with no I/O, DOCTOR: structured output protocol, instruction schema conformance, verdict protocol (PASS/BLOCK/WARN/NEEDS_REVIEW), provider shell convention, advisory diagnostic classification, scored health reporting, AD-19 script-file verification shape"
  - milestone-summaries :: .orchestrator/milestones/M005/M005-SUMMARY.md:25:  - "DOCTOR: protocol (CONSTITUTION, EVENTS, HASHES, RUNIDS, PLANS, PERMISSIONS, PROVIDERS, INSTRUCTIONS), Checks passed: N/M, doctor-history.jsonl"
  - milestone-summaries :: .orchestrator/milestones/M005/M005-SUMMARY.md:28:Milestone M005 delivers foundational infrastructure hardening across 7 phases and 28 tasks. P01 establishes content-hash idempotency for the knowledge layer. P02 adds cost transparency to telemetry. P03 extracts pure transform functions from dispatch scripts. P04 creates the agent instruction schema with conformance checking. P05 establishes the gate verdict protocol and provider shell convention. P06 delivers 5 new diagnostic checks and a scored run-doctor.sh health report. P07 builds the autonomy permission pipeline with AD-19 plan shape guidance. All phases verified passing. Cross-phase boundary validation clean.
  - milestone-summaries :: .orchestrator/milestones/M005/M005-SUMMARY.md:8:  - "P01: content-hash idempotency (hash.sh, create/update/rebuild hash support); P02: cost transparency (record-telemetry.sh cost_source, aggregate-metrics.sh grouping); P03: payload transform extraction (payload-transforms.sh, manifest-builder.sh pure functions); P04: agent instruction schema (instruction-schema.md, check-instructions.sh); P05: gate verdict protocol (verdicts.sh, hooks.sh verdict parsing, provider-convention.md, check-providers.sh); P06: diagnostic suite (5 new checks, scored run-doctor.sh, extension.yml registration); P07: autonomy permission pipeline (generate/write/check-permissions.sh, AD-19 shape guidance)"
  - milestone-summaries :: .orchestrator/milestones/M008/M008-SUMMARY.md:14:  - "scripts/engine/intensity-analyze.sh,scripts/engine/intensity-recommend.sh,scripts/engine/intensity-gate.sh,scripts/engine/intensity-override.sh,scripts/engine/context-pressure.sh,scripts/dispatch/detect-capabilities.sh,scripts/dispatch/detect-runtime.sh,scripts/dispatch/dispatch-interface.sh,scripts/dispatch/backend-registry.sh,scripts/dispatch/adapters/backend/,scripts/dispatch/adapters/runtime/,scripts/dispatch/adapters/format/,scripts/state/resolve-root.sh,scripts/state/detect-speckit.sh,scripts/state/config-system.sh,scripts/state/namespace-aliases.sh,scripts/migrate/migrate-state.sh,scripts/knowledge/intensity-knowledge.sh,scripts/lifecycle/init-project.sh,scripts/lifecycle/detect-project.sh,scripts/lifecycle/reinit-handler.sh,scripts/lifecycle/check-update.sh,scripts/packaging/generate-skills.sh,scripts/packaging/build-bundle.sh,packaging/SKILL.md,packaging/skills/,packaging/bundle/,packaging/install/,templates/intensity-metadata.md,templates/dispatch-result.md,templates/dispatch-error.md,templates/project-instruction.md,commands/init.md"
  - milestone-summaries :: .orchestrator/milestones/M008/M008-SUMMARY.md:16:  - "Centralized stage×intensity matrix in one script (intensity-gate.sh) to avoid drift across 5 command docs; filename-based adapter routing (zero backend-specific code in core) per SC-003; coordination-boundary adapter pattern for in-process tools (MEM018); uniform-interface fallback (adapter always emits dispatch-result even on unavailability); HOME guard mandatory on runtime adapters; one-directional foreign format adapter (speckit.sh rejects --write with exit 4); hard migration per project constitution (no dual code paths); surgical documentation-only refactor of derive-phase.sh per Constitution XV; additive-only command doc refactor preserving MEM012; comment-aware Bash 3.2 compat scanner; open-standard SKILL.md format with YAML frontmatter; thin installer pattern delegating to P05 runtime adapters; offline-safe version check with graceful degradation; reinit default mode is update (preserve existing user context)."
  - milestone-summaries :: .orchestrator/milestones/M008/M008-SUMMARY.md:18:  - "Natural-language scope/risk/complexity pattern matching via keyword tables; pipeline composition via --flag file-or-text inputs for testability; intensity-aware threshold adjustment (Quick tightens, Full loosens); structured dispatch result/error schemas; filename-based adapter auto-discovery; atomic metadata rewrite via awk+mktemp+mv with cksum-sentinel verification; thin wrapper pattern with --dry-run mode; pure resolver pattern (read-only, zero side effects); hermetic standalone e2e via mktemp fixtures (never touch real HOME); runtime adapter HOME guard + project-dir guard; skill generation via single-source-of-truth transform with --check drift detection; project introspection via marker-file presence; user-edit preservation via comment-delimited blocks; top-level pipeline delegation (init orchestrates existing scripts); reinit-with-preservation via awk field-level surgery; wall-clock budget enforcement + hermetic-only static gate."
  - milestone-summaries :: .orchestrator/milestones/M008/M008-SUMMARY.md:8:  - "Standalone multi-runtime orchestrator with adaptive intensity: intensity engine (scope/risk/complexity analyzer + recommendation engine + metadata schema + context-pressure evaluator), uniform backend-agnostic dispatch interface (filename-based adapter routing with local-agent + local-codex backends), intensity-aware pipeline scaling (stage-level gate + override + knowledge wrapper across 5 command docs), state/namespace independence (resolve-root + detect-speckit + unified config + migration tool + namespace aliases), runtime adapters (Claude Code + Codex + Cursor with HOME/project-dir guards), format adapters (native round-trip + spec-kit one-directional read), multi-runtime packaging (SKILL.md spec + 12 skills + bundle + 3 installers + offline-safe version check), and onboarding (detect-project + project-instruction template + init-project + reinit-handler with user-edit preservation)."
  - milestone-summaries :: .orchestrator/milestones/M011/M011-SUMMARY.md:28:M011 (Spec Management) delivers the "paste a spec, get a roadmap" value proposition end-to-end. Seven phases took the orchestrator from an opaque-spec-file consumer to a full ingest→chunk→scope-filter→normalize→fidelity-gate pipeline that treats specs as structured knowledge on equal footing with the existing three-temperature knowledge model (M002/M007). P01–P03 built the deterministic chunker (spec directory tree with six subcategories, SPEC-prefixed natural IDs, nested-directory indexing, create-entry.sh SPEC- namespace support, full markdown→chunk classifier with classify+dispatch decision layer, versioned-ID supersession chains, REMOVED detection, phase-impact REVIEW emission). P04 wired scope-filter.sh to resolve `spec/*` scope tags (graph-mode + spec-scope-tags mode), letting build-context.sh deliver scope-filtered spec chunks in dispatch payloads per Constitution Principle I. P05 wired chunks-first evaluation and intensity-aware roadmap generation into commands/evaluate.md and commands/roadmap.md (spec-metrics.sh counts non-superseded chunks; spec-story-graph.sh emits directional story-to-story depends_on edges via bidirectional-traverse + directional-frontmatter-filter composition; intensity-gate.sh roadmap stage with Quick/Standard/Full modes). P06 delivered the user-facing commands/ingest.md, four doc-shape verify scripts, a full sandbox E2E gate script, a timing harness (<60s on representative specs), and dogfood evidence against specs/016-autonomous-hardening/spec.md (20 chunks, 7s elapsed). P07 closed with format-agnostic intake and a reusable Conversus fidelity gate: detect-spec-shape.sh probe classifier, normalize-spec.sh thin wrapper dispatching through dispatch-interface.sh (no hardcoded LLM calls), templates/spec-normalizer-prompt.md, a reusable Conversus adapter (scripts/dispatch/adapters/tool/conversus.sh with check/gate/parse-verdict subcommands, graceful degradation, CONVERSUS_STUB escape, three-value exit contract 0/1/2), commands/conversus-gate.md designed for M013/M014 reuse with different presets, templates/conversus-presets/normalize-fidelity.yml, templates/gate-result.md, and integration into commands/ingest.md as a pre-chunker 6-step pipeline (detect-shape → normalize → resolve-policy → fidelity-gate → chunker → rebuild) gated by a new `ingest` intensity stage (Quick=normalize; Standard/Full=normalize+fidelity-gate). A 222-line user guide (docs/ingesting-arbitrary-specs.md) covers flag semantics, BLOCK resolution, stub modes, and preset extension.
  - milestone-summaries :: .orchestrator/milestones/M011/M011-SUMMARY.md:36:Next up per the revised forward roadmap: M019 Tier-1 metrics emitter ships first as the kickoff unit after M011 closes, so M012–M014 dogfooding produces measured data on time/tokens/$/quality across task/phase/milestone/project granularities.
  - milestone-summaries :: .orchestrator/milestones/M012/M012-SUMMARY.md:109:- **D011 consequence — M020 promotion** — per `.orchestrator/DECISIONS.md` D011 trigger rule, the 1-of-3-criteria outcome at P02 close promotes **M020** as a committed milestone. Roadmap update positions M020 between M014 and M019 Tier 2/3 per D011's framing. (Review-state per page and dispatch-callable query surface — D011 criteria b/c — remain explicitly out of scope for M012 and land in M020.)
  - milestone-summaries :: .orchestrator/milestones/M012/M012-SUMMARY.md:111:- **Deferred-to-future-milestones (documented in M012-CONTEXT.md out-of-scope):** automated CI deploys on push, editing UI, staging/preview environments, pricing/usage analytics (→ M019 Tier 2/3), comment classification (→ M014), GitHub Issues/Projects/Milestones sync (→ M013), mobile UX / SEO / accessibility (→ M009), offline export, access control, custom search.
  - milestone-summaries :: .orchestrator/milestones/M012/M012-SUMMARY.md:115:None. M012 phase outputs are verified via static gates and per-phase orchestrators; no runtime telemetry, no logs. M019 Tier 1 metrics emitter (shipped `fdc3649`) will instrument future dogfood loops — M012 itself predates that emitter's instrumentation on its phase-close events.
  - milestone-summaries :: .orchestrator/milestones/M012/M012-SUMMARY.md:12:  - "M013 (GitHub Issues/Projects/Milestones sync inherits chained-gate wrapper + loud-fail convention + diagnostic idempotent-helper pattern); M014 (comment triage consumes Giscus threads keyed by pathname + wiki-giscus-remap.sh remap contract); M020 (promoted by D011 evaluation — extends knowledge subtree with review-state + query surface); M019 Tier 2/3 (M012 is first non-self dogfood substrate for unit_close records on future redeploys); .orchestrator/DECISIONS.md roadmap ordering (M020 insertion between M014 and M019 Tier 2/3); live gh-pages branch on this repo (operator-gated); consolidation runbook (wiki-giscus-remap.sh --dry-run invocation before any discussion-bearing rename)"
  - milestone-summaries :: .orchestrator/milestones/M013/M013-SUMMARY.md:16:  - "D014 — Knowledge-Layer Boundary: M020 retains chunk-ID authority; M013 cross-links the M020 schema and never redefines/extends it (zero writes to knowledge/spec/** from any P01–P04 task); FR-9 additive-only emit pinned to existing SPEC-* frontmatter; D015 — P02/P03 scope split applied as load-bearing markers (_deferred to P03_ cells fillable in place); FR-12 Claude-Code-only v1 binding via Constitution X+XII (Codex/Cursor parity demand-driven post-launch via M010); projection-not-peer grounding via XIV+M007+D007 (sidecar operator-owned + gitignored; in-repo template); FR-11 reversibility (all writes have absent/pending exit paths; lock released before exit 3/4); FR-16 rate-limit pre-flight + auth-expiry exit-code contract; FR-17 cost emission in M019 Tier 1 shape with source=runtime hard-coded; FR-18 status --verify-cache as read-only divergence probe; AD-19 single-script-file Check shape preserved across all verify gates with comment-discipline synonyms keeping bash32 scanners self-clean; conversus adapter --strict mode added as reusable invariant for callers requiring adapter presence (M011/P07 graceful-degradation flipped off via --strict / CONVERSUS_STRICT=1)"
  - milestone-summaries :: .orchestrator/milestones/M013/M013-SUMMARY.md:18:  - "additive-helper-append-preserves-byte-identity (P02 12 + P03 1 + P04 5 helpers appended at end-of-helpers; prior bodies untouched); gate-evolution-on-legitimate-advancement (P02→P03 mapping table; P03→P04 reference extensions — earlier-phase assertions evolve to accept both pre/post shapes; byte-identity hashes on unchanged content stay pinned); pinned-SHA-with-awk-range-capture-before-edit (multi-phase additive doc editing with three pinned ranges in references/github-integration.md); FR-12-v1-negative-grep-guard (sibling-runtime byte-identity via negative greps for post_verify/FR-12 markers — cheaper than pinned-sha and self-repairs if codex/cursor gain legitimate content under different markers); AWK-based JSON upsert + count + first-scalar extraction (no jq hard-dep across all 4 phases); M013_GH_STUB_DIR fixture-driven preflight + marker-search stub-selector (env-var-driven stub paths exercise every branch deterministically without gh calls); PATH-shimmed fake-gh for zero-subprocess-call invariant enforcement (auto-mode SC-7 enforcement logs every gh call + asserts log empty); per-call-stderr-mktemp-classify-rm (capture stderr to mktemp + classify_gh_rc rc + file + rm -f on all paths; releases lock before exit 3/4); direct-cmd-subst-rc-propagation (assign $() then read $? on next line; '|| true' in cmd-subst tail silently zeros $? — caught mid-T03); external-watchdog-plus-adapter-internal-timeout (Constitution XII belt-and-suspenders kill -TERM at TIMEOUT then kill -KILL at TIMEOUT+1s); verdict-rc-parity (rc=0 PASS / rc=2 BLOCK / rc=1 ERROR passed verbatim so BLOCK is distinguishable from error); pending-sentinel bootstrap with clobber-refusal exit 2 (extends M012/P04 DEPLOY-RECORD convention); fixture-copy-before-live-gate (cp -R fx/. tmp/ before invoking github-sync.sh because it reassigns ORCHESTRATOR_ROOT and would pollute repo fixture tree); stdout-record + stderr-summary split (fixture diffs stay byte-identical while operator still sees per-run summary); buffered-manifest-body via mktemp tempfile during walker iteration (counts finalized before header emission); FORMAT STABILITY CONTRACT doc-block at helper definition site (pins shape for downstream P03/P04 consumers); section-content byte-identity gate via awk-extract by literal-prefix heading match + shasum compare against embedded hashes (robust against insertion-shifts); load-bearing _deferred to P0N_ cells functioning as scope-boundary markers a later phase replaces in place (D015 split); comment-discipline synonyms in bash32 scanners (assoc-arrays / array-from-stdin builtins / case-conversion expansion / combined-redirect shorthand)"
  - milestone-summaries :: .orchestrator/milestones/M013/M013-SUMMARY.md:47:- **FR-11 reversibility** — All writes have absent/pending exit paths: `status --verify-cache` short-circuits to pending-operator-complete on absent OR pending sidecar; `perform_upsert` releases the lock before rate-limit/auth-expiry exits; post-verify hook fail-as-warning never blocks the dispatch loop.
  - milestone-summaries :: .orchestrator/milestones/M013/M013-SUMMARY.md:8:  - "opt-in GitHub native integration projecting orchestrator state to Issues + Project v2 + Milestones with FR-4 byte-identity markers; operator-owned sidecar at .orchestrator/integrations/github.json (gitignored) with templates/github-integration-sidecar.json schema (sync_mode + sub_issue_mode + per-item cache); tri-state orchestrator:github status command (absent / pending-operator-complete / configured) + status --verify-cache FR-18 read-only divergence probe; orchestrator:github init --re-init FR-14 adoption branch sourcing existing repo state via marker search; orchestrator:github sync 626-line walker + reconcile loop with --dry-run manifest + lock acquisition + updateProjectV2ItemFieldValue Project v2 status closure + FR-16 pre-flight rate-limit probe + per-call classify_gh_rc wrapper releasing lock before exit 3/4 (FR-11 reversibility); UAT bug ingestion loop (.github/ISSUE_TEMPLATE/uat-bug.yml + scripts/integrations/uat-ingest.sh + knowledge/spec/defect/SPEC-DEFECT-NNN schema with chunk-lookup-failed sentinel); FR-9 additive ## Spec Chunks emit in scripts/knowledge/rebuild-index.sh + scripts/knowledge/lib/index-utils.sh emit_spec_chunks_section helper; opt-in conversus UAT PR gate scripts/integrations/github-conversus-gate.sh wrapping M011/P07 adapter --strict with 30s external watchdog (Constitution XII) + FR-13 preset templates/conversus-presets/m013-uat-defect-merge.yml (red-blue defect-advocate vs fix-advocate, BLOCK-on-tie); post-verify lifecycle hook packaging/bundle/hooks/post-verify.json + scripts/lifecycle/after-verify-sync.sh wrapper + claude-code.sh runtime adapter hook_count 5->6 + FR-12 v1 Claude-Code-only enforcement via negative-grep guard on codex/cursor adapters+installers; scripts/integrations/github-common.sh shared lib (12 P02 helpers + 1 P03 helper + 5 P04 helpers — orchestrator-id derivation + FR-4 marker primitives with shasum byte-identity + AWK depth-aware sidecar item upserts + http_probe + sidecar_update_item_cache + emit_tier1_record + classify_gh_rc + emit_conversus_gate_record + gh_marker_search_remote + auth/sub-issue/label-collision preflights); scripts/verify/graphql-call-shape.sh FR-5 three-shape whitelist CI lint; references/github-integration.md full P01..P04 reference doc with Auth Modes / Sub-Issue Representation Modes / Full Mapping Table / Re-init Adoption Contract / Sync Workflow / Sync Action Mapping / Scope Boundary table; commands/github-init.md + commands/github-sync.md + commands/github-status.md (MEM012 structure); FR-17 unit_close + conversus_gate_invocation JSONL emitters at ${ORCHESTRATOR_ROOT}/observability/tier1.jsonl in M019 Tier 1 shape; scripts/integrations/sidecar-init-pending.sh pending-sentinel bootstrap with clobber-refusal exit 2; ~30 verification gates across 4 phase-suite orchestrators (P01 9 + P02 8 + P03 8 + P04 12 + FR-5 lint regression guard)"
  - milestone-summaries :: .orchestrator/milestones/M018/M018-SUMMARY.md:176:- `compression-eval.sh --milestone <M> --tier <N>` — cohort segmentation
  - milestone-summaries :: .orchestrator/milestones/M018/M018-SUMMARY.md:25:  - "execution-log.jsonl record_types: payload_filter, payload_breakdown (additive filter_dropped_tokens / tier1_savings_tokens / tier2_savings_tokens / tier1_invocations / tier3_compression_savings_tokens / tier3_invocations), dispatch_usage (additive same six fields, rolled up), unit_close (additive same six fields, granularity-aware roll-up), compression_underperformance, tier_preservation_violation, tier3_skipped, tier3_failed, tier3_no_savings, runtime_parity; metrics-rollup.sh stdout: FILTER_DROPPED / TIER1_SAVINGS / TIER2_SAVINGS / TIER1_INVOCS / TIER3_SAVINGS / TIER3_INVOCS columns at indices 13-18; efficiency-footer.sh stdout: compression: <pct>% reduction over baseline (filter+tier1+tier2+tier3 / payload_tokens) tail; check-anomalies.sh stdout: FLAGGED ... reasons=compression-regression; compression-eval.sh stdout: cohort + delta block with 95% CIs and regression_flag advisory; m018-runtime-parity.sh / m018-runtime-parity-tier3.sh stdout: per-runtime SHA-256 / routing lines + parity summaries + regression_flag advisory; references/RUNTIME-ASSUMPTIONS.md: # Compression (M018) block with RA-M018-NN divergence rows"
  - milestone-summaries :: .orchestrator/milestones/M018/M018-SUMMARY.md:8:  - "knowledge-aware injection filter; Tier 1 microcompact (tool-result paging + SHA-256 disposable cache); Tier 2 snip (head-drop + protected tail + boundary-refusal walker); Tier 3 auto-compact (dispatch-routed LLM summarization with intensity gate + density pre-check + failure-passthrough); operator surfaces (cost rollup compression columns, efficiency-footer compression line, doctor compression-regression flag); compression-eval.sh cohort-segmentation diagnostic; multi-runtime parity diagnostics (zero-LLM byte-equality + Tier 3 routing); references/RUNTIME-ASSUMPTIONS.md registry; references/compression-grammar.md v1.0.1 (Reviewed via conversus); preservation-check library; cache-prune.sh utility"
  - milestone-summaries :: .orchestrator/milestones/M018/M018-SUMMARY.md:80:  from in-scope payload_breakdown rows; granularity-aware scope match for
  - milestone-summaries :: .orchestrator/milestones/M018/M018-SUMMARY.md:87:  cohort-segmentation diagnostic (Wilson 95% CI for proportions,
  - milestone-summaries :: .orchestrator/milestones/M019/M019-SUMMARY.md:16:  - "AD-1 char-quartile token estimate;AD-2 pricing file + ORCH_PRICING_FILE override;AD-3 quality from existing fields;AD-4 record_type/source/granularity enums;AD-5 stable-before-volatile ordering;AD-7 sentinel-scoped settings overwrite;AD-19 single-script Check shape;SC-6 byte-identical stdout;SC-10 pre-M019 additivity;SC-12 P00->P01 epoch 2026-04-18T02:21:28Z;SC-13 no regression;C2 Goodhart pairing;C4 never-abort pricing degradation;C5 bash 3.2"
  - milestone-summaries :: .orchestrator/milestones/M019/M019-SUMMARY.md:18:  - "Env-var side-channel for legacy-helper behavior injection;Zero-token instrumentation via stdout capture-and-replay;Byte-identical-modulo-normalization diff;Sentinel-scoped JSON span overwrite bash 3.2 no jq;Never-abort degradation via module-scoped warning channel;Module-scoped globals subshell loss workaround via mktemp capture;BSD awk in-keyword pitfall remedy;Split-needle self-match avoidance in pattern gates;Trap-safe mutate/restore;Hermetic tmp-snapshot pattern with ORCHESTRATOR_ROOT override;Filename-routed stub backend adapter;Directory-path artifact check;Lexical ISO-8601 timestamp comparison;Child-granularity pass_rate filter;Prefix-match any-null propagation in cost rollup"
  - milestone-summaries :: .orchestrator/milestones/M019/M019-SUMMARY.md:8:  - "Tier 1 observability and efficiency metrics emitter producing payload_breakdown, dispatch_usage, and unit_close JSONL records at task/phase/milestone boundaries; Goodhart-paired cost+quality blocks on every unit_close; Opus 4.7 adapted dispatch baseline (L1 first-turn completeness, L2 stable-before-volatile ordering with <dispatch-volatile> markers, L3 adaptive-thinking contract, L4 parallel-fan-out directive, L5 positive-example rewrites); AD-7 sentinel-scoped settings overwrite; scripts/lib/pricing.sh sourceable pricing library with never-abort degradation; scripts/verify/m019-schema.sh JSONL validator with source/granularity/record_type enums reserved for Tier 2/3 forward compat; 12 verify gates (4 P00 + 8 P01) enforcing SC-6/SC-10/SC-12/SC-13 contracts"
  - milestone-summaries :: .orchestrator/milestones/M020/M020-SUMMARY.md:16:  - "D024,AD-1,AD-2,AD-3,AD-4,AD-5,AD-6,AD-7,AD-8,FR-1,FR-2,FR-3,FR-4,FR-5,FR-6,FR-7,FR-8,FR-9,FR-10,CON-1,CON-2,CON-3,CON-4,CON-5,THREAT-006,THREAT-007,SC-1,SC-2,SC-3,SC-4,SC-5,SC-7,SC-8,US-1,US-2,US-3,US-4,US-5,OQ-1,OQ-2,OQ-3,OQ-4"
  - milestone-summaries :: .orchestrator/milestones/M029/M029-SUMMARY.md:99:- **M035 P00+P01 next**: dev-ergonomic prep before launch (`--mode=symlink` install + `orchestrator:status` version-drift warning) + multi-consumer-project freshness today; M035 P02–P06 IS the launch event (npm + homebrew + curl-pipe-bash publishing pipelines, GH release automation, install-script integrity, `orchestrator:update` first-class command). Operator note: invoke as `orchestrator:auto milestone=M035` to bypass the M036b deferred-state paper-cut.
  - milestone-summaries :: .orchestrator/milestones/M030/M030-SUMMARY.md:16:  - "D-A1 4-verdict closed-enum (flip_recommendation ∈ {ready, partially_ready, block, evidence_insufficient}); D-A2 programmatic shadow-compare invocation as the FR-9 flip-gate (live routing refuses adapter call if corpus below threshold; emits override_source=shadow_gate_blocked); D-A3 partial-flip safety (only smart-defaulted classes may be enumerated in withheld_classes — fast/balanced classes flip wholesale or block); D-A4 mechanical independence (P00 fixture corpus committed BEFORE P01 classify-task.sh authored — independence-by-construction; verifier graduates post-P01 to git-log ordering check); D-A5 kill-switch precedence over min_tier (model_routing_enabled: false short-circuits before flip-gate or escalation logic; stderr names the bypassed min_tier); D-A6 cost_rates SSOT in templates/model-routing.yml (single source of truth for rollup + counterfactual computation); D-A7 / SC-3a write-path correctness (every shadow record's model_routed agrees with an independent classifier run on the same plan; classifier_confidence emitted end-to-end in P02 not deferred to P03); D-A8 escalation cap CON-5 (3 attempts max: fast → balanced → smart → escalation_cap_hit; no fourth record); D-A9 anomaly-record JSONL append-only convention (separate file .orchestrator/anomalies.jsonl preserves CON-6 dispatch-stream invariant on execution-log.jsonl); P01 classifier-confidence stability metric definition (rolling variance ≤ 0.10 + N=20 minimum class-coverage; consumed end-to-end by shadow-compare.sh); P02 4-verdict closed-enum + shadow-corpus path-resolution priority (env var M030_SHADOW_COMPARE_CORPUS → --corpus flag → default); P03 override-source closed-enum closure + kill-switch precedence over min_tier (compound config tested via SC-7a); P04 escalation-cap programmatic enforcement + flip-gate refusal of below-threshold corpus; P05 cost_rates-absent fallback (warning + zero-savings line) + ORCHESTRATOR_ROOT carve-out for footer fixtures; P06 #Q-4 threshold default (pass_rate 0.50 + min_class_sample 10) + env-only JSONL emit path (M030_ANOMALIES_JSONL_PATH default .orchestrator/anomalies.jsonl) instead of CLI flag; P07 4-task split (corpus + battery + ledger + close-ceremony) + structurally-distinct T04 ceremony fusing phase + milestone close into one atomic commit"
  - milestone-summaries :: .orchestrator/milestones/M035/M035-SUMMARY.md:137:2. **M023 — Design layer** (deferred post-launch). `orchestrator:design` spawns N design-personality agents in parallel via conversus, each producing a DESIGN.md draft + working coded prototype; user picks side-by-side; renderer adapter shaped as MCP clients. Better as a fast-follow once real users arrive with real UI projects.
  - milestone-summaries :: .orchestrator/milestones/M035/M035-SUMMARY.md:16:  - "D-RN-1..D-RN-7 (namespace rename: @build-fractal/orchestrator + orchestrator: cohort prefix + git remote rename + working-dir mv + Claude memory project-key migration + reversible pre-rename tag); D001 (CI runner = ubuntu-latest); D002 (npm pack to local tarball + npm install -g into mktemp prefix; no real-registry calls); D003 (engines.node>=14 + os:[darwin,linux] npm-side Windows fail-closed); D004 (sigstore keyless primary + SHA-256 fallback resolves spec #Q-3); D005 (rollback-marker schema five-field key=value sidecar plus snapshotted manifest); D007 (Homebrew tarball source = re-use the P05-signed npm pack tarball — single source-of-truth for cross-channel byte-equivalence per CON-5); D008 (Tap-push mechanism = secrets.HOMEBREW_TAP_TOKEN fine-grained PAT scoped to Build-Fractal/homebrew-orchestrator:contents:write only); D009 (install.sh URL host = GitHub release asset URL: latest/download for unpinned + download/v<X.Y.Z>/ for pinned); D010 (CON-8 release-workflow CI timeout = 20 minutes per-job on ubuntu-latest with escalation clause); D011 (release cadence = manual stable releases pre-1.0; operator-policy not code surface); D012 (update_source: git|npm|homebrew|none — curl-pipe-bash collapses to npm per D007/D009; literal none is operator opt-out); D013 (5-condition JSONL suppression matrix mapped verbatim from M027); D014 (AD-5 detect-by-install-method-first resolution order: install-meta runtime= → npm root -g → brew --prefix → git fallback; first-match wins; persist detected source); FR-2..FR-22 (full FR set across the spec); CON-3 / AP-009 (compound-chain shape-guard honored across all installers / verifiers / workflow YAML); CON-5 (cross-channel byte-equivalence lifted to load-bearing 3-way at P04 close); CON-6 (no new long-lived secrets; defense-in-depth via job-condition + runtime negative-assertion step); CON-7 (M025 reversibility-gate preserved through every dispatch arm); CON-8 (release-workflow timeout escalation clause); AD-5 (detect-by-install-method-first); AD-19 (single-script-file shape across every verifier + phase-suite + acceptance battery); MIT-2 (canonical exclusion list lives in references/installation.md); MIT-9 (Windows fail-closed via npm os array); SC-1..SC-16 (full SC set verified through phase-suites + acceptance battery + validate-milestone)"
  - milestone-summaries :: .orchestrator/milestones/M035/M035-SUMMARY.md:46:- **P04 (closed 2026-05-09)** — Curl-pipe-bash channel + 3-way cross-channel byte-equivalence: `packaging/install/install.sh` bash 3.2 / POSIX-sh-safe runtime-detect + tarball-fetch + dispatch wrapper hosted at the GitHub release asset URL (D009 — `latest/download` for unpinned + `download/v<X.Y.Z>/` for pinned); job-level `timeout-minutes: 20` with CON-8 escalation clause (D010); D011 manual stable releases pre-1.0 cadence (operator-policy not code surface); cross-channel byte-equivalence lifted from 2-way to load-bearing 3-way (`NPM_HASH = HOMEBREW_HASH = CURL_HASH`); MOS-4 first-release `curl … | bash` smoke + MOS-5 synthetic `v0.0.0-test` fork tag-push deferred to live release time. Phase-suite `pass=6 fail=0`. Duration 39m.
  - milestone-summaries :: .orchestrator/milestones/M035/M035-SUMMARY.md:56:- **#Q-4 (rollback marker schema)** → five-field `key=value` sidecar plus snapshotted manifest at upgrade-time (D005 at P05 T01).
  - milestone-summaries :: .orchestrator/milestones/M035/M035-SUMMARY.md:57:- **#Q-5 (release cadence)** → manual stable releases pre-1.0 (D011 at P04 T04).
  - milestone-summaries :: .orchestrator/milestones/M035/M035-SUMMARY.md:8:  - "Pre-launch dev-ergonomics layer (P00) — installer hardening across the three claude-code/codex/cursor installers (bash 3.2 process-substitution exit-status capture replacement; managed .gitignore block primitive; wiki-stubs-fresh diagnostic + pages.yml pre-build gate; M032 SC-5 fixture-completeness fallback in wiki-init.sh --deploy; D-RN-1 npm-name collision-check evidence). Symlink-mode + drift-line surface (P01) — user-facing --mode=copy|symlink flag promoted from M032's TEST-ONLY --asset-mode-override; symlink branch retargeted directly at $REPO_ROOT/<src> (US-1 dogfood-velocity contract — git pull updates every consumer instantly); install-meta.txt extended with always-present commit_sha= + version= fields; mode-aware uninstall (-L test for symlink-mode, -f test for copy-mode); read-only check-orchestrator-drift.sh helper + status-line drift surface in both TUI + JSON. Spec-kit-orchestrator → orchestrator namespace rename (P01.5) — D-RN-1..D-RN-7 decision block (anchors dr-code-029..dr-code-035); reversible pre-rename git tag v0.9.2-final-spec-kit-name; specs/001-speckit-orchestrator → specs/001-orchestrator git-mv with full history preservation; C1/C2/C3/C4/C5/C6 sweep cohort across 158+ files / ~308 occurrences. npm publishing channel (P02) — package.json + bin/orchestrator with @build-fractal/orchestrator (D-RN-1); engines.node>=14 + os: [darwin,linux] (D003 / MIT-9 Windows fail-closed); packaging/npm/postinstall.sh thin delegate (INIT_CWD-aware + Claude Code soft-fail + DRY_RUN=1); Constitution Principle XVI cross-channel byte-equivalence test (CON-5, single-channel reflexive at P02 close); .github/workflows/release.yml two-job split with CON-6 defense-in-depth (structural job-condition + runtime negative-assertion step); per-subdir .npmignore as load-bearing dogfood-exclusion layer when files: whitelist is present. Homebrew tap publishing channel (P03) — packaging/homebrew/orchestrator.rb.tmpl + render-formula.sh; release.yml homebrew-publish job needs:npm-publish (D007 single-source-of-truth tarball — no separate brew-tarball); HOMEBREW_TAP_TOKEN fine-grained PAT scoped to Build-Fractal/homebrew-orchestrator:contents:write only (D008); cross-channel byte-equivalence lifted to load-bearing 2-way (NPM_HASH = HOMEBREW_HASH). Curl-pipe-bash channel + 3-way byte-equivalence (P04) — packaging/install/install.sh bash 3.2 / POSIX-sh-safe runtime-detect + tarball-fetch + dispatch wrapper hosted at the GitHub release asset URL (D009); job-level timeout-minutes: 20 with CON-8 escalation clause (D010); D011 manual stable releases pre-1.0 cadence; cross-channel byte-equivalence lifted to load-bearing 3-way (NPM_HASH = HOMEBREW_HASH = CURL_HASH). Install-script integrity (P05) — sigstore keyless signing + SHA-256 fallback in npm-publish job (D004 resolves spec #Q-3); job-level permissions:id-token:write override; rollback marker contract (D005 — five-field key=value sidecar plus snapshotted manifest under .orchestrator/.rollback/); orchestrator:update --rollback dispatch with #Q-G8 verbatim symlink-mode refusal advisory; references/installation.md ## Verifying integrity operator-facing section (cosign verify-blob + shasum -c paths). Multi-source dispatch + milestone-grain acceptance battery (P06) — update_source: git|npm|homebrew|none config schema (D012); resolve_update_source + persist_update_source helpers + four-arm dispatch in run-update.sh (D014 AD-5 detect-by-install-method-first); emit_update_run_event + --no-emit-jsonl flag + 5-condition suppression matrix (D013 verbatim from M027); commands/update.md ## Update sources H2 with 4 H3 subsections; tests/m035-acceptance/run-acceptance-battery.sh milestone-grain acceptance battery (SC-15 self-reference); fixture quartet for SC-13; m035-p06-phase-suite.sh aggregator (summing-counters form); m035-p06-milestone-close-shape.sh + M035-VALIDATED + M035-SUMMARY.md + milestone-grain unit_close JSONL (SC-16 milestone-close oracle)"
  - milestone-summaries :: .orchestrator/milestones/M044/M044-SUMMARY.md:88:- **#Q-4** pin `provenance_version: 1` now (P01).
  - milestone-summaries :: .orchestrator/milestones/M045/M045-SUMMARY.md:22:- **Capability** (`detect-capabilities.sh` `headless_reentry`) — can we spawn a
  - milestone-summaries :: .orchestrator/milestones/M045/M045-SUMMARY.md:26:  re-spawns a fresh `claude -p` per rotation-exit until a terminal outcome, the
  - milestone-summaries :: .orchestrator/milestones/M045/M045-SUMMARY.md:53:| SC-2 terminal-never-respawn | P03 `m045-p03-driver-terminal.sh` | PASS |
  - milestone-summaries :: (capped at 5 hits — more may exist)

### Q5 — KEPT

- **question**: - #Q-5 (second-gate-substrate): Is the FR-11 second gate a cheap second-model call, the M042 corpus-gate, or both?
- **disposition**: kept (plan-phase cost/precision trade-off per spec; kept at discuss gate 2026-07-02)
- **terms** (10): Q-5, FR-11, second, gate, substrate, cheap, model, call, m042, corpus
- **hits**:
  - constitution :: .orchestrator/memory/constitution.md:108:- Distribute knowledge hierarchically (not in monolithic files).
  - constitution :: .orchestrator/memory/constitution.md:126:  tokens" typically increases total tokens spent because the agent
  - constitution :: .orchestrator/memory/constitution.md:130:  cheapest pipeline available*. A future execution path that
  - constitution :: .orchestrator/memory/constitution.md:14:  land in the new Quality Gates bullet body — Decision B resolved
  - constitution :: .orchestrator/memory/constitution.md:146:- If verification cannot be performed mechanically, the task plan
  - constitution :: .orchestrator/memory/constitution.md:235:  task cheaper to execute by reducing the context that task needs
  - constitution :: .orchestrator/memory/constitution.md:261:- Removing dead infrastructure is always cheaper than maintaining it.
  - constitution :: .orchestrator/memory/constitution.md:283:- No inline `date` calls — use `$ORCH_STARTED_AT` or run-context
  - constitution :: .orchestrator/memory/constitution.md:302:- Model selection and fallback chains: declared in `routing.yaml`.
  - constitution :: .orchestrator/memory/constitution.md:4:  Version change: 2.2.1 → 2.3.0 (MINOR — Quality Gates section gains
  - constitution :: .orchestrator/memory/constitution.md:5:  a new "Install gates" bullet per follow-on amendment Item 3 of
  - constitution :: .orchestrator/memory/constitution.md:55:    - Quality Gates — at v2.3.0 gains a new "Install gates" bullet
  - constitution :: .orchestrator/memory/constitution.md:85:       templates dynamically load constitution; no hardcoded principle
  - constitution :: .orchestrator/memory/constitution.md:9:  invariant body to constitution Quality Gates as release-time
  - constitution :: (capped at 5 hits — more may exist)
  - decisions :: .orchestrator/DECISIONS.md:102:Inspection of the conversus source (`~/Sites/conversus`) showed that `/conversus gate <phase> <artifact>` already provides the exact CI-shaped primitive orchestrator needs — structured `PASS \
  - decisions :: .orchestrator/DECISIONS.md:127:- **Choice**: Revised sequence: **M011 → [M019/Tier 1 emitter] → M012 → M013 → M014 → [M019/Tier 2+3] → M018 → M009 → M010**. Tier 1 ships as a small standalone unit once M011 is closed — avoids mid-milestone scope insertion (Constitution XV — Surgical Precision), gives a clean boundary for instrumentation to start, and still captures M012+M013+M014 (~3 full milestones of dogfooding) before M018 planning needs a baseline. Cost of not backfilling M011 dispatches: ~15–25 unlogged records from M011/P04–P07; acceptable tradeoff. M019 records time, token cost, estimated dollar cost, retries, deviations, verification pass rate, and domain-unit cost (per ingested spec / merged PR / auto-applied comment) at task/phase/milestone/project/all-time granularities. Three-tier framing: **Tier 1 — Just emit** (~1 day: append `payload_breakdown`, `dispatch_usage`, `unit_close` JSONL records to existing `execution-log.jsonl`; no UI; ships first thing after M011 close so M012–M014 dogfooding produces real data); **Tier 2 — Rollup + `orchestrator:cost` command** (~3 days: `scripts/diagnostics/metrics-rollup.sh`, `.orchestrator/metrics/*.jsonl` aggregates, `orchestrator:status` efficiency footer; worth it if we launch); **Tier 3 — Full polished surface** (~7 days: backend-specific `adapters/backend/*/report-usage.sh`, auto-generated `MNNN-METRICS.md` on consolidate, `orchestrator:doctor` anomaly checks, parity evals across runtimes; only if efficiency becomes M009 launch headline). Full M019 phase outline mirrors the tiers: P01 schema + pricing table, P02 dispatch-time emitter (Tier 1 boundary), P03 backend usage adapters, P04 rollup + `orchestrator:cost` command (Tier 2 boundary), P05 milestone close report, P06 status + doctor integration, P07 evals + multi-runtime parity (Tier 3 boundary). Schema must pair cost metrics with **quality metrics** (verification pass rate, deviation rate) from day one to avoid Goodhart failure mode — optimizing cheap over correct. All records labeled `source: "estimate" \
  - decisions :: .orchestrator/DECISIONS.md:141:- **Choice**: Replace the D008 phase outline's "P02 dispatch compressor (optional filter...)" framing with a tier-ladder model informed by Article 3 (Rohit's Claude Code harness teardown) and logged in `.orchestrator/scratch/articles-synthesis-2026-04-17.md`. Every dispatch runs through tiers in order, cheapest first: **Tier 1 microcompact** (reuse cached tool-call results that haven't changed + persist oversized tool results to disk with `file_path + preview` references — absorbs L6 tool-result budgeting; zero LLM cost); **Tier 2 snip** (head-drop with protected tail; no LLM call); **Tier 3 auto-compact** (summarize via LLM call; existing caveman-style territory); **Tier 4 context collapse** (staged multi-phase compression of tool results → reasoning → sections; feature-flagged). Intensity maps to tier ceiling: Quick→T1–T2 only, Standard→through T3, Full→all tiers available. Revised phase outline: P01 tier grammar + per-tier safety boundaries (what's preservable per tier), P02 Tier 1 microcompact + tool-result budgeting, P03 Tier 2 snip with protected-tail semantics, P04 Tier 3 auto-compact (summarization, intensity-gated), P05 Tier 4 collapse (staged; feature-flagged, off by default), P06 eval harness (3-arm parity tests per tier), P07 multi-runtime parity. D008's original scope envelope (roundtrip-safe by policy, grammar-aware preservation, intensity-bound, explicit opt-out surfaces, telemetry-integrated) carries forward unchanged — only the internal staging model is reframed. Fresh `orchestrator:discuss` at M018 kickoff addresses detailed tier design once M019 Tier 1 data reveals which artifacts dominate token spend. **Conversus integration (locked in per D007 reuse pattern)**: M018/P01 tier-grammar + per-tier-safety-boundary work invokes `scripts/dispatch/adapters/tool/conversus.sh` via `role/red-team` + `domain/security` presets at plan-review time. Rationale: grammar decisions carry parse-regression risk across downstream scripts that grep prose (the risk D008 originally flagged), which is exactly the subjective-quality territory the conversus fidelity-gate adapter earns its cost on. A new preset drops under `templates/conversus-presets/` without adapter modification (same pattern M013/M014 use). Gate is intensity-bound by default (Quick=off, Standard=advisory, Full=blocking); `--no-review` escape available.
  - decisions :: .orchestrator/DECISIONS.md:142:- **Revisable**: Yes — individual tiers can be dropped if M019 Tier 1 data shows they don't earn their complexity (e.g., if measured tool-result sizes are already small, skip T1 microcompact in favor of direct T2 snip). Tier ladder is the framing; specific tier set is open. If M018 kickoff reveals the ladder itself is overkill for orchestrator's payload profile, collapse back to D008's single-filter model with this amendment noted as a considered-and-rejected alternative.
  - decisions :: .orchestrator/DECISIONS.md:144:Single-filter framing would bias planning toward one tier (most likely summarization, the expensive option) and miss the compounding savings of cheaper tiers that handle the majority of cases at zero LLM cost. The four-tier pattern is production-validated in Claude Code's harness. Amendment lands *now* (before M018 planning kicks off) to correct roadmap framing integrity — D008 as written would have anchored discuss-at-kickoff on the wrong mental model. Detailed design questions (per-tier grammar, intensity mapping specifics, parse-regression risk per tier) deferred to M018/P01 discuss once measured data exists.
  - decisions :: .orchestrator/DECISIONS.md:15:- **Choice**: P06 scope expanded by three engine-layer workarounds discovered during P03 implementation: (1) literal-audit-marker printf pattern added after SAFETY_WARNING emissions in scripts/engine/run.sh (T03/T04) because events.sh _orch_events_quote does not quote single-word values, breaking must-have greps that expect quoted form; (2) cd $REPO_ROOT subshell wrapper around check-must-haves.sh and record-result.sh calls in scripts/engine/run.sh (T05) working around check-must-haves.sh PROJECT_ROOT walk-up bug that computes PROJECT_ROOT as .specify/orchestrator/milestones/<m> instead of repo root; (3) execution-log.jsonl entries from engine dispatch do not thread ORCH_RUN_ID because record-result.sh does not currently accept/emit run_id. P06 cleanup should: fix check-must-haves.sh PROJECT_ROOT detection, extend events.sh _orch_events_quote to always quote values, extend record-result.sh to thread run_id from env, then remove all three workarounds from run.sh.
  - decisions :: .orchestrator/DECISIONS.md:155:- **Choice**: Context: `.orchestrator/scratch/articles-synthesis-2026-04-17.md` identified a coherent knowledge-layer theme bundling L7 (wikilinks), L8 (explored state), A1 (preferences layer), A2 (candidate→graduate with required rationale), A3 (review queue in status), A4 (AGENTS.md read-order map), A6 (Jaccard clustering in consolidate). Without a logged trigger, the M020 decision drifts. Evaluation rule at M012/P02 close: assess whether M012's spec-wiki implementation ships **(a)** clickable cross-refs from spec pages to `knowledge/**/MEM*.md` entries via shared ID namespace, **(b)** a review/unreviewed state model on generated pages, **(c)** a query/search surface callable from orchestrator dispatches (wiki-query analog). **Decision rule**: ≥2 of 3 land → M020 dissolves into knowledge-tooling maintenance PRs with no milestone commitment (M012 patterns cover enough of the surface); ≤1 of 3 land → promote M020 as a committed milestone positioned between M014 and M018 (before M019 Tier 2/3, so A5 on_failure flagging can draw on its data). **Minimum-viable subset ships regardless of the M020 decision**: L8 (explored state on auto-generated MEM entries) + A2 minimal guard (`scripts/knowledge/graduate.sh --rationale "..."` flips `status: candidate` → `status: graduated`) bundled into a standalone maintenance PR. Rationale: these are small (~30 lines), address the rubber-stamping failure mode directly, and compound M019 Tier 1's measurement value. The *full* A2 workflow (clustering, staging layer, review queue UI, decision history) waits for the M020 decision. **A4 also ships standalone**, before M019/P00, as an independent concern (session-start file-loading manifest for non-Claude-Code runtimes) — not bundled with P00 because P00's scope discipline is "dispatch-facing payload adaptation only."
  - decisions :: .orchestrator/DECISIONS.md:170:- **Revisable**: No — once the hook is live and the replay corpus is in CI, rolling back is a permission-only change in `.claude/settings.json` (disable the hook entry) but the corpus and AP-005..AP-009 entries stay as knowledge (constitution VII).
  - decisions :: .orchestrator/DECISIONS.md:172:Evidence-grounded: 20 M011/P05–P07 screenshots define the closed pattern matrix (AD-2, AD-5); no speculative additions (constitution XIV). M021 itself dogfoods via its own `orchestrator:auto` execution through P01–P04 (AD-8) — SC-7 attestation is produced by `scripts/verify/m021-p04-dogfood-attestation.sh` and the permanent replay corpus at `tests/fixtures/m021-prompt-corpus.txt`. Adding M021 ahead of M019 costs ~1 milestone of sequencing but eliminates the noise floor that would otherwise contaminate every subsequent metrics measurement — principle I (Context Minimization) applied to the measurement apparatus itself.
  - decisions :: .orchestrator/DECISIONS.md:184:- **Revisable**: Yes -- M020 can be deferred past M013/M014 if those milestones absorb knowledge-layer work organically (e.g., M014 comment-automation classification overlaps with A6 Jaccard clustering). M020 position before M019 Tier 2+3 is load-bearing -- Tier 2/3 rollups use M020 data shape; swapping order requires re-planning both.
  - decisions :: .orchestrator/DECISIONS.md:186:M012/P02 D011-EVALUATION.md (.orchestrator/milestones/M012/phases/P02/D011-EVALUATION.md) records the mechanical outcome: 1 of 3 criteria shipped (cross-refs to knowledge/**/MEM*.md entries via shared ID namespace via the include-markdown pipeline; review/unreviewed state model NOT shipped; query/search surface callable from dispatches NOT shipped). D011's decision rule: <=1 of 3 -> promote as committed milestone, positioned between M014 and M019 Tier 2+3 so A5 on_failure flagging can draw on its data. M012's cross-ref affordance (pathname-keyed wiki stubs with include-markdown) is necessary but not sufficient for the full A1/A2/A3/A6 knowledge-layer workflow; M020 ships the remaining review-state + query-surface + clustering surface. Minimum-viable subset (L8 explored state + A2 minimal graduate.sh guard + A4 session-start manifest) still ships independently before M020 per D011 framing; full A2 workflow and review queue UI wait for M020 kickoff.
  - decisions :: .orchestrator/DECISIONS.md:228:User is dogfooding CC-exclusively right now; Codex parity is a launch concern, not a dogfood concern. Back-loading Codex validation to M009 is constitutionally cleaner (XIV — don't build Codex plumbing speculatively before CC usage teaches us what needs to be portable) and keeps the current dogfooding loop fast. M023/M024 are the adoption layer — M024 ships where its internal dogfood signal is cheapest (post-M020, exercised on every remaining milestone), M023 ships where its external signal is cheapest (pre-launch, since this repo has no UI to dogfood design against). MCP-as-adapter for design renderers means M023 is runtime-agnostic by construction (CC + Codex + Cursor all support MCP). Extending `evaluate` rather than adding a new `orchestrator:start` command keeps the surface small (XIV). Proposal-as-artifact gates the router on Principle III (Design Before Code). The `RUNTIME-ASSUMPTIONS.md` discipline is a lightweight hygiene practice — a few lines of markdown per observation — preventing "CC-first" from silently becoming "CC-only." Supersedes the forward-sequence framing from D004/D005/D006/D008/D013 where M013→M014→M020→M019 T2+3→M018→M009→M010 was the last committed order.
  - decisions :: .orchestrator/DECISIONS.md:29:- **Choice**: P06 scope expanded by five additional items discovered during P05 dispatch refactors: (1) scripts/lib/run-context.sh _orch_run_nonce SIGPIPE under set -o pipefail — causes scripts/engine/run.sh --dry-run to exit 141 when ORCH_RUN_SEED is unset; worked around in T02/T03/T04 with set +o pipefail wrapper around init_run_context (3 replicated call sites to drop after P06 fix). (2) scripts/dispatch/lib/section-handlers.sh handle_phase_summaries has a Bash 3.2 while-read bug with unterminated dep-list temp file — worked around by local _bc_handle_phase_summaries_fixed shim in build-context.sh (to drop after P06 fix). (3) templates/context-recipe.yaml default section order/priority values don't match pre-refactor dispatch-prompt parity — worked around by _bc_display_order/_bc_display_name/_bc_display_priority shim in build-context.sh (to drop after P06 recipe re-authoring). (4) scripts/verify/check-must-haves.sh doesn't discover tasks/TNN-PLAN.md files — all P05 tasks had to verify must-haves manually from payloads rather than via the canonical entrypoint. (5) scripts/dispatch/build-context.sh all emit_event calls are >/dev/null 2>&1
  - decisions :: .orchestrator/DECISIONS.md:296:- **Revisable**: Yes — (a) If reinit free-form-prose loss bites a second user, promote 5a to a dedicated milestone or M014-extended task. (b) Stage bloat 5b can be addressed inside the installer without a D-row (filter in `install-claude-code.sh` Stage 4.5 `find` expression). (c) If `check-settings-state.sh` silent regen failure reproduces, open a spec and a bugfix commit. (d) Upstream-removal 5d can be fixed via manifest-diff in a future install pass. (e) If the four dogfood-bugfix commits should have been a single "fix(dogfood)" squashed commit rather than a 4-commit split, revisit commit-message-style for future hotfix batches — current split prioritizes archaeology readability.
  - decisions :: .orchestrator/DECISIONS.md:312:paid` closed enum (stderr warning + fall-through on bad values), metadata-probe fallback via `pip show conversus` `Home-page:` parsing, short-circuit `edition=unknown reason=stub` under stub mode. Output contract: `edition=` + `reason=` lines on stdout in stable order, warnings on stderr (DC-5). (2) `<TOOL>_EDITION=<value>` env-var name is reserved as the convention for any future OSS-default-with-paid-escape-hatch tool integration in this repo (graduated as MEM030, `knowledge/conventions/MEM030.md`). The paired edition-resolution two-tier-detection pattern is graduated as MEM029 (`knowledge/patterns/MEM029.md`). (3) Preset frontmatter `edition_required: paid` (M026/P03/T01, `scripts/dispatch/adapters/tool/conversus.sh` `_read_preset_edition_required`) refuses paid-only presets on OSS-resolved binaries before any `conversus run` invocation; diagnostic on stderr matches case-insensitive regex `paid-only.*CONVERSUS_EDITION=paid` (SC-7). Diagnostic uses `FAIL:` prefix per the adapter's `_emit_fail` convention rather than the FR-11 literal `ERROR:` opener — body content satisfies the SC-7 regex regardless. (4) CHANGELOG.md records the migration under v0.9.1 heading. (5) Cross-cuts: M013/P04 observability shape unchanged (additive `edition` field per AD-4 adjacency rule); spec-026 M014 shell-impl Pass 3 wiring is the next consumer that may exercise the new resolver under a fresh-install code path.
  - decisions :: .orchestrator/DECISIONS.md:449:- **Choice**: Homebrew tap is `build-fractal/orchestrator` as a single-formula tap (one `Formula/orchestrator.rb` per tap). Single-formula simplifies the M035/P03 tap-publishing plan (one tap repo + one formula path); multi-formula taps are deferred until a second build-fractal tool earns the cost.
  - decisions :: .orchestrator/DECISIONS.md:497:| D001 | M035/P02 | convention | CI runner platform for npm publishing pipeline (#Q-7) | ubuntu-latest | npm tarball assembly and publish are shell+node-only; no macOS-only surface in P02. Cross-channel byte-equivalence test will gain macos-latest matrix when P03 (homebrew) lands. ubuntu-latest is fastest, cheapest, and aligned with CON-2 which permits bash 4+ in CI scripts. | No |
  - decisions :: .orchestrator/DECISIONS.md:500:| D004 | M035/P05 | signing-strategy | Release-artifact signing for the npm publishing pipeline (FR-11 / SC-11 / #Q-3 binding) | sigstore (cosign keyless) primary + SHA-256 checksum fallback | Keyless cosign signing in `.github/workflows/release.yml` (`npm-publish` job) binds each release artifact's signature to the workflow's GitHub OIDC identity (canonical-repo `v*` tag-push) and records it in the public Rekor transparency log — eliminates the GPG private-key blast radius (no long-lived signing key to rotate or compromise). `SHA256SUMS` published alongside the signed artifacts gives operators without `cosign` installed a tooling-free verification path (`shasum -a 256 -c SHA256SUMS`). The signing job uses a job-level `permissions: id-token: write` override; the workflow-level `permissions: contents: read` stays unchanged so other jobs (pr-validate) inherit least-privilege. CON-6 secret-scope discipline preserved (no new long-lived secrets; `secrets.GITHUB_TOKEN` is the default token, scoped by GHA). | No |
  - decisions :: .orchestrator/DECISIONS.md:60:Anthropic Managed Agents is not yet broadly available, so M010 value is externally gated. Meanwhile M011 (spec ingest), M012 (wiki), M013 (GitHub native integration), and M014 (comment automation) unlock the team's end-to-end spec→review→work loop, which is blocking current usability. M010's only technical prerequisite (M008 P03 dispatch adapter) is already satisfied, so order can be revisited without rework.
  - decisions :: .orchestrator/DECISIONS.md:652:1. **Spec recommendation honored.** The spec's `#Q-5` recommendation
  - decisions :: .orchestrator/DECISIONS.md:71:- **Choice**: Revised sequence: M016 (active) → M009 → M017 → M011 → M012 → M013 → M014 → M010. M016 (Autonomous Hardening) inserted before M009 to establish zero-prompt auto mode before launch. M017 (Conversus Deliberation Gate) inserted after M009 and before M011.
  - decisions :: .orchestrator/DECISIONS.md:718:**Bound to**: FR-13, FR-16, SC-13, AD-5, #Q-5.
  - decisions :: .orchestrator/DECISIONS.md:74:M016 is prerequisite for credible launch narrative — autonomous mode must actually work autonomously. M017 adds multi-agent deliberation as an opt-in quality gate, informed by the autoreason paper's generation-evaluation gap findings. Positioned before M011-M014 because spec management milestones involve subjective quality judgments where Conversus provides the most value. M017 is opt-in with no hard dependency from M011, so it can be deferred without rework cost.
  - decisions :: .orchestrator/DECISIONS.md:821:   sources lets the second run skip `npm root -g` and `brew --prefix`
  - decisions :: .orchestrator/DECISIONS.md:830:| D015 | M045 re-entry substrate: process-fresh, not in-session ScheduleWakeup | P01 viability spike (VERDICT PARTIAL) showed in-session ScheduleWakeup re-entry does not reset context and gives no per-rotation relief (weight analog compounds 4->11->18; relief depends on non-rotation-aware harness compaction). Folding M045 into v2b's process-fresh approach: on rotation, hand off to a fresh claude -p re-entry that starts a genuinely fresh context and resumes from disk. P02-P04 mechanism work (deterministic branch, capability detection, safety envelope, observability) carries over ~unchanged; only the re-entry primitive swaps from ScheduleWakeup to claude -p. | architecture | M045/P01 | In-session re-entry is structurally weaker than today's fresh-session re-invoke on the exact axis rotation targets; process-fresh re-entry is the only substrate that restores a genuine per-rotation context reset (spec CON-5 route). Operator-confirmed 2026-07-01. | No |
  - decisions :: .orchestrator/DECISIONS.md:99:- **Choice**: (1) Remove M017 (Conversus Deliberation Gate) from the forward milestone sequence. Revised sequence: **M011 → M012 → M013 → M014 → M009 → M010**. (2) Add M011/P07 (Format-Agnostic Intake & Conversus Fidelity Gate) which lands both the LLM-driven normalizer (any markdown → spec-kit shape) and the reusable `scripts/dispatch/adapters/tool/conversus.sh` adapter. (3) The adapter becomes shared infrastructure: later milestones that want deliberation at specific checkpoints (M013 UAT PR gate, M014 ambiguous-comment classification, roadmap phase-decomposition at Full intensity) invoke it from within their own scope, rather than waiting on a dedicated milestone. (4) Intensity engine owns the policy (when gates fire by default); users own opt-in/out via `--review` / `--no-review`.
  - decisions :: (capped at 5 hits — more may exist)
  - knowledge-entries :: (capped at 5 hits — more may exist)
  - knowledge-entries :: knowledge/conventions/MEM014.md:21:**State -> Dispatch**: `derive-phase.sh` outputs single state word to stdout. `auto.md` reads this to determine loop action. Budget/stuck/lock checks gate dispatch.
  - knowledge-entries :: knowledge/conventions/MEM016.md:19:Telemetry entries carry `cost_source` field (estimated/reported/unknown) with closed enum validation. `aggregate-metrics.sh` groups by cost_source, distinguishes null cost (unknown) from zero cost (free). Legacy entries classified by presence of cost data.
  - knowledge-entries :: knowledge/conventions/MEM017.md:17:# MEM017: Gate Verdict Protocol
  - knowledge-entries :: knowledge/conventions/MEM030.md:33:3. **Precedence**: env-var declaration is **primary**. Metadata-probe fallback is secondary. Operator-supplied absolute overrides (`<TOOL>_HOME`) trump both — they're an explicit "use exactly this binary" instruction. Resolver order from highest to lowest precedence: STUB (test-only) → PATH (`command -v`) → `<TOOL>_HOME` → `<TOOL>_EDITION`-aware user-local probe.
  - knowledge-entries :: knowledge/conventions/MEM030.md:35:4. **Diagnostic surface**: the `check` subcommand of the integration's adapter MUST emit `edition=<value> reason=<resolution-tag>` on stdout so the resolved edition is visible to the operator and to telemetry without a separate probe call.
  - knowledge-entries :: knowledge/conventions/MEM030.md:37:5. **Telemetry shape**: every JSONL record emitted by the integration adapter MUST include an `edition` field alongside the existing identifying fields (e.g., `adapter_version`, `gate_id`). Place adjacent to the version field for readability and for adjacency-invariant tests (M026/P02/T02 pattern).
  - knowledge-entries :: knowledge/conventions/MEM030.md:39:6. **Refusal diagnostic**: when an upstream artifact (preset, config, manifest) declares `edition_required: <edition>` and the resolved edition does not match, the integration MUST refuse the invocation BEFORE any heavy work, with a stderr diagnostic naming both the requirement and the escape — `<TOOL>_EDITION=<required-edition>` (M026/P03 FR-11 pattern).
  - knowledge-entries :: knowledge/conventions/MEM030.md:45:## Gate shape
  - knowledge-entries :: knowledge/conventions/MEM030.md:48:- **JSONL `edition` field presence**: every `*_invocation` record from an edition-aware adapter MUST contain an `edition` field. Verify with `scripts/verify/m026-p02-jsonl-edition-field.sh` (existing) — extend the pattern when adding a second edition-aware adapter.
  - knowledge-entries :: knowledge/patterns/MEM027.md:28:2. **jq-optional merge with structural fallback.** Detect `jq` via `command -v jq`. Under jq, use `jq -S` for deterministic sorted-key output. Without jq, fall back to a `python3`-driven merge (or a bash-3.2-compatible awk script) that preserves every non-orchestrator key byte-identically at the structural level — semantic equivalence is the contract, not byte-identity across the jq/fallback boundary.
  - knowledge-entries :: knowledge/patterns/MEM027.md:30:4. **`--uninstall` strips only tagged entries, with cascade cleanup.** Remove only objects whose `_orchestrator_managed` is `true`; if that leaves a wrapper with an empty `hooks` array, remove the wrapper; if that leaves an event key with an empty array, remove the event key; if that leaves `hooks` empty, remove the `hooks` key. Every other key in the target is preserved byte-identically.
  - knowledge-entries :: knowledge/patterns/MEM027.md:38:- **Idempotency.** Run the installer twice in succession, assert the post-first-install and post-second-install sha256 match. No accretion of duplicate orchestrator entries.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:128:- FR-045 (destructive operation warnings) is delegated to Claude Code's built-in safety checks for v0.1.0. Orchestrator-level detection deferred to future milestone.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:130:- **[milestone:M005]** [2026-04-12] Scored health reporting — run-doctor.sh aggregates 12 checks (4 legacy + 8 DOCTOR:) into Checks passed: N/M with HEALTHY/NEEDS_ATTENTION status. Legacy checks use exit-code pass/fail; new checks parse DOCTOR: status lines. Advisory checks (check-plans.sh) counted separately. History appended to doctor-history.jsonl per run.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:133:- **[milestone:M005]** [2026-04-12] Cost_source closed enum — telemetry entries carry cost_source field (estimated/reported/unknown) with closed enum validation. aggregate-metrics.sh groups by cost_source, distinguishes null cost (unknown) from zero cost (free). Legacy entries classified by presence of cost data. Established in P02 per AD-2.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:145:- **[milestone:M008]** [2026-04-14] Hermetic-first testing for runtime/HOME-touching scripts: all installer, runtime-adapter, and init tests use HOME=$(mktemp -d) or --project-dir $(mktemp -d). HOME guards in adapter scripts refuse HOME="" or HOME=/ to prevent root-directory writes. A static gate (m008-p07-hermetic-only.sh) mechanically enforces this pattern across P07 verifiers. Never invoke --register or install-*.sh against real developer HOME during automated tests.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:152:- **[milestone:M003]** [2026-04-15] Skip-gracefully fixture pattern for integration tests: synthetic fixture always runs (primary), optional live fixture runs when present or SKIPs cleanly when absent (secondary). Strict/warn split absorbs live-fixture concurrent noise (mtime drift) without masking synthetic-path regressions.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:157:- **[milestone:M021]** [2026-04-17] Shape-classifier as single source of truth: a classifier library (scripts/verify/lib/shape-classifier.sh) consumed by both the enforcement layer (pre-Bash hook) and the regression gate (replay-prompt-corpus.sh) guarantees byte-identical decisions and catches classifier drift the moment corpus expectations diverge.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:158:- **[milestone:M021]** [2026-04-17] Permanent fixture corpus with locked entry count: the 20-entry M011/P05-P07 prompt corpus is the authoritative regression surface — every entry traces to a specific screenshot (ID+SCREENSHOT+INPUT+EXPECTED_OUTCOME). Future hook triggers earn a new milestone, not a fixture append (constitution XIV; AD-5).
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:167:- **[milestone:M019]** [2026-04-18] Module-scoped globals and subshell loss: pricing lib sets _PRICING_WARNING_REASON in whatever shell the estimator runs in. Capturing estimator stdout via $(...) loses the warning. Workaround: route estimator stdout to a mktemp file so the estimator runs in the caller's shell and the warning survives. Applied in scripts/dispatch/dispatch-interface.sh _di_emit_dispatch_usage helper.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:174:- **[milestone:M013]** [2026-04-22] Additive-helper-append preserves byte-identity for cross-phase shared libs: scripts/integrations/github-common.sh extended P02 (12 helpers) → P03 (gh_marker_search_remote + manifest_footer 4th-arg additive-suffix) → P04 (http_probe + sidecar_update_item_cache + emit_tier1_record + classify_gh_rc + emit_conversus_gate_record). Every extension lands at end-of-helpers before the self-check block; prior function bodies stay byte-identical so earlier-phase byte-identity gates continue to pass. Optional-arg additive-suffix on existing helpers (manifest_footer 3→4 args) preserves existing call-site behavior.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:180:- **[milestone:M013]** [2026-04-22] External-watchdog-plus-adapter-internal-timeout (Constitution XII belt-and-suspenders): for opt-in external-tool gates (e.g., github-conversus-gate.sh wrapping the M011/P07 conversus adapter), wrap the adapter invocation in an outer 'kill -TERM at TIMEOUT seconds; sleep 1; kill -KILL at TIMEOUT+1' subshell EVEN IF the adapter has its own internal timeout. Defends against adapter timeout bugs and makes the worst-case CI runtime auditable from the wrapper alone.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:182:- **[milestone:M013]** [2026-04-22] FR-12-v1 negative-grep guard for sibling-runtime byte-identity: when one runtime adopts a feature that other runtimes legitimately don't yet support (FR-12 Claude-Code-only v1 for the post-verify hook), guard sibling-runtime files (codex/cursor adapters + installers) with a negative-grep gate for the feature marker (e.g., 'post_verify' / 'FR-12') rather than pinned-sha digests. Cheaper to author and READ, and self-repairs if codex/cursor later gain legitimate content under a DIFFERENT marker. Trade-off: weaker than pinned-sha against accidental drift, but the constraint here is intent-shape not exact bytes.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:202:- **[milestone:M028]** [2026-04-29] Helper-function carve-out for AD-19: function bodies are NOT scanned by the AP-009 inline-command-shape classifier in production. assert_reject(), classify_one(), compute_sha(), count_managed_flags(), extract_corpus_input() and similar multi-step helpers can be hoisted into bash function bodies at top-of-script and called inline; the surrounding script remains AD-19-conformant even when helper bodies contain compound substitutions or multi-stage pipelines. Codified at top-of-file as a comment-block convention starting M028/P02/T05; load-bearing across every M028 verifier authoring extraction or shape-assertion helpers.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:203:- **[milestone:M028]** [2026-04-29] Plan-time classifier-shape pre-validation as authoring discipline: every shape-bearing line introduced by a task plan or inside a hook/script body MUST be empirically run through scripts/verify/lib/shape-classifier.sh::classify_command at plan-authoring time, with the verdict recorded in plan prose. Closes the planner-vs-classifier confabulation gap surfaced repeatedly across M028 (T01 reshaped resolution block; T05 swapped test command from sh-c-body to bare compound chain; P03 verifiers used pinned-INPUT alignment between corpus and verifier). Without the trace, the verdict claim is text not contract.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:205:- **[milestone:M028]** [2026-04-29] Snapshot-ladder shape for byte-equality gates: pre-state -> action-1 -> action-2 -> reverse-action; SHA-256 at each step; assert (action-1 == action-2) for idempotency AND (pre == reverse) for reversibility in a single isolated-HOME exercise. Cheaper than separate idempotency + reversibility verifiers; the four snapshots fit in one tmp dir and one set -u execution context. Codified by M028/P02/T05 install-roundtrip.sh; pinned post-install SHA d47ab7f32ef6bda277e4d3b6ab04ed70ddd92a3388250f13badc79d85389f32a captured in comment block as informational drift signal (hard gate stays byte-equality; pin is human audit trail).
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:58:| D007 | convention | `scope-filter.sh` uses scope tags in KNOWLEDGE.md, `When` column in DECISIONS.md, `--depends` flag for dependency resolution | FR-062/FR-063 scoping rules; caller resolves deps from roadmap to avoid duplicating roadmap parsing |
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:60:### Implementation Decisions (auto-aggregated)
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:63:- Truths without `Check:` sub-items logged as descriptive (Tier 3), not mechanically checked
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:89:`derive-phase.sh` outputs single state word to stdout. `auto.md` reads this to determine loop action. Budget/stuck/lock checks gate dispatch.
  - knowledge-md :: (capped at 5 hits — more may exist)
  - milestone-summaries :: .orchestrator/milestones/M002/M002-SUMMARY.md:14:  - "scripts/knowledge/lib/staleness.sh, scripts/knowledge/lib/index-utils.sh, scripts/knowledge/lib/detail-utils.sh, scripts/knowledge/create-entry.sh, scripts/knowledge/update-entry.sh, scripts/knowledge/supersede-entry.sh, scripts/knowledge/archive-entry.sh, scripts/knowledge/promote-entry.sh, scripts/knowledge/rebuild-index.sh, scripts/knowledge/traverse-graph.sh, scripts/knowledge/resolve-entries.sh, scripts/dispatch/build-context.sh, scripts/dispatch/compress-payload.sh, scripts/dispatch/classify-complexity.sh, scripts/dispatch/select-model.sh, scripts/telemetry/record-telemetry.sh, scripts/telemetry/aggregate-metrics.sh, scripts/diagnostics/run-doctor.sh"
  - milestone-summaries :: .orchestrator/milestones/M002/M002-SUMMARY.md:28:Delivered the complete three-temperature knowledge architecture replacing the flat KNOWLEDGE.md. 7 phases, 27 tasks across CRUD foundation (P01), lifecycle management (P02), graph relationships (P03), pre-inlined dispatch integration (P04), execution telemetry (P05), model routing (P06), and diagnostics (P07). Key deliverables: pipe-delimited index for O(n) scanning, individual detail files with 12-field YAML frontmatter, staleness decay with 180-day horizon, graph traversal with cycle-safe BFS, manifest-header dispatch payloads with static-first ordering, telemetry passthrough in auto-loop, complexity-based model routing, and full diagnostics pipeline. All verification scripts pass across all phases.
  - milestone-summaries :: .orchestrator/milestones/M002/M002-SUMMARY.md:8:  - "Three-temperature knowledge architecture (hot/warm/cold) with KNOWLEDGE-INDEX.md pipe-delimited index, individual detail files at knowledge/{category}/{entry-id}.md, 7 CRUD scripts (create, update, supersede, archive, promote, rebuild-index, scope-filter), 3 shared libraries (staleness.sh, index-utils.sh, detail-utils.sh), lifecycle management (staleness decay, overlap detection, hit count tracking, confidence adjustment), graph traversal (traverse-graph.sh with 1-hop BFS, cycle-safe, max 5 cap), detail resolution (resolve-entries.sh), pre-inlined dispatch with manifest header (FR-111/FR-112/FR-113), execution telemetry pipeline (record-telemetry.sh, aggregate-metrics.sh), model routing configuration (classify-complexity.sh, select-model.sh, routing.yaml), diagnostics command (run-doctor.sh with 4 check scripts)"
  - milestone-summaries :: .orchestrator/milestones/M003/M003-SUMMARY.md:28:M003 Migration Tool milestone complete. Initial build (P01-P06, commit ad3da8a 2026-04-09) delivered the end-to-end migration pipeline: adapter interface with pluggable source formats (GSD2/GSD v1/spec-kit), per-artifact transforms (knowledge, decisions, requirements, milestones with tiering), and CLI with idempotency + reporting. After M007 (knowledge graph) and M008 (standalone multi-runtime + 5-rule state resolver) landed, a delta audit surfaced three drift points: hardcoded .specify/orchestrator/ paths, unpopulated graph database, single-layout idempotency probe. P07 refit wired migrate.sh to resolve-root.sh --absolute, dropped hardcoded paths in three transforms, added dual-root idempotency probing, wired rebuild-index.sh as final pipeline stage, and documented AD-13/14/15 in commands/migrate.md. P08 validated end-to-end via deterministic synthetic GSD2 fixture (primary) and live lakeledger (secondary, skip-gracefully), new scripts/orchestrator/status.sh CLI wrapper making the roadmap demo sentence literally executable, 8 AD-19-safe verify scripts, and a two-pass integration test — all green. Surfaced and fixed one latent P02 bug (missing content_hash frontmatter) during refit. Retroactive phase summaries added for P01-P06 (originally delivered before phase-summary machinery) based on roadmap Revision Note + commit ad3da8a. Migration pipeline now produces resolver-wired output, populates the M007 graph database, and participates in graph traversal — ready for M009 recipe packaging and M010 cloud dispatch.
  - milestone-summaries :: .orchestrator/milestones/M004/M004-SUMMARY.md:14:  - "scripts/lib/errors.sh, scripts/lib/events.sh, scripts/lib/run-context.sh, scripts/lib/guards.sh, scripts/lib/hooks.sh, scripts/engine/run.sh, scripts/engine/checkpoint.sh, scripts/lib/recipe-parser.sh, templates/context-recipe.yaml, templates/hooks.yaml, scripts/dispatch/build-context.sh, scripts/dispatch/compress-payload.sh, scripts/dispatch/select-model.sh, scripts/diagnostics/check-recipe.sh, .specify/memory/constitution.md"
  - milestone-summaries :: .orchestrator/milestones/M004/M004-SUMMARY.md:25:  - "All engine-path scripts emit EVENT: lines to stderr when ORCH_RUN_ID is set; RESULT: lines on every exit; record-result.sh adds run_id to execution-log.jsonl; aggregate-metrics.sh reports by_error_kind; check-recipe.sh emits DOCTOR:RECIPE"
  - milestone-summaries :: .orchestrator/milestones/M004/M004-SUMMARY.md:8:  - "Engine architecture foundation: 5 shared libraries (errors.sh, events.sh, run-context.sh, guards.sh, hooks.sh), engine core (run.sh + checkpoint.sh), YAML recipe system (context-recipe.yaml, hooks.yaml, routing.yaml, recipe-parser.sh), recipe-driven refactored dispatch scripts (build-context.sh, compress-payload.sh, select-model.sh + section-handlers.sh), engine integration for 6 additional scripts (check-must-haves.sh, record-result.sh, record-telemetry.sh, aggregate-metrics.sh, classify-complexity.sh, phase-transition.sh), recipe conformance diagnostic (check-recipe.sh), constitution v2.0 (13 principles + ANTIPATTERNS.md)"
  - milestone-summaries :: .orchestrator/milestones/M005/M005-SUMMARY.md:8:  - "P01: content-hash idempotency (hash.sh, create/update/rebuild hash support); P02: cost transparency (record-telemetry.sh cost_source, aggregate-metrics.sh grouping); P03: payload transform extraction (payload-transforms.sh, manifest-builder.sh pure functions); P04: agent instruction schema (instruction-schema.md, check-instructions.sh); P05: gate verdict protocol (verdicts.sh, hooks.sh verdict parsing, provider-convention.md, check-providers.sh); P06: diagnostic suite (5 new checks, scored run-doctor.sh, extension.yml registration); P07: autonomy permission pipeline (generate/write/check-permissions.sh, AD-19 shape guidance)"
  - milestone-summaries :: .orchestrator/milestones/M007/M007-SUMMARY.md:18:  - "sqlite3 CLI calling convention via db_query wrapper; recursive CTE for graph traversal; two-direction CTE for provenance chains; dynamic SQL WHERE clause construction; iterative component detection; DOCTOR: protocol line for diagnostics"
  - milestone-summaries :: .orchestrator/milestones/M008/M008-SUMMARY.md:28:M008 transformed the orchestrator from a spec-kit extension into a standalone multi-runtime extension with adaptive process intensity. Across 7 phases (P01-P07), delivered: (1) Adaptive Intensity Engine — analyzes task descriptions along scope/risk/complexity axes and recommends Quick/Standard/Full intensity with environment capability awareness. (2) Backend-Agnostic Dispatch Interface — uniform entry point with filename-based adapter routing, two local backends (Claude Code Agent tool + Codex CLI SDK), structured result/error schemas with distinct exit codes. The explicit seam for M010's cloud backends. (3) Intensity-Aware Pipeline Scaling — central 7×3 stage×intensity matrix wires intensity awareness into every pipeline stage (discuss, research, plan-phase, dispatch, verify, knowledge, auto) with atomic mid-workflow override. (4) State & Namespace Independence — 5-rule root resolver, unified config, hard migration tool, surgical derive-phase refactor, namespace aliases. Full workflow runs standalone without spec-kit (SC-004 validated). (5) Runtime & Format Adapters — auto-detection + 3 runtime adapters (claude-code/codex/cursor) with HOME/project-dir guards + 2 format adapters (native round-trip, spec-kit one-directional read). (6) Multi-Runtime Packaging — open-standard SKILL.md format, 12 skills generated from commands, installable bundle, 3 runtime installers delegating to adapters, offline-safe version checker. (7) Init, Onboarding & Spec-Kit Bridge — orchestrator:init detects project (10 languages + 9 frameworks + 6 CI systems + 9 tools), generates config + runtime-appropriate instruction file (CLAUDE.md/AGENTS.md/.cursor/rules), installs skills. Reinit preserves user-edited custom blocks via comment markers. Full onboarding completes in ~1 second wall-clock (SC-005 validated with 2 orders of magnitude margin vs 5min target). Cross-cutting patterns: hermetic-first testing (mktemp fixtures, never touch real HOME), comment-aware Bash 3.2 compat, filename-based auto-discovery mirrored from P02 through P05/P06, thin delegation pattern, user-edit preservation via comment-delimited blocks. M008 is the foundation for M009 (debug protocols) and M010 (cloud dispatch via Managed Agents).
  - milestone-summaries :: .orchestrator/milestones/M011/M011-SUMMARY.md:16:  - "D004 Conversus lands inside M011/P07 as reusable adapter not standalone M017; SPEC-prefixed natural IDs instead of MEM-style sequential; nested category directories knowledge/spec/<subcat>/; classify+dispatch decision layer wrapper around create-entry.sh; versioned-ID chain-walking supersession; chunks-first with raw-spec fallback in command docs; bidirectional-traverse + directional-frontmatter-filter for typed edges; dispatch-interface.sh as uniform LLM boundary (no bash HTTP calls); three-value exit contract 0/1/2 for gate-shaped commands; _STUB=1 env-var escape hatches as first-class deterministic-testing contracts"
  - milestone-summaries :: .orchestrator/milestones/M011/M011-SUMMARY.md:28:M011 (Spec Management) delivers the "paste a spec, get a roadmap" value proposition end-to-end. Seven phases took the orchestrator from an opaque-spec-file consumer to a full ingest→chunk→scope-filter→normalize→fidelity-gate pipeline that treats specs as structured knowledge on equal footing with the existing three-temperature knowledge model (M002/M007). P01–P03 built the deterministic chunker (spec directory tree with six subcategories, SPEC-prefixed natural IDs, nested-directory indexing, create-entry.sh SPEC- namespace support, full markdown→chunk classifier with classify+dispatch decision layer, versioned-ID supersession chains, REMOVED detection, phase-impact REVIEW emission). P04 wired scope-filter.sh to resolve `spec/*` scope tags (graph-mode + spec-scope-tags mode), letting build-context.sh deliver scope-filtered spec chunks in dispatch payloads per Constitution Principle I. P05 wired chunks-first evaluation and intensity-aware roadmap generation into commands/evaluate.md and commands/roadmap.md (spec-metrics.sh counts non-superseded chunks; spec-story-graph.sh emits directional story-to-story depends_on edges via bidirectional-traverse + directional-frontmatter-filter composition; intensity-gate.sh roadmap stage with Quick/Standard/Full modes). P06 delivered the user-facing commands/ingest.md, four doc-shape verify scripts, a full sandbox E2E gate script, a timing harness (<60s on representative specs), and dogfood evidence against specs/016-autonomous-hardening/spec.md (20 chunks, 7s elapsed). P07 closed with format-agnostic intake and a reusable Conversus fidelity gate: detect-spec-shape.sh probe classifier, normalize-spec.sh thin wrapper dispatching through dispatch-interface.sh (no hardcoded LLM calls), templates/spec-normalizer-prompt.md, a reusable Conversus adapter (scripts/dispatch/adapters/tool/conversus.sh with check/gate/parse-verdict subcommands, graceful degradation, CONVERSUS_STUB escape, three-value exit contract 0/1/2), commands/conversus-gate.md designed for M013/M014 reuse with different presets, templates/conversus-presets/normalize-fidelity.yml, templates/gate-result.md, and integration into commands/ingest.md as a pre-chunker 6-step pipeline (detect-shape → normalize → resolve-policy → fidelity-gate → chunker → rebuild) gated by a new `ingest` intensity stage (Quick=normalize; Standard/Full=normalize+fidelity-gate). A 222-line user guide (docs/ingesting-arbitrary-specs.md) covers flag semantics, BLOCK resolution, stub modes, and preset extension.
  - milestone-summaries :: .orchestrator/milestones/M011/M011-SUMMARY.md:32:Cross-cutting patterns established: decision-layer wrapper around creation primitives (P03); chain-walking next-version supersession (P03); intensity-gate stage registration via minimal case-row insertion (P05, P07); chunks-first with raw-spec fallback in command docs (P05); bidirectional-traverse + directional-frontmatter-filter for typed edges over generic graphs (P05); `grep -Fq -- "$tok"` end-of-options idiom for --flag-shaped tokens under BSD grep (P06); sandboxed PROJECT_ROOT=$(mktemp -d) + EXIT-trap for pipeline tests (P06, P07); evidence-present token gate (file-exists + token-contains) as standard dogfood verification shape (P06, P07); per-phase preserve-references regression contract (P06, P07); `_STUB=1` env-var escape hatches as first-class deterministic-testing contracts (P07 NORMALIZER_STUB + CONVERSUS_STUB); three-value exit contract (0/1/2) for gate-shaped commands where BLOCK is distinct from error (P07); dispatch-interface.sh as uniform LLM-agent-dispatch boundary (no bash HTTP calls, P07).
  - milestone-summaries :: .orchestrator/milestones/M012/M012-SUMMARY.md:10:  - "populated .orchestrator/ tree (constitution, DECISIONS.md, KNOWLEDGE.md, milestone-summary.md, milestones/, archive/); populated knowledge/patterns+conventions+lessons/MEM corpus; mkdocs + mkdocs-material + mkdocs-include-markdown-plugin + pymdown-extensions Python toolchain (version-pinned in wiki/requirements.txt); GitHub Pages + GitHub Discussions enabled on the repo for the operator-gated first deploy; gh CLI for wiki-giscus-remap.sh live path; M011 spec-ingest pipeline produced the 022-spec-wiki feature spec; M021 anti-pattern-lint + pre-bash-shape-guard + widened allow-list gates"
  - milestone-summaries :: .orchestrator/milestones/M012/M012-SUMMARY.md:12:  - "M013 (GitHub Issues/Projects/Milestones sync inherits chained-gate wrapper + loud-fail convention + diagnostic idempotent-helper pattern); M014 (comment triage consumes Giscus threads keyed by pathname + wiki-giscus-remap.sh remap contract); M020 (promoted by D011 evaluation — extends knowledge subtree with review-state + query surface); M019 Tier 2/3 (M012 is first non-self dogfood substrate for unit_close records on future redeploys); .orchestrator/DECISIONS.md roadmap ordering (M020 insertion between M014 and M019 Tier 2/3); live gh-pages branch on this repo (operator-gated); consolidation runbook (wiki-giscus-remap.sh --dry-run invocation before any discussion-bearing rename)"
  - milestone-summaries :: .orchestrator/milestones/M012/M012-SUMMARY.md:122:- **M019 Tier 2/3** inherits M012 as its first non-self dogfood substrate; unit_close records from future M012 redeploys (once the operator completes the live deploy) exercise the Tier 1 emitter against a real projection surface.
  - milestone-summaries :: .orchestrator/milestones/M012/M012-SUMMARY.md:30:M012 ships the dogfood spec→wiki loop: a deployable MkDocs Material site that projects every in-scope `.orchestrator/**.md` artifact plus the `knowledge/**/MEM*.md` corpus through a single-source-of-truth include pipeline, overlays per-page Giscus threads, and deploys via a chained wrapper that runs four pre-deploy gates before `mkdocs gh-deploy`. The milestone's vision was "a dogfood-only MkDocs + Giscus site that renders `.orchestrator/` artifacts as a navigable, commentable wiki with one-command deploy" — and that is exactly what landed across four phases (P01 → P02 → P03 → P04), 37 verification gates in total, and a D011 mechanical evaluation that promoted **M020** to a committed milestone.
  - milestone-summaries :: .orchestrator/milestones/M012/M012-SUMMARY.md:39:- **`scripts/wiki/` three-stage pipeline** — `wiki-scan-sources.sh` emits `<category>|<rel-path>|<title>` records for every in-scope `.orchestrator/**.md` in stable lexical order with exclusion policy enforced (`scratch/`, `tmp/`, `config/`, `PLANNING-PAYLOAD`, `VERIFICATION`, non-`.md`); `wiki-generate-stubs.sh` writes ≤ 25-line include stubs per artifact plus per-section indexes; `wiki-generate-nav.sh` atomically splices a deterministic `nav:` block between `# >>> M012-P01 nav` markers with seven fixed top-level sections (Home / Constitution / Decisions / Knowledge / Milestone Summary / Milestones / Archive — Archive conditional on scanner records). `wiki-serve.sh` launcher adds `--probe` for headless config validation.
  - milestone-summaries :: .orchestrator/milestones/M012/M012-SUMMARY.md:44:Extends the P01 pipeline to resolve internal cross-links, surface the `knowledge/**/MEM*.md` corpus through the existing SSOT pipeline, and ships a standalone built-site link checker. Ends with the D011 mechanical evaluation.
  - milestone-summaries :: .orchestrator/milestones/M013/M013-SUMMARY.md:16:  - "D014 — Knowledge-Layer Boundary: M020 retains chunk-ID authority; M013 cross-links the M020 schema and never redefines/extends it (zero writes to knowledge/spec/** from any P01–P04 task); FR-9 additive-only emit pinned to existing SPEC-* frontmatter; D015 — P02/P03 scope split applied as load-bearing markers (_deferred to P03_ cells fillable in place); FR-12 Claude-Code-only v1 binding via Constitution X+XII (Codex/Cursor parity demand-driven post-launch via M010); projection-not-peer grounding via XIV+M007+D007 (sidecar operator-owned + gitignored; in-repo template); FR-11 reversibility (all writes have absent/pending exit paths; lock released before exit 3/4); FR-16 rate-limit pre-flight + auth-expiry exit-code contract; FR-17 cost emission in M019 Tier 1 shape with source=runtime hard-coded; FR-18 status --verify-cache as read-only divergence probe; AD-19 single-script-file Check shape preserved across all verify gates with comment-discipline synonyms keeping bash32 scanners self-clean; conversus adapter --strict mode added as reusable invariant for callers requiring adapter presence (M011/P07 graceful-degradation flipped off via --strict / CONVERSUS_STRICT=1)"
  - milestone-summaries :: .orchestrator/milestones/M013/M013-SUMMARY.md:18:  - "additive-helper-append-preserves-byte-identity (P02 12 + P03 1 + P04 5 helpers appended at end-of-helpers; prior bodies untouched); gate-evolution-on-legitimate-advancement (P02→P03 mapping table; P03→P04 reference extensions — earlier-phase assertions evolve to accept both pre/post shapes; byte-identity hashes on unchanged content stay pinned); pinned-SHA-with-awk-range-capture-before-edit (multi-phase additive doc editing with three pinned ranges in references/github-integration.md); FR-12-v1-negative-grep-guard (sibling-runtime byte-identity via negative greps for post_verify/FR-12 markers — cheaper than pinned-sha and self-repairs if codex/cursor gain legitimate content under different markers); AWK-based JSON upsert + count + first-scalar extraction (no jq hard-dep across all 4 phases); M013_GH_STUB_DIR fixture-driven preflight + marker-search stub-selector (env-var-driven stub paths exercise every branch deterministically without gh calls); PATH-shimmed fake-gh for zero-subprocess-call invariant enforcement (auto-mode SC-7 enforcement logs every gh call + asserts log empty); per-call-stderr-mktemp-classify-rm (capture stderr to mktemp + classify_gh_rc rc + file + rm -f on all paths; releases lock before exit 3/4); direct-cmd-subst-rc-propagation (assign $() then read $? on next line; '|| true' in cmd-subst tail silently zeros $? — caught mid-T03); external-watchdog-plus-adapter-internal-timeout (Constitution XII belt-and-suspenders kill -TERM at TIMEOUT then kill -KILL at TIMEOUT+1s); verdict-rc-parity (rc=0 PASS / rc=2 BLOCK / rc=1 ERROR passed verbatim so BLOCK is distinguishable from error); pending-sentinel bootstrap with clobber-refusal exit 2 (extends M012/P04 DEPLOY-RECORD convention); fixture-copy-before-live-gate (cp -R fx/. tmp/ before invoking github-sync.sh because it reassigns ORCHESTRATOR_ROOT and would pollute repo fixture tree); stdout-record + stderr-summary split (fixture diffs stay byte-identical while operator still sees per-run summary); buffered-manifest-body via mktemp tempfile during walker iteration (counts finalized before header emission); FORMAT STABILITY CONTRACT doc-block at helper definition site (pins shape for downstream P03/P04 consumers); section-content byte-identity gate via awk-extract by literal-prefix heading match + shasum compare against embedded hashes (robust against insertion-shifts); load-bearing _deferred to P0N_ cells functioning as scope-boundary markers a later phase replaces in place (D015 split); comment-discipline synonyms in bash32 scanners (assoc-arrays / array-from-stdin builtins / case-conversion expansion / combined-redirect shorthand)"
  - milestone-summaries :: .orchestrator/milestones/M013/M013-SUMMARY.md:37:- **P02 — US-1 Projection create path** (7 tasks, 282m). 12-public-function `github-common.sh` (orchestrator-id derivation, FR-4 marker primitives with `shasum` byte-identity, AWK depth-aware sidecar item upserts) + 538-line `github-init.sh` create-path workhorse (flag parser, SC-7 auto-mode pending-sentinel, 3 populated preflights, lazy state walker honoring AS-4a Planning-state skip, FR-5 GraphQL whitelist, marker search-before-create idempotency) + FR-15 dry-run manifest format pinned with FORMAT STABILITY CONTRACT doc-block + sidecar `sub_issue_mode` field + `commands/github-init.md` + reference doc Auth Modes / Sub-Issue Representation Modes / Partial Mapping Table (3 cells `_deferred to P03_`) / init Workflow / Dry-Run Manifest Format subsections + 8-gate phase suite. Demo: dry-run produces 14-line byte-identical manifest vs `expected-manifest.txt`; second invocation `upserts=0 skipped=12 errors=0`.
  - milestone-summaries :: .orchestrator/milestones/M013/M013-SUMMARY.md:39:- **P04 — Sync + post-verify hook + conversus UAT PR gate** (6 tasks, 305m). 626-line `github-sync.sh` (state walker + reconcile loop + lock-acquired live mode + dry-run manifest + `updateProjectV2ItemFieldValue` + sidecar cache update at upsert sites) + FR-16 pre-flight rate-limit probe + per-call `classify_gh_rc` wrapper releasing the lock before exit 3/4 (FR-11 reversibility) + FR-17 `unit_close` JSONL emitter (M019 Tier 1 shape, `source=runtime` hard-coded) + post-verify hook descriptor (`packaging/bundle/hooks/post-verify.json` + `scripts/lifecycle/after-verify-sync.sh` wrapper + `claude-code.sh` runtime adapter `hook_count` 5→6) + opt-in `github-conversus-gate.sh` wrapping the M011/P07 conversus adapter `--strict` with 30s external watchdog (Constitution XII belt-and-suspenders) + FR-13 preset `templates/conversus-presets/m013-uat-defect-merge.yml` (red-blue defect-advocate vs fix-advocate, BLOCK-on-tie) + FR-18 `--verify-cache` read-only divergence probe + 12-gate phase suite + FR-5 lint regression guard.
  - milestone-summaries :: .orchestrator/milestones/M013/M013-SUMMARY.md:45:- **FR-12 Claude-Code-only v1** — Binding via X+XII (Single-Hands-On Runtime + Constitution-Bounded Exception). P04's post-verify hook ships only Claude Code wiring; Codex/Cursor adapters and installers are byte-identity-preserved via negative-grep guard (cheaper than pinned-sha and self-repairs if those runtimes legitimately gain P04 content under a different marker). v2 (multi-runtime parity) is demand-driven post-launch.
  - milestone-summaries :: .orchestrator/milestones/M013/M013-SUMMARY.md:47:- **FR-11 reversibility** — All writes have absent/pending exit paths: `status --verify-cache` short-circuits to pending-operator-complete on absent OR pending sidecar; `perform_upsert` releases the lock before rate-limit/auth-expiry exits; post-verify hook fail-as-warning never blocks the dispatch loop.
  - milestone-summaries :: .orchestrator/milestones/M013/M013-SUMMARY.md:8:  - "opt-in GitHub native integration projecting orchestrator state to Issues + Project v2 + Milestones with FR-4 byte-identity markers; operator-owned sidecar at .orchestrator/integrations/github.json (gitignored) with templates/github-integration-sidecar.json schema (sync_mode + sub_issue_mode + per-item cache); tri-state orchestrator:github status command (absent / pending-operator-complete / configured) + status --verify-cache FR-18 read-only divergence probe; orchestrator:github init --re-init FR-14 adoption branch sourcing existing repo state via marker search; orchestrator:github sync 626-line walker + reconcile loop with --dry-run manifest + lock acquisition + updateProjectV2ItemFieldValue Project v2 status closure + FR-16 pre-flight rate-limit probe + per-call classify_gh_rc wrapper releasing lock before exit 3/4 (FR-11 reversibility); UAT bug ingestion loop (.github/ISSUE_TEMPLATE/uat-bug.yml + scripts/integrations/uat-ingest.sh + knowledge/spec/defect/SPEC-DEFECT-NNN schema with chunk-lookup-failed sentinel); FR-9 additive ## Spec Chunks emit in scripts/knowledge/rebuild-index.sh + scripts/knowledge/lib/index-utils.sh emit_spec_chunks_section helper; opt-in conversus UAT PR gate scripts/integrations/github-conversus-gate.sh wrapping M011/P07 adapter --strict with 30s external watchdog (Constitution XII) + FR-13 preset templates/conversus-presets/m013-uat-defect-merge.yml (red-blue defect-advocate vs fix-advocate, BLOCK-on-tie); post-verify lifecycle hook packaging/bundle/hooks/post-verify.json + scripts/lifecycle/after-verify-sync.sh wrapper + claude-code.sh runtime adapter hook_count 5->6 + FR-12 v1 Claude-Code-only enforcement via negative-grep guard on codex/cursor adapters+installers; scripts/integrations/github-common.sh shared lib (12 P02 helpers + 1 P03 helper + 5 P04 helpers — orchestrator-id derivation + FR-4 marker primitives with shasum byte-identity + AWK depth-aware sidecar item upserts + http_probe + sidecar_update_item_cache + emit_tier1_record + classify_gh_rc + emit_conversus_gate_record + gh_marker_search_remote + auth/sub-issue/label-collision preflights); scripts/verify/graphql-call-shape.sh FR-5 three-shape whitelist CI lint; references/github-integration.md full P01..P04 reference doc with Auth Modes / Sub-Issue Representation Modes / Full Mapping Table / Re-init Adoption Contract / Sync Workflow / Sync Action Mapping / Scope Boundary table; commands/github-init.md + commands/github-sync.md + commands/github-status.md (MEM012 structure); FR-17 unit_close + conversus_gate_invocation JSONL emitters at ${ORCHESTRATOR_ROOT}/observability/tier1.jsonl in M019 Tier 1 shape; scripts/integrations/sidecar-init-pending.sh pending-sentinel bootstrap with clobber-refusal exit 2; ~30 verification gates across 4 phase-suite orchestrators (P01 9 + P02 8 + P03 8 + P04 12 + FR-5 lint regression guard)"
  - milestone-summaries :: .orchestrator/milestones/M014/M014-SUMMARY.md:14:  - "commands/specify.md,commands/comments.md,commands/doctor.md,scripts/specify/specify.sh,scripts/comments/fetch.sh,scripts/comments/classify.sh,scripts/comments/apply.sh,scripts/comments/reject.sh,scripts/comments/triage.sh,scripts/comments/comments.sh,scripts/util/dual-write-runtime-md.sh,scripts/knowledge/spec-complexity-probe.sh,scripts/verify/spec-shape-lint.sh,scripts/diagnostics/check-docs.sh,scripts/diagnostics/run-doctor.sh,scripts/lifecycle/init-project.sh,scripts/lifecycle/reinit-handler.sh,scripts/knowledge/consolidate-artifacts.sh,scripts/migrate/m014-p02-migrate-recent-changes.sh,templates/spec-template.md,templates/spec-scaffolder-prompt.md,templates/conversus-presets/spec-pressure-test.yml,templates/conversus-presets/classify-comment.yml,templates/spec-complexity-contradiction-prompt.md,templates/spec-splitter-prompt.md,references/spec-management.md,.orchestrator/config.yml,RUNTIME-ASSUMPTIONS.md,tests/test-specify-shape.sh,tests/test-dual-write-outside-invariant.sh,tests/fixtures/m014-p01/expected-section-headings.txt,tests/fixtures/m014-p01/specify-fixture-prose.txt,tests/fixtures/m014-p03/sample-inbox.jsonl,tests/fixtures/m014-p03/queued-amendment.md,tests/fixtures/m014-p04/corpus-labels.tsv,tests/fixtures/m014-p04/contradictory-prose.txt,tests/fixtures/m014-p04/decomposable-prose.txt,tests/fixtures/m014-p04/amend-seed-spec.md,specs/024-spec-management-extended/planning-inputs/inbox-dogfood.md,CLAUDE.md,AGENTS.md"
  - milestone-summaries :: .orchestrator/milestones/M014/M014-SUMMARY.md:18:  - "Template SSOT with ground-truth heading fixture; marker-bounded atomic splice with byte-preserved outside region (SC-6a); subcommand surface with deferred body (diagnostic-stub exits distinct codes; full semantics land in later phase without command-file re-authoring); hermetic scratch project pattern for end-to-end command testing; verifier self-exemption for rule-embedding gates; awk-field extraction for structured corpus parse; write-site enumeration discipline with scanner gate; allow-list pattern for orthogonal writes (runtime-native full-file renders); hermetic-root via dirname of ORCH_ROOT (decouples dual-write target from repo root); advisory-first drift severity model; one-time migration script pattern with --dry-run/--apply/--force + idempotent already-migrated short-circuit; CC-first with defensive fallback (runtime gate + empty-response bailout — never fails caller); STATE_ROOT vs PROJECT_ROOT separation for mutation sinks vs dependency lookups; CONVERSUS_STUB hermetic testing via fixture-copy path in adapter; dispatch-interface mock via backup-and-swap; section-split awk + per-section classifier; append-with-body-refresh for registry entries (heading byte-identical body refreshed); bash32 scanner comment-strip to avoid false-positive on comment prose; hermetic-test stub via env-var pointing at JSONL fixture (GH_API_STUB, GH_GRAPHQL_STUB); per-surface source_surface filter on shared stub fixture; FR-19 dry-run JSONL action records as no-disk-write counterpart; grep -F literal-string match for URL dedup; stdout-verdict + stderr-INFO split; single-agent cooperative preset for refinement-only LLM calls; patch -N forward-only stale-diff probe; env-overridable sub-script-path hermetic-test pattern (COMMENTS_*); non-executable adapter as routing verdict (graceful-degradation test pattern); deterministic content-addressed filenames for idempotent queue/triage; two-tier must-not-happen invariant verification (source grep + runtime event assertion); bash32+lint omnibus with dynamic enumeration + basename self-exemption; zero-prompts hermetic scratch + fixture replay under --yes; outside-marker shasum invariant pre/post dual-write; phase-suite orchestrator with IFS-newline iteration + per-gate rc capture + FAILED_GATES accumulator"
  - milestone-summaries :: .orchestrator/milestones/M015/M015-SUMMARY.md:10:  - "from:M008 what:5-rule state-root resolver, standalone runtime adapters, multi-runtime packaging, orchestrator:init onboarding — the substrate the cutover operates on; from:M003 what:migration tool + spec-kit adapter + orchestrator-auto-in-Claude-Code-native validation (P07/P08) — source of the migration adapter kept after cutover and source of the auto-behavior evidence the cutover relies on per spec assumption; from:M007 what:no-graceful-degradation discipline — governs the hard-cutover approach; no dual code paths, no feature flags, no compat shims"
  - milestone-summaries :: .orchestrator/milestones/M015/M015-SUMMARY.md:18:  - "Pre-change gate scaffolding (validation-as-task MEM011): write verify scripts before the changes they assert, so every downstream task has an immediate pass/fail signal — applied in every M015 phase (P01 removal gates, P02 migration gates, P03 reframe gates, P04 validation gates); Snapshot-based immutability check: capture at 'first ## [' pre-edit and verify starting at 'second ## [' post-edit — handles prepend-only append patterns (new CHANGELOG entry) without re-flagging the historical tail as modified; Seal-by-sentinel allow-list retirement: replace regex body with __NAME_NEVER_MATCH__ when a transitional tolerance has served its purpose; keeps negated-grep syntax valid while tolerating nothing; Hard-delete cutover discipline: git rm for tracked files; no legacy/ rename; no compat shim; verify scripts use file-absence assertions to gate the cutover; Framing-verifier-friendly migration callouts: describe the relocation without literally naming the legacy path so the no-legacy-install grep stays clean while the narrative stays informative; Config file is override, not canonical-location declaration: Rule 3 directory existence is authoritative; state_root: line in config.yml conflicts with Rule 3 — preferred pattern is an empty or commented-example config; Four-stream evidence capture for validation phases: test-suite + doctor + migration-adapter-against-fixture + clean-clone-shape, each with a named marker line (ALL_SUITES_PASS / DOCTOR_CLEAN / MIGRATION_SUCCESS / CLEAN_CLONE_OK) the gate verifier keys on; Transcript-inline adjustment documentation: when a plan's speculative invocation shape diverges from actual CLI shape, document the divergence IN the evidence transcript so post-hoc reviewers can audit the call; Allow-list with documented reason for regression-guard scripts: verify scripts whose purpose is to PROVE absence of legacy-path regression must reference those paths in their assertions — explicitly allow-list with a comment naming the regression they guard"
  - milestone-summaries :: .orchestrator/milestones/M015/M015-SUMMARY.md:50:Orchestrator state moved from `.specify/orchestrator/` to `.orchestrator/` via `scripts/migrate/migrate-state.sh` (atomic `mv`); the constitution moved to `.orchestrator/memory/constitution.md`. `scripts/state/resolve-root.sh` dropped from 5 rules to 4 — the `.specify/orchestrator/` bridge rule was deleted outright, not feature-flagged; the secondary `.specify/orchestrator/config.yml` probe was also removed. Eight P02 verify scripts gate the migration (state-tree-migrated, constitution-moved, resolver-no-bridge, resolver-resolves-new, no-stale-state-refs, doctor-clean, t01-parse-check, pre-migrate-check). T05's sweep touched ~43 runtime files (lifecycle, knowledge, dispatch, diagnostics, engine, verify, commands, templates, `.gitignore`, `packaging/bundle/README.md`, `ANTIPATTERNS.md`) with literal path replacements. Two M008 P04 bridge-behavior tests were hard-deleted; two regression-guard tests (`m008-p04-standalone-e2e.sh`, `m008-p04-derive-phase-no-hardcode.sh`) were kept and explicitly allow-listed. Decision D003 recorded the removal of the redundant `state_root:` line from `.orchestrator/config.yml`. Migration adapters preserved verbatim per FR-013.
  - milestone-summaries :: .orchestrator/milestones/M020/M020-SUMMARY.md:28:M020 graduates the orchestrator's flat MEM knowledge registry into a queryable, reviewable, clusterable substrate. Across six phases the milestone landed (1) a closed-enum `status:` field schema authority gate with companion `decision_history:` + `archived_into:` fields, (2) a deterministic dispatch-callable read-only query surface, (3) a candidate->graduate cluster workflow with multi-entry atomicity and decision-history append, (4) a Review-Queue surface in `orchestrator:status` with staleness flags, (5) Jaccard clustering inside `orchestrator:consolidate` with conflict-as-advisory diagnostics, and (6) an operator preferences cascade (project > user > built-in default per key) that backs both the query state-filter and the consolidate similarity threshold. Together these surfaces close the spec-025 user stories US-1..US-5 (with the read-only dispatch invariant FR-8/CON-1 holding throughout) and stand up a stable schema contract that downstream milestones (M024 routing, M019 Tier 2+3 observability, M018 context-compression) can consume without further coordination.
  - milestone-summaries :: .orchestrator/milestones/M027/M027-SUMMARY.md:16:  - "D026 cost_estimates JSON shape; D027 verbatim accuracy disclaimer in commands/cost.md; #Q-16 always-on with --no-predict operator-override (no hidden state); #Q-1 anomaly threshold defaults pinned by M012-M026 sampling (cost_multiplier=3.0, retry_threshold=2, pass_rate_threshold=0.5, sample_floor=5); #Q-5 doctor anomaly perf measured at 56ms wall-clock on largest log; #Q-10 anomaly baseline disclaimer text pinned verbatim; FR-12/CON-1 read-only invariant; FR-21/CON-6 zero-LLM-token; FR-22 <100ms predictive latency (inner library); FR-20/CON-4/SC-18 Goodhart pairing extended to predictive + alerting surfaces; CON-3/SC-3/SC-17 byte-identity back-compat for --quiet/--yes/auto suppression; CON-7 bash 3.2 compat; CON-5 never-abort graceful degradation; CON-8 sample-floor; CON-10 operator override preservation; AD-19 single-script-file Check shape; MEM004 emitter-internal carve-out; MEM012 no-reshape"
  - milestone-summaries :: .orchestrator/milestones/M027/M027-SUMMARY.md:28:M027 delivered the user-visible cost+quality surfaces for the orchestrator. M019 Tier 1 ships per-dispatch JSONL records; M027 makes that data visible retrospectively (rollup CLI, cost command, status footer, doctor anomaly check) and actionable predictively (per-tier cost estimates at intensity-recommend and dispatch time). All M027 code paths are zero-LLM-token (FR-21/CON-6), read-only (FR-12/CON-1), bash 3.2 compatible (CON-7), and Goodhart-paired (CON-4 — every cost surface ships paired quality data on the same row). Four phases shipped end-to-end: P00 rollup engine + verifier (M019 Tier 1 → tabular cost+quality summary, FS-race tolerant, corrupt-line tolerant, pricing-degradation graceful). P01 orchestrator:cost retrospective + predictive command + intensity-recommend cost-annotation hook (D026 JSON schema; D027 verbatim accuracy disclaimer; <100ms predictive latency for inner library). P02 efficiency footer (commands/status.md) + dispatch-time predictive surface (commands/dispatch.md) — both byte-identical to pre-M027 under --quiet/--yes/auto suppression (CON-3/SC-3/SC-17), gated mechanically. P03 anomaly detection + doctor --config-check (FR-9 + FR-16, advisory exit codes per FR-8). All phases verified: 4 phase-level suites totaling 35+ per-contract verifiers; all green. validate-milestone: 92/92 PASS. Cross-cutting patterns established and reusable: T04-subsumes-prechecks (4 occurrences); document-shaped phase-task pattern (status.md + dispatch.md + doctor.md, 3 occurrences); 5-condition suppression matrix as load-bearing reusable contract (P02 + P03 = 2 occurrences, the shape applies to any future operator-controlled surface); latency outer-vs-inner split (3 occurrences); comment-hygiene-for-verifier-regex (4 occurrences). Open questions resolved during execution: #Q-1 anomaly thresholds pinned by M012-M026 sampling; #Q-5 doctor anomaly perf measured at 56ms; #Q-10 baseline disclaimer text pinned verbatim; #Q-16 always-on predictive surface with --no-predict operator-override (no hidden state). #Q-11 explicitly deferred beyond M027 (no source: runtime records exist in current data — would need M019 Tier 2/3). One observational pattern recurs across phases: latency RELAX-CANDIDATE on macOS dev boxes (~110ms inner vs 100ms target, well within 250ms hard-fail) — same root cause across P01/P02/P03 (bash startup + intensity-recommend re-fork overhead). Remediation, if needed, is plan-level (loosen target with explicit dev-box vs CI thresholds). M027 unblocks M019 Tier 2/3 (per-task granularity + LLM-source records); the suppression matrix shape and document-shaped phase-task pattern are reusable for future operator-controlled surfaces.
  - milestone-summaries :: .orchestrator/milestones/M028/M028-SUMMARY.md:18:  - "BASH_SOURCE self-location with two-step fallback (install-side sibling + in-tree dev fallback with pwd -P symlink resolution; fail-open exit 0 on both miss); shape-classifier-aware resolution-block authoring (every introduced line empirically validated against classify_command before commit; HOOK_DIR_RAW intermediate replaces nested cmdsub; combined-test conditions split into single-test if-blocks); helper-function carve-out as canonical verifier-authoring shape (function bodies are NOT scanned by AP-009 inline-command-shape classifier; documented as top-of-file comment block convention starting M028/P02/T05 — load-bearing across every M028 verifier authoring assert_reject / classify_one / compute_sha / count_managed_flags / extract_corpus_input helpers); plan-time classifier-shape pre-validation discipline (every shape-bearing line empirically run through classify_command before commit; closes the planner-vs-classifier confabulation gap); per-task verifier co-located with deliverable (cross-task verifier dependency rejected; auto-loop --step=V mechanical resolver always succeeds at task time); direct invocation of project-tree verifiers in plan Verification section (no run-probe.sh wrapping; run-probe.sh is reserved for staged throwaway probes inside /tmp /var/folders <repo>/tmp/); two-mode verifier discipline for install-touching gates (cheap dry-run + isolated-HOME real-install); snapshot-ladder shape for byte-equality gates (pre -> action-1 -> action-2 -> reverse-action; SHA at each step; assert idempotency AND reversibility in single isolated-HOME exercise); pinned-SHA-in-comment as informational drift signal; per-leaf tuple-keyed dedup with single-walk target-key collection (empty post-dedup wrappers skipped); wrapper-OR-leaf managed-detection helper bridging M025-to-M028 shape evolution; strict-tuple match with extra-key guard (closes user-authored false-positive risk); idempotency-by-construction verifier (run cleanup twice, assert byte-equality); reversibility-normalization at the installer layer (file-absent for logically-empty JSON); test-command stability across classifier evolution (bare-form compound chains over sh-c-body-wrapped chains); append-only invariant on documentation registers (new entries strictly appended at EOF; existing entries unchanged byte-for-byte; verifiable via wc -l + grep-pattern preservation); closed-pattern-set discipline (M028 spec Non-Goals close the AP-ID set on the seven-screenshot evidence; AP-015+ deferred to future spec/milestone); evergreen forward-compatibility cross-refs (entries cite corpus IDs and wrapper paths that do not yet exist on disk at append time; pointers become live as targets land in subsequent tasks/phases); AP-014 ordering invariant in classify_command (more-specific reject classes inserted BEFORE more-general ones; documented in classify_command comments AND surfaced as discrete PASS line in verifier output); one-level-deep recursion bound via OPAQUE placeholder substitution (CON-5 implementation pattern); char-by-char quote-state machines for shape detection (single-quote / double-quote / backslash-escape state transitions on ${s:$i:1} index walk; bash 3.2 safe; no [[:alpha:]] lookups inside body; no process substitution; no declare -A); M021-strict-superset case extension discipline (new arms placed BETWEEN existing arms and catch-all default; preserves lookup precedence); two-harness symmetric coverage of one corpus (M021 historical + M028 spec-named, both consuming the same fixture); pinned-INPUT alignment between corpus and per-finding verifier (boundary cases use byte-identical INPUT to assert byte-identical verdict); self-conformance verifier scope-narrowing precedent (resolution + reject_lookup printf bodies, not dispatch — empirical line-by-line classifier scan informs the scope decision, not plan prose); bash-parameter-expansion case-arm body extraction (body=${line#*)}; body=${body%;;} — no sed/awk/external-process needed); defensive flag-refusal as wrapper contract (when a wrapper retires an antipattern, refuse the antipattern's signature flag rather than silently accepting it); self-relative-wrapper fixture pattern (verifier copies wrapper into isolated tmp scripts/util/; the copied wrapper's REPO_ROOT then resolves to the tmp root — no environment overrides); source-level self-conformance with comment-line strip (grep -v leading-hash-then-whitespace before scanning wrapper source for forbidden literals); optional-dependency SKIP discipline (command -v <tool> guards with SKIP not FAIL); summary-contract preservation under additive verifier expansion (clamp pass_count at total over bumping total — preserves operator-grep contract); three-surface documentation discipline for cross-cutting infrastructure (planner-facing + agent-facing + catalog-anchor; each surface frames the same content for its audience); two-axis assertion per element in cross-cutting verifiers (existence on disk AND integration into a downstream consumer); permanent in-tree consumer fixture under tests/fixtures/downstream-project/ as CON-10 anchor for noisy-fail shape gates; live-adapter-emission-vs-fixture-bytes shape gate via mktemp -d isolated HOME; shape-pattern comparison via grep -cE regex over literal-bytes diff; count-equality assertions for shape contracts (additive-axis discipline); installer-staged isolated HOME replay harness; JSON event payload via printf-NOT-heredoc (avoids AP-008 heredoc-with-expansion); negative-control assertion as M021-regression catch-all; wrap-the-harness Truth-Check verifier shape; sub-gate-sequencing-with-tmp-log shape; phase-level close-out task shape (zero new deliverables, pure verification gate); subprocess success-message hygiene (when wrapping verifier asserts absence of literal substring, subprocess MUST NOT emit that substring); pre-existing-task-drift fold-in at final-task close (small documentation-layer must-have lookup misses fold into phase's final task rather than re-dispatching completed tasks); header-comment-as-cross-reference-locator (machine-greppable cross-reference annotations in file headers)"
  - milestone-summaries :: .orchestrator/milestones/M029/M029-SUMMARY.md:103:- **M036a P03 live-LLM smoke test** before 2026-05-08 — single real LLM extraction end-to-end (not stub) against a representative PBJ fixture. Cheap insurance against pilot-day regression on the only mock-only path. M036a itself is closed.
  - milestone-summaries :: .orchestrator/milestones/M029/M029-SUMMARY.md:16:  - "AD-1 single-resolve invocation context (TTY/CI/runtime/non_interactive resolved once via detect-invocation-context.sh three-line env block); AD-2 unconditional ANSI strip in JSON sections (render-status-json.sh single ANSI-strip site); AD-3 four-priority non-interactive policy (auto_proceed:true default + --no-confirm override + standard/full intensity gate + leave-marker-absent failure semantics); AD-4 SC-8 oracle wrapper amendment (predictive-surface.sh → summarize-milestone.sh per CON-3 knowledge-layer-boundary; M029 owns its own helper; Spec Amendment Record entry on specs/037-roadmap-visibility-cli-ux/spec.md); AD-5 display_thresholds.compression_savings_pct:5.0 heuristic-default + FR-8 review trigger (re-evaluate post-10-milestone median savings ≥ 3%); AD-6 cross-milestone milestones: frontmatter list + reverse-lookup advisory; AD-7 schema_version:1.0 from day 1; AD-8 knowledge-layer-boundary discipline (no M020 schema change; unit_close consumes existing M019 emitter); AD-9 sentinel-file mechanism for SC-14; scope tightening 2026-05-05 (cut FR-11 GitHub fold-in + FR-12 --refresh-github; defer to demand-driven post-launch external-tool-adapters); #Q-1 full-re-render; #Q-3 leave-marker-absent failure semantics; #Q-5 collapsed-by-default + --expand-all + active-always-expanded; #Q-G8 canonical compact-form ▽ saved Nk invariant; #Q-G9 p95 ≤ 1.0s live-tail latency methodology; CON-1/FR-14 read-only with documented closure-ceremony write sites; CON-3 silent FR-6 cost-column suppression; CON-4/FR-11 no-GitHub-API anti-coupling; CON-7/AD-8 read-only-consumer discipline; AD-19 straight-line bash; MEM001 Bash 3.2 compatibility; MEM004 carve-out for awk/sed/grep pipes inside renderer body"
  - milestone-summaries :: .orchestrator/milestones/M030/M030-SUMMARY.md:28:M030 (adaptive model selection) closed: a task-character classifier routes each dispatch to the cheapest single model that can do the job correctly, with a two-layer safety story — pre-flip classifier-calibration evidence (FR-7/FR-8 shadow corpus + 4-verdict shadow-compare) and post-flip regression-detection mesh (FR-10 verifier-fail escalation + FR-18 per-class anomaly detection + CON-4 operator kill switch). Eight phases (P00–P07) closed end-to-end across 1526 minutes; 14 success criteria (SC-1 through SC-11 inclusive of SC-2a/SC-3a/SC-7a) verified via the M030 acceptance battery (`tests/m030-acceptance/run-acceptance-battery.sh` → `BATTERY: pass=22 fail=0`); evidence captured at `.orchestrator/milestones/M030/M030-ACCEPTANCE-EVIDENCE.md`.
  - milestone-summaries :: .orchestrator/milestones/M035/M035-SUMMARY.md:57:- **#Q-5 (release cadence)** → manual stable releases pre-1.0 (D011 at P04 T04).
  - milestone-summaries :: .orchestrator/milestones/M035/M035-SUMMARY.md:58:- **#Q-5 (config schema for `update_source`)** → `update_source: git|npm|homebrew|none` (D012 at P06 T01).
  - milestone-summaries :: .orchestrator/milestones/M042/M042-SUMMARY.md:14:  - "scripts/knowledge/corpus-exhaustion-sweep.sh,scripts/dispatch/adapters/tool/corpus-gate.sh,scripts/diagnostics/check-corpus-exhaustion.sh,scripts/diagnostics/run-doctor.sh,scripts/state/read-config.sh,templates/corpus-store-manifest.yml,templates/corpus-exhaustion-artifact.md,templates/orchestrator-config-default.yml,commands/corpus-gate.md,commands/discuss.md,commands/specify.md,commands/roadmap.md,commands/plan-phase.md,commands/materials-intake.md,commands/comments.md,packaging/skills/orchestrator-corpus-gate.md,specs/042-corpus-exhaustion-gate/spec.md,tests/fixtures/corpus-gate/,tools/verify/m042-p01-acceptance-battery.sh,tools/verify/m042-p02-acceptance-battery.sh,.orchestrator/proposals/corpus-exhaustion-gate.md,.orchestrator/milestones/M042/M042-SUMMARY.md"
  - milestone-summaries :: .orchestrator/milestones/M042/M042-SUMMARY.md:20:  - "specs/042-corpus-exhaustion-gate/spec.md,.orchestrator/milestones/M042/M042-ROADMAP.md,.orchestrator/milestones/M042/M042-EVALUATION.md,.orchestrator/proposals/corpus-exhaustion-gate.md"
  - milestone-summaries :: .orchestrator/milestones/M042/M042-SUMMARY.md:25:  - "tools/verify/m042-p01-acceptance-battery.sh BATTERY: pass=8 skip=0 fail=0 (SC-1..SC-6); tools/verify/m042-p02-acceptance-battery.sh BATTERY: pass=10 skip=0 fail=0 (SC-7 x6 + SC-8 x3 + shape); run-doctor.sh emits DOCTOR:CORPUS_EXHAUSTION status=ok artifacts=0 against this repo"
  - milestone-summaries :: .orchestrator/milestones/M042/M042-SUMMARY.md:4:id: "M042"
  - milestone-summaries :: .orchestrator/milestones/M042/M042-SUMMARY.md:6:milestone: "M042"
  - milestone-summaries :: .orchestrator/milestones/M045/M045-SUMMARY.md:39:The originally-specced in-session `ScheduleWakeup` substrate does not relieve
  - milestone-summaries :: (capped at 5 hits — more may exist)

### Q6 — KEPT

- **question**: - #Q-6 (precision-floor-protocol): Who is the independent (non-implementer) measurer for SC-11, what corpus composition is defensible, and does the numeric floor need hard-coding in-spec?
- **disposition**: kept (protocol/governance decision (arbiter ruling RISK-3) not answerable from corpus; kept at discuss gate 2026-07-02)
- **terms** (12): Q-6, SC-11, precision, floor, protocol, independent, implementer, measurer, corpus, composition, defensible, numeric
- **hits**:
  - constitution :: .orchestrator/memory/constitution.md:388:### XV. Surgical Precision
  - decisions :: .orchestrator/DECISIONS.md:127:- **Choice**: Revised sequence: **M011 → [M019/Tier 1 emitter] → M012 → M013 → M014 → [M019/Tier 2+3] → M018 → M009 → M010**. Tier 1 ships as a small standalone unit once M011 is closed — avoids mid-milestone scope insertion (Constitution XV — Surgical Precision), gives a clean boundary for instrumentation to start, and still captures M012+M013+M014 (~3 full milestones of dogfooding) before M018 planning needs a baseline. Cost of not backfilling M011 dispatches: ~15–25 unlogged records from M011/P04–P07; acceptable tradeoff. M019 records time, token cost, estimated dollar cost, retries, deviations, verification pass rate, and domain-unit cost (per ingested spec / merged PR / auto-applied comment) at task/phase/milestone/project/all-time granularities. Three-tier framing: **Tier 1 — Just emit** (~1 day: append `payload_breakdown`, `dispatch_usage`, `unit_close` JSONL records to existing `execution-log.jsonl`; no UI; ships first thing after M011 close so M012–M014 dogfooding produces real data); **Tier 2 — Rollup + `orchestrator:cost` command** (~3 days: `scripts/diagnostics/metrics-rollup.sh`, `.orchestrator/metrics/*.jsonl` aggregates, `orchestrator:status` efficiency footer; worth it if we launch); **Tier 3 — Full polished surface** (~7 days: backend-specific `adapters/backend/*/report-usage.sh`, auto-generated `MNNN-METRICS.md` on consolidate, `orchestrator:doctor` anomaly checks, parity evals across runtimes; only if efficiency becomes M009 launch headline). Full M019 phase outline mirrors the tiers: P01 schema + pricing table, P02 dispatch-time emitter (Tier 1 boundary), P03 backend usage adapters, P04 rollup + `orchestrator:cost` command (Tier 2 boundary), P05 milestone close report, P06 status + doctor integration, P07 evals + multi-runtime parity (Tier 3 boundary). Schema must pair cost metrics with **quality metrics** (verification pass rate, deviation rate) from day one to avoid Goodhart failure mode — optimizing cheap over correct. All records labeled `source: "estimate" \
  - decisions :: .orchestrator/DECISIONS.md:155:- **Choice**: Context: `.orchestrator/scratch/articles-synthesis-2026-04-17.md` identified a coherent knowledge-layer theme bundling L7 (wikilinks), L8 (explored state), A1 (preferences layer), A2 (candidate→graduate with required rationale), A3 (review queue in status), A4 (AGENTS.md read-order map), A6 (Jaccard clustering in consolidate). Without a logged trigger, the M020 decision drifts. Evaluation rule at M012/P02 close: assess whether M012's spec-wiki implementation ships **(a)** clickable cross-refs from spec pages to `knowledge/**/MEM*.md` entries via shared ID namespace, **(b)** a review/unreviewed state model on generated pages, **(c)** a query/search surface callable from orchestrator dispatches (wiki-query analog). **Decision rule**: ≥2 of 3 land → M020 dissolves into knowledge-tooling maintenance PRs with no milestone commitment (M012 patterns cover enough of the surface); ≤1 of 3 land → promote M020 as a committed milestone positioned between M014 and M018 (before M019 Tier 2/3, so A5 on_failure flagging can draw on its data). **Minimum-viable subset ships regardless of the M020 decision**: L8 (explored state on auto-generated MEM entries) + A2 minimal guard (`scripts/knowledge/graduate.sh --rationale "..."` flips `status: candidate` → `status: graduated`) bundled into a standalone maintenance PR. Rationale: these are small (~30 lines), address the rubber-stamping failure mode directly, and compound M019 Tier 1's measurement value. The *full* A2 workflow (clustering, staging layer, review queue UI, decision history) waits for the M020 decision. **A4 also ships standalone**, before M019/P00, as an independent concern (session-start file-loading manifest for non-Claude-Code runtimes) — not bundled with P00 because P00's scope discipline is "dispatch-facing payload adaptation only."
  - decisions :: .orchestrator/DECISIONS.md:158:Subjective triggers ("still feels like flat registry") are non-reproducible and drift. Mechanical criteria tied to M012/P02 deliverables make the decision auditable — anyone can inspect M012's output and apply the rule. The 2-of-3 threshold recognizes M012 is a spec wiki, not a knowledge wiki — it may cover some but not all patterns. Shipping the minimum-viable subset (L8 + A2 minimal guard + A4) independently of the M020 decision means the structural anti-rubber-stamping guard lands early without committing to milestone scaffolding that may prove redundant.
  - decisions :: .orchestrator/DECISIONS.md:169:- **Choice**: Revised sequence: **M011 (closed) → M021 (active) → M019 Tier 1 emitter → M012 → M013 → M014 → M019 Tier 2+3 → M018 → M009 → M010**. M021 closes the residual Class B prompt triggers (12 shape patterns surviving M016's Class A hardening) identified in 20 M011/P05–P07 auto-mode screenshots via three layers: a three-wrapper catalog under `scripts/util/`, linter v2 (AP-005..AP-009 detectors + scope widening to task-PAYLOADs), and a PreToolUse shape-guard hook enforcing a closed 10-pattern rewrite/reject matrix. Ordered before M019 so observability metrics (time/tokens/$/quality) dogfood on a **zero-prompt baseline** rather than mixing cost measurements with interruption overhead — cleaner Tier 1 data from M012–M014 dogfooding, more defensible launch narrative. (ID note: this entry was anticipated as D010 in M021-ROADMAP.md and M021-CONTEXT.md on 2026-04-17; D010/D011 landed first, so this reorder decision is recorded as D012.)
  - decisions :: .orchestrator/DECISIONS.md:170:- **Revisable**: No — once the hook is live and the replay corpus is in CI, rolling back is a permission-only change in `.claude/settings.json` (disable the hook entry) but the corpus and AP-005..AP-009 entries stay as knowledge (constitution VII).
  - decisions :: .orchestrator/DECISIONS.md:172:Evidence-grounded: 20 M011/P05–P07 screenshots define the closed pattern matrix (AD-2, AD-5); no speculative additions (constitution XIV). M021 itself dogfoods via its own `orchestrator:auto` execution through P01–P04 (AD-8) — SC-7 attestation is produced by `scripts/verify/m021-p04-dogfood-attestation.sh` and the permanent replay corpus at `tests/fixtures/m021-prompt-corpus.txt`. Adding M021 ahead of M019 costs ~1 milestone of sequencing but eliminates the noise floor that would otherwise contaminate every subsequent metrics measurement — principle I (Context Minimization) applied to the measurement apparatus itself.
  - decisions :: .orchestrator/DECISIONS.md:186:M012/P02 D011-EVALUATION.md (.orchestrator/milestones/M012/phases/P02/D011-EVALUATION.md) records the mechanical outcome: 1 of 3 criteria shipped (cross-refs to knowledge/**/MEM*.md entries via shared ID namespace via the include-markdown pipeline; review/unreviewed state model NOT shipped; query/search surface callable from dispatches NOT shipped). D011's decision rule: <=1 of 3 -> promote as committed milestone, positioned between M014 and M019 Tier 2+3 so A5 on_failure flagging can draw on its data. M012's cross-ref affordance (pathname-keyed wiki stubs with include-markdown) is necessary but not sufficient for the full A1/A2/A3/A6 knowledge-layer workflow; M020 ships the remaining review-state + query-surface + clustering surface. Minimum-viable subset (L8 explored state + A2 minimal graduate.sh guard + A4 session-start manifest) still ships independently before M020 per D011 framing; full A2 workflow and review queue UI wait for M020 kickoff.
  - decisions :: .orchestrator/DECISIONS.md:225:- **Choice**: Revised sequence: **M013 (P04 remaining) → M014 (extended) → M020 → M024 → M019 Tier 2+3 → M018 → M023 → M009 (extended) → M010 (adjusted)**. (1) **M014 scope extension**: port spec-kit's `specify` shape as native `orchestrator:specify` (CC-first, written portably, treating spec-kit template as the I/O contract rather than a verbatim port); add conversus-suggestion logic so complex/controversial specs auto-propose red-blue pressure-test with optional spec-decomposition-before-conversus for very large specs; dual-write `AGENTS.md` alongside `CLAUDE.md` so Codex stays fed without a separate milestone. (2) **M024 Universal Intake** (new): extend `orchestrator:evaluate` to input-agnostic (idea, paragraph, fragment, full spec, or empty + Q&A) and emit a reviewable proposal artifact at `.orchestrator/intake/<id>/proposal.md` covering six axes (input shape, scope tier, decomposition, design gate, conversus gate, intensity); degenerate fast-path auto-proceeds if Tier A + Quick + no conversus + no design, approval-gated otherwise. Positioned after M020 so we dogfood it through remaining milestones. Design gate degrades gracefully pre-M023 ("design walkthrough lands in M023; author DESIGN.md manually or skip"). (3) **M023 Design Layer** (new): `orchestrator:design` command — conversus spawns N design-personality agents in parallel, each produces a DESIGN.md draft + working coded prototype in user's stack; side-by-side comparison; user picks/arbitrates; canonical DESIGN.md lands at repo root and auto-injects into payloads for ui-tagged phases. Renderer adapter interface shaped as MCP clients (runtime-agnostic; Stitch/v0/Figma/etc. consumed as they expose MCP). Stage-1-only scope — high-fidelity stakeholder-mockup expansion (Stitch/Claude-design rendering) deferred until user demand earns it. Positioned pre-M009 because orchestrator has no internal UI to dogfood against — landing earlier buys zero learning. (4) **M009 scope extension**: add runtime-parity audit as pre-launch gate — dogfood every `orchestrator:*` command end-to-end on CC + Codex + Cursor, consuming the `RUNTIME-ASSUMPTIONS.md` registry accumulated during M013–M018, fix parity bugs before external eyes. (5) **M010 posture adjustment**: ship with Managed Agents primary adapter + Codex Cloud **stubbed** adapter (proves abstraction holds without full feature parity); full Codex Cloud as demand-driven fast-follow rather than launch-critical. (6) **Ultraplan/ultrareview stance**: parked, not integrated — Claude-Code-only (requires CC on web + GitHub repo + Anthropic cloud; not available on Bedrock/Vertex/Foundry), forks multi-runtime UX. M013 GitHub integration delivers the same plan-review value universally via PR comments. Re-evaluate only if real CC-only user demand emerges. (7) **`RUNTIME-ASSUMPTIONS.md` discipline** (no milestone cost): a registry file tracking CC-specific shape assumptions as written (`settings.json` hook formats, CLAUDE.md-only writes, `claude` CLI invocation patterns). M009's parity audit consumes it as a punch-list rather than an open-ended investigation. Optional: fold a single Codex smoke-probe telemetry point into M019 T2+3's emitter for drift detection.
  - decisions :: .orchestrator/DECISIONS.md:239:- **Choice**: M014 pressure-tested pre-discuss via cooperative conversus deliberation (Tier 1; 2 agents — `spec-author` + `devils-advocate`; 1 round; no arbiter). 14 MITs applied pre-discuss covering: (P1) Phase Sequencing table as spec-layer subsection after Minimal Slice; SC-2 tightened to mechanical stdout/exit-code/stderr-grep assertions; FR-2b template SSOT (`templates/spec-template.md`) + FR-18 byte-compatibility fixture test (`tests/test-specify-shape.sh`) + SC-17; US-5 promoted to independent P2 user story with scope-boundary sentence ("US-5 is the only path that edits `specs/<NNN>-<slug>/spec.md` from a comment"); Constitution XV retrofit at SC-5 + Constitution Check section → III (primary) + XIV (supporting), with Constitution Check XV paragraph split into "applies to / does NOT apply to" sub-contexts (FR-12 + FR-14 byte-preservation retained; spec-amendment human-gate retargeted); (P2) SC-4 rewritten as two-branch construction (default: measurement + re-planning trigger; upgrade branch: pinned-shape gate with failure-posture clause); FR-14 three-case semantics (all-placeholder / partial / fully-authored) + SC-14 byte-preservation invariant; SC-6a outside-markers `shasum` invariant + `tests/test-dual-write-outside-invariant.sh`; FR-19 `--dry-run` JSONL manifest format pinned M014-local only (M013 retrofit deferred); SC-15 `RUNTIME-ASSUMPTIONS.md` close-out deliverable; SC-16 dogfood-data sizing for FR-9 classifier-shape decision; (P3) FR-7 interim→M024 migration handshake committed as one-line rule; Open Question #C-11 exhaustive CLAUDE.md write-site enumeration at planning time. Spec frontmatter promoted `Status: Draft` → `Status: Ready-for-discuss`; `Last Revised: 2026-04-22` line added matching M013 precedent. P3 Dependencies prose rewrite skipped as subsumed by Phase Sequencing table (P1 #1). Two Phase 4 disputes (SC-4 default branch; Phase Sequencing table layer) resolved via synthesizer-recommended compromises: SC-4 adopts measurement default with upgrade-branch failure-posture; table lands spec-layer.
  - decisions :: .orchestrator/DECISIONS.md:240:- **Revisable**: Yes — if planning-phase discovery shows FR-9 classifier shape cannot be pinned mechanically (dogfood data too sparse, or no regex/heuristic floor earns its keep), SC-4 stays on the default branch and the upgrade branch never activates. If M024 planning reshapes the `.orchestrator/intake/` manifest schema, FR-7's migration-handshake commitment may need amendment via a new D-row.
  - decisions :: .orchestrator/DECISIONS.md:268:- **Revisable**: Yes — (a) if Full-intensity dogfooding reveals clarify's ≤5 question cap is too tight or too loose, adjust in a follow-up D-row without re-opening the three-pass contract. (b) If smell-test false-escalation rate is high, tune `intensity-analyze.sh` keyword weights independently — D019 commits to the resolution order, not the keyword set. (c) `CONVERSUS_GATE_TODO_THRESHOLD` default can be raised (threshold-2 to allow one placeholder) if authoring pass legitimately leaves sections marked as deferred-to-plan. (d) If Full+BLOCK exit-code-2 semantics conflict with `orchestrator:auto` loop handling, harmonize in an auto-specific D-row. (e) Implementation shell in `scripts/specify/specify.sh` can adopt the contract incrementally (Quick intensity first, then Standard, then Full's clarify wiring) without invalidating the contract itself.
  - decisions :: .orchestrator/DECISIONS.md:295:- **Choice**: (1) **Installer staging** (`feat(install)`): `packaging/install/install-{claude-code,codex,cursor}.sh` now stage `scripts/`, `templates/`, and `references/` into the target project root at install time, record every placed file in `.orchestrator/installed-files.txt`, and honor `--uninstall` by replaying the manifest (emits `runtime_removed=N` in the UNINSTALLED summary). `references/installation.md` + `docs/getting-started.md` rewritten to drop the stale "skill bundle" claim and document the real install → project layout including Upgrading and Uninstall sections. `tests/test-installer-stages-runtime.sh` added (24 assertions, green) covering install-exits-0, stage-to-disk, manifest-written, re-install-idempotent, CLAUDE.md-untouched, and uninstall-via-manifest. (2) **State tier fix** (`fix(state)`): `scripts/state/find-active-milestone.sh` prefers `<mid>-EVALUATION.md` as the tier authority (per `commands/evaluate.md`) and falls back to `<mid>-ROADMAP.md`. Previously read tier from roadmap only, which returned `tier=none` for evaluated-but-unroadmapped milestones and caused `orchestrator:auto` to bail with "no active milestone" mid-flow on Tier C projects. Verified against bbt-companion M001 — now returns `M001 planning C`. (3) **Ingest FR-slug fix** (`fix(knowledge)`): `scripts/knowledge/ingest-spec.sh` accepts both `**FR-N**` and `**FR-N (slug-or-anything)**` marker forms (extract numeric ID, treat optional parenthetical as decoration), and aborts loudly with `ERROR:` on any line that looks like an FR marker but yields no numeric ID. Previous behavior silently dropped 36 chunks (19 requirements / 8 constraints / 9 non-goals) from bbt-companion's spec because the slugged markers fell through the narrow regex. Loud-failure turns silent data loss into actionable spec-authoring feedback. (4) **Reinit sentinel-preservation fix** (`fix(lifecycle)`): `scripts/lifecycle/reinit-handler.sh` canonicalizes `--project-dir` via `cd && pwd` so basename extraction is correct for relative paths; snapshots every `# >>> orchestrator:NAME >>>` sentinel block (not just `project-identity`) before the regenerate pass and re-injects each one via `dual-write-runtime-md.sh` post-write; reads `runtime_confidence` from `config.yml` first then from the rendered instruction-file line, and refuses to downgrade a previously-established `high`/`medium`/`low` to `unknown`. (5) **Known limitations parked for follow-up** (four items, do NOT auto-promote to milestone scope): (a) reinit template-regenerate model still loses free-form CLAUDE.md prose outside sentinel + `<!-- BEGIN CUSTOM -->` blocks — redesign needed to either switch to targeted-edit or expand sentinel surface area; (b) installer stage count of 840 files includes `scripts/verify/m*-p*-*` fixtures and `tests/fixtures/**` that a consumer project does not need — a stage-filter could drop to ~300 files (low priority); (c) `scripts/state/check-settings-state.sh` returned `state=ok` while the regen pipeline silently failed during bbt-companion install (observed, not root-caused); (d) manifest does not track upstream deletions — a file removed from spec-kit-orchestrator between installer runs leaves a stale copy in the consumer project (accepted trade-off in handoff §6, worth revisiting).
  - decisions :: .orchestrator/DECISIONS.md:326:(a) The user explicitly chose this trade-off after being given options (wait-for-data vs proceed-with-relax vs alternate read on P99): "im open to coming back and retuning as needed once weve used the comments more." (b) D018 created P99 as an audit-trail device, not a dispatcher contract — the dispatcher already enforces preflight via `Phase-entry gate (CONTEXT OQ-4)` at task dispatch time per the M014 spec boundary map. Keeping the artifact parseable while preserving the gate at the load-bearing layer is the cleaner separation. (c) FR-9 classifier shape is the only plan-phase decision genuinely sensitive to dogfood data; pinning regex/heuristic v1 is the conservative baseline (no LLM cost, no calibration burden) and matches the spec's documented v1 floor. Retune-via-follow-up-D-row is consistent with D019(a)/(b)/(c) precedent (commit to the contract, tune the parameters in follow-up rows). (d) The roadmap edit removes the parser-hostile `P99` token without losing audit trail (this D-row + the new Preflight note carry the same information in machine-parseable form).
  - decisions :: .orchestrator/DECISIONS.md:500:| D004 | M035/P05 | signing-strategy | Release-artifact signing for the npm publishing pipeline (FR-11 / SC-11 / #Q-3 binding) | sigstore (cosign keyless) primary + SHA-256 checksum fallback | Keyless cosign signing in `.github/workflows/release.yml` (`npm-publish` job) binds each release artifact's signature to the workflow's GitHub OIDC identity (canonical-repo `v*` tag-push) and records it in the public Rekor transparency log — eliminates the GPG private-key blast radius (no long-lived signing key to rotate or compromise). `SHA256SUMS` published alongside the signed artifacts gives operators without `cosign` installed a tooling-free verification path (`shasum -a 256 -c SHA256SUMS`). The signing job uses a job-level `permissions: id-token: write` override; the workflow-level `permissions: contents: read` stays unchanged so other jobs (pr-validate) inherit least-privilege. CON-6 secret-scope discipline preserved (no new long-lived secrets; `secrets.GITHUB_TOKEN` is the default token, scoped by GHA). | No |
  - decisions :: .orchestrator/DECISIONS.md:99:- **Choice**: (1) Remove M017 (Conversus Deliberation Gate) from the forward milestone sequence. Revised sequence: **M011 → M012 → M013 → M014 → M009 → M010**. (2) Add M011/P07 (Format-Agnostic Intake & Conversus Fidelity Gate) which lands both the LLM-driven normalizer (any markdown → spec-kit shape) and the reusable `scripts/dispatch/adapters/tool/conversus.sh` adapter. (3) The adapter becomes shared infrastructure: later milestones that want deliberation at specific checkpoints (M013 UAT PR gate, M014 ambiguous-comment classification, roadmap phase-decomposition at Full intensity) invoke it from within their own scope, rather than waiting on a dedicated milestone. (4) Intensity engine owns the policy (when gates fire by default); users own opt-in/out via `--review` / `--no-review`.
  - decisions :: (capped at 5 hits — more may exist)
  - knowledge-entries :: knowledge/conventions/MEM015.md:17:# MEM015: DOCTOR Structured Output Protocol
  - knowledge-entries :: knowledge/conventions/MEM017.md:17:# MEM017: Gate Verdict Protocol
  - knowledge-entries :: knowledge/lessons/MEM028.md:17:# MEM028: Bash arithmetic silently interprets zero-padded numerics as octal
  - knowledge-entries :: knowledge/lessons/MEM028.md:41:The `10#` prefix is portable across Bash 3.2+ and is the canonical idiom for parsing numerics that may carry leading zeros. Apply it to any arithmetic whose inputs come from (a) directory-name parsing, (b) `printf '%03d'` round-trips, (c) date-field components (`$(date +%H)` returns `08` at 8am), or (d) user-supplied strings.
  - knowledge-entries :: knowledge/lessons/MEM028.md:47:- M014/P01 ships the allocator; the dogfood-first-use revealed the bug. The fix is a one-character change (the `10#` prefix) but the lesson generalizes to any bash script handling numeric directory names, ISO date fields, or versioned filenames.
  - knowledge-entries :: knowledge/patterns/MEM029.md:40:- **Stderr/stdout discipline** (cross-cuts MEM015 DOCTOR Structured Output Protocol): structured fields go to stdout in fixed line order; warnings go to stderr; never cross-contaminate.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:129:- **[milestone:M005]** [2026-04-12] DOCTOR: structured output protocol — all diagnostic scripts emit a single structured line (DOCTOR:<CHECK> status=<ok|warn|skip|drift|missing> key=value ...) for machine parsing by run-doctor.sh. Advisory checks (like check-plans.sh) always exit 0; non-advisory checks exit 0 on ok, 1 on warn. Established in P04, extended through P05-P07.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:134:- **[milestone:M005]** [2026-04-12] Gate verdict protocol — scripts/lib/verdicts.sh provides emit_verdict, parse_verdict, orch_is_verdict with four constants: PASS, BLOCK, WARN, NEEDS_REVIEW. hooks.sh captures hook stdout, parses VERDICT lines, resolves multiple verdicts to most severe, and maps to block/warn/continue behavior. Backward compatible when no VERDICT present. Provider convention documented in references/provider-convention.md. Established in P05 per AD-3.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:157:- **[milestone:M021]** [2026-04-17] Shape-classifier as single source of truth: a classifier library (scripts/verify/lib/shape-classifier.sh) consumed by both the enforcement layer (pre-Bash hook) and the regression gate (replay-prompt-corpus.sh) guarantees byte-identical decisions and catches classifier drift the moment corpus expectations diverge.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:158:- **[milestone:M021]** [2026-04-17] Permanent fixture corpus with locked entry count: the 20-entry M011/P05-P07 prompt corpus is the authoritative regression surface — every entry traces to a specific screenshot (ID+SCREENSHOT+INPUT+EXPECTED_OUTCOME). Future hook triggers earn a new milestone, not a fixture append (constitution XIV; AD-5).
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:193:- **[milestone:M027]** [2026-04-27] 5-condition suppression matrix as load-bearing reusable contract (M027/P02 + P03): operator-controlled surfaces should ship --yes / ORCHESTRATOR_AUTO / --no-<feature> / <knob>: false / structural-carve-out — all 5 paths short-circuit to zero stdout exit 0 before any I/O. Gate mechanically with a per-phase suppression-matrix verifier exercising all 5 paths. Quick-tier carve-out (P02) and sample-floor carve-out (P03) are the canonical structural-carve-out shapes. Future cost/quality/anomaly surfaces should follow this contract.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:202:- **[milestone:M028]** [2026-04-29] Helper-function carve-out for AD-19: function bodies are NOT scanned by the AP-009 inline-command-shape classifier in production. assert_reject(), classify_one(), compute_sha(), count_managed_flags(), extract_corpus_input() and similar multi-step helpers can be hoisted into bash function bodies at top-of-script and called inline; the surrounding script remains AD-19-conformant even when helper bodies contain compound substitutions or multi-stage pipelines. Codified at top-of-file as a comment-block convention starting M028/P02/T05; load-bearing across every M028 verifier authoring extraction or shape-assertion helpers.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:203:- **[milestone:M028]** [2026-04-29] Plan-time classifier-shape pre-validation as authoring discipline: every shape-bearing line introduced by a task plan or inside a hook/script body MUST be empirically run through scripts/verify/lib/shape-classifier.sh::classify_command at plan-authoring time, with the verdict recorded in plan prose. Closes the planner-vs-classifier confabulation gap surfaced repeatedly across M028 (T01 reshaped resolution block; T05 swapped test command from sh-c-body to bare compound chain; P03 verifiers used pinned-INPUT alignment between corpus and verifier). Without the trace, the verdict claim is text not contract.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:209:- **[milestone:M028]** [2026-04-29] M021-strict-superset case extension discipline: when extending a switch-style dispatcher whose existing arms are a contract floor (CON-7), place new arms BETWEEN the existing arms and the catch-all default. Existing arms hit first by case-statement top-down evaluation; new arms hit before catch-all; default behavior preserved. Document the preservation invariant in the function header comment block so future modifiers see the constraint without spelunking history. Within the new block, more-specific reject classes are inserted BEFORE more-general ones — surfaced in M028/P03 where AP-014 was inserted BEFORE the existing AP-009 _sc_count_top_level_stages > 2 check (load-bearing for SC-6); SE-09 verdict regresses to compound-chain-gt2 without that ordering. AP-014 ordering invariant surfaced as discrete PASS line in classifier verifier output.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:212:- **[milestone:M031]** [2026-05-01] Detection-by-knob-absence pattern: when a milestone introduces a config knob, downstream comms surfaces use the knob's absence as the migration trigger. No version field, no migration timestamp, no separate state file — knob absence IS the migration trigger. M031/P04 T02 detects pre-M031 configs by absence of quick_knowledge_token_budget. Mirrors the M031/P03 entry_routing_confidence_floor knob-presence detection convention but inverted.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:215:- **[milestone:M031]** [2026-05-01] Milestone-close evidence-ledger convention (M031 → M030 lineage): operator-facing transcription of green BATTERY: pass=N fail=0 line + per-SC roll-up table + ISO-8601 timestamp + SC-13 Option A/B record + back-link to runner. Lives at .orchestrator/milestones/M###/M###-ACCEPTANCE-EVIDENCE.md. Acceptance-battery aggregator inheritance: same set -uo pipefail + run_sc(label, path, extra) helper + bash <path> literal-sequence invocation; extra third positional carries trailing flags (e.g. SC-11 --compare); final BATTERY: pass=N fail=M envelope + exit 0 iff fail=0. Option-A-vs-B reading discipline: planner writes OPTION.md selecting one option; aggregator omits the alternate run_sc line entirely (no commented-out dead code, no skip flag); inventory N reflects the option.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:217:- **[milestone:M033]** [2026-05-04] M033 closes -- warm conversational orchestrator:start front door with four onboarding branches; orchestrator-native constitution authoring (zero spec-kit dependency per Principle XVI); customblock-drafter strict-aggregation discipline (Constitution XV) with floor-not-ceiling preservation; --with-wiki / --with-github paired-launch passthroughs under MIT-001 two-mode test contract; SC-15 closed via US-8 AS-5 signed-attestation fallback (cold-start UX recruiting deferred to 2026-05-12 deadline)
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:71:- `json_field()` strips trailing commas from bare numeric JSON values
  - milestone-summaries :: .orchestrator/milestones/M005/M005-SUMMARY.md:16:  - "AD-1 sha256 format, AD-2 cost_source enum, AD-3 verdict protocol, AD-4 instruction schema, AD-5 pure functions, AD-6 provider convention, AD-19 AD-19 plan shape"
  - milestone-summaries :: .orchestrator/milestones/M005/M005-SUMMARY.md:18:  - "content-hash idempotency, cost_source closed enum, pure lib functions with no I/O, DOCTOR: structured output protocol, instruction schema conformance, verdict protocol (PASS/BLOCK/WARN/NEEDS_REVIEW), provider shell convention, advisory diagnostic classification, scored health reporting, AD-19 script-file verification shape"
  - milestone-summaries :: .orchestrator/milestones/M005/M005-SUMMARY.md:25:  - "DOCTOR: protocol (CONSTITUTION, EVENTS, HASHES, RUNIDS, PLANS, PERMISSIONS, PROVIDERS, INSTRUCTIONS), Checks passed: N/M, doctor-history.jsonl"
  - milestone-summaries :: .orchestrator/milestones/M005/M005-SUMMARY.md:28:Milestone M005 delivers foundational infrastructure hardening across 7 phases and 28 tasks. P01 establishes content-hash idempotency for the knowledge layer. P02 adds cost transparency to telemetry. P03 extracts pure transform functions from dispatch scripts. P04 creates the agent instruction schema with conformance checking. P05 establishes the gate verdict protocol and provider shell convention. P06 delivers 5 new diagnostic checks and a scored run-doctor.sh health report. P07 builds the autonomy permission pipeline with AD-19 plan shape guidance. All phases verified passing. Cross-phase boundary validation clean.
  - milestone-summaries :: .orchestrator/milestones/M005/M005-SUMMARY.md:8:  - "P01: content-hash idempotency (hash.sh, create/update/rebuild hash support); P02: cost transparency (record-telemetry.sh cost_source, aggregate-metrics.sh grouping); P03: payload transform extraction (payload-transforms.sh, manifest-builder.sh pure functions); P04: agent instruction schema (instruction-schema.md, check-instructions.sh); P05: gate verdict protocol (verdicts.sh, hooks.sh verdict parsing, provider-convention.md, check-providers.sh); P06: diagnostic suite (5 new checks, scored run-doctor.sh, extension.yml registration); P07: autonomy permission pipeline (generate/write/check-permissions.sh, AD-19 shape guidance)"
  - milestone-summaries :: .orchestrator/milestones/M008/M008-SUMMARY.md:18:  - "Natural-language scope/risk/complexity pattern matching via keyword tables; pipeline composition via --flag file-or-text inputs for testability; intensity-aware threshold adjustment (Quick tightens, Full loosens); structured dispatch result/error schemas; filename-based adapter auto-discovery; atomic metadata rewrite via awk+mktemp+mv with cksum-sentinel verification; thin wrapper pattern with --dry-run mode; pure resolver pattern (read-only, zero side effects); hermetic standalone e2e via mktemp fixtures (never touch real HOME); runtime adapter HOME guard + project-dir guard; skill generation via single-source-of-truth transform with --check drift detection; project introspection via marker-file presence; user-edit preservation via comment-delimited blocks; top-level pipeline delegation (init orchestrates existing scripts); reinit-with-preservation via awk field-level surgery; wall-clock budget enforcement + hermetic-only static gate."
  - milestone-summaries :: .orchestrator/milestones/M011/M011-SUMMARY.md:18:  - "decision-layer wrapper around creation primitives; chain-walking next-version supersession; intensity-gate stage registration via minimal case-row insertion; chunks-first with raw-spec fallback command wiring; bidirectional-traverse + directional-frontmatter-filter composition; grep -Fq -- end-of-options idiom for BSD grep; sandboxed PROJECT_ROOT=mktemp -d + EXIT-trap for pipeline tests; evidence-present token gate (file-exists + token-contains); per-phase preserve-references regression contract; _STUB=1 env-var escape hatches; three-value exit contract for gate commands"
  - milestone-summaries :: .orchestrator/milestones/M011/M011-SUMMARY.md:28:M011 (Spec Management) delivers the "paste a spec, get a roadmap" value proposition end-to-end. Seven phases took the orchestrator from an opaque-spec-file consumer to a full ingest→chunk→scope-filter→normalize→fidelity-gate pipeline that treats specs as structured knowledge on equal footing with the existing three-temperature knowledge model (M002/M007). P01–P03 built the deterministic chunker (spec directory tree with six subcategories, SPEC-prefixed natural IDs, nested-directory indexing, create-entry.sh SPEC- namespace support, full markdown→chunk classifier with classify+dispatch decision layer, versioned-ID supersession chains, REMOVED detection, phase-impact REVIEW emission). P04 wired scope-filter.sh to resolve `spec/*` scope tags (graph-mode + spec-scope-tags mode), letting build-context.sh deliver scope-filtered spec chunks in dispatch payloads per Constitution Principle I. P05 wired chunks-first evaluation and intensity-aware roadmap generation into commands/evaluate.md and commands/roadmap.md (spec-metrics.sh counts non-superseded chunks; spec-story-graph.sh emits directional story-to-story depends_on edges via bidirectional-traverse + directional-frontmatter-filter composition; intensity-gate.sh roadmap stage with Quick/Standard/Full modes). P06 delivered the user-facing commands/ingest.md, four doc-shape verify scripts, a full sandbox E2E gate script, a timing harness (<60s on representative specs), and dogfood evidence against specs/016-autonomous-hardening/spec.md (20 chunks, 7s elapsed). P07 closed with format-agnostic intake and a reusable Conversus fidelity gate: detect-spec-shape.sh probe classifier, normalize-spec.sh thin wrapper dispatching through dispatch-interface.sh (no hardcoded LLM calls), templates/spec-normalizer-prompt.md, a reusable Conversus adapter (scripts/dispatch/adapters/tool/conversus.sh with check/gate/parse-verdict subcommands, graceful degradation, CONVERSUS_STUB escape, three-value exit contract 0/1/2), commands/conversus-gate.md designed for M013/M014 reuse with different presets, templates/conversus-presets/normalize-fidelity.yml, templates/gate-result.md, and integration into commands/ingest.md as a pre-chunker 6-step pipeline (detect-shape → normalize → resolve-policy → fidelity-gate → chunker → rebuild) gated by a new `ingest` intensity stage (Quick=normalize; Standard/Full=normalize+fidelity-gate). A 222-line user guide (docs/ingesting-arbitrary-specs.md) covers flag semantics, BLOCK resolution, stub modes, and preset extension.
  - milestone-summaries :: .orchestrator/milestones/M012/M012-SUMMARY.md:10:  - "populated .orchestrator/ tree (constitution, DECISIONS.md, KNOWLEDGE.md, milestone-summary.md, milestones/, archive/); populated knowledge/patterns+conventions+lessons/MEM corpus; mkdocs + mkdocs-material + mkdocs-include-markdown-plugin + pymdown-extensions Python toolchain (version-pinned in wiki/requirements.txt); GitHub Pages + GitHub Discussions enabled on the repo for the operator-gated first deploy; gh CLI for wiki-giscus-remap.sh live path; M011 spec-ingest pipeline produced the 022-spec-wiki feature spec; M021 anti-pattern-lint + pre-bash-shape-guard + widened allow-list gates"
  - milestone-summaries :: .orchestrator/milestones/M012/M012-SUMMARY.md:108:- **P04-SUMMARY walkthrough** — authoring the US1..US5 + SC-1..SC-11 walkthrough in `P04-SUMMARY.md` flips the `m012-p04-summary-walkthrough.sh` gate from SKIP to PASS. Summary is a phase-close artifact, not a task-execution artifact.
  - milestone-summaries :: .orchestrator/milestones/M012/M012-SUMMARY.md:16:  - "AD-1 D011 ship cross-refs only (defer review-state + query surface to M020); AD-2 deploy to same repo gh-pages + same Discussions; AD-3 SSOT via include-markdown plugin (no copy, no symlinks); AD-4 archive included labeled with no date cutoff; AD-5 Giscus mapping=pathname + documented remap script; AD-6 all phases/tasks included with collapsible per-milestone nav; AD-7 MkDocs + Material + include-markdown-plugin + Giscus + GitHub Pages framework stack locked; AD-19 single-script-file Check shape with MEM004 carve-out for scanner/diagnostic/wrapper internals; D011 mechanical evaluation rule (1 of 3 criteria -> M020 promoted); Constitution VI state-on-disk-is-truth preserved via include-only projection; Constitution XIV no-speculative-complexity (no flags, no plugin drivers, no YAML-driven wrappers); Constitution XV surgical precision (each phase touches disjoint file surfaces; cross-phase additions via marker-bounded blocks + EOF-append); MEM001 Bash 3.2 discipline across all shipped scripts; loud-fail-over-silent-empty for ENV empty defaults; SC-10 self-contained wiki/ + sibling-gate allow-list extension policy; SC-12 pending-sentinel deploy record for operator-gated outcomes"
  - milestone-summaries :: .orchestrator/milestones/M012/M012-SUMMARY.md:30:M012 ships the dogfood spec→wiki loop: a deployable MkDocs Material site that projects every in-scope `.orchestrator/**.md` artifact plus the `knowledge/**/MEM*.md` corpus through a single-source-of-truth include pipeline, overlays per-page Giscus threads, and deploys via a chained wrapper that runs four pre-deploy gates before `mkdocs gh-deploy`. The milestone's vision was "a dogfood-only MkDocs + Giscus site that renders `.orchestrator/` artifacts as a navigable, commentable wiki with one-command deploy" — and that is exactly what landed across four phases (P01 → P02 → P03 → P04), 37 verification gates in total, and a D011 mechanical evaluation that promoted **M020** to a committed milestone.
  - milestone-summaries :: .orchestrator/milestones/M012/M012-SUMMARY.md:44:Extends the P01 pipeline to resolve internal cross-links, surface the `knowledge/**/MEM*.md` corpus through the existing SSOT pipeline, and ships a standalone built-site link checker. Ends with the D011 mechanical evaluation.
  - milestone-summaries :: .orchestrator/milestones/M013/M013-SUMMARY.md:10:  - "M011 KNOWLEDGE-INDEX.md + SPEC-* frontmatter authority in knowledge/spec/**/SPEC-*.md (Knowledge-Layer Boundary D014); M011/P07 scripts/dispatch/adapters/tool/conversus.sh adapter with --strict + parse-verdict + CONVERSUS_STUB contract; M016/M021 scripts/verify/anti-pattern-lint.sh invariant; M008/P05 runtime adapter --probe/--register/--hook-config/--dry-run interface contract (claude-code.sh adapter target); M001 scripts/lifecycle/lock-manager.sh create/break subcommands; M019 Tier 1 unit_close + payload_breakdown JSONL shape + 4-rule ORCHESTRATOR_ROOT resolver; M007/D007 single-user/project hard-migrate stance (sidecar-as-projection-not-peer); Constitution X (Single-Hands-On Runtime) + XII (Bounded External Dep) + XIV (No Speculative Complexity) + XV (Surgical Precision); MEM001 Bash 3.2 compatibility; gh CLI for live mode (auth scopes: repo + project); GitHub Issues + Project v2 + Milestones features enabled on the operator's repo; .gitignore covers .orchestrator/integrations/github.json (operator-owned sidecar) + specs/**/conversus/ (ephemeral conversus output)"
  - milestone-summaries :: .orchestrator/milestones/M013/M013-SUMMARY.md:86:5. **`phase-transition.sh` non-numeric `duration:` bug** — worked around with integer-minutes throughout M013 (P01 left "unreported"; P02/P03/P04 use minutes). Fixed during M013 close commit-pass cleanup (`scripts/lifecycle/phase-transition.sh` now tolerates non-numeric and SIGPIPE under `set -o pipefail`).
  - milestone-summaries :: .orchestrator/milestones/M014/M014-SUMMARY.md:12:  - "Every subsequent milestone's spec is authored via orchestrator:specify rather than hand-copied (SC-13 dogfood gate); M020/M024/M023/M018/M009/M010 consume orchestrator:specify as the scaffolding path; M024 (Universal Intake) consumes FR-7 decomposition-manifest path convention with forward-compat schema; M023 (Design Layer) consumes spec FR-2 Section Contract as input shape; M009 (Launch) consumes RUNTIME-ASSUMPTIONS.md as Codex parity punch-list and orchestrator:specify as external-adopter onboarding path; M019 Tier 2/3 consumes specify-cost, classifier-cost, conversus-cost-per-spec-draft, and comment-auto-apply-rate metrics via execution-log emitter; M020 consumes decision-append auto-apply entries + applied spec-amendments as review-state lifecycle inputs"
  - milestone-summaries :: .orchestrator/milestones/M014/M014-SUMMARY.md:14:  - "commands/specify.md,commands/comments.md,commands/doctor.md,scripts/specify/specify.sh,scripts/comments/fetch.sh,scripts/comments/classify.sh,scripts/comments/apply.sh,scripts/comments/reject.sh,scripts/comments/triage.sh,scripts/comments/comments.sh,scripts/util/dual-write-runtime-md.sh,scripts/knowledge/spec-complexity-probe.sh,scripts/verify/spec-shape-lint.sh,scripts/diagnostics/check-docs.sh,scripts/diagnostics/run-doctor.sh,scripts/lifecycle/init-project.sh,scripts/lifecycle/reinit-handler.sh,scripts/knowledge/consolidate-artifacts.sh,scripts/migrate/m014-p02-migrate-recent-changes.sh,templates/spec-template.md,templates/spec-scaffolder-prompt.md,templates/conversus-presets/spec-pressure-test.yml,templates/conversus-presets/classify-comment.yml,templates/spec-complexity-contradiction-prompt.md,templates/spec-splitter-prompt.md,references/spec-management.md,.orchestrator/config.yml,RUNTIME-ASSUMPTIONS.md,tests/test-specify-shape.sh,tests/test-dual-write-outside-invariant.sh,tests/fixtures/m014-p01/expected-section-headings.txt,tests/fixtures/m014-p01/specify-fixture-prose.txt,tests/fixtures/m014-p03/sample-inbox.jsonl,tests/fixtures/m014-p03/queued-amendment.md,tests/fixtures/m014-p04/corpus-labels.tsv,tests/fixtures/m014-p04/contradictory-prose.txt,tests/fixtures/m014-p04/decomposable-prose.txt,tests/fixtures/m014-p04/amend-seed-spec.md,specs/024-spec-management-extended/planning-inputs/inbox-dogfood.md,CLAUDE.md,AGENTS.md"
  - milestone-summaries :: .orchestrator/milestones/M014/M014-SUMMARY.md:18:  - "Template SSOT with ground-truth heading fixture; marker-bounded atomic splice with byte-preserved outside region (SC-6a); subcommand surface with deferred body (diagnostic-stub exits distinct codes; full semantics land in later phase without command-file re-authoring); hermetic scratch project pattern for end-to-end command testing; verifier self-exemption for rule-embedding gates; awk-field extraction for structured corpus parse; write-site enumeration discipline with scanner gate; allow-list pattern for orthogonal writes (runtime-native full-file renders); hermetic-root via dirname of ORCH_ROOT (decouples dual-write target from repo root); advisory-first drift severity model; one-time migration script pattern with --dry-run/--apply/--force + idempotent already-migrated short-circuit; CC-first with defensive fallback (runtime gate + empty-response bailout — never fails caller); STATE_ROOT vs PROJECT_ROOT separation for mutation sinks vs dependency lookups; CONVERSUS_STUB hermetic testing via fixture-copy path in adapter; dispatch-interface mock via backup-and-swap; section-split awk + per-section classifier; append-with-body-refresh for registry entries (heading byte-identical body refreshed); bash32 scanner comment-strip to avoid false-positive on comment prose; hermetic-test stub via env-var pointing at JSONL fixture (GH_API_STUB, GH_GRAPHQL_STUB); per-surface source_surface filter on shared stub fixture; FR-19 dry-run JSONL action records as no-disk-write counterpart; grep -F literal-string match for URL dedup; stdout-verdict + stderr-INFO split; single-agent cooperative preset for refinement-only LLM calls; patch -N forward-only stale-diff probe; env-overridable sub-script-path hermetic-test pattern (COMMENTS_*); non-executable adapter as routing verdict (graceful-degradation test pattern); deterministic content-addressed filenames for idempotent queue/triage; two-tier must-not-happen invariant verification (source grep + runtime event assertion); bash32+lint omnibus with dynamic enumeration + basename self-exemption; zero-prompts hermetic scratch + fixture replay under --yes; outside-marker shasum invariant pre/post dual-write; phase-suite orchestrator with IFS-newline iteration + per-gate rc capture + FAILED_GATES accumulator"
  - milestone-summaries :: .orchestrator/milestones/M014/M014-SUMMARY.md:34:**P04 (240m, 7 tasks, 12-gate suite)** converted P01 stubs into full implementations — FR-5 complexity probe with five axes and OR-semantics plus `hardening_spec_exception` boolean override; US-3 three-way prompt wiring conversus adapter via `--strict` invocation; FR-7 `split` subcommand with LLM-assisted decomposition (CC-only v1); FR-14 three-case `--amend` body; `templates/conversus-presets/spec-pressure-test.yml` red-blue preset; calibration corpus pinned thresholds; `references/spec-management.md` SC-11 completion.
  - milestone-summaries :: .orchestrator/milestones/M014/M014-SUMMARY.md:8:  - "Native orchestrator:specify command (P01 create-path + D019 three-pass scaffold/author/gate contract), AGENTS.md dual-write at every CLAUDE.md write-site (P02) with SC-6a outside-markers byte-preservation invariant + FR-13 drift detector, comment→workflow classifier with four-class regex/heuristic v1 baseline per D023 and human-gated spec-amendment apply path (P03), conversus auto-propose for risky drafts with full FR-5 complexity probe + FR-6 pressure-test preset + FR-7 decomposition + FR-14 three-case --amend (P04). commands/specify.md + commands/comments.md + commands/doctor.md extension; scripts/specify/specify.sh + scripts/comments/fetch|classify|apply|reject|triage|comments.sh; scripts/util/dual-write-runtime-md.sh; scripts/knowledge/spec-complexity-probe.sh; scripts/verify/spec-shape-lint.sh; scripts/diagnostics/check-docs.sh --check drift; templates/spec-template.md FR-2b SSOT + templates/spec-scaffolder-prompt.md FR-3 + templates/conversus-presets/spec-pressure-test.yml + templates/conversus-presets/classify-comment.yml + templates/spec-complexity-contradiction-prompt.md + templates/spec-splitter-prompt.md; .orchestrator/config.yml specify: section + comments: section + dual_write_agents top-level key; references/spec-management.md (SC-11 complete with Comment Classification section); tests/test-specify-shape.sh (FR-18) + tests/test-dual-write-outside-invariant.sh (SC-6a); RUNTIME-ASSUMPTIONS.md registry (D016 scaffold) with FR-3, FR-5-full, FR-7 entries; specs/024-spec-management-extended/planning-inputs/inbox-dogfood.md (SC-16 best-available per D023). Closes the M015 bootstrapping loop: every future milestone's spec authored via orchestrator:specify + reviewed via orchestrator:comments."
  - milestone-summaries :: .orchestrator/milestones/M018/M018-SUMMARY.md:16:  - "SC-9 calibrated to 34.7% mean payload-token reduction floor (P00 probe-derived 80% CI lower bound replaces unbacked 25% placeholder); CON-5 additive-only emitter schema (cols 1-12 stable; 13-16 P05; 17-18 P06); phase-transition.sh --write canonical close path (write-summary.sh phase retired after P05/T04 SYNC:MISMATCH); P05/P06 inversion vs CONTEXT.md AD-1 (eval harness gates T3 close per RISK-3); Tier 3 master enabled:true but operator-binary-required to engage (provider-resolution ladder takes failure-passthrough without ORCH_TIER3_LLM_BIN or claude on PATH); CONVERSUS_PROVIDER=claude-code mandatory on OAuth (anthropic 429s are policy gates not transient); SHA-256 full-digest cache key with mtime-only prune (no reference counting needed when key is content-addressed); MEM004 emitter-internal carve-out extends to JSONL rollup helpers and diagnostic CLI bodies; documented-divergence carve-out on P07 verifiers (accepts regression_flag: divergence iff RA-M018-NN row documents it)"
  - milestone-summaries :: .orchestrator/milestones/M018/M018-SUMMARY.md:35:every tier is independently togglable; with the master flag off the dispatch
  - milestone-summaries :: .orchestrator/milestones/M018/M018-SUMMARY.md:43:  amended SC-9 to a probe-derived 34.7% mean payload-token reduction floor
  - milestone-summaries :: .orchestrator/milestones/M018/M018-SUMMARY.md:59:  signal emitter (running-mean savings vs SC-9 floor, never blocks dispatch).
  - milestone-summaries :: .orchestrator/milestones/M018/M018-SUMMARY.md:84:  sav_total > 0 AND ratio < SC-9 floor (configurable via
  - milestone-summaries :: .orchestrator/milestones/M018/M018-SUMMARY.md:85:  `compression.regression_floor`, default 0.347).
  - milestone-summaries :: .orchestrator/milestones/M020/M020-SUMMARY.md:40:P06 (preferences) shipped `scripts/knowledge/lib/preferences.sh` exposing `pref_resolve <key>` for the FR-6/US-5 cascade. Closed-enum key vocabulary {`default_state_filter`, `similarity_threshold`, `staleness_threshold`, `preferred_cluster_size`, `operator_identifier`} with built-in defaults (`graduated`, 0.7, 14, 8, `unknown@local`) and per-key validators. THREAT-007 disposition: each key resolves independently with project > user > built-in-default precedence (partial overlaps land cleanly). Malformed values fall back to next source per the precedence chain with a single-line stderr `WARN` diagnostic naming key + offending value + selected fallback; the preferences file is NEVER mutated. AD-5 grep+sed YAML parsing keeps the helper yq-free. The deferred-resolution sentinel pattern (empty-string sentinel + seen-on-cli flag set in the CLI arm + post-argparse pref_resolve cascade) is how `query.sh` and `consolidate-artifacts.sh` integrate: CLI > project > user > built-in default. The existing closed-enum validation block runs against the resolved value as belt-and-suspenders defence. `references/preferences.md` (191 lines) documents the cascade with worked partial-overlap and malformed examples plus three runbooks. `tests/test-preferences-resolution.sh` (272 lines, MEM002) covers SC-5 end-to-end through both query.sh and consolidate-artifacts.sh --cluster; the test caught and forced correction of a T03 drive-by `pref_resolve 2>/dev/null` that had silenced the malformed-WARN diagnostic that US-5 requires.
  - milestone-summaries :: .orchestrator/milestones/M020/M020-SUMMARY.md:48:Downstream affects: M024 (Universal Intake & Routing) consumes the `query.sh` deterministic surface for proposal reasoning and the `consolidate_cluster` JSONL surface for cluster-state lookups. M019 Tier 2+3 (observability emitter, downstream tiers) consumes `knowledge_graduate`/`knowledge_archive`/`consolidate_cluster` JSONL records as latency + cluster-health signals; if the rebuild-on-each-query cost becomes load-bearing per THREAT-005, M019 Tier 2+3 metrics are the trigger. M018 (context compression) inherits the CON-5 v2 feature vector (frontmatter `title` + `topic` + `tags[]` + `relates_to[]` + `source_unit` + body-cap-200) as a stable contract; full-body tokenization remains deferred. THREAT-004 (review-queue growth) and THREAT-007 (preferences cascade ambiguity) are accepted-with-revisit-trigger (queue >50 candidates or runtime >2s; per-key independent resolution documented in `references/preferences.md`). The schema authority remains held by M020 -- consuming milestones are READERS only; future field additions require an M020 D-row and a conventions-MEM update.
  - milestone-summaries :: .orchestrator/milestones/M021/M021-SUMMARY.md:16:  - "AD-2 rejects-dominate-rewrites precedence, AD-5 locked corpus size (20 entries), AD-6 hook_reject_recovered is the success signal not a failure, AD-8 dogfood-attestation pattern (continues M016), AD-11 append-only discipline for DECISIONS+ANTIPATTERNS, AD-19 single-script-file invocation shape for agent-facing commands, D012 M021-before-M019 sequencing, MEM004 gate-internals carve-out, constitution IX Bash 3.2 compat, constitution XIV no speculative complexity (locked entry count), constitution XV surgical precision (D012 row normalization)"
  - milestone-summaries :: .orchestrator/milestones/M025/M025-SUMMARY.md:11:  - "M013/P04/T04 commit d33b8a7 as read-only regression context; M014/P01 scripts/util/dual-write-runtime-md.sh; M012 scripts/knowledge/rebuild-index.sh + wiki include-markdown projection; M013/P04 FR-12-v1-negative-grep-guard pattern; M008/P05 runtime adapter --probe/--register/--hook-config interface; Constitution VII (Knowledge Compounds), XIV (No Speculative Complexity), XV (Surgical Precision); MEM001 Bash 3.2 compatibility; python3 as de-facto gate baseline (precedent: m013-p04-* gates)"
  - milestone-summaries :: .orchestrator/milestones/M029/M029-SUMMARY.md:38:- **P03 (live + preflight + auto-chain)** — `--live` branch on `render-position.sh` (FR-7, #Q-1 full-re-render on every appended `dispatch_usage` record, #Q-G9 `p95 ≤ 1.0s` measurement methodology via `measure-live-tail-latency.sh` with three-tier nanosecond-clock portability shim); canonical compact-form `▽ saved Nk` marker (FR-8, #Q-G8 — verbose-suffix forms forbidden) gated by AD-5 `display_thresholds.compression_savings_pct: 5.0` config knob (templates/orchestrator-config-default.yml + scripts/state/read-config.sh VALID_KEYS extension + references/file-formats.md docs + FR-8 review trigger); `commands/auto.md ## Preflight Summary` section (FR-9, AD-3 four-priority non-interactive policy, AD-4 oracle wrapper); `commands/start.md --auto-chain` flag + `scripts/lifecycle/start.sh` chain-driver passthrough (FR-10, #Q-3 leave-marker-absent failure semantics, AUTO_CHAIN_STAGE_STUB fixture-only escape hatch); `tests/m029-acceptance/measure-live-tail-latency.sh` harness; SC-7 / SC-8 / SC-9 / SC-10 fixtures + acceptance scripts; AD-4 Spec Amendment Record entry on `specs/037-roadmap-visibility-cli-ux/spec.md`; SC-11 milestone-grain `run-acceptance-battery.sh` chaining all three per-phase batteries + emitting `BATTERY: pass=14 fail=0`; SC-12 `m029-p03-validate-milestone-pass.sh` hook asserting `validate-milestone.sh M029 VALIDATE: PASS` exit 0; closure ceremony (this `M029-SUMMARY.md` + `M029-VALIDATED` marker + milestone-grain `unit_close` JSONL record auto-emitted by `write-summary.sh milestone`). Six tasks, thirteen-gate phase suite PASS at close-time.
  - milestone-summaries :: .orchestrator/milestones/M029/M029-SUMMARY.md:8:  - "M029 (roadmap visibility & CLI UX) closes the orchestrator's invocation-context + tree-rendering + auto-chain onboarding surfaces. P01 foundation: scripts/state/detect-invocation-context.sh AD-1 single-resolve env block + references/status-headline-shape.md FR-2 + references/status-json-schema.md FR-3 + commands/status.md headline block + scripts/diagnostics/render-status-json.sh AD-2 unconditional ANSI strip + commands/context.md FR-4 read-only single-screen runtime profile skill. P02 at-rest tree: scripts/diagnostics/render-position.sh ~470-line tree renderer engine FR-5 four-glyph alphabet + commands/where.md skill + references/cross-milestone-feature-shape.md AD-6 contract + scripts/diagnostics/summarize-milestone.sh AD-4 SC-8 oracle wrapper + SC-5/6/13/14 fixtures + AD-9 sentinel-file mechanism + FR-6 silent pre-M019 cost-column suppression. P03 live + preflight + auto-chain: --live branch on render-position.sh FR-7 + #Q-G9 p95 ≤ 1.0s methodology via measure-live-tail-latency.sh three-tier nanosecond-clock portability shim + ▽ saved Nk canonical compact-form FR-8 #Q-G8 gated by AD-5 display_thresholds.compression_savings_pct config knob + commands/auto.md ## Preflight Summary FR-9 AD-3 four-priority non-interactive policy AD-4 oracle wrapper + commands/start.md --auto-chain flag + scripts/lifecycle/start.sh chain-driver passthrough FR-10 #Q-3 leave-marker-absent failure semantics AUTO_CHAIN_STAGE_STUB fixture-only escape hatch + SC-7/8/9/10 fixtures + AD-4 Spec Amendment Record on specs/037-roadmap-visibility-cli-ux/spec.md + SC-11 milestone-grain run-acceptance-battery.sh BATTERY: pass=14 fail=0 + SC-12 validate-milestone-pass.sh VALIDATE: PASS hook + closure ceremony M029-VALIDATED + M029-SUMMARY.md + milestone-grain unit_close JSONL record. Three phases all green; acceptance battery pass=14 fail=0; validate-milestone.sh M029 reports VALIDATE: PASS — 101/101 checks passed."
  - milestone-summaries :: .orchestrator/milestones/M030/M030-SUMMARY.md:16:  - "D-A1 4-verdict closed-enum (flip_recommendation ∈ {ready, partially_ready, block, evidence_insufficient}); D-A2 programmatic shadow-compare invocation as the FR-9 flip-gate (live routing refuses adapter call if corpus below threshold; emits override_source=shadow_gate_blocked); D-A3 partial-flip safety (only smart-defaulted classes may be enumerated in withheld_classes — fast/balanced classes flip wholesale or block); D-A4 mechanical independence (P00 fixture corpus committed BEFORE P01 classify-task.sh authored — independence-by-construction; verifier graduates post-P01 to git-log ordering check); D-A5 kill-switch precedence over min_tier (model_routing_enabled: false short-circuits before flip-gate or escalation logic; stderr names the bypassed min_tier); D-A6 cost_rates SSOT in templates/model-routing.yml (single source of truth for rollup + counterfactual computation); D-A7 / SC-3a write-path correctness (every shadow record's model_routed agrees with an independent classifier run on the same plan; classifier_confidence emitted end-to-end in P02 not deferred to P03); D-A8 escalation cap CON-5 (3 attempts max: fast → balanced → smart → escalation_cap_hit; no fourth record); D-A9 anomaly-record JSONL append-only convention (separate file .orchestrator/anomalies.jsonl preserves CON-6 dispatch-stream invariant on execution-log.jsonl); P01 classifier-confidence stability metric definition (rolling variance ≤ 0.10 + N=20 minimum class-coverage; consumed end-to-end by shadow-compare.sh); P02 4-verdict closed-enum + shadow-corpus path-resolution priority (env var M030_SHADOW_COMPARE_CORPUS → --corpus flag → default); P03 override-source closed-enum closure + kill-switch precedence over min_tier (compound config tested via SC-7a); P04 escalation-cap programmatic enforcement + flip-gate refusal of below-threshold corpus; P05 cost_rates-absent fallback (warning + zero-savings line) + ORCHESTRATOR_ROOT carve-out for footer fixtures; P06 #Q-4 threshold default (pass_rate 0.50 + min_class_sample 10) + env-only JSONL emit path (M030_ANOMALIES_JSONL_PATH default .orchestrator/anomalies.jsonl) instead of CLI flag; P07 4-task split (corpus + battery + ledger + close-ceremony) + structurally-distinct T04 ceremony fusing phase + milestone close into one atomic commit"
  - milestone-summaries :: .orchestrator/milestones/M031/M031-SUMMARY.md:16:  - "AD-1..AD-20 folded in-place into specs/034-right-sized-entry/spec.md (P00); SC-13 Option A active per fixtures/empirical-baseline/SC13-OPTION.md (corpus has no committed git history at evaluation time; battery N=15 not 16); FR-4 collapse of commands/dispatch.md:21 Skip-payload-assembly branch is the load-bearing change that makes Quick knowledge-unconditional; Quick profile traversal scope = touched-files-only + 1-hop + no Decisions section + glossary slice over touched terms (per-profile scope/traversal/decisions/glossary policy); --meta-out JSON sidecar emitted by every Quick dispatch with mem_count + total_tokens + profile + compression_applied + snip_applied (CON-1 invariant); deterministic task-slug derivation = first 40 chars lower-cased + hyphenated + alphanumerics-only + 4-char SHA-1 collision suffix (AD-10); ORCH_<SUBSURFACE>_<NOUN> env-override seam naming as the universal test-seam idiom; passive-observation-surface discipline (m031_compound_change_check returns 0 unconditionally — never increments checks_passed/total/advisory_warnings); detection-by-knob-absence pattern (no version field, no migration timestamp — knob absence IS the migration trigger); window=7 + threshold-multiplier=1.1 + median-not-mean for QUICK_BUDGET_DRIFT (operator-meaningful summary, absorbs single-task variance); QUIET=0 gate as byte-identity preservation discipline (additive emission surfaces never break --quiet zero-stdout contract); P04/T04 follow-up commit 6979afa: empirical-baseline.sh COMPARE verdict frame inverted from 'fewer tokens' to 'post pass-rate >= pre pass-rate' (M031's restoration thesis vs the pre-M031 thrift thesis) — pass-rate parity is the load-bearing CON-5 + Principle II claim, token-budget discipline is owned separately by SC-15; auto_proceed: true default flip framed via single CHANGELOG bullet using 'compound' keyword (AD-9); milestone-grain SC-12 scope-guard distinct from per-phase scope-guards (operator-facing acceptance-test surface vs internal phase-suite gate); doc-comment-only Key-links remediation pattern at phase-close (single comment block in cross-referenced script naming the literal target so check-must-haves grep resolves) — established P01 → P02 → P03 → P04, now canonical; verbless 'orchestrator <task>' entry reserved for post-M009 runtime parity (M031 ships orchestrator:do <task> only)"
  - milestone-summaries :: .orchestrator/milestones/M031/M031-SUMMARY.md:18:  - "Per-profile dispatch shape (Quick / Standard / Full each have distinct scope + traversal + Decisions inclusion + glossary slice + budget); --meta-out JSON sidecar as the universal cross-phase plumbing for downstream consumers (P03 fast-path summary reads it; P04 budget-drift surface reads it); test-only env-override seam at the top of every resolution chain (ORCH_<SUBSURFACE>_<NOUN> — four consecutive surfaces); 4-layer config-knob resolver via direct YAML grep (env / .orchestrator/config.yml / template default / hardcoded); detection-by-knob-absence as zero-state-file migration trigger; passive-observation-surface discipline for milestone-announcement banners (printf message + return 0); windowed-median informational warning (helper trio — config resolver + windowed-statistic computer + trigger emitter — wired into a CLI consumer with env-override seam at top of resolution chain); half-full-window early-return discipline (windowed statistics emit empty stdout when window holds fewer than N records, downstream consumers early-return 0 on empty stat); QUIET=0 byte-identity preservation gate (additive emission surfaces gate on QUIET=0 to preserve --quiet zero-stdout contracts); compound-change comms framing (passive surface in CHANGELOG + active surface in doctor share vocabulary so operator gets unified migration story); two-layer absence-check separation (production target read by SC test, SC test source read by shape verifier — no duplication); POSIX-bash discipline for cross-runtime acceptance tests (CON-6/DC-7 — keeps M009 multi-runtime parity audit free of rewrite work); dual envelope on milestone-grain scope-guard (RESULT: SC-12 pass for acceptance + SUMMARY: scope-guard.sh pass=N for shape verifiers); allow-list third permissive prefix at milestone grain (.orchestrator/milestones/M###/phases/) covers every phase/task plan + payload + summary file without per-file enumeration; acceptance-battery aggregator inheritance (M030 → M031 lineage — same set -uo pipefail + run_sc helper + bash <path> literal-sequence + final BATTERY: envelope); Option-A-vs-B reading discipline (planner writes OPTION.md selecting one option; aggregator omits the alternate run_sc line entirely — no commented-out dead code); expected-fail shape-verifier-shipped-before-artifact pattern (T04 ships ledger-shape verifier failing until T05 commits the artifact, documented inline so future maintainers do not 'fix' the failure by deleting the verifier); milestone-close evidence-ledger convention (mirrors M030 — operator-facing transcription of green BATTERY line + per-SC roll-up + ISO-8601 timestamp + back-link to runner); doc-comment-only Key-links remediation at phase-close as canonical resolution pattern for plan-time key-link gap discovery (4 phases consecutive); deterministic task-slug derivation (40-char lower-cased + hyphenated + alphanumerics-only + 4-char SHA-1 collision suffix — AD-10); plain-language y/n/c approval prompt with inline summary truncated to budget + (N more lines at <path>) ellipsis when oversize + stderr audit line under --yes (AD-7+AD-20)"
  - milestone-summaries :: .orchestrator/milestones/M032/M032-SUMMARY.md:8:  - "Project-asset distribution (P01) — packaging/bundle/manifest.yml project_assets schema with read-project-assets.sh shared reader + install-asset-mode.sh per-mode handler (copy + symlink + windows fail-closed) + install-collision-check.sh FR-22 dual-oracle hierarchy (tracking-file + MIT-006 bootstrapping + operator-owned); install-{claude-code,codex,cursor}.sh migrated to project_assets-driven staging with --asset-mode-override flag and cross-installer parity; .orchestrator/installed-files.txt per-asset mode tracking (FR-4 + tab-delimited column-1 invariant); SC-1/SC-2/SC-10 acceptance scripts. Wiki distribution + init integration (P02) — orchestrator:wiki-init command + scripts/lifecycle/wiki-init.sh (FR-5 default-scope canonical implementation) + wiki/mkdocs.yml four-field placeholder amendment + FR-6 self-application loop closed against orchestrator repo + sequential-atomicity dispatch of wiki-init.sh per FR-11/MIT-011; commands/init.md --with-wiki/--with-giscus/--deploy documentation + init-project.sh composition validation + passthrough; wiki/glossary.md US-6 path convention with three populated entries; scripts/wiki/wiki-scan-sources.sh --include-glossary additive flag + scripts/wiki/wiki-generate-nav.sh + scripts/wiki/wiki-generate-stubs.sh top:glossary routing; scripts/knowledge/lookup-mems.sh --kind=glossary READER adapter (M020 schema-authority preserved + M031 Quick/Standard/Full profile contract honored + MIT-010 safe-default-no-terms fallback); SC-3/SC-7 acceptance scripts; tests/paired-m032-m033/seam-{A,B,C}.sh paired-launch contracts shared with M033. Wiki composition layer (P03) — FR-7 Giscus partial templating (wiki/overrides/partials/comments.html dual-template interpolation) + --with-giscus scope on wiki-init.sh; FR-9/MIT-007/MIT-008 --deploy scope on wiki-init.sh (four-step ordered remote-state mutation: gh api PATCH discussions=true -> wiki-deploy.sh -> MIT-007 read-before-write Pages guard -> gh api PUT /pages); FR-10 cwd-vs-repo_url sanity gate-0 on wiki-deploy.sh (Finding J counter-pattern); structured wiki-deploy-mutation NDJSON audit-trail BEFORE side-effect-print (Constitution VI applied to remote-state mutations); M032_DEPLOY_GH_API_STUB[_DIR] + M032_WIKI_DEPLOY_BYPASS_CWD_GATE test-only stub envelopes per M026/MEM030; FR-14 region split on wiki-generate-nav.sh (auto-nav regenerated wholly + custom-nav preserved verbatim + legacy M012-P01 markers recognized only for one-time migration); FR-13 progressive-opt-in --with-<feature> flag pattern documented at references/installation.md (default-off + independently composable + reversibility); AD-7/CON-5 throwaway-fixture-protocol document with timestamp-prefix naming + gh repo create --private --add-readme creation contract + trap-EXIT teardown + four no-orphan-state invariants + recovery-on-partial-failure runbook; SC-4/SC-5/SC-6 acceptance scripts (SC-5 with three-category exit semantics 0/77/non-zero per MIT-001). Scanner extensions + decorator + acceptance battery + milestone close (P04) — FR-17/18/19 scanner extensions on scripts/wiki/wiki-scan-sources.sh (proposals:* records with [stage] badge derivation; extra:<dirname> records driven by wiki.extra_dirs config; knowledge-flat records on .orchestrator/knowledge/*.md flat files); FR-17/18/19 nav rendering in scripts/wiki/wiki-generate-nav.sh under # >>> auto-nav region; FR-20 build-time code-shorthand decorator stub scripts/wiki/wiki-decorate-codes.sh (US-8 P3 surface); SC-8/SC-9/SC-11 acceptance scripts; SC-12 milestone-grain acceptance battery tests/m032-acceptance/run-acceptance-battery.sh (MIT-001 three-category exit semantics); --with-wiki no-op in-flight repair on scripts/lifecycle/wiki-init.sh (FR-11 passthrough symmetry); milestone-close ceremony surfaces (M032-SUMMARY.md + conditional M032-VALIDATED marker per MIT-001 + SC-14 + milestone-grain unit_close JSONL record + M032-ACCEPTANCE-EVIDENCE.md per the M030/M031 evidence-ledger convention); twenty-eight tools/verify/m032-p0[1-4]-* shape verifiers + four phase-suite aggregators + four scope-guards + four baseline-ref fixtures."
  - milestone-summaries :: .orchestrator/milestones/M035/M035-SUMMARY.md:113:- Decisions-block as D-RN-N heading cohort (one `### heading per decision in a contiguous block; anchors run in numeric sequence `dr-code-NNN..`).
  - milestone-summaries :: .orchestrator/milestones/M035/M035-SUMMARY.md:18:  - "BATTERY-line-shape-convention (BATTERY: pass=N fail=N skip=M canonical milestone-grain rollup mirrors m029/m030/m032/m037/m035-p015 enabling consolidate-time grep aggregation across milestone batteries); summing-counters phase-suite form (right shape when child verifiers have heterogeneous BATTERY counts; established at P05 T06 reused at P06 T06; M035 P02/P03/P04 used the chain-the-children form because their counts were homogeneous); chain-the-children phase-suite form (per-verifier-PASS counting form; right shape for homogeneous BATTERY counts); load-bearing-default-OFF test-mode env vars (M035_P04_LOCAL_TARBALL + STAGE_ONLY + STAGE_DIR + COSIGN_AVAILABLE + M035_P05_LIVE_RELEASE_DIR — production path byte-identical when env vars unset; tests gate test-mode behavior via explicit env var; mirrors M030 P03 ORCHESTRATOR_TIER2_LIVE convention); single-source-of-truth-tarball-cross-channel-binding (D007 — homebrew formula references the npm pack tarball uploaded by P02's npm-publish job + P05's signing pass; install.sh fetches the same tarball via curl-pipe-bash; same bits same hash same staged tree modulo channel-specific exclusions); CON-6-defense-in-depth (workflow uses two layers — structural job-condition AND runtime negative-assertion step asserting NPM_TOKEN/HOMEBREW_TAP_TOKEN absent in PR context — either layer load-bearing; both together survive single misconfig); regression-guard-via-negative-grep (verifier needle grep -qE 'secrets\\.(CURL|INSTALL_SH|PIPE_BASH)' failing-if-matched — catches future authors accidentally introducing a curl-channel secret); test-IS-the-smoke-functional-verifier (run the test end-to-end with stdout captured to a tempfile + grep the captured log for canonical PASS message; AP-009-safe single-script shape); 3-way-supersedes-2-way-with-fallback-preservation (cross-channel byte-equivalence assertion ladders gracefully when arms are unavailable — 3-way → 2-way → SKIP); two-H2-section-append-at-EOF (consumer-facing ## Installing via X + operator-facing ## Releasing via X — symmetric across npm/homebrew/curl-pipe-bash channels); inline D-number rationale blocks in operator prose (operators reading installation.md don't need to context-switch to DECISIONS.md; structurally enforced by verifier grepping for literal D-numbers); MOS-numbered operator procedures (MOS-1..MOS-5 named callouts in installation.md; gives operators stable references at launch-rehearsal time); jq-free version read from package.json (grep -E + head -1 + sed -E pipeline; bash 3.2 compatible no toolchain dependency); REPO_ROOT defensive fallback in staged probes (run-probe.sh does not export REPO_ROOT; probes use parameter-expansion fallback for portability); BSD-grep-safe flag pattern (grep -qF -- '--flag' for patterns starting with --); BSD-sed-portability via POSIX character classes ([[:space:]] over \\s); sigstore-keyless-OIDC-primary plus shasum fallback (eliminates GPG private-key blast-radius; binds signing identity to GHA OIDC token via canonical workflow-file URL); sign-the-checksum-file-itself + LC_ALL=C-sort for locale-independent SHA256SUMS; pinned-cosign-action AND pinned-cosign-release for dependency immutability (Constitution Principle XVI); rollback-state-capture-at-upgrade-time (decouples rollback from source-repo reachability); job-level permissions override preserves workflow-level least-privilege; release-artifacts-glob-loop-with-case-continue-skip; per-subdir .npmignore is load-bearing dogfood-exclusion layer when package.json files: whitelist is present (root .npmignore is largely subordinate); contract-via-grep-of-source-not-runtime (bundle-hygiene-filter verifier asserts build-bundle.sh CARRIES the filter logic via grep on source rather than running the filter — works regardless of bundle dir state); real-tarball-as-real-bundle-smoke (npm-pack-contents verifier runs actual npm pack and inspects the tarball — Plan-Time Discipline Rule 5 analog); upstream-verifier-regression-discipline (additive workflow edits re-run all pre-existing release.yml verifiers post-edit to confirm no contract regression); job-level timeout-minutes with CON-comment cross-reference; needs-npm-publish-as-tarball-availability-gate; user-facing-flag-promoted-from-test-only-alias-without-deprecating-alias (M035 P01 — --mode supersedes --asset-mode-override at the surface; alias preserved byte-identically); symlink-target-equals-src-abs (link_target=SRC retires the M032/P01 11-line runtime-cache resolution block); -L/-f conditional uninstall (symlink branch tests -L only; copy branch tests -f only); read-only drift helper exits 0 always (FR-15 — consumers branch on data not exit code); decisions-block-as-D-RN-N-heading-cohort (one ### heading per decision in contiguous block + anchors run in numeric sequence dr-code-NNN); D-row heading-shape continuity (### D### — title heading shape preserved across D004–D014; anchor-shape vs heading-shape divergence with D-RN-* remains a known paper-cut); detect-by-install-method-first as AD-5 canonical pattern (read-only bounded probes in deterministic order; persist detected value back to config); 5-condition JSONL suppression matrix verbatim from M027 (FR-16 inheritance discipline); SKIP-for-missing-sub-aggregators in milestone-grain battery (allows phased shipping); self-reference for SC-15 (acceptance-battery-shape verifier in the chain; verifier asserts battery shape not its own result; no circularity); milestone-close ordering load-bearing (battery PASS → validate-milestone PASS → write SUMMARY → write VALIDATED → emit unit_close → re-run milestone-close-shape verifier — inverting falsely advertises closure)"
  - milestone-summaries :: .orchestrator/milestones/M037/M037-SUMMARY.md:16:  - "FR-1,FR-5,FR-6,FR-7,FR-8,FR-9,FR-10,FR-11,FR-18,FR-19,FR-20,FR-21,FR-22a,FR-22b,US-10,US-11,US-12,US-13,US-14,SC-13,SC-14,SC-15,SC-16,SC-17,Q-6,F8,F9-superseded,F10,F11,F12,F13,B5,MIT-01,MIT-02,MIT-03,AD-19,CON-2,CON-3,CON-4,P03-de-facto-narrowing,FR-12-deferred,FR-13-partial,FR-14-deferred,FR-15-deferred,FR-16-deferred,FR-17-deferred,navigation.sections-dropped,yaml-merge-list-element-gap-deferred"
  - milestone-summaries :: .orchestrator/milestones/M041/M041-SUMMARY.md:16:  - "FR-1,FR-2,FR-3,FR-4,FR-5,FR-6,FR-7,FR-8,FR-9,FR-10,SC-1,SC-2,SC-3,SC-4,SC-5,SC-6,SC-7,NG-1,NG-2,CON-1,CON-2,CON-3,CON-4,conversus-gate-3-P0-amendments(RISK-02/03/04),RISK-01-false-positive(constitution-has-15-principles-not-7),#Q-1-corpus-validation-advisory,#Q-5-partial-failure-sequence,#Q-6-ORCHESTRATOR_ROOT-prefix-disambiguation,review-B1-through-B8,depth-tracked-json-parser,glob-expansion-disabled,title-json-escape,empty-root-no-false-positive,FR-8-selective-firing-NG-1-discipline,doctor-specific-symptom-B5,FR-9-gate-wraps-mock-and-live"
  - milestone-summaries :: .orchestrator/milestones/M043/M043-SUMMARY.md:105:`#Q-6` API assumptions remain doc-derived until the US-4 live pass confirms or
  - milestone-summaries :: (capped at 5 hits — more may exist)

### Q7 — KEPT

- **question**: - #Q-7 (wall-clock-default): Is the wall-clock ceiling a fixed internal non-configurable constant (what value?) or a future operator-facing flag?
- **disposition**: kept (operator decision at plan-phase (gate-result RISK-6); kept at discuss gate 2026-07-02)
- **terms** (12): Q-7, wall, clock, default, ceiling, fixed, internal, configurable, constant, value, future, operator
- **hits**:
  - constitution :: .orchestrator/memory/constitution.md:130:  cheapest pipeline available*. A future execution path that
  - constitution :: .orchestrator/memory/constitution.md:234:- Each task that generates good documentation makes every future
  - constitution :: .orchestrator/memory/constitution.md:254:"for future use" or "just in case" violates Context Minimization
  - constitution :: .orchestrator/memory/constitution.md:255:(Principle I) by consuming context budget without delivering value.
  - constitution :: .orchestrator/memory/constitution.md:316:  (task > phase > milestone > default).
  - constitution :: .orchestrator/memory/constitution.md:32:      templates/orchestrator-config-default.yml (41 leaves, baseline 0
  - constitution :: .orchestrator/memory/constitution.md:348:- Hook timeout is enforced (default 30s). Hooks that exceed timeout
  - constitution :: .orchestrator/memory/constitution.md:75:    - Operator-routed Path 1 (accept headline PASS, defer blind's
  - decisions :: .orchestrator/DECISIONS.md:141:- **Choice**: Replace the D008 phase outline's "P02 dispatch compressor (optional filter...)" framing with a tier-ladder model informed by Article 3 (Rohit's Claude Code harness teardown) and logged in `.orchestrator/scratch/articles-synthesis-2026-04-17.md`. Every dispatch runs through tiers in order, cheapest first: **Tier 1 microcompact** (reuse cached tool-call results that haven't changed + persist oversized tool results to disk with `file_path + preview` references — absorbs L6 tool-result budgeting; zero LLM cost); **Tier 2 snip** (head-drop with protected tail; no LLM call); **Tier 3 auto-compact** (summarize via LLM call; existing caveman-style territory); **Tier 4 context collapse** (staged multi-phase compression of tool results → reasoning → sections; feature-flagged). Intensity maps to tier ceiling: Quick→T1–T2 only, Standard→through T3, Full→all tiers available. Revised phase outline: P01 tier grammar + per-tier safety boundaries (what's preservable per tier), P02 Tier 1 microcompact + tool-result budgeting, P03 Tier 2 snip with protected-tail semantics, P04 Tier 3 auto-compact (summarization, intensity-gated), P05 Tier 4 collapse (staged; feature-flagged, off by default), P06 eval harness (3-arm parity tests per tier), P07 multi-runtime parity. D008's original scope envelope (roundtrip-safe by policy, grammar-aware preservation, intensity-bound, explicit opt-out surfaces, telemetry-integrated) carries forward unchanged — only the internal staging model is reframed. Fresh `orchestrator:discuss` at M018 kickoff addresses detailed tier design once M019 Tier 1 data reveals which artifacts dominate token spend. **Conversus integration (locked in per D007 reuse pattern)**: M018/P01 tier-grammar + per-tier-safety-boundary work invokes `scripts/dispatch/adapters/tool/conversus.sh` via `role/red-team` + `domain/security` presets at plan-review time. Rationale: grammar decisions carry parse-regression risk across downstream scripts that grep prose (the risk D008 originally flagged), which is exactly the subjective-quality territory the conversus fidelity-gate adapter earns its cost on. A new preset drops under `templates/conversus-presets/` without adapter modification (same pattern M013/M014 use). Gate is intensity-bound by default (Quick=off, Standard=advisory, Full=blocking); `--no-review` escape available.
  - decisions :: .orchestrator/DECISIONS.md:15:- **Choice**: P06 scope expanded by three engine-layer workarounds discovered during P03 implementation: (1) literal-audit-marker printf pattern added after SAFETY_WARNING emissions in scripts/engine/run.sh (T03/T04) because events.sh _orch_events_quote does not quote single-word values, breaking must-have greps that expect quoted form; (2) cd $REPO_ROOT subshell wrapper around check-must-haves.sh and record-result.sh calls in scripts/engine/run.sh (T05) working around check-must-haves.sh PROJECT_ROOT walk-up bug that computes PROJECT_ROOT as .specify/orchestrator/milestones/<m> instead of repo root; (3) execution-log.jsonl entries from engine dispatch do not thread ORCH_RUN_ID because record-result.sh does not currently accept/emit run_id. P06 cleanup should: fix check-must-haves.sh PROJECT_ROOT detection, extend events.sh _orch_events_quote to always quote values, extend record-result.sh to thread run_id from env, then remove all three workarounds from run.sh.
  - decisions :: .orchestrator/DECISIONS.md:18:No downstream phases (P05, P07) invalidated — P05 consumes run.sh as an opaque coordinator, not its internal workaround patterns; P07 has no dependency on these items. P06 was already medium-risk cleanup of engine-managed scripts; these additions fit within its original scope boundary. No re-planning required.
  - decisions :: .orchestrator/DECISIONS.md:197:- **Choice**: Narrow M013 scope per `specs/023-github-native-integration/conversus/` run: (1) FR-12 handler installation is Claude-Code-only for v1 (CV-3 / RD-2 binding ruling) — Codex CLI + Cursor fall back to `manual` sync at init, non-Claude-Code runtime-adapter work deferred to a future milestone; (2) Minimal Slice (CV-1 / RD-3 binding ruling) pins Phase 1 load-bearing scope to full US-3 + minimal US-1 scaffolding (sidecar config + UAT template install) + US-2 idempotency applied to UAT entries only; (3) M020 sequencing unchanged (RD-1 reclassified) — M013 -> M014 -> M020 per D013; schema authority bounded by MIT-2 narrowing FR-9 to additive-emit-only with chunk-ID pinned to existing `SPEC-*` frontmatter + new Knowledge-Layer Boundary subsection explicitly forbidding review-state / query-surface / clustering; (4) 13 unmitigated P5 risks landed as MIT-1..MIT-13 spec edits (sub-issue fallback Constraint, Constitution Check section, FR-5 three-shape rewrite, FR-6 per-item cache schema, FR-7 sync lock, FR-9 narrowing, FR-12 prose correction + scope, FR-13 `--strict` provenance + 30s timeout + drop intensity-engine coupling, FR-14 re-init via marker search + label-collision preflight, FR-15 dry-run generalized to init+sync, FR-16 rate-limit + auth-expiry detection, FR-17 cost emission, FR-18 cache reconciliation, plus SC-8/SC-9 reconciliation and new SC-12/SC-13 scope + overhead caps); Open Questions #1/#3/#6/#7 closed; new OQ on PAT-vs-App default for M009. Spec status promoted Draft -> Ready-for-discuss.
  - decisions :: .orchestrator/DECISIONS.md:214:Operator request post-P01 completion; mechanical rather than conversus-gated decision. P01 artifact prose (task summaries + PLAN files) contains forward references to the old P03 numbering — those references now point to what is P04 and are historical. DECISIONS.md is authoritative.
  - decisions :: .orchestrator/DECISIONS.md:225:- **Choice**: Revised sequence: **M013 (P04 remaining) → M014 (extended) → M020 → M024 → M019 Tier 2+3 → M018 → M023 → M009 (extended) → M010 (adjusted)**. (1) **M014 scope extension**: port spec-kit's `specify` shape as native `orchestrator:specify` (CC-first, written portably, treating spec-kit template as the I/O contract rather than a verbatim port); add conversus-suggestion logic so complex/controversial specs auto-propose red-blue pressure-test with optional spec-decomposition-before-conversus for very large specs; dual-write `AGENTS.md` alongside `CLAUDE.md` so Codex stays fed without a separate milestone. (2) **M024 Universal Intake** (new): extend `orchestrator:evaluate` to input-agnostic (idea, paragraph, fragment, full spec, or empty + Q&A) and emit a reviewable proposal artifact at `.orchestrator/intake/<id>/proposal.md` covering six axes (input shape, scope tier, decomposition, design gate, conversus gate, intensity); degenerate fast-path auto-proceeds if Tier A + Quick + no conversus + no design, approval-gated otherwise. Positioned after M020 so we dogfood it through remaining milestones. Design gate degrades gracefully pre-M023 ("design walkthrough lands in M023; author DESIGN.md manually or skip"). (3) **M023 Design Layer** (new): `orchestrator:design` command — conversus spawns N design-personality agents in parallel, each produces a DESIGN.md draft + working coded prototype in user's stack; side-by-side comparison; user picks/arbitrates; canonical DESIGN.md lands at repo root and auto-injects into payloads for ui-tagged phases. Renderer adapter interface shaped as MCP clients (runtime-agnostic; Stitch/v0/Figma/etc. consumed as they expose MCP). Stage-1-only scope — high-fidelity stakeholder-mockup expansion (Stitch/Claude-design rendering) deferred until user demand earns it. Positioned pre-M009 because orchestrator has no internal UI to dogfood against — landing earlier buys zero learning. (4) **M009 scope extension**: add runtime-parity audit as pre-launch gate — dogfood every `orchestrator:*` command end-to-end on CC + Codex + Cursor, consuming the `RUNTIME-ASSUMPTIONS.md` registry accumulated during M013–M018, fix parity bugs before external eyes. (5) **M010 posture adjustment**: ship with Managed Agents primary adapter + Codex Cloud **stubbed** adapter (proves abstraction holds without full feature parity); full Codex Cloud as demand-driven fast-follow rather than launch-critical. (6) **Ultraplan/ultrareview stance**: parked, not integrated — Claude-Code-only (requires CC on web + GitHub repo + Anthropic cloud; not available on Bedrock/Vertex/Foundry), forks multi-runtime UX. M013 GitHub integration delivers the same plan-review value universally via PR comments. Re-evaluate only if real CC-only user demand emerges. (7) **`RUNTIME-ASSUMPTIONS.md` discipline** (no milestone cost): a registry file tracking CC-specific shape assumptions as written (`settings.json` hook formats, CLAUDE.md-only writes, `claude` CLI invocation patterns). M009's parity audit consumes it as a punch-list rather than an open-ended investigation. Optional: fold a single Codex smoke-probe telemetry point into M019 T2+3's emitter for drift detection.
  - decisions :: .orchestrator/DECISIONS.md:228:User is dogfooding CC-exclusively right now; Codex parity is a launch concern, not a dogfood concern. Back-loading Codex validation to M009 is constitutionally cleaner (XIV — don't build Codex plumbing speculatively before CC usage teaches us what needs to be portable) and keeps the current dogfooding loop fast. M023/M024 are the adoption layer — M024 ships where its internal dogfood signal is cheapest (post-M020, exercised on every remaining milestone), M023 ships where its external signal is cheapest (pre-launch, since this repo has no UI to dogfood design against). MCP-as-adapter for design renderers means M023 is runtime-agnostic by construction (CC + Codex + Cursor all support MCP). Extending `evaluate` rather than adding a new `orchestrator:start` command keeps the surface small (XIV). Proposal-as-artifact gates the router on Principle III (Design Before Code). The `RUNTIME-ASSUMPTIONS.md` discipline is a lightweight hygiene practice — a few lines of markdown per observation — preventing "CC-first" from silently becoming "CC-only." Supersedes the forward-sequence framing from D004/D005/D006/D008/D013 where M013→M014→M020→M019 T2+3→M018→M009→M010 was the last committed order.
  - decisions :: .orchestrator/DECISIONS.md:239:- **Choice**: M014 pressure-tested pre-discuss via cooperative conversus deliberation (Tier 1; 2 agents — `spec-author` + `devils-advocate`; 1 round; no arbiter). 14 MITs applied pre-discuss covering: (P1) Phase Sequencing table as spec-layer subsection after Minimal Slice; SC-2 tightened to mechanical stdout/exit-code/stderr-grep assertions; FR-2b template SSOT (`templates/spec-template.md`) + FR-18 byte-compatibility fixture test (`tests/test-specify-shape.sh`) + SC-17; US-5 promoted to independent P2 user story with scope-boundary sentence ("US-5 is the only path that edits `specs/<NNN>-<slug>/spec.md` from a comment"); Constitution XV retrofit at SC-5 + Constitution Check section → III (primary) + XIV (supporting), with Constitution Check XV paragraph split into "applies to / does NOT apply to" sub-contexts (FR-12 + FR-14 byte-preservation retained; spec-amendment human-gate retargeted); (P2) SC-4 rewritten as two-branch construction (default: measurement + re-planning trigger; upgrade branch: pinned-shape gate with failure-posture clause); FR-14 three-case semantics (all-placeholder / partial / fully-authored) + SC-14 byte-preservation invariant; SC-6a outside-markers `shasum` invariant + `tests/test-dual-write-outside-invariant.sh`; FR-19 `--dry-run` JSONL manifest format pinned M014-local only (M013 retrofit deferred); SC-15 `RUNTIME-ASSUMPTIONS.md` close-out deliverable; SC-16 dogfood-data sizing for FR-9 classifier-shape decision; (P3) FR-7 interim→M024 migration handshake committed as one-line rule; Open Question #C-11 exhaustive CLAUDE.md write-site enumeration at planning time. Spec frontmatter promoted `Status: Draft` → `Status: Ready-for-discuss`; `Last Revised: 2026-04-22` line added matching M013 precedent. P3 Dependencies prose rewrite skipped as subsumed by Phase Sequencing table (P1 #1). Two Phase 4 disputes (SC-4 default branch; Phase Sequencing table layer) resolved via synthesizer-recommended compromises: SC-4 adopts measurement default with upgrade-branch failure-posture; table lands spec-layer.
  - decisions :: .orchestrator/DECISIONS.md:253:- **Choice**: P03 external preflight not met (M012/P04 DEPLOY-RECORD has 3 pending sentinels + inbox-dogfood data requires ≥1 week real capture). Auto-dispatch redirected to P02/P04 for this run per roadmap guidance; P03 queues until operator completes wiki deploy and seeds inbox data
  - decisions :: .orchestrator/DECISIONS.md:256:Roadmap explicitly predicts P02+P04 closing before P03; preflight is operator-gated and cannot be resolved autonomously
  - decisions :: .orchestrator/DECISIONS.md:267:- **Choice**: (1) **Three-pass contract** pinned in `commands/specify.md`: Pass 1 (scaffold) always runs — creates file, populates frontmatter + Input, writes Section Contract `<TODO:` placeholders. Pass 2 (author) drafts the body from Input + DECISIONS.md + constitution + neighboring CLAUDE.md/AGENTS.md Recent Changes; Quick = agent-only no review, Standard = agent + self-review checklist warnings, Full = agent + `speckit.clarify` loop (≤5 load-bearing ambiguity questions, operator-answered, agent-revised). Pass 3 (gate) adversarially reviews authored body via `scripts/dispatch/adapters/tool/conversus.sh gate spec-pressure-test`; Quick = skip, Standard = advisory (findings folded into Open Questions, no BLOCK), Full = strict (BLOCK halts with dispute list + "revise then --amend" message). On Full+PASS, spec status promoted `Draft` → `Ready-for-discuss` + `Last Revised:` line added per M013/M014 precedent. (2) **Intensity resolution order**: project default from `.orchestrator/config.yml` → smell-test escalation via `scripts/engine/intensity-analyze.sh --description <prose>` (can only escalate, never de-escalate) → CLI `--intensity` override (trumps both). One `specify_intensity_resolution` JSONL record emitted at run start. (3) **Gate TODO pre-flight invariant** (universal): `scripts/dispatch/adapters/tool/conversus.sh gate` refuses any artifact containing ≥ `CONVERSUS_GATE_TODO_THRESHOLD` (default 1) `<TODO:` markers, with actionable error pointing at the author pass. Bypass: `CONVERSUS_GATE_SKIP_TODO_CHECK=1` (reserved for tests + intentional-stub preset authoring). Protects M013/M014/M023/M024 + any future gate consumer, not just `orchestrator:specify`. Applied to adapter in `scripts/dispatch/adapters/tool/conversus.sh:150-169` + regression test in `tests/test-conversus-adapter-shim.sh` section 1b. (4) **Reuse over rebuild**: all intensity machinery already exists (`intensity-analyze.sh`, `intensity-override.sh`, `intensity-gate.sh`, `spec-complexity-probe.sh`) — D019 wires them into the specify flow rather than adding parallel infrastructure. (5) **Implementation posture**: command contract is the SSOT (landed 2026-04-23 in `commands/specify.md`); shell implementation in `scripts/specify/specify.sh` lands in the next M014 extended phase. Until then, agents invoking the command execute passes 2 and 3 manually per the doc.
  - decisions :: .orchestrator/DECISIONS.md:270:Dogfooding M014/P01 on M020 (spec 025) surfaced a silent dead zone: the shipped `orchestrator:specify` scaffolds TODOs but does not author, and the pressure-test gate runs probe-and-prompt against whatever body exists. On a freshly scaffolded spec, the probe reads below-threshold (no FRs, no user stories, just TODOs) and the gate is skipped — so the gate's adversarial value is never exercised on the first draft where it matters most. The pressure-test only earns its cost against authored content; running it against placeholders produces nonsense findings, running it too late means authoring defects propagate into evaluate/discuss/roadmap before anyone catches them. Intensity-scaling the three passes respects the "right tool for the right job" ethos already pervading the orchestrator (Quick trusts the agent + skips the gate for low-risk specs; Standard adds a non-blocking safety net; Full invokes human judgment + strict gate where architecturally load-bearing work earns the cost). Smell-test-only-escalates honors operator intent (user setting Quick isn't silently upgraded to Full unless CLI override forces it) while protecting against accidental under-classification (an architecturally heavy spec at project-default Quick gets recognized and bumped). The gate TODO pre-flight is a universal invariant rather than a specify-only check so every future gate consumer (M013 UAT gate, M023 design gate, M024 routing gate) gets the protection without each one reimplementing it. Spec 025 also surfaced two upstream conversus bugs (claude-code false-fail; anthropic parallel-429) captured separately in `specs/025-knowledge-layer-maturation/conversus/PRESSURE-TEST-FINDINGS.md` + handoff in `CONVERSUS-PR-HANDOFF.md` — orthogonal to D019's contract work.
  - decisions :: .orchestrator/DECISIONS.md:284:Both frictions surfaced real this session and caused workarounds rather than clean flows. Recording them immediately prevents re-discovery cost in future specify runs. Rule of thumb: if a workaround was needed, a D-row or note lands before the session ends or the lesson evaporates with context.
  - decisions :: .orchestrator/DECISIONS.md:29:- **Choice**: P06 scope expanded by five additional items discovered during P05 dispatch refactors: (1) scripts/lib/run-context.sh _orch_run_nonce SIGPIPE under set -o pipefail — causes scripts/engine/run.sh --dry-run to exit 141 when ORCH_RUN_SEED is unset; worked around in T02/T03/T04 with set +o pipefail wrapper around init_run_context (3 replicated call sites to drop after P06 fix). (2) scripts/dispatch/lib/section-handlers.sh handle_phase_summaries has a Bash 3.2 while-read bug with unterminated dep-list temp file — worked around by local _bc_handle_phase_summaries_fixed shim in build-context.sh (to drop after P06 fix). (3) templates/context-recipe.yaml default section order/priority values don't match pre-refactor dispatch-prompt parity — worked around by _bc_display_order/_bc_display_name/_bc_display_priority shim in build-context.sh (to drop after P06 recipe re-authoring). (4) scripts/verify/check-must-haves.sh doesn't discover tasks/TNN-PLAN.md files — all P05 tasks had to verify must-haves manually from payloads rather than via the canonical entrypoint. (5) scripts/dispatch/build-context.sh all emit_event calls are >/dev/null 2>&1
  - decisions :: .orchestrator/DECISIONS.md:296:- **Revisable**: Yes — (a) If reinit free-form-prose loss bites a second user, promote 5a to a dedicated milestone or M014-extended task. (b) Stage bloat 5b can be addressed inside the installer without a D-row (filter in `install-claude-code.sh` Stage 4.5 `find` expression). (c) If `check-settings-state.sh` silent regen failure reproduces, open a spec and a bugfix commit. (d) Upstream-removal 5d can be fixed via manifest-diff in a future install pass. (e) If the four dogfood-bugfix commits should have been a single "fix(dogfood)" squashed commit rather than a 4-commit split, revisit commit-message-style for future hotfix batches — current split prioritizes archaeology readability.
  - decisions :: .orchestrator/DECISIONS.md:497:| D001 | M035/P02 | convention | CI runner platform for npm publishing pipeline (#Q-7) | ubuntu-latest | npm tarball assembly and publish are shell+node-only; no macOS-only surface in P02. Cross-channel byte-equivalence test will gain macos-latest matrix when P03 (homebrew) lands. ubuntu-latest is fastest, cheapest, and aligned with CON-2 which permits bash 4+ in CI scripts. | No |
  - decisions :: .orchestrator/DECISIONS.md:60:Anthropic Managed Agents is not yet broadly available, so M010 value is externally gated. Meanwhile M011 (spec ingest), M012 (wiki), M013 (GitHub native integration), and M014 (comment automation) unlock the team's end-to-end spec→review→work loop, which is blocking current usability. M010's only technical prerequisite (M008 P03 dispatch adapter) is already satisfied, so order can be revisited without rework.
  - decisions :: .orchestrator/DECISIONS.md:623:3. **CON-8 escalation clause is the safety net.** If wall-clock
  - decisions :: .orchestrator/DECISIONS.md:74:M016 is prerequisite for credible launch narrative — autonomous mode must actually work autonomously. M017 adds multi-agent deliberation as an opt-in quality gate, informed by the autoreason paper's generation-evaluation gap findings. Positioned before M011-M014 because spec management milestones involve subjective quality judgments where Conversus provides the most value. M017 is opt-in with no hard dependency from M011, so it can be deferred without rework cost.
  - decisions :: .orchestrator/DECISIONS.md:88:Team needs to dogfood end-to-end spec→wiki→GitHub workflow before producing external-facing launch artifacts. Dogfooding M011–M014 will surface the rough edges that M009 docs/examples need to document. Producing launch materials before internal usage would mean rewriting them after discovering gaps. M017 repositioned after the spec management block since those milestones will reveal whether deliberation adds value at spec-quality judgment points. Supersedes positioning from D004/D005.
  - decisions :: .orchestrator/DECISIONS.md:99:- **Choice**: (1) Remove M017 (Conversus Deliberation Gate) from the forward milestone sequence. Revised sequence: **M011 → M012 → M013 → M014 → M009 → M010**. (2) Add M011/P07 (Format-Agnostic Intake & Conversus Fidelity Gate) which lands both the LLM-driven normalizer (any markdown → spec-kit shape) and the reusable `scripts/dispatch/adapters/tool/conversus.sh` adapter. (3) The adapter becomes shared infrastructure: later milestones that want deliberation at specific checkpoints (M013 UAT PR gate, M014 ambiguous-comment classification, roadmap phase-decomposition at Full intensity) invoke it from within their own scope, rather than waiting on a dedicated milestone. (4) Intensity engine owns the policy (when gates fire by default); users own opt-in/out via `--review` / `--no-review`.
  - decisions :: (capped at 5 hits — more may exist)
  - knowledge-entries :: (capped at 5 hits — more may exist)
  - knowledge-entries :: knowledge/conventions/MEM013.md:19:YAML frontmatter with `schema_version` + `type` fields. Body uses `{{placeholder}}` syntax for dynamic values. No hardcoded milestone/phase/task IDs (context-free per FR-074).
  - knowledge-entries :: knowledge/conventions/MEM014.md:27:**Templates -> Output**: Commands use `templates/*.md` as starting points. Agent fills `{{placeholder}}` values. Template `schema_version` field enables future format migration.
  - knowledge-entries :: knowledge/conventions/MEM015.md:22:DOCTOR:<CHECK> status=<ok|warn|skip|drift|missing> key=value ...
  - knowledge-entries :: knowledge/conventions/MEM017.md:19:`scripts/lib/verdicts.sh` provides `emit_verdict`, `parse_verdict`, `orch_is_verdict` with four constants: PASS, BLOCK, WARN, NEEDS_REVIEW. `hooks.sh` captures hook stdout, parses VERDICT lines, resolves multiple verdicts to most severe, and maps to block/warn/continue behavior. Backward compatible when no VERDICT present. Provider convention documented in `references/provider-convention.md`.
  - knowledge-entries :: knowledge/conventions/MEM030.md:17:# MEM030: `<TOOL>_EDITION=<value>` env-var convention for OSS-default escape hatches
  - knowledge-entries :: knowledge/conventions/MEM030.md:21:When an orchestrator integration flips its default from a paid build to an OSS build, the operator still needs a discoverable, undestructive way to reach the paid build for one-off invocations (debugging a paid-only feature, reproducing a paid-build regression, running a preset that depends on paid-only upstream plumbing). Three anti-patterns to avoid:
  - knowledge-entries :: knowledge/conventions/MEM030.md:23:1. **Path-only escape** — requiring the operator to set `<TOOL>_HOME=/explicit/path/to/paid/build` per invocation. Undiscoverable, easy to forget, and brittle across machines with different install paths.
  - knowledge-entries :: knowledge/conventions/MEM030.md:24:2. **Magic-value escape** — using a generic feature flag like `USE_PAID=1` or `LEGACY_MODE=1`. Doesn't express edition intent, doesn't compose with other build-channel distinctions, and is inconsistent across tools.
  - knowledge-entries :: knowledge/conventions/MEM030.md:29:1. **Naming**: `<TOOL>_EDITION` (uppercase tool name + literal `_EDITION` suffix). Examples: `CONVERSUS_EDITION`, `<NEWTOOL>_EDITION`. Reads naturally in shell history, in JSONL telemetry, and in operator-facing error messages.
  - knowledge-entries :: knowledge/conventions/MEM030.md:33:3. **Precedence**: env-var declaration is **primary**. Metadata-probe fallback is secondary. Operator-supplied absolute overrides (`<TOOL>_HOME`) trump both — they're an explicit "use exactly this binary" instruction. Resolver order from highest to lowest precedence: STUB (test-only) → PATH (`command -v`) → `<TOOL>_HOME` → `<TOOL>_EDITION`-aware user-local probe.
  - knowledge-entries :: knowledge/conventions/MEM030.md:35:4. **Diagnostic surface**: the `check` subcommand of the integration's adapter MUST emit `edition=<value> reason=<resolution-tag>` on stdout so the resolved edition is visible to the operator and to telemetry without a separate probe call.
  - knowledge-entries :: knowledge/conventions/MEM030.md:43:The M026 migration is the first OSS-default escape-hatch landing in this repo. Future migrations (e.g., when M010 ships and the orchestrator starts integrating with multiple LLM-provider editions, or when M023 ships and design-renderer adapters need to distinguish freemium tier vs paid tier) will face the same shape. Naming it as a convention now means the next migration can copy the pattern verbatim instead of re-deliberating the env-var name in each milestone.
  - knowledge-entries :: knowledge/conventions/MEM030.md:47:- **Env-var-name lint** (advisory): a future `scripts/diagnostics/check-edition-conventions.sh` could grep adapter-tree env-var references and flag any non-`<TOOL>_EDITION`-shaped escape-hatch names.
  - knowledge-entries :: knowledge/conventions/MEM031.md:27:| `graduated` | Reviewed and accepted; visible to the default query surface (FR-2).     |
  - knowledge-entries :: knowledge/conventions/MEM031.md:36:## Default semantics for pre-M020 entries
  - knowledge-entries :: knowledge/conventions/MEM031.md:54:  place. Compaction is deferred (NG-6); a future D-row will define
  - knowledge-entries :: knowledge/lessons/MEM026.md:21:Commit `d33b8a7` (M013/P04/T04) shipped a `scripts/dispatch/adapters/runtime/claude-code.sh --hook-config` emitter whose root object carried wrapper metadata (`runtime`, `hook_count`, `target_file`) alongside orchestrator-internal event names (`before_tasks`, `after_tasks`, `before_implement`, `after_implement`, `before_commit`, `post_verify`) that Claude Code does not recognize. The companion installer `packaging/install/install-claude-code.sh` wrote that invalid document unconditionally to `$HOME/.claude/settings.json`, overwriting any sibling tool's configuration (notably GSD-authored `statusLine`, `SessionStart`, `PostToolUse`, and `permissions` keys) with no merge path and no reversibility.
  - knowledge-entries :: knowledge/lessons/MEM026.md:25:Every P04 verification script — including `scripts/verify/m013-p04-post-verify-hook.sh` — ran against an empty `$HOME/.claude/` fixture. No gate seeded a pre-existing non-orchestrator `settings.json`, so the overwrite path was never exercised. The schema-validity failure was invisible because CC itself was not invoked against the emitted document in any P04 gate; the gates only asserted presence of the wrapper JSON's internal keys, not conformance to the CC `hooks` schema. The regression required a real user on a real multi-tool system to observe.
  - knowledge-entries :: knowledge/lessons/MEM028.md:43:Regression coverage: `tests/test-specify-number-allocator.sh` pre-seeds a scratch `specs/` with `008/009/024` sentinels and asserts the expected `009/010/025` outputs. Any future allocator that consumes zero-padded input belongs in that test's fixture matrix.
  - knowledge-entries :: knowledge/patterns/MEM001.md:23:Structured output: all scripts emit prefixed lines to stdout (`PASS:`, `FAIL:`, `LOCK:`, `STUCK:`, `BUDGET:`, `SUMMARY:`, `DECISION:`, `KNOWLEDGE:`, `ROLLBACK:`, `VALIDATE:`, `CONSOLIDATE:`). Errors to stderr. Exit 0 on success, 1 on failure.
  - knowledge-entries :: knowledge/patterns/MEM008.md:21:ISO 8601 Standardization: all timestamps use `date -u +%Y-%m-%dT%H:%M:%SZ` (UTC, ISO 8601). Rollback-phase.sh was using `date +%Y%m%dT%H%M%S` — fixed for consistency.
  - knowledge-entries :: knowledge/patterns/MEM009.md:19:Writing documentation and running every documented command catches drift that tests miss. M006 found and fixed a stale routing fallback value in references/file-formats.md through this process. The mechanical discipline of verify-as-you-write surfaces real bugs.
  - knowledge-entries :: knowledge/patterns/MEM029.md:40:- **Stderr/stdout discipline** (cross-cuts MEM015 DOCTOR Structured Output Protocol): structured fields go to stdout in fixed line order; warnings go to stderr; never cross-contaminate.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:10:- **Structured output**: All scripts emit prefixed lines to stdout (`PASS:`, `FAIL:`, `LOCK:`, `STUCK:`, `BUDGET:`, `SUMMARY:`, `DECISION:`, `KNOWLEDGE:`, `ROLLBACK:`, `VALIDATE:`, `CONSOLIDATE:`). Errors to stderr. Exit 0 on success, 1 on failure.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:106:- All timestamps use `date -u +%Y-%m-%dT%H:%M:%SZ` (UTC, ISO 8601). Rollback-phase.sh was using `date +%Y%m%dT%H%M%S` — fixed for consistency with lock-manager and recovery-briefing.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:128:- FR-045 (destructive operation warnings) is delegated to Claude Code's built-in safety checks for v0.1.0. Orchestrator-level detection deferred to future milestone.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:129:- **[milestone:M005]** [2026-04-12] DOCTOR: structured output protocol — all diagnostic scripts emit a single structured line (DOCTOR:<CHECK> status=<ok|warn|skip|drift|missing> key=value ...) for machine parsing by run-doctor.sh. Advisory checks (like check-plans.sh) always exit 0; non-advisory checks exit 0 on ok, 1 on warn. Established in P04, extended through P05-P07.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:134:- **[milestone:M005]** [2026-04-12] Gate verdict protocol — scripts/lib/verdicts.sh provides emit_verdict, parse_verdict, orch_is_verdict with four constants: PASS, BLOCK, WARN, NEEDS_REVIEW. hooks.sh captures hook stdout, parses VERDICT lines, resolves multiple verdicts to most severe, and maps to block/warn/continue behavior. Backward compatible when no VERDICT present. Provider convention documented in references/provider-convention.md. Established in P05 per AD-3.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:135:- **[milestone:M005]** [2026-04-12] Autonomy permission pipeline — generate-permissions.sh introspects project toolchain (package.json, Makefile, extension.yml, config files, agent host markers) and emits canonical JSON. write-permissions.sh translates to .claude/settings.json with additive merge for user-authored files. check-permissions.sh detects permission drift. Policy is declarative in autonomy-defaults.yaml read via recipe-parser.sh. AD-19 script-file verification shape: task plan Check: commands must use single-script invocations, not inline compound bash. Established in P07.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:142:- **[milestone:M006]** [2026-04-14] Documentation-as-verification (verify-as-you-write) surfaces real bugs: M006 found and fixed a stale routing fallback value in references/file-formats.md. The mechanical discipline of running every documented command catches drift that tests miss.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:147:- **[milestone:M008]** [2026-04-14] User-edit preservation via comment-delimited blocks: templates include <!-- BEGIN CUSTOM --> / <!-- END CUSTOM --> markers on their own lines. Regeneration scripts (reinit-handler.sh --mode update) extract the custom block via awk line matching and re-inject it into the regenerated file. Pair with field-level awk surgery for YAML/config (regenerate auto-detected top-level keys, preserve user-added keys). Default regeneration mode should be update (preserving), not reset.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:150:- **[milestone:M003]** [2026-04-15] Three-tier root resolution (flag → env var → resolve-root.sh fallback) is the standard CLI contract for orchestrator-root-consuming scripts. Makes scripts work under both ORCHESTRATOR_ROOT env and explicit --root, with config-driven default. Reference: scripts/orchestrator/status.sh.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:154:- **[milestone:M016]** [2026-04-16] write-summary.sh --completed_at is optional. Omit it to default to current UTC timestamp. Pass --completed_at=now as a sentinel. This eliminates the #1 source of Claude Code safety prompts (command substitution in timestamp generation). See AP-004 in ANTIPATTERNS.md.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:158:- **[milestone:M021]** [2026-04-17] Permanent fixture corpus with locked entry count: the 20-entry M011/P05-P07 prompt corpus is the authoritative regression surface — every entry traces to a specific screenshot (ID+SCREENSHOT+INPUT+EXPECTED_OUTCOME). Future hook triggers earn a new milestone, not a fixture append (constitution XIV; AD-5).
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:160:- **[milestone:M021]** [2026-04-17] MEM004 gate-internals carve-out: AD-19 single-script-file invocation shape constrains only agent-facing tool invocations. Verification-script INTERNALS may use pipes, command substitution, awk, etc. freely. The anti-pattern linter's scope rules (commands/, templates/, scripts/dispatch/lib/, tasks/*-PAYLOAD.md) enforce the distinction.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:169:- **[milestone:M019]** [2026-04-18] Opus 4.7 dispatch payload structure (M019/P00 L1-L5): every TASK-branch payload emits a First-Turn Completeness block (Intent/Constraints/Acceptance Criteria/Files To Touch, re-surfacing existing content, no new prose); stable sections before <dispatch-volatile> marker, volatile sections after, </dispatch-volatile> close; conditional Parallel Fan-Out directive only when recipe or task plan declares parallelizable work; intensity-gate.sh documents adaptive-thinking contract as a comment block (no fixed thinking_budget literals); expressive guidance rewritten positive with a narrow dotfile whitelist for Constitution XV retained-negatives.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:171:- **[milestone:M012]** [2026-04-21] Chained-gate deploy wrapper pattern: N sequential single-script-file gate invocations with first-non-zero-aborts semantics, fixed-verb per-gate stdout output (GATE/BUILD/DEPLOY/DRY-RUN/OK/FAIL) for deterministic downstream capture, and stderr-indent-on-FAIL for machine-readable triage. Four flags (--dry-run/--help/--root/--skip-smoke) cover the operator surface without a YAML driver or plugin system (Constitution XIV).
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:172:- **[milestone:M012]** [2026-04-21] Pending-sentinel artifact for operator-gated outcomes: when a phase artifact's completion requires human action outside auto-mode (e.g., live deploy, real credentials), ship a structured record with literal 'pending' values in the operator-supplied fields and 'skip' in gate-result fields. Verification gate accepts both pending-sentinel and live-populated shapes, so auto-mode advances while the human completes the live path during consolidation.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:178:- **[milestone:M013]** [2026-04-22] PATH-shimmed fake-gh enforces zero-subprocess-call invariant in CI: SC-7 auto-mode safety requires that 'github-init.sh' / 'github-sync.sh' make ZERO 'gh' calls when running without TTY + --i-am-operator. The verify gate prepends a tempdir containing a fake 'gh' executable to PATH that logs every invocation to a file, runs the command, then asserts the call log is empty. Catches accidental gh calls from new code paths immediately. Pattern extends to any shell-script invariant of the form 'must NOT invoke <external-binary>'.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:180:- **[milestone:M013]** [2026-04-22] External-watchdog-plus-adapter-internal-timeout (Constitution XII belt-and-suspenders): for opt-in external-tool gates (e.g., github-conversus-gate.sh wrapping the M011/P07 conversus adapter), wrap the adapter invocation in an outer 'kill -TERM at TIMEOUT seconds; sleep 1; kill -KILL at TIMEOUT+1' subshell EVEN IF the adapter has its own internal timeout. Defends against adapter timeout bugs and makes the worst-case CI runtime auditable from the wrapper alone.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:185:- **[milestone:M020]** [2026-04-25] Plan-deviation invariant for verifiers under auto-loop dispatch: any task whose plan names a verifier in its Verification section MUST author that verifier inline at task close, regardless of which downstream task originally owned it. Auto-loop --step=V parses the plan's Verification section per task with no cross-task ordering awareness — verifiers referenced by a future task will fail-not-yet-present at task close. Reusable across all auto-loop-driven phase plans (validated across M020/P04 T01/T02/T03).
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:186:- **[milestone:M020]** [2026-04-25] Deferred-resolution sentinel pattern for CLI-vs-preferences precedence in in-place script extensions: replace eager default initialization with empty-string sentinel + seen-on-cli flag (set to 1 inside the CLI arm); after the argparse loop and BEFORE existing closed-enum validation, run pref_resolve <key> only when the seen-on-cli flag is 0; net precedence becomes CLI > project preferences > user preferences > built-in default. Existing validation block runs against the resolved value as belt-and-suspenders defence (catches future helper bugs or vocabulary drift without modifying the validator). Stderr from pref_resolve must NOT be redirected to /dev/null because malformed-value WARN diagnostics are an operator-actionable signal.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:192:- **[milestone:M024]** [2026-04-26] Idempotent halt-and-flip cycle for operator-authored external artifacts: when a workflow needs the operator to author a file outside the orchestrator (e.g. DESIGN.md), the implementing script should (1) on first invoke, set a transient pending_* flag and halt with a pointer to the expected path; (2) on follow-up invokes, check for the artifact's existence — if missing, no-op and re-halt; if present, flip the transient flag off, set the terminal flag on, and require operator approval. Encoded as D025 closed-enum semantics in M024/P07.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:196:- **[milestone:M027]** [2026-04-27] Latency outer-vs-inner split (M027/P01/T04 → P02/T04 → P03/T04): for predictive/diagnostic latency verifiers, hard-fail at 250ms on the inner library measurement (with appropriate fast-path env-vars pre-set, bypassing inner re-forks). Outer wall-clock includes ~200ms bash startup + fork on macOS dev boxes — report informationally with WARN: RELAX-CANDIDATE annotation forwarded by the suite. Rationale: bash startup overhead is not part of the surface author's optimizable budget.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:217:- **[milestone:M033]** [2026-05-04] M033 closes -- warm conversational orchestrator:start front door with four onboarding branches; orchestrator-native constitution authoring (zero spec-kit dependency per Principle XVI); customblock-drafter strict-aggregation discipline (Constitution XV) with floor-not-ceiling preservation; --with-wiki / --with-github paired-launch passthroughs under MIT-001 two-mode test contract; SC-15 closed via US-8 AS-5 signed-attestation fallback (cold-start UX recruiting deferred to 2026-05-12 deadline)
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:36:- Body uses `{{placeholder}}` syntax for dynamic values
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:66:- Knowledge entries without scope tags included by default (project-level)
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:71:- `json_field()` strips trailing commas from bare numeric JSON values
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:98:Commands use `templates/*.md` as starting points. Agent fills `{{placeholder}}` values. Template `schema_version` field enables future format migration.
  - knowledge-md :: (capped at 5 hits — more may exist)
  - milestone-summaries :: .orchestrator/milestones/M003/M003-SUMMARY.md:12:  - "M009 (recipe packaging can assume .orchestrator/ default target); future milestone-management work"
  - milestone-summaries :: .orchestrator/milestones/M003/M003-SUMMARY.md:28:M003 Migration Tool milestone complete. Initial build (P01-P06, commit ad3da8a 2026-04-09) delivered the end-to-end migration pipeline: adapter interface with pluggable source formats (GSD2/GSD v1/spec-kit), per-artifact transforms (knowledge, decisions, requirements, milestones with tiering), and CLI with idempotency + reporting. After M007 (knowledge graph) and M008 (standalone multi-runtime + 5-rule state resolver) landed, a delta audit surfaced three drift points: hardcoded .specify/orchestrator/ paths, unpopulated graph database, single-layout idempotency probe. P07 refit wired migrate.sh to resolve-root.sh --absolute, dropped hardcoded paths in three transforms, added dual-root idempotency probing, wired rebuild-index.sh as final pipeline stage, and documented AD-13/14/15 in commands/migrate.md. P08 validated end-to-end via deterministic synthetic GSD2 fixture (primary) and live lakeledger (secondary, skip-gracefully), new scripts/orchestrator/status.sh CLI wrapper making the roadmap demo sentence literally executable, 8 AD-19-safe verify scripts, and a two-pass integration test — all green. Surfaced and fixed one latent P02 bug (missing content_hash frontmatter) during refit. Retroactive phase summaries added for P01-P06 (originally delivered before phase-summary machinery) based on roadmap Revision Note + commit ad3da8a. Migration pipeline now produces resolver-wired output, populates the M007 graph database, and participates in graph traversal — ready for M009 recipe packaging and M010 cloud dispatch.
  - milestone-summaries :: .orchestrator/milestones/M004/M004-SUMMARY.md:12:  - "Conversus integration (hook lifecycle points), future milestones (structured events, recipe-driven assembly)"
  - milestone-summaries :: .orchestrator/milestones/M006/M006-SUMMARY.md:28:M006 Documentation & Quality milestone complete. 6 phases, 22 tasks dispatched across 22 dispatch cycles. Produced: 8 new reference docs (architecture, engine, events, errors, hooks, recipes, routing, constitution-walkthrough), updated 1 existing reference (file-formats 802→1105 lines), 4 new user guides (getting-started, recipe-authoring, hook-development, knowledge-management), 1 contributor guide rewrite (AGENTS.md 48→323 lines), 1 new diagnostic (check-docs.sh, 19 files verified), CHANGELOG.md updated with M001-M006 entries, CLAUDE.md updated, extension.yml verified. Total doc output: ~6,400 lines across 14 new/updated files. One code fix: routing.yaml fallback value corrected (found via references/file-formats.md). Doctor final sweep: 9/13 pass, 4 advisory warnings all pre-existing from M002-M005. Documentation completeness: 19/19 PASS.
  - milestone-summaries :: .orchestrator/milestones/M007/M007-SUMMARY.md:12:  - "Future milestones: sqlite-vec vector search, impact analysis, community detection"
  - milestone-summaries :: .orchestrator/milestones/M007/M007-SUMMARY.md:28:Milestone M007 delivers a SQLite graph backend for the knowledge system across 4 phases and 10 tasks. P01 created graph-db.sh library (6 functions) with 3-table schema (entries, edges, scope_tags) and updated rebuild-index.sh to populate the DB. P02 rewrote traverse-graph.sh from 172-line Bash BFS to recursive CTE with bidirectional traversal, hop configuration, and path-distance ranking, plus added --graph mode to scope-filter.sh. P03 added --provenance mode for supersession chain queries. P04 created graph-aware diagnostics (statistics, orphans, components, integrity checks) integrated into run-doctor.sh. Technology choice: SQLite via sqlite3 CLI — zero new dependencies, file-based, no running process. Schema includes NULL vector column for future sqlite-vec integration. All 24 verification scripts pass across all phases.
  - milestone-summaries :: .orchestrator/milestones/M008/M008-SUMMARY.md:12:  - "M003 (provides standalone foundation for M003 context management), M009 (future debug/fix protocols inherit adaptive intensity + backend-agnostic dispatch), M010 (dispatch interface is the explicit seam for Managed Agents cloud backend)"
  - milestone-summaries :: .orchestrator/milestones/M008/M008-SUMMARY.md:16:  - "Centralized stage×intensity matrix in one script (intensity-gate.sh) to avoid drift across 5 command docs; filename-based adapter routing (zero backend-specific code in core) per SC-003; coordination-boundary adapter pattern for in-process tools (MEM018); uniform-interface fallback (adapter always emits dispatch-result even on unavailability); HOME guard mandatory on runtime adapters; one-directional foreign format adapter (speckit.sh rejects --write with exit 4); hard migration per project constitution (no dual code paths); surgical documentation-only refactor of derive-phase.sh per Constitution XV; additive-only command doc refactor preserving MEM012; comment-aware Bash 3.2 compat scanner; open-standard SKILL.md format with YAML frontmatter; thin installer pattern delegating to P05 runtime adapters; offline-safe version check with graceful degradation; reinit default mode is update (preserve existing user context)."
  - milestone-summaries :: .orchestrator/milestones/M008/M008-SUMMARY.md:18:  - "Natural-language scope/risk/complexity pattern matching via keyword tables; pipeline composition via --flag file-or-text inputs for testability; intensity-aware threshold adjustment (Quick tightens, Full loosens); structured dispatch result/error schemas; filename-based adapter auto-discovery; atomic metadata rewrite via awk+mktemp+mv with cksum-sentinel verification; thin wrapper pattern with --dry-run mode; pure resolver pattern (read-only, zero side effects); hermetic standalone e2e via mktemp fixtures (never touch real HOME); runtime adapter HOME guard + project-dir guard; skill generation via single-source-of-truth transform with --check drift detection; project introspection via marker-file presence; user-edit preservation via comment-delimited blocks; top-level pipeline delegation (init orchestrates existing scripts); reinit-with-preservation via awk field-level surgery; wall-clock budget enforcement + hermetic-only static gate."
  - milestone-summaries :: .orchestrator/milestones/M008/M008-SUMMARY.md:28:M008 transformed the orchestrator from a spec-kit extension into a standalone multi-runtime extension with adaptive process intensity. Across 7 phases (P01-P07), delivered: (1) Adaptive Intensity Engine — analyzes task descriptions along scope/risk/complexity axes and recommends Quick/Standard/Full intensity with environment capability awareness. (2) Backend-Agnostic Dispatch Interface — uniform entry point with filename-based adapter routing, two local backends (Claude Code Agent tool + Codex CLI SDK), structured result/error schemas with distinct exit codes. The explicit seam for M010's cloud backends. (3) Intensity-Aware Pipeline Scaling — central 7×3 stage×intensity matrix wires intensity awareness into every pipeline stage (discuss, research, plan-phase, dispatch, verify, knowledge, auto) with atomic mid-workflow override. (4) State & Namespace Independence — 5-rule root resolver, unified config, hard migration tool, surgical derive-phase refactor, namespace aliases. Full workflow runs standalone without spec-kit (SC-004 validated). (5) Runtime & Format Adapters — auto-detection + 3 runtime adapters (claude-code/codex/cursor) with HOME/project-dir guards + 2 format adapters (native round-trip, spec-kit one-directional read). (6) Multi-Runtime Packaging — open-standard SKILL.md format, 12 skills generated from commands, installable bundle, 3 runtime installers delegating to adapters, offline-safe version checker. (7) Init, Onboarding & Spec-Kit Bridge — orchestrator:init detects project (10 languages + 9 frameworks + 6 CI systems + 9 tools), generates config + runtime-appropriate instruction file (CLAUDE.md/AGENTS.md/.cursor/rules), installs skills. Reinit preserves user-edited custom blocks via comment markers. Full onboarding completes in ~1 second wall-clock (SC-005 validated with 2 orders of magnitude margin vs 5min target). Cross-cutting patterns: hermetic-first testing (mktemp fixtures, never touch real HOME), comment-aware Bash 3.2 compat, filename-based auto-discovery mirrored from P02 through P05/P06, thin delegation pattern, user-edit preservation via comment-delimited blocks. M008 is the foundation for M009 (debug protocols) and M010 (cloud dispatch via Managed Agents).
  - milestone-summaries :: .orchestrator/milestones/M011/M011-SUMMARY.md:16:  - "D004 Conversus lands inside M011/P07 as reusable adapter not standalone M017; SPEC-prefixed natural IDs instead of MEM-style sequential; nested category directories knowledge/spec/<subcat>/; classify+dispatch decision layer wrapper around create-entry.sh; versioned-ID chain-walking supersession; chunks-first with raw-spec fallback in command docs; bidirectional-traverse + directional-frontmatter-filter for typed edges; dispatch-interface.sh as uniform LLM boundary (no bash HTTP calls); three-value exit contract 0/1/2 for gate-shaped commands; _STUB=1 env-var escape hatches as first-class deterministic-testing contracts"
  - milestone-summaries :: .orchestrator/milestones/M011/M011-SUMMARY.md:18:  - "decision-layer wrapper around creation primitives; chain-walking next-version supersession; intensity-gate stage registration via minimal case-row insertion; chunks-first with raw-spec fallback command wiring; bidirectional-traverse + directional-frontmatter-filter composition; grep -Fq -- end-of-options idiom for BSD grep; sandboxed PROJECT_ROOT=mktemp -d + EXIT-trap for pipeline tests; evidence-present token gate (file-exists + token-contains); per-phase preserve-references regression contract; _STUB=1 env-var escape hatches; three-value exit contract for gate commands"
  - milestone-summaries :: .orchestrator/milestones/M011/M011-SUMMARY.md:28:M011 (Spec Management) delivers the "paste a spec, get a roadmap" value proposition end-to-end. Seven phases took the orchestrator from an opaque-spec-file consumer to a full ingest→chunk→scope-filter→normalize→fidelity-gate pipeline that treats specs as structured knowledge on equal footing with the existing three-temperature knowledge model (M002/M007). P01–P03 built the deterministic chunker (spec directory tree with six subcategories, SPEC-prefixed natural IDs, nested-directory indexing, create-entry.sh SPEC- namespace support, full markdown→chunk classifier with classify+dispatch decision layer, versioned-ID supersession chains, REMOVED detection, phase-impact REVIEW emission). P04 wired scope-filter.sh to resolve `spec/*` scope tags (graph-mode + spec-scope-tags mode), letting build-context.sh deliver scope-filtered spec chunks in dispatch payloads per Constitution Principle I. P05 wired chunks-first evaluation and intensity-aware roadmap generation into commands/evaluate.md and commands/roadmap.md (spec-metrics.sh counts non-superseded chunks; spec-story-graph.sh emits directional story-to-story depends_on edges via bidirectional-traverse + directional-frontmatter-filter composition; intensity-gate.sh roadmap stage with Quick/Standard/Full modes). P06 delivered the user-facing commands/ingest.md, four doc-shape verify scripts, a full sandbox E2E gate script, a timing harness (<60s on representative specs), and dogfood evidence against specs/016-autonomous-hardening/spec.md (20 chunks, 7s elapsed). P07 closed with format-agnostic intake and a reusable Conversus fidelity gate: detect-spec-shape.sh probe classifier, normalize-spec.sh thin wrapper dispatching through dispatch-interface.sh (no hardcoded LLM calls), templates/spec-normalizer-prompt.md, a reusable Conversus adapter (scripts/dispatch/adapters/tool/conversus.sh with check/gate/parse-verdict subcommands, graceful degradation, CONVERSUS_STUB escape, three-value exit contract 0/1/2), commands/conversus-gate.md designed for M013/M014 reuse with different presets, templates/conversus-presets/normalize-fidelity.yml, templates/gate-result.md, and integration into commands/ingest.md as a pre-chunker 6-step pipeline (detect-shape → normalize → resolve-policy → fidelity-gate → chunker → rebuild) gated by a new `ingest` intensity stage (Quick=normalize; Standard/Full=normalize+fidelity-gate). A 222-line user guide (docs/ingesting-arbitrary-specs.md) covers flag semantics, BLOCK resolution, stub modes, and preset extension.
  - milestone-summaries :: .orchestrator/milestones/M011/M011-SUMMARY.md:32:Cross-cutting patterns established: decision-layer wrapper around creation primitives (P03); chain-walking next-version supersession (P03); intensity-gate stage registration via minimal case-row insertion (P05, P07); chunks-first with raw-spec fallback in command docs (P05); bidirectional-traverse + directional-frontmatter-filter for typed edges over generic graphs (P05); `grep -Fq -- "$tok"` end-of-options idiom for --flag-shaped tokens under BSD grep (P06); sandboxed PROJECT_ROOT=$(mktemp -d) + EXIT-trap for pipeline tests (P06, P07); evidence-present token gate (file-exists + token-contains) as standard dogfood verification shape (P06, P07); per-phase preserve-references regression contract (P06, P07); `_STUB=1` env-var escape hatches as first-class deterministic-testing contracts (P07 NORMALIZER_STUB + CONVERSUS_STUB); three-value exit contract (0/1/2) for gate-shaped commands where BLOCK is distinct from error (P07); dispatch-interface.sh as uniform LLM-agent-dispatch boundary (no bash HTTP calls, P07).
  - milestone-summaries :: .orchestrator/milestones/M011/M011-SUMMARY.md:8:  - "End-to-end spec management: knowledge/spec/ six-subcategory tree, SPEC-prefixed natural IDs, nested-directory indexing, deterministic markdown chunker with classify+dispatch decision layer, versioned-ID supersession chains, REMOVED detection pass, phase-impact REVIEW emission, scope-filter.sh spec/* scope tag resolution, spec-metrics.sh non-superseded chunk counts, spec-story-graph.sh directional depends_on edges, intensity-gate.sh roadmap + ingest stages, commands/evaluate.md + commands/roadmap.md + commands/ingest.md + commands/conversus-gate.md user-facing docs, detect-spec-shape.sh probe classifier, normalize-spec.sh wrapper (via dispatch-interface.sh), templates/spec-normalizer-prompt.md, reusable Conversus adapter (scripts/dispatch/adapters/tool/conversus.sh check/gate/parse-verdict with graceful degradation + STUB escape), templates/conversus-presets/normalize-fidelity.yml, templates/gate-result.md, docs/ingesting-arbitrary-specs.md user guide, 45+ m011-*.sh verify scripts, dogfood evidence sets under phases P06/P07/evidence/"
  - milestone-summaries :: .orchestrator/milestones/M012/M012-SUMMARY.md:10:  - "populated .orchestrator/ tree (constitution, DECISIONS.md, KNOWLEDGE.md, milestone-summary.md, milestones/, archive/); populated knowledge/patterns+conventions+lessons/MEM corpus; mkdocs + mkdocs-material + mkdocs-include-markdown-plugin + pymdown-extensions Python toolchain (version-pinned in wiki/requirements.txt); GitHub Pages + GitHub Discussions enabled on the repo for the operator-gated first deploy; gh CLI for wiki-giscus-remap.sh live path; M011 spec-ingest pipeline produced the 022-spec-wiki feature spec; M021 anti-pattern-lint + pre-bash-shape-guard + widened allow-list gates"
  - milestone-summaries :: .orchestrator/milestones/M012/M012-SUMMARY.md:119:- **M013 (GitHub Issues/Projects/Milestones sync)** inherits the chained-gate deploy wrapper pattern, the loud-fail-over-silent-empty convention for missing external config, and the `scripts/diagnostics/` idempotent-helper convention (dry-run-default, `DRY-RUN`/`OK`/`NOOP`/`FAIL` fixed-verb output).
  - milestone-summaries :: .orchestrator/milestones/M012/M012-SUMMARY.md:12:  - "M013 (GitHub Issues/Projects/Milestones sync inherits chained-gate wrapper + loud-fail convention + diagnostic idempotent-helper pattern); M014 (comment triage consumes Giscus threads keyed by pathname + wiki-giscus-remap.sh remap contract); M020 (promoted by D011 evaluation — extends knowledge subtree with review-state + query surface); M019 Tier 2/3 (M012 is first non-self dogfood substrate for unit_close records on future redeploys); .orchestrator/DECISIONS.md roadmap ordering (M020 insertion between M014 and M019 Tier 2/3); live gh-pages branch on this repo (operator-gated); consolidation runbook (wiki-giscus-remap.sh --dry-run invocation before any discussion-bearing rename)"
  - milestone-summaries :: .orchestrator/milestones/M012/M012-SUMMARY.md:16:  - "AD-1 D011 ship cross-refs only (defer review-state + query surface to M020); AD-2 deploy to same repo gh-pages + same Discussions; AD-3 SSOT via include-markdown plugin (no copy, no symlinks); AD-4 archive included labeled with no date cutoff; AD-5 Giscus mapping=pathname + documented remap script; AD-6 all phases/tasks included with collapsible per-milestone nav; AD-7 MkDocs + Material + include-markdown-plugin + Giscus + GitHub Pages framework stack locked; AD-19 single-script-file Check shape with MEM004 carve-out for scanner/diagnostic/wrapper internals; D011 mechanical evaluation rule (1 of 3 criteria -> M020 promoted); Constitution VI state-on-disk-is-truth preserved via include-only projection; Constitution XIV no-speculative-complexity (no flags, no plugin drivers, no YAML-driven wrappers); Constitution XV surgical precision (each phase touches disjoint file surfaces; cross-phase additions via marker-bounded blocks + EOF-append); MEM001 Bash 3.2 discipline across all shipped scripts; loud-fail-over-silent-empty for ENV empty defaults; SC-10 self-contained wiki/ + sibling-gate allow-list extension policy; SC-12 pending-sentinel deploy record for operator-gated outcomes"
  - milestone-summaries :: .orchestrator/milestones/M012/M012-SUMMARY.md:18:  - "scanner-to-stubs-to-nav three-stage pipeline with additive append-only emission blocks keeping downstream generators stable across phases; marker-bounded atomic writes to shared config files with shasum byte-identity verification across edits; include-only SSOT with spec-quote exclusion for task-plan and payload files; pure-string Bash 3.2 primitives (PID-suffixed /tmp list files as associative-array replacement; IFS-slash positional-parameter normalize_path with no realpath dep; parallel indexed-array pattern registries; mktemp list-file + while-IFS-read as find-pipe-while replacement); SKIP-as-PASS at gate boundaries for optional toolchain (mkdocs / built site / phase-close summary); phase-suite orchestrator parallel-indexed-variable pattern with eval indirection and stderr-indent-on-FAIL; cross-phase allow-list extension as sibling-gate maintenance; self-scan carve-out via assignment-line regex skip in compat gates; MEM004 carve-out for scanner and diagnostic and wrapper internals with single-script-file Check-layer shape; loud-fail-over-silent-empty convention for missing external config; chained-gate deploy wrapper with first-non-zero-aborts semantics and fixed-verb per-gate output (GATE/BUILD/DEPLOY/DRY-RUN/OK/FAIL); graceful-absent-tool and graceful-absent-artifact patterns with dry-run-exit-0 and live-path-exit-1 split; pending-sentinel artifact for operator-gated outcomes; D011 mechanical-evaluation record as first-class phase artifact; EOF-append pattern for multi-author operator guides preserving prior section byte-identity; heading-as-section-boundary convention for README (distinct from mkdocs.yml marker-comment convention); theme-override as AD-3-compliant injection surface (append-at-render vs body-rewrite); scanner-presence-gated subtree emission (Archive / Knowledge Entries disappear cleanly when scanner sees zero records); stderr-noise-stdout-success diagnostic convention for machine-readable capture"
  - milestone-summaries :: .orchestrator/milestones/M012/M012-SUMMARY.md:44:Extends the P01 pipeline to resolve internal cross-links, surface the `knowledge/**/MEM*.md` corpus through the existing SSOT pipeline, and ships a standalone built-site link checker. Ends with the D011 mechanical evaluation.
  - milestone-summaries :: .orchestrator/milestones/M012/M012-SUMMARY.md:57:- **Giscus injection surface** — `wiki/overrides/partials/comments.html` (Material comments-partial override) emits the Giscus `<script>` block populated from `config.extra.giscus.*`. `wiki/mkdocs.yml` gains `theme.custom_dir: overrides` + a marker-bounded `extra.giscus` block (`repo`, `repo_id`, `category`, `category_id` via `!ENV NAME,''` with empty defaults; `mapping: pathname` literal).
  - milestone-summaries :: .orchestrator/milestones/M012/M012-SUMMARY.md:8:  - "deployable MkDocs Material wiki projecting .orchestrator/**.md + knowledge/**/MEM*.md via include-markdown SSOT pipeline; scripts/wiki/ three-stage pipeline (wiki-scan-sources.sh + wiki-generate-stubs.sh + wiki-generate-nav.sh) with additive-extension invariant + marker-bounded atomic nav/extra splices in wiki/mkdocs.yml; wiki/overrides/partials/comments.html Material theme override for per-page Giscus threads keyed by pathname (AD-5); scripts/diagnostics/ Bash 3.2 diagnostic trio (wiki-link-check.sh built-site walker, wiki-giscus-config-check.sh loud-fail pre-build gate, wiki-giscus-smoke.sh post-build walker, wiki-giscus-remap.sh idempotent thread remap); scripts/wiki/wiki-deploy.sh chained deploy wrapper (4 pre-deploy gates + mkdocs gh-deploy, --dry-run/--help/--root/--skip-smoke, exit 0/1/2, GATE/BUILD/DEPLOY output lines); wiki/docs/index.md finalized 65-line orientation home page with SSOT paragraph-overlap guard; wiki/README.md consolidated 420-line operator guide (install/preview/regenerate/scope/link resolution/Giscus mapping/remap/first-deploy checklist/deploy wrapper); .orchestrator/milestones/M012/phases/P02/D011-EVALUATION.md structured 1-of-3-criteria record promoting M020; .orchestrator/milestones/M012/phases/P04/DEPLOY-RECORD.md first-deploy record contract with pending-sentinel path for operator-gated completion; 37 verification gates across 4 phase-suite orchestrators (P01 9 + P02 9 + P03 8 + P04 11)"
  - milestone-summaries :: .orchestrator/milestones/M012/M012-SUMMARY.md:84:- **MEM004 carve-out for scanner/diagnostic/wrapper internals** — internal pipes, `$(…)`, `awk`, `grep -oE`, `find` permitted inside `scripts/wiki/` and `scripts/diagnostics/` because their agent-facing `Check:` invocations stay single-script-file shape (AD-19).
  - milestone-summaries :: .orchestrator/milestones/M013/M013-SUMMARY.md:18:  - "additive-helper-append-preserves-byte-identity (P02 12 + P03 1 + P04 5 helpers appended at end-of-helpers; prior bodies untouched); gate-evolution-on-legitimate-advancement (P02→P03 mapping table; P03→P04 reference extensions — earlier-phase assertions evolve to accept both pre/post shapes; byte-identity hashes on unchanged content stay pinned); pinned-SHA-with-awk-range-capture-before-edit (multi-phase additive doc editing with three pinned ranges in references/github-integration.md); FR-12-v1-negative-grep-guard (sibling-runtime byte-identity via negative greps for post_verify/FR-12 markers — cheaper than pinned-sha and self-repairs if codex/cursor gain legitimate content under different markers); AWK-based JSON upsert + count + first-scalar extraction (no jq hard-dep across all 4 phases); M013_GH_STUB_DIR fixture-driven preflight + marker-search stub-selector (env-var-driven stub paths exercise every branch deterministically without gh calls); PATH-shimmed fake-gh for zero-subprocess-call invariant enforcement (auto-mode SC-7 enforcement logs every gh call + asserts log empty); per-call-stderr-mktemp-classify-rm (capture stderr to mktemp + classify_gh_rc rc + file + rm -f on all paths; releases lock before exit 3/4); direct-cmd-subst-rc-propagation (assign $() then read $? on next line; '|| true' in cmd-subst tail silently zeros $? — caught mid-T03); external-watchdog-plus-adapter-internal-timeout (Constitution XII belt-and-suspenders kill -TERM at TIMEOUT then kill -KILL at TIMEOUT+1s); verdict-rc-parity (rc=0 PASS / rc=2 BLOCK / rc=1 ERROR passed verbatim so BLOCK is distinguishable from error); pending-sentinel bootstrap with clobber-refusal exit 2 (extends M012/P04 DEPLOY-RECORD convention); fixture-copy-before-live-gate (cp -R fx/. tmp/ before invoking github-sync.sh because it reassigns ORCHESTRATOR_ROOT and would pollute repo fixture tree); stdout-record + stderr-summary split (fixture diffs stay byte-identical while operator still sees per-run summary); buffered-manifest-body via mktemp tempfile during walker iteration (counts finalized before header emission); FORMAT STABILITY CONTRACT doc-block at helper definition site (pins shape for downstream P03/P04 consumers); section-content byte-identity gate via awk-extract by literal-prefix heading match + shasum compare against embedded hashes (robust against insertion-shifts); load-bearing _deferred to P0N_ cells functioning as scope-boundary markers a later phase replaces in place (D015 split); comment-discipline synonyms in bash32 scanners (assoc-arrays / array-from-stdin builtins / case-conversion expansion / combined-redirect shorthand)"
  - milestone-summaries :: .orchestrator/milestones/M018/M018-SUMMARY.md:48:  ceilings, and FR-2 failure semantics. Conversus `--strict` red/blue gate
  - milestone-summaries :: .orchestrator/milestones/M018/M018-SUMMARY.md:84:  sav_total > 0 AND ratio < SC-9 floor (configurable via
  - milestone-summaries :: .orchestrator/milestones/M027/M027-SUMMARY.md:16:  - "D026 cost_estimates JSON shape; D027 verbatim accuracy disclaimer in commands/cost.md; #Q-16 always-on with --no-predict operator-override (no hidden state); #Q-1 anomaly threshold defaults pinned by M012-M026 sampling (cost_multiplier=3.0, retry_threshold=2, pass_rate_threshold=0.5, sample_floor=5); #Q-5 doctor anomaly perf measured at 56ms wall-clock on largest log; #Q-10 anomaly baseline disclaimer text pinned verbatim; FR-12/CON-1 read-only invariant; FR-21/CON-6 zero-LLM-token; FR-22 <100ms predictive latency (inner library); FR-20/CON-4/SC-18 Goodhart pairing extended to predictive + alerting surfaces; CON-3/SC-3/SC-17 byte-identity back-compat for --quiet/--yes/auto suppression; CON-7 bash 3.2 compat; CON-5 never-abort graceful degradation; CON-8 sample-floor; CON-10 operator override preservation; AD-19 single-script-file Check shape; MEM004 emitter-internal carve-out; MEM012 no-reshape"
  - milestone-summaries :: .orchestrator/milestones/M027/M027-SUMMARY.md:18:  - "T04-subsumes-prechecks pattern (4 occurrences across P00/P01/P02/P03): tasks shipping structural changes ship a single-script-file precheck under scripts/verify/m027-p<phase>-t<task>-shape-precheck.sh asserting only that task's must-haves; T04 deletes them once canonical phase-level verifiers ship and check-must-haves.sh is green. Document-shaped phase-task pattern: integrate helpers into runtime-rendered command documents (commands/<cmd>.md) at a stable attach point + ship a verbatim-tail baseline fixture under tests/fixtures/<phase>/; T04 ships the byte-identity verifier. Pre-edit canonical sections preserved in pre-edit order. Multi-key co-location pattern (P02/T01, P03/T01): when multiple downstream tasks each need new VALID_KEYS entries, edit the list once with all keys appended in canonical order — avoids merge churn and preserves a single source-of-truth touch per file. 5-condition suppression matrix as load-bearing reusable contract: --yes / ORCHESTRATOR_AUTO / --no-<feature> / <knob>: false / structural-carve-out — all 5 paths short-circuit to zero stdout exit 0 before any I/O, gated mechanically by per-phase suppression-matrix verifier. Latency outer-vs-inner split (3 occurrences across P01/P02/P03): hard-fail at 250ms on the inner library measurement (with appropriate fast-path env-vars); outer wall-clock reported informationally with WARN: RELAX-CANDIDATE annotation forwarded by suite. Comment-hygiene-for-verifier-regex (4 occurrences across P00/P01/P02/P03): doc-comments use safe phrasing (no associative arrays / no herestring redirect) rather than spelling literal bash-4 forbidden tokens, so the bash32-compat verifier grep regex stays clean against the file body. Sourceable-CLI duality via BASH_SOURCE-zero == zero-arg guard at bottom of file; load-time _<MODULE>_SH_SOURCED re-source guard. Section-order assertion via grep -n line-position monotonic walk over a canonical heading list. Self-applying scanner pattern: split-literal token assembly (FORBID_A='declare'' -A') keeps bash32-compat + zero-llm-token verifiers from matching their own source even when included in the scan-set. Markdown-aware bash32-compat scanner: extract only fenced bash/sh code blocks from .md files before applying forbidden-construct regex. Operator-override-as-informational-prompt (CON-10): helpers render override prompts but do NOT read stdin; runtime layer handles capture. Verifier-author-vs-helper-author boundary: when phase-plan key-link asserts a transitive reference, the cleanest fix is a one-sentence transitive-reference note inside an existing section (rather than a new bullet that breaks a baseline fixture). read-config null-sentinel pattern: read-config.sh returns the literal string null for unset-but-registered keys; CLI threshold resolution must treat both empty AND null as use-built-in-default. Goodhart-pairing isolated test environment: mktemp tmp_root + ORCHESTRATOR_ROOT env var override; avoids polluting the real .orchestrator tree. Read-only-with-WARN-skip-on-dirty-pre-run pattern: capture git diff --quiet exit pre-run; if non-zero, WARN + exit 0 rather than false-fail; pathspec-excludes for known orchestrator side-effect files (e.g. .orchestrator/doctor-history.jsonl)"
  - milestone-summaries :: .orchestrator/milestones/M029/M029-SUMMARY.md:30:**Three phases, all green** (P01–P03, ~525m total wall clock when summed across phase summaries; every phase verification_result=pass; acceptance battery `pass=14 fail=0`).
  - milestone-summaries :: .orchestrator/milestones/M029/M029-SUMMARY.md:8:  - "M029 (roadmap visibility & CLI UX) closes the orchestrator's invocation-context + tree-rendering + auto-chain onboarding surfaces. P01 foundation: scripts/state/detect-invocation-context.sh AD-1 single-resolve env block + references/status-headline-shape.md FR-2 + references/status-json-schema.md FR-3 + commands/status.md headline block + scripts/diagnostics/render-status-json.sh AD-2 unconditional ANSI strip + commands/context.md FR-4 read-only single-screen runtime profile skill. P02 at-rest tree: scripts/diagnostics/render-position.sh ~470-line tree renderer engine FR-5 four-glyph alphabet + commands/where.md skill + references/cross-milestone-feature-shape.md AD-6 contract + scripts/diagnostics/summarize-milestone.sh AD-4 SC-8 oracle wrapper + SC-5/6/13/14 fixtures + AD-9 sentinel-file mechanism + FR-6 silent pre-M019 cost-column suppression. P03 live + preflight + auto-chain: --live branch on render-position.sh FR-7 + #Q-G9 p95 ≤ 1.0s methodology via measure-live-tail-latency.sh three-tier nanosecond-clock portability shim + ▽ saved Nk canonical compact-form FR-8 #Q-G8 gated by AD-5 display_thresholds.compression_savings_pct config knob + commands/auto.md ## Preflight Summary FR-9 AD-3 four-priority non-interactive policy AD-4 oracle wrapper + commands/start.md --auto-chain flag + scripts/lifecycle/start.sh chain-driver passthrough FR-10 #Q-3 leave-marker-absent failure semantics AUTO_CHAIN_STAGE_STUB fixture-only escape hatch + SC-7/8/9/10 fixtures + AD-4 Spec Amendment Record on specs/037-roadmap-visibility-cli-ux/spec.md + SC-11 milestone-grain run-acceptance-battery.sh BATTERY: pass=14 fail=0 + SC-12 validate-milestone-pass.sh VALIDATE: PASS hook + closure ceremony M029-VALIDATED + M029-SUMMARY.md + milestone-grain unit_close JSONL record. Three phases all green; acceptance battery pass=14 fail=0; validate-milestone.sh M029 reports VALIDATE: PASS — 101/101 checks passed."
  - milestone-summaries :: .orchestrator/milestones/M033/M033-SUMMARY.md:18:  - "deterministic-curatorial-fixture-with-README-oracle (P01); SSOT-with-byte-matched-implementation-parity-via-grep-F-fixed-string-cross-check (P01); fenced-rule-block-convention-with-load-bearing-token-tripwires (P01); SKIP-gate-for-verifiers-co-authored-before-implementation-lands (P01); closed-enum-as-fenced-SSOT-grep-token-tripwire (P02); idempotent-marker-with-first-completion-timestamp-preservation (P02); stub-helper-with-stable-name-for-T04-replacement (P02); sourceability-guard-via-BASH_SOURCE-vs-dollar-zero (P02); recommendation-not-interrogation-prefix-ordering (P02); awk-single-pass-alphabetized-insert (P02); bidirectional-scope-guard-pattern-forbidden-plus-allowed-whitelist (P01-P05); phase-suite-aggregator-pattern-with-newline-delimited-verifier-list-iterated-under-IFS-swap (P01-P05); fenced-SSOT-block-for-closed-surface-file-set-with-awk-block-extraction (P03); positive-and-negative-mktemp-d-functional-smoke-tests (P03); self-reference-elision-pattern-for-docs-that-document-content-detection-gates (P03); ask_one-uncaptured-stdout-plus-accumulator-readback (P03); negative-grep-determinism-invariant-claude-code-plus-conversus-plus-model_routing-must-not-appear (P03); stable-id-by-source-path-plus-signal-kind-hash (P03); reserved-fenced-block-with-true-no-op-stub-for-future-task-fill-in-place (P03); byte-deterministic-stack-fixture-with-README-oracle (P03); additive-closed-enum-extension-pattern (P03 -- P02 emitter 11-to-12); downstream-traverser-skip-clause-_*-continue (P03); 4-fenced-SSOT-blocks-pattern (P04 -- material-extensions plus labeling-enum plus drift-categories plus resolution-enum); recommend_label-filename-heuristic-via-uppercase-tr-plus-case-glob-pattern-match (P04); 3-detector-pipeline-id-misalignment-plus-scheme-contradiction-plus-orphan-reference (P04); byte-deterministic-prespec-with-canonical-section-ordering (P04); ideate_one-helper-as-MIT-007-third-arg-wiring-proof-point (P04); CON-6-in-flight-resume-scan-takes-precedence-over-env-override (P04); deprecated-alias-forwarding-pattern-with-load-bearing-token-emission-preserved-for-AD-15-cross-phase-regression-passthrough (P04); dry-run-gate-pattern-skip-side-effects-but-emit-load-bearing-tokens (P04); spec-shape-impl-shape-translation-helper-pattern (P04); strict-aggregation-driver-pattern-every-emitted-line-traces-verbatim-to-upstream-sub-flow-output-file-NO-LLM-NO-conversus-NO-model-routing-Constitution-XV (P05); negative-grep-for-strict-aggregation-invariant (P05); floor-not-ceiling-pattern-prescribed-section-set-as-closed-floor-operator-additions-preserved (P05); branch-dependent-section-variant-pattern-detect-upstream-output-file-presence-then-render-Source-Docs-XOR-Entry-Points (P05); doc-prose-alias-token-in-comment-to-satisfy-load-bearing-grep-tripwire (P05); set-e-safe-rc-capture-pattern (P05); stub-mode-vs-real-mode-vs-genuine-failure-three-way-branch-as-MIT-001-two-mode-test-contract-template (P05); paired-launch-passthrough-mirror-pattern (P05); post-gate-ordering-via-code-position (P05); explicit-enumeration-discovery-model-with-closed-enum-roster-comment-block-as-traceability-documentation (P05 / MIT-002); awk-block-injection-pattern-for-cross-platform-Notes-section-insert (P05); config.yml-seed-pattern-for-acceptance-tests-against-staged-dirs (P05); PROJECT_DIR-threading-as-load-bearing-emitter-prefix-pattern (P05); milestone-close-gate-procedural-authorship-pattern-AD-7 (P05); scope-guard-forbidden-list-pre-existing-path-deviation-pattern (P05); direct-printf-append-for-one-time-milestone-grain-events-not-in-shipped-closed-enum (P05); US-8-AS-5-signed-attestation-fallback-discipline-cold-start-risk-acknowledgment-plus-recruiting-outreach-deadline-plus-maintainer-signature (P05)"
  - milestone-summaries :: .orchestrator/milestones/M033/M033-SUMMARY.md:8:  - "orchestrator:start warm conversational front door (FR-1/FR-2/FR-20/FR-21/FR-22) with four onboarding branches (greenfield-empty / greenfield-with-materials / existing-codebase / migrating); orchestrator-native constitution authoring (FR-3/FR-4/FR-5/FR-6/CON-3) -- zero spec-kit dependency at launch via Principle XVI standalone-gate; deterministic codebase ingestion (FR-7/FR-8/MIT-005) with rich-context import and supersede-aware MEM cross-references; materials intake with deterministic CON-4 drift detection (FR-9) -- 4 fenced SSOT blocks + 3-detector pipeline + byte-deterministic reconciled pre-spec; ideation with MIT-007 live contradiction detection (FR-10/FR-17) -- 7-question grilling-protocol flow + opt-in conversus stress-test; migrate-routing glue (FR-11/FR-12) with FR-12 migrate-then-ingest dup-prevention sentinel; customblock drafter (FR-13/FR-14) with strict aggregation discipline (Constitution XV) -- floor-not-ceiling preservation + branch-dependent variant + idempotency gate; --with-wiki paired-launch passthrough (FR-15/CON-1/MIT-001) two-mode contract under stub OR real M032/P02 wiki-init invocation; --with-github passthrough (FR-16) ordered after wiki-init; grilling-shell + glossary inline-update writer (FR-17/FR-18) with 4 fenced SSOT blocks for contradiction-pairs + glossary-triggers; friendly-tester pass artifact + validate-report.sh mechanical gate (FR-19/SC-15) with US-8 AS-5 signed-attestation fallback path"
  - milestone-summaries :: .orchestrator/milestones/M035/M035-SUMMARY.md:59:- **#Q-7 (CI runner platform)** → ubuntu-latest (D001 at P02 T04).
  - milestone-summaries :: .orchestrator/milestones/M035/M035-SUMMARY.md:60:- **#Q-7 (symlink mode hardlinks)** → symlinks-only at v1, hardlinks deferred (P01 T01).
  - milestone-summaries :: .orchestrator/milestones/M041/M041-SUMMARY.md:125:  targets. The threshold is now configurable (`detective.match_threshold`,
  - milestone-summaries :: .orchestrator/milestones/M041/M041-SUMMARY.md:8:  - "orchestrator:detective command — triages orchestrator-internal issues (NOT user-project bugs), captures a structured 6-section triage report, searches a configurable GitHub repo for keyword matches, and files/comments with the report under an operator confirmation gate. Six phases: P01 core triage engine (triage-issue.sh + commands/detective.md), P02 GitHub integration + GH_MOCK_DIR offline harness (search-issues.sh + file-issue.sh + tests/fixtures/detective/gh-mock/), P03 doctor recommendation hook + acceptance battery (detective-recommend.sh + run-doctor.sh hook), P04 FR-8 completion (auto-loop.sh unexpected-state hook + verify.md/dispatch.md guidance + doctor specific-symptom emission), P05 FR-9 confirmation gate (--yes / interactive prompt / non-interactive degrade in file-issue.sh), P06 deferred follow-ups (detective.repo + detective.match_threshold config keys + meets_threshold field + detective-validate-threshold.sh #Q-1 corpus harness)."
  - milestone-summaries :: .orchestrator/milestones/M042/M042-SUMMARY.md:18:  - "Search-before-ask gate: a deterministic grep sweep over a configurable store manifest produces a per-question evidence artifact (terms,stores-searched,citations,verdict) and a PASS|BLOCK that forces the agent to read candidate answers before posting a question to a human. The artifact doubles as packet provenance for the SME,Reuse the conversus gate-artifact convention rather than inventing one: corpus-gate.sh mirrors conversus.sh check/gate/parse-verdict with the identical 0-PASS/SKIP / 2-BLOCK / 1-error exit contract; callers wire against the same shape,Disposition-on-the-question-line: corpus hits are resolved by annotating the question (@disposition=dropped|kept) rather than editing the generated artifact — keeps the sweep a pure function of (questions+corpus) so re-runs are reproducible,Reproducible-under-test engine: no date/$RANDOM in the sweep (caller-supplied --generated-at) so fixtures are byte-stable,CON-3 caveat over silent skip: an unreachable required store yields IRREDUCIBLE-WITH-CAVEAT naming the store, never a silent skip or a permanent deadlock,Low-noise doctor lint: warn only on an unresolved BLOCK artifact (a gate that blocked and was never resolved), zero-noise on projects with no artifacts"
  - milestone-summaries :: .orchestrator/milestones/M042/M042-SUMMARY.md:39:tokens minus stopwords), greps every reachable store in a configurable manifest,
  - milestone-summaries :: .orchestrator/milestones/M044/M044-SUMMARY.md:61:  incl Quick (DQ-7, no net-new verb) without changing the auto-pipeline step counts;
  - milestone-summaries :: (capped at 5 hits — more may exist)

### Q8 — KEPT

- **question**: - #Q-8 (ledger-schema): What is the FR-18 attempts-ledger's on-disk schema, retention policy, and how much is fed to a fresh rotation — full replay vs compacted summary?
- **disposition**: kept (new FR-18 surface introduced by 2026-07-03 LFD amendment — no prior art in corpus; design decision at plan-phase)
- **terms** (12): Q-8, FR-18, ledger, schema, attempts, disk, retention, policy, much, fresh, rotation, full
- **hits**:
  - constitution :: .orchestrator/memory/constitution.md:111:- Use fresh sessions per task (not accumulated garbage from prior
  - constitution :: .orchestrator/memory/constitution.md:119:  total task tokens across the full dispatch cycle, including
  - constitution :: .orchestrator/memory/constitution.md:13:  Operational definitions for "fresh project fixture" and "works"
  - constitution :: .orchestrator/memory/constitution.md:137:No task is marked complete without fresh verification evidence.
  - constitution :: .orchestrator/memory/constitution.md:192:### V. Fresh Context Per Unit
  - constitution :: .orchestrator/memory/constitution.md:208:### VI. State On Disk Is Truth
  - constitution :: .orchestrator/memory/constitution.md:211:from files on disk.
  - constitution :: .orchestrator/memory/constitution.md:213:- The state machine reads disk state, determines the next action,
  - constitution :: .orchestrator/memory/constitution.md:214:  executes, and persists results back to disk.
  - constitution :: .orchestrator/memory/constitution.md:219:  dispatched twice without progress artifacts appearing on disk, the
  - constitution :: .orchestrator/memory/constitution.md:267:schema-variable, and documented-consumer surfaces with
  - constitution :: .orchestrator/memory/constitution.md:275:XII discharge the orchestrator's full dead-infrastructure coverage.
  - constitution :: .orchestrator/memory/constitution.md:294:Configuration and policy MUST be declared in templates (YAML recipes,
  - constitution :: .orchestrator/memory/constitution.md:296:Scripts implement mechanics; templates declare policy.
  - constitution :: .orchestrator/memory/constitution.md:354:### XIII. Agent Instruction Schema
  - constitution :: .orchestrator/memory/constitution.md:357:MUST follow a declared, inspectable schema. Ad-hoc instruction
  - constitution :: .orchestrator/memory/constitution.md:361:- The instruction schema declares required sections, optional
  - constitution :: .orchestrator/memory/constitution.md:364:  schema declaration.
  - constitution :: .orchestrator/memory/constitution.md:410:- MUST degrade gracefully:
  - constitution :: .orchestrator/memory/constitution.md:60:      "fresh project fixture" and "works" — Decision B resolved
  - constitution :: (capped at 5 hits — more may exist)
  - decisions :: .orchestrator/DECISIONS.md:113:- **Choice**: Revised sequence: **M011 → M012 → M013 → M014 → M019 → M018 → M009 → M010** (M019 inserted by D009). M018 adopts caveman-style (github.com/JuliusBrussee/caveman) token compression as a first-class pipeline stage aligned with Constitution Principle I (Context Minimization). Proposed phase outline (subject to detailed planning closer to kickoff): P01 compression grammar (what's safe to compress per artifact class; preserve frontmatter, code fences, paths, MEM IDs, commands, URLs), P02 dispatch compressor (optional filter in `scripts/dispatch/build-context.sh`, emits `payload.compressed.md` beside original), P03 intensity mapping (Quick→ultra, Standard→full, Full→lite/off via `scripts/engine/intensity-gate.sh`), P04 memory compression (opt-in `CLAUDE.compressed.md` / `KNOWLEDGE.compressed.md` with `.original` siblings), P05 agent output profile (inject terseness directive into payload prefix), P06 eval harness (3-arm port of caveman's evals against orchestrator payloads, gate on accuracy parity), P07 multi-runtime parity (Claude Code / Codex / Cursor). Differentiation vs. installing caveman as-is: roundtrip-safe by policy (Principle VI — originals authoritative, compress only at dispatch/load boundaries), grammar-aware preservation of orchestrator artifact structure, intensity-bound (not always-on), explicit opt-out surfaces (constitution, spec.md, PR descriptions, user-visible docs), and telemetry integrated with existing cost tracking so savings are measurable per dispatch.
  - decisions :: .orchestrator/DECISIONS.md:127:- **Choice**: Revised sequence: **M011 → [M019/Tier 1 emitter] → M012 → M013 → M014 → [M019/Tier 2+3] → M018 → M009 → M010**. Tier 1 ships as a small standalone unit once M011 is closed — avoids mid-milestone scope insertion (Constitution XV — Surgical Precision), gives a clean boundary for instrumentation to start, and still captures M012+M013+M014 (~3 full milestones of dogfooding) before M018 planning needs a baseline. Cost of not backfilling M011 dispatches: ~15–25 unlogged records from M011/P04–P07; acceptable tradeoff. M019 records time, token cost, estimated dollar cost, retries, deviations, verification pass rate, and domain-unit cost (per ingested spec / merged PR / auto-applied comment) at task/phase/milestone/project/all-time granularities. Three-tier framing: **Tier 1 — Just emit** (~1 day: append `payload_breakdown`, `dispatch_usage`, `unit_close` JSONL records to existing `execution-log.jsonl`; no UI; ships first thing after M011 close so M012–M014 dogfooding produces real data); **Tier 2 — Rollup + `orchestrator:cost` command** (~3 days: `scripts/diagnostics/metrics-rollup.sh`, `.orchestrator/metrics/*.jsonl` aggregates, `orchestrator:status` efficiency footer; worth it if we launch); **Tier 3 — Full polished surface** (~7 days: backend-specific `adapters/backend/*/report-usage.sh`, auto-generated `MNNN-METRICS.md` on consolidate, `orchestrator:doctor` anomaly checks, parity evals across runtimes; only if efficiency becomes M009 launch headline). Full M019 phase outline mirrors the tiers: P01 schema + pricing table, P02 dispatch-time emitter (Tier 1 boundary), P03 backend usage adapters, P04 rollup + `orchestrator:cost` command (Tier 2 boundary), P05 milestone close report, P06 status + doctor integration, P07 evals + multi-runtime parity (Tier 3 boundary). Schema must pair cost metrics with **quality metrics** (verification pass rate, deviation rate) from day one to avoid Goodhart failure mode — optimizing cheap over correct. All records labeled `source: "estimate" \
  - decisions :: .orchestrator/DECISIONS.md:128:- **Revisable**: Runtime cost of tracking is effectively zero (JSONL append + reuse of existing `estimate_tokens` function; no added LLM tokens — agent does NOT self-report, runtime hooks supply actuals). Build cost is the real decision, hence the tiered framing so M019's final scope is set by what the Tier 1 data reveals rather than up-front speculation. M019 before M018 because compression needs a measured baseline. M019 before M009 because launch narrative differentiates on published receipts ("M011 shipped in N phases, M tasks, $X spend, Y% first-try verification pass") — no other orchestrator tool publishes this. Also surfaces Principle V's fresh-context cost quantitatively, making the Context Minimization claim auditable.
  - decisions :: .orchestrator/DECISIONS.md:130:"runtime"` so estimates and ground-truth usage (from Claude Code `SessionEnd`, Codex/Cursor where available) don't silently mix. Pricing table lives at `config/pricing.yml`; stale rates degrade gracefully with last-updated date surfaced in reports.
  - decisions :: .orchestrator/DECISIONS.md:141:- **Choice**: Replace the D008 phase outline's "P02 dispatch compressor (optional filter...)" framing with a tier-ladder model informed by Article 3 (Rohit's Claude Code harness teardown) and logged in `.orchestrator/scratch/articles-synthesis-2026-04-17.md`. Every dispatch runs through tiers in order, cheapest first: **Tier 1 microcompact** (reuse cached tool-call results that haven't changed + persist oversized tool results to disk with `file_path + preview` references — absorbs L6 tool-result budgeting; zero LLM cost); **Tier 2 snip** (head-drop with protected tail; no LLM call); **Tier 3 auto-compact** (summarize via LLM call; existing caveman-style territory); **Tier 4 context collapse** (staged multi-phase compression of tool results → reasoning → sections; feature-flagged). Intensity maps to tier ceiling: Quick→T1–T2 only, Standard→through T3, Full→all tiers available. Revised phase outline: P01 tier grammar + per-tier safety boundaries (what's preservable per tier), P02 Tier 1 microcompact + tool-result budgeting, P03 Tier 2 snip with protected-tail semantics, P04 Tier 3 auto-compact (summarization, intensity-gated), P05 Tier 4 collapse (staged; feature-flagged, off by default), P06 eval harness (3-arm parity tests per tier), P07 multi-runtime parity. D008's original scope envelope (roundtrip-safe by policy, grammar-aware preservation, intensity-bound, explicit opt-out surfaces, telemetry-integrated) carries forward unchanged — only the internal staging model is reframed. Fresh `orchestrator:discuss` at M018 kickoff addresses detailed tier design once M019 Tier 1 data reveals which artifacts dominate token spend. **Conversus integration (locked in per D007 reuse pattern)**: M018/P01 tier-grammar + per-tier-safety-boundary work invokes `scripts/dispatch/adapters/tool/conversus.sh` via `role/red-team` + `domain/security` presets at plan-review time. Rationale: grammar decisions carry parse-regression risk across downstream scripts that grep prose (the risk D008 originally flagged), which is exactly the subjective-quality territory the conversus fidelity-gate adapter earns its cost on. A new preset drops under `templates/conversus-presets/` without adapter modification (same pattern M013/M014 use). Gate is intensity-bound by default (Quick=off, Standard=advisory, Full=blocking); `--no-review` escape available.
  - decisions :: .orchestrator/DECISIONS.md:197:- **Choice**: Narrow M013 scope per `specs/023-github-native-integration/conversus/` run: (1) FR-12 handler installation is Claude-Code-only for v1 (CV-3 / RD-2 binding ruling) — Codex CLI + Cursor fall back to `manual` sync at init, non-Claude-Code runtime-adapter work deferred to a future milestone; (2) Minimal Slice (CV-1 / RD-3 binding ruling) pins Phase 1 load-bearing scope to full US-3 + minimal US-1 scaffolding (sidecar config + UAT template install) + US-2 idempotency applied to UAT entries only; (3) M020 sequencing unchanged (RD-1 reclassified) — M013 -> M014 -> M020 per D013; schema authority bounded by MIT-2 narrowing FR-9 to additive-emit-only with chunk-ID pinned to existing `SPEC-*` frontmatter + new Knowledge-Layer Boundary subsection explicitly forbidding review-state / query-surface / clustering; (4) 13 unmitigated P5 risks landed as MIT-1..MIT-13 spec edits (sub-issue fallback Constraint, Constitution Check section, FR-5 three-shape rewrite, FR-6 per-item cache schema, FR-7 sync lock, FR-9 narrowing, FR-12 prose correction + scope, FR-13 `--strict` provenance + 30s timeout + drop intensity-engine coupling, FR-14 re-init via marker search + label-collision preflight, FR-15 dry-run generalized to init+sync, FR-16 rate-limit + auth-expiry detection, FR-17 cost emission, FR-18 cache reconciliation, plus SC-8/SC-9 reconciliation and new SC-12/SC-13 scope + overhead caps); Open Questions #1/#3/#6/#7 closed; new OQ on PAT-vs-App default for M009. Spec status promoted Draft -> Ready-for-discuss.
  - decisions :: .orchestrator/DECISIONS.md:200:Red-blue deliberation with constitution-arbiter binding on three disputes. Spec was pinning scope intentions that had not been converted into Constitution-XIV/XV-bounded commitments; deliberation surfaced 13 concrete unmitigated risks + 3 arbitrated disputes. Arbiter ruled on XIV ("current demonstrable need" governs FR-12 and Minimal Slice) and on XV (narrow FR-9 to minimum schema authority — no sequence re-open). Landing edits pre-discuss means `orchestrator:discuss` operates on the narrowed spec and cannot silently re-expand scope. See `specs/023-github-native-integration/conversus/summary/final.md` + `conversus/arbitration/resolution.md` for full register + rulings.
  - decisions :: .orchestrator/DECISIONS.md:239:- **Choice**: M014 pressure-tested pre-discuss via cooperative conversus deliberation (Tier 1; 2 agents — `spec-author` + `devils-advocate`; 1 round; no arbiter). 14 MITs applied pre-discuss covering: (P1) Phase Sequencing table as spec-layer subsection after Minimal Slice; SC-2 tightened to mechanical stdout/exit-code/stderr-grep assertions; FR-2b template SSOT (`templates/spec-template.md`) + FR-18 byte-compatibility fixture test (`tests/test-specify-shape.sh`) + SC-17; US-5 promoted to independent P2 user story with scope-boundary sentence ("US-5 is the only path that edits `specs/<NNN>-<slug>/spec.md` from a comment"); Constitution XV retrofit at SC-5 + Constitution Check section → III (primary) + XIV (supporting), with Constitution Check XV paragraph split into "applies to / does NOT apply to" sub-contexts (FR-12 + FR-14 byte-preservation retained; spec-amendment human-gate retargeted); (P2) SC-4 rewritten as two-branch construction (default: measurement + re-planning trigger; upgrade branch: pinned-shape gate with failure-posture clause); FR-14 three-case semantics (all-placeholder / partial / fully-authored) + SC-14 byte-preservation invariant; SC-6a outside-markers `shasum` invariant + `tests/test-dual-write-outside-invariant.sh`; FR-19 `--dry-run` JSONL manifest format pinned M014-local only (M013 retrofit deferred); SC-15 `RUNTIME-ASSUMPTIONS.md` close-out deliverable; SC-16 dogfood-data sizing for FR-9 classifier-shape decision; (P3) FR-7 interim→M024 migration handshake committed as one-line rule; Open Question #C-11 exhaustive CLAUDE.md write-site enumeration at planning time. Spec frontmatter promoted `Status: Draft` → `Status: Ready-for-discuss`; `Last Revised: 2026-04-22` line added matching M013 precedent. P3 Dependencies prose rewrite skipped as subsumed by Phase Sequencing table (P1 #1). Two Phase 4 disputes (SC-4 default branch; Phase Sequencing table layer) resolved via synthesizer-recommended compromises: SC-4 adopts measurement default with upgrade-branch failure-posture; table lands spec-layer.
  - decisions :: .orchestrator/DECISIONS.md:240:- **Revisable**: Yes — if planning-phase discovery shows FR-9 classifier shape cannot be pinned mechanically (dogfood data too sparse, or no regex/heuristic floor earns its keep), SC-4 stays on the default branch and the upgrade branch never activates. If M024 planning reshapes the `.orchestrator/intake/` manifest schema, FR-7's migration-handshake commitment may need amendment via a new D-row.
  - decisions :: .orchestrator/DECISIONS.md:270:Dogfooding M014/P01 on M020 (spec 025) surfaced a silent dead zone: the shipped `orchestrator:specify` scaffolds TODOs but does not author, and the pressure-test gate runs probe-and-prompt against whatever body exists. On a freshly scaffolded spec, the probe reads below-threshold (no FRs, no user stories, just TODOs) and the gate is skipped — so the gate's adversarial value is never exercised on the first draft where it matters most. The pressure-test only earns its cost against authored content; running it against placeholders produces nonsense findings, running it too late means authoring defects propagate into evaluate/discuss/roadmap before anyone catches them. Intensity-scaling the three passes respects the "right tool for the right job" ethos already pervading the orchestrator (Quick trusts the agent + skips the gate for low-risk specs; Standard adds a non-blocking safety net; Full invokes human judgment + strict gate where architecturally load-bearing work earns the cost). Smell-test-only-escalates honors operator intent (user setting Quick isn't silently upgraded to Full unless CLI override forces it) while protecting against accidental under-classification (an architecturally heavy spec at project-default Quick gets recognized and bumped). The gate TODO pre-flight is a universal invariant rather than a specify-only check so every future gate consumer (M013 UAT gate, M023 design gate, M024 routing gate) gets the protection without each one reimplementing it. Spec 025 also surfaced two upstream conversus bugs (claude-code false-fail; anthropic parallel-429) captured separately in `specs/025-knowledge-layer-maturation/conversus/PRESSURE-TEST-FINDINGS.md` + handoff in `CONVERSUS-PR-HANDOFF.md` — orthogonal to D019's contract work.
  - decisions :: .orchestrator/DECISIONS.md:295:- **Choice**: (1) **Installer staging** (`feat(install)`): `packaging/install/install-{claude-code,codex,cursor}.sh` now stage `scripts/`, `templates/`, and `references/` into the target project root at install time, record every placed file in `.orchestrator/installed-files.txt`, and honor `--uninstall` by replaying the manifest (emits `runtime_removed=N` in the UNINSTALLED summary). `references/installation.md` + `docs/getting-started.md` rewritten to drop the stale "skill bundle" claim and document the real install → project layout including Upgrading and Uninstall sections. `tests/test-installer-stages-runtime.sh` added (24 assertions, green) covering install-exits-0, stage-to-disk, manifest-written, re-install-idempotent, CLAUDE.md-untouched, and uninstall-via-manifest. (2) **State tier fix** (`fix(state)`): `scripts/state/find-active-milestone.sh` prefers `<mid>-EVALUATION.md` as the tier authority (per `commands/evaluate.md`) and falls back to `<mid>-ROADMAP.md`. Previously read tier from roadmap only, which returned `tier=none` for evaluated-but-unroadmapped milestones and caused `orchestrator:auto` to bail with "no active milestone" mid-flow on Tier C projects. Verified against bbt-companion M001 — now returns `M001 planning C`. (3) **Ingest FR-slug fix** (`fix(knowledge)`): `scripts/knowledge/ingest-spec.sh` accepts both `**FR-N**` and `**FR-N (slug-or-anything)**` marker forms (extract numeric ID, treat optional parenthetical as decoration), and aborts loudly with `ERROR:` on any line that looks like an FR marker but yields no numeric ID. Previous behavior silently dropped 36 chunks (19 requirements / 8 constraints / 9 non-goals) from bbt-companion's spec because the slugged markers fell through the narrow regex. Loud-failure turns silent data loss into actionable spec-authoring feedback. (4) **Reinit sentinel-preservation fix** (`fix(lifecycle)`): `scripts/lifecycle/reinit-handler.sh` canonicalizes `--project-dir` via `cd && pwd` so basename extraction is correct for relative paths; snapshots every `# >>> orchestrator:NAME >>>` sentinel block (not just `project-identity`) before the regenerate pass and re-injects each one via `dual-write-runtime-md.sh` post-write; reads `runtime_confidence` from `config.yml` first then from the rendered instruction-file line, and refuses to downgrade a previously-established `high`/`medium`/`low` to `unknown`. (5) **Known limitations parked for follow-up** (four items, do NOT auto-promote to milestone scope): (a) reinit template-regenerate model still loses free-form CLAUDE.md prose outside sentinel + `<!-- BEGIN CUSTOM -->` blocks — redesign needed to either switch to targeted-edit or expand sentinel surface area; (b) installer stage count of 840 files includes `scripts/verify/m*-p*-*` fixtures and `tests/fixtures/**` that a consumer project does not need — a stage-filter could drop to ~300 files (low priority); (c) `scripts/state/check-settings-state.sh` returned `state=ok` while the regen pipeline silently failed during bbt-companion install (observed, not root-caused); (d) manifest does not track upstream deletions — a file removed from spec-kit-orchestrator between installer runs leaves a stale copy in the consumer project (accepted trade-off in handoff §6, worth revisiting).
  - decisions :: .orchestrator/DECISIONS.md:298:bbt-companion is the first external project the orchestrator has been installed into at scale, and the install surfaced a class of bugs the `test-s*` suite cannot catch because every `test-s*` runs against the development-tree layout where scripts sit beside their callers. Bundle-infrastructure gap was the root (installer wasn't staging the files `commands/*.md` reference via project-relative paths); three downstream bugs (#2, #3, #4) compounded on top because they'd never been exercised against a genuinely-fresh `.orchestrator/` state tree with no prior chunks or milestones. Fixing as out-of-band D-rows (not a new milestone) because each fix is <50 lines of code, scoped to a single script, and orthogonal to M026. Recording as D021 rather than folding into M026 keeps the conversus-OSS migration audit-trail clean. The four parked limitations (5a-d) get a D-row line instead of immediate follow-up work because (a) reinit redesign is architecturally non-trivial (targeted-edit vs sentinel expansion is a real decision); (b) installer stage bloat is cosmetic; (c) check-settings-state is speculative without reproduction; (d) upstream-removal is acknowledged-accepted. Bringing them up explicitly here means they don't get lost to context-expiry between now and when the operator decides to tackle them.
  - decisions :: .orchestrator/DECISIONS.md:312:paid` closed enum (stderr warning + fall-through on bad values), metadata-probe fallback via `pip show conversus` `Home-page:` parsing, short-circuit `edition=unknown reason=stub` under stub mode. Output contract: `edition=` + `reason=` lines on stdout in stable order, warnings on stderr (DC-5). (2) `<TOOL>_EDITION=<value>` env-var name is reserved as the convention for any future OSS-default-with-paid-escape-hatch tool integration in this repo (graduated as MEM030, `knowledge/conventions/MEM030.md`). The paired edition-resolution two-tier-detection pattern is graduated as MEM029 (`knowledge/patterns/MEM029.md`). (3) Preset frontmatter `edition_required: paid` (M026/P03/T01, `scripts/dispatch/adapters/tool/conversus.sh` `_read_preset_edition_required`) refuses paid-only presets on OSS-resolved binaries before any `conversus run` invocation; diagnostic on stderr matches case-insensitive regex `paid-only.*CONVERSUS_EDITION=paid` (SC-7). Diagnostic uses `FAIL:` prefix per the adapter's `_emit_fail` convention rather than the FR-11 literal `ERROR:` opener — body content satisfies the SC-7 regex regardless. (4) CHANGELOG.md records the migration under v0.9.1 heading. (5) Cross-cuts: M013/P04 observability shape unchanged (additive `edition` field per AD-4 adjacency rule); spec-026 M014 shell-impl Pass 3 wiring is the next consumer that may exercise the new resolver under a fresh-install code path.
  - decisions :: .orchestrator/DECISIONS.md:330:### M020 schema authority and the status: field evolution for knowledge entries { #dr-code-024 }
  - decisions :: .orchestrator/DECISIONS.md:393:- **Choice**: **Skip re-gate; proceed to plan P00.** All three required mitigations (P0 MIT-1 emitter coverage, P0 MIT-2 SC-9 empirical calibration, P1 MIT-3 eval-harness timing) were absorbed structurally into the spec via `--amend` before the gate's unit_close: (a) MIT-1 → US-0 / FR-0a / FR-0b as P00 hard prerequisite gating all tier code (spec line 296, A-3); (b) MIT-2 → SC-9 threshold deferred to P00 section-level distribution probe with `--amend` commit before P01 closes (spec lines 241, 295); (c) MIT-3 → US-7 promoted P3→P2, must ship before/alongside Tier 3 (spec lines 162, 222). Section-level probe input data (`Knowledge=45.9%`, `Task Plan=24.0%`, `mean=16,797 tok` over 169 dispatches) already on disk at `.orchestrator/scratch/m018-telemetry-probe-report.txt` — MIT-2's empirical foundation is captured. P00 carries the proof: its mechanical acceptance criteria (≥95% `dispatch_usage` parity over 20-dispatch sample; probe-derived SC-9 threshold landed via `--amend`) demonstrate the mitigations operationally rather than through another advocate exchange.
  - decisions :: .orchestrator/DECISIONS.md:499:| D003 | M035/P02 | convention | Windows postinstall guard binding (MIT-9 / #Q-G9) | package.json engines + os fields + postinstall uname check | package.json declares engines.node >=14 and os: [darwin, linux] (npm refuses install on win32 with EBADPLATFORM). Belt-and-suspenders: postinstall script also exits non-zero with a clear stderr message on uname -s = MINGW*/CYGWIN*/MSYS*/Windows_NT before invoking install-claude-code.sh. Aligned with #Q-8 deferral: Windows symlink-mode is post-launch M009 territory; until then, Windows is fail-closed at install time. | No |
  - decisions :: .orchestrator/DECISIONS.md:550:  3. **GitHub App migration is a clean fast-follow** if rotation
  - decisions :: .orchestrator/DECISIONS.md:553:     one-secret rotation with no formula changes.
  - decisions :: .orchestrator/DECISIONS.md:638:### D011 — Release cadence: manual stable releases pre-1.0 (operator-policy)
  - decisions :: .orchestrator/DECISIONS.md:830:| D015 | M045 re-entry substrate: process-fresh, not in-session ScheduleWakeup | P01 viability spike (VERDICT PARTIAL) showed in-session ScheduleWakeup re-entry does not reset context and gives no per-rotation relief (weight analog compounds 4->11->18; relief depends on non-rotation-aware harness compaction). Folding M045 into v2b's process-fresh approach: on rotation, hand off to a fresh claude -p re-entry that starts a genuinely fresh context and resumes from disk. P02-P04 mechanism work (deterministic branch, capability detection, safety envelope, observability) carries over ~unchanged; only the re-entry primitive swaps from ScheduleWakeup to claude -p. | architecture | M045/P01 | In-session re-entry is structurally weaker than today's fresh-session re-invoke on the exact axis rotation targets; process-fresh re-entry is the only substrate that restores a genuine per-rotation context reset (spec CON-5 route). Operator-confirmed 2026-07-01. | No |
  - decisions :: .orchestrator/DECISIONS.md:99:- **Choice**: (1) Remove M017 (Conversus Deliberation Gate) from the forward milestone sequence. Revised sequence: **M011 → M012 → M013 → M014 → M009 → M010**. (2) Add M011/P07 (Format-Agnostic Intake & Conversus Fidelity Gate) which lands both the LLM-driven normalizer (any markdown → spec-kit shape) and the reusable `scripts/dispatch/adapters/tool/conversus.sh` adapter. (3) The adapter becomes shared infrastructure: later milestones that want deliberation at specific checkpoints (M013 UAT PR gate, M014 ambiguous-comment classification, roadmap phase-decomposition at Full intensity) invoke it from within their own scope, rather than waiting on a dedicated milestone. (4) Intensity engine owns the policy (when gates fire by default); users own opt-in/out via `--review` / `--no-review`.
  - decisions :: (capped at 5 hits — more may exist)
  - knowledge-entries :: (capped at 5 hits — more may exist)
  - knowledge-entries :: knowledge/conventions/MEM013.md:19:YAML frontmatter with `schema_version` + `type` fields. Body uses `{{placeholder}}` syntax for dynamic values. No hardcoded milestone/phase/task IDs (context-free per FR-074).
  - knowledge-entries :: knowledge/conventions/MEM013.md:21:Summary frontmatter: 15-field base schema for tasks (`schema_version`, `type`, `id`, `parent`, `milestone`, `provides`, `requires`, `affects`, `key_files`, `key_decisions`, `patterns_established`, `drill_down_paths`, `duration`, `verification_result`, `completed_at`); phase/milestone summaries add `observability_surfaces` for 16 fields.
  - knowledge-entries :: knowledge/conventions/MEM014.md:27:**Templates -> Output**: Commands use `templates/*.md` as starting points. Agent fills `{{placeholder}}` values. Template `schema_version` field enables future format migration.
  - knowledge-entries :: knowledge/conventions/MEM018.md:22:- **await-completion / collect-result**: The agent runtime handles task execution and writes artifacts to disk. The orchestrator detects completion via file presence (task summary exists = done).
  - knowledge-entries :: knowledge/conventions/MEM019.md:20:- **Warm** (knowledge/{category}/{entry-id}.md, loaded on scope-match): full detail files with YAML frontmatter and body content. Resolved by resolve-entries.sh from MEM IDs.
  - knowledge-entries :: knowledge/conventions/MEM031.md:10:source_type: schema-evolution
  - knowledge-entries :: knowledge/conventions/MEM031.md:17:# MEM031: Knowledge entry `status:` field vocabulary (M020 schema evolution)
  - knowledge-entries :: knowledge/lessons/MEM028.md:25:## Why the FR-18 shape test missed it
  - knowledge-entries :: knowledge/patterns/MEM003.md:19:9 states derived from file presence on disk (priority-ordered rules in `derive-phase.sh`).
  - knowledge-entries :: knowledge/patterns/MEM007.md:19:`generate-permissions.sh` introspects project toolchain (package.json, Makefile, config files, agent host markers) and emits canonical JSON. `write-permissions.sh` translates to `.claude/settings.json` with additive merge for user-authored files. `check-permissions.sh` detects permission drift. Policy is declarative in `autonomy-defaults.yaml` read via `recipe-parser.sh`.
  - knowledge-entries :: knowledge/patterns/MEM027.md:21:User-scope config files (`~/.claude/settings.json`, `~/.codex/*.toml`, `~/.cursor/*.json`, shell rc files, git `core.hooksPath`) are jointly owned by multiple tools — the host runtime itself, sibling workflow tools (GSD, spec-kit, orchestrator), user hand-edits, and corporate MDM policy. Any installer that writes these files with `printf > "$file"` or `cp` silently destroys sibling configurations and user edits. The regression surfaced at M013/P04/T04 (see MEM026) made this concrete: the orchestrator clobbered GSD's `statusLine`, `SessionStart`, and `PostToolUse` hooks with no warning and no rollback.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:122:  - **await-completion / collect-result**: The agent runtime handles task execution and writes artifacts to disk. The orchestrator detects completion via file presence (task summary exists = done).
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:135:- **[milestone:M005]** [2026-04-12] Autonomy permission pipeline — generate-permissions.sh introspects project toolchain (package.json, Makefile, extension.yml, config files, agent host markers) and emits canonical JSON. write-permissions.sh translates to .claude/settings.json with additive merge for user-authored files. check-permissions.sh detects permission drift. Policy is declarative in autonomy-defaults.yaml read via recipe-parser.sh. AD-19 script-file verification shape: task plan Check: commands must use single-script invocations, not inline compound bash. Established in P07.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:152:- **[milestone:M003]** [2026-04-15] Skip-gracefully fixture pattern for integration tests: synthetic fixture always runs (primary), optional live fixture runs when present or SKIPs cleanly when absent (secondary). Strict/warn split absorbs live-fixture concurrent noise (mtime drift) without masking synthetic-path regressions.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:159:- **[milestone:M021]** [2026-04-17] Dogfood attestation from state on disk: milestone closure gate reads auto-loop-result.txt + execution-log.jsonl + P*-SUMMARY.md and greps for user_prompt/safety_prompt/hook_reject_unexpected events. hook_reject_recovered is deliberately OMITTED from needles because recovery events are the success signal (AD-6). Gate is self-referential-safe — passes during its own phase by skipping in-flight summaries.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:164:- **[milestone:M019]** [2026-04-18] Goodhart pairing enforced at emission time: every unit_close record carries both a cost block (estimated_cost_usd, pricing_version) and a quality block (verification_pass_rate, deviation_count, retry_count derived from existing attempt/outcome/verification_result per AD-3). Schema validator rejects records missing either. Prevents Tier 2 from optimizing cost in isolation.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:168:- **[milestone:M019]** [2026-04-18] Tier 1 metrics emitter schema (AD-4): reserve three enums at record emission time so Tier 2 rollup and Tier 3 backend-actuals land additively without rewriting emitter code. record_type: payload_breakdown | dispatch_usage | unit_close. source: estimate | runtime | aggregate. granularity: task | phase | milestone. Validator scripts/verify/m019-schema.sh enforces.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:190:- **[milestone:M024]** [2026-04-26] Env-override matrix testing for not-yet-shipped milestones: when a script depends on a probe of another milestone's ship state, expose a closed-enum env-override (e.g. M023_SHIPPED_PROBE_OVERRIDE=stub|live) so test fixtures can deterministically exercise both branches. Pair with a synthetic-ROOT positive disk-probe path so the natural ship-detection logic also gets coverage. Reusable for any cross-milestone dependency probe.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:199:- **[milestone:M018]** [2026-04-28] phase-transition.sh --write is the canonical phase-close path (NOT write-summary.sh phase). Write-summary.sh phase updates only the on-disk summary; phase-transition.sh --write atomically updates BOTH the summary and the roadmap checkbox in one transition. P05/T04 hit a SYNC:MISMATCH using write-summary.sh (roadmap and summary diverged); P06/T04 + P07/T03 confirmed phase-transition.sh --write is the right surface. Pair with --body-file=<path> to keep multiline narrative out of CLI args (AD-19 single-script-file shape compliance + AP-009 bash-shape-guard compliance — no inline $(cat ...) or compound chains > 2). Carry forward to all future closing tasks across milestones.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:213:- **[milestone:M031]** [2026-05-01] Per-profile dispatch shape: Quick / Standard / Full each have distinct scope (touched-files-only / module / full) + traversal hops (1 / 2 / unbounded) + Decisions section inclusion (no / no / yes) + glossary slice (touched terms only / touched + adjacent / full) + token budget. Implemented via build-context.sh --profile=quick|standard|full flag (M031/P01) with --meta-out JSON sidecar emitted on every dispatch path (CON-1 invariant — sidecar carries mem_count + total_tokens + profile + compression_applied + snip_applied for downstream consumers like P03 FR-12 stderr summary + P04 AD-19 budget-drift surface).
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:215:- **[milestone:M031]** [2026-05-01] Milestone-close evidence-ledger convention (M031 → M030 lineage): operator-facing transcription of green BATTERY: pass=N fail=0 line + per-SC roll-up table + ISO-8601 timestamp + SC-13 Option A/B record + back-link to runner. Lives at .orchestrator/milestones/M###/M###-ACCEPTANCE-EVIDENCE.md. Acceptance-battery aggregator inheritance: same set -uo pipefail + run_sc(label, path, extra) helper + bash <path> literal-sequence invocation; extra third positional carries trailing flags (e.g. SC-11 --compare); final BATTERY: pass=N fail=M envelope + exit 0 iff fail=0. Option-A-vs-B reading discipline: planner writes OPTION.md selecting one option; aggregator omits the alternate run_sc line entirely (no commented-out dead code, no skip flag); inventory N reflects the option.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:35:- YAML frontmatter with `schema_version` + `type` fields
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:38:- Summary frontmatter: 15-field base schema for tasks (`schema_version`, `type`, `id`, `parent`, `milestone`, `provides`, `requires`, `affects`, `key_files`, `key_decisions`, `patterns_established`, `drill_down_paths`, `duration`, `verification_result`, `completed_at`); phase/milestone summaries add `observability_surfaces` for 16 fields
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:41:- 9 states derived from file presence on disk (priority-ordered rules in `derive-phase.sh`)
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:98:Commands use `templates/*.md` as starting points. Agent fills `{{placeholder}}` values. Template `schema_version` field enables future format migration.
  - knowledge-md :: (capped at 5 hits — more may exist)
  - milestone-summaries :: .orchestrator/milestones/M002/M002-SUMMARY.md:2:schema_version: "1.0"
  - milestone-summaries :: .orchestrator/milestones/M002/M002-SUMMARY.md:28:Delivered the complete three-temperature knowledge architecture replacing the flat KNOWLEDGE.md. 7 phases, 27 tasks across CRUD foundation (P01), lifecycle management (P02), graph relationships (P03), pre-inlined dispatch integration (P04), execution telemetry (P05), model routing (P06), and diagnostics (P07). Key deliverables: pipe-delimited index for O(n) scanning, individual detail files with 12-field YAML frontmatter, staleness decay with 180-day horizon, graph traversal with cycle-safe BFS, manifest-header dispatch payloads with static-first ordering, telemetry passthrough in auto-loop, complexity-based model routing, and full diagnostics pipeline. All verification scripts pass across all phases.
  - milestone-summaries :: .orchestrator/milestones/M003/M003-SUMMARY.md:18:  - "adapter-interface extract() contract for source-format pluggability; tiered milestone layout (active/recent/historical/archived); three-tier root resolution (flag→env→resolver) as standard CLI contract; dual-layout state probe for idempotency; warn-but-continue post-pipeline rebuild; progressive-disclosure ADR documentation in command docs; Tier-1 static+behavioral verify-per-truth with single-file AD-19 invocations; skip-gracefully integration-test pattern; strict/warn split for synthetic-vs-live validation; deterministic SQLite fixtures under 50KB (PRAGMA page_size=1024 + hard-coded ISO-8601); .gitignore negation for test fixtures sharing ignored names"
  - milestone-summaries :: .orchestrator/milestones/M003/M003-SUMMARY.md:2:schema_version: "1.0"
  - milestone-summaries :: .orchestrator/milestones/M003/M003-SUMMARY.md:28:M003 Migration Tool milestone complete. Initial build (P01-P06, commit ad3da8a 2026-04-09) delivered the end-to-end migration pipeline: adapter interface with pluggable source formats (GSD2/GSD v1/spec-kit), per-artifact transforms (knowledge, decisions, requirements, milestones with tiering), and CLI with idempotency + reporting. After M007 (knowledge graph) and M008 (standalone multi-runtime + 5-rule state resolver) landed, a delta audit surfaced three drift points: hardcoded .specify/orchestrator/ paths, unpopulated graph database, single-layout idempotency probe. P07 refit wired migrate.sh to resolve-root.sh --absolute, dropped hardcoded paths in three transforms, added dual-root idempotency probing, wired rebuild-index.sh as final pipeline stage, and documented AD-13/14/15 in commands/migrate.md. P08 validated end-to-end via deterministic synthetic GSD2 fixture (primary) and live lakeledger (secondary, skip-gracefully), new scripts/orchestrator/status.sh CLI wrapper making the roadmap demo sentence literally executable, 8 AD-19-safe verify scripts, and a two-pass integration test — all green. Surfaced and fixed one latent P02 bug (missing content_hash frontmatter) during refit. Retroactive phase summaries added for P01-P06 (originally delivered before phase-summary machinery) based on roadmap Revision Note + commit ad3da8a. Migration pipeline now produces resolver-wired output, populates the M007 graph database, and participates in graph traversal — ready for M009 recipe packaging and M010 cloud dispatch.
  - milestone-summaries :: .orchestrator/milestones/M004/M004-SUMMARY.md:2:schema_version: "1.0"
  - milestone-summaries :: .orchestrator/milestones/M005/M005-SUMMARY.md:2:schema_version: "1.0"
  - milestone-summaries :: .orchestrator/milestones/M005/M005-SUMMARY.md:8:  - "P01: content-hash idempotency (hash.sh, create/update/rebuild hash support); P02: cost transparency (record-telemetry.sh cost_source, aggregate-metrics.sh grouping); P03: payload transform extraction (payload-transforms.sh, manifest-builder.sh pure functions); P04: agent instruction schema (instruction-schema.md, check-instructions.sh); P05: gate verdict protocol (verdicts.sh, hooks.sh verdict parsing, provider-convention.md, check-providers.sh); P06: diagnostic suite (5 new checks, scored run-doctor.sh, extension.yml registration); P07: autonomy permission pipeline (generate/write/check-permissions.sh, AD-19 shape guidance)"
  - milestone-summaries :: .orchestrator/milestones/M008/M008-SUMMARY.md:18:  - "Natural-language scope/risk/complexity pattern matching via keyword tables; pipeline composition via --flag file-or-text inputs for testability; intensity-aware threshold adjustment (Quick tightens, Full loosens); structured dispatch result/error schemas; filename-based adapter auto-discovery; atomic metadata rewrite via awk+mktemp+mv with cksum-sentinel verification; thin wrapper pattern with --dry-run mode; pure resolver pattern (read-only, zero side effects); hermetic standalone e2e via mktemp fixtures (never touch real HOME); runtime adapter HOME guard + project-dir guard; skill generation via single-source-of-truth transform with --check drift detection; project introspection via marker-file presence; user-edit preservation via comment-delimited blocks; top-level pipeline delegation (init orchestrates existing scripts); reinit-with-preservation via awk field-level surgery; wall-clock budget enforcement + hermetic-only static gate."
  - milestone-summaries :: .orchestrator/milestones/M008/M008-SUMMARY.md:25:  - "Per-phase observability surfaces rolled up: intensity recommendation stdout (Quick/Standard/Full + confidence + reasoning); dispatch-interface.sh stdout (dispatch-result) + stderr (dispatch-error with distinct exit codes 2-6); intensity-gate.sh stdout (execute_substeps + skip_substeps); detect-runtime.sh stdout (runtime + confidence); runtime adapter --probe/--hook-config/--dry-run stdout; check-update.sh stdout (installed_version + latest_version + update_available + update_instructions); init-project.sh SUMMARY: line with installed skills count + paths"
  - milestone-summaries :: .orchestrator/milestones/M011/M011-SUMMARY.md:28:M011 (Spec Management) delivers the "paste a spec, get a roadmap" value proposition end-to-end. Seven phases took the orchestrator from an opaque-spec-file consumer to a full ingest→chunk→scope-filter→normalize→fidelity-gate pipeline that treats specs as structured knowledge on equal footing with the existing three-temperature knowledge model (M002/M007). P01–P03 built the deterministic chunker (spec directory tree with six subcategories, SPEC-prefixed natural IDs, nested-directory indexing, create-entry.sh SPEC- namespace support, full markdown→chunk classifier with classify+dispatch decision layer, versioned-ID supersession chains, REMOVED detection, phase-impact REVIEW emission). P04 wired scope-filter.sh to resolve `spec/*` scope tags (graph-mode + spec-scope-tags mode), letting build-context.sh deliver scope-filtered spec chunks in dispatch payloads per Constitution Principle I. P05 wired chunks-first evaluation and intensity-aware roadmap generation into commands/evaluate.md and commands/roadmap.md (spec-metrics.sh counts non-superseded chunks; spec-story-graph.sh emits directional story-to-story depends_on edges via bidirectional-traverse + directional-frontmatter-filter composition; intensity-gate.sh roadmap stage with Quick/Standard/Full modes). P06 delivered the user-facing commands/ingest.md, four doc-shape verify scripts, a full sandbox E2E gate script, a timing harness (<60s on representative specs), and dogfood evidence against specs/016-autonomous-hardening/spec.md (20 chunks, 7s elapsed). P07 closed with format-agnostic intake and a reusable Conversus fidelity gate: detect-spec-shape.sh probe classifier, normalize-spec.sh thin wrapper dispatching through dispatch-interface.sh (no hardcoded LLM calls), templates/spec-normalizer-prompt.md, a reusable Conversus adapter (scripts/dispatch/adapters/tool/conversus.sh with check/gate/parse-verdict subcommands, graceful degradation, CONVERSUS_STUB escape, three-value exit contract 0/1/2), commands/conversus-gate.md designed for M013/M014 reuse with different presets, templates/conversus-presets/normalize-fidelity.yml, templates/gate-result.md, and integration into commands/ingest.md as a pre-chunker 6-step pipeline (detect-shape → normalize → resolve-policy → fidelity-gate → chunker → rebuild) gated by a new `ingest` intensity stage (Quick=normalize; Standard/Full=normalize+fidelity-gate). A 222-line user guide (docs/ingesting-arbitrary-specs.md) covers flag semantics, BLOCK resolution, stub modes, and preset extension.
  - milestone-summaries :: .orchestrator/milestones/M012/M012-SUMMARY.md:16:  - "AD-1 D011 ship cross-refs only (defer review-state + query surface to M020); AD-2 deploy to same repo gh-pages + same Discussions; AD-3 SSOT via include-markdown plugin (no copy, no symlinks); AD-4 archive included labeled with no date cutoff; AD-5 Giscus mapping=pathname + documented remap script; AD-6 all phases/tasks included with collapsible per-milestone nav; AD-7 MkDocs + Material + include-markdown-plugin + Giscus + GitHub Pages framework stack locked; AD-19 single-script-file Check shape with MEM004 carve-out for scanner/diagnostic/wrapper internals; D011 mechanical evaluation rule (1 of 3 criteria -> M020 promoted); Constitution VI state-on-disk-is-truth preserved via include-only projection; Constitution XIV no-speculative-complexity (no flags, no plugin drivers, no YAML-driven wrappers); Constitution XV surgical precision (each phase touches disjoint file surfaces; cross-phase additions via marker-bounded blocks + EOF-append); MEM001 Bash 3.2 discipline across all shipped scripts; loud-fail-over-silent-empty for ENV empty defaults; SC-10 self-contained wiki/ + sibling-gate allow-list extension policy; SC-12 pending-sentinel deploy record for operator-gated outcomes"
  - milestone-summaries :: .orchestrator/milestones/M012/M012-SUMMARY.md:39:- **`scripts/wiki/` three-stage pipeline** — `wiki-scan-sources.sh` emits `<category>|<rel-path>|<title>` records for every in-scope `.orchestrator/**.md` in stable lexical order with exclusion policy enforced (`scratch/`, `tmp/`, `config/`, `PLANNING-PAYLOAD`, `VERIFICATION`, non-`.md`); `wiki-generate-stubs.sh` writes ≤ 25-line include stubs per artifact plus per-section indexes; `wiki-generate-nav.sh` atomically splices a deterministic `nav:` block between `# >>> M012-P01 nav` markers with seven fixed top-level sections (Home / Constitution / Decisions / Knowledge / Milestone Summary / Milestones / Archive — Archive conditional on scanner records). `wiki-serve.sh` launcher adds `--probe` for headless config validation.
  - milestone-summaries :: .orchestrator/milestones/M012/M012-SUMMARY.md:40:- **P01 verify suite** — 9 gates (`wiki-self-contained`, `requirements-pinned`, `include-plugin`, `ssot`, `exclusion-policy`, `nav-structure`, `serve-smoke`, `index-placeholder`, `bash32-compat`) + `m012-p01-phase-suite.sh` orchestrator. `serve-smoke` SKIPs gracefully when `mkdocs` is absent.
  - milestone-summaries :: .orchestrator/milestones/M012/M012-SUMMARY.md:50:- **P02 verify suite** — 9 gates (`link-rewrite-config`, `mem-stubs`, `mem-anchors`, `link-check-contract`, `link-check-help`, `readme-policy`, `link-check-smoke`, `bash32-compat`, `d011-evaluation`) + `m012-p02-phase-suite.sh` orchestrator. Mkdocs-dependent gates use SKIP-as-PASS on hosts without mkdocs.
  - milestone-summaries :: .orchestrator/milestones/M012/M012-SUMMARY.md:93:- **Milestone validator** — `scripts/verify/validate-milestone.sh .orchestrator/milestones/M012` — **pass** (all four phase summaries present, boundary maps consistent, key files on disk).
  - milestone-summaries :: .orchestrator/milestones/M013/M013-SUMMARY.md:12:  - "M014 (spec management — inherits github-common.sh helper API + sidecar schema + sync wrapper); M020 (Knowledge-Layer Boundary established — chunk-ID authority preserved; spec/defect category extended); M009 (launch-readiness audit — consumes RUNTIME-ASSUMPTIONS.md FR-12 v1 entry); M010 (Codex/Cursor parity for post-verify hook — fast-follow demand-driven post-launch); M019 Tier 2/3 (M013 emits unit_close + conversus_gate_invocation in Tier 1 shape; Tier 2/3 will aggregate); future GSD/orchestrator users (opt-in projection only — orchestrator state on disk remains authoritative); operator workflow (one-time orchestrator:github init bootstrap + post-verify hook auto-sync on transition); .orchestrator/DECISIONS.md (D014 Knowledge-Layer Boundary + D015 P02/P03 scope split); references/github-integration.md as cross-phase byte-identity reference contract"
  - milestone-summaries :: .orchestrator/milestones/M013/M013-SUMMARY.md:16:  - "D014 — Knowledge-Layer Boundary: M020 retains chunk-ID authority; M013 cross-links the M020 schema and never redefines/extends it (zero writes to knowledge/spec/** from any P01–P04 task); FR-9 additive-only emit pinned to existing SPEC-* frontmatter; D015 — P02/P03 scope split applied as load-bearing markers (_deferred to P03_ cells fillable in place); FR-12 Claude-Code-only v1 binding via Constitution X+XII (Codex/Cursor parity demand-driven post-launch via M010); projection-not-peer grounding via XIV+M007+D007 (sidecar operator-owned + gitignored; in-repo template); FR-11 reversibility (all writes have absent/pending exit paths; lock released before exit 3/4); FR-16 rate-limit pre-flight + auth-expiry exit-code contract; FR-17 cost emission in M019 Tier 1 shape with source=runtime hard-coded; FR-18 status --verify-cache as read-only divergence probe; AD-19 single-script-file Check shape preserved across all verify gates with comment-discipline synonyms keeping bash32 scanners self-clean; conversus adapter --strict mode added as reusable invariant for callers requiring adapter presence (M011/P07 graceful-degradation flipped off via --strict / CONVERSUS_STRICT=1)"
  - milestone-summaries :: .orchestrator/milestones/M013/M013-SUMMARY.md:30:M013 ships the GitHub-native integration: orchestrator state projects to GitHub Issues + Project v2 + Milestones with FR-4 byte-identity markers, a sync cycle wires lifecycle hooks for closure, UAT bug ingestion closes the loop back to spec chunks, and an opt-in conversus PR gate enforces adversarial review at merge points. Four phases (P01 → P02 → P03 → P04), 25 task summaries, ~30 verify gates across four phase-suite orchestrators. The integration is **opt-in** via an operator-owned sidecar (gitignored at `.orchestrator/integrations/github.json`); orchestrator state on disk remains authoritative — GitHub is a projection, not a peer (Constitution XIV + M007 + D007).
  - milestone-summaries :: .orchestrator/milestones/M013/M013-SUMMARY.md:32:The milestone's binding pre-discuss commitments (D014, applied via conversus red-blue pressure test of `specs/023-github-native-integration/spec.md`) all landed: **FR-12 Claude-Code-only v1** (Codex/Cursor parity demand-driven post-launch); **Minimal Slice subsection** (P01 ships an end-to-end demo before any sync surface); **FR-9 narrowed to additive-only emit** with chunk-IDs pinned to existing `SPEC-*` frontmatter (Knowledge-Layer Boundary preserves M020 chunk-ID authority); **FR-16** rate-limit + auth-expiry exit-code contracts; **FR-17** cost emission via M019 Tier 1 shape; **FR-18** `status --verify-cache` read-only divergence probe.
  - milestone-summaries :: .orchestrator/milestones/M013/M013-SUMMARY.md:39:- **P04 — Sync + post-verify hook + conversus UAT PR gate** (6 tasks, 305m). 626-line `github-sync.sh` (state walker + reconcile loop + lock-acquired live mode + dry-run manifest + `updateProjectV2ItemFieldValue` + sidecar cache update at upsert sites) + FR-16 pre-flight rate-limit probe + per-call `classify_gh_rc` wrapper releasing the lock before exit 3/4 (FR-11 reversibility) + FR-17 `unit_close` JSONL emitter (M019 Tier 1 shape, `source=runtime` hard-coded) + post-verify hook descriptor (`packaging/bundle/hooks/post-verify.json` + `scripts/lifecycle/after-verify-sync.sh` wrapper + `claude-code.sh` runtime adapter `hook_count` 5→6) + opt-in `github-conversus-gate.sh` wrapping the M011/P07 conversus adapter `--strict` with 30s external watchdog (Constitution XII belt-and-suspenders) + FR-13 preset `templates/conversus-presets/m013-uat-defect-merge.yml` (red-blue defect-advocate vs fix-advocate, BLOCK-on-tie) + FR-18 `--verify-cache` read-only divergence probe + 12-gate phase suite + FR-5 lint regression guard.
  - milestone-summaries :: .orchestrator/milestones/M013/M013-SUMMARY.md:8:  - "opt-in GitHub native integration projecting orchestrator state to Issues + Project v2 + Milestones with FR-4 byte-identity markers; operator-owned sidecar at .orchestrator/integrations/github.json (gitignored) with templates/github-integration-sidecar.json schema (sync_mode + sub_issue_mode + per-item cache); tri-state orchestrator:github status command (absent / pending-operator-complete / configured) + status --verify-cache FR-18 read-only divergence probe; orchestrator:github init --re-init FR-14 adoption branch sourcing existing repo state via marker search; orchestrator:github sync 626-line walker + reconcile loop with --dry-run manifest + lock acquisition + updateProjectV2ItemFieldValue Project v2 status closure + FR-16 pre-flight rate-limit probe + per-call classify_gh_rc wrapper releasing lock before exit 3/4 (FR-11 reversibility); UAT bug ingestion loop (.github/ISSUE_TEMPLATE/uat-bug.yml + scripts/integrations/uat-ingest.sh + knowledge/spec/defect/SPEC-DEFECT-NNN schema with chunk-lookup-failed sentinel); FR-9 additive ## Spec Chunks emit in scripts/knowledge/rebuild-index.sh + scripts/knowledge/lib/index-utils.sh emit_spec_chunks_section helper; opt-in conversus UAT PR gate scripts/integrations/github-conversus-gate.sh wrapping M011/P07 adapter --strict with 30s external watchdog (Constitution XII) + FR-13 preset templates/conversus-presets/m013-uat-defect-merge.yml (red-blue defect-advocate vs fix-advocate, BLOCK-on-tie); post-verify lifecycle hook packaging/bundle/hooks/post-verify.json + scripts/lifecycle/after-verify-sync.sh wrapper + claude-code.sh runtime adapter hook_count 5->6 + FR-12 v1 Claude-Code-only enforcement via negative-grep guard on codex/cursor adapters+installers; scripts/integrations/github-common.sh shared lib (12 P02 helpers + 1 P03 helper + 5 P04 helpers — orchestrator-id derivation + FR-4 marker primitives with shasum byte-identity + AWK depth-aware sidecar item upserts + http_probe + sidecar_update_item_cache + emit_tier1_record + classify_gh_rc + emit_conversus_gate_record + gh_marker_search_remote + auth/sub-issue/label-collision preflights); scripts/verify/graphql-call-shape.sh FR-5 three-shape whitelist CI lint; references/github-integration.md full P01..P04 reference doc with Auth Modes / Sub-Issue Representation Modes / Full Mapping Table / Re-init Adoption Contract / Sync Workflow / Sync Action Mapping / Scope Boundary table; commands/github-init.md + commands/github-sync.md + commands/github-status.md (MEM012 structure); FR-17 unit_close + conversus_gate_invocation JSONL emitters at ${ORCHESTRATOR_ROOT}/observability/tier1.jsonl in M019 Tier 1 shape; scripts/integrations/sidecar-init-pending.sh pending-sentinel bootstrap with clobber-refusal exit 2; ~30 verification gates across 4 phase-suite orchestrators (P01 9 + P02 8 + P03 8 + P04 12 + FR-5 lint regression guard)"
  - milestone-summaries :: .orchestrator/milestones/M014/M014-SUMMARY.md:16:  - "D016 (M014 extended scope — native orchestrator:specify + AGENTS.md dual-write + comment classifier); D017 (14 MITs applied pre-discuss via cooperative conversus deliberation); D018 (P03 deferred pending M012 wiki deploy + inbox dogfood; retired by D023); D019 (three-pass scaffold→author→gate flow intensity-scaled per existing intensity engine + universal TODO pre-flight guard on conversus gate adapter); D020 (spec-metrics chunks-first scoping bug + scaffold-placeholder-in-backticks gate false-positive); D023 (M014/P03 unblock — synthetic P99 placeholder retired in favor of explicit Preflight note + SC-16 relaxed to best-available signal with regex/heuristic v1 classifier pin and retune trigger ≥30 actioned comments OR ≥20 percent calibration divergence); byte-identical dual-write (not transform-based) between CLAUDE.md and AGENTS.md across all P02 write-sites; loud-error on slug collision (not silent overwrite); stub-to-full transition contract (P01 stubs emit stable structured fields with zero values so downstream phases replace body without changing caller contract — used for spec-complexity-probe, commands/specify subcommands, RUNTIME-ASSUMPTIONS entries, references/spec-management sections); append-only registry discipline for RUNTIME-ASSUMPTIONS (multiple phases append entries, heading byte-identical across body refreshes); hardening-spec exception keyed on fr_count==0 AND user_story_count>=5 (M016/M021 pattern); OR-semantics across five complexity axes with boolean hardening override; D007 reuse discipline honored throughout — conversus adapter byte-identical pre/post M014 despite two new presets landing; two-tier SC-5 invariant verification (source-level grep in scripts/comments excluding apply.sh + runtime-event assertion in pipeline verifier); patch -N forward-only for stale-diff probe (vanilla --dry-run auto-reverses silently); env-overridable sub-script paths (COMMENTS_FETCH, COMMENTS_CLASSIFY, COMMENTS_ADAPTER, COMMENTS_UAT_INGEST, COMMENTS_APPLY, COMMENTS_REJECT, COMMENTS_TRIAGE) for hermetic testability without D007 violation; deterministic content-addressed queue/triage filenames (shasum-256 first 8 hex of comment URL); adapter-missing as routing verdict not failure (M013/FR-13 graceful-degradation at comments seam)"
  - milestone-summaries :: .orchestrator/milestones/M014/M014-SUMMARY.md:18:  - "Template SSOT with ground-truth heading fixture; marker-bounded atomic splice with byte-preserved outside region (SC-6a); subcommand surface with deferred body (diagnostic-stub exits distinct codes; full semantics land in later phase without command-file re-authoring); hermetic scratch project pattern for end-to-end command testing; verifier self-exemption for rule-embedding gates; awk-field extraction for structured corpus parse; write-site enumeration discipline with scanner gate; allow-list pattern for orthogonal writes (runtime-native full-file renders); hermetic-root via dirname of ORCH_ROOT (decouples dual-write target from repo root); advisory-first drift severity model; one-time migration script pattern with --dry-run/--apply/--force + idempotent already-migrated short-circuit; CC-first with defensive fallback (runtime gate + empty-response bailout — never fails caller); STATE_ROOT vs PROJECT_ROOT separation for mutation sinks vs dependency lookups; CONVERSUS_STUB hermetic testing via fixture-copy path in adapter; dispatch-interface mock via backup-and-swap; section-split awk + per-section classifier; append-with-body-refresh for registry entries (heading byte-identical body refreshed); bash32 scanner comment-strip to avoid false-positive on comment prose; hermetic-test stub via env-var pointing at JSONL fixture (GH_API_STUB, GH_GRAPHQL_STUB); per-surface source_surface filter on shared stub fixture; FR-19 dry-run JSONL action records as no-disk-write counterpart; grep -F literal-string match for URL dedup; stdout-verdict + stderr-INFO split; single-agent cooperative preset for refinement-only LLM calls; patch -N forward-only stale-diff probe; env-overridable sub-script-path hermetic-test pattern (COMMENTS_*); non-executable adapter as routing verdict (graceful-degradation test pattern); deterministic content-addressed filenames for idempotent queue/triage; two-tier must-not-happen invariant verification (source grep + runtime event assertion); bash32+lint omnibus with dynamic enumeration + basename self-exemption; zero-prompts hermetic scratch + fixture replay under --yes; outside-marker shasum invariant pre/post dual-write; phase-suite orchestrator with IFS-newline iteration + per-gate rc capture + FAILED_GATES accumulator"
  - milestone-summaries :: .orchestrator/milestones/M014/M014-SUMMARY.md:8:  - "Native orchestrator:specify command (P01 create-path + D019 three-pass scaffold/author/gate contract), AGENTS.md dual-write at every CLAUDE.md write-site (P02) with SC-6a outside-markers byte-preservation invariant + FR-13 drift detector, comment→workflow classifier with four-class regex/heuristic v1 baseline per D023 and human-gated spec-amendment apply path (P03), conversus auto-propose for risky drafts with full FR-5 complexity probe + FR-6 pressure-test preset + FR-7 decomposition + FR-14 three-case --amend (P04). commands/specify.md + commands/comments.md + commands/doctor.md extension; scripts/specify/specify.sh + scripts/comments/fetch|classify|apply|reject|triage|comments.sh; scripts/util/dual-write-runtime-md.sh; scripts/knowledge/spec-complexity-probe.sh; scripts/verify/spec-shape-lint.sh; scripts/diagnostics/check-docs.sh --check drift; templates/spec-template.md FR-2b SSOT + templates/spec-scaffolder-prompt.md FR-3 + templates/conversus-presets/spec-pressure-test.yml + templates/conversus-presets/classify-comment.yml + templates/spec-complexity-contradiction-prompt.md + templates/spec-splitter-prompt.md; .orchestrator/config.yml specify: section + comments: section + dual_write_agents top-level key; references/spec-management.md (SC-11 complete with Comment Classification section); tests/test-specify-shape.sh (FR-18) + tests/test-dual-write-outside-invariant.sh (SC-6a); RUNTIME-ASSUMPTIONS.md registry (D016 scaffold) with FR-3, FR-5-full, FR-7 entries; specs/024-spec-management-extended/planning-inputs/inbox-dogfood.md (SC-16 best-available per D023). Closes the M015 bootstrapping loop: every future milestone's spec authored via orchestrator:specify + reviewed via orchestrator:comments."
  - milestone-summaries :: .orchestrator/milestones/M021/M021-SUMMARY.md:49:| SC-6 | Fresh ≥4-task phase produces zero prompts under default settings | PASS — P04 itself (5 tasks) executed prompt-free |
  - milestone-summaries :: .orchestrator/milestones/M024/M024-SUMMARY.md:53:- **D025 (M024/P07)**: `pending_design_authored_manually` transient frontmatter key for the FR-7 manual-branch halt-and-flip cycle. Closed-enum `true | false`, initialized false on every fresh emit, mutated only by `design-gate-degradation.sh --branch manual`. Anchors M024's schema-evolution authority over the intake-proposal frontmatter (parallel to M020's authority over knowledge-entry frontmatter per D024).
  - milestone-summaries :: .orchestrator/milestones/M026/M026-SUMMARY.md:12:  - "downstream consumers M013/M014/M018/M023/M024 inherit OSS-by-default behavior with paid escape hatch; spec-026 (M014 shell-impl Pass 3) is the next consumer that may exercise the new resolver under a fresh-install code path"
  - milestone-summaries :: .orchestrator/milestones/M030/M030-SUMMARY.md:16:  - "D-A1 4-verdict closed-enum (flip_recommendation ∈ {ready, partially_ready, block, evidence_insufficient}); D-A2 programmatic shadow-compare invocation as the FR-9 flip-gate (live routing refuses adapter call if corpus below threshold; emits override_source=shadow_gate_blocked); D-A3 partial-flip safety (only smart-defaulted classes may be enumerated in withheld_classes — fast/balanced classes flip wholesale or block); D-A4 mechanical independence (P00 fixture corpus committed BEFORE P01 classify-task.sh authored — independence-by-construction; verifier graduates post-P01 to git-log ordering check); D-A5 kill-switch precedence over min_tier (model_routing_enabled: false short-circuits before flip-gate or escalation logic; stderr names the bypassed min_tier); D-A6 cost_rates SSOT in templates/model-routing.yml (single source of truth for rollup + counterfactual computation); D-A7 / SC-3a write-path correctness (every shadow record's model_routed agrees with an independent classifier run on the same plan; classifier_confidence emitted end-to-end in P02 not deferred to P03); D-A8 escalation cap CON-5 (3 attempts max: fast → balanced → smart → escalation_cap_hit; no fourth record); D-A9 anomaly-record JSONL append-only convention (separate file .orchestrator/anomalies.jsonl preserves CON-6 dispatch-stream invariant on execution-log.jsonl); P01 classifier-confidence stability metric definition (rolling variance ≤ 0.10 + N=20 minimum class-coverage; consumed end-to-end by shadow-compare.sh); P02 4-verdict closed-enum + shadow-corpus path-resolution priority (env var M030_SHADOW_COMPARE_CORPUS → --corpus flag → default); P03 override-source closed-enum closure + kill-switch precedence over min_tier (compound config tested via SC-7a); P04 escalation-cap programmatic enforcement + flip-gate refusal of below-threshold corpus; P05 cost_rates-absent fallback (warning + zero-savings line) + ORCHESTRATOR_ROOT carve-out for footer fixtures; P06 #Q-4 threshold default (pass_rate 0.50 + min_class_sample 10) + env-only JSONL emit path (M030_ANOMALIES_JSONL_PATH default .orchestrator/anomalies.jsonl) instead of CLI flag; P07 4-task split (corpus + battery + ledger + close-ceremony) + structurally-distinct T04 ceremony fusing phase + milestone close into one atomic commit"
  - milestone-summaries :: .orchestrator/milestones/M030/M030-SUMMARY.md:18:  - "pre-implementation fixture corpus + version-controlled labels (P00 — closed-milestone-only sourcing avoids in-flight bias; rubric application per-plan with judgment, no automated keyword pre-pass; rationale field as audit trail); classifier-as-pure-bash + heuristic-table-as-SSOT (P01 — deterministic <100ms no-network with no jq/python3 hard dependency; bash 3.2 compatible); 4-verdict closed-enum shadow-compare (P02 — flip_recommendation always ∈ {ready, partially_ready, block, evidence_insufficient}, no string interpolation); additive-jsonl-schema CON-2/FR-19 preserved across 6 schema extensions (P02 added 4 fields, P03 added 1, P04 added 3, P06 added 1 — all additive, all preserve byte-equality on shadow-off branch); pre-amendment golden-baseline + delegate-and-pass-through cross-phase wrappers (P05/T01 inverted P04/T01 pattern: capture HEAD output BEFORE amendment, delegate post-amendment SC-11 verifier to upstream P02 wrapper); ORCHESTRATOR_ROOT carve-out for fixture-routing without modifying the surface's resolver (P05/T01); additive-field-on-shadow-on-emit lineage (P02/T02 + P04/T03 + P06/T02 — single field appended to printf format string + arg list on shadow-on branch only; shadow-off branch byte-untouched; SC-11 contract via P02 delegate-and-pass-through wrapper); env-var-seam-for-{shadow-mode,corpus-path,routing-table-path,anomaly-jsonl-path,allow-dirty-mark} convention (M030_SHADOW_MODE / M030_SHADOW_COMPARE_CORPUS / M030_ROUTING_TABLE_PATH / M030_ANOMALIES_JSONL_PATH / ORCHESTRATOR_ALLOW_DIRTY_MARK); phase-suite straight-line aggregator pattern (P02–P07 all share the no-loops-no-eval-no-compound-chains-AD-19-clean shape; per-gate parallel scalars + bash <path> + rc=$? + if-block accumulation; single SUMMARY: line); plan-amendment-not-task-reopen pattern applied at every phase close for artifact-grep predicate divergences (line-count relaxation + key-link source inversion to forward-direction reachable file with parenthetical reason); structurally-distinct-T04 ceremony fusing phase + milestone close (P07 only — when final phase IS milestone-close gate); idempotent corpus synthesizer + sha256-equality verifier (P05/T01 + P06/T01 + P07/T01 lineage); acceptance-battery as straight-line SC delegator (P07/T02); one-shot evidence ledger discipline (P07/T03 — committed snapshot at milestone close, never regenerated dynamically; operators re-run the contract for fresh stdout); milestone-grain unit_close convention (P07/T04 — first-of-kind record introduced; granularity=milestone + phase_count=N); pre-amendment-tolerant verifier pattern (P03/T01 — same verifier flips from tolerant to strict as deliverable lands); CON-3 closure verifier (P02/P03/P04 — runtime awk-extraction of resolution table from templates/model-routing.yml prevents hardcoded model IDs; HEAD-vs-WT per-pattern grep counts mechanically re-checkable); append-only invariant on JSONL via inode preservation + first-N-lines bit-identity + line-count delta = +1 (P02/P04/P06)"
  - milestone-summaries :: .orchestrator/milestones/M030/M030-SUMMARY.md:25:  - "scripts/diagnostics/metrics-rollup.sh --by-model (per-tier dispatch counts + aggregated_cost_usd + counterfactual_all_smart_cost_usd lines); scripts/diagnostics/efficiency-footer.sh model_mix: line; scripts/diagnostics/doctor.sh --config-check (routing-table syntax validation with file:line diagnostics); scripts/diagnostics/check-anomalies.sh model_routing_regression text + JSONL record (rolling-window per-class verifier-fail-rate); scripts/diagnostics/shadow-compare.sh 4-verdict flip_recommendation= line; .orchestrator/milestones/M030/M030-ACCEPTANCE-EVIDENCE.md one-shot ledger; tests/m030-acceptance/run-acceptance-battery.sh BATTERY: pass=N fail=N canonical line; classifier-confidence stability metric in shadow-compare per-class evidence lines; .orchestrator/anomalies.jsonl append-only stream"
  - milestone-summaries :: .orchestrator/milestones/M030/M030-SUMMARY.md:38:P04 (Live routing + verifier-fail escalation + flip-gate enforcement, 257m) amended `dispatch-interface.sh` with the live-routing branch (FR-9 with programmatic shadow-compare invocation per D-A2; per-class partial-flip authorization per D-A3) + escalation logic (FR-10 with CON-5 hard-cap of 3 attempts: fast → balanced → smart → `escalation_cap_hit`); added 3 JSONL fields (`escalation_count`, `escalation_reason`, `shadow_gate_blocked` verdict) + the `escalation_cap_hit` record type. Patterns: live-routing branch short-circuits at kill-switch check before flip-gate or escalation logic (CON-4 amended); FR-9 programmatic flip-gate enforces shadow-corpus threshold mechanically; escalation produces NEW records with new timestamps (CON-6 append-only invariant preserved).
  - milestone-summaries :: .orchestrator/milestones/M030/M030-SUMMARY.md:8:  - "P00 — version-controlled classifier ground-truth fixture corpus at tests/fixtures/m030-classifier-corpus/labels.yml (40 hand-labeled pre-M030 PLAN.md references; 20 mechanical / 15 standard / 5 novel) + README.md methodology + 5 corpus-shape verifiers under tools/verify/p00-*.sh; P01 — scripts/dispatch/classify-task.sh (deterministic <100ms no-network classifier emitting character + confidence) + templates/model-routing.yml SSOT (routing + per-runtime resolution + cost_rates) + references/model-routing.md operator docs + classifier-confidence stability metric (rolling variance ≤ 0.10 + N=20 minimum class-coverage); P02 — scripts/dispatch/dispatch-interface.sh shadow-mode hook (CC-only gated on M030_SHADOW_MODE=1 && CLAUDECODE=1) + 5 additive JSONL fields (model_routed,model_used,partial_flip_active,withheld_classes,classifier_confidence) + scripts/diagnostics/shadow-compare.sh (4-verdict closed-enum aggregator: ready|partially_ready|block|evidence_insufficient); P03 — operator-overrides resolution path (kill-switch → plan-frontmatter → milestone-floor → none precedence chain) + override_source closed-enum field + references/model-routing.md ## Operator Overrides; P04 — live-routing branch + verifier-fail escalation (FR-10 with CON-5 hard-cap) + FR-9 programmatic flip-gate enforcement + escalation_count/escalation_reason/shadow_gate_blocked JSONL fields + escalation_cap_hit record type; P05 — scripts/diagnostics/metrics-rollup.sh --by-model + scripts/diagnostics/efficiency-footer.sh model_mix: line + scripts/diagnostics/doctor.sh --config-check; P06 — scripts/diagnostics/check-anomalies.sh model_routing_regression check (rolling-window per-class verifier-fail-rate) + dispatch-interface character emit + .orchestrator/anomalies.jsonl emit path + references/model-routing.md ## Anomaly Records; P07 — tests/m030-acceptance/shadow-corpus-fixtures.sh idempotent synthesizer + 4 corpora (corpus-50-per-class.jsonl 150 records, corpus-zero.jsonl, corpus-2-class-only.jsonl 100 records, corpus-block.jsonl 60 records) + tests/m030-acceptance/run-acceptance-battery.sh end-to-end SC runner (BATTERY: pass=22 fail=0 across 14 SCs) + .orchestrator/milestones/M030/M030-ACCEPTANCE-EVIDENCE.md one-shot ledger + 10 P07 verifiers + p07-phase-suite.sh aggregator + M030-VALIDATED marker + this M030-SUMMARY.md"
  - milestone-summaries :: .orchestrator/milestones/M035/M035-SUMMARY.md:61:- **#Q-8 (symlink-mode platform)** → Unix-only at v1, copy-mode is platform-agnostic (P01 T01).
  - milestone-summaries :: .orchestrator/milestones/M036/M036-SUMMARY.md:13:- **P03 (Tier 2 LLM extraction + M030 routing + conversus fidelity gate — closed 2026-05-02; live-LLM smoke 2026-05-07)**: `auto`-mode summary-mode flips on `tier=2` to invoke an M030-routed LLM extractor; resulting structured Markdown is gated through a conversus cooperative deliberation (extractor-advocate + fidelity-advocate + constitution-grounded arbiter) returning PASS or BLOCK. PASS promotes `.structured.md` to the chunk store; BLOCK retains only a `block.md` audit log (FR-18 "no silent failure"). Two test-mode seams preserve CON-3 closure: `EXTRACT_TIER_2_DISPATCH=stub:pass|stub:block|live` for LLM dispatch and `CONVERSUS_STUB=1` + `CONVERSUS_STUB_VERDICT=PASS|BLOCK` for the gate. Live-LLM smoke test against `tests/fixtures/m036-live-llm-smoke/policy-data-retention.md` exercised 2026-05-07 — YELLOW verdict in `M036-ACCEPTANCE-EVIDENCE.md` (pipeline works end-to-end; three operator-facing caveats deferred to post-launch follow-up).
  - milestone-summaries :: .orchestrator/milestones/M036/M036-SUMMARY.md:27:- **#Q-8 (per-category default tier)** — `cms-rule: 2`, `training-material: 2`, `glossary: 2`, `regulatory-doc: 1` (P00 `references/reference-source-types.yaml`).
  - milestone-summaries :: .orchestrator/milestones/M036/M036-SUMMARY.md:32:- **Live-LLM smoke deferred-validation acknowledgment**: M036-ACCEPTANCE-EVIDENCE.md captures the 2026-05-07 live-LLM run against `tests/fixtures/m036-live-llm-smoke/policy-data-retention.md`. YELLOW verdict — pipeline works end-to-end; three operator-facing caveats deferred to post-launch follow-up.
  - milestone-summaries :: .orchestrator/milestones/M037/M037-SUMMARY.md:18:  - "Iterative polish loop under live dogfood signal (P03): each round = (1) PBJ install+observe+file defects, (2) tactical bundle commit, (3) push+SHA-range-ping, (4) PBJ retest. Loop terminates on satisfaction signal. Trade-off: faster turnaround on visible-defect remediation; weaker artifact trail (round commits, no per-round task plan) — closure pays the artifact cost retroactively. Pattern reusable when a phase's success contract is dogfood-acceptance rather than mechanical FR coverage,Cross-bundle staging-gap detection: if a bundle change ships content (round-3.5 wiki/overrides/) but install/init paths short-circuit on the surface (operator-owned oracle + PRE_STAGE_NO_OP), the change reaches greenfield only — existing projects need a framework-managed refresh step. Round-4 added that step explicitly for wiki/overrides/. Reusable diagnosis-and-fix pattern for any bundle-staged content type,yaml-merge list-element preservation gap (round-5 discovery): managed top-level key with present sub-key whose value is a YAML list — yaml-merge preserves target's list byte-identical (operator-wins-byte-identical at the sub-key level). Dropping a list element from the framework default does not propagate to existing projects. Pattern: any framework-default change that drops/replaces list elements requires either (a) operator-side manual edit + diagnostic, or (b) yaml-merge primitive extension to support list-managed-namespace semantics. Deferred post-launch,Retroactive plan/summary authoring at closure (P03): when a phase ships as direct commits without going through orchestrator:plan-phase + dispatch, the closure step authors P0X-PLAN.md + P0X-SUMMARY.md retroactively documenting actually-shipped scope. Acceptable when (a) phase scope was de-facto narrowed by external signal mid-flight, (b) the rounds were small enough that per-round task plans would have added overhead without much insight, and (c) the demo sentence is satisfied by an external acceptance signal. NOT acceptable as a default,Card-grid template primitive established in P01 (templates/wiki-index-cards.md.tmpl) — config-only customization via wiki.landing_cards: schema. P03 reuse for /knowledge/ deferred via FR-14 to post-launch but the primitive is in place,Workflow-based Pages publishing handoff-doc-verbatim pattern: P02 emit_pages_workflow() copies .github/workflows/pages.yml byte-identical from PBJ commit e7a722e reference impl. CON-3 no-clobber on pre-existing workflow file. Single-quoted heredoc preserves ${{ ... }} interpolation. Reusable pattern for any future install-template-emitted GitHub Actions workflow,wiki-deploy.sh demote pattern: legacy live-deploy primitive demoted to pre-push validation only — gates 1-4 retained, gate 5 (mkdocs gh-deploy --force) removed, replaced with OWNER/REPO resolver + post-gate report printing 'OK pre-deploy gates PASS. Push to main to trigger workflow deploy: git push origin main' + workflow URL + exit 0 cleanly. Cross-milestone touch on M032's wiki/README.md First-deploy checklist sections rewritten to git-push-triggers-workflow flow (M032 closed, but the closure-set is FR-20-scoped done-definition, not a milestone reopen),OUT-OF-SCOPE diagnostic-budget collapse pattern: two-pass walker writes findings to FINDINGS_FILE then post-pass splits into OOS-vs-non-OOS via grep + per-target tally + emit-or-collapse via awk; THRESHOLD=5 high-fanout collapse (collapsed) marker + BUDGET=5 small-fanout per-occurrence cap (collapsed,budget) marker preserves diagnostic distinction; --verbose escape hatch as bypass-mechanism cat-the-buffer-verbatim path; defaults stay clean for production deploys"
  - milestone-summaries :: .orchestrator/milestones/M044/M044-SUMMARY.md:15:(16-agent deliberation, 0 rejections, DQ-1…DQ-8 binding). Four phases, all green;
  - milestone-summaries :: .orchestrator/milestones/M044/M044-SUMMARY.md:95:`comments`; embeddings. Live runtime-memory read is **cut** (DQ-8, M009).
  - milestone-summaries :: .orchestrator/milestones/M045/M045-SUMMARY.md:15:itself across context-rotation boundaries via **process-fresh `claude -p`
  - milestone-summaries :: .orchestrator/milestones/M045/M045-SUMMARY.md:17:re-invoking at each rotation.
  - milestone-summaries :: .orchestrator/milestones/M045/M045-SUMMARY.md:20:  `AUTO:SELF_CONTINUE` / `AUTO:ROTATE_EXIT reason=...` / `AUTO:NO_ROTATION` per the
  - milestone-summaries :: .orchestrator/milestones/M045/M045-SUMMARY.md:21:  armed × capable × rotation truth table (Principle X: policy in shell).
  - milestone-summaries :: .orchestrator/milestones/M045/M045-SUMMARY.md:26:  re-spawns a fresh `claude -p` per rotation-exit until a terminal outcome, the
  - milestone-summaries :: (capped at 5 hits — more may exist)

### Q9 — KEPT

- **question**: - #Q-9 (instrument-surface): Do the FR-19 introspection CLIs reuse existing surfaces (`self-continue-status.sh`, M019 cost rollup) or require new commands, and what is the exact read-only command set?
- **disposition**: kept (new FR-19 surface introduced by 2026-07-03 LFD amendment — reuse-vs-new is a plan-phase design decision; corpus hits are the candidate surfaces themselves, not an answer)
- **terms** (12): Q-9, FR-19, instrument, surface, introspection, clis, reuse, existing, surfaces, self, continue, status
- **hits**:
  - constitution :: .orchestrator/memory/constitution.md:157:  coordination — they are NOT optional instrumentation.
  - constitution :: .orchestrator/memory/constitution.md:172:- The design step MUST surface uncertainty, not hide it. When a
  - constitution :: .orchestrator/memory/constitution.md:186:  plan without reading any file not referenced in the plan itself.
  - constitution :: .orchestrator/memory/constitution.md:19:  self-derivable without cross-reference to CONFORMANCE.md.)
  - constitution :: .orchestrator/memory/constitution.md:26:    - Tier 2 XXII (Distribution Surface Integrity) — inherited from
  - constitution :: .orchestrator/memory/constitution.md:267:schema-variable, and documented-consumer surfaces with
  - constitution :: .orchestrator/memory/constitution.md:319:- Roadmap phase status: the roadmap file is the single source; phase
  - constitution :: .orchestrator/memory/constitution.md:320:  directories are artifacts, not status indicators.
  - constitution :: .orchestrator/memory/constitution.md:369:  migration: new instructions conform immediately, existing
  - constitution :: .orchestrator/memory/constitution.md:376:(Principle I) and increases maintenance surface.
  - constitution :: .orchestrator/memory/constitution.md:38:      proxy-status annotation.
  - constitution :: .orchestrator/memory/constitution.md:397:- Match the existing style even if you would do it differently.
  - constitution :: .orchestrator/memory/constitution.md:423:  passing verification status.
  - constitution :: .orchestrator/memory/constitution.md:44:      constitutionally self-derivable from VIII's plain text without
  - constitution :: .orchestrator/memory/constitution.md:457:    Where a runtime requires pre-existing scaffold files before the
  - constitution :: .orchestrator/memory/constitution.md:465:    resulting project's status command exits with code 0 and emits
  - constitution :: .orchestrator/memory/constitution.md:72:    - Originating + self-consistency + blind deliberations 2026-05-11
  - constitution :: .orchestrator/memory/constitution.md:89:    - 2.2.1: VIII bullet 1 PATCH (scope boundary self-derivable;
  - constitution :: (capped at 5 hits — more may exist)
  - decisions :: .orchestrator/DECISIONS.md:113:- **Choice**: Revised sequence: **M011 → M012 → M013 → M014 → M019 → M018 → M009 → M010** (M019 inserted by D009). M018 adopts caveman-style (github.com/JuliusBrussee/caveman) token compression as a first-class pipeline stage aligned with Constitution Principle I (Context Minimization). Proposed phase outline (subject to detailed planning closer to kickoff): P01 compression grammar (what's safe to compress per artifact class; preserve frontmatter, code fences, paths, MEM IDs, commands, URLs), P02 dispatch compressor (optional filter in `scripts/dispatch/build-context.sh`, emits `payload.compressed.md` beside original), P03 intensity mapping (Quick→ultra, Standard→full, Full→lite/off via `scripts/engine/intensity-gate.sh`), P04 memory compression (opt-in `CLAUDE.compressed.md` / `KNOWLEDGE.compressed.md` with `.original` siblings), P05 agent output profile (inject terseness directive into payload prefix), P06 eval harness (3-arm port of caveman's evals against orchestrator payloads, gate on accuracy parity), P07 multi-runtime parity (Claude Code / Codex / Cursor). Differentiation vs. installing caveman as-is: roundtrip-safe by policy (Principle VI — originals authoritative, compress only at dispatch/load boundaries), grammar-aware preservation of orchestrator artifact structure, intensity-bound (not always-on), explicit opt-out surfaces (constitution, spec.md, PR descriptions, user-visible docs), and telemetry integrated with existing cost tracking so savings are measurable per dispatch.
  - decisions :: .orchestrator/DECISIONS.md:116:Dogfooding M011–M014 is the prerequisite that surfaces which artifacts actually dominate token spend in steady-state orchestrator use; planning M018 earlier would be speculative (Constitution XIV). Slotting before M009 means launch narrative ships with measured savings from real usage rather than hypothetical numbers. M018 does not gate launch — if compression work balloons or regresses accuracy in P06, shrink scope to dispatch-only (P02+P03+P06) and defer memory/output compression. Main risk: parse-regression in downstream scripts that grep prose — P01 grammar work is where that risk lives.
  - decisions :: .orchestrator/DECISIONS.md:127:- **Choice**: Revised sequence: **M011 → [M019/Tier 1 emitter] → M012 → M013 → M014 → [M019/Tier 2+3] → M018 → M009 → M010**. Tier 1 ships as a small standalone unit once M011 is closed — avoids mid-milestone scope insertion (Constitution XV — Surgical Precision), gives a clean boundary for instrumentation to start, and still captures M012+M013+M014 (~3 full milestones of dogfooding) before M018 planning needs a baseline. Cost of not backfilling M011 dispatches: ~15–25 unlogged records from M011/P04–P07; acceptable tradeoff. M019 records time, token cost, estimated dollar cost, retries, deviations, verification pass rate, and domain-unit cost (per ingested spec / merged PR / auto-applied comment) at task/phase/milestone/project/all-time granularities. Three-tier framing: **Tier 1 — Just emit** (~1 day: append `payload_breakdown`, `dispatch_usage`, `unit_close` JSONL records to existing `execution-log.jsonl`; no UI; ships first thing after M011 close so M012–M014 dogfooding produces real data); **Tier 2 — Rollup + `orchestrator:cost` command** (~3 days: `scripts/diagnostics/metrics-rollup.sh`, `.orchestrator/metrics/*.jsonl` aggregates, `orchestrator:status` efficiency footer; worth it if we launch); **Tier 3 — Full polished surface** (~7 days: backend-specific `adapters/backend/*/report-usage.sh`, auto-generated `MNNN-METRICS.md` on consolidate, `orchestrator:doctor` anomaly checks, parity evals across runtimes; only if efficiency becomes M009 launch headline). Full M019 phase outline mirrors the tiers: P01 schema + pricing table, P02 dispatch-time emitter (Tier 1 boundary), P03 backend usage adapters, P04 rollup + `orchestrator:cost` command (Tier 2 boundary), P05 milestone close report, P06 status + doctor integration, P07 evals + multi-runtime parity (Tier 3 boundary). Schema must pair cost metrics with **quality metrics** (verification pass rate, deviation rate) from day one to avoid Goodhart failure mode — optimizing cheap over correct. All records labeled `source: "estimate" \
  - decisions :: .orchestrator/DECISIONS.md:128:- **Revisable**: Runtime cost of tracking is effectively zero (JSONL append + reuse of existing `estimate_tokens` function; no added LLM tokens — agent does NOT self-report, runtime hooks supply actuals). Build cost is the real decision, hence the tiered framing so M019's final scope is set by what the Tier 1 data reveals rather than up-front speculation. M019 before M018 because compression needs a measured baseline. M019 before M009 because launch narrative differentiates on published receipts ("M011 shipped in N phases, M tasks, $X spend, Y% first-try verification pass") — no other orchestrator tool publishes this. Also surfaces Principle V's fresh-context cost quantitatively, making the Context Minimization claim auditable.
  - decisions :: .orchestrator/DECISIONS.md:141:- **Choice**: Replace the D008 phase outline's "P02 dispatch compressor (optional filter...)" framing with a tier-ladder model informed by Article 3 (Rohit's Claude Code harness teardown) and logged in `.orchestrator/scratch/articles-synthesis-2026-04-17.md`. Every dispatch runs through tiers in order, cheapest first: **Tier 1 microcompact** (reuse cached tool-call results that haven't changed + persist oversized tool results to disk with `file_path + preview` references — absorbs L6 tool-result budgeting; zero LLM cost); **Tier 2 snip** (head-drop with protected tail; no LLM call); **Tier 3 auto-compact** (summarize via LLM call; existing caveman-style territory); **Tier 4 context collapse** (staged multi-phase compression of tool results → reasoning → sections; feature-flagged). Intensity maps to tier ceiling: Quick→T1–T2 only, Standard→through T3, Full→all tiers available. Revised phase outline: P01 tier grammar + per-tier safety boundaries (what's preservable per tier), P02 Tier 1 microcompact + tool-result budgeting, P03 Tier 2 snip with protected-tail semantics, P04 Tier 3 auto-compact (summarization, intensity-gated), P05 Tier 4 collapse (staged; feature-flagged, off by default), P06 eval harness (3-arm parity tests per tier), P07 multi-runtime parity. D008's original scope envelope (roundtrip-safe by policy, grammar-aware preservation, intensity-bound, explicit opt-out surfaces, telemetry-integrated) carries forward unchanged — only the internal staging model is reframed. Fresh `orchestrator:discuss` at M018 kickoff addresses detailed tier design once M019 Tier 1 data reveals which artifacts dominate token spend. **Conversus integration (locked in per D007 reuse pattern)**: M018/P01 tier-grammar + per-tier-safety-boundary work invokes `scripts/dispatch/adapters/tool/conversus.sh` via `role/red-team` + `domain/security` presets at plan-review time. Rationale: grammar decisions carry parse-regression risk across downstream scripts that grep prose (the risk D008 originally flagged), which is exactly the subjective-quality territory the conversus fidelity-gate adapter earns its cost on. A new preset drops under `templates/conversus-presets/` without adapter modification (same pattern M013/M014 use). Gate is intensity-bound by default (Quick=off, Standard=advisory, Full=blocking); `--no-review` escape available.
  - decisions :: .orchestrator/DECISIONS.md:142:- **Revisable**: Yes — individual tiers can be dropped if M019 Tier 1 data shows they don't earn their complexity (e.g., if measured tool-result sizes are already small, skip T1 microcompact in favor of direct T2 snip). Tier ladder is the framing; specific tier set is open. If M018 kickoff reveals the ladder itself is overkill for orchestrator's payload profile, collapse back to D008's single-filter model with this amendment noted as a considered-and-rejected alternative.
  - decisions :: .orchestrator/DECISIONS.md:155:- **Choice**: Context: `.orchestrator/scratch/articles-synthesis-2026-04-17.md` identified a coherent knowledge-layer theme bundling L7 (wikilinks), L8 (explored state), A1 (preferences layer), A2 (candidate→graduate with required rationale), A3 (review queue in status), A4 (AGENTS.md read-order map), A6 (Jaccard clustering in consolidate). Without a logged trigger, the M020 decision drifts. Evaluation rule at M012/P02 close: assess whether M012's spec-wiki implementation ships **(a)** clickable cross-refs from spec pages to `knowledge/**/MEM*.md` entries via shared ID namespace, **(b)** a review/unreviewed state model on generated pages, **(c)** a query/search surface callable from orchestrator dispatches (wiki-query analog). **Decision rule**: ≥2 of 3 land → M020 dissolves into knowledge-tooling maintenance PRs with no milestone commitment (M012 patterns cover enough of the surface); ≤1 of 3 land → promote M020 as a committed milestone positioned between M014 and M018 (before M019 Tier 2/3, so A5 on_failure flagging can draw on its data). **Minimum-viable subset ships regardless of the M020 decision**: L8 (explored state on auto-generated MEM entries) + A2 minimal guard (`scripts/knowledge/graduate.sh --rationale "..."` flips `status: candidate` → `status: graduated`) bundled into a standalone maintenance PR. Rationale: these are small (~30 lines), address the rubber-stamping failure mode directly, and compound M019 Tier 1's measurement value. The *full* A2 workflow (clustering, staging layer, review queue UI, decision history) waits for the M020 decision. **A4 also ships standalone**, before M019/P00, as an independent concern (session-start file-loading manifest for non-Claude-Code runtimes) — not bundled with P00 because P00's scope discipline is "dispatch-facing payload adaptation only."
  - decisions :: .orchestrator/DECISIONS.md:172:Evidence-grounded: 20 M011/P05–P07 screenshots define the closed pattern matrix (AD-2, AD-5); no speculative additions (constitution XIV). M021 itself dogfoods via its own `orchestrator:auto` execution through P01–P04 (AD-8) — SC-7 attestation is produced by `scripts/verify/m021-p04-dogfood-attestation.sh` and the permanent replay corpus at `tests/fixtures/m021-prompt-corpus.txt`. Adding M021 ahead of M019 costs ~1 milestone of sequencing but eliminates the noise floor that would otherwise contaminate every subsequent metrics measurement — principle I (Context Minimization) applied to the measurement apparatus itself.
  - decisions :: .orchestrator/DECISIONS.md:197:- **Choice**: Narrow M013 scope per `specs/023-github-native-integration/conversus/` run: (1) FR-12 handler installation is Claude-Code-only for v1 (CV-3 / RD-2 binding ruling) — Codex CLI + Cursor fall back to `manual` sync at init, non-Claude-Code runtime-adapter work deferred to a future milestone; (2) Minimal Slice (CV-1 / RD-3 binding ruling) pins Phase 1 load-bearing scope to full US-3 + minimal US-1 scaffolding (sidecar config + UAT template install) + US-2 idempotency applied to UAT entries only; (3) M020 sequencing unchanged (RD-1 reclassified) — M013 -> M014 -> M020 per D013; schema authority bounded by MIT-2 narrowing FR-9 to additive-emit-only with chunk-ID pinned to existing `SPEC-*` frontmatter + new Knowledge-Layer Boundary subsection explicitly forbidding review-state / query-surface / clustering; (4) 13 unmitigated P5 risks landed as MIT-1..MIT-13 spec edits (sub-issue fallback Constraint, Constitution Check section, FR-5 three-shape rewrite, FR-6 per-item cache schema, FR-7 sync lock, FR-9 narrowing, FR-12 prose correction + scope, FR-13 `--strict` provenance + 30s timeout + drop intensity-engine coupling, FR-14 re-init via marker search + label-collision preflight, FR-15 dry-run generalized to init+sync, FR-16 rate-limit + auth-expiry detection, FR-17 cost emission, FR-18 cache reconciliation, plus SC-8/SC-9 reconciliation and new SC-12/SC-13 scope + overhead caps); Open Questions #1/#3/#6/#7 closed; new OQ on PAT-vs-App default for M009. Spec status promoted Draft -> Ready-for-discuss.
  - decisions :: .orchestrator/DECISIONS.md:211:- **Choice**: Original P02 shipped 7 produces (init + common helpers + marker invariant + GraphQL lint + re-adoption + sidecar extensions + doc extensions) in a single risk=high phase. Post-P01 retrospective showed P02 was the highest risk concentration in M013. Split cuts P02 to create-path-only (reuses all primitives from P01 + common helpers authored in P02), leaves a medium-risk P03 for the harder idempotency cases (re-adoption + GraphQL lint), and pushes the sync phase to P04.
  - decisions :: .orchestrator/DECISIONS.md:239:- **Choice**: M014 pressure-tested pre-discuss via cooperative conversus deliberation (Tier 1; 2 agents — `spec-author` + `devils-advocate`; 1 round; no arbiter). 14 MITs applied pre-discuss covering: (P1) Phase Sequencing table as spec-layer subsection after Minimal Slice; SC-2 tightened to mechanical stdout/exit-code/stderr-grep assertions; FR-2b template SSOT (`templates/spec-template.md`) + FR-18 byte-compatibility fixture test (`tests/test-specify-shape.sh`) + SC-17; US-5 promoted to independent P2 user story with scope-boundary sentence ("US-5 is the only path that edits `specs/<NNN>-<slug>/spec.md` from a comment"); Constitution XV retrofit at SC-5 + Constitution Check section → III (primary) + XIV (supporting), with Constitution Check XV paragraph split into "applies to / does NOT apply to" sub-contexts (FR-12 + FR-14 byte-preservation retained; spec-amendment human-gate retargeted); (P2) SC-4 rewritten as two-branch construction (default: measurement + re-planning trigger; upgrade branch: pinned-shape gate with failure-posture clause); FR-14 three-case semantics (all-placeholder / partial / fully-authored) + SC-14 byte-preservation invariant; SC-6a outside-markers `shasum` invariant + `tests/test-dual-write-outside-invariant.sh`; FR-19 `--dry-run` JSONL manifest format pinned M014-local only (M013 retrofit deferred); SC-15 `RUNTIME-ASSUMPTIONS.md` close-out deliverable; SC-16 dogfood-data sizing for FR-9 classifier-shape decision; (P3) FR-7 interim→M024 migration handshake committed as one-line rule; Open Question #C-11 exhaustive CLAUDE.md write-site enumeration at planning time. Spec frontmatter promoted `Status: Draft` → `Status: Ready-for-discuss`; `Last Revised: 2026-04-22` line added matching M013 precedent. P3 Dependencies prose rewrite skipped as subsumed by Phase Sequencing table (P1 #1). Two Phase 4 disputes (SC-4 default branch; Phase Sequencing table layer) resolved via synthesizer-recommended compromises: SC-4 adopts measurement default with upgrade-branch failure-posture; table lands spec-layer.
  - decisions :: .orchestrator/DECISIONS.md:267:- **Choice**: (1) **Three-pass contract** pinned in `commands/specify.md`: Pass 1 (scaffold) always runs — creates file, populates frontmatter + Input, writes Section Contract `<TODO:` placeholders. Pass 2 (author) drafts the body from Input + DECISIONS.md + constitution + neighboring CLAUDE.md/AGENTS.md Recent Changes; Quick = agent-only no review, Standard = agent + self-review checklist warnings, Full = agent + `speckit.clarify` loop (≤5 load-bearing ambiguity questions, operator-answered, agent-revised). Pass 3 (gate) adversarially reviews authored body via `scripts/dispatch/adapters/tool/conversus.sh gate spec-pressure-test`; Quick = skip, Standard = advisory (findings folded into Open Questions, no BLOCK), Full = strict (BLOCK halts with dispute list + "revise then --amend" message). On Full+PASS, spec status promoted `Draft` → `Ready-for-discuss` + `Last Revised:` line added per M013/M014 precedent. (2) **Intensity resolution order**: project default from `.orchestrator/config.yml` → smell-test escalation via `scripts/engine/intensity-analyze.sh --description <prose>` (can only escalate, never de-escalate) → CLI `--intensity` override (trumps both). One `specify_intensity_resolution` JSONL record emitted at run start. (3) **Gate TODO pre-flight invariant** (universal): `scripts/dispatch/adapters/tool/conversus.sh gate` refuses any artifact containing ≥ `CONVERSUS_GATE_TODO_THRESHOLD` (default 1) `<TODO:` markers, with actionable error pointing at the author pass. Bypass: `CONVERSUS_GATE_SKIP_TODO_CHECK=1` (reserved for tests + intentional-stub preset authoring). Protects M013/M014/M023/M024 + any future gate consumer, not just `orchestrator:specify`. Applied to adapter in `scripts/dispatch/adapters/tool/conversus.sh:150-169` + regression test in `tests/test-conversus-adapter-shim.sh` section 1b. (4) **Reuse over rebuild**: all intensity machinery already exists (`intensity-analyze.sh`, `intensity-override.sh`, `intensity-gate.sh`, `spec-complexity-probe.sh`) — D019 wires them into the specify flow rather than adding parallel infrastructure. (5) **Implementation posture**: command contract is the SSOT (landed 2026-04-23 in `commands/specify.md`); shell implementation in `scripts/specify/specify.sh` lands in the next M014 extended phase. Until then, agents invoking the command execute passes 2 and 3 manually per the doc.
  - decisions :: .orchestrator/DECISIONS.md:268:- **Revisable**: Yes — (a) if Full-intensity dogfooding reveals clarify's ≤5 question cap is too tight or too loose, adjust in a follow-up D-row without re-opening the three-pass contract. (b) If smell-test false-escalation rate is high, tune `intensity-analyze.sh` keyword weights independently — D019 commits to the resolution order, not the keyword set. (c) `CONVERSUS_GATE_TODO_THRESHOLD` default can be raised (threshold-2 to allow one placeholder) if authoring pass legitimately leaves sections marked as deferred-to-plan. (d) If Full+BLOCK exit-code-2 semantics conflict with `orchestrator:auto` loop handling, harmonize in an auto-specific D-row. (e) Implementation shell in `scripts/specify/specify.sh` can adopt the contract incrementally (Quick intensity first, then Standard, then Full's clarify wiring) without invalidating the contract itself.
  - decisions :: .orchestrator/DECISIONS.md:340:Anchors the schema evolution in the audit trail BEFORE code lands (T02-T05 in P01 and downstream phases). The closed enum prevents downstream surfaces from inventing alternate state names (e.g. pending, superseded) that would fragment the query surface (FR-2). Append-only decision_history: keeps a non-destructive review log; compaction is deferred (NG-6). Incremental on-touch migration honors NG-3 (no retroactive bulk migration). Centralizing the schema-authority gate at M020 means future field requests from consumer milestones go through a single review point rather than racing into the frontmatter from multiple directions.
  - decisions :: .orchestrator/DECISIONS.md:43:- **Choice**: Spec Truth #4 requires resolver to report source=existing:.orchestrator, but T02 preserved the redundant state_root: declaration in config, causing Rule 2 to win over Rule 3. Removing the declaration aligns config semantics: config is override mechanism, directory existence is canonical.
  - decisions :: .orchestrator/DECISIONS.md:715:   value reuses the existing operator-opt-out semantics rather
  - decisions :: .orchestrator/DECISIONS.md:88:Team needs to dogfood end-to-end spec→wiki→GitHub workflow before producing external-facing launch artifacts. Dogfooding M011–M014 will surface the rough edges that M009 docs/examples need to document. Producing launch materials before internal usage would mean rewriting them after discovering gaps. M017 repositioned after the spec management block since those milestones will reveal whether deliberation adds value at spec-quality judgment points. Supersedes positioning from D004/D005.
  - decisions :: (capped at 5 hits — more may exist)
  - knowledge-entries :: (capped at 5 hits — more may exist)
  - knowledge-entries :: knowledge/conventions/MEM012.md:32:Commands reference scripts by relative path in "Referenced Scripts" sections. Integration tests verify all cross-references resolve to existing, executable files.
  - knowledge-entries :: knowledge/conventions/MEM013.md:21:Summary frontmatter: 15-field base schema for tasks (`schema_version`, `type`, `id`, `parent`, `milestone`, `provides`, `requires`, `affects`, `key_files`, `key_decisions`, `patterns_established`, `drill_down_paths`, `duration`, `verification_result`, `completed_at`); phase/milestone summaries add `observability_surfaces` for 16 fields.
  - knowledge-entries :: knowledge/conventions/MEM014.md:19:**Scripts -> Commands**: Commands reference scripts by relative path in "Referenced Scripts" sections. Integration tests verify all cross-references resolve to existing, executable files.
  - knowledge-entries :: knowledge/conventions/MEM015.md:22:DOCTOR:<CHECK> status=<ok|warn|skip|drift|missing> key=value ...
  - knowledge-entries :: knowledge/conventions/MEM017.md:19:`scripts/lib/verdicts.sh` provides `emit_verdict`, `parse_verdict`, `orch_is_verdict` with four constants: PASS, BLOCK, WARN, NEEDS_REVIEW. `hooks.sh` captures hook stdout, parses VERDICT lines, resolves multiple verdicts to most severe, and maps to block/warn/continue behavior. Backward compatible when no VERDICT present. Provider convention documented in `references/provider-convention.md`.
  - knowledge-entries :: knowledge/conventions/MEM030.md:25:3. **No escape** — routing all paid-only access through a separate command or wrapper. Forces the orchestrator to maintain two parallel invocation paths with the same surface, doubling test burden.
  - knowledge-entries :: knowledge/conventions/MEM030.md:35:4. **Diagnostic surface**: the `check` subcommand of the integration's adapter MUST emit `edition=<value> reason=<resolution-tag>` on stdout so the resolved edition is visible to the operator and to telemetry without a separate probe call.
  - knowledge-entries :: knowledge/conventions/MEM030.md:37:5. **Telemetry shape**: every JSONL record emitted by the integration adapter MUST include an `edition` field alongside the existing identifying fields (e.g., `adapter_version`, `gate_id`). Place adjacent to the version field for readability and for adjacency-invariant tests (M026/P02/T02 pattern).
  - knowledge-entries :: knowledge/conventions/MEM030.md:48:- **JSONL `edition` field presence**: every `*_invocation` record from an edition-aware adapter MUST contain an `edition` field. Verify with `scripts/verify/m026-p02-jsonl-edition-field.sh` (existing) — extend the pattern when adding a second edition-aware adapter.
  - knowledge-entries :: knowledge/conventions/MEM031.md:17:# MEM031: Knowledge entry `status:` field vocabulary (M020 schema evolution)
  - knowledge-entries :: knowledge/conventions/MEM031.md:22:`status:` frontmatter field with one of three values from a **closed enum**:
  - knowledge-entries :: knowledge/conventions/MEM031.md:27:| `graduated` | Reviewed and accepted; visible to the default query surface (FR-2).     |
  - knowledge-entries :: knowledge/conventions/MEM031.md:38:Entries that exist on main without a `status:` field are treated as `graduated` on first read (most conservative — do not re-review what was
  - knowledge-entries :: knowledge/conventions/MEM031.md:53:  Append-only invariant: existing records are never edited or removed in
  - knowledge-entries :: knowledge/lessons/MEM022.md:19:`$(bash lock-manager.sh create ...)` runs in a subshell whose PID dies before the status check. Fix: tests patch the lock file PID to a known-alive process (PID 1) after creation.
  - knowledge-entries :: knowledge/lessons/MEM026.md:25:Every P04 verification script — including `scripts/verify/m013-p04-post-verify-hook.sh` — ran against an empty `$HOME/.claude/` fixture. No gate seeded a pre-existing non-orchestrator `settings.json`, so the overwrite path was never exercised. The schema-validity failure was invisible because CC itself was not invoked against the emitted document in any P04 gate; the gates only asserted presence of the wrapper JSON's internal keys, not conformance to the CC `hooks` schema. The regression required a real user on a real multi-tool system to observe.
  - knowledge-entries :: knowledge/lessons/MEM026.md:29:Every user-scope config write — `~/.claude/settings.json`, `~/.codex/*.toml`, `~/.cursor/*.json`, shell rc files, git hooks paths, and anything else jointly owned with sibling tools — requires a coexistence gate driven by a pre-seeded non-orchestrator fixture (pattern realized by M025 at `tests/fixtures/m025-p01/gsd-baseline/`). Empty-home fixtures are insufficient: they cannot surface overwrite damage or schema-shape issues that only manifest when the host tool actually parses the file. Pair the coexistence gate with (a) a round-trip reversibility gate (install → uninstall → sha256 byte-match) and (b) an idempotency gate (double-install → sha256 byte-match) to lock the invariant triad.
  - knowledge-entries :: knowledge/patterns/MEM002.md:25:Cross-reference validation: integration tests extract script/template paths from command files via `grep -oE` regex, then verify existence/executability. Self-maintaining as commands evolve.
  - knowledge-entries :: knowledge/patterns/MEM002.md:27:Self-diagnostic pattern: test files verify their own `fail()` messages include actionable file paths or contract identifiers.
  - knowledge-entries :: knowledge/patterns/MEM005.md:19:Knowledge entries carry `content_hash: sha256:{hex}` in frontmatter. `hash.sh` provides `compute_content_hash` (string) and `compute_file_body_hash` (file body). `create-entry.sh` writes hash at creation; `update-entry.sh` recomputes on `--body` changes; `rebuild-index.sh` compares stored vs computed hashes for change detection and self-heals drift. `record-result.sh` accepts `outcome=unchanged` for stagnation signaling.
  - knowledge-entries :: knowledge/patterns/MEM009.md:19:Writing documentation and running every documented command catches drift that tests miss. M006 found and fixed a stale routing fallback value in references/file-formats.md through this process. The mechanical discipline of verify-as-you-write surfaces real bugs.
  - knowledge-entries :: knowledge/patterns/MEM011.md:19:When scripts pre-exist from prior milestones, phase tasks verify correctness rather than creating new code. T01 creates verification scripts, T02+ runs them. Most phases in M002 P04-P07 required minimal or no code changes — the verification process itself was the deliverable.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:12:- **Idempotent operations**: All scaffolding/creation scripts check-before-create with early exit on existing state.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:129:- **[milestone:M005]** [2026-04-12] DOCTOR: structured output protocol — all diagnostic scripts emit a single structured line (DOCTOR:<CHECK> status=<ok|warn|skip|drift|missing> key=value ...) for machine parsing by run-doctor.sh. Advisory checks (like check-plans.sh) always exit 0; non-advisory checks exit 0 on ok, 1 on warn. Established in P04, extended through P05-P07.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:130:- **[milestone:M005]** [2026-04-12] Scored health reporting — run-doctor.sh aggregates 12 checks (4 legacy + 8 DOCTOR:) into Checks passed: N/M with HEALTHY/NEEDS_ATTENTION status. Legacy checks use exit-code pass/fail; new checks parse DOCTOR: status lines. Advisory checks (check-plans.sh) counted separately. History appended to doctor-history.jsonl per run.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:132:- **[milestone:M005]** [2026-04-12] Content-hash idempotency — knowledge entries carry content_hash: sha256:{hex} in frontmatter. hash.sh provides compute_content_hash (string) and compute_file_body_hash (file body). create-entry.sh writes hash at creation; update-entry.sh recomputes on --body changes; rebuild-index.sh compares stored vs computed hashes for change detection and self-heals drift. record-result.sh accepts outcome=unchanged for stagnation signaling. Established in P01.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:134:- **[milestone:M005]** [2026-04-12] Gate verdict protocol — scripts/lib/verdicts.sh provides emit_verdict, parse_verdict, orch_is_verdict with four constants: PASS, BLOCK, WARN, NEEDS_REVIEW. hooks.sh captures hook stdout, parses VERDICT lines, resolves multiple verdicts to most severe, and maps to block/warn/continue behavior. Backward compatible when no VERDICT present. Provider convention documented in references/provider-convention.md. Established in P05 per AD-3.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:137:- **[milestone:M002]** [2026-04-13] Validation-as-task pattern: when scripts pre-exist from prior milestones, phase tasks verify correctness rather than creating new code. T01 creates verification scripts, T02+ runs them. Most phases in M002 P04-P07 required minimal or no code changes — the verification process itself was the deliverable.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:142:- **[milestone:M006]** [2026-04-14] Documentation-as-verification (verify-as-you-write) surfaces real bugs: M006 found and fixed a stale routing fallback value in references/file-formats.md. The mechanical discipline of running every documented command catches drift that tests miss.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:146:- **[milestone:M008]** [2026-04-14] Bash 3.2 compat scanner must be comment-aware: grep -vE '^[[:space:]]*#' before checking for forbidden constructs. Documentation strings like '# Bash 3.2 compatible: no declare -A, no readarray, no |&' would otherwise produce false positives. The scanner itself must use fragment-built regexes for the forbidden tokens so it does not self-match when included in its own scan target list.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:148:- **[milestone:M008]** [2026-04-14] Thin delegation pattern: top-level orchestration scripts (init-project.sh, installers) should delegate to existing single-responsibility scripts rather than duplicate logic. init-project.sh delegates to detect-runtime (P05), detect-capabilities (P01), detect-project (P07), resolve-root (P04), and runtime-specific installer (P06). Installers delegate --probe/--register/--hook-config to P05 runtime adapters. This keeps each script testable in isolation and prevents behavior drift across layers.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:150:- **[milestone:M003]** [2026-04-15] Three-tier root resolution (flag → env var → resolve-root.sh fallback) is the standard CLI contract for orchestrator-root-consuming scripts. Makes scripts work under both ORCHESTRATOR_ROOT env and explicit --root, with config-driven default. Reference: scripts/orchestrator/status.sh.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:158:- **[milestone:M021]** [2026-04-17] Permanent fixture corpus with locked entry count: the 20-entry M011/P05-P07 prompt corpus is the authoritative regression surface — every entry traces to a specific screenshot (ID+SCREENSHOT+INPUT+EXPECTED_OUTCOME). Future hook triggers earn a new milestone, not a fixture append (constitution XIV; AD-5).
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:163:- **[milestone:M019]** [2026-04-18] Zero-token instrumentation via stdout capture-and-replay: PAYLOAD_CAPTURE tempfile then cat then emitter. Emitter runs strictly after stdout and contributes zero bytes. SC-6 byte-identical stdout enforced this way in scripts/dispatch/build-context.sh.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:164:- **[milestone:M019]** [2026-04-18] Goodhart pairing enforced at emission time: every unit_close record carries both a cost block (estimated_cost_usd, pricing_version) and a quality block (verification_pass_rate, deviation_count, retry_count derived from existing attempt/outcome/verification_result per AD-3). Schema validator rejects records missing either. Prevents Tier 2 from optimizing cost in isolation.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:169:- **[milestone:M019]** [2026-04-18] Opus 4.7 dispatch payload structure (M019/P00 L1-L5): every TASK-branch payload emits a First-Turn Completeness block (Intent/Constraints/Acceptance Criteria/Files To Touch, re-surfacing existing content, no new prose); stable sections before <dispatch-volatile> marker, volatile sections after, </dispatch-volatile> close; conditional Parallel Fan-Out directive only when recipe or task plan declares parallelizable work; intensity-gate.sh documents adaptive-thinking contract as a comment block (no fixed thinking_budget literals); expressive guidance rewritten positive with a narrow dotfile whitelist for Constitution XV retained-negatives.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:171:- **[milestone:M012]** [2026-04-21] Chained-gate deploy wrapper pattern: N sequential single-script-file gate invocations with first-non-zero-aborts semantics, fixed-verb per-gate stdout output (GATE/BUILD/DEPLOY/DRY-RUN/OK/FAIL) for deterministic downstream capture, and stderr-indent-on-FAIL for machine-readable triage. Four flags (--dry-run/--help/--root/--skip-smoke) cover the operator surface without a YAML driver or plugin system (Constitution XIV).
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:174:- **[milestone:M013]** [2026-04-22] Additive-helper-append preserves byte-identity for cross-phase shared libs: scripts/integrations/github-common.sh extended P02 (12 helpers) → P03 (gh_marker_search_remote + manifest_footer 4th-arg additive-suffix) → P04 (http_probe + sidecar_update_item_cache + emit_tier1_record + classify_gh_rc + emit_conversus_gate_record). Every extension lands at end-of-helpers before the self-check block; prior function bodies stay byte-identical so earlier-phase byte-identity gates continue to pass. Optional-arg additive-suffix on existing helpers (manifest_footer 3→4 args) preserves existing call-site behavior.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:177:- **[milestone:M013]** [2026-04-22] Direct-cmd-subst rc-propagation gotcha (caught mid-T03 in P04): writing 'probe_out=$(http_probe …  || true)' silently zeros $? — the '|| true' tail masks the rate-limited (rc=3) and auth-expired (rc=4) signals. Fix: assign $() result then read $? on the next line. This silently broke FR-16 detection and was caught only by the rate-limit gate's negative-path assertion. Add to bash-3.2 review checklist for any helper that needs to surface non-zero rc through a captured stdout.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:18:- **Cross-reference validation**: Integration tests extract script/template paths from command files via `grep -oE` regex, then verify existence/executability. Self-maintaining as commands evolve.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:19:- **Self-diagnostic pattern**: Test files verify their own `fail()` messages include actionable file paths or contract identifiers.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:193:- **[milestone:M027]** [2026-04-27] 5-condition suppression matrix as load-bearing reusable contract (M027/P02 + P03): operator-controlled surfaces should ship --yes / ORCHESTRATOR_AUTO / --no-<feature> / <knob>: false / structural-carve-out — all 5 paths short-circuit to zero stdout exit 0 before any I/O. Gate mechanically with a per-phase suppression-matrix verifier exercising all 5 paths. Quick-tier carve-out (P02) and sample-floor carve-out (P03) are the canonical structural-carve-out shapes. Future cost/quality/anomaly surfaces should follow this contract.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:207:- **[milestone:M028]** [2026-04-29] Three-surface documentation discipline for cross-cutting infrastructure: planner-facing (commands/) + agent-facing (templates/dispatch-prompt.md) + catalog-anchor (ANTIPATTERNS.md). Each surface frames the same content for its audience — table for browsing, imperative bullets for in-payload reading, three-column wrapper-catalog table for cross-ref. Every dispatched subagent reads templates/dispatch-prompt.md in-payload, so agent-facing surface is load-bearing. Plan-level verifier asserts (a) section header presence, (b) name coverage across all three surfaces. Surfaced and codified in M028/P04 for the four investigation-pattern wrappers; reusable for any future cross-cutting infrastructure documentation.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:212:- **[milestone:M031]** [2026-05-01] Detection-by-knob-absence pattern: when a milestone introduces a config knob, downstream comms surfaces use the knob's absence as the migration trigger. No version field, no migration timestamp, no separate state file — knob absence IS the migration trigger. M031/P04 T02 detects pre-M031 configs by absence of quick_knowledge_token_budget. Mirrors the M031/P03 entry_routing_confidence_floor knob-presence detection convention but inverted.
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:38:- Summary frontmatter: 15-field base schema for tasks (`schema_version`, `type`, `id`, `parent`, `milestone`, `provides`, `requires`, `affects`, `key_files`, `key_decisions`, `patterns_established`, `drill_down_paths`, `duration`, `verification_result`, `completed_at`); phase/milestone summaries add `observability_surfaces` for 16 fields
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:69:- `resume.md` treats mixed state (continue file + stale lock) as crash recovery
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:70:- `discuss.md` uses frontmatter `status` field (`draft`→`finalized`) for state transition
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:81:| L002 | Lock manager `$$` shows STALE in tests | `$(bash lock-manager.sh create ...)` runs in subshell whose PID dies before status check | Tests patch lock file PID to known-alive process (PID 1) after creation |
  - knowledge-md :: .orchestrator/KNOWLEDGE.md:86:Commands reference scripts by relative path in "Referenced Scripts" sections. Integration tests verify all cross-references resolve to existing, executable files.
  - knowledge-md :: (capped at 5 hits — more may exist)
  - milestone-summaries :: .orchestrator/milestones/M002/M002-SUMMARY.md:10:  - "spec-kit >=0.1.0, Bash 3.2+, existing orchestrator infrastructure from M001"
  - milestone-summaries :: .orchestrator/milestones/M002/M002-SUMMARY.md:18:  - "Sourceable library pattern in scripts/knowledge/lib/; atomic temp-file-then-mv for index writes; self-contained verification scripts with PROJECT_ROOT isolation; validation-as-task pattern for phases where scripts pre-exist; E2E test pattern with isolated fixtures"
  - milestone-summaries :: .orchestrator/milestones/M002/M002-SUMMARY.md:24:observability_surfaces:
  - milestone-summaries :: .orchestrator/milestones/M003/M003-SUMMARY.md:14:  - "scripts/migrate/migrate.sh,scripts/migrate/adapter-interface.sh,scripts/migrate/adapters/gsd2.sh,scripts/migrate/adapters/gsd1.sh,scripts/migrate/adapters/speckit.sh,scripts/migrate/transform/knowledge.sh,scripts/migrate/transform/knowledge-index.sh,scripts/migrate/transform/decisions.sh,scripts/migrate/transform/requirements.sh,scripts/migrate/transform/milestone-tiering.sh,scripts/migrate/transform/active-milestone.sh,scripts/migrate/transform/milestone-rollup.sh,scripts/migrate/transform/telemetry-aggregator.sh,scripts/migrate/transform/report.sh,scripts/migrate/lib/idempotency.sh,scripts/migrate/lib/sqlite-reader.sh,scripts/migrate/lib/json-fallback.sh,scripts/migrate/lib/category-mapper.sh,scripts/migrate/lib/supersession-chain.sh,scripts/migrate/lib/scope-tag.sh,scripts/migrate/lib/decision-numbering.sh,scripts/migrate/lib/category-inferrer.sh,scripts/migrate/lib/error-handler.sh,scripts/orchestrator/status.sh,commands/migrate.md,tests/fixtures/m003-p08-gsd-minimal/,tests/integration/test-m003-e2e-migration.sh"
  - milestone-summaries :: .orchestrator/milestones/M003/M003-SUMMARY.md:18:  - "adapter-interface extract() contract for source-format pluggability; tiered milestone layout (active/recent/historical/archived); three-tier root resolution (flag→env→resolver) as standard CLI contract; dual-layout state probe for idempotency; warn-but-continue post-pipeline rebuild; progressive-disclosure ADR documentation in command docs; Tier-1 static+behavioral verify-per-truth with single-file AD-19 invocations; skip-gracefully integration-test pattern; strict/warn split for synthetic-vs-live validation; deterministic SQLite fixtures under 50KB (PRAGMA page_size=1024 + hard-coded ISO-8601); .gitignore negation for test fixtures sharing ignored names"
  - milestone-summaries :: .orchestrator/milestones/M003/M003-SUMMARY.md:24:observability_surfaces:
  - milestone-summaries :: .orchestrator/milestones/M003/M003-SUMMARY.md:28:M003 Migration Tool milestone complete. Initial build (P01-P06, commit ad3da8a 2026-04-09) delivered the end-to-end migration pipeline: adapter interface with pluggable source formats (GSD2/GSD v1/spec-kit), per-artifact transforms (knowledge, decisions, requirements, milestones with tiering), and CLI with idempotency + reporting. After M007 (knowledge graph) and M008 (standalone multi-runtime + 5-rule state resolver) landed, a delta audit surfaced three drift points: hardcoded .specify/orchestrator/ paths, unpopulated graph database, single-layout idempotency probe. P07 refit wired migrate.sh to resolve-root.sh --absolute, dropped hardcoded paths in three transforms, added dual-root idempotency probing, wired rebuild-index.sh as final pipeline stage, and documented AD-13/14/15 in commands/migrate.md. P08 validated end-to-end via deterministic synthetic GSD2 fixture (primary) and live lakeledger (secondary, skip-gracefully), new scripts/orchestrator/status.sh CLI wrapper making the roadmap demo sentence literally executable, 8 AD-19-safe verify scripts, and a two-pass integration test — all green. Surfaced and fixed one latent P02 bug (missing content_hash frontmatter) during refit. Retroactive phase summaries added for P01-P06 (originally delivered before phase-summary machinery) based on roadmap Revision Note + commit ad3da8a. Migration pipeline now produces resolver-wired output, populates the M007 graph database, and participates in graph traversal — ready for M009 recipe packaging and M010 cloud dispatch.
  - milestone-summaries :: .orchestrator/milestones/M004/M004-SUMMARY.md:18:  - "Double-sourcing guard in first 5 lines, emit_result on every exit path, emit_event at lifecycle boundaries, ORCH_RUN_ID standalone detection, recipe-driven context assembly, hook sandbox with chmod 444 frozen snapshots, chained EXIT traps for scripts with pre-existing cleanup"
  - milestone-summaries :: .orchestrator/milestones/M004/M004-SUMMARY.md:24:observability_surfaces:
  - milestone-summaries :: .orchestrator/milestones/M005/M005-SUMMARY.md:24:observability_surfaces:
  - milestone-summaries :: .orchestrator/milestones/M006/M006-SUMMARY.md:24:observability_surfaces:
  - milestone-summaries :: .orchestrator/milestones/M006/M006-SUMMARY.md:25:  - "run-doctor 9/13 pass (4 pre-existing advisory warnings)"
  - milestone-summaries :: .orchestrator/milestones/M006/M006-SUMMARY.md:28:M006 Documentation & Quality milestone complete. 6 phases, 22 tasks dispatched across 22 dispatch cycles. Produced: 8 new reference docs (architecture, engine, events, errors, hooks, recipes, routing, constitution-walkthrough), updated 1 existing reference (file-formats 802→1105 lines), 4 new user guides (getting-started, recipe-authoring, hook-development, knowledge-management), 1 contributor guide rewrite (AGENTS.md 48→323 lines), 1 new diagnostic (check-docs.sh, 19 files verified), CHANGELOG.md updated with M001-M006 entries, CLAUDE.md updated, extension.yml verified. Total doc output: ~6,400 lines across 14 new/updated files. One code fix: routing.yaml fallback value corrected (found via references/file-formats.md). Doctor final sweep: 9/13 pass, 4 advisory warnings all pre-existing from M002-M005. Documentation completeness: 19/19 PASS.
  - milestone-summaries :: .orchestrator/milestones/M007/M007-SUMMARY.md:25:  - "DOCTOR:GRAPH_HEALTH status line in run-doctor.sh; REBUILT: knowledge.db counts in rebuild-index.sh; PROVENANCE: chain output in traverse-graph.sh"
  - milestone-summaries :: .orchestrator/milestones/M008/M008-SUMMARY.md:16:  - "Centralized stage×intensity matrix in one script (intensity-gate.sh) to avoid drift across 5 command docs; filename-based adapter routing (zero backend-specific code in core) per SC-003; coordination-boundary adapter pattern for in-process tools (MEM018); uniform-interface fallback (adapter always emits dispatch-result even on unavailability); HOME guard mandatory on runtime adapters; one-directional foreign format adapter (speckit.sh rejects --write with exit 4); hard migration per project constitution (no dual code paths); surgical documentation-only refactor of derive-phase.sh per Constitution XV; additive-only command doc refactor preserving MEM012; comment-aware Bash 3.2 compat scanner; open-standard SKILL.md format with YAML frontmatter; thin installer pattern delegating to P05 runtime adapters; offline-safe version check with graceful degradation; reinit default mode is update (preserve existing user context)."
  - milestone-summaries :: .orchestrator/milestones/M008/M008-SUMMARY.md:18:  - "Natural-language scope/risk/complexity pattern matching via keyword tables; pipeline composition via --flag file-or-text inputs for testability; intensity-aware threshold adjustment (Quick tightens, Full loosens); structured dispatch result/error schemas; filename-based adapter auto-discovery; atomic metadata rewrite via awk+mktemp+mv with cksum-sentinel verification; thin wrapper pattern with --dry-run mode; pure resolver pattern (read-only, zero side effects); hermetic standalone e2e via mktemp fixtures (never touch real HOME); runtime adapter HOME guard + project-dir guard; skill generation via single-source-of-truth transform with --check drift detection; project introspection via marker-file presence; user-edit preservation via comment-delimited blocks; top-level pipeline delegation (init orchestrates existing scripts); reinit-with-preservation via awk field-level surgery; wall-clock budget enforcement + hermetic-only static gate."
  - milestone-summaries :: .orchestrator/milestones/M011/M011-SUMMARY.md:12:  - "commands/evaluate.md chunks-first wiring, commands/roadmap.md intensity-aware wiring, commands/ingest.md 6-step pre-chunker pipeline, scripts/engine/intensity-gate.sh new roadmap + ingest stages, scripts/dispatch/scope-filter.sh spec/* resolution, scripts/state/spec-metrics.sh + spec-story-graph.sh helpers, M013/M014 Conversus gate reuse via adapter, all downstream dispatch payloads benefit from scope-filtered spec chunks"
  - milestone-summaries :: .orchestrator/milestones/M011/M011-SUMMARY.md:28:M011 (Spec Management) delivers the "paste a spec, get a roadmap" value proposition end-to-end. Seven phases took the orchestrator from an opaque-spec-file consumer to a full ingest→chunk→scope-filter→normalize→fidelity-gate pipeline that treats specs as structured knowledge on equal footing with the existing three-temperature knowledge model (M002/M007). P01–P03 built the deterministic chunker (spec directory tree with six subcategories, SPEC-prefixed natural IDs, nested-directory indexing, create-entry.sh SPEC- namespace support, full markdown→chunk classifier with classify+dispatch decision layer, versioned-ID supersession chains, REMOVED detection, phase-impact REVIEW emission). P04 wired scope-filter.sh to resolve `spec/*` scope tags (graph-mode + spec-scope-tags mode), letting build-context.sh deliver scope-filtered spec chunks in dispatch payloads per Constitution Principle I. P05 wired chunks-first evaluation and intensity-aware roadmap generation into commands/evaluate.md and commands/roadmap.md (spec-metrics.sh counts non-superseded chunks; spec-story-graph.sh emits directional story-to-story depends_on edges via bidirectional-traverse + directional-frontmatter-filter composition; intensity-gate.sh roadmap stage with Quick/Standard/Full modes). P06 delivered the user-facing commands/ingest.md, four doc-shape verify scripts, a full sandbox E2E gate script, a timing harness (<60s on representative specs), and dogfood evidence against specs/016-autonomous-hardening/spec.md (20 chunks, 7s elapsed). P07 closed with format-agnostic intake and a reusable Conversus fidelity gate: detect-spec-shape.sh probe classifier, normalize-spec.sh thin wrapper dispatching through dispatch-interface.sh (no hardcoded LLM calls), templates/spec-normalizer-prompt.md, a reusable Conversus adapter (scripts/dispatch/adapters/tool/conversus.sh with check/gate/parse-verdict subcommands, graceful degradation, CONVERSUS_STUB escape, three-value exit contract 0/1/2), commands/conversus-gate.md designed for M013/M014 reuse with different presets, templates/conversus-presets/normalize-fidelity.yml, templates/gate-result.md, and integration into commands/ingest.md as a pre-chunker 6-step pipeline (detect-shape → normalize → resolve-policy → fidelity-gate → chunker → rebuild) gated by a new `ingest` intensity stage (Quick=normalize; Standard/Full=normalize+fidelity-gate). A 222-line user guide (docs/ingesting-arbitrary-specs.md) covers flag semantics, BLOCK resolution, stub modes, and preset extension.
  - milestone-summaries :: .orchestrator/milestones/M012/M012-SUMMARY.md:115:None. M012 phase outputs are verified via static gates and per-phase orchestrators; no runtime telemetry, no logs. M019 Tier 1 metrics emitter (shipped `fdc3649`) will instrument future dogfood loops — M012 itself predates that emitter's instrumentation on its phase-close events.
  - milestone-summaries :: .orchestrator/milestones/M012/M012-SUMMARY.md:12:  - "M013 (GitHub Issues/Projects/Milestones sync inherits chained-gate wrapper + loud-fail convention + diagnostic idempotent-helper pattern); M014 (comment triage consumes Giscus threads keyed by pathname + wiki-giscus-remap.sh remap contract); M020 (promoted by D011 evaluation — extends knowledge subtree with review-state + query surface); M019 Tier 2/3 (M012 is first non-self dogfood substrate for unit_close records on future redeploys); .orchestrator/DECISIONS.md roadmap ordering (M020 insertion between M014 and M019 Tier 2/3); live gh-pages branch on this repo (operator-gated); consolidation runbook (wiki-giscus-remap.sh --dry-run invocation before any discussion-bearing rename)"
  - milestone-summaries :: .orchestrator/milestones/M012/M012-SUMMARY.md:16:  - "AD-1 D011 ship cross-refs only (defer review-state + query surface to M020); AD-2 deploy to same repo gh-pages + same Discussions; AD-3 SSOT via include-markdown plugin (no copy, no symlinks); AD-4 archive included labeled with no date cutoff; AD-5 Giscus mapping=pathname + documented remap script; AD-6 all phases/tasks included with collapsible per-milestone nav; AD-7 MkDocs + Material + include-markdown-plugin + Giscus + GitHub Pages framework stack locked; AD-19 single-script-file Check shape with MEM004 carve-out for scanner/diagnostic/wrapper internals; D011 mechanical evaluation rule (1 of 3 criteria -> M020 promoted); Constitution VI state-on-disk-is-truth preserved via include-only projection; Constitution XIV no-speculative-complexity (no flags, no plugin drivers, no YAML-driven wrappers); Constitution XV surgical precision (each phase touches disjoint file surfaces; cross-phase additions via marker-bounded blocks + EOF-append); MEM001 Bash 3.2 discipline across all shipped scripts; loud-fail-over-silent-empty for ENV empty defaults; SC-10 self-contained wiki/ + sibling-gate allow-list extension policy; SC-12 pending-sentinel deploy record for operator-gated outcomes"
  - milestone-summaries :: .orchestrator/milestones/M012/M012-SUMMARY.md:18:  - "scanner-to-stubs-to-nav three-stage pipeline with additive append-only emission blocks keeping downstream generators stable across phases; marker-bounded atomic writes to shared config files with shasum byte-identity verification across edits; include-only SSOT with spec-quote exclusion for task-plan and payload files; pure-string Bash 3.2 primitives (PID-suffixed /tmp list files as associative-array replacement; IFS-slash positional-parameter normalize_path with no realpath dep; parallel indexed-array pattern registries; mktemp list-file + while-IFS-read as find-pipe-while replacement); SKIP-as-PASS at gate boundaries for optional toolchain (mkdocs / built site / phase-close summary); phase-suite orchestrator parallel-indexed-variable pattern with eval indirection and stderr-indent-on-FAIL; cross-phase allow-list extension as sibling-gate maintenance; self-scan carve-out via assignment-line regex skip in compat gates; MEM004 carve-out for scanner and diagnostic and wrapper internals with single-script-file Check-layer shape; loud-fail-over-silent-empty convention for missing external config; chained-gate deploy wrapper with first-non-zero-aborts semantics and fixed-verb per-gate output (GATE/BUILD/DEPLOY/DRY-RUN/OK/FAIL); graceful-absent-tool and graceful-absent-artifact patterns with dry-run-exit-0 and live-path-exit-1 split; pending-sentinel artifact for operator-gated outcomes; D011 mechanical-evaluation record as first-class phase artifact; EOF-append pattern for multi-author operator guides preserving prior section byte-identity; heading-as-section-boundary convention for README (distinct from mkdocs.yml marker-comment convention); theme-override as AD-3-compliant injection surface (append-at-render vs body-rewrite); scanner-presence-gated subtree emission (Archive / Knowledge Entries disappear cleanly when scanner sees zero records); stderr-noise-stdout-success diagnostic convention for machine-readable capture"
  - milestone-summaries :: .orchestrator/milestones/M012/M012-SUMMARY.md:40:- **P01 verify suite** — 9 gates (`wiki-self-contained`, `requirements-pinned`, `include-plugin`, `ssot`, `exclusion-policy`, `nav-structure`, `serve-smoke`, `index-placeholder`, `bash32-compat`) + `m012-p01-phase-suite.sh` orchestrator. `serve-smoke` SKIPs gracefully when `mkdocs` is absent.
  - milestone-summaries :: .orchestrator/milestones/M012/M012-SUMMARY.md:81:- **Phase-suite orchestrator with parallel-indexed-variable pattern** — `gates_0..gates_N` + `eval` indirection (no Bash 4 `${!name}`), stderr captured to `TMP_LOG` and two-space-indented on FAIL. P02 established the shape; P03/P04 reused verbatim.
  - milestone-summaries :: .orchestrator/milestones/M013/M013-SUMMARY.md:14:  - "scripts/integrations/github-common.sh; scripts/integrations/github-init.sh; scripts/integrations/github-sync.sh; scripts/integrations/github-status.sh; scripts/integrations/github-conversus-gate.sh; scripts/integrations/uat-ingest.sh; scripts/integrations/sidecar-init-pending.sh; templates/github-integration-sidecar.json; templates/conversus-presets/m013-uat-defect-merge.yml; commands/github-init.md; commands/github-sync.md; commands/github-status.md; references/github-integration.md; .github/ISSUE_TEMPLATE/uat-bug.yml; knowledge/spec/defect/README.md; scripts/knowledge/rebuild-index.sh; scripts/knowledge/lib/index-utils.sh; scripts/knowledge/write-summary.sh; scripts/lifecycle/after-verify-sync.sh; scripts/dispatch/adapters/runtime/claude-code.sh; packaging/bundle/hooks/post-verify.json; packaging/install/install-claude-code.sh; scripts/verify/graphql-call-shape.sh; scripts/verify/m013-p01-phase-suite.sh; scripts/verify/m013-p02-phase-suite.sh; scripts/verify/m013-p03-phase-suite.sh; scripts/verify/m013-p04-phase-suite.sh"
  - milestone-summaries :: .orchestrator/milestones/M013/M013-SUMMARY.md:77:- **`conversus_gate_invocation` JSONL records** at the same path (preset/verdict/duration/rc fields via `emit_conversus_gate_record`, T03 ownership reused by T05 standalone gate).
  - milestone-summaries :: .orchestrator/milestones/M013/M013-SUMMARY.md:8:  - "opt-in GitHub native integration projecting orchestrator state to Issues + Project v2 + Milestones with FR-4 byte-identity markers; operator-owned sidecar at .orchestrator/integrations/github.json (gitignored) with templates/github-integration-sidecar.json schema (sync_mode + sub_issue_mode + per-item cache); tri-state orchestrator:github status command (absent / pending-operator-complete / configured) + status --verify-cache FR-18 read-only divergence probe; orchestrator:github init --re-init FR-14 adoption branch sourcing existing repo state via marker search; orchestrator:github sync 626-line walker + reconcile loop with --dry-run manifest + lock acquisition + updateProjectV2ItemFieldValue Project v2 status closure + FR-16 pre-flight rate-limit probe + per-call classify_gh_rc wrapper releasing lock before exit 3/4 (FR-11 reversibility); UAT bug ingestion loop (.github/ISSUE_TEMPLATE/uat-bug.yml + scripts/integrations/uat-ingest.sh + knowledge/spec/defect/SPEC-DEFECT-NNN schema with chunk-lookup-failed sentinel); FR-9 additive ## Spec Chunks emit in scripts/knowledge/rebuild-index.sh + scripts/knowledge/lib/index-utils.sh emit_spec_chunks_section helper; opt-in conversus UAT PR gate scripts/integrations/github-conversus-gate.sh wrapping M011/P07 adapter --strict with 30s external watchdog (Constitution XII) + FR-13 preset templates/conversus-presets/m013-uat-defect-merge.yml (red-blue defect-advocate vs fix-advocate, BLOCK-on-tie); post-verify lifecycle hook packaging/bundle/hooks/post-verify.json + scripts/lifecycle/after-verify-sync.sh wrapper + claude-code.sh runtime adapter hook_count 5->6 + FR-12 v1 Claude-Code-only enforcement via negative-grep guard on codex/cursor adapters+installers; scripts/integrations/github-common.sh shared lib (12 P02 helpers + 1 P03 helper + 5 P04 helpers — orchestrator-id derivation + FR-4 marker primitives with shasum byte-identity + AWK depth-aware sidecar item upserts + http_probe + sidecar_update_item_cache + emit_tier1_record + classify_gh_rc + emit_conversus_gate_record + gh_marker_search_remote + auth/sub-issue/label-collision preflights); scripts/verify/graphql-call-shape.sh FR-5 three-shape whitelist CI lint; references/github-integration.md full P01..P04 reference doc with Auth Modes / Sub-Issue Representation Modes / Full Mapping Table / Re-init Adoption Contract / Sync Workflow / Sync Action Mapping / Scope Boundary table; commands/github-init.md + commands/github-sync.md + commands/github-status.md (MEM012 structure); FR-17 unit_close + conversus_gate_invocation JSONL emitters at ${ORCHESTRATOR_ROOT}/observability/tier1.jsonl in M019 Tier 1 shape; scripts/integrations/sidecar-init-pending.sh pending-sentinel bootstrap with clobber-refusal exit 2; ~30 verification gates across 4 phase-suite orchestrators (P01 9 + P02 8 + P03 8 + P04 12 + FR-5 lint regression guard)"
  - milestone-summaries :: .orchestrator/milestones/M014/M014-SUMMARY.md:16:  - "D016 (M014 extended scope — native orchestrator:specify + AGENTS.md dual-write + comment classifier); D017 (14 MITs applied pre-discuss via cooperative conversus deliberation); D018 (P03 deferred pending M012 wiki deploy + inbox dogfood; retired by D023); D019 (three-pass scaffold→author→gate flow intensity-scaled per existing intensity engine + universal TODO pre-flight guard on conversus gate adapter); D020 (spec-metrics chunks-first scoping bug + scaffold-placeholder-in-backticks gate false-positive); D023 (M014/P03 unblock — synthetic P99 placeholder retired in favor of explicit Preflight note + SC-16 relaxed to best-available signal with regex/heuristic v1 classifier pin and retune trigger ≥30 actioned comments OR ≥20 percent calibration divergence); byte-identical dual-write (not transform-based) between CLAUDE.md and AGENTS.md across all P02 write-sites; loud-error on slug collision (not silent overwrite); stub-to-full transition contract (P01 stubs emit stable structured fields with zero values so downstream phases replace body without changing caller contract — used for spec-complexity-probe, commands/specify subcommands, RUNTIME-ASSUMPTIONS entries, references/spec-management sections); append-only registry discipline for RUNTIME-ASSUMPTIONS (multiple phases append entries, heading byte-identical across body refreshes); hardening-spec exception keyed on fr_count==0 AND user_story_count>=5 (M016/M021 pattern); OR-semantics across five complexity axes with boolean hardening override; D007 reuse discipline honored throughout — conversus adapter byte-identical pre/post M014 despite two new presets landing; two-tier SC-5 invariant verification (source-level grep in scripts/comments excluding apply.sh + runtime-event assertion in pipeline verifier); patch -N forward-only for stale-diff probe (vanilla --dry-run auto-reverses silently); env-overridable sub-script paths (COMMENTS_FETCH, COMMENTS_CLASSIFY, COMMENTS_ADAPTER, COMMENTS_UAT_INGEST, COMMENTS_APPLY, COMMENTS_REJECT, COMMENTS_TRIAGE) for hermetic testability without D007 violation; deterministic content-addressed queue/triage filenames (shasum-256 first 8 hex of comment URL); adapter-missing as routing verdict not failure (M013/FR-13 graceful-degradation at comments seam)"
  - milestone-summaries :: .orchestrator/milestones/M014/M014-SUMMARY.md:18:  - "Template SSOT with ground-truth heading fixture; marker-bounded atomic splice with byte-preserved outside region (SC-6a); subcommand surface with deferred body (diagnostic-stub exits distinct codes; full semantics land in later phase without command-file re-authoring); hermetic scratch project pattern for end-to-end command testing; verifier self-exemption for rule-embedding gates; awk-field extraction for structured corpus parse; write-site enumeration discipline with scanner gate; allow-list pattern for orthogonal writes (runtime-native full-file renders); hermetic-root via dirname of ORCH_ROOT (decouples dual-write target from repo root); advisory-first drift severity model; one-time migration script pattern with --dry-run/--apply/--force + idempotent already-migrated short-circuit; CC-first with defensive fallback (runtime gate + empty-response bailout — never fails caller); STATE_ROOT vs PROJECT_ROOT separation for mutation sinks vs dependency lookups; CONVERSUS_STUB hermetic testing via fixture-copy path in adapter; dispatch-interface mock via backup-and-swap; section-split awk + per-section classifier; append-with-body-refresh for registry entries (heading byte-identical body refreshed); bash32 scanner comment-strip to avoid false-positive on comment prose; hermetic-test stub via env-var pointing at JSONL fixture (GH_API_STUB, GH_GRAPHQL_STUB); per-surface source_surface filter on shared stub fixture; FR-19 dry-run JSONL action records as no-disk-write counterpart; grep -F literal-string match for URL dedup; stdout-verdict + stderr-INFO split; single-agent cooperative preset for refinement-only LLM calls; patch -N forward-only stale-diff probe; env-overridable sub-script-path hermetic-test pattern (COMMENTS_*); non-executable adapter as routing verdict (graceful-degradation test pattern); deterministic content-addressed filenames for idempotent queue/triage; two-tier must-not-happen invariant verification (source grep + runtime event assertion); bash32+lint omnibus with dynamic enumeration + basename self-exemption; zero-prompts hermetic scratch + fixture replay under --yes; outside-marker shasum invariant pre/post dual-write; phase-suite orchestrator with IFS-newline iteration + per-gate rc capture + FAILED_GATES accumulator"
  - milestone-summaries :: .orchestrator/milestones/M019/M019-SUMMARY.md:18:  - "Env-var side-channel for legacy-helper behavior injection;Zero-token instrumentation via stdout capture-and-replay;Byte-identical-modulo-normalization diff;Sentinel-scoped JSON span overwrite bash 3.2 no jq;Never-abort degradation via module-scoped warning channel;Module-scoped globals subshell loss workaround via mktemp capture;BSD awk in-keyword pitfall remedy;Split-needle self-match avoidance in pattern gates;Trap-safe mutate/restore;Hermetic tmp-snapshot pattern with ORCHESTRATOR_ROOT override;Filename-routed stub backend adapter;Directory-path artifact check;Lexical ISO-8601 timestamp comparison;Child-granularity pass_rate filter;Prefix-match any-null propagation in cost rollup"
  - milestone-summaries :: .orchestrator/milestones/M019/M019-SUMMARY.md:65:- **Zero-token instrumentation via stdout capture-and-replay** — PAYLOAD_CAPTURE tempfile + `cat` makes the emitter run strictly after stdout, contributing zero bytes. Generalizes to any "log this work without polluting the artifact" requirement.
  - milestone-summaries :: .orchestrator/milestones/M019/M019-SUMMARY.md:68:- **Never-abort degradation via module-scoped warning channel** — estimator returns empty + sets `_PRICING_WARNING_REASON`; emitter records `null` + `pricing_warning` and exits 0. Dispatch continues.
  - milestone-summaries :: .orchestrator/milestones/M019/M019-SUMMARY.md:78:- **SC-1** exactly-one-dispatch_usage-per-dispatch; **SC-2** exactly-one unit_close per write-summary; **SC-3** Goodhart cost+quality pairing mandatory; **SC-4** source + granularity enum additivity for Tier 2/3 forward compat; **SC-6** byte-identical stdout (zero-token instrumentation); **SC-7** fixture-rollup greppability demo; **SC-10** pre-M019 consumer additivity; **SC-12** P00→P01 hard ordering at epoch `2026-04-18T02:21:28Z`; **SC-13** no regression of existing tests/anti-pattern-lint/M021 P04.
  - milestone-summaries :: .orchestrator/milestones/M021/M021-SUMMARY.md:16:  - "AD-2 rejects-dominate-rewrites precedence, AD-5 locked corpus size (20 entries), AD-6 hook_reject_recovered is the success signal not a failure, AD-8 dogfood-attestation pattern (continues M016), AD-11 append-only discipline for DECISIONS+ANTIPATTERNS, AD-19 single-script-file invocation shape for agent-facing commands, D012 M021-before-M019 sequencing, MEM004 gate-internals carve-out, constitution IX Bash 3.2 compat, constitution XIV no speculative complexity (locked entry count), constitution XV surgical precision (D012 row normalization)"
  - milestone-summaries :: .orchestrator/milestones/M021/M021-SUMMARY.md:56:- **Concatenation-split forbidden literals** — compat gates and linter patterns split their needle strings so the gate source doesn't self-match during its own scan. Established P03, continued P04.
  - milestone-summaries :: .orchestrator/milestones/M028/M028-SUMMARY.md:34:P03 (Findings B + C + G classifier — AP-010 through AP-014, 215m) extended the shape-guard surface end-to-end across documentation, classifier, hook reject_lookup, regression corpus, and per-finding verifier coverage. ANTIPATTERNS.md grew 214 → 338 lines via append-only edits adding AP-010 (cmd-sub-in-pattern), AP-011 (quoted-arg-newline-hash), AP-012 (multiline-quoted-script), AP-013 (unquoted-brace-glob), AP-014 (xargs-sh-c-compound-body). Five new private detectors landed in `scripts/verify/lib/shape-classifier.sh` as bash 3.2 + POSIX-sh-safe character-by-character quote-state machines; AP-014 inserted BEFORE the existing AP-009 `_sc_count_top_level_stages > 2` check (load-bearing for SC-6 — without that ordering SE-09 regresses to compound-chain-gt2); AP-010..AP-013 inserted AFTER the four M021 reject checks so the more-general rules continue to fire first. CON-5 one-level body-descent implemented via OPAQUE placeholder substitution. Five new case arms landed in the hook `reject_lookup`; existing four M021 baseline arms preserved byte-for-byte (CON-7). Seven verbatim entries appended to `tests/fixtures/m021-prompt-corpus.txt` (IDs 21..27: 5 AP-anchored evidence + 1 negative-control + 1 boundary case); IDs 01..20 preserved byte-for-byte. New 27-entry replay harness `tests/run-prompt-corpus-replay.sh` (M028 SC-1 spec-named gate) coexists with the M021 historical harness (both consume the same fixture, dual symmetric coverage). Five per-finding verifiers (finding-B, finding-C, finding-G-classifier, finding-G-self-conformance) plus `run-all.sh` rolling up to the canonical `M028: <pass>/7 findings verified` summary line. Patterns: append-only invariant on documentation registers, closed-pattern-set discipline, AP-014 ordering invariant in `classify_command`, OPAQUE-token one-level-deep recursion bound, char-by-char quote-state machines for shape detection, M021-strict-superset case extension discipline, self-conformance verifier scope-narrowing precedent (resolution + reject_lookup printf bodies, not dispatch), bash-parameter-expansion case-arm body extraction, two-harness symmetric coverage of one corpus, pinned-INPUT alignment between corpus and verifier, verifier-as-precedence-claim (SC-6 surfaced as discrete PASS line), header-comment-as-cross-reference-locator.
  - milestone-summaries :: .orchestrator/milestones/M030/M030-SUMMARY.md:18:  - "pre-implementation fixture corpus + version-controlled labels (P00 — closed-milestone-only sourcing avoids in-flight bias; rubric application per-plan with judgment, no automated keyword pre-pass; rationale field as audit trail); classifier-as-pure-bash + heuristic-table-as-SSOT (P01 — deterministic <100ms no-network with no jq/python3 hard dependency; bash 3.2 compatible); 4-verdict closed-enum shadow-compare (P02 — flip_recommendation always ∈ {ready, partially_ready, block, evidence_insufficient}, no string interpolation); additive-jsonl-schema CON-2/FR-19 preserved across 6 schema extensions (P02 added 4 fields, P03 added 1, P04 added 3, P06 added 1 — all additive, all preserve byte-equality on shadow-off branch); pre-amendment golden-baseline + delegate-and-pass-through cross-phase wrappers (P05/T01 inverted P04/T01 pattern: capture HEAD output BEFORE amendment, delegate post-amendment SC-11 verifier to upstream P02 wrapper); ORCHESTRATOR_ROOT carve-out for fixture-routing without modifying the surface's resolver (P05/T01); additive-field-on-shadow-on-emit lineage (P02/T02 + P04/T03 + P06/T02 — single field appended to printf format string + arg list on shadow-on branch only; shadow-off branch byte-untouched; SC-11 contract via P02 delegate-and-pass-through wrapper); env-var-seam-for-{shadow-mode,corpus-path,routing-table-path,anomaly-jsonl-path,allow-dirty-mark} convention (M030_SHADOW_MODE / M030_SHADOW_COMPARE_CORPUS / M030_ROUTING_TABLE_PATH / M030_ANOMALIES_JSONL_PATH / ORCHESTRATOR_ALLOW_DIRTY_MARK); phase-suite straight-line aggregator pattern (P02–P07 all share the no-loops-no-eval-no-compound-chains-AD-19-clean shape; per-gate parallel scalars + bash <path> + rc=$? + if-block accumulation; single SUMMARY: line); plan-amendment-not-task-reopen pattern applied at every phase close for artifact-grep predicate divergences (line-count relaxation + key-link source inversion to forward-direction reachable file with parenthetical reason); structurally-distinct-T04 ceremony fusing phase + milestone close (P07 only — when final phase IS milestone-close gate); idempotent corpus synthesizer + sha256-equality verifier (P05/T01 + P06/T01 + P07/T01 lineage); acceptance-battery as straight-line SC delegator (P07/T02); one-shot evidence ledger discipline (P07/T03 — committed snapshot at milestone close, never regenerated dynamically; operators re-run the contract for fresh stdout); milestone-grain unit_close convention (P07/T04 — first-of-kind record introduced; granularity=milestone + phase_count=N); pre-amendment-tolerant verifier pattern (P03/T01 — same verifier flips from tolerant to strict as deliverable lands); CON-3 closure verifier (P02/P03/P04 — runtime awk-extraction of resolution table from templates/model-routing.yml prevents hardcoded model IDs; HEAD-vs-WT per-pattern grep counts mechanically re-checkable); append-only invariant on JSONL via inode preservation + first-N-lines bit-identity + line-count delta = +1 (P02/P04/P06)"
  - milestone-summaries :: .orchestrator/milestones/M031/M031-SUMMARY.md:44:**Forward-pointing notes**: (a) milestone-level housekeeping commit pending — pre-existing working-tree drift (M030 ROADMAP, AGENTS.md, KNOWLEDGE-INDEX.md, `commands/dispatch.md`, `references/RUNTIME-ASSUMPTIONS.md`, `scripts/dispatch/build-context.sh`, `templates/orchestrator-config-default.yml`, `tools/verify/p00-phase-suite.sh`, `.orchestrator/doctor-history.jsonl`, MEM `hit_count` increments) remains uncommitted across the milestone; recommended sweep before `orchestrator:consolidate`. (b) `direct-mode-execution-log.jsonl` knowledge-section-tokens reports 0 in some records — likely instrumentation gap in `build-context.sh` counting only resolved-knowledge inserts not standard frontmatter MEM body; surfaces as forward signal for future budget-tightening work, not a blocker. (c) M031 closes the launch queue's "right-sized entry" slot per the forward roadmap; M032 (wiki distribution + init integration) is next, then M033 (project onboarding experience), then M029 (CLI UX), then M035 (packaging & distribution) — that final triplet is the launch event.
  - milestone-summaries :: .orchestrator/milestones/M032/M032-SUMMARY.md:16:  - "FR-1,FR-2,FR-3,FR-4,FR-5,FR-6,FR-7,FR-8,FR-9,FR-10,FR-11,FR-12,FR-13,FR-14,FR-15,FR-16,FR-17,FR-18,FR-19,FR-20,FR-21,FR-22,MIT-001,MIT-002,MIT-005,MIT-006,MIT-007,MIT-008,MIT-010,MIT-011,AD-5,AD-7,AD-19,CON-1,CON-3,CON-4,CON-5,CON-6,US-1,US-2,US-3,US-5,US-6,US-7,US-8,SC-1,SC-2,SC-3,SC-4,SC-5,SC-6,SC-7,SC-8,SC-9,SC-10,SC-11,SC-12,SC-13,SC-14,Finding-J,NG-9,Constitution-I,Constitution-VI,Constitution-VIII,MEM001,MEM012,MEM013,MEM030,MEM031,M026-stub-envelope,M030-lineage,M031-lineage"
  - milestone-summaries :: .orchestrator/milestones/M033/M033-SUMMARY.md:8:  - "orchestrator:start warm conversational front door (FR-1/FR-2/FR-20/FR-21/FR-22) with four onboarding branches (greenfield-empty / greenfield-with-materials / existing-codebase / migrating); orchestrator-native constitution authoring (FR-3/FR-4/FR-5/FR-6/CON-3) -- zero spec-kit dependency at launch via Principle XVI standalone-gate; deterministic codebase ingestion (FR-7/FR-8/MIT-005) with rich-context import and supersede-aware MEM cross-references; materials intake with deterministic CON-4 drift detection (FR-9) -- 4 fenced SSOT blocks + 3-detector pipeline + byte-deterministic reconciled pre-spec; ideation with MIT-007 live contradiction detection (FR-10/FR-17) -- 7-question grilling-protocol flow + opt-in conversus stress-test; migrate-routing glue (FR-11/FR-12) with FR-12 migrate-then-ingest dup-prevention sentinel; customblock drafter (FR-13/FR-14) with strict aggregation discipline (Constitution XV) -- floor-not-ceiling preservation + branch-dependent variant + idempotency gate; --with-wiki paired-launch passthrough (FR-15/CON-1/MIT-001) two-mode contract under stub OR real M032/P02 wiki-init invocation; --with-github passthrough (FR-16) ordered after wiki-init; grilling-shell + glossary inline-update writer (FR-17/FR-18) with 4 fenced SSOT blocks for contradiction-pairs + glossary-triggers; friendly-tester pass artifact + validate-report.sh mechanical gate (FR-19/SC-15) with US-8 AS-5 signed-attestation fallback path"
  - milestone-summaries :: .orchestrator/milestones/M035/M035-SUMMARY.md:62:- **#Q-9 (install-meta.txt schema extension)** → bundle-hygiene rule + magic-comment opt-out (absorbed into P02 T05).
  - milestone-summaries :: .orchestrator/milestones/M037/M037-SUMMARY.md:8:  - "Wiki team-feedback-ready surface (P01) — homepage card grid + wiki.landing_cards: config schema (FR-1) + version: → nav title via stub generator (FR-5) + DR-### heading-shape pivot + authoring convention (FR-6) + navigation.tabs + toc_depth:2 + content.action.edit (FR-7..FR-9) + install-template config.yml clobber fix (FR-10/FR-11). Publishing-robustness paper-cut bundle (P02) — feedback:* routing arm in stub generator (FR-18) + workflow-based Pages publishing scaffold + wiki-deploy.sh demote to pre-push validation (FR-19/FR-20) + private-repo site_url visibility branch (FR-21) + OUT-OF-SCOPE diagnostic-budget collapse with --verbose escape hatch (FR-22a) + org-level discussions redirect callout in wiki/README.md (FR-22b). PBJ-feedback polish series (P03 round-3.5 / round-3.5-sync / round-4 / round-5) — pymdownx.emoji material.icons generator (P1.1) + render_landing_cards orphan-card pre-filter (P1.2) + wiki-init.sh final-step stubs+nav regen (P1.3) + glossary include-wrapper idempotent stub-replacement (P1.4) + frontmatter version: projection + published:-desc sort (P2.1) + file:// → repo_url/blob/main/ projection in metadata tables (P2.2 partial slice of FR-13) + [unknown] suffix dropped on proposals (P2.3) + wiki/overrides/main.html breadcrumb shim absorbed from PBJ verbatim (P3.1) + file-feedback affordance banner above Comments (P3.2) + framework-managed wiki/overrides/ refresh step in wiki-init.sh (round-4) + dropped navigation.sections from theme.features for collapsible-drawer left-nav (round-5)."
  - milestone-summaries :: (capped at 5 hits — more may exist)

