---
schema_version: "1.0"
type: phase-summary
id: "P01"
parent: "M046"
milestone: "M046"
provides:
  - "Throwaway default-DENY PreToolUse probe evidence: deny-drive.log (6/6 PASS + live-e2e=deferred-to-SC-5) and install-matrix.log (shape A+B all-1s) proving the FR-9 hook premise for #Q-1,#Q-4 JSONL-half verdict: unit_close records readable at unit grain mid-segment (4/4 before loop_exit,0.35-0.75s observe lag); unit_close estimated_cost_usd nullable via any-null propagation; recommended FR-7/FR-8 cost-source split in CADENCE-FINDINGS.md,P01 decision-gate output: P01-VIABILITY-EVIDENCE.md with grep-stable 'VERDICT: #Q-1 PARTIAL' (deny + install legs PASS,live-e2e discharged by milestone-blocking SC-5; FR-9 proceeds as specced) and 'VERDICT: #Q-4 PASS' (SC-3 precondition satisfied; FR-7 reserve+duration primary with non-null per-unit JSONL reconciles,FR-8 true-up from claude -p total_cost_usd) plus decision-gate routing for P04/P05; five durable verifiers tools/verify/m046-p01-*.sh including the 4/4 phase-suite aggregator"
requires:
  - "none"
affects:
  - "P04,P05"
key_files:
  - ".orchestrator/milestones/M046/phases/P01/spike/hook/unattended-deny-probe.sh,spike/hook/drive-hook-case.sh,spike/hook/run-install-matrix.sh,spike/hook/deny-drive.log,spike/hook/install-matrix.log,spike/hook/policy.txt,spike/hook/fixtures/,.orchestrator/milestones/M046/phases/P01/spike/cost/run-cadence-probe.sh,.orchestrator/milestones/M046/phases/P01/spike/cost/drive-segment.sh,.orchestrator/milestones/M046/phases/P01/spike/cost/cadence.jsonl,.orchestrator/milestones/M046/phases/P01/spike/cost/CADENCE-FINDINGS.md,.orchestrator/milestones/M046/phases/P01/spike/cost/fixture/milestones/MFIX/,.orchestrator/milestones/M046/phases/P01/P01-VIABILITY-EVIDENCE.md,tools/verify/m046-p01-viability-evidence.sh,tools/verify/m046-p01-hook-deny-proof.sh,tools/verify/m046-p01-install-matrix.sh,tools/verify/m046-p01-cadence-log.sh,tools/verify/m046-p01-phase-suite.sh"
key_decisions:
  - "Bash default-behavior scope: non-dangerous Bash passes in the probe (full Bash default-deny is P05 policy design); allow_bash proven to override the dangerous-pattern deny; --all truncates the log for deterministic re-runs,stub realized as driver-writes-summaries-via-real-write-summary.sh (auto-loop.sh has no --dispatch-stub flag; M031 seam is Tier-A-only in do-entry.sh); fixture root doubles as ORCHESTRATOR_ROOT so unit_close routes to fixture log; loop_exit stamped at exit-detection so post-exit drains classify conservatively; P02 seeded one labeled synthetic dispatch_usage to exercise cost aggregation with zero spend,#Q-1 rated PARTIAL not PASS because the live-e2e leg was deferred - honest verdict per Principle II,safe because SC-5 is already milestone-blocking and non-stubbed; #Q-4 rated PASS with the nullable-cost caveat folded into the FR-7/FR-8 split (JSONL supplies grain,JSON result supplies truth) rather than downgrading to PARTIAL,since the roadmap gate's negative branch (records only at loop exit) did not occur; verifiers written against the actual T01/T02 log shapes (six case= lines + live-e2e deferral line; jsonl_append observations with record_type unit_close) not the plan's guessed shapes"
patterns_established:
  - "New PreToolUse hook = staged file + one managed settings-merge fragment (matcher Write|Edit|Bash|mcp__.*),zero installer changes; isolated-HOME probe discipline,cadence-probe shape: background segment driver + 0.2s JSONL poller + per-record boundary-ordering analysis,all inside one wrapper script (AD-19),grep-stable per-question verdict lines (VERDICT: #Q-N STATUS - consequence) at line start in decision-gate evidence docs; deny-proof verifier asserts exact case=NAME expected=N actual=N result=PASS lines"
drill_down_paths:
  - ".orchestrator/milestones/M046/phases/P01/tasks/T01-hook-portability-spike-SUMMARY.md, .orchestrator/milestones/M046/phases/P01/tasks/T02-cost-cadence-probe-SUMMARY.md, .orchestrator/milestones/M046/phases/P01/tasks/T03-evidence-verdicts-SUMMARY.md"
duration: "2136m"
verification_result: "pass"
completed_at: "2026-07-13T15:40:26Z"
observability_surfaces:
  - "none"
---

P01 resolved both CON-6 decision-gate questions with real, non-stubbed evidence, exactly on the M045-P01 spikes-first pattern. T01 (hook-install portability, #Q-1): a throwaway default-DENY PreToolUse probe proved deny semantics through the real stdin/exit-2 hook contract — 6/6 direct-drive cases (out-of-scope Write, git push, and MCP call denied with stderr reasons; allowlisted Write/Bash passed; missing policy failed closed), verified under literal /bin/bash 3.2.57 — and the M028 consumer path accepted the NEW hook on both install shapes (source-tree and bundle-staged) via a staged file plus one managed settings-merge fragment, coexisting with the shape-guard, idempotent on re-merge, clean on uninstall. All runs used isolated scratch HOMEs; the operator's real ~/.claude was never touched. The optional live claude -p leg was deferred to SC-5 (already milestone-blocking), so #Q-1 verdicts PARTIAL: FR-9 proceeds as specced with no rerouting Decision row. T02 (cost-read cadence, #Q-4): a zero-LLM-spend stub-driven probe of the real auto-loop.sh showed unit_close records readable mid-segment at unit grain (4/4 before loop_exit, 0.35-0.75s observe lag) — the SC-3 precondition holds — but surfaced that JSONL cost values are nullable advisory estimates (any-null propagation; step-G --cost lands under a disjoint key the aggregator never reads). #Q-4 verdicts PASS with the source split fixed: FR-7's in-segment watchdog keeps the conservative reserve + duration probe primary and reconciles per completed unit only on non-null estimates; FR-8 trues up the ledger at each segment boundary from the exiting child's claude -p --output-format json total_cost_usd (P00-proven authoritative actual). JSONL supplies grain, JSON result supplies truth. T03 consolidated both into P01-VIABILITY-EVIDENCE.md with grep-stable VERDICT lines plus decision-gate routing, and authored five durable verifiers including the 4/4 phase-suite aggregator. Verification: phase suite 4/4, check-must-haves all PASS (4 Truths, 6 Artifacts, 2 Key Links). P04 and P05 may now build on confirmed premises; neither decision-gate negative branch fired.
