---
schema_version: "1.0"
type: phase-summary
id: "P02"
parent: "M046"
milestone: "M046"
provides:
  - "auto-loop.sh env-gated deterministic outcome-marker mechanism (ORCHESTRATOR_SELF_CONTINUE_MARKER=1 gate,atomic temp+rename write to $MILESTONE_DIR/.self-continue-outcome,exit-code + exit-0-substate keyed vocabulary),FR-17 legacy-parity golden + verifier,shared m046-p02 verifying-tree MFIX fixture,Hardened self-continue driver: FR-15 charset allowlist on milestone-dir + argv-array run_child (no sh -c re-parse) + FR-14 deterministic CHILD_ABORT wrapper truth table + continue-class marker mapping (rotation/planning/phase_complete/validating -> CONTEXT:ROTATE) + distinct SELF_CONTINUE:CHILD_ABORT terminal + atomic child_abort marker write + ORCHESTRATOR_SELF_CONTINUE_MARKER=1 env-gate export to the child,SC-9 non-stubbed full-exit-set battery m046-p02-marker-exit-contract.sh (11/11: exit-0 substates planning/phase_complete/validating plus 1/2/3/10/11/12/13/14,dual exit+marker assertion) and m046-p02-child-abort.sh (5/5 kill/crash/stall cases through the real driver) plus 11 checked-in fixture trees under tests/fixtures/m046-p02/exit-trees/,Atomic-write-discipline verifier (static grep leg on both writers + behavioral no-residue leg),commands/auto.md FR-14 outcome-marker contract amendment (writer of record,child_abort driver terminal,continue-class table,atomicity,FR-17 attended parity),and the P02 phase-suite aggregator running all 7 P02 verifiers plus 4 M045 driver regressions (11/11 gate)"
requires:
  - "none"
affects:
  - "P04,P07"
key_files:
  - "scripts/lifecycle/auto-loop.sh,tools/verify/m046-p02-marker-unit.sh,tools/verify/m046-p02-legacy-parity.sh,tests/fixtures/m046-p02/verifying-tree/MFIX/,tests/fixtures/m046-p02/legacy-parity.golden,scripts/lifecycle/self-continue-drive.sh,tools/verify/m046-p02-injection-reject.sh,tools/verify/m046-p02-driver-continue-class.sh,tools/verify/m046-p02-marker-exit-contract.sh,tools/verify/m046-p02-child-abort.sh,tests/fixtures/m046-p02/exit-trees/,tools/verify/m046-p02-atomic-write-discipline.sh,tools/verify/m046-p02-phase-suite.sh,commands/auto.md"
key_decisions:
  - "CON-2 satisfied as one logical mechanism in two purely-additive insertions (writer function + EXIT trap after MILESTONE_DIR validation,substate-capture case at top of _auto_output funnel); git diff audit 54 insertions 0 deletions; exit-14 rotation phase word best-effort via read-roadmap active-phase; unknown exit codes and mid-segment exit-0 substates write nothing (last-write-wins),--auto-cmd semantic change: whitespace-split verbatim with globbing disabled,no shell evaluation (documented in header); AUTO_CMD string default removed — default claude spawn built as argv with milestone-dir inside one pre-validated element; charset allowlist rejects empty/leading-dash/dot-dot/non-[A-Za-z0-9_./-] with exit 2 before any command line; CHILD_ABORT truth table: rc>=128 overwrite,1..127 no-marker write child_abort,1..127 with-marker keep,rc=0 no-marker preserves M045 STALLED,budget (exit 2) and rotate (exit 14) thresholds driven via read-config layer-1 env overrides SPECKIT_ORCHESTRATOR_DISPATCH_BUDGET=1 / SPECKIT_ORCHESTRATOR_SESSION_WEIGHT_LIMIT=1 because auto-loop.sh passes no config files to read-config.sh so fixture-root config.yml is never consulted on this path; dispatch log records use the canonical event:dispatch fixture shape that budget-checker/stuck-detector literally grep (record-result dispatch_method shape does NOT match); planning-ok and planning-failed nest trees as root/milestones/MFIX so ORCH_ROOT resolves to a milestones-bearing root; planning-failed realized honestly via missing roadmap with phases/ present,Direct-redirect detection via two regexes (quoted/unquoted redirect target ending in .self-continue-outcome for auto-loop; $OUTCOME_FILE target for driver),both mutation-tested against violation shapes; suite cd's to REPO_ROOT because M045 members use repo-relative paths; SUITE:/SUMMARY: line protocol per task plan (vs p01 PASS:/FAIL: style); mid-write-kill torn-marker guarantee accepted as structural Tier-3 residual (rename(2) atomicity proven as only write path),P04 SIGKILL watchdog inherits it live"
patterns_established:
  - "env-gated EXIT-trap outcome marker with atomic temp+rename and fully ${VAR:-}-guarded trap body; byte-equality golden parity verifier per M045 P03 precedent; scratch-copy-only fixture discipline for auto-loop invocations,Argv-array child spawn in POSIX sh via set -f; set -- $CMD; set +f inside a function; driver-owned abort marker written atomically via tmp.$$ + mv -f in same directory,dual exit-code+marker-content assertion as the anti-false-pass mechanism for exit-contract batteries; whole-case-dir scratch staging (cp -R case dir,defensive marker rm) so sibling files like orchestrator.lock travel with the tree,Atomic-write-discipline verifier shape: static grep leg (temp-path present + mv-rename present + zero direct redirects) paired with behavioral residue leg (live run leaves correct marker,no .tmp.* residue)"
drill_down_paths:
  - ".orchestrator/milestones/M046/phases/P02/tasks/T01-marker-write-SUMMARY.md, .orchestrator/milestones/M046/phases/P02/tasks/T02-driver-hardening-SUMMARY.md, .orchestrator/milestones/M046/phases/P02/tasks/T03-exit-contract-battery-SUMMARY.md, .orchestrator/milestones/M046/phases/P02/tasks/T04-atomicity-docs-suite-SUMMARY.md"
duration: "2710m"
verification_result: "pass"
completed_at: "2026-07-13T16:53:30Z"
observability_surfaces:
  - "none"
---

P02 closed Gaps 3 and 4 from the spec's problem statement: the self-continue marker contract is now deterministic across the process boundary, and the driver no longer evaluates interpolated shell strings. T01 landed the single CON-2-authorized additive change to auto-loop.sh (54 insertions, 0 deletions, git-diff-audited additions-only): an env-gated (ORCHESTRATOR_SELF_CONTINUE_MARKER=1, default OFF) deterministic outcome-marker writer — a trap-on-EXIT writer function plus substate capture in the existing _auto_output() funnel — writing $MILESTONE_DIR/.self-continue-outcome atomically (temp + mv -f) with a full exit-code-to-marker vocabulary (exit-0 substates planning/phase_complete/validating; 1/2/3/10/11/12/13 error/budget/stuck/complete/pause/unexpected_state/planning_failed; 14 rotation). FR-17 byte parity proven: gate absent is byte-identical (6/6 assertions) and the M045 legacy golden stays green. T02 hardened self-continue-drive.sh: a strict charset allowlist rejects metacharacter-bearing milestone dirs before any command line (SELF_CONTINUE:REJECT, exit 2); run_child() spawns the child as an argv array (whitespace-split --auto-cmd under set -f, no sh -c evaluation); the deterministic wrapper captures CHILD_RC and implements the FR-14 division of labor — rc>=128 overwrites any stale marker with child_abort, crash-without-marker writes child_abort, real error exits keep auto-loop's authoritative marker, and rc=0-no-marker preserves the M045 STALLED path. All four M045 driver regressions stay green. T03 proved SC-9 non-stubbed: 11/11 exit cases against the REAL auto-loop.sh (each asserting exit code AND marker, budget/rotate driven through the real budget-checker/context-monitor via the documented env config layer) plus 5/5 kill/crash/stall cases through the real driver — no stubs, no seeded markers. T04 added the atomic-write-discipline verifier (static shape leg mutation-tested against three violation shapes, plus behavioral zero-residue leg), rewrote commands/auto.md's outcome-marker section into the five FR-14 subsections (writer of record, driver-owned child_abort, continue-vs-terminal split, atomicity, FR-17 parity), and shipped the 11-member phase suite (7 P02 + 4 M045) at 11/11. check-must-haves: 8/8 truths, all artifacts and key links PASS. P04 can now build the unattended envelope on a marker contract that is proven for the common exit-0 case, the error family, and the killed-child case alike.
