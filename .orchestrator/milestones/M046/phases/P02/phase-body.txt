P02 closed Gaps 3 and 4 from the spec's problem statement: the self-continue marker contract is now deterministic across the process boundary, and the driver no longer evaluates interpolated shell strings. T01 landed the single CON-2-authorized additive change to auto-loop.sh (54 insertions, 0 deletions, git-diff-audited additions-only): an env-gated (ORCHESTRATOR_SELF_CONTINUE_MARKER=1, default OFF) deterministic outcome-marker writer — a trap-on-EXIT writer function plus substate capture in the existing _auto_output() funnel — writing $MILESTONE_DIR/.self-continue-outcome atomically (temp + mv -f) with a full exit-code-to-marker vocabulary (exit-0 substates planning/phase_complete/validating; 1/2/3/10/11/12/13 error/budget/stuck/complete/pause/unexpected_state/planning_failed; 14 rotation). FR-17 byte parity proven: gate absent is byte-identical (6/6 assertions) and the M045 legacy golden stays green. T02 hardened self-continue-drive.sh: a strict charset allowlist rejects metacharacter-bearing milestone dirs before any command line (SELF_CONTINUE:REJECT, exit 2); run_child() spawns the child as an argv array (whitespace-split --auto-cmd under set -f, no sh -c evaluation); the deterministic wrapper captures CHILD_RC and implements the FR-14 division of labor — rc>=128 overwrites any stale marker with child_abort, crash-without-marker writes child_abort, real error exits keep auto-loop's authoritative marker, and rc=0-no-marker preserves the M045 STALLED path. All four M045 driver regressions stay green. T03 proved SC-9 non-stubbed: 11/11 exit cases against the REAL auto-loop.sh (each asserting exit code AND marker, budget/rotate driven through the real budget-checker/context-monitor via the documented env config layer) plus 5/5 kill/crash/stall cases through the real driver — no stubs, no seeded markers. T04 added the atomic-write-discipline verifier (static shape leg mutation-tested against three violation shapes, plus behavioral zero-residue leg), rewrote commands/auto.md's outcome-marker section into the five FR-14 subsections (writer of record, driver-owned child_abort, continue-vs-terminal split, atomicity, FR-17 parity), and shipped the 11-member phase suite (7 P02 + 4 M045) at 11/11. check-must-haves: 8/8 truths, all artifacts and key links PASS. P04 can now build the unattended envelope on a marker contract that is proven for the common exit-0 case, the error family, and the killed-child case alike.
