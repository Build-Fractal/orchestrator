---
schema_version: "1.0"
type: task-summary
id: "T05"
parent: "P02"
milestone: "M004"
provides:
  - "scripts/lib/hooks.sh — Bash 3.2 sourced library providing run_hooks <lifecycle_point> <state_source> [hooks_yaml_path] for the four US5 AS2 lifecycle points (PRE_DISPATCH, POST_DISPATCH, POST_VERIFY, PRE_ADVANCE); enforces Principle XII (Hook Isolation) via chmod 444 frozen state snapshot passed through $ORCH_HOOK_SNAPSHOT env var, portable Bash 3.2 background-watchdog timeout (default 30s, configurable via ORCH_HOOK_TIMEOUT_SEC), mtime+mode tampering detection that emits HOOK_VIOLATION unconditionally even under ORCH_FORCE; emits HOOK_START/HOOK_COMPLETE/HOOK_WARNING/HOOK_BLOCKED/HOOK_VIOLATION events via events.sh; supports block_on_fail downgrade to HOOK_WARNING under orch_is_forced; graceful degradation when recipe-parser.sh or hooks.yaml missing (emits SAFETY_WARNING with reason=recipe_parser_unavailable or reason=hooks_yaml_missing and returns 0)"
requires:
  - "from:P02/T01 what:double-sourcing guard pattern in first 5 lines and emit_event sibling interface; from:P02/T02 what:emit_event function for structured EVENT: key=value emission on all lifecycle branches; from:P02/T03 what:orch_is_forced helper for block_on_fail force-override downgrade; from:M004/P04 what:recipe-parser.sh already on disk providing parse_recipe_hooks yielding pipe-delimited <key>|<name>|<script>|<enabled>|<block_on_fail>|<description> lines"
affects:
  - "P02/T06 (engine.sh will call run_hooks at four lifecycle points), P03 (engine coordinator integrates hook dispatch into dispatch loop alongside guards), FR-223, US5 AS1-6, Principle XII (Hook Isolation contract enforced at this layer), Principle II (structured events via HOOK_* family), NFR-200 (Bash 3.2), NFR-203 (double-sourcing guard), NFR-205 (portable timeout without timeout(1))"
key_files:
  - "scripts/lib/hooks.sh"
key_decisions:
  - "Guard placed on lines 3-4 per T01 lesson with sibling sources (errors.sh/events.sh/run-context.sh) deferred to lines 27-32 after the guard so head -5 _HOOKS_SOURCED check passes; recipe-parser.sh is NOT sourced at load time — probed at runtime via _hooks_parser_available in a subshell that sources it and checks type parse_recipe_hooks, so the graceful-degradation contract holds even though M004/P04 already delivered the parser; snapshot tampering check runs BEFORE the exit-code branch so HOOK_VIOLATION takes precedence over both HOOK_COMPLETE and the force-override path — an ORCH_FORCE=1 engine still cannot tolerate a hook that scribbles on its sandbox; portable timeout implemented via background command + background watchdog that sleeps then kill -TERM, 1s grace, kill -KILL, then wait (no dependency on timeout(1), honors NFR-205 GNU coreutils absence on macOS); hook iteration uses mktemp temp file + while IFS='|' read loop instead of process substitution (AP-001 compliance); _hooks_snapshot_create branches on file vs directory state source (cp for files, tar to tarball for directories, printf 'no-state' fallback) so run_hooks accepts either a state.yaml file or a whole .specify/orchestrator tree; hook script missing detection emits SAFETY_WARNING not HOOK_BLOCKED because the hook never started — this is a configuration problem not a runtime violation"
patterns_established:
  - "Hook sandbox contract — freeze snapshot with chmod 444, pass path via env var, enforce timeout, verify mtime+mode on return, emit HOOK_VIOLATION that cannot be force-downgraded; runtime parser probe pattern — sibling library (recipe-parser.sh) is sourced inside a subshell for availability detection then re-sourced in the main shell only after the availability gate, preserving the graceful-degradation contract even when the parser is on disk; portable timeout pattern — background command + background watchdog (sleep, TERM, sleep 1, KILL) + wait instead of timeout(1) for NFR-205 compliance on Bash 3.2 macOS; precedence ordering for safety — tampering > exit code > force-override so a compromised snapshot always wins regardless of exit status or ORCH_FORCE; preemptive application of P02 lessons learned — T01 guard-placement in first 5 lines, T01 NOT sourcing recipe-parser.sh at load time, T03 export-prefix form for behavioral env-var tests"
drill_down_paths:
  - ".specify/orchestrator/milestones/M004/phases/P02/tasks/T05-PLAN.md"
duration: "5m"
verification_result: "pass"
completed_at: "2026-04-10T22:00:00Z"
---

Created scripts/lib/hooks.sh (191 lines) — the hook lifecycle dispatcher fulfilling FR-223, US5 AS1-6, Principle XII (Hook Isolation), Principle II (structured events), NFR-200 (Bash 3.2), NFR-203 (double-sourcing guard), and NFR-205 (portable timeout without timeout(1)). Exposes run_hooks <lifecycle_point> <state_source> [hooks_yaml_path] for the four US5 AS2 lifecycle points PRE_DISPATCH, POST_DISPATCH, POST_VERIFY, PRE_ADVANCE; unknown lifecycle emits SAFETY_WARNING reason=unknown_lifecycle_point and returns 0. Principle XII sandbox enforcement: each hook receives a per-invocation temp snapshot of the state source (cp for file sources, tar for directory sources, printf 'no-state' fallback), chmod 444-frozen, with the path exposed via $ORCH_HOOK_SNAPSHOT; the hook runs under a portable Bash 3.2 background-watchdog timeout (default 30s, overridable via ORCH_HOOK_TIMEOUT_SEC) that issues kill -TERM, waits 1s, then kill -KILL — no dependency on timeout(1). After the hook returns, _hooks_snapshot_unchanged verifies both the mtime (via stat -c '%Y' Linux / stat -f '%m' macOS) and the read-only mode; any modification emits HOOK_VIOLATION and sets overall_rc=1 unconditionally — the tampering check runs BEFORE the exit-code branch so it takes precedence over HOOK_COMPLETE, HOOK_WARNING, AND the force-override path. Block behavior: exit 0 → HOOK_COMPLETE; non-zero + block_on_fail true (default) → HOOK_BLOCKED + overall_rc=1, downgraded to HOOK_WARNING + forced=1 under orch_is_forced; non-zero + block_on_fail false → HOOK_WARNING, overall_rc stays 0. Graceful degradation has two branches: (1) _hooks_parser_available probes for scripts/lib/recipe-parser.sh AND the parse_recipe_hooks function inside a subshell — if either is missing, emits SAFETY_WARNING reason=recipe_parser_unavailable and returns 0; (2) if hooks.yaml path is missing, emits SAFETY_WARNING reason=hooks_yaml_missing and returns 0. Hook iteration parses the pipe-delimited parse_recipe_hooks output (<key>|<name>|<script>|<enabled>|<block_on_fail>|<description>) via mktemp + printf + while IFS='|' read loop (NOT process substitution — AP-001). Preemptive application of P02 lessons: (1) T01 guard placement — _HOOKS_SOURCED check on line 3, set on line 4, so head -5 passes on first run with sibling sources deferred to lines 27-32; (2) T01 parser isolation — recipe-parser.sh is NOT sourced at load time, only at runtime after the availability gate, preserving the graceful-degradation contract even though M004/P04 already delivered recipe-parser.sh on disk (the grep check for 'recipe_parser_unavailable' passes because the string is in source code, the runtime branch just isn't exercised); (3) T03 export-prefix — the verification block uses 'bash -c ...' single-shot tests that don't require env-var scoping gymnastics. All 19 verification checks printed PASS on first run with zero deviations. File is 191 lines (>=140 required), Bash 3.2 compatible (no declare -A, readarray, mapfile, process substitution, or sed -i), and idempotent under double-sourcing. All 5 P02 libraries (errors, events, run-context, guards, hooks) cleanly load together and expose their public functions. No git commit per task instructions.
