---
schema_version: "1.0"
type: task-summary
id: "T02"
parent: "P05"
milestone: "M004"
provides:
  - "scripts/dispatch/build-context.sh refactored (837 lines) as a recipe interpreter — sources scripts/lib/recipe-parser.sh + scripts/dispatch/lib/section-handlers.sh, resolves templates/context-recipe.yaml via resolve_recipe (FR-211 task > phase > milestone > default specificity), parses sections via parse_recipe_sections, dispatches each section to its handler via dispatch_section_handler, and assembles the manifest/frontmatter/title/sections payload with byte-for-byte parity against the pre-refactor output; preserves the existing positional CLI (<orch_root> <milestone> <phase> <task>) so scripts/engine/run.sh invokes it unchanged; adds --recipe <path> override; emits a single RESULT line to stderr via EXIT trap; .specify/orchestrator/milestones/M004/phases/P05/fixtures/golden-payload-M004-P04-T04.md (747-line golden fixture captured from the pre-refactor script for T05 parity enforcement)"
requires:
  - "from:M004/P02/T01 what:scripts/lib/errors.sh emit_result; from:M004/P02/T02 what:scripts/lib/events.sh emit_event; from:M004/P02/T03 what:scripts/lib/run-context.sh init_run_context + orch_now; from:M004/P04/T04 what:scripts/lib/recipe-parser.sh parse_recipe_sections + resolve_recipe; from:M004/P05/T01 what:scripts/dispatch/lib/section-handlers.sh dispatch_section_handler + 8 handlers; from:pre-existing what:scripts/dispatch/scope-filter.sh + scripts/knowledge/traverse-graph.sh + scripts/knowledge/resolve-entries.sh + scripts/knowledge/increment-hits.sh (used by the planning branch verbatim port)"
affects:
  - "M004/P05/T03 (compress-payload.sh refactor reuses recipe-parser.sh patterns established here); M004/P05/T05 (golden fixture + parity harness); scripts/engine/run.sh (unchanged, still invokes build-context.sh with existing positional args); FR-210 (recipe-driven assembly); FR-211 (recipe specificity resolution); Principle X (Templating Over Inference); Principle XIII (Agent Instruction Schema)"
key_files:
  - "scripts/dispatch/build-context.sh,.specify/orchestrator/milestones/M004/phases/P05/fixtures/golden-payload-M004-P04-T04.md"
key_decisions:
  - "Planning-payload branch (IS_PLANNING=true) is a verbatim port into helper function _bc_assemble_planning_payload; recipe interpretation ONLY applies to task-dispatch mode per P05-PLAN scope; display-order/priority shim (_bc_display_order/_bc_display_name/_bc_display_priority) re-orders recipe sections for manifest parity because templates/context-recipe.yaml is locked (order/priority in default recipe do not match pre-refactor dispatch-prompt sequence — P06 candidate); phase_summaries section routed to local _bc_handle_phase_summaries_fixed shim because T01 handle_phase_summaries has a Bash-3.2 read-r bug with unterminated comma-separated dep lists (P06 candidate); RESULT line written to stderr via EXIT trap so payload stdout stays pure for engine redirection; pipefail temporarily disabled around init_run_context to work around a pre-existing SIGPIPE in _orch_run_nonce's /dev/urandom|tr|head -c pipeline (P06 candidate)"
patterns_established:
  - "Recipe-interpreter pattern — source recipe-parser.sh + section-handlers.sh, resolve via FR-211 specificity, parse_recipe_sections into temp file, iterate via while-read, dispatch per section to temp file, then run shared manifest-assembly function; EXIT trap writes RESULT to stderr + rm -rf temp workspace + rm -f ids file; display-metadata shim (_bc_display_*) as explicit P06-removal-candidate abstraction for parity preservation; golden-fixture-first workflow — capture pre-refactor output before editing, normalize volatile columns (line-range, ~tokens, (N entries)) with sed -E, diff -q to prove parity; verbatim isolation of out-of-scope branches into _bc_* helper functions"
drill_down_paths:
  - "scripts/dispatch/build-context.sh,.specify/orchestrator/milestones/M004/phases/P05/tasks/T02-PLAN.md,.specify/orchestrator/milestones/M004/phases/P05/fixtures/golden-payload-M004-P04-T04.md"
duration: "25m"
verification_result: "pass"
completed_at: "2026-04-11T12:55:00Z"
---

Refactored scripts/dispatch/build-context.sh (734 -> 837 lines) into a recipe interpreter that reads templates/context-recipe.yaml via scripts/lib/recipe-parser.sh and dispatches each section through scripts/dispatch/lib/section-handlers.sh (T01 deliverable). Sources all 3 P02 shared libraries (errors.sh, events.sh, run-context.sh) plus recipe-parser.sh plus section-handlers.sh. Installs an EXIT trap that emits exactly one RESULT: line to stderr (so payload stdout stays clean for engine redirection) + cleans up temp workspace + ids file. Preserves the existing positional CLI (<orch_root> <milestone> <phase> <task>) so scripts/engine/run.sh keeps working unchanged; adds --recipe <path> override. Byte-for-byte parity against the pre-refactor output verified via a golden fixture at .specify/orchestrator/milestones/M004/phases/P05/fixtures/golden-payload-M004-P04-T04.md captured BEFORE any edits, normalized with sed -E (line-range + ~tokens + (N entries) columns stripped), diff -q clean. All 16 T02 task-plan checks pass, all 12 payload must-haves pass, and the PHASE_PLAN planning branch byte-matches pre-refactor output (271 lines, identical manifest). Three pre-existing defects were discovered and worked around rather than fixed in-place because they live in files locked by P05-PLAN constraint 8: (1) templates/context-recipe.yaml default recipe declares the wrong order/priority values for dispatch-prompt parity (knowledge=10, decisions=20, constraints=30, scope=40, upstream=50, state=60, task_plan=60 does not match pre-refactor Knowledge|Decisions|Scope|Upstream|TaskPlan|State|Constraints sequence; constraints is optional but pre-refactor shows required; upstream is compressible but pre-refactor shows required) — worked around with a canonical display-order/name/priority shim in build-context.sh; (2) T01 section-handlers.sh handle_phase_summaries has a Bash-3.2 while-read bug where an unterminated dep list (printf '%s' P01 | tr ',' '\n' with no trailing newline) never enters the loop body — worked around by routing source=phase_summaries to a local _bc_handle_phase_summaries_fixed shim; (3) scripts/lib/run-context.sh _orch_run_nonce uses 'tr -dc a-z0-9 < /dev/urandom | head -c 8' which triggers SIGPIPE and exits 141 under 'set -o pipefail' — worked around by disabling pipefail around init_run_context (this is a pre-existing defect; scripts/engine/run.sh --dry-run also exits 141 with the pre-refactor build-context.sh, verified via stash). All three are flagged as P06 candidates in the commit message. Planning-payload branch (IS_PLANNING=true, task=PHASE_PLAN) isolated verbatim into _bc_assemble_planning_payload helper plus three pre-refactor knowledge/decisions/upstream gather helpers; logic unchanged. Shared manifest-assembly logic (frontmatter -> title -> ## Manifest -> table rows -> concatenated sections -> hit-count loop -> context-budget stderr line) hoisted into _bc_assemble_manifest_and_emit so both branches share the same final-payload path. No inline date calls, no jq, Bash 3.2 compatible (no declare -A/readarray/mapfile), AP-001 compliant (while-read uses temp files, never process substitution). Did NOT touch scripts/dispatch/lib/section-handlers.sh (T01), scripts/dispatch/compress-payload.sh (T03), scripts/dispatch/select-model.sh (T04), scripts/engine/run.sh, scripts/verify/check-must-haves.sh, scripts/lib/events.sh, scripts/lifecycle/record-result.sh, or templates/context-recipe.yaml.
