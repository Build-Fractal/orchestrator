# scripts/verify/.npmignore — M035 P02 T05 bundle-hygiene contract.
# Excludes dogfood-only milestone-internal verifiers from the npm
# tarball. Mirrors packaging/bundle/build-bundle.sh
# apply_bundle_hygiene_filter rule 1.
m[0-9]*-p[0-9]*-*
m[0-9][0-9][0-9]/
m[0-9][0-9][0-9]-*/
