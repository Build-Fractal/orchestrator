# templates/conversus-presets/.npmignore — M035 P02 T05 bundle-hygiene.
# Excludes milestone-internal preset experiments from the npm tarball.
# Mirrors packaging/bundle/build-bundle.sh apply_bundle_hygiene_filter
# rule 1.
m[0-9]*-p[0-9]*-*
m[0-9][0-9][0-9]-*
