---
schema_version: "1.0"
type: corpus-exhaustion
verdict: "PASS"            # PASS | BLOCK — written by corpus-exhaustion-sweep.sh
checkpoint: "<checkpoint>" # sme-packet | discuss | comments | specify | plan-phase | roadmap | materials-intake
generated_at: "<iso8601>"  # caller-supplied (CON-7 — the sweep never calls date)
manifest: "<path>"
questions_total: 0
questions_surviving: 0
clean: 0
hits_pending: 0
dropped: 0
kept: 0
caveat: 0
# unreachable_required_stores: "<labels>"   # present only when a required store could not be searched
---

# Corpus-Exhaustion Artifact — <checkpoint>

<!--
This file is GENERATED by scripts/knowledge/corpus-exhaustion-sweep.sh. It is
the gate's evidence + audit trail and doubles as packet provenance for the
human ("here is why each surviving question is genuinely open").

Workflow:
  1. The agent writes candidate questions to a questions file (one per line,
     optional [id] prefix).
  2. Run the sweep. Questions with corpus hits are marked HITS (pending) and
     the gate BLOCKs.
  3. The agent reads the cited locations and annotates each HITS question on
     its line in the questions file:
         @disposition=dropped reason: <answer + citation>   (already answered)
         @disposition=kept     reason: <why still open>      (genuinely open)
  4. Re-run the sweep. With no pending HITS the gate PASSes; the surviving
     (non-dropped) questions are the human packet.

Verdict legend (per question):
  CLEAN                    zero hits, all required stores searched — safe to ask
  HITS                     >=1 hit, not dispositioned — BLOCKS (read first)
  DROPPED                  >=1 hit, @disposition=dropped — answered, removed from packet
  KEPT                     >=1 hit, @disposition=kept — genuinely open, stays in packet
  IRREDUCIBLE-WITH-CAVEAT  zero hits but a required store was unreachable — surfaced
-->

## Per-question evidence

### Q1 — CLEAN

- **question**: [Q1] <question text>
- **disposition**: pending
- **terms** (N): term-a, term-b, "quoted phrase", DR-012
- **hits**: (none)
