# py-cli-fixture

Synthetic byte-deterministic Python CLI fixture for the M033 P03
ingest-codebase verifier (FR-7). Not a real CLI.

It exercises the top-level-doc, package-manifest (pyproject.toml),
directory-structure (src/cli/), and test-directory signal sources.
