# rust-library-fixture

Synthetic byte-deterministic Rust library fixture for the M033 P03
ingest-codebase verifier (FR-7). Not a real crate.

It exercises the top-level-doc, package-manifest (Cargo.toml),
directory-structure (src/), and test-directory signal sources.
