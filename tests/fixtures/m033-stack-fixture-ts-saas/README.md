# ts-saas-fixture

Synthetic byte-deterministic TypeScript SaaS fixture for the M033 P03
ingest-codebase verifier (FR-7). Not a real application.

It exercises the top-level-doc, package-manifest, directory-structure,
test-directory, and prior-tooling signal sources documented in the
ingest-codebase.sh fenced SSOT block.
