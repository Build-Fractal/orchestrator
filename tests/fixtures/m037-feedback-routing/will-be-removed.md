# Will Be Removed

Synthetic test fixture exercising idempotent removal — the test
creates this file, runs the generator, removes the source, re-runs
the generator, and asserts the corresponding stub disappears.
