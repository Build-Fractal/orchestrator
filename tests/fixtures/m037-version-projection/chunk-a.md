---
schema_version: "1.0"
type: reference-chunk
chunk_id: "REF-CHUNK-A"
version: "Human Label A"
topic_tags:
  - test
  - m037
---

# REF-CHUNK-A

This is fixture chunk A. Source `version:` carries a plain human label;
expected projected stub `title:` is exactly `"Human Label A"`.

The body content is non-empty so the stub generator does not route
through the body-empty `metadata-only` arm.
