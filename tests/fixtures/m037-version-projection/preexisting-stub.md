---
title: "Operator Custom Title"
auto_generated: false
---

<!-- Pre-existing operator-edited stub for MIT-02 P0 escape-hatch test.
     Staged into <docs>/<extra-dir>/chunk-d-escape-hatch.md BEFORE the
     generator runs. The generator MUST preserve this file byte-identical
     across re-runs because auto_generated is set to false. -->

This is the operator-authored body content. It must survive across two
consecutive `wiki-generate-stubs.sh` invocations without mutation.
