# Synthetic Plan

Test fixture spec markdown — exercises the pipe-table code-row scanner for
prefixes `AN`, `BG`, `OD`.

## 1. Requirements

| Code     | Title                                            | Notes |
|----------|--------------------------------------------------|-------|
| AN-001   | Decorate codes with hover-tooltip titles         | core  |
| AN-002   | Decorate file-path references                    | core  |
| BG-001   | Synthetic build gate placeholder                 | gate  |
| OD-001   | Synthetic open decision placeholder              | open  |

## 2. Sections

### 2.1 Validation

This sentence references the validation section above as `§2.1` so the
§-resolution pass can resolve it against the nearby
`.orchestrator/spec/synthetic-plan.md` path mention.

### 2.2 Anchoring

Provides a heading the §-pass can target.
