# M035 P05 T05 fixture placeholder. Real signature artifacts are
# produced by .github/workflows/release.yml (T03) at release time
# via cosign sign-blob against the GitHub Actions OIDC issuer.
# This placeholder lets the shape-mode verifier pass without
# requiring real cosign output. Live cosign verification is gated
# behind COSIGN_AVAILABLE=1 + M035_P05_LIVE_RELEASE_DIR env vars.
