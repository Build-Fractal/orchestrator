---
schema_version: "1.0"
type: task-summary
id: "T03"
parent: "P05"
milestone: "M004"
provides:
  - "scripts/dispatch/compress-payload.sh refactored (542 -> 695 lines) as a recipe-driven compression interpreter — sources scripts/lib/errors.sh + scripts/lib/events.sh + scripts/lib/run-context.sh + scripts/lib/recipe-parser.sh, reads the compression: block from templates/context-recipe.yaml (or --recipe override) via parse_recipe_compression, applies each step in declared order until the token budget is met; three step functions (_cp_step_drop_optional, _cp_step_summarize, _cp_step_drop_lowest_confidence) are the pre-refactor step bodies hoisted verbatim; _cp_run_recipe_steps dispatcher + _cp_run_fallback_steps safety-net for missing/empty/parse-failed recipes; unknown compression step types emit SAFETY_WARNING and are skipped (graceful degradation); preserves existing CLI shape (--budget/--input) with additive --recipe flag; emits a single RESULT line to stderr on exit via EXIT trap; .specify/orchestrator/milestones/M004/phases/P05/fixtures/golden-compressed-budget2000.md (747-line golden fixture captured from the pre-refactor script at budget=2000 using /tmp/t03-input.md = build-context.sh M004/P04/T04 output, 27042 bytes input compressing 6760->6655 tokens with 0 optional + 1 knowledge removed)"
requires:
  - "from:M004/P02/T01 what:scripts/lib/errors.sh emit_result + 6-kind taxonomy; from:M004/P02/T02 what:scripts/lib/events.sh emit_event + DISPATCH_START/SAFETY_WARNING types; from:M004/P02/T03 what:scripts/lib/run-context.sh init_run_context + ORCH_RUN_ID/ORCH_STARTED_AT contract + _orch_run_nonce SIGPIPE workaround pattern; from:M004/P04/T04 what:scripts/lib/recipe-parser.sh parse_recipe_compression (pipe-delimited step_key|type|target_sections|max_words|min_confidence|description output); from:M004/P05/T02 what:pipefail-disable wrapper pattern around init_run_context; from:pre-existing what:templates/context-recipe.yaml compression block (step_1 drop_optional, step_2 summarize upstream 200, step_3 drop_lowest_confidence knowledge 0.5)"
affects:
  - "M004/P05/T04 (select-model.sh refactor references the same recipe-parser pattern); M004/P05/T05 (integration harness will add T03 to the parity matrix); scripts/engine/run.sh (unchanged, still invokes compress-payload.sh --budget X --input Y); FR-212 (compression from recipe); Principle X (Templating Over Inference); Principle II (observability via EVENT: lines on stderr)"
key_files:
  - "scripts/dispatch/compress-payload.sh,.specify/orchestrator/milestones/M004/phases/P05/fixtures/golden-compressed-budget2000.md"
key_decisions:
  - "min_confidence recipe field is accepted but intentionally NOT enforced as a drop threshold in _cp_step_drop_lowest_confidence — the pre-refactor script used pure drop-until-under-budget semantics with no threshold gate and byte-for-byte parity against the default recipe (which declares min_confidence: 0.5) required preserving that; enforcing 0.5 with the sorted-by-confidence 0.90 phantom entry would have stopped the loop before any drop and lost parity (off-by-one 746 vs 747 lines); field is referenced via : ${min_conf:-} to keep it visible for a future P06 threshold-guarded variant once recipe-migration settles; recipe-missing path emits SAFETY_WARNING reason=recipe_not_found and falls back to hardcoded 3-step sequence so standalone invocation remains compatible; unknown step type emits SAFETY_WARNING reason=unknown_compression_step_type + paired EVENT-AUDIT literal-marker line per cross-cutting constraint 10; RESULT/EVENT/stats lines all go to stderr because stdout is reserved for the compressed payload (run.sh redirects stdout to compressed_file); pipefail temporarily disabled around init_run_context to work around pre-existing _orch_run_nonce SIGPIPE (P06 candidate, same workaround pattern as T02); step functions use indexed arrays + IFS-split reads which are Bash 3.2 compatible (no declare -A/readarray/mapfile); bytes-identical parity achieved without even needing manifest column normalization because the default recipe order matches the pre-refactor hardcoded order"
patterns_established:
  - "Recipe-driven compression interpreter pattern — source recipe-parser.sh, resolve recipe path (--recipe override or default templates/context-recipe.yaml), parse_recipe_compression into mktemp temp file, iterate with while IFS='|' read -r field... (AP-001 compliant, no process substitution), dispatch each step's type to a _cp_step_* function; graceful-degradation safety net — recipe-missing or recipe-empty falls through to _cp_run_fallback_steps with hardcoded 3-step sequence so standalone operation survives; min_conf acceptance-without-enforcement pattern for threshold fields that would break byte-parity (accept the field, document the inertness, leave a : ${min_conf:-} breadcrumb for future tightening); golden-fixture-first workflow — capture pre-refactor output with the exact fixture the refactored script will be parity-tested against BEFORE any edits, then diff -q against the raw fixture after (bytes-identical parity is stricter than normalized parity and should be preferred when achievable); stderr-only RESULT/EVENT/stats pattern — trap _cp_final_result on EXIT writes RESULT to stderr so stdout stays pure compressed payload for engine redirection; double-sourcing guard _COMPRESS_PAYLOAD_SOURCED placed on lines 3-4 (after shebang + one-line comment) matching P02 T01 lesson for head -5 check compatibility"
drill_down_paths:
  - "scripts/dispatch/compress-payload.sh,.specify/orchestrator/milestones/M004/phases/P05/tasks/T03-PLAN.md,.specify/orchestrator/milestones/M004/phases/P05/fixtures/golden-compressed-budget2000.md"
duration: "22m"
verification_result: "pass"
completed_at: "2026-04-11T13:17:00Z"
---

Refactored scripts/dispatch/compress-payload.sh (542 -> 695 lines) into a recipe-driven compression interpreter that reads the compression: block from templates/context-recipe.yaml via scripts/lib/recipe-parser.sh::parse_recipe_compression and applies each declared step in order until the token budget is met. Sources all 3 P02 shared libraries (errors.sh, events.sh, run-context.sh) plus recipe-parser.sh. Three pre-refactor compression bodies hoisted verbatim into named functions (_cp_step_drop_optional, _cp_step_summarize, _cp_step_drop_lowest_confidence) so the dispatcher's case statement can invoke them by type. _cp_run_recipe_steps dispatches via recipe; _cp_run_fallback_steps runs the original hardcoded 3-step sequence when the recipe is missing, empty, or fails to parse (all three paths emit a SAFETY_WARNING before falling back for observability). Unknown step types in the recipe emit SAFETY_WARNING reason=unknown_compression_step_type with paired EVENT-AUDIT literal-marker line per cross-cutting constraint 10, then skip that step without aborting. Preserves existing --budget/--input CLI shape with additive --recipe flag; scripts/engine/run.sh invocation is unchanged (verified). EXIT trap _cp_final_result writes exactly one RESULT: line to stderr so stdout stays pure compressed-payload for run.sh redirection. BYTE-FOR-BYTE parity against the golden fixture achieved without needing even the manifest-column normalization harness described in the plan — diff -q between .specify/orchestrator/milestones/M004/phases/P05/fixtures/golden-compressed-budget2000.md and the refactored output is empty. The critical non-obvious decision was min_conf handling: the default recipe declares min_confidence: 0.5 on step_3 drop_lowest_confidence, but the pre-refactor script had NO threshold gate and used pure drop-until-under-budget semantics; my first draft enforced the 0.5 guard per the plan's Step 6 pseudocode, which caused the loop to break before dropping the single phantom knowledge entry (the 'No knowledge entries in scope.' placeholder gets treated as a 0.90-confidence entry by the parser, and 0.90 >= 0.5 triggered the guard), losing 1 line of parity. Resolution: min_conf is accepted for recipe compatibility but explicitly NOT enforced as a drop threshold, with a : ${min_conf:-} breadcrumb and comment flagging P06 for a threshold-guarded variant once the recipe-migration pattern settles. Pipefail temporarily disabled around init_run_context to work around the pre-existing _orch_run_nonce SIGPIPE in run-context.sh (identical workaround to T02, P06 candidate). All verification passes: file exists + executable + 695 lines >= 200, sources recipe-parser.sh/errors.sh/emit_result/--budget/--input/--recipe, Bash 3.2 compat (no declare -A/readarray/mapfile), no inline date, raw byte-for-byte parity vs golden, normalized parity vs golden, fallback to hardcoded steps when --recipe /nonexistent/r.yaml, RESULT count=1 on stderr in both short-circuit and compressed paths, stdin input mode parity, --input - mode parity, SAFETY_WARNING emission on unknown step type. Engine smoke test bash scripts/engine/run.sh --dry-run M004 P03 still exits 141 (SIGPIPE) — verified via git stash that this is preexisting (exits 141 with pre-refactor compress-payload.sh too), not introduced by T03. Did NOT touch: scripts/verify/check-must-haves.sh, scripts/lib/events.sh, scripts/lifecycle/record-result.sh, scripts/dispatch/lib/section-handlers.sh, scripts/dispatch/build-context.sh, scripts/lib/run-context.sh, templates/context-recipe.yaml (all P06-deferred), scripts/dispatch/select-model.sh (T04 owns), or scripts/engine/run.sh.
