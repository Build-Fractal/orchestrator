---
schema_version: "1.0"
type: task-summary
id: "T01"
parent: "P07"
milestone: "M011"
provides:
  - "scripts/knowledge/detect-spec-shape.sh (agent-free heading-probe classifier emitting shape=speckit|foreign plus reasons=<csv>); scripts/knowledge/normalize-spec.sh (thin wrapper dispatching through scripts/dispatch/dispatch-interface.sh, NORMALIZER_STUB=1 escape hatch, atomic temp->rename, source_hash-based idempotency, --force bypass); templates/spec-normalizer-prompt.md (schema_version 1.0 + type normalizer-prompt frontmatter, {{source_markdown}}/{{slug}} placeholders, full spec-kit section-layout instructions, no-new-requirements directive); four per-artifact verify scripts: m011-p07-shape-detect.sh, m011-p07-normalize-wrapper-shape.sh, m011-p07-normalizer-template.sh, m011-p07-normalize-idempotent.sh"
requires:
  - "scripts/dispatch/dispatch-interface.sh (MEM018 backend-agnostic entry point); templates/ directory convention (MEM013); bash 3.2 compat rules (MEM001); BSD grep -Fq -- token idiom for --flag safety (MEM012, P06 extension); structured PASS/FAIL output (MEM002)"
affects:
  - "T02 (intensity gate + preserve-references tests run alongside T01 artifacts), T03 (conversus tool adapter + gate command doc consume the same dispatch-routing idiom established here), T04 (regression e2e will drive normalize-spec.sh via NORMALIZER_STUB=1 deterministic path), downstream ingest-spec.sh (unchanged; normalize-spec.sh feeds specs/<slug>/spec.md into its existing pipeline)"
key_files:
  - "scripts/knowledge/detect-spec-shape.sh, scripts/knowledge/normalize-spec.sh, templates/spec-normalizer-prompt.md, scripts/verify/m011-p07-shape-detect.sh, scripts/verify/m011-p07-normalize-wrapper-shape.sh, scripts/verify/m011-p07-normalizer-template.sh, scripts/verify/m011-p07-normalize-idempotent.sh"
key_decisions:
  - "Shape classifier uses grep -Eiq heading-pattern probes with >=3 probe-family threshold (captures user_stories, functional_requirements, acceptance, non_goals/constraints/success, id_tags (FR|US|AC|NFR-NNN), gherkin Given/When/Then, As-a-I-want); normalize-spec.sh routes through dispatch-interface.sh --task-plan/--payload file shape (existing MEM018 contract); source_hash computed via shasum -a 256 with sha256sum/md5/md5sum fallbacks and stored as HTML-comment marker prepended to output (readable without YAML parser); atomic write via <output-dir>/<slug>/.spec.normalized.$$.tmp then mv; NORMALIZER_STUB=1 env var short-circuits dispatch and copies tests/fixtures/normalized-stub.md for deterministic T04 CI; awk-based placeholder substitution avoids sed metacharacter pitfalls in {{source_markdown}} bodies"
patterns_established:
  - "Agent-free probe-first classifier: grep -Eiq heading-pattern probes with matched-family counter and threshold as preferred shape-detection idiom (sub-100ms, no LLM dependency); thin-wrapper + dispatch-interface.sh as the no-hardcoded-LLM pattern for any future agent-requiring script; NORMALIZER_STUB=1 environment escape hatch as the standard CI-determinism pattern for agent-dependent scripts; source_hash HTML-comment marker (<!-- source_hash: <hex> -->) as readable idempotency tag for agent-written artifacts; parallel indexed arrays probe_name_N / probe_re_N as the bash 3.2 replacement for declare -A probe maps"
drill_down_paths:
  - ".orchestrator/milestones/M011/phases/P07/tasks/T01-PAYLOAD.md, .orchestrator/milestones/M011/phases/P07/tasks/T01-PLAN.md"
duration: "45m"
verification_result: "pass"
completed_at: "2026-04-17T11:38:28Z"
---

T01 delivered the Format-Agnostic Intake foundation for M011/P07: an agent-free shape classifier, a thin wrapper that routes through the existing dispatch-interface.sh (no hardcoded LLM calls per MEM018), the normalizer prompt template, and four per-artifact verify scripts. All seven artifacts are pure-additive; no P02/P03 code was modified. detect-spec-shape.sh uses seven heading-pattern probe families (user_stories, functional_requirements, acceptance, non_goals/constraints/success, id_tags FR|US|AC|NFR-NNN, gherkin Given/When/Then, As-a-I-want-) with a >=3-family threshold for shape=speckit. normalize-spec.sh implements the full idempotency contract (source_hash compare via HTML-comment marker, --force bypass, atomic temp-then-rename at <output-dir>/<slug>/.spec.normalized.$$.tmp -> specs/<slug>/spec.md) plus the NORMALIZER_STUB=1 escape hatch required for T04 deterministic CI. templates/spec-normalizer-prompt.md carries schema_version 1.0 + type normalizer-prompt frontmatter, {{source_markdown}} and {{slug}} placeholders, full spec-kit section-layout instructions (# Feature Specification, ## Problem Statement, ## User Scenarios & Testing, ## Functional Requirements, ## Acceptance Scenarios, ## Constraints, ## Non-Goals, ## Success Criteria), and an explicit 'Do not introduce new requirements' directive. Verification: 4/4 verify scripts PASS. m011-p07-shape-detect.sh asserts correct classification on both speckit and foreign fixtures. m011-p07-normalize-wrapper-shape.sh asserts the wrapper has all required tokens (--spec-path, --slug, --force, dispatch-interface.sh, NORMALIZED:, SKIPPED:, NORMALIZER_STUB) and no curl/wget. m011-p07-normalizer-template.sh asserts frontmatter, placeholders, layout headings, and non-introduction directive. m011-p07-normalize-idempotent.sh drives the NORMALIZER_STUB=1 path end-to-end in a sandbox: run1 emits NORMALIZED:, run2 emits SKIPPED: (source unchanged), run3 with --force emits NORMALIZED: again. Patterns established: probe-first agent-free classifier; NORMALIZER_STUB=1 CI-determinism escape hatch; source_hash HTML-comment marker for agent-written artifact idempotency; parallel indexed probe_name_N/probe_re_N arrays as bash 3.2 replacement for declare -A; awk-based placeholder substitution to avoid sed metacharacter pitfalls on multi-line {{source_markdown}} bodies.
