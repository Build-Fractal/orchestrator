---
schema_version: "1.0"
type: phase-summary
id: "P02"
parent: "M013"
milestone: "M013"
provides:
  - "scripts/integrations/github-common.sh (12 public functions: orchestrator_id_for, emit_marker, find_marker_in_body, shasum_marker_byte_identity, sidecar_path, sidecar_get_field, sidecar_set_top_field, sidecar_upsert_item, sidecar_item_exists, gh_auth_preflight, gh_subissue_rest_preflight, gh_label_collision_preflight); tests/fixtures/m013-p02/ tree with M013-ROADMAP + P02/P03 phase plans + 2 task stubs + expected-manifest.txt + 6 gh-stub-responses files; scripts/verify/m013-p02-github-common.sh gate (13 assertions + final summary), scripts/integrations/github-init.sh (538-line US-1 create-path workhorse with flag parsing, SC-7 auto-mode pending-sentinel path, lazy state walker with AS-4a skip, dry-run manifest emit byte-identical with fixture, live create fan-out with FR-4 marker invariant + FR-5 GraphQL whitelist + sidecar upsert); scripts/integrations/github-common.sh (three preflights populated: gh_auth_preflight with scope enumeration, gh_subissue_rest_preflight with native/labeled-fallback probe, gh_label_collision_preflight with adopt-vs-strict paths; fixture-driven stub paths via M013_GH_STUB_DIR); scripts/verify/m013-p02-github-init-fixture.sh (5 assertions, byte-identical diff gate); scripts/verify/m013-p02-github-init-preflight.sh (7 assertions covering green + missing-scope + native + labeled-fallback + no-collision + strict-refuse + non-strict-adopt paths); scripts/verify/m013-p02-auto-mode-pending.sh (4 assertions including zero-gh-calls enforcement via PATH-shimmed fake gh), FR-15 --dry-run manifest format pinned in scripts/integrations/github-common.sh helpers (manifest_header/manifest_upsert_line/manifest_footer); github-init.sh refactored to emit pinned MANIFEST/UPSERT/footer contract on both dry-run and live; expected-manifest.txt + expected-manifest-noop.txt fixture SSOTs; scripts/verify/m013-p02-dry-run-manifest.sh gate with 5 PASS assertions, commands/github-init.md subcommand definition (MEM012 structure); scripts/verify/m013-p02-github-init-command.sh 6-assertion gate, references/github-integration.md P02 extensions (Auth Modes subsection with 4-mode scopes table + rate-limit budget; Sub-Issue Representation Modes subsection covering native vs labeled-fallback with sidecar-recorded choice; Partial Mapping Table with 6 populated rows + 3 deferred-to-P03 rows + FR-14 Label Collision subsection; 12-step init Workflow walk-through; Dry-Run Manifest Format FR-15 pinned-shape subsection; Scope Boundary table P02 column populated; Referenced Artifacts extended with P02 additions block); scripts/verify/m013-p02-reference-extensions.sh (7-PASS gate with awk-section-extract + shasum byte-identity check against 8 embedded P01 section hashes), sub_issue_mode field extension to templates/github-integration-sidecar.json schema; github-status.sh required-fields + pending-sentinel list updates + SUB_ISSUE_MODE configured-output line (Edit C), scripts/verify/m013-p02-bash32-compat.sh; scripts/verify/m013-p02-phase-suite.sh; extended m013-p02-github-common.sh (manifest_* helpers 14-16); extended m013-p02-github-init-fixture.sh (bash -n + line-count + --help + unknown-flag + manifest-shape)"
requires:
  - "M013/P01/T01 sidecar-init-pending.sh; M013/P01/T06 references/github-integration.md P01 skeleton (FR-4 marker format SSOT); M016/M021 anti-pattern-lint.sh invariant, from:M013/P02/T01 github-common.sh preflight echo-stubs + sidecar helpers + marker primitives; from:M013/P01/T01 sidecar-init-pending.sh; from:M013/P01 templates/github-integration-sidecar.json; MEM001 bash 3.2 compat; M016/M021 anti-pattern-lint invariant, from:T01 what:orchestrator-state+gh-stub fixture tree; from:T02 what:github-init.sh create-path walker, from:T02 what:scripts/integrations/github-init.sh flag surface; from:T01 what:scripts/integrations/github-common.sh helper API; from:P01/T02 what:commands/github-status.md sibling structure precedent, from:P01/T06 what:references/github-integration.md skeleton with 3 TODO P02 reserved stubs + Scope Boundary table + P01-authored sections; from:P02/T01 what:github-common.sh helpers referenced by init Workflow step 7 and Dry-Run Manifest Format; from:P02/T02-T03 what:github-init.sh flags and preflight helpers referenced in init Workflow walk-through, from:M013/P01/T01 what:templates/github-integration-sidecar.json baseline schema; from:M013/P01/T02 what:scripts/integrations/github-status.sh + commands/github-status.md baseline; from:M013/P01/T01 what:scripts/integrations/sidecar-init-pending.sh _schema_docs AWK stripper, from:T01..T06 what:github-common.sh + github-init.sh + commands/github-init.md + references/github-integration.md + templates/github-integration-sidecar.json + tests/fixtures/m013-p02/; scripts/verify/anti-pattern-lint.sh; scripts/verify/m013-p01-bash32-compat.sh (pattern precedent)"
affects:
  - "M013/P02/T02 (sources github-common.sh + fills the three gh_*_preflight stubs with real gh calls); M013/P02/T03 (consumes tests/fixtures/m013-p02/ expected-manifest.txt + gh-stub-responses/); M013/P02/T04-T07 (consume same fixture tree); M013/P03 (re-adoption branch sources github-common.sh); M013/P04 github-sync.sh (sources github-common.sh), M013/P02/T03 (consumes dry-run manifest format for T03 dry-run gate); M013/P02/T04 (github-init.md command wraps this script); M013/P02/T07 (phase-suite consumes these 3 gates); M013/P03 (re-adoption branch sources the same github-common.sh preflights); M013/P04 (github-sync.sh will add FR-5 shape 3 updateProjectV2ItemFieldValue alongside these two), P03 (re-init dry-run consumes same format); P04 (sync --dry-run consumes same format); T07 (phase suite diff-gates against fixture SSOTs), P02/T07 (phase-suite gate ordering); P03 (sync command will mirror this doc shape); M013 operator-facing UX, P03 (will fill 3 bold rows in Partial Mapping Table + 3 TODO P03 stubs + FR-14 full re-adoption + FR-5 GraphQL three-shape lint); P04 (consumes Dry-Run Manifest Format verbatim for sync --dry-run; references Sub-Issue Representation Modes sidecar field); T07 phase-suite (invokes m013-p02-reference-extensions.sh), M013/P02/T02 consumes sub_issue_mode writer via github-init.sh; M013/P02/T03 preflight emits SUBISSUE_MODE matching the sidecar field; M013/P02/T05 references/github-integration.md documents the field, P03 (consumes 8-gate phase-suite for milestone-close composition); P04 (m013-milestone-suite aggregator will invoke p02-phase-suite verbatim)"
key_files:
  - "scripts/integrations/github-common.sh; scripts/verify/m013-p02-github-common.sh; tests/fixtures/m013-p02/orchestrator-state/.orchestrator/milestones/M013/M013-ROADMAP.md; tests/fixtures/m013-p02/orchestrator-state/.orchestrator/milestones/M013/phases/P02/P02-PLAN.md; tests/fixtures/m013-p02/orchestrator-state/.orchestrator/milestones/M013/phases/P02/tasks/T01-PLAN.md; tests/fixtures/m013-p02/orchestrator-state/.orchestrator/milestones/M013/phases/P02/tasks/T02-PLAN.md; tests/fixtures/m013-p02/orchestrator-state/.orchestrator/milestones/M013/phases/P03/P03-PLAN.md; tests/fixtures/m013-p02/expected-manifest.txt; tests/fixtures/m013-p02/gh-stub-responses/, scripts/integrations/github-init.sh; scripts/integrations/github-common.sh; scripts/verify/m013-p02-github-init-fixture.sh; scripts/verify/m013-p02-github-init-preflight.sh; scripts/verify/m013-p02-auto-mode-pending.sh, scripts/integrations/github-common.sh; scripts/integrations/github-init.sh; scripts/verify/m013-p02-dry-run-manifest.sh; scripts/verify/m013-p02-github-init-fixture.sh; tests/fixtures/m013-p02/expected-manifest.txt; tests/fixtures/m013-p02/expected-manifest-noop.txt, commands/github-init.md; scripts/verify/m013-p02-github-init-command.sh, references/github-integration.md; scripts/verify/m013-p02-reference-extensions.sh, templates/github-integration-sidecar.json,scripts/integrations/github-status.sh,commands/github-status.md,scripts/verify/m013-p01-github-status.sh, scripts/verify/m013-p02-bash32-compat.sh; scripts/verify/m013-p02-phase-suite.sh; scripts/verify/m013-p02-github-common.sh; scripts/verify/m013-p02-github-init-fixture.sh"
key_decisions:
  - "none — T01 ships pre-declared contract from T01-PAYLOAD verbatim; AWK-based items.<oid> upsert chosen over jq (no hard-dep per MEM001); depth-aware brace walker inside items block isolates per-oid entries without jq, dry-run manifest records emitted on stdout with summary moved to stderr to preserve byte-identical diff against fixture; fixture gate strips comment-prefix lines before compare; auto-mode short-circuits BEFORE any gh call (PATH-shim in gate 3 proves zero-call invariant); preflights use fixture-driven stub selector env vars (M013_GH_STUB_AUTH, M013_GH_STUB_SUBISSUE, M013_GH_STUB_LABELS) so gate 2 exercises every branch deterministically, footer-on-live: footer line promoted to pinned-format stdout on both dry-run and live (reconciling T02 which emitted footer to stderr); SCRIPT_DIR-vs-PROJECT_ROOT: source helpers from SCRIPT_DIR not PROJECT_ROOT so --root can point at fixture trees without scripts/, MEM012-strict (preserve exact section order + headers); SC-7 auto-mode safety documented under Prerequisites (not Idempotency) for discoverability; description field begins 'Use when' per commands/*.md convention, D014 (Knowledge-Layer Boundary cross-linked not redefined); D015 (P02 scope split — chunk/AC/verification-status deferred to P03 load-bearing via _deferred to P03_ cells), Edit C taken — SUB_ISSUE_MODE configured-output line added (consistent with P01 one-field-one-line contract); P01 gate m013-p01-github-status.sh updated additively (sed-replace for sub_issue_mode during configured-branch setup) per plan Step 6 arbitration path (a), mirror P01 bash32-compat self-exclusion case shape (no regex paraphrase needed); inline existing gates with missing assertions rather than re-authoring; 8-gate SUMMARY line convention pass=N fail=M then final self-named PASS"
patterns_established:
  - "AWK-based JSON object upsert with depth-aware brace walking (items.<oid> insert/replace without jq); pure-library pattern with set -u + direct-execution usage hint (BASH_SOURCE[0] == 0); echo-stub preflight pattern (gh_*_preflight return 0 at T01, T02 fills with real gh calls), fixture-driven preflight with env-var stub selectors (M013_GH_STUB_AUTH/SUBISSUE/LABELS); PATH-shimmed fake-gh enforcing zero-subprocess invariant (auto-mode gate logs every call + asserts log empty); stdout-record + stderr-summary split so fixture diffs stay byte-identical while operator still sees summary; state walker reads frontmatter state field directly via awk bracket-parse without depending on derive-phase.sh which expects live .orchestrator layout; live path guards every gh failure with or-true + errors counter to surface all issues in one run, buffered-manifest-body pattern — mktemp tempfile collects UPSERT lines during walker iteration, counts are finalized before header emission; sidecar-state-driven reason resolution — dry-run inspects sidecar repo_slug/project_v2_id + items.<oid> to emit create vs skip-existing-marker without any gh calls; FORMAT STABILITY CONTRACT doc-block at helper definition site pins shape for downstream P03/P04 consumers, 6-assertion gate shape — 1 content/length + 1 structural + 1 Referenced-Scripts-resolve + 1 Referenced-Templates-resolve + 1 description-prefix + 1 section-scoped-SC-7; awk-scoped section localisation for 'token must appear inside Prerequisites'; tri-token SC-7 contract check (SC-7 + pending-operator-complete + --i-am-operator), section-content byte-identity gate: awk-extract by literal-prefix heading match up to next ^## anchor + shasum-compare against hashes embedded in the gate script — robust against line-number shifts from insertions elsewhere in the doc; ### TODO P0N: heading-form distinguished from 'TODO P0N' prose references (gate greps ^### TODO P02: not literal 'TODO P02') to allow P01-authored prose mentions to remain byte-identical while gating only stub-heading removal; deferral-as-load-bearing-content: bold '**_deferred to P03_**' cells function as scope-boundary markers that a later phase replaces in place (D015 split), schema-extension additive-only — new top-level field added between sync_mode and recommended_cron with matching _schema_docs entry; pending-sentinel field convention extends cleanly (new field defaults to 'pending' value, auto-included in PENDING_FIELDS csv until operator completes init via github-init.sh live run), gate-script self-exclusion via case-branch on BASH_SOURCE-style file match; P02 phase-suite mirrors P01 structure with SUMMARY line form adjusted to 'pass=N fail=M' per T07 plan; comment-discipline synonyms (assoc-arrays, array-from-stdin, case-conversion expansion, combined-redirect shorthand) keep the bash32 gate self-clean under its own scanner"
drill_down_paths:
  - ".orchestrator/milestones/M013/phases/P02/tasks/T01-SUMMARY.md, .orchestrator/milestones/M013/phases/P02/tasks/T02-SUMMARY.md, .orchestrator/milestones/M013/phases/P02/tasks/T03-SUMMARY.md, .orchestrator/milestones/M013/phases/P02/tasks/T04-SUMMARY.md, .orchestrator/milestones/M013/phases/P02/tasks/T05-SUMMARY.md, .orchestrator/milestones/M013/phases/P02/tasks/T06-SUMMARY.md, .orchestrator/milestones/M013/phases/P02/tasks/T07-SUMMARY.md"
duration: "282m"
verification_result: "pass"
completed_at: "2026-04-21T23:48:03Z"
observability_surfaces:
  - "none"
---

M013/P02 (US-1 Projection create path) shipped the orchestrator→GitHub projection workhorse and pinned the FR-15 dry-run manifest format as a load-bearing contract for P03/P04. Seven tasks, 8/8 phase-suite gates PASS, all 5 P01 gates still green (one additive gate update for `sub_issue_mode`).

T01 shipped `scripts/integrations/github-common.sh` (12 public functions: orchestrator-id derivation, FR-4 marker primitives with byte-identity verification via `shasum`, sidecar read/write with AWK depth-aware brace walker for `items.<id>` upserts — no jq hard-dep) plus the `tests/fixtures/m013-p02/` fixture tree (seed orchestrator-state with P02 in-flight + P03 planning-state for AS-4a exercise, six gh-stub responses). T02 shipped the 538-line `scripts/integrations/github-init.sh` create-path workhorse: flag surface, SC-7 auto-mode pending-sentinel path (zero `gh` calls when no TTY + no `--i-am-operator`), populated the three T01 preflight echo-stubs (auth scope enumeration, sub-issue REST probe returning native vs labeled-fallback, label-collision adopt-vs-strict), lazy state walker honoring US-1 AS-4a (Planning-state phases skipped), marker search-before-create idempotency, FR-5 GraphQL whitelist limited to `createProjectV2` + `addProjectV2ItemById` (third shape `updateProjectV2ItemFieldValue` correctly deferred to P04). T03 pinned the FR-15 dry-run manifest format (`manifest_header`/`manifest_upsert_line`/`manifest_footer` helpers with FORMAT STABILITY CONTRACT doc block) and fixed a latent T02 `--root` resolution bug (SCRIPT_DIR-vs-PROJECT_ROOT split). T04 authored `commands/github-init.md` following MEM012 structure. T05 extended `references/github-integration.md` in place (+98 lines) with Auth Modes, Sub-Issue Representation Modes, Partial Mapping Table (3 rows `_deferred to P03_` as load-bearing markers per D015), `init` Workflow, and Dry-Run Manifest Format subsections; P01-authored sections byte-identical via section-content sha256 gating (robust against line-number shifts). T06 extended the sidecar schema with `sub_issue_mode` field (enum: native|labeled-fallback|pending) and took Edit C to add the SUB_ISSUE_MODE configured-output line consistent with the P01 one-field-one-line contract. T07 authored the 8-gate phase-suite orchestrator + bash-3.2 compat sweep with comment-discipline synonyms to keep the scanner self-clean.

Demo criterion satisfied: `github-init.sh --dry-run --root tests/fixtures/m013-p02/orchestrator-state/ --repo-slug test/test` with fixture stubs produces a 14-line manifest byte-identical with `expected-manifest.txt` (1 MANIFEST header + 12 UPSERT lines + 1 footer). AS-4a lazy projection verified: Planning-state P03 absent from manifest. SC-7 zero-prompts verified: auto-mode gate PATH-shims a fake `gh` and asserts the call log is empty. FR-4 marker invariant verified via `shasum_marker_byte_identity`. Second invocation with pre-populated sidecar produces `upserts=0 skipped=12 errors=0` via marker search-before-create (idempotency).

D015 scope split honored throughout — P03 deferrals (spec chunk / AC / verification-status mapping rows, FR-5 GraphQL three-shape lint, FR-14 re-init adoption from sidecar-absent + marker-bearing remote) are present as load-bearing `_deferred to P03_` markers rather than stub prose. Knowledge-Layer Boundary (FR-9 + D014) respected — no SPEC-* frontmatter changes, no `KNOWLEDGE-INDEX.md` extensions, no `scripts/knowledge/rebuild-index.sh` modifications.

Key patterns established: (1) AWK-based JSON object upsert with depth-aware brace walking — no jq hard-dep; (2) PATH-shimmed fake-gh for zero-subprocess-call invariant enforcement in tests; (3) buffered-manifest-body pattern — mktemp tempfile collects UPSERT lines during walker iteration with counts finalized before header emission; (4) FORMAT STABILITY CONTRACT doc-block at helper definition site pins shape for downstream consumers; (5) section-content byte-identity gate via awk-extract by literal-prefix heading match + shasum compare against embedded hashes — robust against insertion-shifts; (6) load-bearing `_deferred to P03_` cells functioning as scope-boundary markers that a later phase replaces in place; (7) fixture-driven preflight with env-var stub selectors (`M013_GH_STUB_AUTH`/`SUBISSUE`/`LABELS`); (8) sidecar-state-driven dry-run reason resolution (inspects repo_slug/project_v2_id + items.<id> to emit create vs skip-existing-marker without any gh calls).

Verification: phase suite exit 0, 8/8 P02 gates PASS. All P01 gates remain green (one additive update to `m013-p01-github-status.sh` for the new `sub_issue_mode` field per T06 plan Step 6 arbitration path). Zero `gh` subprocess invocations in CI.

Judgment calls surfaced during execution (for operator review): (a) auto-mode strict SC-7 stance — no `gh` writes ever without TTY + `--i-am-operator`, confirmed as the right call for dogfooded integration; (b) T02's `create_project_v2` GraphQL mutation uses `ownerId:"PLACEHOLDER"` — real live runs require operator-provided owner node ID (acceptable for P02 since live creation is operator-attested at dogfood, not CI-exercised); (c) state walker treats done phases as projectable for Project v2 discoverability — P04 drives status-field closure via `updateProjectV2ItemFieldValue`; (d) `phase-transition.sh` non-numeric duration bug (P01 inheritance) — all P02 task summaries use integer-minutes durations; (e) `commands/README.md` index not updated per task plan deferral; (f) T07 SUMMARY line form uses self-named `SUMMARY: m013-p02-phase-suite.sh pass=N fail=M` (differs from P01's shape) — T07 plan explicitly prescribed this self-named form.

Observability surfaces: none at P02 scope. P03 introduces `unit_close` + `conversus_gate_invocation` JSONL emitters in the [M019](../../../../milestones/M019/index.md) Tier 1 shape (per roadmap).
