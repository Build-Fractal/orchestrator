---
schema_version: "1.0"
type: task-summary
id: "T02"
parent: "P04"
milestone: "M013"
provides:
  - "scripts/integrations/github-sync.sh core (state walker + reconcile loop + --dry-run manifest + lock acquisition + updateProjectV2ItemFieldValue mutation + sidecar_update_item_cache call sites); scripts/verify/m013-p04-github-sync.sh (14/14 PASS); scripts/verify/m013-p04-dry-run-manifest.sh (4/4 PASS)"
requires:
  - "from:P04/T01 what:tests/fixtures/m013-p04/sync-cycle/ fixture tree; from:P04/T01 what:github-common.sh helpers http_probe sidecar_update_item_cache emit_tier1_record; from:P03/T03 what:graphql-call-shape.sh whitelist with updateProjectV2ItemFieldValue pre-approved; from:P02 what:github-init.sh walker shape and flag-parser layout; from:M001 what:lock-manager.sh create/break subcommands"
affects:
  - "scripts/integrations/github-sync.sh; scripts/verify/m013-p04-github-sync.sh; scripts/verify/m013-p04-dry-run-manifest.sh"
key_files:
  - "scripts/integrations/github-sync.sh; scripts/verify/m013-p04-github-sync.sh; scripts/verify/m013-p04-dry-run-manifest.sh"
key_decisions:
  - "Lock-manager invocation uses LOCK_MGR variable (not literal lock-manager.sh on invocation line) per idiomatic shell patterns — gate assertion updated to accept both shapes rather than inline the path; Walker emits phases-first-milestone-last (vs init.sh milestone-first) to match expected-sync-dryrun-manifest.txt SSOT; Tasks under ready phases ARE walked (resolve to skip-nochange via synced:true in sidecar) — payload Step 7 'only for Done phases' was an imprecise hint; Dry-run path does NOT acquire the lifecycle lock (read-only, safe to race); Conversus-gate invocation kept as commented scaffold for T05 surgical extension; perform_upsert stub helpers (lookup_project_v2_item_id / lookup_status_field_id / lookup_done_option_id) ship signatures only in T02 — T03 wires live-path resolution"
patterns_established:
  - "walker-shape-symmetry: sync reuses init's projectable-phase walker with phases-first-milestone-last ordering swap for manifest-SSOT alignment; idiomatic-lock-variable-gate: assertion accepts LOCK_MGR=<path>/lock-manager.sh + bash $LOCK_MGR create as valid invocation shape without requiring literal one-line match; perform_upsert-stub-signatures: live-path helpers (lookup_*_id) ship empty-string returns under stub mode so T03 can layer fixture-driven values without touching sync's shape; conversus-gate-scaffold-comment: T05 extension point preserved as commented block with full argument shape; temp-file-plus-diff: gate scripts write actual output to /tmp and diff against expected snapshot (AD-19 compliant alternative to process substitution)"
drill_down_paths:
  - ".orchestrator/milestones/M013/phases/P04/tasks/T02-PAYLOAD.md"
duration: "75"
verification_result: "pass"
completed_at: "2026-04-22T03:35:15Z"
---

T02 shipped scripts/integrations/github-sync.sh — the reconcile engine symmetric with P02's github-init.sh create path. 547 lines commented. Flag-parser mirrors init (--dry-run, --i-am-operator, --root, --repo-slug, --conversus-gate no-op scaffold, --timeout). SC-7 auto-mode short-circuit fires before any lock acquisition or gh call. FR-11 reversibility: sidecar absent / pending sentinel → STATUS: pending-operator-complete + exit 0 (no-op) with zero side-effects. FR-7 lifecycle lock acquired via scripts/lifecycle/lock-manager.sh create .orchestrator/orchestrator.lock on live paths (dry-run is read-only and skips acquisition); released via EXIT/INT/TERM/HUP trap on every exit. State walker iterates projectable phases in lexical P##-* order emitting phase-issue then task-subissue rows; milestone row emitted LAST (byte-identity with expected-sync-dryrun-manifest.txt SSOT). Reconcile logic: desired=done + synced=false + phase-issue → status-sync; desired=done + synced=false + task-subissue → close; else skip-nochange. Third FR-5 whitelisted mutation (updateProjectV2ItemFieldValue) shape matches graphql-call-shape.sh anchor. Live mode calls sidecar_update_item_cache on success (last_error=null, status_field_synced=true, project_v2_attached=true) and on error (last_error=upsert-failed, status_field_synced=false). JSONL emission (unit_close) scaffolded as comment — T03 wires call sites. Conversus-gate invocation scaffolded as commented block — T05 extension point. 14/14 assertions PASS on m013-p04-github-sync.sh; 4/4 PASS on m013-p04-dry-run-manifest.sh; graphql-call-shape.sh detects 3 mutation shapes all whitelisted; T01 regression gates (m013-p04-sync-fixture 28/28, m013-p04-github-common-p04 19/19) still green; P02/P03 phase suites regression-clean vs pre-existing baseline failures. Key judgment calls: (a) lock-manager invocation uses LOCK_MGR variable rather than literal lock-manager.sh on the invocation line — gate assertion expanded to accept both shapes; (b) tasks under ready phases are walked (resolve to skip-nochange via synced=true in sidecar) rather than skipped — matches expected manifest SSOT which includes M013-FIX-P02-FIX-T01 row; (c) sidecar parser tolerates both single-line and multi-line item shapes; (d) perform_upsert live-path lookups (project_v2_item_id / status_field_id / done_option_id) return empty under stub mode, wiring deferred to T03. D014 Knowledge-Layer Boundary respected — no SPEC-* / KNOWLEDGE-INDEX.md / rebuild-index.sh writes. P02/P03 byte-identity preserved.
