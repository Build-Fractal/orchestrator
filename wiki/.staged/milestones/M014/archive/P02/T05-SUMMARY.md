---
schema_version: "1.0"
type: task-summary
id: "T05"
parent: "P02"
milestone: "M014"
provides:
  - "scripts/migrate/m014-p02-migrate-recent-changes.sh; scripts/verify/m014-p02-migration-idempotent.sh; live-repo migration of 7 legacy Recent Changes entries into marker region; stale 021-test-exporter dogfood entry removed from CLAUDE.md and AGENTS.md"
requires:
  - "from:disk what:CLAUDE.md pre-existing ## Recent Changes section + P01 dogfood marker region + AGENTS.md; from:P01 what:scripts/util/dual-write-runtime-md.sh"
affects:
  - "P02/T08 phase-suite (gate included)"
key_files:
  - "scripts/migrate/m014-p02-migrate-recent-changes.sh,scripts/verify/m014-p02-migration-idempotent.sh,CLAUDE.md,AGENTS.md"
key_decisions:
  - "dry-run dual-write-region target_path joined with -and- instead of brace-expansion to stay lint-clean; legacy_entries actual=7 not 6 as payload text stated"
patterns_established:
  - "one-time migration with --dry-run/--apply/--force; already-migrated short-circuit via combined signals (marker present + no stale dogfood + no legacy section below marker); strip-then-dual-write via helper"
drill_down_paths:
  - ".orchestrator/milestones/M014/phases/P02/tasks/T05-PAYLOAD.md"
duration: "15m"
verification_result: "pass"
completed_at: "2026-04-22T23:56:55Z"
---

One-time migration of pre-existing ## Recent Changes section (7 entries) into the orchestrator:recent-changes marker region of CLAUDE.md, with matching AGENTS.md regenerated byte-identically. Stale P01 dogfood entry (- 021-test-exporter: foo) dropped. Dry-run first showed legacy_entries=7 has_stale=1 has_legacy_section=1; --apply wrote both files and reported applied legacy_entries=7 stale_dogfood_removed=1. Post-migration verification: gate verifier PASS, SC-6a outside-invariant test PASS, anti-pattern-lint PASS on both new scripts. Idempotent: re-run prints SUMMARY: already-migrated. --force bypass exercised in verifier for dry-run output. Minor plan deviation: payload text said 6 legacy entries but the live repo had 7; verbatim script handled it correctly via wc -l. Dry-run target_path for dual-write-region record uses hyphen-joined string instead of brace-expansion to stay anti-pattern-lint clean.
