---
schema_version: "1.0"
type: phase-summary
id: "P01"
parent: "M019"
milestone: "M019"
provides:
  - "scripts/lib/pricing.sh sourceable library with pricing_file_path, pricing_last_updated, pricing_stale_days, pricing_is_stale, pricing_resolve_alias, pricing_lookup_rates, pricing_estimate_cost_usd, pricing_warning_reason, chars_to_tokens_quartile; scripts/verify/m019-schema.sh JSONL validator enforcing record_type/source/granularity enums, unit_close cost+quality pairing, pre-M019 additivity, scripts/dispatch/build-context.sh payload_breakdown JSONL emitter appended to .orchestrator/milestones/<Mxxx>/execution-log.jsonl; _bc_emit_payload_breakdown helper; PAYLOAD_CAPTURE tempfile + cat pattern preserving byte-identical stdout (SC-6), scripts/dispatch/dispatch-interface.sh dispatch_usage JSONL emitter; _di_emit_dispatch_usage helper with pricing-degradation C4 handling; tmp-file cost capture that preserves _PRICING_WARNING_REASON across subshell boundary, unit_close JSONL emitter in write-summary.sh across task/phase/milestone granularities; Goodhart-paired cost+quality blocks per record; validator source-enum extended to accept aggregate, five M019/P01 per-gate verify scripts (m019-p01-emitter-presence.sh, m019-p01-pricing-degradation.sh, m019-p01-source-enum.sh, m019-p01-zero-token-growth.sh, m019-p01-additive-compat.sh); fixture tree under tests/fixtures/m019-p01/ (pre-M019 log, post-M019 rollup demo, 4 bad-records, minimal M999 milestone); hermetic stub dispatch backend; ORCH_M019_EMIT test seam in all three emitters, m019-p01-bash32-compat.sh, m019-p01-no-pre-p00-emission.sh (SC-12 ordering guard), m019-p01-fixture-rollup.sh (SC-7 greppability demo), m019-p01-phase-suite.sh (8-gate orchestrator); directory-aware artifact check in check-must-haves.sh"
requires:
  - "from:M019/P00/T04 what:.orchestrator/config/pricing.yml, from:P01/T01 what:scripts/lib/pricing.sh (chars_to_tokens_quartile); from:P01/T01 what:scripts/verify/m019-schema.sh (payload_breakdown record_type validation); from:M019/P00/T01 what:post-payload-assembly hook point in build-context.sh, from:P01/T01 what:scripts/lib/pricing.sh (pricing_estimate_cost_usd, pricing_warning_reason, pricing_last_updated, chars_to_tokens_quartile); from:P01/T01 what:scripts/verify/m019-schema.sh (dispatch_usage + source enum); from:P01/T02 what:fixture-mode log-path carve-out pattern, from:P01/T01 what:scripts/verify/m019-schema.sh (record_type + unit_close shape validator); from:P01/T02 what:payload_breakdown records keyed by unitId; from:P01/T03 what:dispatch_usage records carrying estimated_cost_usd+pricing_version, from:P01/T01 what:scripts/verify/m019-schema.sh;from:P01/T01 what:scripts/lib/pricing.sh;from:P01/T02 what:build-context.sh payload_breakdown emitter;from:P01/T03 what:dispatch-interface.sh dispatch_usage emitter;from:P01/T04 what:write-summary.sh unit_close emitter, from:P01/T05 what:five per-gate verify scripts + fixtures + pricing lib + schema validator; from:P00/T05 what:phase-suite mirror template"
affects:
  - "P01/T02,P01/T03,P01/T04,P01/T05, T03,T04,T05,T06,P01, T04,T05,T06,P01, T05,T06,P01,M019, T06,P01,M019, P01,M019"
key_files:
  - "scripts/lib/pricing.sh,scripts/verify/m019-schema.sh, scripts/dispatch/build-context.sh, scripts/dispatch/dispatch-interface.sh, scripts/knowledge/write-summary.sh,scripts/verify/m019-schema.sh, scripts/verify/m019-p01-emitter-presence.sh,scripts/verify/m019-p01-pricing-degradation.sh,scripts/verify/m019-p01-source-enum.sh,scripts/verify/m019-p01-zero-token-growth.sh,scripts/verify/m019-p01-additive-compat.sh,scripts/dispatch/adapters/backend/stub.sh,tests/fixtures/m019-p01/fixture-milestone/M999-ROADMAP.md,tests/fixtures/m019-p01/fixture-milestone/phases/P01/P01-PLAN.md,tests/fixtures/m019-p01/fixture-milestone/phases/P01/tasks/T01-PLAN.md,tests/fixtures/m019-p01/pre-m019-execution-log.jsonl,tests/fixtures/m019-p01/post-m019-rollup-demo.jsonl,tests/fixtures/m019-p01/bad-records/missing-cost-block.jsonl,tests/fixtures/m019-p01/bad-records/missing-quality-block.jsonl,tests/fixtures/m019-p01/bad-records/bad-source-enum.jsonl,tests/fixtures/m019-p01/bad-records/bad-granularity.jsonl,scripts/dispatch/build-context.sh,scripts/dispatch/dispatch-interface.sh,scripts/knowledge/write-summary.sh, scripts/verify/m019-p01-bash32-compat.sh,scripts/verify/m019-p01-no-pre-p00-emission.sh,scripts/verify/m019-p01-fixture-rollup.sh,scripts/verify/m019-p01-phase-suite.sh,scripts/verify/check-must-haves.sh"
key_decisions:
  - "AD-1 char-quartile token estimate;AD-2 pricing file + ORCH_PRICING_FILE override;AD-4 three record_type enum + source enum + granularity enum, AD-1 char-quartile token estimate; SC-6 byte-identical stdout; C5 bash 3.2 compatibility; MEM004 carve-out for dispatch-internal emitter code, C4 never-abort-on-pricing-degradation; SC-1 exactly-one-dispatch_usage-per-dispatch; SC-6 byte-identical-stdout; C5 bash 3.2; MEM004 carve-out for dispatch-internal emitter, C2 Goodhart pairing mandatory; SC-2 exactly-one unit_close per write-summary; SC-3 cost+quality pairing; SC-4 granularity enum + source enum extended to estimate|runtime|aggregate; AD-3 quality-from-existing-fields no new event surface; C3/SC-10 additive to existing summary output byte-for-byte, ORCH_M019_EMIT=0 early-return seam added to all three emitters for SC-6 verification; hit_count normalization in zero-token-growth gate (knowledge shared-state drift is orthogonal to payload-bytes contract); hermetic tmp-snapshot pattern across all gates (ORCHESTRATOR_ROOT override routes writes into fixture copies, never touching on-disk fixture or live .orchestrator tree); trap EXIT INT TERM restore in pricing-degradation so a failed gate never leaves pricing.yml backed up, AD-19 single-script-file Check shape; SC-12 epoch 2026-04-18T02:21:28Z lexical-compare against ISO-8601 UTC timestamps; MEM021/P04 split-needle self-match avoidance"
patterns_established:
  - "Module-scoped _PRICING_WARNING_REASON channel set by pricing_estimate_cost_usd and read by pricing_warning_reason (never aborts caller, always exit 0); never-abort degradation returns empty string on missing/stale/no-rate; awk variable naming avoids in_ identifier collision with awk in keyword on BSD awk; JSONL validator uses pure grep/sed shape-check with no jq dep and additivity for pre-M019 records, PAYLOAD_CAPTURE tempfile + cat emission pattern — capture via stdout redirection, replay via cat, measure via wc -c; fixture-mode log-path carve-out (ORCH_ROOT IS milestone dir); bail-safe emitter (mkdir/append failures stderr-noted, never abort dispatch); JSON double-quote escape in section_tokens display-name keys via sed, tmp-file cost capture pattern — route pricing_estimate_cost_usd stdout to a mktemp file so module-scoped _PRICING_WARNING_REASON survives in the calling shell (subshell side-channel loss workaround); adapter-failed / adapter-malformed override warnings for pricing-independent error paths; post-BACKEND three-branch emit (backend_crashed exit 5; backend_malformed schema exit 6; backend_malformed type exit 6) plus happy-path emit = exactly one dispatch_usage per dispatch after BACKEND is resolved, child-granularity filter in pass_rate awk — phase counts child tasks, milestone counts child phases (strict Must-Haves interpretation over generic unit_close child match); duration-suffix parser _ws_parse_duration_seconds supports m/h/s suffix and bare integers; prefix-match any-null propagation — null estimated_cost_usd in any contributing record propagates null up the aggregation chain (Tier 1 degradation signal preserved across rollup); fixture-mode log-path carve-out reuses T02/T03 pattern (orch_root/phases marker), tmp-snapshot-then-route pattern — cp -R fixture-milestone to TMPDIR and export ORCHESTRATOR_ROOT so emitters land writes there; byte-identical-modulo-normalization diff — strip legit-drift lines (hit_count) before cmp to isolate emitter contribution; trap-safe pricing rename/restore with backup sentinel filename; filename-routed stub backend drop-in (no changes to dispatch-interface.sh); env-var test seam (ORCH_M019_EMIT) with default-on production behavior, directory-path artifact check via trailing-slash detect + -d test (skips min-lines/contains); lexical ISO-8601 timestamp comparison as ordering guard; awk-extract estimated_cost_usd via regex (numeric-or-null)"
drill_down_paths:
  - ".orchestrator/milestones/M019/phases/P01/tasks/T01-SUMMARY.md, .orchestrator/milestones/M019/phases/P01/tasks/T02-SUMMARY.md, .orchestrator/milestones/M019/phases/P01/tasks/T03-SUMMARY.md, .orchestrator/milestones/M019/phases/P01/tasks/T04-SUMMARY.md, .orchestrator/milestones/M019/phases/P01/tasks/T05-SUMMARY.md, .orchestrator/milestones/M019/phases/P01/tasks/T06-SUMMARY.md"
duration: "245m"
verification_result: "pass"
completed_at: "2026-04-18T04:08:40Z"
observability_surfaces:
  - "payload_breakdown,dispatch_usage,unit_close JSONL records in .orchestrator/milestones/<M>/execution-log.jsonl"
---

## What Was Built

P01 ships the Tier 1 emitter for observability and efficiency metrics: a zero-token-growth JSONL record stream covering payload composition, dispatch usage, and unit close across task / phase / milestone granularities, with Goodhart-paired cost + quality blocks on every close record. No Tier 2/3 surface — no `orchestrator:cost` command, no rollup script, no efficiency footer, no anomaly checks, no UI.

Six tasks landed in strict linear order:

- **T01** (`773fea7`) — `scripts/lib/pricing.sh` (sourceable bash 3.2 lib: `pricing_file_path`, `pricing_last_updated`, `pricing_stale_days`, `pricing_is_stale`, `pricing_resolve_alias`, `pricing_lookup_rates`, `pricing_estimate_cost_usd`, `pricing_warning_reason`, `chars_to_tokens_quartile`; honors `ORCH_PRICING_FILE`; never aborts caller — returns empty + module-scoped `_PRICING_WARNING_REASON` on missing / stale / no-rate). `scripts/verify/m019-schema.sh` (pure-bash JSONL validator: enforces `record_type` / `source` / `granularity` enums, `unit_close` cost + quality pairing, pre-M019 additivity). Pitfall captured for downstream tasks: BSD awk treats `in` as a keyword, so awk vars starting with `in_` silently zero; use `ival` / `oval`.

- **T02** (`9e4c0e5`) — `scripts/dispatch/build-context.sh` emits one `payload_breakdown` JSONL record per invocation to `.orchestrator/milestones/<Mxxx>/execution-log.jsonl` (fields: record_type, unitId, milestone, phase, task, payload_chars, payload_tokens_estimate via `chars_to_tokens_quartile`, section_tokens per context-recipe section, token_estimate_method: "char-quartile", model, source: "estimate", timestamp). SC-6 byte-identical stdout preserved via a `PAYLOAD_CAPTURE` tempfile + `cat` pattern — the emitter runs after stdout and never contributes bytes to the payload.

- **T03** (`eab79f3`) — `scripts/dispatch/dispatch-interface.sh` emits one `dispatch_usage` JSONL record per invocation after BACKEND is resolved, on both happy and error paths (backend_crashed exit 5; backend_malformed schema / type exit 6). Fields: input_tokens_estimate, output_tokens_estimate placeholder, estimated_cost_usd via pricing lib, pricing_version from pricing.yml `last_updated`, source: "estimate". C4 degradation: when pricing.yml is missing or stale >90 days, `estimated_cost_usd: null` + `pricing_warning`, exit 0. Resolved T01's subshell pitfall — estimator stdout captured via tmp-file so the module-scoped `_PRICING_WARNING_REASON` survives for the warning read.

- **T04** (`aa9d383`) — `scripts/knowledge/write-summary.sh` emits one `unit_close` JSONL record per `task | phase | milestone` invocation, carrying BOTH a cost block (summed from the unit's `payload_breakdown` + `dispatch_usage` records; null propagates up the aggregation chain — Tier 1 degradation signal preserved across rollup) AND a quality block (verification_pass_rate, deviation_count, retry_count derived per AD-3 from existing `attempt` / `outcome` / `verification_result` fields — no new event surface). `m019-schema.sh` source enum extended to `estimate | runtime | aggregate`. Summary file stdout unchanged byte-for-byte (C3 / SC-10).

- **T05** (`4edf615`) — fixtures + 5 per-gate scripts: `m019-p01-emitter-presence.sh`, `m019-p01-pricing-degradation.sh` (trap-safe rename/restore with backup sentinel), `m019-p01-source-enum.sh`, `m019-p01-zero-token-growth.sh`, `m019-p01-additive-compat.sh`. Fixture tree under `tests/fixtures/m019-p01/`: pre-M019 log, post-M019 rollup demo, 4 bad-record fixtures, minimal M999 fixture milestone. Hermetic `scripts/dispatch/adapters/backend/stub.sh` (filename-routed, no change to dispatch-interface). `ORCH_M019_EMIT=0` early-return seam added to all three emitters for SC-6 verification; default-on production behavior unchanged.

- **T06** (`532f8d7`) — `m019-p01-bash32-compat.sh` (split-needle self-match avoidance, M021/P04 pattern), `m019-p01-no-pre-p00-emission.sh` (SC-12 ordering guard; lexical ISO-8601 compare against the P00 epoch `2026-04-18T02:21:28Z`; [M011](../../../../milestones/M011/index.md) and earlier exempted per D009), `m019-p01-fixture-rollup.sh` (SC-7 greppability demo — VERIFICATION ASSET, no command shipped), `m019-p01-phase-suite.sh` (8-gate orchestrator). Directory-path artifact check added to `scripts/verify/check-must-haves.sh` (root-cause fix for silently-failing directory-artifact lines).

## Key Decisions

- **Zero-token instrumentation (SC-6).** Emitters write to JSONL log files, never into the payload. `PAYLOAD_CAPTURE` tempfile + `cat` pattern in `build-context.sh` is the load-bearing trick: stdout is produced by `cat`, the emitter runs after and contributes zero bytes. `zero-token-growth` gate normalizes `hit_count: N` lines before `cmp` (knowledge shared-state drift is orthogonal to payload-bytes).
- **Goodhart pairing (C2 / SC-3).** Every `unit_close` carries both cost and quality; schema validator rejects records missing either. Pairing happens at emission time, not at rollup, so Tier 2 cannot accidentally optimize cost in isolation.
- **Schema additivity for Tier 2/3 forward compat (SC-4).** `source: estimate | runtime | aggregate` and `granularity: task | phase | milestone` enums reserved at Tier 1. `record_type` is a discriminator. Tier 3 backend-actuals adapters land additively — no Tier 1 record-writer needs modification.
- **Pre-M019 consumer additivity (C3 / SC-10).** New fields are additive only; `duration_s`, `outcome`, `verification_result`, `attempt`, `unitId` keep current semantics + positions. `scripts/state/derive-phase.sh` unchanged. Pre-M019 records still validate.
- **Never-abort degradation (C4).** Pricing lib returns empty on missing / stale / no-rate, sets module-scoped warning; emitter records `estimated_cost_usd: null` + `pricing_warning`, exits 0. Dispatch continues.
- **Test seam (ORCH_M019_EMIT).** Env-var early-return on all three emitters so SC-6 byte-identity can be measured in-place; production defaults on.
- **SC-12 epoch ordering (D009).** `2026-04-18T02:21:28Z` (P00-SUMMARY `completed_at`) is the first instant any emitter record may appear in a post-M011 milestone's log. Enforced by lexical ISO-8601 compare. Live scan on [M015](../../../../milestones/M015/index.md) / [M016](../../../../milestones/M016/index.md) / M019 / [M021](../../../../milestones/M021/index.md) is clean.

## Verification Results

`scripts/verify/m019-p01-phase-suite.sh` — PASS 8 / FAIL 0:

- `m019-p01-emitter-presence.sh` — payload_breakdown, dispatch_usage, and unit_close each emitted exactly once per corresponding boundary
- `m019-p01-pricing-degradation.sh` — pricing.yml renamed mid-run; dispatch writes `estimated_cost_usd: null` + `pricing_warning`, exit 0; trap restores pricing.yml on exit
- `m019-p01-source-enum.sh` — `estimate | runtime | aggregate` accepted, others rejected
- `m019-p01-zero-token-growth.sh` — 23971 bytes pre / 23971 bytes post with emitters on; delta = 0 modulo hit_count normalization
- `m019-p01-fixture-rollup.sh` — granularity-keyed totals print from ~10 fixture records (SC-7 greppability)
- `m019-p01-additive-compat.sh` — pre-M019 log fixture still validates
- `m019-p01-no-pre-p00-emission.sh` — no emitter record predates the P00 epoch in any post-M011 milestone
- `m019-p01-bash32-compat.sh` — all 15 P01-touched/created `.sh` files clean

Plus: `m019-p00-phase-suite.sh` still PASS 4 / FAIL 0 — P00 regression gate preserved. `tests/test-s05.sh` 100/100, `tests/test-s06.sh` 57/57. `scripts/verify/anti-pattern-lint.sh` clean.

## What M019 Delivers to [M012](../../../../milestones/M012/index.md)–[M014](../../../../milestones/M014/index.md)

M012–M014 dogfooding now runs on top of a measured baseline: every task / phase / milestone close produces a JSONL record with paired cost + quality, greppable by `record_type` and `granularity`. Tier 2 rollups and Tier 3 backend-actuals land additively — no Tier 1 rewrite.
