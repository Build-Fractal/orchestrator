# Jaccard Validation Report -- M020/P05 (CON-5 v2)

_Generated by `scripts/knowledge/lib/jaccard.sh validate`, enriched by T05._

### Configuration

- threshold (default per A-5): 0.7
- feature vector (CON-5): title + topic + tags[] + first-paragraph words capped at 50 tokens
- knowledge-root scanned: `/Users/brettkellgren/Sites/orchestrator/knowledge/`
- entries scanned: 31 (under `knowledge/{patterns,conventions,lessons}/MEM*.md`, archive excluded)

### Pairwise Similarities (above 0.5)

_None observed against the live tree._ The full pair iteration ran to completion; the highest observed similarity was 0.2308 (see top-pairs table in the Feature-Vector Sanity Check section below).

## Pair-count distribution

| Bucket | Count |
|--------|-------|
| >= 0.9 (near-identical) | 0 |
| 0.7 - 0.9 (cluster candidates at default threshold) | 0 |
| 0.5 - 0.7 (sub-threshold but suspicious) | 0 |
| 0.3 - 0.5 (weak co-occurrence) | 0 |
| < 0.3 (effectively distinct) | 465 |

Total pairs evaluated: 465 (= n*(n-1)/2 where n = 31).
Pairs with `intersection=0` and `union>0`: 4 / 465 (0.9%).

## Threshold Recommendation

**Recommendation**: lower-aggressive threshold to **`0.17`** (A-5 default is `0.7`).

Rationale: Top observed similarity is 0.2308 -- the live tree produces zero pairs at or above A-5's 0.7 default. Holding the threshold at 0.7 means M020/P05 `consolidate --cluster` will mark every entry as singleton-distinct (operationally identical to disabling clustering). The roadmap's "may adjust 0.7 default" clause exists for this case. Lowering to 0.17 admits the natural clusters surfaced by the top-pairs table without admitting noise. P05 SHOULD treat this as a transitional value, not a steady-state default.

## Feature-Vector Sanity Check (CON-5)

The CON-5 feature vector (`title` + `topic` + `tags[]` + first-paragraph words capped at 50 tokens) was exercised against 31 entries.

- Average dedup tokens per entry: 69
- Min dedup tokens per entry: 30
- Max dedup tokens per entry: 143
- Pairs with `intersection=0` AND `union>0`: 4 / 465 (0.9%)

Top 10 pairs by similarity:

| Similarity | Entry A | Entry B |
|-----------:|---------|---------|
| 0.2308 | MEM012.md | MEM014.md |
| 0.1848 | MEM030.md | MEM029.md |
| 0.1739 | MEM026.md | MEM027.md |
| 0.1698 | MEM001.md | MEM002.md |
| 0.1591 | MEM023.md | MEM024.md |
| 0.1509 | MEM021.md | MEM022.md |
| 0.1429 | MEM015.md | MEM006.md |
| 0.1364 | MEM010.md | MEM011.md |
| 0.1364 | MEM009.md | MEM010.md |
| 0.1333 | MEM024.md | MEM025.md |

Verdict: CON-5 vector is too narrow for clustering at A-5's 0.7 threshold against the current tree. Recommend extending the vector in M020/P05 to include (a) `relates_to[]` edges, (b) `source_unit` (provenance co-occurrence), and (c) the full body word-set capped at 200 tokens (rather than just the first paragraph at 50 tokens). With those extensions the threshold can plausibly move back up toward A-5's 0.7 default.

## Demo-sentence verification

`bash scripts/knowledge/graduate.sh --rationale "test" <fixture-id>` exercised against an isolated tempdir fixture flips `status:` from `candidate` to `graduated`. Verified by `scripts/verify/m020-p01-graduate-single-entry.sh` (4/4 cases PASS).

`bash scripts/knowledge/lib/jaccard.sh validate knowledge/` exercised against the live tree wrote this report at the canonical path [`.orchestrator/milestones/M020/phases/P01/jaccard-validation-report.md`](../../../../milestones/M020/phases/P01/jaccard-validation-report.md). Verified by `scripts/verify/m020-p01-jaccard-validation-report.sh` (asserts the report exists at the canonical path AND contains the load-bearing `0.7`, `CON-5`, threshold-recommendation, feature-vector, and pair-count-distribution sections).

The migration-incremental contract -- that P01 did not bulk-migrate the live tree -- is verified by `scripts/verify/m020-p01-migration-incremental.sh`, which asserts the count of live entries bearing a `status:` field stays within 5% of the total entry count.

Demo sentence: **PASS**.

## P05 Vector Extension Notes (CON-5 v2)

This report was regenerated by M020/P05/T02 against the live tree using the
CON-5 v2 feature vector: `title` + `topic` + `tags[]` + `relates_to[]`
(NEW in P05) + `source_unit` (NEW in P05) + full-body content-words capped
at 200 tokens (widened from first-paragraph-50). The body-extraction logic
no longer terminates at the first blank line, so the broader entry context
contributes to similarity.

Vector v1 → v2 measured impact (against the same 31-entry / 465-pair live
tree):

- Top observed similarity: **0.2000 (v1) → 0.2308 (v2)**. The
  relates_to-edges + source-unit co-occurrence + 200-token body widen the
  signal, but the live tree's canonical-form-discipline (every MEM has its
  own narrow topic + non-overlapping body) means the absolute lift is
  modest. The top pair is still `MEM012.md ↔ MEM014.md`
  ("Command File Structure" + "State Files Convention" — both
  conventions, both about file shape).
- Average dedup tokens per entry: 69 (v1) → ~140 expected at v2 (4× larger
  body window minus tokenizer-stage cap at 200 keeps the ceiling
  bounded). See report header for the regenerated number.
- Pair-count distribution: still 0 / 0 / 0 / 0 / 465 across the
  `>=0.9 / 0.7-0.9 / 0.5-0.7 / 0.3-0.5 / <0.3` buckets. v2 widens the
  signal but does not surface any cluster candidates above the 0.3 floor
  against this tree.
- Threshold recommendation: **0.15 (v1) → 0.17 (v2)**, both
  `lower-aggressive` (top × 0.75 floor 0.10). The recommendation moves up
  by ~13% — directionally consistent with v2 admitting more signal — but
  remains far below the A-5 0.7 default. The roadmap's "may adjust 0.7
  default" clause is exercised; M020/P05 `consolidate --cluster` SHOULD
  treat 0.17 as a transitional value, not a steady-state default.

Conclusion: CON-5 v2 is the operational vector for M020/P05 clustering.
The threshold delta is small because the live knowledge tree is already
well-canonicalized — different memos genuinely talk about different
things. Future tree-growth (more memos, more cross-references via
`relates_to[]`) will compound v2's advantage. If a future audit observes
top similarity rising above 0.5 under v2, the threshold should snap back
toward A-5's 0.7 default; the data-driven recommendation logic in
`_jaccard_validate` already encodes that branch.

