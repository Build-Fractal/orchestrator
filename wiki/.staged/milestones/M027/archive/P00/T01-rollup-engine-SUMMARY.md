---
schema_version: "1.0"
type: task-summary
id: "T01"
parent: "P00"
milestone: "M027"
provides:
  - "scripts/diagnostics/metrics-rollup.sh — sourceable bash library + CLI for M027 rollup engine. Exposes metrics_rollup_snapshot, metrics_rollup_normalize, metrics_rollup_aggregate, metrics_rollup_render, metrics_rollup_main. CLI accepts --granularity task|phase|milestone|project, --milestone Mxxx, --phase Pxx, --task <id>, --source estimate|runtime|aggregate|all, --log <path>, --help. Implements FR-1/FR-2 paired cost+quality rows, FR-3 source filter with empty-result annotation, FR-4 Goodhart pairing (cost columns always paired with quality columns; pass_rate=unknown sentinel for degenerate quality input), FR-11 (N missing) suffix on cost cells with pricing warnings, FR-12/CON-1 read-only via mktemp+cp snapshot + EXIT trap, FR-14 corrupt-line tolerance (WARN: corrupt JSONL line N), FR-17 input-schema validation (WARN: input-schema line N), FR-18/AD-1 aggregation precedence aggregate-greater-than-runtime-greater-than-estimate, FR-19/AD-3 copy-then-aggregate FS-race semantics, CON-5 graceful degradation on missing log, CON-6/FR-21 zero LLM tokens (bash+awk+grep+sed), CON-7 bash 3.2 compat. Live invocation against .orchestrator/milestones/M019/execution-log.jsonl prints exactly one milestone row carrying both cost block and quality block on the same row, exit 0."
requires:
  - "from:M019/P01 what:execution-log.jsonl JSONL schema (record_type ∈ {payload_breakdown,dispatch_usage,unit_close}; source ∈ {estimate,runtime,aggregate}; granularity ∈ {task,phase,milestone}); from:scripts/lib/pricing.sh what:pricing_warning_reason concept (consumed only via record-level pricing_warning field; rollup does not source pricing.sh at runtime in this task)"
affects:
  - "P00/T02,P00/T03,P00/T04"
key_files:
  - "scripts/diagnostics/metrics-rollup.sh"
key_decisions:
  - "AD-1,AD-3,AD-19"
patterns_established:
  - "Sourceable-CLI duality via [ BASH_SOURCE-zero == zero-arg ] guard at bottom of file; load-time _METRICS_ROLLUP_SH_SOURCED re-source guard pattern (mirrors pricing.sh); MEM004 carve-out applied — pipes/awk/dollar-paren permitted inside emitter-internal library while AD-19 single-script-file shape rule binds only Check: commands at task/phase plan level; awk single-pass aggregation with parallel-array buckets keyed by (scope,source,granularity) plus per-scope highest-priority-source selection in END block — sidesteps bash 3.2 lack of associative arrays; pre-declared snapshot/normalized/rolled vars before EXIT trap to keep set -u from blowing up the trap when an early-return path skips assignment; bash-3.2-clean comment hygiene — neutralized literal forbidden-construct tokens in comments to avoid tripping the T03 bash32-compat grep regex against the file body"
drill_down_paths:
  - ".orchestrator/milestones/M027/phases/P00/tasks/T01-rollup-engine-PAYLOAD.md,.orchestrator/milestones/M027/phases/P00/tasks/T01-rollup-engine-PLAN.md"
duration: "35m"
verification_result: "pass"
completed_at: "2026-04-26T22:21:01Z"
---

Created scripts/diagnostics/metrics-rollup.sh (734 lines, executable, sourceable). Implements the M027 P00 rollup engine as both a sourceable bash library (functions only on import; metrics_rollup_snapshot, metrics_rollup_normalize, metrics_rollup_aggregate, metrics_rollup_render, metrics_rollup_main) and a CLI invoked only when running as the entry point (BASH_SOURCE-zero == zero-arg guard at the bottom of the file). Reads [M019](../../../../milestones/M019/index.md) Tier 1 JSONL, applies copy-then-aggregate FS-race semantics via mktemp+cp snapshot under TMPDIR with EXIT trap cleanup (FR-19/AD-3), tolerates corrupt lines with stderr WARN diagnostics (FR-14), validates input schema per record_type (FR-17), enforces aggregation precedence aggregate-greater-than-runtime-greater-than-estimate at every granularity (FR-18/AD-1), supports --source filter with no-records-match-filter empty annotation (FR-3), surfaces pricing warnings as (N missing) cost-cell suffixes (FR-11), and prints paired cost+quality rows (FR-4) at task/phase/milestone/project granularity (FR-1/FR-2). Read-only (FR-12/CON-1) — only writes are temp files. Bash 3.2 only (CON-7) — no associative-array decl, no herestrings, no mapfile, no process substitution; uses parallel arrays in awk where bash 3.2 cannot. Zero LLM tokens (FR-21/CON-6) — bash+awk+grep+sed only. Verification: live invocation against M019 → exit 0 with one milestone row carrying both cost block (EST_COST_USD/TOKENS_EST/P50_COST/P95_COST) and quality block (PASS_RATE/DEVIATIONS/RETRIES); against NONEXISTENT milestone → no Tier 1 records yet, exit 0; unknown flag → exit 2 with usage; sourcing produces no stdout/stderr; all five public functions defined; --source runtime against M019 (estimate-only) → no records match filter, exit 0; grep for forbidden bash 3.2 constructs and zero-LLM-token patterns both return exit 1 (no matches). Two notable implementation decisions: (1) pre-declared snapshot/normalized/rolled with empty-string defaults before installing the EXIT trap, otherwise set -u panics on early-return paths; (2) reworded a doc comment that originally listed the forbidden tokens literally, since the T03 bash32-compat verifier greps the entire file body and would false-positive on prose. The remaining contract verifiers (Goodhart pairing, source-filter, aggregation precedence, corrupt-line, FS-race, perf bound, etc.) are explicitly T03 scope and the phase-suite gate is T04 scope per the T01-PLAN Verification block.
