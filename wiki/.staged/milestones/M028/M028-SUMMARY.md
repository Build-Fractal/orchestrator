---
schema_version: "1.0"
type: milestone-summary
id: "M028"
parent: "031-autonomous-hardening-v3"
milestone: "M028"
provides:
  - "hook portability across consumer projects (Finding A — BASH_SOURCE self-location + adapter absolute-path emission + installer-staged ~/.claude/orchestrator-hooks/ runtime-stable payload); lifecycle-script PATH lookup closure (Finding F — bare-name hooks retired in favor of absolute bash <abs-path>.sh emission); five new shape classes AP-010 through AP-014 (cmd-sub-in-pattern; quoted-arg-newline-hash; multiline-quoted-script; unquoted-brace-glob; xargs-sh-c-compound-body) with CON-5 one-level body-descent; four investigation-pattern wrappers under scripts/util/ (grep-files.sh; cleanup-stale-results.sh; node-eval.sh; peek-files.sh) retiring the screenshot investigation shapes; install-side dedup via (event,matcher,command) tuple closing the M018-close 5-Stop / 7-PreToolUse-dupes regression; install-roundtrip pinned-SHA gate extending M025 reversibility; permanent in-tree consumer fixture at tests/fixtures/downstream-project/ as CON-10 contract anchor for adapter --hook-config emission shape; M028 close-out regression gate sequencing 4 sub-gates (install-roundtrip + corpus-replay-27-entry + per-finding-run-all + downstream-fixture-replay); 8 per-finding verifiers (finding-A through finding-G plus G-classifier and G-wrapper) rolling up to canonical 'M028: 7/7 findings verified (skipped: 0,failed: 0)' summary line; 27-entry replay harness tests/run-prompt-corpus-replay.sh with WOULD_PROMPT=0/27 canonical line; CON-7 strict-superset preserved (every M021 baseline verdict on every M021 corpus entry unchanged); three-surface investigation-pattern documentation (commands/dispatch.md table + templates/dispatch-prompt.md bullets + ANTIPATTERNS.md catalog cross-ref)"
requires:
  - "M021 shape-classifier surface (scripts/verify/lib/shape-classifier.sh git SHA 12fcd98) + M021 21-entry prompt corpus + M021 pre-bash-shape-guard.sh hook + M021 ANTIPATTERNS.md AP-001..AP-009 baseline; M025 _orchestrator_managed:true uninstall-cascade invariant + M025 install/uninstall reversibility contract; M008 lifecycle hook enrollment (after-verify-sync + before-commit); Claude Code runtime adapter --hook-config emission contract; M018-close operator settings.json snapshot (sanitized to tests/fixtures/m028-pre-repair-snapshot.json); auto-loop --step=V mechanical verification resolver"
affects:
  - "M030 (adaptive model selection — next pre-launch milestone unblocked by M028 hook stability); every consumer project running orchestrator:auto in any non-orchestrator-repo CLAUDE_PROJECT_DIR context (Finding A blast radius now closed end-to-end); future shape-class additions (AP-015+ — closed pattern-set discipline preserves the M028 evidence floor; new evidence required for new AP-IDs); future investigation-pattern wrappers (defensive flag-refusal + self-relative-wrapper fixture + three-surface documentation discipline are reusable patterns); future close-out regression gates (sub-gate-sequencing-with-tmp-log + non-short-circuit semantics + per-sub-gate clean verifier shape are reusable)"
key_files:
  - "scripts/hooks/pre-bash-shape-guard.sh,scripts/verify/lib/shape-classifier.sh,scripts/dispatch/adapters/runtime/claude-code.sh,packaging/install/install-claude-code.sh,scripts/util/settings-merge.sh,scripts/util/grep-files.sh,scripts/util/cleanup-stale-results.sh,scripts/util/node-eval.sh,scripts/util/peek-files.sh,scripts/lifecycle/before-commit.sh,ANTIPATTERNS.md,tests/fixtures/m021-prompt-corpus.txt,tests/fixtures/m028-pre-repair-snapshot.json,tests/fixtures/m028-post-repair-canonical.json,tests/fixtures/downstream-project/.claude/settings.json,tests/run-prompt-corpus-replay.sh,tests/run-downstream-fixture.sh,scripts/verify/m028/install-roundtrip.sh,scripts/verify/m028/run-all.sh,scripts/verify/m028/p05-regression-gate.sh,scripts/verify/m028/finding-A-verifier.sh,scripts/verify/m028/finding-F-verifier.sh,scripts/verify/m028/finding-B-verifier.sh,scripts/verify/m028/finding-C-verifier.sh,scripts/verify/m028/finding-D-verifier.sh,scripts/verify/m028/finding-E-verifier.sh,scripts/verify/m028/finding-G-classifier-verifier.sh,scripts/verify/m028/finding-G-self-conformance.sh,scripts/verify/m028/finding-G-wrapper-verifier.sh,commands/dispatch.md,templates/dispatch-prompt.md,scripts/verify/replay-prompt-corpus.sh,scripts/verify/m025-p01-hook-schema.sh"
key_decisions:
  - "collapse-decision pinned to full-5-phase via per-screenshot causal trace (M=0 of N=7 threshold not met) — hook portability alone resolves zero events because the four B-family screenshots all carry existing-classifier verdict allow; AP-014 ordered BEFORE the existing AP-009 _sc_count_top_level_stages > 2 check (load-bearing for SC-6); AP-010..AP-013 ordered AFTER the four M021 reject checks (more-general rules fire first); CON-5 one-level body-descent via OPAQUE placeholder substitution (avoids unbounded recursion; bash 3.2 flat); CON-7 strict-superset preserved by case-extension discipline (new arms between existing M021 baseline arms and catch-all default); M025 baseline assertions 5+6 (orchestrator-post-verify / orchestrator-before-commit bare names) updated to absolute-path contract in lockstep with adapter rewrite (verifier-as-contract-test, not immutable artifact); per-leaf tuple-keyed dedup with single-walk target-key collection (empty post-dedup wrappers skipped to keep settings.json byte-stable); wrapper-OR-leaf managed-detection for repair path (preserves both pre-T02 wrapper-level and post-T02 leaf-level flagging shapes); strict-tuple match with extra-key guard for orphan removal (preserves user-authored entries that happen to share a tuple but add extra fields); pinned-SHA-in-comment as informational drift signal (hard gate stays byte-equality; pin is human audit trail); test-command stability across classifier evolution (bare-form compound chains over sh-c-body-wrapped chains); permanent in-tree consumer fixture (CON-10 — checked into version control NOT generated at test time); shape-pattern comparison via grep -cE regex over literal-bytes diff (HOME-agnostic shape contract); summary-contract preservation under additive verifier expansion (clamp pass_count at total=7 over bumping to 8); defensive flag-refusal as wrapper contract (node-eval.sh refuses -e/-p as load-bearing defense for AP-012); AP-014 wrapper choice for AP-011 (read-range.sh as diagnostic anchor; actual remedy is operator-command-shape change documented in ANTIPATTERNS.md); helper-function carve-out codified at top-of-file as canonical AD-19 verifier-authoring shape (function bodies are NOT scanned by AP-009 inline-shape classifier in production)"
patterns_established:
  - "BASH_SOURCE self-location with two-step fallback (install-side sibling + in-tree dev fallback with pwd -P symlink resolution; fail-open exit 0 on both miss); shape-classifier-aware resolution-block authoring (every introduced line empirically validated against classify_command before commit; HOOK_DIR_RAW intermediate replaces nested cmdsub; combined-test conditions split into single-test if-blocks); helper-function carve-out as canonical verifier-authoring shape (function bodies are NOT scanned by AP-009 inline-command-shape classifier; documented as top-of-file comment block convention starting M028/P02/T05 — load-bearing across every M028 verifier authoring assert_reject / classify_one / compute_sha / count_managed_flags / extract_corpus_input helpers); plan-time classifier-shape pre-validation discipline (every shape-bearing line empirically run through classify_command before commit; closes the planner-vs-classifier confabulation gap); per-task verifier co-located with deliverable (cross-task verifier dependency rejected; auto-loop --step=V mechanical resolver always succeeds at task time); direct invocation of project-tree verifiers in plan Verification section (no run-probe.sh wrapping; run-probe.sh is reserved for staged throwaway probes inside /tmp /var/folders <repo>/tmp/); two-mode verifier discipline for install-touching gates (cheap dry-run + isolated-HOME real-install); snapshot-ladder shape for byte-equality gates (pre -> action-1 -> action-2 -> reverse-action; SHA at each step; assert idempotency AND reversibility in single isolated-HOME exercise); pinned-SHA-in-comment as informational drift signal; per-leaf tuple-keyed dedup with single-walk target-key collection (empty post-dedup wrappers skipped); wrapper-OR-leaf managed-detection helper bridging M025-to-M028 shape evolution; strict-tuple match with extra-key guard (closes user-authored false-positive risk); idempotency-by-construction verifier (run cleanup twice, assert byte-equality); reversibility-normalization at the installer layer (file-absent for logically-empty JSON); test-command stability across classifier evolution (bare-form compound chains over sh-c-body-wrapped chains); append-only invariant on documentation registers (new entries strictly appended at EOF; existing entries unchanged byte-for-byte; verifiable via wc -l + grep-pattern preservation); closed-pattern-set discipline (M028 spec Non-Goals close the AP-ID set on the seven-screenshot evidence; AP-015+ deferred to future spec/milestone); evergreen forward-compatibility cross-refs (entries cite corpus IDs and wrapper paths that do not yet exist on disk at append time; pointers become live as targets land in subsequent tasks/phases); AP-014 ordering invariant in classify_command (more-specific reject classes inserted BEFORE more-general ones; documented in classify_command comments AND surfaced as discrete PASS line in verifier output); one-level-deep recursion bound via OPAQUE placeholder substitution (CON-5 implementation pattern); char-by-char quote-state machines for shape detection (single-quote / double-quote / backslash-escape state transitions on ${s:$i:1} index walk; bash 3.2 safe; no [[:alpha:]] lookups inside body; no process substitution; no declare -A); M021-strict-superset case extension discipline (new arms placed BETWEEN existing arms and catch-all default; preserves lookup precedence); two-harness symmetric coverage of one corpus (M021 historical + M028 spec-named, both consuming the same fixture); pinned-INPUT alignment between corpus and per-finding verifier (boundary cases use byte-identical INPUT to assert byte-identical verdict); self-conformance verifier scope-narrowing precedent (resolution + reject_lookup printf bodies, not dispatch — empirical line-by-line classifier scan informs the scope decision, not plan prose); bash-parameter-expansion case-arm body extraction (body=${line#*)}; body=${body%;;} — no sed/awk/external-process needed); defensive flag-refusal as wrapper contract (when a wrapper retires an antipattern, refuse the antipattern's signature flag rather than silently accepting it); self-relative-wrapper fixture pattern (verifier copies wrapper into isolated tmp scripts/util/; the copied wrapper's REPO_ROOT then resolves to the tmp root — no environment overrides); source-level self-conformance with comment-line strip (grep -v leading-hash-then-whitespace before scanning wrapper source for forbidden literals); optional-dependency SKIP discipline (command -v <tool> guards with SKIP not FAIL); summary-contract preservation under additive verifier expansion (clamp pass_count at total over bumping total — preserves operator-grep contract); three-surface documentation discipline for cross-cutting infrastructure (planner-facing + agent-facing + catalog-anchor; each surface frames the same content for its audience); two-axis assertion per element in cross-cutting verifiers (existence on disk AND integration into a downstream consumer); permanent in-tree consumer fixture under tests/fixtures/downstream-project/ as CON-10 anchor for noisy-fail shape gates; live-adapter-emission-vs-fixture-bytes shape gate via mktemp -d isolated HOME; shape-pattern comparison via grep -cE regex over literal-bytes diff; count-equality assertions for shape contracts (additive-axis discipline); installer-staged isolated HOME replay harness; JSON event payload via printf-NOT-heredoc (avoids AP-008 heredoc-with-expansion); negative-control assertion as M021-regression catch-all; wrap-the-harness Truth-Check verifier shape; sub-gate-sequencing-with-tmp-log shape; phase-level close-out task shape (zero new deliverables, pure verification gate); subprocess success-message hygiene (when wrapping verifier asserts absence of literal substring, subprocess MUST NOT emit that substring); pre-existing-task-drift fold-in at final-task close (small documentation-layer must-have lookup misses fold into phase's final task rather than re-dispatching completed tasks); header-comment-as-cross-reference-locator (machine-greppable cross-reference annotations in file headers)"
drill_down_paths:
  - ".orchestrator/milestones/M028/phases/P01/P01-SUMMARY.md, .orchestrator/milestones/M028/phases/P02/P02-SUMMARY.md, .orchestrator/milestones/M028/phases/P03/P03-SUMMARY.md, .orchestrator/milestones/M028/phases/P04/P04-SUMMARY.md, .orchestrator/milestones/M028/phases/P05/P05-SUMMARY.md"
duration: "900m"
verification_result: "pass"
completed_at: "2026-04-30T00:04:37Z"
observability_surfaces:
  - "execution-log.jsonl JSONL stream extended with M028 dispatch records; install-roundtrip.sh pinned-SHA d47ab7f32ef6bda277e4d3b6ab04ed70ddd92a3388250f13badc79d85389f32a as informational drift signal; M028: 7/7 findings verified summary line as canonical close-out grep contract; M028 close-out: 4/4 sub-gates clean as regression gate consolidated artifact; WOULD_PROMPT=0/27 canonical line on tests/run-prompt-corpus-replay.sh; WOULD_PROMPT=0/N canonical line on tests/run-downstream-fixture.sh"
---

M028 (autonomous hardening v3) closed the gap between [M021](../../milestones/M021/index.md)'s shape-guard infrastructure and the actual autonomous-run experience in any consumer project. Seven findings (A hook portability, B four new shape classes, C investigation-compound preservation, D missing cleanup wrapper, E missing investigation wrappers, F lifecycle-script PATH lookup, G xargs-sh-c-compound-body body descent) closed end-to-end across five phases. The pre-launch slice that lets `orchestrator:auto` run cleanly inside any downstream project, not just the orchestrator repo.

P01 (Empirical Baseline + Collapse-Decision Evidence, 120m) replayed all 9 M028 source events through the M021 classifier (git SHA `12fcd98`), pinned the collapse decision to **`full-5-phase`** via per-screenshot causal trace, and staged the canonical pre-repair fixture (`tests/fixtures/m028-pre-repair-snapshot.json`) — a sanitized M018-close `~/.claude/settings.json.bak` with partial-flag shape modeling realistic post-[M025](../../milestones/M025/index.md) mixed state. Patterns: staged-probe replay shape, two-stage deterministic sanitization (BSD-portable sed -E + python3), partial-flag fixture realism, rubric-driven Findings-A-through-G attribution.

P02 (Findings A + F — hook portability + adapter+installer dedup, 300m) retired `CLAUDE_PROJECT_DIR` from the shape-guard hook in favor of `BASH_SOURCE`-derived two-step fallback (install-side sibling + in-tree dev) with `pwd -P` symlink resolution; rewrote the Claude Code adapter `--hook-config` to emit absolute `bash <abs-path>/<name>.sh` commands for three leaves with `_orchestrator_managed: true` per leaf; added installer stage 2.5 staging a 5-file payload (4 scripts + MANIFEST) under `${HOME}/.claude/orchestrator-hooks/` with MANIFEST-driven uninstall; promoted settings-merge dedup key from M025's command-only to a `(event, matcher, command)` tuple via new `collect_managed_keys()` (closes the operator's M018-close 5-Stop-dupes / 7-PreToolUse-dupes regression); added installer `--repair` mode with `--repair --dry-run` and companion `settings-merge.sh repair` walking `~/.claude/settings.json` to remove flag-less M025 orphans against a hard-coded `ORPHAN_TUPLES` table; extended M025 reversibility via the four-snapshot pinned-SHA `install-roundtrip.sh` ladder (sha0 pre-install, sha1+sha2 post-install pair, sha3 post-uninstall; sha1==sha2 idempotency; sha0==sha3 reversibility extended); authored permissive no-op `scripts/lifecycle/before-commit.sh` shim closing a pre-existing [M008](../../milestones/M008/index.md) packaging gap. Patterns: BASH_SOURCE self-location with two-step fallback, shape-classifier-aware resolution-block authoring (every introduced line empirically validated against `classify_command` before commit), per-leaf tuple-keyed dedup with single-walk target-key collection, wrapper-OR-leaf managed-detection helper, strict-tuple match with extra-key guard, idempotency-by-construction verifier, helper-function carve-out for AD-19 multi-step computations (function bodies are NOT scanned by AP-009 inline-shape classifier — codified at top-of-file), reversibility-normalization at the installer layer (file-absent for logically-empty JSON), pinned-SHA-in-comment as informational drift signal, snapshot-ladder shape for byte-equality gates.

P03 (Findings B + C + G classifier — AP-010 through AP-014, 215m) extended the shape-guard surface end-to-end across documentation, classifier, hook reject_lookup, regression corpus, and per-finding verifier coverage. ANTIPATTERNS.md grew 214 → 338 lines via append-only edits adding AP-010 (cmd-sub-in-pattern), AP-011 (quoted-arg-newline-hash), AP-012 (multiline-quoted-script), AP-013 (unquoted-brace-glob), AP-014 (xargs-sh-c-compound-body). Five new private detectors landed in `scripts/verify/lib/shape-classifier.sh` as bash 3.2 + POSIX-sh-safe character-by-character quote-state machines; AP-014 inserted BEFORE the existing AP-009 `_sc_count_top_level_stages > 2` check (load-bearing for SC-6 — without that ordering SE-09 regresses to compound-chain-gt2); AP-010..AP-013 inserted AFTER the four M021 reject checks so the more-general rules continue to fire first. CON-5 one-level body-descent implemented via OPAQUE placeholder substitution. Five new case arms landed in the hook `reject_lookup`; existing four M021 baseline arms preserved byte-for-byte (CON-7). Seven verbatim entries appended to `tests/fixtures/m021-prompt-corpus.txt` (IDs 21..27: 5 AP-anchored evidence + 1 negative-control + 1 boundary case); IDs 01..20 preserved byte-for-byte. New 27-entry replay harness `tests/run-prompt-corpus-replay.sh` (M028 SC-1 spec-named gate) coexists with the M021 historical harness (both consume the same fixture, dual symmetric coverage). Five per-finding verifiers (finding-B, finding-C, finding-G-classifier, finding-G-self-conformance) plus `run-all.sh` rolling up to the canonical `M028: <pass>/7 findings verified` summary line. Patterns: append-only invariant on documentation registers, closed-pattern-set discipline, AP-014 ordering invariant in `classify_command`, OPAQUE-token one-level-deep recursion bound, char-by-char quote-state machines for shape detection, M021-strict-superset case extension discipline, self-conformance verifier scope-narrowing precedent (resolution + reject_lookup printf bodies, not dispatch), bash-parameter-expansion case-arm body extraction, two-harness symmetric coverage of one corpus, pinned-INPUT alignment between corpus and verifier, verifier-as-precedence-claim (SC-6 surfaced as discrete PASS line), header-comment-as-cross-reference-locator.

P04 (Findings D + E + G investigation wrappers, 175m) shipped four wrappers under `scripts/util/` retiring the screenshot investigation shapes: `grep-files.sh` (replaces Screenshot 1 grep;echo;grep compound), `cleanup-stale-results.sh` (replaces Screenshot 2 / Finding D `rm -f .../*.txt && ls .../*.txt` compound; uses `find -path` not `-exec sh -c`), `node-eval.sh` (refuses `-e/-p/--eval/--print` as load-bearing defense against rebuilding AP-012 inside the wrapper), `peek-files.sh` (find + while-read internal impl, no `-exec sh -c`, no `xargs sh -c` — AP-014 self-conforming). Three-surface documentation discipline (planner-facing `commands/dispatch.md` table; agent-facing `templates/dispatch-prompt.md` bullets in dispatch payloads; catalog-anchor `ANTIPATTERNS.md` cross-ref subsection) — each carries the same four wrappers framed for its audience. Three per-finding verifiers (finding-D, finding-E, finding-G-wrapper) closed the runtime-behavior contract; `run-all.sh` extended with finding-G-wrapper-verifier.sh under preserved `M028: 7/7 findings verified (skipped: 0, failed: 0)` summary contract via pass_count clamp at total (preserves the operator-grep contract). Patterns: defensive flag-refusal as wrapper contract (when a wrapper retires an antipattern, refuse the antipattern's signature flag rather than silently accepting it), self-relative-wrapper fixture pattern (verifier copies wrapper into isolated tmp `scripts/util/` so `script_dir/../..` resolves to the tmp tree), source-level self-conformance with comment-line strip (wrappers' docstrings legitimately reference the shapes they replace), optional-dependency SKIP discipline (`command -v <tool>` guards with SKIP not FAIL when the wrapper itself returns a clean exit code), summary-contract preservation under additive verifier expansion (clamp pass_count, don't bump total), three-surface documentation discipline, two-axis assertion per element in cross-cutting verifiers (existence on disk AND integration into a downstream consumer), PAYLOAD-SUMMARY pairing for post-task lint stability.

P05 (Close-Out — Downstream Fixture + Replay Harness + Regression Gate, 90m) staged the permanent in-tree consumer fixture at `tests/fixtures/downstream-project/` (CON-10 contract anchor — checked into version control, not generated at test time) mirroring the live adapter `--hook-config` emission shape with literal HOME placeholder bytes for HOME-agnostic shape comparison; authored `tests/run-downstream-fixture.sh` autonomous-loop replay harness (installer-staged isolated HOME, replays Finding A + corpus IDs 21-25, 27 + a Stop event, emits canonical `WOULD_PROMPT=0/N`); authored the M028 close-out regression gate `scripts/verify/m028/p05-regression-gate.sh` sequencing four sub-gates (install-roundtrip → corpus-replay-27-entry → per-finding-run-all → downstream-fixture-replay) with non-short-circuit semantics and per-sub-gate log capture. Phase-level `bash scripts/verify/check-must-haves.sh` reported 42/42 PASS; regression gate reported `M028 close-out: 4/4 sub-gates clean`. Patterns: live-adapter-emission-vs-fixture-bytes shape gate via `mktemp -d` isolated HOME, shape-pattern comparison via `grep -cE` regex (not literal-bytes diff), count-equality assertions for shape contracts (additive-axis discipline), installer-staged isolated HOME replay harness, JSON event payload via printf-NOT-heredoc (avoids AP-008), negative-control assertion as M021-regression catch-all, wrap-the-harness Truth-Check verifier shape, sub-gate-sequencing-with-tmp-log shape, phase-level close-out task shape (zero new deliverables, pure verification gate), subprocess success-message hygiene.

Verification: validate-milestone reports 81/81 checks PASS across 5 phases; phase-level check-must-haves on each phase reports 22/22 (P01), 54/54 (P02), 68/68 (P03), 80/80 (P04), 42/42 (P05) all PASS; `bash scripts/verify/m028/run-all.sh` reports `M028: 7/7 findings verified (skipped: 0, failed: 0)` across 8 per-finding verifiers; `bash scripts/verify/m028/p05-regression-gate.sh` reports `M028 close-out: 4/4 sub-gates clean`; M025 reversibility extended via install-roundtrip pinned-SHA gate.

Cross-cutting outcomes:
- **Hook portability** (Finding A) closed end-to-end: hook self-locates via `BASH_SOURCE`; adapter emits absolute paths; installer stages payload to runtime-stable `~/.claude/orchestrator-hooks/`; works in any consumer project with no `CLAUDE_PROJECT_DIR` dependency.
- **Lifecycle-script PATH lookup** (Finding F) closed: bare-name `orchestrator-post-verify` / `orchestrator-before-commit` retired in favor of absolute `bash ~/.claude/orchestrator-hooks/<name>.sh`; pre-existing M008 `before-commit.sh` packaging gap closed via permissive shim.
- **Five new shape classes** (AP-010..AP-014) live in classifier + hook + corpus + per-finding verifier; CON-7 strict-superset preserved (every M021 baseline verdict on every M021 corpus entry unchanged); AP-014 ordered before AP-009 so SE-09 receives the more-specific verdict.
- **Four investigation-pattern wrappers** under `scripts/util/` retire the screenshot investigation shapes; defensive flag-refusal prevents callers from rebuilding antipatterns inside the wrappers; three-surface documentation makes them discoverable to planners + dispatched agents + catalog readers.
- **Install-side dedup** via `(event, matcher, command)` tuple closes the M018-close 5-Stop-dupes / 7-PreToolUse-dupes regression; install-roundtrip pinned-SHA gate locks idempotency + reversibility under one isolated-HOME exercise.
- **Permanent in-tree consumer fixture** at `tests/fixtures/downstream-project/` is the CON-10 contract anchor for adapter emission shape — drift caught at CI time, not discovered downstream.

Reusable patterns (carried across phases): **helper-function carve-out for AD-19** (function bodies are NOT scanned by the AP-009 inline-shape classifier — load-bearing for verifier authoring; codified as top-of-file comment block convention starting M028/P02/T05); **plan-time classifier-shape pre-validation** (every shape-bearing line authored is empirically run through `classify_command` before commit — closes the planner-vs-classifier confabulation gap surfaced repeatedly across phases); **per-task verifier co-located with deliverable** (CLAUDE.md hotfix; cross-task verifier dependency rejected; auto-loop `--step=V` mechanical resolver always succeeds at task time); **direct invocation of project-tree verifiers** in `## Verification` sections (no `run-probe.sh` wrapping; surfaced as a recurring CLAUDE.md hotfix); **pre-existing-task-drift fold-in at final-task close** (small documentation-layer must-have lookup misses across earlier tasks fold into the phase's final task rather than re-dispatching completed tasks).

Dogfood findings surfaced and captured in CLAUDE.md hotfix list during M028 execution: auto-loop `--step=V` evaluating `Expected output:` example fences as commands (parser-side defensive verdict-prefix-skip + plan-author guidance, fixed in commit `73effdc`); `run-probe.sh` is not a generic invocation harness (rejects paths outside `/tmp`/`/var/folders`/`<repo>/tmp/` with exit 3 — every P02 task plan needed `## Verification` patched from `bash run-probe.sh scripts/verify/...` to `bash scripts/verify/...` directly); CLAUDE.md commit-message-via-HEREDOC guidance is in tension with the active shape-guard hook (AP-008 rejects inline `$(cat <<EOF...)`; safe form is `git commit -F <file>`); planner template emits `scripts/verify/...` for project-owned phase verifiers, colliding with framework-staged `scripts/`; AD-19 helper-function carve-out is undocumented (load-bearing across M028 verifiers); plan-time prerequisite-existence verification gap (T03 had to author `before-commit.sh` shim mid-task); plan-time classifier-shape pre-validation discipline missing; plan-time verifier-availability cross-check missing (M028/P03/T01-T04 all referenced T05-deliverable verifiers in `## Verification`); `settings-merge.sh uninstall` arm uses leaf-only managed detection (T04's repair fixes wrapper-OR-leaf for repair path; uninstall does not — symmetric ~10-line patch); stale `m025-p01-merge-preservation.sh` and `m013-p04-post-verify-hook.sh` (out-of-scope for M028 P02; bundle into next M025-touching paper-cut sweep). All captured in CLAUDE.md hotfix queue for the next paper-cut sweep PR.

Roadmap state: M028 closes the load-bearing pre-launch slice of the M028 → [M030](../../milestones/M030/index.md) → [M031](../../milestones/M031/index.md) → [M032](../../milestones/M032/index.md) → [M033](../../milestones/M033/index.md) → [M029](../../milestones/M029/index.md) → [M035](../../milestones/M035/index.md) → launch sequence. Next up: M030 (adaptive model selection). M025 reversibility extended; CON-7 strict-superset preserved; CON-10 noisy-fail discipline extended to in-tree consumer fixture.
