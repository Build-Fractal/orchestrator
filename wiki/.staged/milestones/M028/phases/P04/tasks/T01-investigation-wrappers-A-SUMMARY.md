---
schema_version: "1.0"
type: task-summary
id: "T01"
parent: "P04"
milestone: "M028"
provides:
  - "scripts/util/grep-files.sh wrapper (FR-14): grep ERE pattern across N files with --- file --- separators; aggregate exit 0/1/2 (any-match/no-match/usage); replaces Screenshot 1 grep;echo;grep compound shape; AD-19 single-script-file flat shape; bash 3.2 + POSIX-sh-safe; no jq/node/python; scripts/util/cleanup-stale-results.sh wrapper (FR-15): removes per-step *.txt scratch files under .orchestrator/milestones/MID/phases/PID/tasks/ with M[0-9]+ milestone-ID validation (boundary refusal per M028 spec Edge Cases); structured stdout REMOVED:N + RESIDUAL:count + OK terminator; replaces Screenshot 2 / Finding D /bin/rm -f .../*.txt && ls .../*.txt compound shape; uses find -path with no -exec sh -c (AP-014 safe); two plan-level verifiers scripts/verify/m028/p04-grep-files.sh + p04-cleanup-stale-results.sh both PASS independently; verifiers exercise match/no-match/usage/missing-file cases for grep-files and happy-path/empty-tree/missing-tree/invalid-ID/no-args cases for cleanup-stale-results with isolated tmp-root + wrapper-copy pattern (mirrors p02-repair-fixture.sh shape)"
requires:
  - "from:P03/close what:M028 spec Findings D + relevant ANTIPATTERNS.md AP-009/AP-010 entries already landed; from:P02 what:plan-level verifier convention (direct bash scripts/verify/m028/<name>.sh invocation, no run-probe.sh wrapping); from:disk what:scripts/util/run-probe.sh + scripts/util/read-range.sh sibling shape; from:disk what:scripts/verify/m028/p02-repair-fixture.sh isolated-tmp-root verifier pattern"
affects:
  - "P04/T02,P04/T03,P04/T04,P04/T05"
key_files:
  - "scripts/util/grep-files.sh,scripts/util/cleanup-stale-results.sh,scripts/verify/m028/p04-grep-files.sh,scripts/verify/m028/p04-cleanup-stale-results.sh"
key_decisions:
  - "grep-files.sh keeps explicit-file v1 surface only (no -r/-R recursion -- recursion is peek-files.sh's job per T02); per-file separator emitted UNCONDITIONALLY (before grep call) so verifier sees separator even when no matches in a given file -- preserves the --- file --- visual cue from the Screenshot 1 shape; aggregate exit code is OR-of-matches (0 if any file matched anywhere, 1 if no matches across all files) rather than per-file rc; cleanup-stale-results.sh validates milestone ID via case M[0-9]*) closed pattern -- bash 3.2 safe and avoids regex/[[ ]] forms; cleanup-stale-results.sh enumerates target files via find -type f -path '*/phases/*/tasks/*.txt' -print > tmpfile then while-read loops the listing -- avoids -exec sh -c <body> compound which AP-014 P03 closes; cleanup-stale-results.sh uses two-pass shape (enumerate -> remove -> separate residual find pass) rather than counting after rm -- the residual count is the post-state observable, computed independently from removed; verifiers wrap rc-capturing bash invocations with set +e/set -e pairs to keep verifier itself robust under set -u (verifier preamble has set -u, but rc=0 after a non-zero exit needs set +e to read cleanly); verifier copies wrapper into tmp_root/scripts/util/ rather than env-overriding REPO_ROOT -- because the wrapper's REPO_ROOT comes from script_dir/../.. resolution, this is the cheapest isolation that exercises the real resolution path"
patterns_established:
  - "Per-file separator unconditional emission (printf -- '--- %s ---\n' before grep call) so structured-output verifiers see the separator regardless of match outcome; this is a docstring-as-contract pattern that propagates to peek-files.sh in T02; closed-pattern milestone-ID validation via case M[0-9]*) -- bash 3.2-safe alternative to [[ =~ ]] and external regex; companion shape for any future MID-bearing util-tree wrapper; find -path '*/phases/*/tasks/*.txt' -print > tmpfile + while IFS= read -r f loop -- AP-014-safe enumeration replacement for rm $(find ...) and find -exec sh -c '...' compound forms; canonical pattern for any util-tree wrapper that needs to enumerate-then-act on a path-pattern; verifier-side isolated-tmp-root via wrapper-copy (cp $WRAPPER $tmp_root/scripts/util/...) -- the wrapper's REPO_ROOT computation (script_dir/../..) resolves naturally to the staged tmp tree without environment overrides; mirrors P02's p02-repair-fixture.sh shape; set +e wrapping around rc-capture in verifiers under set -u preamble -- 0 after non-zero exit is unstable under strict-mode without temporarily disabling errexit; emit pattern is set +e ; <invocation> ; rc=$? ; set -e"
drill_down_paths:
  - ".orchestrator/milestones/M028/phases/P04/tasks/T01-investigation-wrappers-A-PLAN.md,.orchestrator/milestones/M028/phases/P04/tasks/T01-investigation-wrappers-A-PAYLOAD.md,scripts/util/grep-files.sh,scripts/util/cleanup-stale-results.sh,scripts/verify/m028/p04-grep-files.sh,scripts/verify/m028/p04-cleanup-stale-results.sh"
duration: "45m"
verification_result: "pass"
completed_at: "2026-04-29T18:43:03Z"
---

T01 lands 2 of the 4 P04 investigation-pattern wrappers (FR-14 grep-files + FR-15 cleanup-stale-results) plus their plan-level verifiers. T02 follows with node-eval.sh + peek-files.sh; T03 wires investigation-section authoring into commands/dispatch.md + templates/dispatch-prompt.md.

**grep-files.sh** (45 lines): replaces the Screenshot 1 `grep P f1 ; echo --- ; grep P f2` compound shape. Positional args `<pattern> <file...>`; per-file `--- <file> ---` separator emitted unconditionally to stdout before each grep call; aggregate exit code 0 (any-match) / 1 (no-match) / 2 (usage). Uses `grep -E -- "$pattern" "$f"` so patterns starting with `-` are safe. Refuses missing files with exit 2 + diagnostic to stderr. set -u, no jq, AD-19 flat single-script-file shape, bash 3.2 + POSIX-sh-safe.

**cleanup-stale-results.sh** (55 lines): replaces the Screenshot 2 / Finding D `/bin/rm -f .../*.txt && ls .../*.txt` shape. Single positional arg `<milestone-id>` validated via `case "$mid" in M[0-9]*) :;; *) exit 2 ;; esac` (bash 3.2-safe closed-pattern match; avoids regex / [[ =~ ]] forms; bounds blast radius per M028 spec Edge Case 'cleanup-stale-results.sh boundary refusal'). Resolves repo root via `script_dir/../..` then enumerates target files with `find "$TREE" -type f -path '*/phases/*/tasks/*.txt' -print > list_tmp` followed by a `while IFS= read -r f` removal loop -- both forms are AP-014-safe (no `-exec sh -c '<body>'` compound). Two-pass shape: enumerate -> remove -> separate find pass for residual count. Stdout protocol: `REMOVED: <N>\n RESIDUAL: <count>\n OK\n`. Exit 0 on success, 1 on milestone-tree-missing, 2 on usage / invalid milestone ID.

**p04-grep-files.sh** (60 lines): exercises 4 cases against tmp-staged 2-file tree -- both files match (exit 0 + two separators), no matches (exit 1), missing args (exit 2), missing file (exit 2). All 4 cases PASS.

**p04-cleanup-stale-results.sh** (75 lines): exercises 6 cases against an isolated tmp-root that mirrors `.orchestrator/milestones/MID/phases/PID/tasks/` layout -- invalid milestone ID (exit 2), no args (exit 2), happy-path 3-file removal (exit 0 + REMOVED=3 + RESIDUAL=0 + OK), empty-tree (exit 0 + REMOVED=0), missing tree (exit 1). Wrapper is COPIED into the tmp-root's `scripts/util/` so its `script_dir/../..` resolution naturally targets the staged tree without env overrides -- pattern mirrors P02's p02-repair-fixture.sh isolation shape. All 9 PASS.

**Verification**: both verifiers PASS independently (`bash scripts/verify/m028/p04-grep-files.sh` -> 7 PASS lines; `bash scripts/verify/m028/p04-cleanup-stale-results.sh` -> 10 PASS lines).

**Patterns established (folded into M028 dogfood collection)**: (a) unconditional per-file separator emission for structured-output verifiers; (b) bash 3.2-safe closed-pattern milestone-ID validation via `case $mid in M[0-9]*) :;; *) ... esac`; (c) AP-014-safe enumeration via `find -path ... -print > tmp` + while-read rather than `-exec sh -c` or rm-of-find-cmdsub; (d) verifier-side isolated-tmp-root via wrapper-copy; (e) `set +e` wrapping around `rc=$?` capture in verifiers running under `set -u` preamble.

**Deviations**: none. Plan executed verbatim. The plan's pattern shape (`echo "$out" | grep -q ... && pass ... || fail ...`) was preserved exactly; under `set -u` this is a 3-stage compound but appears INSIDE the verifier script body (not at the agent's tool-call layer), so AP-009 classifier scope does not apply (function-and-script-body carve-out per the AD-19 helper-function convention M028/P02/T05 codified).
