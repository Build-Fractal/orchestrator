---
schema_version: "1.0"
type: phase-summary
id: "P05"
parent: "M028"
milestone: "M028"
provides:
  - "tests/fixtures/downstream-project/ permanent in-tree consumer-project fixture (CON-10 -- checked into version control NOT generated at test time) with .claude/settings.json mirroring scripts/dispatch/adapters/runtime/claude-code.sh::--hook-config emission shape (Stop hook = after-verify-sync.sh; PreToolUse Bash hooks = pre-bash-shape-guard.sh + before-commit.sh; every leaf carries _orchestrator_managed: true M025 invariant; fixture uses literal HOME placeholder bytes for HOME-agnostic shape comparison) and README.md (~30 lines) documenting purpose + noisy-fail discipline + cross-references; scripts/verify/m028/p05-fixture-permanent.sh (~50 lines AD-19 single-script-file flat shape bash 3.2 + POSIX-sh-safe no jq -- existence-only Truth-Check on directory + .claude/settings.json >=8 lines + README.md >=6 lines containing downstream-project token; 5 PASS rows + verdict line); scripts/verify/m028/p05-downstream-fixture-shape.sh (~115 lines AD-19 single-script-file flat shape bash 3.2 + POSIX-sh-safe no jq -- CON-10 noisy-fail shape gate: captures adapter live --hook-config emission with isolated HOME tmp dir then asserts every command field matches the shape pattern command-bash-anything-dot-sh in both fixture AND adapter emission,_orchestrator_managed flag count matches between fixture and adapter (currently 3:3),command line count matches (3:3),three runtime-stable hook basenames (after-verify-sync.sh,pre-bash-shape-guard.sh,before-commit.sh) appear in both; 9 PASS rows + verdict line; helper-function carve-out applied to count_command_lines / count_managed_flags / all_commands_have_shape extraction helpers per M028/P02/T05 codification),tests/run-downstream-fixture.sh (~190 lines AD-19 single-script-file flat shape bash 3.2 + POSIX-sh-safe no jq -- M028 autonomous-loop replay harness; stages installer payload into isolated HOME=tmp/m028-p05-replay-PID via packaging/install/install-claude-code.sh --project-dir; routes 9 synthetic Bash hook events through staged pre-bash-shape-guard.sh and 1 Stop event through staged after-verify-sync.sh; CLAUDE_PROJECT_DIR=tmp/fake-project mirrors Finding A non-orchestrator-repo wild context; emits canonical WOULD_PROMPT=0/9 summary line + PASS verdict on clean run; helper-function carve-out applied to invoke_hook + extract_corpus_input + pass + fail per M028/P02/T05 codification; printf-based JSON authoring avoids AP-008 heredoc-with-expansion shape; real-LF -> JSON backslash-n re-encoding for ID 22 round-trip mirrors tests/run-prompt-corpus-replay.sh:101-107),scripts/verify/m028/p05-downstream-fixture-clean.sh (~62 lines AD-19 single-script-file flat shape bash 3.2 + POSIX-sh-safe no jq -- cross-cutting Truth-Check verifier wrapping the harness; asserts harness exit 0 + canonical WOULD_PROMPT=0/N summary line via grep -E + no command not found substring + at least one PASS line; 4 PASS rows + final verdict line + exit 0 on clean run),scripts/verify/m028/p05-corpus-replay-clean.sh (~50 lines,AD-19 single-script-file flat shape,bash 3.2 + POSIX-sh-safe,no jq) wrapping tests/run-prompt-corpus-replay.sh and asserting exit 0 + WOULD_PROMPT=0/27 literal substring + no ^FAIL: lines; scripts/verify/m028/p05-run-all-clean.sh (~55 lines,P05-scoped sibling of pre-existing p04-run-all-clean.sh) wrapping scripts/verify/m028/run-all.sh and asserting exit 0 + 'M028: 7/7 findings verified' substring + 'skipped: 0' substring + no ^FAIL: lines; scripts/verify/m028/p05-regression-gate.sh (~80 lines,M028 close-out regression gate,the consolidated CI-runnable artifact) sequencing four sub-gates in stable dependency-aligned order (install-roundtrip -> corpus-replay -> per-finding-run-all -> downstream-fixture-replay) via shared run_sub_gate helper-function (AD-19 carve-out documented at top-of-file),capturing per-sub-gate output to ${TMPDIR:-/tmp}/m028-p05-regression-gate-$$/<label>.log,reporting per-sub-gate PASS/FAIL with rc + one-line tail,emitting consolidated 'M028 close-out: <pass>/4 sub-gates clean' final line; non-short-circuit semantics (all four sub-gates run even if early ones fail; operator-visibility over fail-fast); exits 0 on all-pass with tmp dir cleanup,exits 1 on any sub-gate fail with logs preserved.,No new artifacts authored. T04 is the phase-level close-out sweep — runs the full verification surface against the M028/P05 phase plan and produces empirical close-out evidence: (1) all six P05 Truth-Check leaves invoked individually emit final PASS lines and exit 0 (p05-fixture-permanent.sh,p05-downstream-fixture-shape.sh,p05-downstream-fixture-clean.sh,p05-corpus-replay-clean.sh,p05-run-all-clean.sh,p05-regression-gate.sh); (2) p05-regression-gate.sh end-to-end reports 'M028 close-out: 4/4 sub-gates clean' with all four sub-gates PASS (install-roundtrip rc=0 idempotency=98351856f438341650c0e6914bb41fa06612d2d766b4983618544a704f428f45 reversibility=EMPTY; corpus-replay-27-entry rc=0 tail=PASS:tests/run-prompt-corpus-replay.sh; per-finding-run-all rc=0 tail='M028: 7/7 findings verified (skipped: 0,failed: 0)'; downstream-fixture-replay rc=0 tail=PASS:tests/run-downstream-fixture.sh); (3) check-must-haves.sh against .orchestrator/milestones/M028/phases/P05 emits 42/42 PASS lines (6 truths + 27 artifact sub-assertions across 9 artifacts × 3 checks each + 9 key-links) and exits 0 — every Truth,every Artifact (existence + min-line-count + contains-pattern),every Key Link (source-references-target basename) clean; (4) run-all.sh reports 'M028: 7/7 findings verified (skipped: 0,failed: 0)' across the 8 per-finding verifiers (finding-A through finding-G plus G-classifier and G-wrapper)."
requires:
  - "P03,P04"
affects:
  - "none"
key_files:
  - "tests/fixtures/downstream-project/.claude/settings.json,tests/fixtures/downstream-project/README.md,scripts/verify/m028/p05-fixture-permanent.sh,scripts/verify/m028/p05-downstream-fixture-shape.sh,tests/run-downstream-fixture.sh,scripts/verify/m028/p05-downstream-fixture-clean.sh,scripts/verify/m028/p05-corpus-replay-clean.sh,scripts/verify/m028/p05-run-all-clean.sh,scripts/verify/m028/p05-regression-gate.sh,scripts/verify/check-must-haves.sh,scripts/verify/m028/run-all.sh"
key_decisions:
  - "Use literal HOME placeholder bytes in fixture rather than expanded HOME path -- adapter live emission contains the runtime HOME path which would tie the fixture to one execution; placeholder form keeps fixture HOME-agnostic and the shape verifier compares the regex pattern bash anything dot sh rather than literal expansion; this is the intended contract surface (shape NOT identity).,Co-author both verifiers with the deliverable in T01 -- per CLAUDE.md plan-time verifier-availability cross-check discipline; auto-loop --step=V resolves at T01 time without cross-task verifier dependency on T02-T05 deliverables; mirrors P02/P03/P04 pattern.,Helper-function carve-out applied to extraction helpers (count_command_lines / count_managed_flags / all_commands_have_shape) in p05-downstream-fixture-shape.sh -- function bodies are NOT classifier-scanned per M028/P02/T05 codification; documented in top-of-file comment block so future readers see the contract at read time without spelunking history.,Direct invocation of project-tree verifiers in plan Verification section (bash scripts/verify/m028/<name>.sh) NOT wrapped in run-probe.sh per the M028/P02 dogfood finding -- run-probe.sh is reserved for staged throwaway probes inside /tmp /var/folders or <repo>/tmp/; both T01 verifiers are project-tree scripts and invoke directly.,Files-To-Touch in payload listed nine files but Steps + Verification + Must-Haves only require four T01 deliverables (fixture settings.json + README.md + two verifiers) -- the other five files (tests/run-downstream-fixture.sh,p05-downstream-fixture-clean.sh,p05-regression-gate.sh,p05-run-all-clean.sh,p05-corpus-replay-clean.sh) are T02/T03/T04 deliverables; honored Steps + Verification + Must-Haves as the contract for THIS task per dispatch-orchestrator scope discipline.,Plan-time classifier-shape pre-validation -- each Verification line is a single-stage bash path.sh invocation; M028 classifier returns allow (no nested cmdsub,no compound chain,no AP-014 body,no quoted-arg-newline-hash); both verifiers exist on disk by task close so the auto-loop --step=V mechanical resolver succeeds.,Slug-suffixed summary filename T01-downstream-fixture-SUMMARY.md per CLAUDE.md M028 hotfix on slug-suffix asymmetry -- write-summary.sh accepts the literal output path so the slug-bearing form aligns with the bilateral-tolerance contract on the read side.,Use canonical awk grammar from tests/run-prompt-corpus-replay.sh:68-69 (^ID: substr 5 + ^INPUT: substr 8) NOT the plan-text grammar (^# ID:) -- corpus IDs are unprefixed and the plan-text awk pattern would silently extract empty strings; verified empirically against the on-disk corpus before authoring,Re-encode real LFs to JSON backslash-n in the JSON event payload (mirrors tests/run-prompt-corpus-replay.sh:105) NOT raw printf-decoded injection -- ID 22 carries a real LF after printf %b decoding and would invalidate the JSON if injected raw; canonical harness pattern preserves round-trip,Replace plan-text Stop event PASS message that contained literal command not found substring -- the Truth-Check verifier greps for absence of that substring in subprocess output and the literal phrase inside a PASS message false-fails the assertion; PASS message rewritten to clean stderr; threading paper-cut into next M028 sweep,Direct invocation of project-tree verifiers in plan Verification section (bash scripts/verify/m028/p05-downstream-fixture-clean.sh + bash tests/run-downstream-fixture.sh) NOT wrapped in run-probe.sh per CLAUDE.md M028/P02 dogfood finding,Co-author both deliverables in T02 -- harness + clean verifier both exist on disk by task close so auto-loop --step=V mechanical resolver succeeds; no cross-task verifier dependency on T03/T04,Helper-function carve-out (pass,fail,invoke_hook,extract_corpus_input) per M028/P02/T05 codification documented in top-of-file comment block,Slug-suffixed summary filename T02-replay-harness-SUMMARY.md per CLAUDE.md M028 hotfix on slug-suffix asymmetry,printf-based JSON event payload authoring avoids AP-008 heredoc-with-expansion shape,Files-To-Touch payload field listed nine files; Steps + Verification + Must-Haves only require two T02 deliverables -- honored Steps + Verification + Must-Haves as the contract for THIS task per dispatch-orchestrator scope discipline,Plan-time classifier-shape pre-validation -- both Verification lines are single-stage bash path.sh invocations classifying as allow under M028,Two-layer architecture: regression gate (consolidated CI artifact) is distinct from per-sub-gate clean verifiers (granular Truth-Check leaves consumed by check-must-haves.sh); both layers consume the same underlying harnesses but serve different consumers; non-redundant by design. Non-short-circuit sub-gate sequencing (all 4 run regardless of earlier failures) trades fail-fast for full-coverage close-out evidence -- operator inspecting the close-out artifact wants to see all four states,not stop on the first FAIL. P05 sibling p05-run-all-clean.sh coexists with pre-existing P04/T05 p04-run-all-clean.sh by phase-scoped naming convention; both verifiers serve their respective phase plan's Truth-Check binding.,No new commits authored within T04 (per plan CON-7 / 'No new commits in this task' constraint — orchestrator handles the M028/P05 phase-boundary commit after the SUMMARY lands). T04 records empirical PASS counts (42/42 check-must-haves assertions; 4/4 close-out sub-gates; 7/7 per-finding verifiers; 6/6 P05 Truth-Check leaves) so the phase summary's verification_result field has citable evidence. No drift / dogfood findings surfaced during the sweep — T01-T03 deliverables passed cleanly on first invocation; CLAUDE.md hotfix queue not extended by this task."
patterns_established:
  - "Permanent in-tree consumer fixture under tests/fixtures/downstream-project/ as CON-10 anchor for noisy-fail shape gates -- contract reference fixture for the adapter --hook-config emission shape lives under version control; drift caught at CI time NOT discovered downstream; fixture authored to mirror the live emission so first-run verifier exit is clean.,Live-adapter-emission-vs-fixture-bytes shape gate via mktemp -d isolated HOME -- verifier captures HOME equals tmp/fake-home bash adapter --hook-config redirected to tmp/adapter-emission.json then runs shape assertions against both fixture and capture; isolated HOME prevents the verifier from polluting the developer real ~/.claude/ tree and keeps the verifier reentrant and CI-safe; trap rm -rf tmp EXIT cleanup mirrors the P02/P03/P04 verifier convention.,Shape-pattern comparison via grep -cE on a regex matching command-bash-anything-dot-sh rather than literal-bytes diff -- fixture uses literal HOME placeholder while adapter emits expanded HOME path; comparing the shape via regex captures the contract intent (every command is a bash invocation of a .sh file under a runtime-stable hooks directory) without tying the fixture to any one HOME at any one execution.,Count-equality assertions for shape contracts (managed-flag count + command-line count between fixture and adapter emission) -- additive-axis discipline: when the adapter adds a new hook event the fixture count drifts from adapter count and the verifier fails loudly; same shape catches fixture-side drift (fixture adds an unauthorized hook the adapter does not emit); two-way drift detection with a single equality assertion.,Helper-function carve-out as canonical extraction-helper shape -- p05-downstream-fixture-shape.sh defines count_command_lines / count_managed_flags / all_commands_have_shape inside bash function bodies; top-of-file comment block documents the carve-out is load-bearing for AD-19 compliance under the M021/M028 inline-shape classifier; reusable shape for any future verifier needing structure-bearing extraction over JSON or other text fixtures without sourcing jq.,Three runtime-stable hook basenames (after-verify-sync.sh,pre-bash-shape-guard.sh,before-commit.sh) as the M028 contract floor for adapter --hook-config emission -- any future hook addition must update the fixture and the verifier loop simultaneously to keep the count-equality assertions clean; the basename loop in p05-downstream-fixture-shape.sh is the codified extension point.,Slug-suffixed task summary filename pattern (T01-slug-SUMMARY.md not T01-SUMMARY.md) emitted by write-summary.sh task literal output path -- aligns with CLAUDE.md M028 hotfix on slug-suffix asymmetry; readers using parameter-expansion task-id-strip match either form; bilateral-tolerance contract on the read side keeps both shapes valid at lookup time.,Installer-staged isolated HOME replay harness pattern -- harness invokes packaging/install/install-claude-code.sh --project-dir tmp_home with HOME=tmp_home CLAUDECODE=1 then routes synthetic Bash hook events through the staged pre-bash-shape-guard.sh; CLAUDE_PROJECT_DIR=tmp/fake-project models the wild non-orchestrator-repo context that surfaced Finding A; trap rm -rf EXIT cleanup keeps the harness reentrant and CI-safe; reusable shape for any future end-to-end hook-event replay test under M028 successor milestones,Corpus-driven hook-event replay via runtime awk extraction -- extract_corpus_input <id> helper extracts verbatim INPUT bytes from tests/fixtures/m021-prompt-corpus.txt at runtime via the canonical awk grammar; harness fails noisily if corpus IDs drift (CON-10 noisy-fail discipline applied to corpus references not just fixture references); printf %b decode + JSON-side LF re-encoding preserves multi-line entry round-trip through the JSON event payload,JSON event payload via printf-NOT-heredoc shape -- printf JSON-template with safe-escaped command bytes avoids AP-008 heredoc-with-expansion classifier reject; reusable shape for any future verifier authoring synthetic JSON tool-call events; safe escaping covers backslash + double-quote + LF -> JSON backslash-n in three parameter expansions,Negative-control assertion as M021-regression catch-all -- harness includes a benign single-stage echo hello assertion that classifies allow under both M021 and M028 baseline; if the classifier becomes over-broad (CON-7 strict-superset violation) the negative-control fails by construction; reusable shape for any future shape-guard regression harness,Truth-Check verifier wrapping a harness with grep-based output-shape assertions -- p05-downstream-fixture-clean.sh captures harness stdout+stderr to mktemp and asserts exit 0 + canonical summary regex + absence of error substring + presence of success substring; reusable shape for any future close-out gate that wraps a longer-running harness with cheap shape-only assertions,Subprocess success-message hygiene -- when a wrapping verifier asserts the absence of a literal substring in subprocess output the subprocess MUST NOT include that substring in its own success messages; rewrite Stop event PASS message from no command not found to clean stderr; pattern applies to any future harness-plus-wrapper-verifier pair,Slug-suffixed task summary filename pattern (T02-slug-SUMMARY.md not T02-SUMMARY.md) emitted by write-summary.sh task literal output path per CLAUDE.md M028 hotfix,Wrap-the-harness Truth-Check verifier shape: thin (~50 line) bash 3.2 + POSIX-sh-safe verifier sources its target harness exactly once,captures stdout+stderr to mktemp,asserts (a) exit 0,(b) presence of canonical summary substring,(c) absence of ^FAIL: lines,(d) optional auxiliary substrings (e.g. skipped: 0); local pass()/fail() helpers with scalar fail_count integer accounting (no arrays per CON-2); trap 'rm -f' EXIT for tmp cleanup; emits final PASS:/FAIL: <verifier-name> line. Established by T02's p05-downstream-fixture-clean.sh; T03 mirrors the shape across two new verifiers. Sub-gate-sequencing-with-tmp-log shape: regression gate uses run_sub_gate helper (AD-19 carve-out for body shape) with per-label log files under ${TMPDIR:-/tmp}/m028-p05-regression-gate-$$/,captures rc + tail-line for human-readable per-sub-gate report,preserves logs on any FAIL for post-hoc inspection,cleans up tmp dir on all-PASS.,Phase-level close-out task shape (mirrors M028/P04/T05): zero new deliverables,pure verification gate,runs (a) every Truth-Check leaf individually,(b) the consolidated regression gate end-to-end,(c) phase-level check-must-haves.sh,(d) the per-finding aggregator. Records empirical PASS counts in the task summary so phase-transition has citable evidence. Verification section invokes project-tree verifiers directly via 'bash <path>' single-stage invocations — no run-probe.sh wrapping (run-probe.sh is reserved for staged throwaway probes inside /tmp / /var/folders / <repo>/tmp/). All three Verification lines classify as 'allow' under the M028 shape classifier."
drill_down_paths:
  - ".orchestrator/milestones/M028/phases/P05/tasks/T01-downstream-fixture-SUMMARY.md, .orchestrator/milestones/M028/phases/P05/tasks/T02-replay-harness-SUMMARY.md, .orchestrator/milestones/M028/phases/P05/tasks/T03-regression-gate-SUMMARY.md, .orchestrator/milestones/M028/phases/P05/tasks/T04-close-out-sweep-SUMMARY.md"
duration: "90m"
verification_result: "pass"
completed_at: "2026-04-29T19:44:12Z"
observability_surfaces:
  - "none"
---

P05 (close-out + downstream fixture + regression gate) lands the M028 close-out axis. Four tasks executed: T01 staged the permanent in-tree consumer fixture at `tests/fixtures/downstream-project/` (CON-10 contract anchor — checked into version control, not generated at test time) plus existence + adapter-shape verifiers; T02 authored the autonomous-loop replay harness `tests/run-downstream-fixture.sh` (installer-staged isolated HOME, replays Finding A + corpus IDs 21-25, 27 + a Stop event, emits canonical `WOULD_PROMPT=0/N`) plus the wrap-the-harness clean verifier; T03 authored the close-out regression gate sequencing four sub-gates (install-roundtrip → corpus-replay-27-entry → per-finding-run-all → downstream-fixture-replay) plus thin sub-gate clean verifiers (`p05-corpus-replay-clean.sh`, `p05-run-all-clean.sh`); T04 was the close-out sweep — zero new deliverables, pure verification gate confirming all six P05 Truth-Check leaves PASS individually, the regression gate emits `M028 close-out: 4/4 sub-gates clean`, `check-must-haves.sh` reports 42/42 PASS, and `run-all.sh` confirms `M028: 7/7 findings verified (skipped: 0, failed: 0)`.

Verification results: all four task verifiers PASS on first invocation; phase-level `bash scripts/verify/check-must-haves.sh .orchestrator/milestones/M028/phases/P05` reports 42/42 PASS (6 truths + 27 artifact sub-assertions across 9 artifacts × 3 checks each + 9 key-links); `bash scripts/verify/m028/p05-regression-gate.sh` reports 4/4 sub-gates clean with rc=0 and canonical PASS tails on every sub-gate; `bash scripts/verify/m028/run-all.sh` reports 7/7 findings verified across 8 per-finding verifiers (finding-A through finding-G plus G-classifier and G-wrapper).

Patterns established for downstream / future close-out work: (1) live-adapter-emission-vs-fixture-bytes shape gate via mktemp -d isolated HOME (verifier captures `HOME=tmp/fake-home bash adapter --hook-config > tmp/adapter-emission.json` then runs shape assertions against both fixture and capture); (2) shape-pattern comparison via grep -cE on a regex matching `bash <anything>.sh` rather than literal-bytes diff (fixture uses literal HOME placeholder while adapter emits expanded HOME path; comparing shape via regex captures contract intent without tying fixture to one HOME at one execution); (3) count-equality assertions for shape contracts (managed-flag count + command-line count between fixture and adapter — additive-axis discipline catches drift in either direction); (4) installer-staged isolated HOME replay harness (harness invokes the installer with HOME=tmp_home then routes synthetic Bash hook events through the staged shape-guard hook; reusable across future end-to-end hook-event replay tests); (5) JSON event payload via printf-NOT-heredoc shape (printf JSON-template with safe-escaped command bytes avoids AP-008 heredoc-with-expansion classifier reject); (6) negative-control assertion as M021-regression catch-all (harness includes a benign single-stage `echo hello` assertion that classifies allow under both [M021](../../../../milestones/M021/index.md) and M028 baseline; over-broad classifier fails the negative control by construction); (7) wrap-the-harness Truth-Check verifier shape (~50 line bash verifier captures stdout+stderr to mktemp, asserts exit 0 + canonical summary substring + absence of `^FAIL:` lines + optional auxiliary substrings); (8) sub-gate-sequencing-with-tmp-log shape (regression gate uses `run_sub_gate` helper with per-label log files under `${TMPDIR:-/tmp}/m028-p05-regression-gate-$$/`, captures rc + tail-line for human-readable per-sub-gate report, preserves logs on FAIL, cleans up on all-PASS); (9) phase-level close-out task shape (zero new deliverables, pure verification gate, runs every Truth-Check leaf individually + the consolidated regression gate + phase-level check-must-haves + the per-finding aggregator); (10) subprocess success-message hygiene (when a wrapping verifier asserts absence of a literal substring in subprocess output, the subprocess MUST NOT emit that substring in its own success messages — surfaced by P05/T02 dogfooding when a Stop event PASS message contained `command not found` and false-failed the wrapping verifier).

Roadmap reassessment: P05 is the final phase of M028; downstream affects=none. No DECISIONS register entries required. The two-layer architecture (regression gate as consolidated CI artifact distinct from per-sub-gate clean verifiers as granular Truth-Check leaves consumed by check-must-haves.sh) is non-redundant by design — both layers consume the same underlying harnesses but serve different consumers. P05 sibling `p05-run-all-clean.sh` coexists with pre-existing P04/T05 `p04-run-all-clean.sh` by phase-scoped naming convention.

External-modification audit: phase-transition.sh flagged the four P04/T04 deliverables (T04-per-finding-verifiers-SUMMARY.md, finding-D/E/G-verifier.sh) — these were committed mid-task during P04/T04 as commit a823f06 on main; the warning is expected and the modifications are recorded.

M028 milestone is ready for `orchestrator:consolidate`.
