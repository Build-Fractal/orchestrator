---
schema_version: "1.0"
type: task-summary
id: "T01"
parent: "P05"
milestone: "M028"
provides:
  - "tests/fixtures/downstream-project/ permanent in-tree consumer-project fixture (CON-10 -- checked into version control NOT generated at test time) with .claude/settings.json mirroring scripts/dispatch/adapters/runtime/claude-code.sh::--hook-config emission shape (Stop hook = after-verify-sync.sh; PreToolUse Bash hooks = pre-bash-shape-guard.sh + before-commit.sh; every leaf carries _orchestrator_managed: true M025 invariant; fixture uses literal HOME placeholder bytes for HOME-agnostic shape comparison) and README.md (~30 lines) documenting purpose + noisy-fail discipline + cross-references; scripts/verify/m028/p05-fixture-permanent.sh (~50 lines AD-19 single-script-file flat shape bash 3.2 + POSIX-sh-safe no jq -- existence-only Truth-Check on directory + .claude/settings.json >=8 lines + README.md >=6 lines containing downstream-project token; 5 PASS rows + verdict line); scripts/verify/m028/p05-downstream-fixture-shape.sh (~115 lines AD-19 single-script-file flat shape bash 3.2 + POSIX-sh-safe no jq -- CON-10 noisy-fail shape gate: captures adapter live --hook-config emission with isolated HOME tmp dir then asserts every command field matches the shape pattern command-bash-anything-dot-sh in both fixture AND adapter emission, _orchestrator_managed flag count matches between fixture and adapter (currently 3:3), command line count matches (3:3), three runtime-stable hook basenames (after-verify-sync.sh, pre-bash-shape-guard.sh, before-commit.sh) appear in both; 9 PASS rows + verdict line; helper-function carve-out applied to count_command_lines / count_managed_flags / all_commands_have_shape extraction helpers per M028/P02/T05 codification)"
requires:
  - "from:P02/T02 what:scripts/dispatch/adapters/runtime/claude-code.sh::--hook-config emission shape -- the contract this fixture mirrors and the live source the shape verifier captures at run time"
affects:
  - "P05/T02,P05/T03,P05/T04"
key_files:
  - "tests/fixtures/downstream-project/.claude/settings.json,tests/fixtures/downstream-project/README.md,scripts/verify/m028/p05-fixture-permanent.sh,scripts/verify/m028/p05-downstream-fixture-shape.sh"
key_decisions:
  - "Use literal HOME placeholder bytes in fixture rather than expanded HOME path -- adapter live emission contains the runtime HOME path which would tie the fixture to one execution; placeholder form keeps fixture HOME-agnostic and the shape verifier compares the regex pattern bash anything dot sh rather than literal expansion; this is the intended contract surface (shape NOT identity).,Co-author both verifiers with the deliverable in T01 -- per CLAUDE.md plan-time verifier-availability cross-check discipline; auto-loop --step=V resolves at T01 time without cross-task verifier dependency on T02-T05 deliverables; mirrors P02/P03/P04 pattern.,Helper-function carve-out applied to extraction helpers (count_command_lines / count_managed_flags / all_commands_have_shape) in p05-downstream-fixture-shape.sh -- function bodies are NOT classifier-scanned per M028/P02/T05 codification; documented in top-of-file comment block so future readers see the contract at read time without spelunking history.,Direct invocation of project-tree verifiers in plan Verification section (bash scripts/verify/m028/<name>.sh) NOT wrapped in run-probe.sh per the M028/P02 dogfood finding -- run-probe.sh is reserved for staged throwaway probes inside /tmp /var/folders or <repo>/tmp/; both T01 verifiers are project-tree scripts and invoke directly.,Files-To-Touch in payload listed nine files but Steps + Verification + Must-Haves only require four T01 deliverables (fixture settings.json + README.md + two verifiers) -- the other five files (tests/run-downstream-fixture.sh, p05-downstream-fixture-clean.sh, p05-regression-gate.sh, p05-run-all-clean.sh, p05-corpus-replay-clean.sh) are T02/T03/T04 deliverables; honored Steps + Verification + Must-Haves as the contract for THIS task per dispatch-orchestrator scope discipline.,Plan-time classifier-shape pre-validation -- each Verification line is a single-stage bash path.sh invocation; M028 classifier returns allow (no nested cmdsub, no compound chain, no AP-014 body, no quoted-arg-newline-hash); both verifiers exist on disk by task close so the auto-loop --step=V mechanical resolver succeeds.,Slug-suffixed summary filename T01-downstream-fixture-SUMMARY.md per CLAUDE.md M028 hotfix on slug-suffix asymmetry -- write-summary.sh accepts the literal output path so the slug-bearing form aligns with the bilateral-tolerance contract on the read side."
patterns_established:
  - "Permanent in-tree consumer fixture under tests/fixtures/downstream-project/ as CON-10 anchor for noisy-fail shape gates -- contract reference fixture for the adapter --hook-config emission shape lives under version control; drift caught at CI time NOT discovered downstream; fixture authored to mirror the live emission so first-run verifier exit is clean.,Live-adapter-emission-vs-fixture-bytes shape gate via mktemp -d isolated HOME -- verifier captures HOME equals tmp/fake-home bash adapter --hook-config redirected to tmp/adapter-emission.json then runs shape assertions against both fixture and capture; isolated HOME prevents the verifier from polluting the developer real ~/.claude/ tree and keeps the verifier reentrant and CI-safe; trap rm -rf tmp EXIT cleanup mirrors the P02/P03/P04 verifier convention.,Shape-pattern comparison via grep -cE on a regex matching command-bash-anything-dot-sh rather than literal-bytes diff -- fixture uses literal HOME placeholder while adapter emits expanded HOME path; comparing the shape via regex captures the contract intent (every command is a bash invocation of a .sh file under a runtime-stable hooks directory) without tying the fixture to any one HOME at any one execution.,Count-equality assertions for shape contracts (managed-flag count + command-line count between fixture and adapter emission) -- additive-axis discipline: when the adapter adds a new hook event the fixture count drifts from adapter count and the verifier fails loudly; same shape catches fixture-side drift (fixture adds an unauthorized hook the adapter does not emit); two-way drift detection with a single equality assertion.,Helper-function carve-out as canonical extraction-helper shape -- p05-downstream-fixture-shape.sh defines count_command_lines / count_managed_flags / all_commands_have_shape inside bash function bodies; top-of-file comment block documents the carve-out is load-bearing for AD-19 compliance under the M021/M028 inline-shape classifier; reusable shape for any future verifier needing structure-bearing extraction over JSON or other text fixtures without sourcing jq.,Three runtime-stable hook basenames (after-verify-sync.sh, pre-bash-shape-guard.sh, before-commit.sh) as the M028 contract floor for adapter --hook-config emission -- any future hook addition must update the fixture and the verifier loop simultaneously to keep the count-equality assertions clean; the basename loop in p05-downstream-fixture-shape.sh is the codified extension point.,Slug-suffixed task summary filename pattern (T01-slug-SUMMARY.md not T01-SUMMARY.md) emitted by write-summary.sh task literal output path -- aligns with CLAUDE.md M028 hotfix on slug-suffix asymmetry; readers using parameter-expansion task-id-strip match either form; bilateral-tolerance contract on the read side keeps both shapes valid at lookup time."
drill_down_paths:
  - "tests/fixtures/downstream-project/.claude/settings.json,tests/fixtures/downstream-project/README.md,scripts/verify/m028/p05-fixture-permanent.sh,scripts/verify/m028/p05-downstream-fixture-shape.sh"
duration: "35m"
verification_result: "pass"
completed_at: "2026-04-29T19:27:51Z"
---

T01 stages the permanent in-tree downstream-project fixture under tests/fixtures/downstream-project/ (CON-10 -- fixture is checked into version control, NOT generated at test time) plus two co-authored verifiers under scripts/verify/m028/. The fixture is intentionally minimal: a single .claude/settings.json mirroring the runtime adapter --hook-config emission shape (Stop hook = after-verify-sync.sh; PreToolUse Bash = pre-bash-shape-guard.sh + before-commit.sh; every leaf carries _orchestrator_managed: true) plus a README.md documenting the noisy-fail discipline and cross-references. No internal scripts/hooks/ directory -- the fixture is shaped like a real consumer project that gets the hooks staged at ~/.claude/orchestrator-hooks/ via the [M025](../../../../../milestones/M025/index.md)+M028 install path.

Fixture .claude/settings.json uses literal HOME placeholder bytes (HOME-agnostic). The shape verifier compares the pattern command-bash-anything-dot-sh via grep -cE rather than the literal expanded path -- fixture and adapter emission both satisfy the shape regex regardless of HOME at run time. Choice recorded: literal HOME placeholder over expanded path because the adapter live emission contains the runtime HOME (e.g., /tmp/m028-p05-probe-NNNNN/.claude/...) which would tie the fixture to a single execution.

p05-fixture-permanent.sh (~50 lines, AD-19 single-script-file flat shape, bash 3.2 + POSIX-sh-safe, no jq) is the existence-only Truth-Check: asserts directory presence, settings.json exists with >=8 lines, README.md exists with >=6 lines and contains the literal downstream-project token. 5 PASS rows + final PASS verdict line.

p05-downstream-fixture-shape.sh (~115 lines, AD-19 single-script-file flat shape, bash 3.2 + POSIX-sh-safe, no jq) is the CON-10 noisy-fail shape gate: captures the runtime adapter live --hook-config emission via HOME=isolated-tmp bash scripts/dispatch/adapters/runtime/claude-code.sh --hook-config, then asserts (a) fixture exists; (b) adapter exited 0; (c) every command field in fixture matches command-bash-anything-dot-sh shape; (d) same for adapter emission; (e) count of _orchestrator_managed: true flags matches between fixture and adapter (currently 3:3); (f) count of command keys matches (3:3); (g) the three runtime-stable hook basenames (after-verify-sync.sh, pre-bash-shape-guard.sh, before-commit.sh) appear in both. 9 PASS rows + final PASS verdict line.

Helper-function carve-out applied: count_command_lines / count_managed_flags / all_commands_have_shape live inside bash function bodies in p05-downstream-fixture-shape.sh -- the AP-009 inline-shape classifier does NOT scan function bodies (codified at M028/P02/T05 as the canonical verifier-authoring shape). Top-of-file comment block documents the carve-out so future readers see the contract at read time.

Verification: bash scripts/verify/m028/p05-fixture-permanent.sh exits 0 with 5 PASS rows; bash scripts/verify/m028/p05-downstream-fixture-shape.sh exits 0 with 9 PASS rows. Adapter --hook-config emission matches fixture shape on first run (fixture authored to mirror the live emission so no drift exists at T01 time). T02 will consume the fixture as input to the autonomous-loop replay harness; T03 will sequence both verifiers as sub-gates of the M028 close-out regression gate.

CON-1 (AD-19) honored: both verifiers are flat single-file shapes with inline pass/fail closures; no sourcing; no compound chains in command-line shape. CON-2 (bash 3.2): no mapfile, no here-string, no declare -A, no double-bracket regex outside guarded contexts. CON-6 (no new runtime deps): pure bash + grep + wc + mktemp. CON-10 (downstream-fixture permanence): fixture lives in-tree under version control, drift caught noisily by the shape verifier.

Plan-time discipline applied: each Verification line was empirically a single-stage bash path.sh invocation -- the M028 classifier returns allow on both. No nested cmdsub, no compound chain, no AP-014 body, no quoted-arg-newline-hash. Co-authoring rule honored: both verifiers exist on disk by end of this task; no cross-task verifier dependency. run-probe.sh NOT used in the Verification section per CLAUDE.md M028/P02 dogfood discipline (run-probe.sh is reserved for staged throwaway probes inside /tmp, /var/folders, or repo/tmp/; project-tree verifiers invoke directly).

Slug-suffixed summary filename emitted at T01-downstream-fixture-SUMMARY.md per CLAUDE.md M028 hotfix on slug-suffix asymmetry -- write-summary.sh accepts the literal output path so the slug-bearing form is used for the bilateral-tolerance contract on the read side.
