---
schema_version: "1.0"
type: task-summary
id: "T03"
parent: "P02"
milestone: "M029"
provides:
  - "FR-5/FR-6/CON-1/CON-3/CON-4 at-rest tree renderer engine at scripts/diagnostics/render-position.sh emitting the canonical glyph alphabet (✓ ▶ ◇ ✗) over feature -> milestone -> phase -> task tree with progress bar (▓░ X% (k/n phases)) + per-row cost column gated by FR-6 dispatch_usage detection probe; sources AD-1 detect-invocation-context.sh single-resolve env block; supports --milestone/--expand-all/--feature/--no-cost/--root flags; AD-6 exactly-one-of milestone:/milestones: schema parsing via awk helpers; reverse-lookup advisory against M*-EVALUATION.md feature_ref:; #Q-5 inactive-render collapsed-by-default + active-always-expanded; canonical orchestrator:where skill at commands/where.md (10 H2 sections mirrored from commands/context.md) declaring CON-1/FR-14 read-only + CON-4/FR-11 no-GitHub-API + the canonical compact ▽ saved Nk savings form (#Q-G8) referenced in legend (renderer never emits ▽ -- P03 --live mode only); paired AD-19 single-script-file shape verifiers tools/verify/m029-p02-render-position-shape.sh (20/20 PASS) + tools/verify/m029-p02-where-skill-shape.sh (24/24 PASS) gating glyph alphabet, contract tokens, anti-coupling /integrations/github absent, forbidden verbose-suffix form via tier1 cache reuse absent, AD-1 resolver invocation, M027 metrics-rollup invocation + FR-6 dispatch_usage probe presence, all 10 required H2 sections, all 5 canonical glyphs, all 3 required references"
requires:
  - "from:P02/T01 what:references/cross-milestone-feature-shape.md (AD-6 schema + glyph alphabet + #Q-G8 canonical form + reverse-lookup contract); from:P02/T02 what:scripts/diagnostics/summarize-milestone.sh (--milestone <M###> --format=keys four-line block consumed for inactive-milestone collapsed progress bar)"
affects:
  - "P02-T04,P02-T05,P03"
key_files:
  - "scripts/diagnostics/render-position.sh,commands/where.md,tools/verify/m029-p02-render-position-shape.sh,tools/verify/m029-p02-where-skill-shape.sh"
key_decisions:
  - "AD-1 single-resolve discipline (renderer reads detect-invocation-context.sh's three-line env block; never re-derives TTY/CI/runtime),AD-6 exactly-one-of milestone:/milestones: schema with stderr WARN + prefer-plural on both-present,#Q-5 collapsed-by-default + --expand-all + active-always-expanded inactive-render shape,#Q-G8 canonical compact form ▽ saved Nk (verbose-suffix forms reserved for future --verbose mode),CON-1/FR-14 read-only with sole allowed write site /tmp/m029-rp.$$/,CON-3 silent FR-6 cost-column suppression on pre-M019 milestones,CON-4/FR-11 no-GitHub-API via anti-coupling /integrations/github absent invariant,MEM004 carve-out for awk/sed/grep pipes inside renderer body (AD-19 verifier-shape rules apply only at Check: command level)"
patterns_established:
  - "renderer-engine-plus-LLM-instruction-skill split (P01 commands/context.md precedent; production rendering performed by the engine -- the skill instructs the agent to invoke the engine and pass output through unchanged); AD-1 resolver capture-to-tempfile-then-grep-line pattern (no $() pipe in public surface;  parameter expansion); AD-6 frontmatter parsing via two awk helpers (_rp_yaml_scalar singular + _rp_yaml_inline_list plural [..]); FR-6 / CON-3 silent suppression via single grep -m1 -F probe for literal dispatch_usage with on-miss empty-string return + caller printf-concat (visually identical row minus dollar amount); reverse-lookup advisory pattern (enumerate EVALUATION feature_ref + sort both sets + sorted-string equality + stderr WARN on mismatch + render-from-spec per Principle XI); negative-assertion verifier discipline (T01 precedent: verifier code names forbidden tokens in assertion strings; deliverable body must paraphrase semantically); 8-section command-doc shape mirroring commands/context.md (Prerequisites/Core Workflow/Glyph Legend/Flags/Output/Idempotency/Error Handling/Constraints/Referenced Scripts/Reference Files); MEM004 carve-out applied: awk/sed/grep pipes inside renderer body; AD-19 single-script-file straight-line bash for verifiers (no $(cmd | grep), no plain subshells, no process substitution)"
drill_down_paths:
  - ".orchestrator/milestones/M029/phases/P02/tasks/T03-render-position-and-where-skill-PLAN.md,scripts/diagnostics/render-position.sh,commands/where.md,tools/verify/m029-p02-render-position-shape.sh,tools/verify/m029-p02-where-skill-shape.sh"
duration: "45m"
verification_result: "pass"
completed_at: "2026-05-06T00:28:32Z"
---

T03 ships the two coupled FR-5 / FR-6 / CON-1 / CON-3 / CON-4 deliverables that together constitute the orchestrator:where engine + skill: scripts/diagnostics/render-position.sh (the at-rest tree renderer) and commands/where.md (the LLM-instruction skill that invokes it). Both are gated by mechanical shape verifiers under tools/verify/m029-p02-*-shape.sh; T04 will add the SC-5 byte-identity behavioral acceptance against the dedicated mixed-state golden fixture.

Deliverables (all four on disk):

- scripts/diagnostics/render-position.sh (~470 lines, executable, bash 3.2 compatible per MEM001). Re-source guard + script-dir/project-root resolution mirrors metrics-rollup.sh / efficiency-footer.sh / summarize-milestone.sh. Argument parser handles --milestone / --expand-all / --feature / --no-cost / --root / -h|--help (exit 2 on unknown flag). AD-1 single-resolve via bash detect-invocation-context.sh captured to ${TMPDIR:-/tmp}/m029-rp.$$/dic.out (no $() pipe in public surface; trap on EXIT removes the dir). Feature-spec frontmatter parsing via two awk helpers (_rp_yaml_scalar, _rp_yaml_inline_list) honors the AD-6 exactly-one-of milestone:/milestones: schema; both-present case emits stderr WARN and prefers plural per Principle XV. Reverse-lookup advisory enumerates .orchestrator/milestones/M*/M*-EVALUATION.md feature_ref:, sorts both sets, emits stderr WARN on mismatch, renders from spec (Principle XI). FR-6 detection probe is a single grep -m1 -F for the literal token dispatch_usage; on miss, the cost column is OMITTED ENTIRELY (no blank cell, no stderr warning per CON-3). Per-row cost cell pulls the first decimal from the matching metrics-rollup.sh --granularity task row. Inactive milestones render the AD-6 #Q-5 collapsed shape (<glyph> M### <name>  ▓░ X% (k/n phases)) by default; --expand-all expands every milestone; the active milestone is always expanded. Phase-state derivation: SUMMARY -> ✓, PLAN-without-SUMMARY -> ▶, last verify_result fail in execution-log.jsonl -> ✗, otherwise ◇. Task-state derivation mirrors the phase rule against T##-*-PLAN/SUMMARY.md. Read-only invariant: the only allowed write site is /tmp/m029-rp.$$/ (trap-cleaned).

- commands/where.md (~95 lines, the canonical 8-section command-doc shape mirrored from commands/context.md): description frontmatter + H1 + 10 H2 sections (Prerequisites/State Check, Core Workflow, Glyph Legend, Flags, Output, Idempotency, Error Handling, Constraints, Referenced Scripts, Reference Files). Embeds the canonical glyph alphabet (✓ ▶ ◇ ✗ ▽) with the #Q-G8 canonical compact form ▽ saved Nk pinned in the legend; documents the at-rest renderer NEVER emits ▽ itself (P03 --live mode only). Declares CON-1/FR-14 read-only contract + CON-4/FR-11 no-GitHub-API contract. References scripts/diagnostics/render-position.sh + scripts/state/detect-invocation-context.sh + references/cross-milestone-feature-shape.md per the verifier contract. Documents all five renderer flags + behavior + degraded-state error envelopes (no active milestone -> one-line notice + exit 0; reverse-lookup mismatch -> stderr WARN + render proceeds; metrics absent -> silent cost-column suppression).

- tools/verify/m029-p02-render-position-shape.sh (~180 lines, executable, AD-19 single-script-file straight-line bash). 20 assertions across 7 categories: file existence + executable bit (2), the four canonical glyphs literal-present (4), ▽ savings glyph documented in comments (1), forbidden verbose-suffix form via tier1 cache reuse absent (1), read-only contract token + no-GitHub-API contract token + bash-3.2 token + AD-6 reference (4), anti-coupling /integrations/github absent (1), invokes detect-invocation-context.sh + metrics-rollup.sh + FR-6 detection probe dispatch_usage (3), four CLI flags --milestone/--expand-all/--no-cost/--root documented (4). Negative-assertion pattern (forbidden tokens) mirrors the T01 verifier discipline: the verifier code itself contains the literal forbidden tokens in its assertion strings, but asserts absence in the renderer body — verifier code is not deliverable text.

- tools/verify/m029-p02-where-skill-shape.sh (~190 lines, executable, AD-19). 24 assertions across 5 categories: file existence (1), 10 required H2 sections (10), five canonical glyphs literal-present (5), three required references render-position.sh + detect-invocation-context.sh + cross-milestone-feature-shape.md (3), read-only token + no-GitHub-API token + anti-coupling /integrations/github absent + canonical ▽ saved Nk present + forbidden verbose form absent (5).

Verification (both Must-Haves green):

- bash tools/verify/m029-p02-render-position-shape.sh — 20/20 PASS, exit 0. SUMMARY: m029-p02-render-position-shape.sh pass=20 fail=0.
- bash tools/verify/m029-p02-where-skill-shape.sh — 24/24 PASS, exit 0. SUMMARY: m029-p02-where-skill-shape.sh pass=24 fail=0.

Behavioral spot-checks against live tree (Expected Output: smoke-invocation against M029 exits 0):

- bash render-position.sh --help -> usage block, exit 0.
- bash render-position.sh --milestone M029 -> ▶ M029 header + ▓░ 50% (1/2 phases) + collapsed ✓ P01 + expanded ▶ P02 with five tasks (T01/T02 ✓ + cost cell, T03/T04/T05 ▶); exit 0.
- bash render-position.sh --milestone [M030](../../../../../milestones/M030/index.md) --expand-all -> ✓ M030 header + ▓░ 100% (8/8 phases) + 8 ✓ phase rows; exit 0.
- bash render-position.sh --milestone M001 -> collapsed line (M001 has no phases/ dir; ◇ glyph with 0/0 phases); exit 0.
- bash render-position.sh --bogus -> stderr usage diagnostic + exit 2.
- bash render-position.sh --no-cost --milestone M029 -> cost cells suppressed; tree otherwise identical; exit 0.
- Default invocation (no flags) -> feature: 037-roadmap-visibility-cli-ux header + the M029 cross-milestone view (single-element list since spec 037 declares milestone: M029 singular).

Plan-shape paper-cut surfaced and worked around in-flight: the renderer's header documentation block originally named the forbidden #Q-G8 verbose form (via tier1 cache reuse) and the forbidden [M013](../../../../../milestones/M013/index.md) sidecar path (/integrations/github) literally as part of describing what the renderer MUST NOT contain. That tripped the verifier's negative-assertion checks. Fix: rephrase the header comments to describe the forbidden tokens semantically (any verbose suffix that appends provenance after the magnitude, the M013 sidecar github.json file under the integrations subtree of .orchestrator/) without containing the literal forbidden strings. The verifier code itself still contains the literal forbidden tokens in its assertion strings (per T01's verifier-discipline pattern); the renderer body and the skill body do not.

CON-7 / AD-8 boundary discipline preserved: T03 introduces NO schema additions to M013 sidecar, [M019](../../../../../milestones/M019/index.md) JSONL, [M020](../../../../../milestones/M020/index.md) KNOWLEDGE.md, or [M027](../../../../../milestones/M027/index.md) surfaces. The four new files (one renderer + one skill + two verifiers) are the only artifacts. render-position.sh is a read-only consumer of detect-invocation-context.sh, find-active-milestone.sh, summarize-milestone.sh, metrics-rollup.sh, read-roadmap.sh, plus on-disk milestone/phase/task artifacts and execution-log.jsonl. No M027 modification (per task plan constraint).

Patterns established (load-bearing for T04 fixtures + T05 phase-suite):

1. Sourceable+CLI dual-shape with re-source guard mirrors summarize-milestone.sh / metrics-rollup.sh / efficiency-footer.sh; _RENDER_POSITION_SH_SOURCED idempotency idiom.
2. AD-1 resolver capture-to-tempfile-then-grep-line pattern: bash detect-invocation-context.sh > /tmp/dir/dic.out; grep -E '^renderer=' "$out"; ${line#renderer=} parameter expansion. Avoids $() pipe in public surface.
3. AD-6 frontmatter parsing via two awk helpers (_rp_yaml_scalar for milestone:, _rp_yaml_inline_list for milestones: [..]); both-present case emits stderr WARN and prefers plural per Principle XV.
4. FR-6 / CON-3 silent suppression: single grep -m1 -F probe for literal dispatch_usage; on miss, return 0 from _rp_cost_column with no output. The renderer's printf consumer concatenates the empty cost cell with the task row, producing visually-identical output to the M019-equipped path minus the dollar amount.
5. #Q-5 inactive-render: collapsed by default (single line via summarize-milestone.sh keys), --expand-all overrides, active milestone always expanded.
6. Reverse-lookup advisory enumerates EVALUATION feature_ref: + sorts both spec-declared and discovered sets + diffs sorted-string equality; emits stderr WARN on mismatch with set-notation { M### M### } payload; render proceeds from spec (Principle XI).
7. Negative-assertion verifier discipline (T01 precedent): the verifier code names the forbidden tokens in its assertion strings, but asserts absence in the deliverable body. Renderer/skill bodies must paraphrase (semantic description without literal token) to pass the negative assertions.

Plan-shape compliance: the task plan listed deliverables in the description-suffixed shape (- `path` — description), avoiding the auto-loop bare-backtick parser regression. No in-flight plan repair needed.

T04 hooks: the SC-5 mixed-state golden fixture will exercise this renderer against a fixture milestone tree (M998 with all four glyph states ✓ ▶ ◇ ✗) under --root <fixture-dir>; T04's byte-identity acceptance script will compare stdout against tests/m029-acceptance/fixtures/where-mixed-state.golden through the #Q-G6 timestamp-strip pipeline. SC-6 (pre-M019 cost-column silent suppression) will exercise --milestone M997 against a fixture with execution-log.jsonl carrying NO dispatch_usage records and assert byte-identical absence of the cost column.
