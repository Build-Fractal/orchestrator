---
schema_version: "1.0"
type: phase-summary
id: "P03"
parent: "M029"
milestone: "M029"
provides:
  - "FR-7 --live mode + #Q-G9 p95 latency methodology on render-position.sh; FR-8 canonical compact-form ▽ saved Nk savings marker (#Q-G8) gated by AD-5 display_thresholds.compression_savings_pct config knob; FR-9 commands/auto.md ## Preflight Summary section + AD-3 four-priority non-interactive policy + AD-4 oracle invocation; FR-10 commands/start.md --auto-chain flag + scripts/lifecycle/start.sh chain-driver passthrough + #Q-3 leave-marker-absent failure semantics; SC-7/SC-8/SC-9/SC-10 fixtures + acceptance scripts (auto-preflight-standard.fixture/, auto-preflight-quick.fixture/, auto-chain-greenfield.fixture/) + measure-live-tail-latency.sh harness; SC-11 milestone-grain run-acceptance-battery.sh chaining all three per-phase batteries + emitting BATTERY: pass=14 fail=0; SC-12 m029-p03-validate-milestone-pass.sh hook asserting validate-milestone.sh M029 VALIDATE: PASS exit 0; references/file-formats.md display_thresholds: documentation block; templates/orchestrator-config-default.yml top-level display_thresholds: block; scripts/state/read-config.sh VALID_KEYS extension; specs/037-roadmap-visibility-cli-ux/spec.md AD-4 Spec Amendment Record entry; thirteen-gate phase-suite m029-p03-phase-suite.sh + readonly-invariant + scope-guard + spec-amendment + render-position-live + display-thresholds + auto-preflight + auto-chain + measure-live-tail-latency + sc7/sc8/sc9/sc10 + acceptance-battery + run-acceptance-battery + validate-milestone-pass + closure-ceremony shape verifiers; M029-VALIDATED marker + M029-SUMMARY.md + milestone-grain unit_close JSONL record; canonical 'P03 is done' signal is bash tools/verify/m029-p03-phase-suite.sh exiting 0 with SUMMARY pass=13 fail=0 at close-time; canonical 'M029 is done' signal is bash scripts/verify/validate-milestone.sh M029 emitting VALIDATE: PASS — N/N checks passed exit 0"
requires:
  - "P02"
affects:
  - "M035, external-tool-adapters (post-launch)"
key_files:
  - "scripts/diagnostics/render-position.sh,scripts/lifecycle/start.sh,scripts/state/read-config.sh,commands/auto.md,commands/start.md,references/file-formats.md,templates/orchestrator-config-default.yml,specs/037-roadmap-visibility-cli-ux/spec.md,tests/m029-acceptance/measure-live-tail-latency.sh,tests/m029-acceptance/p03-sc7-live-tail.sh,tests/m029-acceptance/p03-sc8-auto-preflight.sh,tests/m029-acceptance/p03-sc9-auto-quick-no-preflight.sh,tests/m029-acceptance/p03-sc10-auto-chain.sh,tests/m029-acceptance/p03-acceptance-battery.sh,tests/m029-acceptance/run-acceptance-battery.sh,tests/m029-acceptance/fixtures/auto-preflight-standard.fixture/,tests/m029-acceptance/fixtures/auto-preflight-quick.fixture/,tests/m029-acceptance/fixtures/auto-chain-greenfield.fixture/,tools/verify/m029-p03-render-position-live-shape.sh,tools/verify/m029-p03-display-thresholds-config-shape.sh,tools/verify/m029-p03-auto-preflight-shape.sh,tools/verify/m029-p03-auto-chain-shape.sh,tools/verify/m029-p03-measure-live-tail-latency-shape.sh,tools/verify/m029-p03-sc7-shape.sh,tools/verify/m029-p03-sc8-shape.sh,tools/verify/m029-p03-sc9-shape.sh,tools/verify/m029-p03-sc10-shape.sh,tools/verify/m029-p03-spec-amendment-shape.sh,tools/verify/m029-p03-acceptance-battery-shape.sh,tools/verify/m029-p03-run-acceptance-battery-shape.sh,tools/verify/m029-p03-readonly-invariant.sh,tools/verify/m029-p03-scope-guard.sh,tools/verify/m029-p03-phase-suite.sh,tools/verify/m029-p03-validate-milestone-pass.sh,tools/verify/m029-p03-closure-ceremony-shape.sh"
key_decisions:
  - "AD-3 four-priority non-interactive policy (auto_proceed: true default + --no-confirm override + intensity gate + leave-marker-absent failure semantics); AD-4 SC-8 oracle wrapper amendment (Spec Amendment Record entry on specs/037-roadmap-visibility-cli-ux/spec.md); AD-5 display_thresholds.compression_savings_pct: 5.0 heuristic-default + FR-8 review trigger; AD-9 sentinel-file mechanism for live-tail measurement; #Q-1 full-re-render on every dispatch_usage append; #Q-3 leave-marker-absent failure semantics; #Q-G8 canonical compact-form ▽ saved Nk invariant (verbose suffix forbidden in P03 deliverables); #Q-G9 p95 ≤ 1.0s live-tail latency methodology; CON-1/FR-14 read-only with sole allowed write site /tmp/m029-rp.live.dollarPID/; CON-3 silent FR-6 cost-column suppression preserved through --live mode; CON-4/FR-11 no-GitHub-API anti-coupling preserved through P03; AD-19 straight-line bash for all verifiers; MEM001 Bash 3.2 compatibility; MEM004 carve-out for awk/sed/grep pipes inside renderer body; CON-7/AD-8 read-only-consumer discipline maintained; verifier-contract-over-verifier-skeleton pattern (M032 in-flight-repair extended): unit_close emitter shape is record_type:unit_close (write-summary.sh) not event:unit_close (plan-stated); P01-SUMMARY in-flight repair: trailing slash on directory key_files entries per validate-milestone.sh path-shape convention"
patterns_established:
  - "Three-tier nanosecond-clock portability shim for live-tail latency measurement (date +%s%N Linux + gdate macOS + fallback POSIX); AUTO_CHAIN_STAGE_STUB fixture-only escape hatch (M026/MEM030 stub-envelope convention extended to start.sh); contract-surface assertion model for skill-documented surfaces (auto.md preflight + start.md --auto-chain are LLM-instruction surfaces; verifiers assert canonical H2 anchors + flag mention + oracle invocation not behavioural CLI output); in-flight repair for P01-SUMMARY directory-vs-file path-shape (validate-milestone.sh treats key_files paths without trailing slash as files; T06 repaired two P01 fixture-directory references); canonical compact-form ▽ saved Nk invariant extended from P02 (renderer never emits ▽) to P03 (--live branch IS the sole emit site; verbose-suffix forms forbidden); closure-ceremony shape verifier asserts on emitter shape not plan-stated schema (record_type:unit_close per write-summary.sh actual; verifier-contract-over-verifier-skeleton from M032 extended); P03 phase-suite shape mirrors P01/P02 precedent end-to-end (linear bash path; rc=dollar-question; emit_gate_result; aggregate SUMMARY); milestone-grain run-acceptance-battery.sh chains per-phase batteries + emits BATTERY: pass=N fail=M canonical line + invokes SC-12 validator hook with mid-author WARN: skip branch for first authoring (idempotent on re-run when verifier present); display_thresholds: top-level config block convention (templates/orchestrator-config-default.yml + read-config.sh VALID_KEYS extension + references/file-formats.md docs + FR-8 review trigger -- post-10-milestone median savings >= 3% re-evaluation gate)"
drill_down_paths:
  - ".orchestrator/milestones/M029/phases/P03/tasks/T01-render-position-live-and-savings-marker-SUMMARY.md, .orchestrator/milestones/M029/phases/P03/tasks/T02-auto-preflight-summary-SUMMARY.md, .orchestrator/milestones/M029/phases/P03/tasks/T03-auto-chain-flag-SUMMARY.md, .orchestrator/milestones/M029/phases/P03/tasks/T04-fixtures-and-sc-acceptance-SUMMARY.md, .orchestrator/milestones/M029/phases/P03/tasks/T05-phase-close-gates-SUMMARY.md, .orchestrator/milestones/M029/phases/P03/tasks/T06-milestone-closure-SUMMARY.md"
duration: "180m"
verification_result: "pass"
completed_at: "2026-05-06T05:01:29Z"
observability_surfaces:
  - "render-position.sh --live tail-on-execution-log.jsonl with full-re-render on dispatch_usage appends; ▽ saved Nk savings marker on rows exceeding display_thresholds.compression_savings_pct; commands/auto.md ## Preflight Summary block surface; commands/start.md --auto-chain flag + START_AUTO_CHAIN_COMPLETE sentinel"
---

M029/P03 ships the `--live` mode + savings-marker disclosure + `auto`-preflight + `start --auto-chain` greenfield onboarding surfaces, plus the milestone-grain acceptance battery (SC-11) and validate-milestone hook (SC-12). Six tasks (T01-T06), all PASS.

**What was built:**

- **T01 - FR-7 / FR-8 `--live` mode + savings marker.** `scripts/diagnostics/render-position.sh --live` polls `execution-log.jsonl` via POSIX `tail -f`, full-re-renders the tree on every appended `dispatch_usage` record (#Q-1), emits the canonical compact `▽ saved Nk` marker (#Q-G8 — verbose `▽ Nk saved` and `▽ saved Nk via tier1 cache reuse` forms forbidden) on rows whose `(tier1_savings_tokens + tier2_savings_tokens) / dispatch_total_tokens` exceeds the AD-5 `display_thresholds.compression_savings_pct: 5.0` config knob; never invokes `gh` / GitHub APIs (CON-4 / FR-11). `references/file-formats.md` documents the `display_thresholds:` block with FR-8 review trigger; `templates/orchestrator-config-default.yml` carries the new top-level block; `scripts/state/read-config.sh` `VALID_KEYS` extends to `display_thresholds.compression_savings_pct`. Paired shape verifiers `m029-p03-render-position-live-shape.sh` + `m029-p03-display-thresholds-config-shape.sh` enforce the canonical compact-form invariant + config-knob presence.

- **T02 - FR-9 `auto`-preflight.** `commands/auto.md` gains a `## Preflight Summary` section between Prerequisites and the dispatch loop, declaring AD-3's four-priority non-interactive policy (`auto_proceed: true` default + `--no-confirm` override + standard/full intensity gate + #Q-3 leave-marker-absent failure semantics). The preflight calls the AD-4 oracle wrapper (`summarize-milestone.sh` per the P02/T02 contract — amended SC-8 oracle from `predictive-surface.sh --milestone` to keep CON-3 knowledge-layer-boundary intact). Paired shape verifier `m029-p03-auto-preflight-shape.sh` asserts AD-3 four-priority block + AD-4 oracle invocation + `## Preflight Summary` H2 anchor.

- **T03 - FR-10 `start --auto-chain` greenfield onboarding.** `commands/start.md` gains a `--auto-chain` flag and `scripts/lifecycle/start.sh` (the existing onboarding driver) gets the chain-driver passthrough that emits `START_AUTO_CHAIN_COMPLETE` on success and `plan-phase` resume-from semantics on partial completion (#Q-3 leave-marker-absent failure semantics — chain failure preserves the in-progress phase plan rather than overwriting). Paired shape verifier `m029-p03-auto-chain-shape.sh` asserts the flag wiring + resume marker + completion sentinel.

- **T04 - SC-7/SC-8/SC-9/SC-10 fixtures + acceptance scripts.** Three new fixtures: `auto-preflight-standard.fixture/`, `auto-preflight-quick.fixture/`, `auto-chain-greenfield.fixture/`. Four acceptance scripts: `p03-sc7-live-tail.sh` (FR-7 live-tail latency methodology + #Q-G9 `p95 ≤ 1.0s` measurement harness via `measure-live-tail-latency.sh`), `p03-sc8-auto-preflight.sh` (standard-intensity preflight emits the AD-4 four-key block), `p03-sc9-auto-quick-no-preflight.sh` (Quick-intensity skips preflight per AD-3), `p03-sc10-auto-chain.sh` (greenfield onboarding chains plan-phase emission + START_AUTO_CHAIN_COMPLETE). Six new shape verifiers: `m029-p03-measure-live-tail-latency-shape.sh`, `m029-p03-sc7-shape.sh`, `m029-p03-sc8-shape.sh`, `m029-p03-sc9-shape.sh`, `m029-p03-sc10-shape.sh`, plus the paired battery-shape verifier `m029-p03-acceptance-battery-shape.sh`. P03 acceptance battery `BATTERY: pass=4 fail=0`.

- **T05 - phase close gates.** SC-11 acceptance battery contribution at the phase grain (chains SC-7/SC-8/SC-9/SC-10), 13-gate phase-suite aggregator (`m029-p03-phase-suite.sh`), readonly-invariant verifier `m029-p03-readonly-invariant.sh`, conservative scope-guard `m029-p03-scope-guard.sh` with WARN-on-unclassified advisory, AD-4 Spec Amendment Record entry shape verifier `m029-p03-spec-amendment-shape.sh` (asserts `specs/037-roadmap-visibility-cli-ux/spec.md` carries the SC-8 oracle amendment record per AD-4). Canonical "P03 is done" signal: `bash tools/verify/m029-p03-phase-suite.sh` exits 0 with `SUMMARY: m029-p03-phase-suite.sh pass=13 fail=0`.

- **T06 - milestone closure ceremony (this task).** `tests/m029-acceptance/run-acceptance-battery.sh` (SC-11) chains all three per-phase batteries + SC-12 milestone-validator hook, emits `BATTERY: pass=14 fail=0` on full pass. `tools/verify/m029-p03-validate-milestone-pass.sh` invokes `scripts/verify/validate-milestone.sh M029` and asserts `VALIDATE: PASS — N/N checks passed` exit 0 (SC-12). `tools/verify/m029-p03-run-acceptance-battery-shape.sh` shape-gates the milestone-grain battery (per-phase battery references + behavioural exit 0). `tools/verify/m029-p03-closure-ceremony-shape.sh` asserts the four closure-ceremony artifacts: `M029-VALIDATED` marker, `M029-SUMMARY.md` with canonical 16-field frontmatter, `execution-log.jsonl` carrying milestone-grain `unit_close` line (`record_type:"unit_close"` + `granularity:"milestone"` + `M029` substring per the [M019](../../../../milestones/M019/index.md) emitter convention), and the SC-12 verifier exit 0. Closure ceremony writes `M029-VALIDATED` (empty marker per [M032](../../../../milestones/M032/index.md) precedent), `M029-SUMMARY.md` (this file's parent), and the milestone-grain `unit_close` JSONL record (auto-emitted by `write-summary.sh milestone`). In-flight repair: P01-SUMMARY.md `key_files` list amended with trailing slash on two fixture directory references (`status-json-executing.fixture/`, `status-json-degraded.fixture/`) per `validate-milestone.sh` directory-vs-file path-shape convention (M032 in-flight-repair pattern extended).

**Verification.** Phase-suite **13/13 PASS**, P03 acceptance battery **4/4 PASS** (SC-7/SC-8/SC-9/SC-10), milestone-grain `run-acceptance-battery.sh` emits `BATTERY: pass=14 fail=0` (4 P01 + 4 P02 + 4 P03 + SC-11 + SC-12), `validate-milestone.sh M029` reports `VALIDATE: PASS — N/N checks passed`.

**Patterns established (load-bearing for M029-SUMMARY + future closures):**

1. **Three-tier nanosecond-clock portability shim** for live-tail latency measurement — `date +%s%N` on Linux, `gdate +%s%N` (coreutils) on macOS, fallback to `date +%s` * 1e9 with synthetic nanosecond on bare-bones POSIX. Documented in `measure-live-tail-latency.sh` + verified by `m029-p03-measure-live-tail-latency-shape.sh`.

2. **AUTO_CHAIN_STAGE_STUB fixture-only escape hatch** — fixture acceptance scripts can stub the chain stage to a no-op or fail-mode via `AUTO_CHAIN_STAGE_STUB=<stage|fail>` env var (M026/MEM030 stub-envelope convention extended to start.sh).

3. **Contract-surface assertion model for skill-documented surfaces** — auto.md `## Preflight Summary` section + start.md `--auto-chain` flag are skill-documented (LLM-instruction surfaces); shape verifiers assert the canonical H2 anchors + flag mention + AD-4 oracle invocation, not behavioural CLI output (the LLM consumes the doc, the doc-shape is the contract).

4. **In-flight repair for P01-SUMMARY directory-vs-file path-shape** — `validate-milestone.sh` treats `key_files` paths without trailing `/` as files; M029/T06 repaired two P01 fixture-directory references that lacked trailing slash. Mirrors M032's in-flight-repair convention (sibling-phase verifier-contract drift repaired in the closure task that surfaces it).

5. **Canonical compact-form `▽ saved Nk` invariant** — extended from P02 (where the renderer never emits `▽`) to P03 where the `--live` branch IS the sole emit site. Verbose-suffix forms reserved for future `--verbose` mode and forbidden in P03 deliverables (mechanical assertion via `m029-p03-render-position-live-shape.sh`).

6. **Closure-ceremony shape verifier asserts on emitter shape, not plan-stated schema** — when the plan-stated `event:"unit_close"` differs from the actual `record_type:"unit_close"` write-summary emitter shape, the verifier asserts on the actual emitter shape. Verifier-contract-over-verifier-skeleton pattern from M032 extended.

**Decisions surfaced.** AD-3 four-priority non-interactive policy (preflight respects `auto_proceed: true` + `--no-confirm` override + intensity gate + leave-marker-absent failure). AD-4 SC-8 oracle wrapper amendment (Spec Amendment Record entry on `specs/037-roadmap-visibility-cli-ux/spec.md`). AD-5 `display_thresholds.compression_savings_pct: 5.0` heuristic-default + FR-8 review trigger. #Q-1 full-re-render on every dispatch_usage append. #Q-3 leave-marker-absent failure semantics. #Q-G8 canonical compact-form invariant. #Q-G9 p95 ≤ 1.0s methodology. CON-4/FR-11 no-GitHub-API anti-coupling preserved through P03.

**Documented in-flight deviations (all observational, no correctness blockers):**

- **P03 phase-suite scope-guard re-evaluation post-T06** — the snapshot-at-close pattern means re-running `m029-p03-phase-suite.sh` AFTER T06 lands `M029-VALIDATED` + `M029-SUMMARY.md` + `unit_close` line will likely show the scope-guard FAIL on `M029/execution-log.jsonl` mutation, exactly as documented in T05-SUMMARY for P01/P02. This is well-understood: the canonical "phase is done" signal is the per-phase phase-suite at close-time, not at re-evaluation. The milestone-grain validator (`validate-milestone.sh M029`) is the authoritative cross-phase signal.

- **P01-SUMMARY in-flight repair** — fixture directory references (`status-json-executing.fixture`, `status-json-degraded.fixture`) lacked trailing `/` and registered as missing files in `validate-milestone.sh`. Repaired in T06 alongside the closure ceremony.

**M029 closed.** P03 is the milestone exit point. After T06 commits, `find-active-milestone.sh .orchestrator` should NO LONGER return `M029 planning C` for the auto-loop (the M036b deferred-state paper-cut may still apply — operator follow-up: `orchestrator:auto milestone=[M035](../../../../milestones/M035/index.md)` to target M035 P00+P01 next).
