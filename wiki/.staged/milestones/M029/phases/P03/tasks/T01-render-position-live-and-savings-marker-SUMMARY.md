---
schema_version: "1.0"
type: task-summary
id: "T01"
parent: "P03"
milestone: "M029"
provides:
  - "render-position.sh --live additive branch (FR-7 / #Q-1) polling execution-log.jsonl via POSIX tail -f and full-re-rendering the tree on every appended dispatch_usage record; canonical compact-form savings marker (FR-8 / #Q-G8) gated by AD-5 display_thresholds.compression_savings_pct knob (default 5.0) with Principle XI fail-open WARN-fallback; new top-level display_thresholds: config block in templates/orchestrator-config-default.yml with FR-8/AD-5 review-trigger annotation; references/file-formats.md documentation under the Configuration section; scripts/state/read-config.sh VALID_KEYS extension + nested-block walker for the dotted form (mirrors the model_routing_regression precedent); paired AD-19 single-script-file shape verifiers tools/verify/m029-p03-render-position-live-shape.sh (14/14 PASS) and tools/verify/m029-p03-display-thresholds-config-shape.sh (14/14 PASS); negative-assertion verifier discipline preserved (verifier code names the forbidden literal in assertion strings while the renderer body does NOT contain it)"
requires:
  - "P02"
affects:
  - "P03/T02,P03/T04"
key_files:
  - "scripts/diagnostics/render-position.sh,templates/orchestrator-config-default.yml,references/file-formats.md,scripts/state/read-config.sh,tools/verify/m029-p03-render-position-live-shape.sh,tools/verify/m029-p03-display-thresholds-config-shape.sh"
key_decisions:
  - "AD-5 display_thresholds.compression_savings_pct heuristic-default knob (5.0) with FR-8 review-trigger annotation; #Q-G8 canonical compact-form invariant (verbose-suffix form reserved for future --verbose mode); #Q-1 full-re-render on every appended dispatch_usage record (no incremental row tracking; bounded tree size makes cost negligible); CON-2 POSIX tail -f as the live-tail primitive (no inotify/fswatch); CON-1/FR-14 read-only --live branch (sole allowed write site is the existing pre-allocated /tmp/m029-rp.PID/ scratch dir); CON-3 silent-suppression preserved on pre-M019 milestones; Principle XI fail-open on read-config.sh non-zero rc / null sentinel (hard-coded 5.0 default + single-line stderr WARN advisory); AD-19 single-script-file shape preserved for verifier Check lines (MEM004 carve-out applies only to renderer body pipes)"
patterns_established:
  - "Refactor at-rest render block into _rp_render_tree function called once at-rest and repeatedly under --live; tail -f piped to while-read loop with case-substring match on dispatch_usage record_type triggers full re-render; awk-based JSONL field extraction inside renderer body via MEM004 carve-out; printf-pipe-awk float arithmetic for savings_pct + awk integer-ceiling for Nk magnitude (bash 3.2 safe; no herestring; no associative arrays); read-config.sh nested-block walker pattern extended to display_thresholds.* (mirrors model_routing_regression.* precedent); pkill+kill -TERM cascade for clean tail-loop shutdown in smoke harness; negative-assertion verifier discipline carried forward from P02/T03 (verifier code names forbidden literal in assertion strings; deliverable body must paraphrase semantically)"
drill_down_paths:
  - ".orchestrator/milestones/M029/phases/P02/tasks/T03-render-position-and-where-skill-SUMMARY.md"
duration: "45m"
verification_result: "pass"
completed_at: "2026-05-06T03:45:38Z"
---

T01 ships the foundational live-tail surface and the FR-8 savings marker. Six surfaces touched: (1) scripts/diagnostics/render-position.sh gains an additive --live branch + three new helper functions (_rp_read_savings_threshold, _rp_latest_dispatch_usage_for_task, _rp_savings_marker) + the at-rest render block refactored into _rp_render_tree so live-mode can call it repeatedly; (2) templates/orchestrator-config-default.yml gains a top-level display_thresholds block with compression_savings_pct 5.0 and the AD-5/FR-8 review-trigger annotation; (3) references/file-formats.md gains a Display Thresholds (M029) sub-section under Configuration with the AD-5 rationale verbatim and the canonical compact-form cross-reference; (4) scripts/state/read-config.sh extends VALID_KEYS to include display_thresholds.compression_savings_pct and adds a nested-block walker mirroring the model_routing_regression.* precedent; (5) tools/verify/m029-p03-render-position-live-shape.sh asserts 14 invariants on the renderer; (6) tools/verify/m029-p03-display-thresholds-config-shape.sh asserts 14 invariants across the three config surfaces. Both verifiers PASS 14/14. The existing P02 m029-p02-render-position-shape.sh + p02-sc5-where-mixed-state.sh + p02-sc6-where-pre-m019.sh continue to PASS unchanged (no regression). Live-mode hand-verification: renderer launched in background against the where-mixed-state fixture; synthetic dispatch_usage record (savings 3000 / total 6000 = 50%) triggered the canonical compact-form marker on a full re-render; below-threshold synthetic record (savings 100 / total 6000 = 1.6%) correctly suppressed the marker. Three constraints actively enforced: #Q-G8 canonical-form invariant (renderer body free of the forbidden verbose-suffix literal verified by negative-assertion); CON-1/FR-14 read-only (--live writes only to the existing pre-allocated scratch dir); CON-3 silent-suppression on pre-[M019](../../../../../milestones/M019/index.md) milestones. Bash 3.2 discipline: printf-pipe-awk float division for savings_pct; awk integer-ceiling for Nk; parallel indexed accumulators throughout; no associative arrays; no herestring. AD-19 verifier shape preserved at Check command level; MEM004 carve-out (awk/sed/grep pipes inside renderer + helper bodies) applied. P03/T02 (auto preflight) and P03/T04 (acceptance scripts) consume this surface.
