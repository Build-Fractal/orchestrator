---
schema_version: "1.0"
type: task-summary
id: "T03"
parent: "P03"
milestone: "M029"
provides:
  - "FR-10 --auto-chain flag on orchestrator:start; chain-driver walks evaluate -> discuss -> roadmap -> plan-phase one stage at a time; .orchestrator/start-state/STAGE.complete marker writes after each successful stage with ISO-8601 timestamp + stage name body; #Q-3 leave-marker-absent failure semantics (no alternate failure marker; re-runs re-execute); resume-from-first-incomplete-marker on re-invocation; AD-3 four-tier non-interactive policy (--yes then auto_proceed:true then non-TTY refusal M029_AUTOCHAIN_NEEDS_CONFIRMATION then TTY prompt) cross-referenced from commands/auto.md to keep policy single-sourced; AUTO_CHAIN_STAGE_STUB env var as SC-10 fixture-only escape hatch; ordering invariant chain-driver fires AFTER --with-wiki and --with-github gates; START_AUTO_CHAIN_COMPLETE / START_AUTO_CHAIN_STAGE_FAILED / START_AUTO_CHAIN_ABORTED audit-trail tokens; commands/start.md additive H2 section; scripts/lifecycle/start.sh USAGE-string-extends + flag-parser arm + chain-driver block landed; tools/verify/m029-p03-auto-chain-shape.sh shape verifier (23 assertions, AD-19 single-script-file straight-line bash + MEM004 carve-out for grep-based helpers, MEM001 bash 3.2)"
requires:
  - "P02,P03/T01,P03/T02"
affects:
  - "P03/T04"
key_files:
  - "commands/start.md,scripts/lifecycle/start.sh,tools/verify/m029-p03-auto-chain-shape.sh"
key_decisions:
  - "FR-10 entry-chain walker shape; #Q-3 leave-marker-absent invariant (no alternate failure marker; resume convention checks .complete only); AD-3 four-tier non-interactive policy single-sourced from commands/auto.md; marker filename canonical form STAGE.complete with ISO-8601 timestamp + stage name body; M029_AUTOCHAIN_NEEDS_CONFIRMATION byte-stable refusal token distinct from M029_PREFLIGHT_NEEDS_CONFIRMATION (audit-trail clarity which surface refused); AUTO_CHAIN_STAGE_STUB fixture-only escape hatch (real-mode chain-driver gates and marks; harness invokes the deep skill); CON-1/FR-14 read-only exception: .orchestrator/start-state/STAGE.complete is the ONE M029 write site (SC-14 acceptance script excludes start-state markers from sentinel check when --auto-chain is unit under test); CON-2 bash+ANSI only (read prompt + printf marker writes); Bash 3.2 (parallel scalars; no declare -A; no herestring; case y|Y|yes|YES shape)"
patterns_established:
  - "Additive H2 section in commands/*.md without disturbing existing surface (mirrors how --with-wiki and --with-github landed in start.md historically); USAGE-string-extends pattern: append [--flag] token to canonical USAGE literal then mirror in flag-parser arm and verifier assertion; Negative-assertion verifier discipline applied to start.sh: verifier code names .failed literal in assertion string while load-bearing assertion is absence-of-token in deliverable body (mirrors P02 T01/T02 precedent); Marker-name-by-shell-expansion with canonical-name comment block: chain-driver builds MARKER via parameter expansion but the four canonical filenames (evaluate.complete / discuss.complete / roadmap.complete / plan-phase.complete) are enumerated in a comment header so verifier grep -F finds the literal tokens in source (drift-resistant pattern); Fixture-only env-var escape hatch (AUTO_CHAIN_STAGE_STUB) mirrors --dry-run discipline: real-mode unaffected; SC-10 acceptance scripts inject no-op stubs to exercise chain-driver semantics without invoking deep skill behaviour; Cross-referenced policy single-sourcing: commands/start.md AD-3 section points at commands/auto.md instead of restating the four-tier policy (FR-9 preflight + FR-10 chain-driver share the policy; one source of truth); Existing convention reuse over payload-suggestion: payload mentioned YES_FLAG and REPO_ROOT but in-repo convention is YES (line 51) and CWD-relative invocation; chain-driver matches existing convention to keep diff additive"
drill_down_paths:
  - ".orchestrator/milestones/M029/phases/P03/tasks/T03-auto-chain-flag-PAYLOAD.md"
duration: "45m"
verification_result: "pass"
completed_at: "2026-05-06T04:00:36Z"
---

T03 ships the FR-10 --auto-chain flag on orchestrator:start. Three additive surfaces. (1) commands/start.md gains an H2 section above ## Referenced Scripts. The section documents the four-stage chain (evaluate -> discuss -> roadmap -> plan-phase), the .orchestrator/start-state/STAGE.complete marker shape (ISO-8601 timestamp + stage name body), #Q-3 leave-marker-absent failure semantics, AD-3 four-tier between-stage gate policy (cross-referenced from commands/auto.md to keep the policy single-sourced), idempotency contract on re-invocation, and the post-wiki / post-github ordering invariant. (2) scripts/lifecycle/start.sh receives three additive changes: USAGE literal extends to include [--auto-chain]; AUTO_CHAIN=0 init alongside WITH_GITHUB=0; --auto-chain flag-parser arm (mirrors the existing --with-wiki / --with-github shape); and the chain-driver block lands in main() AFTER the --with-github passthrough gate. The chain-driver creates START_STATE_DIR under PROJECT_DIR/.orchestrator/start-state, resolves auto_proceed from scripts/state/read-config.sh, then for each STAGE in evaluate discuss roadmap plan-phase: if the MARKER exists it prints SKIP: STAGE (marker present) and continues; else applies the AD-3 gate (--yes wins, then auto_proceed:true, then non-TTY M029_AUTOCHAIN_NEEDS_CONFIRMATION refusal exit 2, then TTY prompt); on PROCEED=0 emits START_AUTO_CHAIN_ABORTED stage=STAGE exit 1; invokes the AUTO_CHAIN_STAGE_STUB if set (SC-10 fixture-only escape hatch -- real-mode chain-driver assumes the orchestrator harness invokes the deep skill between successive start.sh --auto-chain calls); on STAGE_RC nonzero emits START_AUTO_CHAIN_STAGE_FAILED stage=STAGE exit 1 with marker absent (#Q-3); on success writes a single-line marker (NOW timestamp + STAGE name) and prints OK: STAGE -> MARKER. After the loop emits START_AUTO_CHAIN_COMPLETE. (3) tools/verify/m029-p03-auto-chain-shape.sh is a 23-assertion AD-19 straight-line shape verifier with parallel pass/fail scalars (MEM001 bash 3.2 safe). It pairs _assert_present_in / _assert_absent_in helpers (MEM004 carve-out for grep -F -q -e bodies) and asserts (a) commands/start.md names --auto-chain, FR-10, the four stages, the marker dir, the .complete suffix, the M029_AUTOCHAIN_NEEDS_CONFIRMATION refusal token, and #Q-3; (b) scripts/lifecycle/start.sh names --auto-chain, AUTO_CHAIN, the four canonical marker filenames (evaluate.complete / discuss.complete / roadmap.complete / plan-phase.complete), FR-10, START_STATE_DIR, #Q-3, M029_AUTOCHAIN_NEEDS_CONFIRMATION, START_AUTO_CHAIN_COMPLETE, and the [--auto-chain] USAGE token; (c) the load-bearing #Q-3 negative assertion that scripts/lifecycle/start.sh contains no .failed token (re-introducing a .failed marker write would silently break the resume convention because re-runs check for .complete only). Final verifier output: SUMMARY: m029-p03-auto-chain-shape.sh pass=23 fail=0. End-to-end smoke (out-of-repo /tmp project): four-stage chain with passing stub writes all four markers in canonical form, prints START_AUTO_CHAIN_COMPLETE, exits 0; second invocation prints SKIP: STAGE (marker present) for all four; failing-stub smoke (stub exits 7 on discuss) leaves discuss.complete absent, prints START_AUTO_CHAIN_STAGE_FAILED stage=discuss on stderr, exits 1, with evaluate.complete still present from the prior successful stage (resume convention works on re-run). Non-regression: T01 verifiers (m029-p03-render-position-live-shape.sh pass=14, m029-p03-display-thresholds-config-shape.sh pass=14) and T02 verifier (m029-p03-auto-preflight-shape.sh pass=14) all green. One drift-from-payload: the payload chain-driver pseudocode used YES_FLAG and REPO_ROOT but the existing in-repo convention is YES (initialized at line 51) and CWD-relative invocation (no REPO_ROOT defined); chain-driver matches the existing convention to keep the diff additive. The marker filename canonical-name comment block (the four .complete filenames enumerated as comments) was added to satisfy the verifier literal-token grep -- the chain-driver builds MARKER via shell expansion which leaves the literal tokens absent from the source unless enumerated; this is a drift-resistant pattern and matches the negative-assertion-verifier discipline established in P02.
