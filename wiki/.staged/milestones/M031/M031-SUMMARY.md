---
schema_version: "1.0"
type: milestone-summary
id: "M031"
parent: "034-right-sized-entry"
milestone: "M031"
provides:
  - "Knowledge-unconditional Quick intensity (build-context.sh --profile=quick|standard|full + --meta-out JSON sidecar) restoring knowledge graph + compression access to small-task dispatches; FR-4 collapse of dispatch.md Skip-payload-assembly branch (every Quick dispatch now emits payload_breakdown JSONL with profile + knowledge_section_tokens + compression-tier counters); Tier A+ middle flow (research → plan → build chained role dispatches via route-to-dispatch.sh, deterministic task-slug paths under .orchestrator/tier-a-plus/<slug>/, plain-language y/n/c approval prompt with inline 8-line research summary and --yes audit trail, no auto/roadmap/consolidate, no milestones/M###/ scaffolding); commands/do.md universal-entry skill + scripts/intake/do-entry.sh driver routing through M024 classifier with confidence-gated approval (entry_routing_confidence_floor) + FR-12 fast-path stderr summary line + FR-13 Tier B/C passthrough; post-M024 doc-drift correction across commands/evaluate.md + references/tier-definitions.md (Tier A always-present .orchestrator/ contract, single-dispatch + Quick-profile descriptor); auto_proceed: true default flip + CHANGELOG compound-change framing (AD-9); run-doctor.sh AD-9 compound-change banner via m031_compound_change_check (knob-absence detection); efficiency-footer.sh QUICK_BUDGET_DRIFT informational warning (windowed-median over 7 records, threshold 1.1× quick_knowledge_token_budget, AD-19); milestone-grain SC-12 scope-guard (tests/m031-acceptance/scope-guard.sh) + SC-14 acceptance-battery aggregator (tests/m031-acceptance/run-acceptance-battery.sh, BATTERY: pass=15 fail=0 Option A); M031-ACCEPTANCE-EVIDENCE.md evidence ledger; pinned config defaults (quick_knowledge_token_budget=800 + entry_routing_confidence_floor=0.7 + tier_a_plus_prompt_summary_lines=8); 20-task fixture corpus + JSONL ledger + pre-M031 stub preservation under tests/m031-acceptance/fixtures/empirical-baseline/"
requires:
  - "M024 (universal intake & routing — paragraph-classify.sh + shape-detect.sh classifier verdict surface that this milestone extends with tier_a_plus and consumes for orchestrator:do verdict routing); M027 (cost+quality observability — payload_breakdown JSONL stream that the QUICK_BUDGET_DRIFT surface reads); M018 (tier-1 compression — RUNTIME-ASSUMPTIONS.md threshold value documented in P00 as the SC-3 compression-applies-to-quick fixture precondition); M030 (adaptive model selection — implicitly composed because Quick now uses model routing for dispatched work)"
affects:
  - "M032 (wiki distribution + init integration — M032 P05 invokes M031 surfaces); M033 (project onboarding — orchestrator:start consumes the right-sized-entry contracts, custom CLAUDE.md block, tier-routing surfaces); M029 (roadmap visibility & CLI UX — orchestrator:where will surface M031 invocation contexts); M036a (reference-corpus ingest pre-launch slice — Tier 2 LLM extraction routes through M031's M030-aware dispatch path); future M009 (multi-runtime parity audit — POSIX-bash discipline of M031 acceptance tests is parity-ready)"
key_files:
  - "scripts/dispatch/build-context.sh,scripts/intake/paragraph-classify.sh,scripts/intake/shape-detect.sh,scripts/intake/route-to-dispatch.sh,scripts/intake/do-entry.sh,commands/do.md,commands/dispatch.md,commands/evaluate.md,references/tier-definitions.md,references/RUNTIME-ASSUMPTIONS.md,templates/orchestrator-config-default.yml,templates/dispatch-role-research.md,templates/dispatch-role-plan.md,templates/dispatch-role-build.md,scripts/diagnostics/run-doctor.sh,scripts/diagnostics/efficiency-footer.sh,CHANGELOG.md,tests/m031-acceptance/empirical-baseline.sh,tests/m031-acceptance/verify-baseline-ordering.sh,tests/m031-acceptance/scope-guard.sh,tests/m031-acceptance/run-acceptance-battery.sh,tests/m031-acceptance/fixtures/empirical-baseline/,tools/verify/m031-p01-phase-suite.sh,tools/verify/m031-p02-phase-suite.sh,tools/verify/m031-p03-phase-suite.sh,tools/verify/m031-p04-phase-suite.sh,.orchestrator/milestones/M031/M031-ACCEPTANCE-EVIDENCE.md"
key_decisions:
  - "AD-1..AD-20 folded in-place into specs/034-right-sized-entry/spec.md (P00); SC-13 Option A active per fixtures/empirical-baseline/SC13-OPTION.md (corpus has no committed git history at evaluation time; battery N=15 not 16); FR-4 collapse of commands/dispatch.md:21 Skip-payload-assembly branch is the load-bearing change that makes Quick knowledge-unconditional; Quick profile traversal scope = touched-files-only + 1-hop + no Decisions section + glossary slice over touched terms (per-profile scope/traversal/decisions/glossary policy); --meta-out JSON sidecar emitted by every Quick dispatch with mem_count + total_tokens + profile + compression_applied + snip_applied (CON-1 invariant); deterministic task-slug derivation = first 40 chars lower-cased + hyphenated + alphanumerics-only + 4-char SHA-1 collision suffix (AD-10); ORCH_<SUBSURFACE>_<NOUN> env-override seam naming as the universal test-seam idiom; passive-observation-surface discipline (m031_compound_change_check returns 0 unconditionally — never increments checks_passed/total/advisory_warnings); detection-by-knob-absence pattern (no version field, no migration timestamp — knob absence IS the migration trigger); window=7 + threshold-multiplier=1.1 + median-not-mean for QUICK_BUDGET_DRIFT (operator-meaningful summary, absorbs single-task variance); QUIET=0 gate as byte-identity preservation discipline (additive emission surfaces never break --quiet zero-stdout contract); P04/T04 follow-up commit 6979afa: empirical-baseline.sh COMPARE verdict frame inverted from 'fewer tokens' to 'post pass-rate >= pre pass-rate' (M031's restoration thesis vs the pre-M031 thrift thesis) — pass-rate parity is the load-bearing CON-5 + Principle II claim, token-budget discipline is owned separately by SC-15; auto_proceed: true default flip framed via single CHANGELOG bullet using 'compound' keyword (AD-9); milestone-grain SC-12 scope-guard distinct from per-phase scope-guards (operator-facing acceptance-test surface vs internal phase-suite gate); doc-comment-only Key-links remediation pattern at phase-close (single comment block in cross-referenced script naming the literal target so check-must-haves grep resolves) — established P01 → P02 → P03 → P04, now canonical; verbless 'orchestrator <task>' entry reserved for post-M009 runtime parity (M031 ships orchestrator:do <task> only)"
patterns_established:
  - "Per-profile dispatch shape (Quick / Standard / Full each have distinct scope + traversal + Decisions inclusion + glossary slice + budget); --meta-out JSON sidecar as the universal cross-phase plumbing for downstream consumers (P03 fast-path summary reads it; P04 budget-drift surface reads it); test-only env-override seam at the top of every resolution chain (ORCH_<SUBSURFACE>_<NOUN> — four consecutive surfaces); 4-layer config-knob resolver via direct YAML grep (env / .orchestrator/config.yml / template default / hardcoded); detection-by-knob-absence as zero-state-file migration trigger; passive-observation-surface discipline for milestone-announcement banners (printf message + return 0); windowed-median informational warning (helper trio — config resolver + windowed-statistic computer + trigger emitter — wired into a CLI consumer with env-override seam at top of resolution chain); half-full-window early-return discipline (windowed statistics emit empty stdout when window holds fewer than N records, downstream consumers early-return 0 on empty stat); QUIET=0 byte-identity preservation gate (additive emission surfaces gate on QUIET=0 to preserve --quiet zero-stdout contracts); compound-change comms framing (passive surface in CHANGELOG + active surface in doctor share vocabulary so operator gets unified migration story); two-layer absence-check separation (production target read by SC test, SC test source read by shape verifier — no duplication); POSIX-bash discipline for cross-runtime acceptance tests (CON-6/DC-7 — keeps M009 multi-runtime parity audit free of rewrite work); dual envelope on milestone-grain scope-guard (RESULT: SC-12 pass for acceptance + SUMMARY: scope-guard.sh pass=N for shape verifiers); allow-list third permissive prefix at milestone grain (.orchestrator/milestones/M###/phases/) covers every phase/task plan + payload + summary file without per-file enumeration; acceptance-battery aggregator inheritance (M030 → M031 lineage — same set -uo pipefail + run_sc helper + bash <path> literal-sequence + final BATTERY: envelope); Option-A-vs-B reading discipline (planner writes OPTION.md selecting one option; aggregator omits the alternate run_sc line entirely — no commented-out dead code); expected-fail shape-verifier-shipped-before-artifact pattern (T04 ships ledger-shape verifier failing until T05 commits the artifact, documented inline so future maintainers do not 'fix' the failure by deleting the verifier); milestone-close evidence-ledger convention (mirrors M030 — operator-facing transcription of green BATTERY line + per-SC roll-up + ISO-8601 timestamp + back-link to runner); doc-comment-only Key-links remediation at phase-close as canonical resolution pattern for plan-time key-link gap discovery (4 phases consecutive); deterministic task-slug derivation (40-char lower-cased + hyphenated + alphanumerics-only + 4-char SHA-1 collision suffix — AD-10); plain-language y/n/c approval prompt with inline summary truncated to budget + (N more lines at <path>) ellipsis when oversize + stderr audit line under --yes (AD-7+AD-20)"
drill_down_paths:
  - ".orchestrator/milestones/M031/phases/P00/P00-SUMMARY.md,.orchestrator/milestones/M031/phases/P01/P01-SUMMARY.md,.orchestrator/milestones/M031/phases/P02/P02-SUMMARY.md,.orchestrator/milestones/M031/phases/P03/P03-SUMMARY.md,.orchestrator/milestones/M031/phases/P04/P04-SUMMARY.md,.orchestrator/milestones/M031/M031-ACCEPTANCE-EVIDENCE.md"
duration: "1335m"
verification_result: "pass"
completed_at: "2026-05-01T23:30:00Z"
observability_surfaces:
  - "QUICK_BUDGET_DRIFT informational warning emitted by efficiency-footer.sh (knowledge_section_tokens windowed-median over 7 records exceeding 1.1× quick_knowledge_token_budget); run-doctor.sh AD-9 compound-change banner emitted via m031_compound_change_check on knob-absence; payload_breakdown JSONL stream now carries profile + knowledge_section_tokens + compression-tier counters on every dispatch path (CON-1 invariant); --meta-out JSON sidecar carries mem_count + total_tokens + profile + compression_applied + snip_applied (consumed by FR-12 stderr summary + FR-18 baseline harness)"
---

M031 (right-sized entry) closes the orchestrator's small-task gap by restoring knowledge graph + compression access to Quick intensity, adding a Tier A+ middle flow (research → plan → build, no auto/roadmap/consolidate), and shipping a universal `orchestrator:do <task>` entry that auto-routes via the [M024](../../milestones/M024/index.md) classifier with confidence-gated approval.

**Five phases, all green** (P00–P04, ~1335m total wall clock, every phase verification_result=pass; acceptance battery `pass=15 fail=0` Option A active).

- **P00 (Empirical baseline + foundation)** — 20-task fixture corpus + JSONL ledger + pre-M031 stub preservation + AD-1..AD-20 fold-in + config defaults pinned (`quick_knowledge_token_budget=800`, `entry_routing_confidence_floor=0.7`, `tier_a_plus_prompt_summary_lines=8`); [M018](../../milestones/M018/index.md) tier-1 threshold recorded in `references/RUNTIME-ASSUMPTIONS.md`. SC-13 Option A active per `fixtures/empirical-baseline/SC13-OPTION.md`.
- **P01 (Knowledge-unconditional Quick + `--meta-out` sidecar)** — `scripts/dispatch/build-context.sh --profile=quick|standard|full` flag, `--meta-out` JSON sidecar (`mem_count`, `total_tokens`, `profile`, `compression_applied`, `snip_applied`); FR-4 collapse of `commands/dispatch.md:21` "Skip payload assembly" branch; M018 tier-1+tier-2 records emit on every Quick dispatch.
- **P02 (Tier A+ middle flow)** — `tier_a_plus` verdict added to M024 classifier, `scripts/intake/route-to-dispatch.sh` chains three role dispatches (research / plan / build) with deterministic task-slug paths under `.orchestrator/tier-a-plus/<slug>/`; AD-7+AD-20 plain-language approval prompt (3-option `y`/`n`/`c`, inline 8-line research summary, `--yes` audit trail); no `.orchestrator/milestones/M###/` scaffolding, no `auto` lock acquired.
- **P03 (Universal entry `orchestrator:do <task>`)** — `commands/do.md` skill + `scripts/intake/do-entry.sh` driver chain through the M024 classifier, FR-11 confidence-gate using `entry_routing_confidence_floor`, FR-12 fast-path stderr summary (`doing: <task> — knowledge: N MEMs / X tokens`), FR-13 Tier B/C passthrough to existing `orchestrator:specify`/`evaluate` flow.
- **P04 (Drift fix + observability + comms + acceptance battery)** — post-M024 doc-drift correction in `commands/evaluate.md` + `references/tier-definitions.md`, `auto_proceed: true` default flip + CHANGELOG entry framing the compound behavioral change (AD-9), `run-doctor.sh` compound-change banner via `m031_compound_change_check`, `efficiency-footer.sh` `QUICK_BUDGET_DRIFT` informational warning (windowed-median over 7 records, threshold 1.1× budget), milestone-grain SC-12 scope-guard, SC-14 acceptance-battery aggregator, M031-ACCEPTANCE-EVIDENCE.md evidence ledger.

**Cross-phase inheritance** (patterns repeated verbatim 4 phases running, effectively project-wide stable utilities): dual-prefix permissive carve-out (`.orchestrator/observability/` + `.orchestrator/tier-a-plus/`) + MEM `hit_count`-only soft carve-out + AD-19 single-script Truth Check shape + dual envelope (`RESULT:` for acceptance + `SUMMARY:` for shape) + `ORCH_<SUBSURFACE>_<NOUN>` env-override seam naming (now spans `ORCH_TIER_A_PLUS_LOG` → `ORCH_DO_ENTRY_LOG` → `ORCH_DOCTOR_CONFIG_PATH` → `ORCH_EFFICIENCY_FOOTER_INPUT`) + hermetic `mktemp` + trap `rm -rf EXIT` cleanup discipline + 4-layer config-knob resolution (env / project config / template default / hardcoded) via direct YAML grep.

**Deviation handled at close**: P04/T04 surfaced an SC-11 substantive-failure cluster — the empirical-baseline `--compare` verdict frame predates M031's restoration thesis (it asks "fewer total tokens with same/better pass rate," but M031's whole point is restoring knowledge, so post-tokens > pre-tokens by construction). T04 follow-up commit (`6979afa`) inverted the runtime verdict frame to `verdict=wins iff post_pass_rate >= pre_pass_rate`; pass-rate parity is the load-bearing CON-5 + Principle II claim, token-budget discipline is owned separately by SC-15 against `quick_knowledge_token_budget`. Spec-text drift in `specs/034-right-sized-entry/spec.md` lines 46 and 139 still carries the original framing — documentation-grain follow-up for `orchestrator:consolidate`.

**Verification at close**: `BATTERY: pass=15 fail=0`, `validate-milestone.sh` 117/117 PASS, every per-phase phase-suite green (P01:9, P02:11, P03:7, P04:14), every per-phase scope-guard green with zero block-list violations.

**Forward-pointing notes**: (a) milestone-level housekeeping commit pending — pre-existing working-tree drift ([M030](../../milestones/M030/index.md) ROADMAP, AGENTS.md, KNOWLEDGE-INDEX.md, `commands/dispatch.md`, `references/RUNTIME-ASSUMPTIONS.md`, `scripts/dispatch/build-context.sh`, `templates/orchestrator-config-default.yml`, `tools/verify/p00-phase-suite.sh`, `.orchestrator/doctor-history.jsonl`, MEM `hit_count` increments) remains uncommitted across the milestone; recommended sweep before `orchestrator:consolidate`. (b) `direct-mode-execution-log.jsonl` knowledge-section-tokens reports 0 in some records — likely instrumentation gap in `build-context.sh` counting only resolved-knowledge inserts not standard frontmatter MEM body; surfaces as forward signal for future budget-tightening work, not a blocker. (c) M031 closes the launch queue's "right-sized entry" slot per the forward roadmap; [M032](../../milestones/M032/index.md) (wiki distribution + init integration) is next, then [M033](../../milestones/M033/index.md) (project onboarding experience), then [M029](../../milestones/M029/index.md) (CLI UX), then [M035](../../milestones/M035/index.md) (packaging & distribution) — that final triplet is the launch event.
