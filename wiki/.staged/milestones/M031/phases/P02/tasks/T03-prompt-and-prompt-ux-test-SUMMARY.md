---
schema_version: "1.0"
type: task-summary
id: "T03"
parent: "P02"
milestone: "M031"
provides:
  - "scripts/intake/lib/tier-a-plus-prompt.sh sourceable + directly-invokable AD-7 + AD-20 Tier A+ approval prompt helper (273 lines bash 3.2 compatible -- _tap_read_summary_lines _tap_resume_marker _tap_emit_audit _tap_emit_prompt_body tier_a_plus_prompt + direct-invocation dispatcher),tests/m031-acceptance/test-tier-a-plus-prompt-ux.sh (203 lines SC-16 acceptance test exercising all seven AD-20 UX-protocol clauses),tools/verify/m031-p02-prompt-shape.sh (16 checks AD-19 single-script Truth Check for the helper),tools/verify/m031-p02-test-tier-a-plus-prompt-ux-shape.sh (12 checks AD-19 single-script Truth Check for the SC-16 test)"
requires:
  - "P02/T01: scripts/intake/shape-detect.sh tier_a_plus verdict + FIXTURE-PROVENANCE.md + tier-a-plus-input.txt on disk,P02/T02: scripts/intake/lib/task-slug.sh sourceable + templates/dispatch-role-research.md (declares .orchestrator/tier-a-plus/<task-slug>/research.md output-path convention) on disk,templates/orchestrator-config-default.yml declares tier_a_plus_prompt_summary_lines: 8 (P00 pinned),scripts/state/read-config.sh present (canonical config-reader; helper resolves the new knob via direct YAML grep equivalent because read-config.sh VALID_KEYS list does not yet whitelist this knob)"
affects:
  - "P02/T04 (router invokes the helper between research and plan dispatches and writes the .session-id sidecar on first session entry per task-slug),P02/T05 (phase-suite aggregates these two new shape verifiers)"
key_files:
  - "scripts/intake/lib/tier-a-plus-prompt.sh,tests/m031-acceptance/test-tier-a-plus-prompt-ux.sh,tools/verify/m031-p02-prompt-shape.sh,tools/verify/m031-p02-test-tier-a-plus-prompt-ux-shape.sh"
key_decisions:
  - "AD-20 mechanical contract implemented with strict BEGIN/END PROMPT BODY sentinels around _tap_emit_prompt_body so the prohibited token assertions (null brace scaffold-placeholder bracket-TODO) gate against the operator-facing prose region only -- not against helper-internal logic such as 2>/dev/null shell-substitution braces or comment text mentioning the prohibited tokens by name (mirrors the M031 P01 inverted-polarity surface-region convention),A4 open question (session-ID sidecar mechanism) answered: a single-line .session-id file under <task-slug>/ holding the session ID; helper reads it and flips the [fresh research from this session] vs [resume from prior research] marker on match/mismatch (T04 router writes the sidecar on first session entry),tier_a_plus_prompt_summary_lines resolution path -- direct YAML grep against .orchestrator/config.yml then templates/orchestrator-config-default.yml then hardcoded P00 default 8 (read-config.sh VALID_KEYS list does not yet whitelist this knob; the equivalent permitted by the task-plan note keeps T03 scope bounded -- P04 or M032 may extend VALID_KEYS as a separate single-line amendment),--no-prompt-mode y|n|c test-only flag added to bypass the read -r -n 1 -t 60 interactive path so the SC-16 test exercises every UX clause deterministically without driving stdin (production default stays the interactive read path),60-second read timeout with empty/EOF/timeout all collapsing to the c cancel default per AD-20 clause 3 default-on-no-answer requirement,SC-16 test constructs prohibited byte patterns at runtime via printf octal escapes (173 175 133) so the test source itself does not embed the literal forbidden bytes -- same source-side discipline the helper applies to its own prompt body,helper writes the audit line research: <path> to stderr in BOTH --yes and interactive modes so callers grep stderr regardless of mode (path emission discipline AD-20 clause 7),AD-19 single-script-file Truth Check shape preserved across both new verifiers (no inline compound bash no process substitution no plain subshells in verifier bodies)"
patterns_established:
  - "bracketed prompt-body sentinel pattern (BEGIN PROMPT BODY / END PROMPT BODY) for source-side enforcement of operator-facing prose token discipline (verifier extracts the region with awk and asserts the prohibited tokens do not appear inside),test-only --no-prompt-mode bypass for interactive UX surfaces (test exercises every UX clause deterministically without driving stdin; production keeps the interactive read path),direct-YAML-grep config knob resolution as a permitted equivalent when the canonical reader VALID_KEYS list lags behind a new knob (4-layer precedence preserved: env-var implicit / .orchestrator/config.yml / templates default / hardcoded P00 default),session-ID sidecar pattern under <task-slug>/.session-id (single-line file holding session ID; presence + match flips a UX marker; non-state-machine scratch artifact under .orchestrator/tier-a-plus/ permissive prefix),runtime construction of prohibited byte patterns via printf octal escapes for D020/CON-7 self-application (test/verifier source itself does not embed the literal forbidden bytes)"
drill_down_paths:
  - ".orchestrator/milestones/M031/phases/P02/tasks/T03-prompt-and-prompt-ux-test-PLAN.md"
duration: "120m"
verification_result: "pass"
completed_at: "2026-05-01T19:15:19Z"
---

T03 ships the AD-7 + AD-20 Tier A+ approval prompt -- the single load-bearing UX surface for the entire Tier A+ middle flow. scripts/intake/lib/tier-a-plus-prompt.sh (273 lines bash 3.2 compatible per MEM001) implements the seven AD-20 mechanical contract clauses end-to-end. The helper is sourceable + directly invokable; CLI surface is --research-path REQUIRED --task-slug REQUIRED [--yes] [--session-id ID] [--no-prompt-mode y|n|c]. Exit codes 0 (proceed to plan) / 1 (re-run research) / 2 (abort flow OR timeout/EOF/empty-line default).

Clause-by-clause: clause 1 plain-language framing isolates the operator-facing prose into _tap_emit_prompt_body delimited by # BEGIN PROMPT BODY and # END PROMPT BODY sentinels (the verifier extracts that region with awk and asserts ZERO occurrences of the literal token null the open-brace and close-brace bytes and the CON-7 scaffold-placeholder bracket-TODO byte pattern); clause 2 inline summary reads N=tier_a_plus_prompt_summary_lines from .orchestrator/config.yml then templates/orchestrator-config-default.yml then hardcoded P00 default 8 (direct YAML grep mirroring read_yaml_value in scripts/state/read-config.sh because the canonical reader VALID_KEYS list does not yet whitelist this knob -- equivalent resolution per task-plan note); clause 3 three named single-keystroke options y/n/c plus default-on-no-answer = c via the read -r -n 1 -t 60 path (empty/EOF/timeout all collapse to c); clause 4 ellipsis (M more lines at PATH) emitted only when M = total_lines - inline > 0; clause 5 resume-vs-rerun marker derived from the .session-id sidecar at <task-slug>/.session-id (sidecar match -> [fresh research from this session]; sidecar mismatch or missing -> [resume from prior research] -- the helper reads the sidecar; T04 router writes it on first session entry per AD-20 sidecar mechanism resolving open question A4); clause 6 --yes mode skips the prompt and emits a single research: <path> stderr audit line; clause 7 path emission discipline -- the audit line emits in BOTH --yes and interactive modes so a single grep against stderr is sufficient regardless of mode.

D020/CON-7 token hygiene: the prompt body region is provably clean of null brace and the bracket-TODO byte pattern (verified by grep against the awk-extracted region); the helper file as a whole is also clean of the bracket-TODO byte pattern (file-wide assertion). The SC-16 acceptance test constructs the prohibited byte patterns at runtime via printf escape sequences (octal 173 / 175 for braces; octal 133 + literal TODO + colon + space for the scaffold pattern) so the test source itself does not embed the prohibited literals -- mirrors the source-side discipline the helper applies to its own prompt body.

tests/m031-acceptance/test-tier-a-plus-prompt-ux.sh (203 lines) is the SC-16 acceptance test. It stages a 20-line fixture under a per-pid sandbox at $TMPDIR/m031-p02-t03-sc16-$$/.orchestrator/tier-a-plus/sc16-test/research.md drives both --yes and interactive (via --no-prompt-mode y) paths and asserts every AD-20 clause: --yes exit 0 + exactly one research: <path> stderr audit line; interactive exit 0 + first 8 fixture lines verbatim + literal substring (12 more lines at <fixture-path>) + all three option labels + resume-or-fresh marker + zero null + zero brace + zero scaffold-placeholder. RESULT: SC-16 pass envelope per the M031 P01 SC-* convention.

tools/verify/m031-p02-prompt-shape.sh (16 checks) and tools/verify/m031-p02-test-tier-a-plus-prompt-ux-shape.sh (12 checks) are the AD-19 single-script Truth Check verifiers for the helper and SC-16 test respectively. Both emit SUMMARY: <name> pass=N fail=M envelope per the M031 P01 shape-verifier convention.

Verification results: m031-p02-prompt-shape pass=16 fail=0; m031-p02-test-tier-a-plus-prompt-ux-shape pass=12 fail=0; SC-16 acceptance test RESULT: SC-16 pass exit 0. scripts/intake/route-to-dispatch.sh is byte-identical to its pre-T03 state (T04 owns the router amend per AD-20 sidecar-write contract). scripts/intake/shape-detect.sh and scripts/intake/paragraph-classify.sh are untouched (T01 owns those edits). scripts/intake/lib/task-slug.sh is untouched (T02 owns that file). templates/dispatch-role-*.md are untouched (T02 owns those files). templates/orchestrator-config-default.yml is untouched by T03 (P00 owns the knobs). The SC-12 block-list (knowledge/** scripts/cost/ scripts/dispatch/adapters/router/ scripts/auto/loop/) is fully respected.

T03 leaves the prompt helper on disk plus a green SC-16 acceptance test. T04 builds on T03 by amending scripts/intake/route-to-dispatch.sh to invoke the helper between the research and plan dispatches and to write the .session-id sidecar on first session entry per <task-slug>. Open question A4 (session-ID sidecar mechanism) is now answered by T03's implementation: the sidecar is a single-line file at <task-slug>/.session-id holding the session ID; its presence and matching value flips the marker between [fresh research from this session] and [resume from prior research]. Open questions A2 (router CLI surface) A3 (SC-6 stub vs real dispatch) and A5 (.orchestrator/tier-a-plus/ allow-list prefix) remain deferred to T04 and T05 per the strict T01 to T05 dependency chain.
