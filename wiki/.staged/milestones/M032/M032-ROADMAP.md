---
schema_version: "1.0"
type: roadmap
milestone: "M032"
feature_ref: "035-wiki-distribution-init-integration"
feature_spec: "specs/035-wiki-distribution-init-integration/spec.md"
vision: "M032 turns wiki distribution and `init` integration into managed surface — `packaging/bundle/manifest.yml` `project_assets:` schema replaces the unmanaged `RUNTIME_DIRS` bulk-copy, `orchestrator:wiki-init` produces a working consumer-side mkdocs Material wiki with three composable scopes (default / `--with-giscus` / `--deploy`), `wiki/glossary.md` lands as the load-bearing M033 grilling-protocol surface, and FR-6 self-application keeps the orchestrator's own wiki serving continuously during paired development with M033."
tier: "C"
created_at: "2026-05-03"
updated_at: "2026-05-03"
paired_with: "M033"
---

## Phases

- [x] **P01**: Managed project-asset distribution + staged-dirs collision invariant — "Running `bash packaging/install/install-claude-code.sh --project-dir tests/fixtures/m032-fresh-project-fixture/` against a fresh fixture populates all four runtime dirs (`commands/`, `scripts/`, `references/`, `templates/`) with file counts matching pre-M032 behavior; a second run is byte-identical (idempotent / [M025](../../milestones/M025/index.md) pinned-sha extension); `<fixture>/.orchestrator/installed-files.txt` exists with a `mode:` field on every entry; on a POSIX host with `~/.claude/orchestrator-runtime/<version>/` populated, an entry declared `mode: symlink` resolves to the runtime cache and the consumer's git history shows zero framework files; on Windows without symlink privilege, `mode: symlink` exits non-zero with a clear `POSIX-only in v1` diagnostic; against a pre-M032 consumer (no `installed-files.txt`), the bootstrapping oracle (per FR-22 / MIT-006) treats existing `target:` paths as framework-installed and writes `installed-files.txt` reflecting the bootstrapped state without blocking; an operator-owned file colliding with a `project_assets:` `target:` path fails closed naming both sides of the collision; `bash tests/m032-acceptance/p01-managed-bundle-shape.sh`, `p01-symlink-mode.sh`, and `p01-staged-dirs-collision.sh` exit 0."
  - Risk: high
  - Depends: none
  - Boundary Map:
    - Produces: amended `packaging/bundle/manifest.yml` with `project_assets:` section (FR-1 — `source:` / `target:` / `mode: copy|symlink` per entry); amended `packaging/install/install-{claude-code,codex,cursor}.sh` reading from `project_assets:` instead of hardcoded `RUNTIME_DIRS` loop at `install-claude-code.sh:287-330` (FR-2 — byte-identical at `mode: copy` per CON-4); per-asset mode handler in installer for `mode: copy` and `mode: symlink` POSIX-only with Windows fail-closed (FR-3); `.orchestrator/installed-files.txt` format extended with per-asset `mode:` field (FR-4 — line-oriented, human-readable, downstream uninstall-compatible); collision-invariant + dual-oracle hierarchy implementation per FR-22 / MIT-003 (tracking-file oracle primary; bootstrapping oracle for pre-M032 consumers per MIT-006; operator-owned oracle tertiary); `tests/fixtures/m032-fresh-project-fixture/` (referenced by P02 + P03 + P04 tests); `tests/m032-acceptance/p01-managed-bundle-shape.sh` (SC-1 — verifies FR-1 + FR-2 + FR-4); `tests/m032-acceptance/p01-symlink-mode.sh` (SC-2 — verifies FR-3 POSIX symlink resolution + Windows fail-closed); `tests/m032-acceptance/p01-staged-dirs-collision.sh` (SC-10 — verifies FR-22 dual-oracle hierarchy)
    - Consumes: nothing (foundation phase per AD-6 — no P00 empirical-baseline phase; the empirical signal for the 11 mitigations is the 2026-04-28 PBJ bootstrap session evidence already on disk)

- [x] **P02**: `wiki-init` default scope + glossary surface + `init --with-wiki` passthrough + paired-launch seam scripts — "Running `bash scripts/lifecycle/wiki-init.sh --project-dir tests/fixtures/m032-fresh-project-fixture/` against a fixture with the project's git remote pointing at `<owner>/<repo>` copies `wiki/mkdocs.yml` from the bundle, sed-substitutes the four templated fields (`site_name=<repo>` default, `site_url=https://<owner>.github.io/<repo>/`, `repo_url=https://github.com/<owner>/<repo>`, `site_description` empty default), leaves the Giscus partial in placeholder state, and `bash <fixture>/scripts/wiki/wiki-serve.sh` returns HTTP 200 at `:8000`; running the same command against the orchestrator repo itself (`--project-dir .` per AD-5 / MIT-002 self-application loop) resolves placeholders to the orchestrator's own identity (`site_name: orchestrator`, `repo_url: https://github.com/Build-Fractal/orchestrator`) and the orchestrator's own `bash scripts/wiki/wiki-serve.sh` continues to return HTTP 200 (FR-6 self-application loop closure); when `python3` is missing, the probe exits non-zero with platform-aware diagnostic (`brew install python3` on darwin / `apt install python3` on linux) and writes nothing to disk (FR-12); running `orchestrator:init --with-wiki` in a fresh project pass-throughs to `wiki-init` with the sequential atomicity contract per FR-11 / MIT-011 (init outputs preserved on `wiki-init` failure; exit code is `wiki-init`'s exit code; non-zero → `init-complete, wiki-pending` partial state); `wiki/glossary.md` exists as a path-convention surface and `wiki-scan-sources.sh --include-glossary` (default on) prepends Glossary as the second top-level nav entry after Constitution (FR-15); `bash scripts/knowledge/lookup-mems.sh --kind=glossary` synthesizes records with stable IDs and respects [M031](../../milestones/M031/index.md)'s Quick traversal contract — Quick payloads inject only touched terms (per inline definition in FR-16 / MIT-010), Standard/Full inject the full glossary, and Quick falls back to NO terms when neither task description nor file-change set is available; the three paired-launch seam scripts at `tests/paired-m032-m033/seam-{A,B,C}.sh` are present and exit 0 — Seam-A asserts the `project_assets:` schema shape [M033](../../milestones/M033/index.md) consumes, Seam-B asserts the `--with-wiki` failure-propagation contract (FR-11 atomicity per MIT-011), Seam-C asserts the `wiki/glossary.md` format invariant (alphabetized term entries with one-line definitions and at most two-line elaborations per US-6); `bash tests/m032-acceptance/p02-wiki-init-default-scope.sh` and `tests/m032-acceptance/p0X-glossary-surface.sh` exit 0."
  - Risk: high
  - Depends: P01
  - Blocked by: M033/P05 START is gated on this phase closing at least 1 week before M033/P05 begins (per #Q-C). M033/P05 currently targets the 2026-05-15 PBJ pilot deadline (Assumption A-3); deviation requires explicit re-coordination of both roadmaps.
  - Boundary Map:
    - Produces: `commands/wiki-init.md` (new command surface per FR-5); `scripts/lifecycle/wiki-init.sh` (FR-5 — default invocation reads wiki tooling from `project_assets:` entries shipped by P01, sed-substitutes `{{site_name}}` / `{{site_description}}` / `{{site_url}}` / `{{repo_url}}` from git remote, leaves Giscus partial in placeholder state, probes `python3` + `pip3`); amended `wiki/mkdocs.yml` with `{{site_name}}` / `{{site_description}}` / `{{site_url}}` / `{{repo_url}}` placeholders (FR-6 — replaces hard-coded `Build-Fractal/orchestrator` site identity); FR-6 self-application loop closure (per AD-5 / MIT-002 — `bash scripts/lifecycle/wiki-init.sh --project-dir .` runs against the orchestrator repo itself within the FR-6 implementation task, resolving all `{{…}}` tokens with the orchestrator's own identity, and the orchestrator repo's wiki-serve continues to function for the duration of paired M033 development); amended `commands/init.md` + `scripts/lifecycle/init-project.sh` adding `--with-wiki [--with-giscus] [--deploy]` flag chain with sequential-atomicity model (FR-11 / MIT-011 — init outputs preserved on `wiki-init` failure; exit code is `wiki-init`'s; non-zero → `init-complete, wiki-pending` partial state); `python3` + `pip3` toolchain probe with platform-aware diagnostics and `--auto-pip` opt-in escape hatch per #Q-2 (FR-12 — print-and-exit default); `wiki/glossary.md` path-convention surface (FR-15 — canonical project-glossary path; alphabetized term entries with one-line definitions and at most two-line elaborations per US-6 format invariant); amended `scripts/wiki/wiki-scan-sources.sh` adding `--include-glossary` toggle default-on (FR-15 — additive flag, no overlap with P04's enumeration extensions); amended `scripts/wiki/wiki-generate-nav.sh` placing Glossary as second top-level nav entry after Constitution (FR-15 — additive placement, no overlap with P03's region split); `scripts/knowledge/lookup-mems.sh --kind=glossary` adapter (FR-16 / MIT-010 — synthesizes records with stable IDs derivable from term name; honors M031 Quick/Standard/Full traversal contract; "touched" inline-defined as exact-or-stemmed task description match OR file-change-set inclusion, with safe-default-no-terms fallback when neither signal is available); `tests/m032-acceptance/p02-wiki-init-default-scope.sh` (SC-3 — verifies FR-5 + FR-6 templating fired, `wiki-serve.sh` succeeds at `:8000`, HTTP `GET /` returns valid HTML); `tests/m032-acceptance/p0X-glossary-surface.sh` (SC-7 — verifies FR-15 + FR-16 glossary enumerated, knowledge adapter emits records, Quick-profile injection respects traversal contract); `tests/paired-m032-m033/seam-A.sh` (#Q-B — asserts shared install-bundle surface invariant: `project_assets:` schema shape M033 consumes for its 7-new-commands + 6-new-scripts shipping); `tests/paired-m032-m033/seam-B.sh` (#Q-B — asserts `--with-wiki` failure-propagation contract: FR-11 atomicity per MIT-011, exit-code-of-wiki-init-not-init, partial-state preservation, M033/P01..P04 stub-mode compatibility per M033-MIT-001); `tests/paired-m032-m033/seam-C.sh` (#Q-B — asserts `wiki/glossary.md` format invariant: alphabetized term entries, one-line definitions, at-most-two-line elaborations; both M032 (path owner) and M033 (primary writer via grilling-shell FR-18) treat as shared invariant)
    - Consumes: P01 `project_assets:` schema (FR-1 — wiki tooling ships via `project_assets:` entries, not as a parallel install path); P01 installer mode handler (FR-3 — wiki tooling installs at `mode: copy` default); P01 `installed-files.txt` per-asset mode tracking (FR-4 — wiki/glossary.md and wiki/* paths declared in tracking file); P01 collision invariant (FR-22 — wiki/glossary.md `target:` path declared in manifest, asserted against operator-owned oracle); `tests/fixtures/m032-fresh-project-fixture/` (P01 fixture reused for default-scope test)

- [x] **P03**: `--with-giscus` + `--deploy` + custom-nav region + throwaway-GH-fixture protocol — "Running `bash scripts/lifecycle/wiki-init.sh --with-giscus --repo fixture-owner/fixture-repo --category 'Wiki Comments' --project-dir <fixture>` (with `giscus-ids-from-gh.sh` mocked for deterministic fixture IDs) substitutes `{{giscus_repo}}` / `{{giscus_repo_id}}` / `{{giscus_category}}` / `{{giscus_category_id}}` placeholders in the Giscus partial under `<fixture>/wiki/overrides/` so no orchestrator values remain; running `bash scripts/diagnostics/wiki-giscus-config-check.sh --project-dir <fixture>` exits 0; against an invalid `--repo` flag, the command fails fast leaving the partial in placeholder state with exit code non-zero and `integration-giscus-config-failed: <reason>` diagnostic; running `bash scripts/lifecycle/wiki-init.sh --with-wiki --with-giscus --deploy --project-dir <fixture> --repo <ts>-m032-fixture` against a throwaway GH repo (created via `gh repo create <ts>-m032-fixture --private`, deleted via `gh repo delete <ts>-m032-fixture --yes`) per AD-7 and `tests/m032-acceptance/throwaway-fixture-protocol.md` enables Discussions, creates `gh-pages` branch, sets Pages source to `gh-pages` root, prints the live URL, and the URL responds 200 within `M032_DEPLOY_PROPAGATION_TIMEOUT` (default 90s) with the served HTML containing the per-fixture `data-repo` attribute (proves the partial templating loop closes end-to-end); the read-before-write Pages guard per MIT-007 detects existing-Pages-with-incompatible-source and exits non-zero with the documented diagnostic unless `--force-pages-reconfigure` is passed; after each deploy step succeeds, `.orchestrator/execution-log.jsonl` contains a structured `wiki-deploy-mutation` record per MIT-008 (`{event_type, timestamp, repo, mutations: [...], result}`) appended before the live URL prints to stdout; the cwd-vs-`repo_url:` sanity gate (Finding J counter-pattern) detects a mismatch and exits non-zero with a cross-project-hazard diagnostic; running `bash scripts/wiki/wiki-generate-nav.sh --root .` against an `mkdocs.yml` with both `# >>> auto-nav` ... `# <<< auto-nav end` and `# >>> custom-nav` ... `# <<< custom-nav end` regions populated rewrites the auto-nav region and leaves the custom-nav region byte-identical; against a legacy `# >>> M012-P01 nav` block with non-empty content, the migration moves that content verbatim into the new `# >>> custom-nav` region per MIT-005 and emits a stdout diagnostic naming the count of preserved entries (no silent data loss for the named PBJ pilot population); teardown via `gh repo delete <fixture-name> --yes` leaves no orphan branches in the orchestrator repo and no leaked `.orchestrator/` files in the fixture; `bash tests/m032-acceptance/p02-wiki-init-with-giscus.sh`, `p03-wiki-init-deploy-live.sh`, and `p02-wiki-generate-nav-custom-region.sh` exit 0 (or POSIX exit 77 / `SKIP_REASON` in the SC-5 deploy case if `gh` unauthenticated in CI, distinct from pass per MIT-001)."
  - Risk: high
  - Depends: P02
  - Boundary Map:
    - Produces: amended `wiki/overrides/` Giscus partial replacing hard-coded `data-repo` / `data-repo-id` / `data-category` / `data-category-id` with `{{giscus_repo}}` / `{{giscus_repo_id}}` / `{{giscus_category}}` / `{{giscus_category_id}}` placeholders (FR-7); amended `scripts/lifecycle/wiki-init.sh` adding `--with-giscus --repo <owner>/<repo> --category <name>` scope invoking `scripts/diagnostics/giscus-ids-from-gh.sh`, parsing the four `export GISCUS_*` lines, sed-substituting the partial, running `wiki-giscus-config-check.sh` as post-step verifier (FR-8); amended `scripts/lifecycle/wiki-init.sh` adding `--deploy` scope per FR-9 / MIT-007 / MIT-008 (sequential `gh api PATCH /repos/<owner>/<repo>` with `has_discussions=true`; `bash <fixture>/scripts/wiki/wiki-deploy.sh`; read-before-write Pages guard via `gh api GET /repos/<owner>/<repo>/pages` with explicit fail-closed-on-incompatible-source diagnostic and `--force-pages-reconfigure` escape hatch; `gh api PUT /repos/<owner>/<repo>/pages` with `source: { branch: gh-pages, path: / }` for compatible-or-absent state; live URL print; structured `wiki-deploy-mutation` JSONL audit-trail record appended before live URL prints, satisfying Constitution VI per MIT-008); amended `scripts/wiki/wiki-deploy.sh` adding `cd "$PROJECT_ROOT"` plus `repo_url:`-vs-`git remote get-url origin` sanity gate failing closed on mismatch (FR-10 / Finding J counter-pattern); `references/installation.md` documenting the `--with-<feature>` flag pattern as the precedent for future progressive opt-ins (FR-13 — default-off, independently composable; e.g. `--with-github-integration`, `--with-design-layer`); amended `scripts/wiki/wiki-generate-nav.sh` splitting nav block into `# >>> auto-nav` ... `# <<< auto-nav end` (regenerated) and `# >>> custom-nav` ... `# <<< custom-nav end` (preserved verbatim) regions, with non-empty legacy content branch per MIT-005 (move legacy `# >>> M012-P01 nav` non-empty content verbatim to custom-nav region with stdout diagnostic naming preserved-entry count) and empty-custom self-healing per US-5 AS-3 (FR-14); `tests/m032-acceptance/throwaway-fixture-protocol.md` per AD-7 (explicit `gh repo create <ts>-m032-fixture --private` + `gh repo delete <ts>-m032-fixture --yes` teardown protocol with no-orphan-branches + no-leaked-`.orchestrator/`-files verification before SC-5 considered shippable); `tests/m032-acceptance/p02-wiki-init-with-giscus.sh` (SC-4 — verifies FR-7 + FR-8 partial substituted with fixture IDs, `wiki-giscus-config-check.sh` exits 0; mocks `giscus-ids-from-gh.sh` to avoid GraphQL rate-limit dependence in CI per Edge Cases); `tests/m032-acceptance/p03-wiki-init-deploy-live.sh` (SC-5 — verifies FR-9 + FR-10 + FR-21 against throwaway GH repo per CON-5; emits POSIX exit 77 / `SKIP_REASON` distinct from pass exit 0 per MIT-001 when `gh` unauthenticated); `tests/m032-acceptance/p02-wiki-generate-nav-custom-region.sh` (SC-6 — verifies FR-14 auto-nav rewritten, custom-nav byte-preserved, legacy markers migrated with non-empty-content branch behavior); `M032_DEPLOY_PROPAGATION_TIMEOUT` config knob (default 90s, deliberate accommodation of GH Pages propagation latency per Assumption A-2)
    - Consumes: P02 `commands/wiki-init.md` surface (--with-giscus and --deploy scopes layer onto the default-scope command); P02 `wiki/mkdocs.yml` templating (FR-6 — Giscus partial templating layers onto same parameterized base); P02 `init --with-wiki` passthrough (FR-11 — `--with-wiki --with-giscus --deploy` flag chain extends FR-11 sequential atomicity model); P02 paired-launch seam-B script (Seam-B exercises the FR-11 failure-propagation contract that P03's `--deploy` failure modes feed into); P01 `tests/fixtures/m032-fresh-project-fixture/` (deploy test reuses fixture); P02 `wiki-init.sh` python toolchain probe (FR-12 — `--deploy` requires probe to have passed)

- [x] **P04**: Scanner extensions + code-shorthand decorator + acceptance battery + milestone close — "Running `bash scripts/wiki/wiki-scan-sources.sh --root .` against the orchestrator's own repo enumerates `.orchestrator/proposals/*.md` (currently 11+ entries) under a top-level `Proposals` nav section per #Q-5, each rendered with its frontmatter `stage:` badge (`stub | brief | specified | active | closed`) or `stage: unknown` for entries lacking the field (no exclusion); against a consumer config with `wiki.extra_dirs: [specs/, decisions/]`, the scanner enumerates `<consumer>/specs/**/*.md` and `<consumer>/decisions/*.md` under operator-configurable nav sections (with no extra-dirs section emitted when config absent); the scanner enumerates `.orchestrator/knowledge/*.md` flat files under `Knowledge — Flat` separate from categorized `knowledge/<category>/MEM*.md` entries; running the build-time code-shorthand decorator against a fixture page containing three known codes and one unknown code rewrites the three known codes as `CODE (Title)` linked (first occurrence), link-only on subsequent occurrences, and leaves the unknown code byte-identical; running `bash scripts/diagnostics/run-doctor.sh` against an `init --with-wiki --with-giscus --deploy`-bootstrapped fixture project exits 0 with no `dead-infrastructure` or `unreferenced-asset` warnings per Constitution VIII; `bash scripts/wiki/wiki-serve.sh` returns HTTP 200 at `:8000` against the orchestrator repo's own `wiki/` source tree (closes the FR-6 self-application loop per MIT-002); `jq '.[] | select(.event_type == \"wiki-deploy-mutation\")' .orchestrator/execution-log.jsonl` against the fixture project returns ≥ 1 record per MIT-008 (Constitution VI); running `bash tests/m032-acceptance/run-acceptance-battery.sh` outputs `BATTERY: pass=10 skip=1 fail=0` (three-category format per MIT-001 — pass counter NOT incremented on SC-5's exit-77 SKIP_REASON; skip=1 acceptable only with signed attestation in `M032-SUMMARY.md` declaring SC-5 was executed in an authenticated environment and produced pass); running `bash scripts/verify/validate-milestone.sh M032` reports `PASS` for every entry with final line `M032: NNN/NNN PASS` where NNN ≥ the per-script assertion-group sum derived in this phase's planning task (NNN floor non-negotiable per MIT-004; planner setting NNN below 10 violates SC-13); the `M032-VALIDATED` marker file is present at `.orchestrator/milestones/M032/`, `M032-SUMMARY.md` is present and references SC-1..SC-13 verdicts (with signed-attestation block if SC-12 closed at `skip=1`), and one `unit_close` record at milestone-grain is appended to `.orchestrator/execution-log.jsonl`."
  - Risk: medium
  - Depends: P02, P03
  - Boundary Map:
    - Produces: amended `scripts/wiki/wiki-scan-sources.sh` adding `.orchestrator/proposals/*.md` enumeration with per-entry `stage:` frontmatter badge derivation (FR-17 — `stub | brief | specified | active | closed | unknown`); same script adding `wiki.extra_dirs:` config honor for consumer-configured extra paths (FR-18 — empty-or-absent config produces no extra-dirs section, no false-positive); same script adding `.orchestrator/knowledge/*.md` flat-file enumeration under `Knowledge — Flat` nav section (FR-19 — separate from categorized `knowledge/<category>/MEM*.md`); build-time code-shorthand decorator script (path TBD at planning, US-8 P3 stub-shaped framing per #Q-5 + Principle XIV — surface exists, polish deferred to post-launch wiki-UX-deep proposal) regex-scanning rendered pages for `[A-Z]{2,4}-\d+`, `M\d{3}`, `DR-[A-Z]+-\d+`, `AP-\d+` patterns, resolving against glossary (US-6 surface from P02) or codebase definition fallback, rewriting first-occurrence-per-page as `CODE (Title)` linked with subsequent occurrences link-only, leaving unresolved patterns byte-identical (FR-20 — Finding G readability blocker partial address); `tests/m032-acceptance/p0X-scanner-extensions.sh` (SC-8 — verifies FR-17 + FR-18 + FR-19 against orchestrator's own repo and a fixture: 11+ proposals enumerated with stage badges; `wiki.extra_dirs:` honored; flat-knowledge entries surfaced); `tests/m032-acceptance/p0X-code-decorator.sh` (SC-9 — verifies FR-20 known-codes-decorated, unknown-codes-byte-identical, second-occurrence-link-only); `tests/m032-acceptance/sc11-doctor-no-warnings.sh` (SC-11 amended per MIT-002 + MIT-008 — verifies `run-doctor.sh` against bootstrapped fixture exits 0 with no `dead-infrastructure` or `unreferenced-asset` warnings; `wiki-serve.sh` returns HTTP 200 against orchestrator's own `wiki/` source tree, closing FR-6 self-application loop; `wiki-deploy-mutation` JSONL record present after `--deploy` run); `tests/m032-acceptance/run-acceptance-battery.sh` (SC-12 amended per MIT-001 — three-category exit-code aggregator chaining SC-1..SC-11 in sequence, distinguishing POSIX exit 77 SKIP_REASON from pass exit 0, emitting `BATTERY: pass=10 skip=1 fail=0` shape; modeled on `tests/m030-acceptance/run-acceptance-battery.sh` and `tests/m031-acceptance/run-acceptance-battery.sh` with three-category extension); `M032-VALIDATED` marker file at `.orchestrator/milestones/M032/` (SC-14 — creation conditioned on SC-12 `skip=0` outcome OR signed attestation in `M032-SUMMARY.md`); `M032-SUMMARY.md` referencing SC-1..SC-13 verdicts including signed-attestation block when SC-12 closed at `skip=1` (SC-14 + AD-2 milestone-close discipline mirroring M030/M031); per-script-assertion-group derivation list summing to NNN (SC-13 per MIT-004 — the audit-trace `validate-milestone.sh M032` reports against; planner-task-breakdown deliverable, NNN ≥ 10 floor non-negotiable); milestone-grain `unit_close` JSONL record appended to `.orchestrator/execution-log.jsonl` (SC-14)
    - Consumes: P02 `wiki/glossary.md` surface (FR-20 decorator falls back to glossary lookup before codebase scan; SC-9 fixture writes a glossary mapping per US-8 Independent Test); P02 `commands/wiki-init.md` + FR-6 self-application loop (SC-11 includes `wiki-serve.sh` HTTP 200 check against orchestrator's own `wiki/` source tree); P02 `--include-glossary` toggle on `wiki-scan-sources.sh` (FR-17 / FR-18 / FR-19 enumerations are additive — they extend the same script without overlapping with FR-15's glossary-prepend behavior; plan-phase MUST mark scanner amendments as additive, no overlapping flags); P03 `wiki-deploy-mutation` JSONL records (SC-11 verifies presence after deploy run); P03 throwaway-GH-fixture protocol (SC-12 battery aggregator runs SC-5 against the same protocol); every prior phase's acceptance script (battery aggregator chains them all — SC-1, SC-2, SC-3, SC-4, SC-5, SC-6, SC-7, SC-10 from P01 + P02 + P03)

## Cross-Cutting Concerns

- **CON-3 paired-launch seams (Seam-A / Seam-B / Seam-C)** — affects P01, P02, P03. P01 produces the Seam-A surface (`project_assets:` schema in `manifest.yml`). P02 ships all three seam-test scripts at `tests/paired-m032-m033/seam-{A,B,C}.sh` per #Q-B (alongside `wiki-init` shipping) so they are exercisable from M033's first paired-development commit. P03's `--with-wiki` deploy-failure-mode work feeds Seam-B's failure-propagation assertions. CI on both M032 and M033 milestone branches blocks on any seam failure (paired-launch invariant per AD-4). Plan-phase for P02 MUST include the three seam-test scripts as named deliverables — they are not a P03 or P04 item.
- **CON-4 byte-identical migration (CON-4 + MIT-006 pre-M032 bootstrapping)** — affects P01 only. The hardcoded `RUNTIME_DIRS → project_assets:` migration MUST produce byte-identical output for every existing consumer at `mode: copy`. The bootstrapping policy (FR-22 oracle 2 / MIT-006) is the load-bearing test of this discipline against pre-M032 consumers without `installed-files.txt`. Downstream phases (P02, P03, P04) consume the migrated bundle without modification — none re-litigates the migration contract.
- **CON-5 live-fixture-discipline (M013/[M014](../../milestones/M014/index.md) counter-pattern)** — affects P03 (writes the live test) + P04 (battery aggregator chains it). FR-21's deploy end-to-end test MUST hit a *live throwaway GH repo* per AD-7's `tests/m032-acceptance/throwaway-fixture-protocol.md` with explicit `gh repo create <ts>-m032-fixture --private` + `gh repo delete <ts>-m032-fixture --yes` teardown verification. Synthetic stubs in this seam re-create the M013/M014 verification-skip failure mode CON-5 was written to prevent. The MIT-001 three-category battery format (POSIX exit 77 / SKIP_REASON distinct from pass exit 0) + MIT-001 signed-attestation gate are the explicit closure of the M013/M014 failure mode at milestone close time.
- **CON-6 glossary-load-bearing-for-M033** — affects P02 only. FR-15 (glossary path convention + scanner integration) and FR-16 (knowledge adapter) MUST land in P02 — deferring to P03 or P04 breaks the M033/P05 grilling-protocol contract. Phase placement was resolved at clarify-2026-05-03 via #Q-4. CON-6's invariant is non-negotiable: glossary surface ships in M032, not as a post-launch fast-follow.
- **AD-5 / MIT-002 FR-6 self-application loop** — affects P02 only. The FR-6 `mkdocs.yml` templating delivery PR MUST include running `bash scripts/lifecycle/wiki-init.sh --project-dir .` against the orchestrator repo itself, resolving placeholders to the orchestrator's own identity, before merge. Without this self-application step, the orchestrator's own `wiki-serve.sh` breaks for the duration of M032 + M033 paired development (CON-3 + CON-5 invariant). Plan-phase for P02 MUST list the self-application step as part of the FR-6 implementation task, not a separate task and not deferred.
- **AD-7 throwaway-fixture protocol** — affects P03. `tests/m032-acceptance/throwaway-fixture-protocol.md` is a P03 deliverable; explicit teardown verification (no orphan branches in this repo, no leaked `.orchestrator/` files in the fixture) MUST be documented before SC-5 is considered shippable. Plan-phase for P03 MUST list the protocol document as a named deliverable alongside the deploy-test script itself.
- **#Q-C M032/P02 close-date binding** — affects P02 only. P02 closure is a hard pre-condition for M033/P05 *START* (not just close); P02 must close at least 1 week before M033/P05 begins (M033/P05 currently targets the 2026-05-15 PBJ pilot per Assumption A-3). This binds P02's ship-date to M033/P05's start-date; deviation requires explicit re-coordination of both roadmaps. Plan-phase for P02 MUST surface this as a calendar gate, not just a logical dependency.
- **MIT-008 `wiki-deploy-mutation` audit trail (Constitution VI — State On Disk Is Truth)** — affects P03 (writes records) + P04 (SC-11 verifies presence). Every successful `--deploy` step appends a structured JSONL record to `.orchestrator/execution-log.jsonl` before the live URL prints to stdout. Failed steps that retry MUST log the failure and retry separately. Plan-phase for P03 MUST verify the record is appended *before* the URL print, not after — the ordering is the audit-trail correctness invariant.
- **MIT-001 three-category battery format** — affects P03 (SC-5 emits the SKIP_REASON exit 77) + P04 (battery aggregator implements the three-category exit-code semantics). Pass counter MUST NOT be incremented on a 77 exit; `skip=0` is required for milestone close via SC-14 unless signed attestation is present. Plan-phase for P03 + P04 MUST cite this invariant as a pre-merge check on any test-runner amendment.
- **AD-2 milestone-close discipline (mirrors M030/M031)** — affects P04 only. Acceptance battery + `M032-VALIDATED` marker + `M032-SUMMARY.md` + milestone-grain `unit_close` JSONL record + signed attestation for SC-5 skip-vs-pass distinction. Plan-phase for P04 MUST follow the M030/M031 precedent verbatim — these are not new patterns to invent.
- **CC-only launch posture (CON-1)** — affects every phase. Codex CLI / Cursor pass-through is in-scope at the bundle/install layer (FR-2 covers all three installers); live `--deploy` testing exercises only the CC path; multi-runtime parity for `gh api` calls is M009's job (deferred post-launch). Plan-phase for P03 MUST NOT introduce Codex CLI / Cursor adapter logic for the deploy path; runtime gaps are noted in `references/RUNTIME-ASSUMPTIONS.md` if surfaced.
- **Knowledge-Layer Boundary (M032 vs. [M020](../../milestones/M020/index.md) vs. M031 vs. M033)** — affects P02 only. M032 SHALL NOT add a new knowledge-graph kind; the glossary is *project-shaped* (one per project, single file at `wiki/glossary.md`), distinct from M020's *content-shaped* kinds (`pattern | convention | lesson | decision`). The intersection is FR-16's adapter — `lookup-mems.sh --kind=glossary` reads the project file and synthesizes records for the existing knowledge-graph injection path; it MUST NOT write to `knowledge/<category>/`. Plan-phase for P02 MUST grep the FR-16 diff for any write to `knowledge/<category>/` as a pre-merge check.

## Dependency Graph

```
P01 ──→ P02 ──→ P03 ──┐
        │             │
        └─────────────┴──→ P04
```

Edges:
- `P01 → P02` — `project_assets:` schema (FR-1) and installer mode handler (FR-3) gate wiki-tooling shipping via the managed bundle; P02's `wiki-init` reads from `project_assets:` entries shipped by P01's installer migration
- `P02 → P03` — `commands/wiki-init.md` default-scope surface (FR-5) gates `--with-giscus` and `--deploy` scope additions; FR-6 templated `wiki/mkdocs.yml` gates Giscus-partial templating layering; FR-11 `init --with-wiki` passthrough gates the `--with-wiki --with-giscus --deploy` flag chain
- `P02 → P04` — FR-15 glossary surface gates SC-9 decorator's glossary-fallback branch; FR-6 self-application loop closure gates SC-11's `wiki-serve.sh` HTTP 200 check against orchestrator repo
- `P03 → P04` — `wiki-deploy-mutation` JSONL records (FR-9 / MIT-008) gate SC-11's audit-trail check; throwaway-GH-fixture protocol (AD-7) gates SC-12's battery-aggregator running SC-5 against a live deploy; battery aggregator (SC-12) chains every prior phase's acceptance scripts

## Execution Order

1. **P01** — foundation, no dependencies. Riskiest mechanical change in the milestone (CON-4 byte-identical migration + MIT-006 pre-M032 consumer bootstrapping policy + FR-22 dual-oracle hierarchy). Strict prerequisite to all wiki-tooling work — wiki tooling distribution rides the `project_assets:` schema, not a parallel install path.
2. **P02** — `wiki-init` default + glossary + `init --with-wiki` passthrough + paired-launch seam scripts. **Date-bound by #Q-C: must close ≥ 1 week before M033/P05 starts (M033/P05 targets 2026-05-15 PBJ pilot per Assumption A-3).** High risk because (a) AD-5 / MIT-002 FR-6 self-application loop must close in the same delivery PR or the orchestrator's own wiki breaks, and (b) #Q-C calendar gate is non-negotiable. Cannot start until P01 ships `project_assets:` schema.
3. **P03** — `--with-giscus` + `--deploy` + custom-nav region + throwaway-GH-fixture protocol. High risk because (a) CON-5 live-fixture-discipline / M013/M014 counter-pattern, (b) FR-10 cross-project-cwd hazard (Finding J), (c) FR-9 read-before-write Pages guard / MIT-007 has multiple non-trivial state-detection branches, (d) MIT-008 JSONL audit-trail ordering invariant. Cannot start until P02 ships `wiki-init` + FR-11 init-passthrough.
4. **P04** — scanner extensions + decorator + acceptance battery + milestone close. Medium risk; mostly mechanical scanner extensions plus the SC-11 / SC-12 / SC-13 / SC-14 milestone-close gate mirroring M030/M031 precedent. Depends on P02 (glossary surface for FR-20 fallback + FR-6 self-application loop for SC-11) AND P03 (deploy artifacts + `wiki-deploy-mutation` JSONL for SC-11). Milestone-close gate.

For `orchestrator:auto`: phases run strictly sequentially. There is no parallelism opportunity — every phase has at least one upstream dependency that gates start. (P02 and P03 are the only candidates for parallelism in principle, but P03 depends on P02's `commands/wiki-init.md` + FR-6 templating + FR-11 passthrough, so P03 cannot start until P02 closes.)

## Validation

- **No conflicting producers**: PASS. Each phase produces a disjoint set of files with the following declared additive overlaps explicitly marked at plan-phase pre-merge time:
  - `packaging/bundle/manifest.yml` — amended only by P01 (FR-1 `project_assets:` schema). No other phase touches it.
  - `packaging/install/install-{claude-code,codex,cursor}.sh` — amended only by P01 (FR-2 managed-bulk-copy replacement). No other phase touches them.
  - `wiki/mkdocs.yml` — templated only by P02 (FR-6). P03's custom-nav region split (FR-14) writes to the *runtime* `mkdocs.yml` of the consumer fixture and the orchestrator's own `mkdocs.yml`, but the templated-placeholder substitution layer is exclusively P02's.
  - `wiki/overrides/` Giscus partial — templated only by P03 (FR-7). P02 leaves the partial in placeholder state; P03 substitutes the four `data-*` attributes.
  - `scripts/wiki/wiki-scan-sources.sh` — additively amended by P02 (FR-15 `--include-glossary` toggle) AND P04 (FR-17 / FR-18 / FR-19 enumerations). Each phase's plan-phase MUST declare its amendment as additive (no overlapping flags or duplicate enumeration logic).
  - `scripts/wiki/wiki-generate-nav.sh` — additively amended by P02 (FR-15 glossary-as-second-nav-entry placement) AND P03 (FR-14 auto-nav vs custom-nav region split). Each phase's plan-phase MUST declare its amendment as additive (no nav-region-marker conflicts).
  - `scripts/lifecycle/wiki-init.sh` — created by P02 (FR-5 default scope) AND amended by P03 (FR-8 `--with-giscus` scope, FR-9 `--deploy` scope). P03's scopes are documented as additive `--with-*` flag-handlers per FR-13.
  - `scripts/wiki/wiki-deploy.sh` — amended only by P03 (FR-10 cwd-vs-`repo_url:` sanity gate). No other phase touches it.
  - `commands/init.md` + `scripts/lifecycle/init-project.sh` — amended only by P02 (FR-11 `--with-wiki [--with-giscus] [--deploy]` flag chain). P03 extends the flag chain's downstream behavior via `wiki-init.sh` scope additions, not by re-amending `init-project.sh`.
  - `tests/m032-acceptance/` and `tests/paired-m032-m033/` — populated additively across all phases. No two phases produce a file at the same path. Two test scripts have spec-named filenames prefixed `p02-` (`p02-wiki-init-with-giscus.sh` for SC-4 and `p02-wiki-generate-nav-custom-region.sh` for SC-6) but live in this roadmap's P03 scope — the `p02-` prefix in the spec text predated the clarify-resolved phase placement; planner notes this as a naming-vs-phase mismatch (filename retained for spec-text continuity).
- **All consumed items have producers**: PASS. P01 has no consumes (foundation). P02 consumes P01's `project_assets:` schema, installer mode handler, `installed-files.txt` per-asset mode tracking, and collision invariant — all P01 produces. P03 consumes P02's `wiki-init.sh` default scope, `wiki/mkdocs.yml` templating, `init --with-wiki` passthrough, paired-launch seam-B script, fixture from P01, and `python3` toolchain probe — all P02 (or P01) produces. P04 consumes P02's glossary surface, FR-6 self-application loop, scanner-extension-additive contract, and P03's `wiki-deploy-mutation` JSONL records, throwaway-GH-fixture protocol, and every prior phase's acceptance script — all P02 + P03 produces. No unresolved-dependency entries.
- **DAG is acyclic**: PASS. Strict partial order rooted at P01 and terminating at P04. No back-edges. The single non-linear edge — `P02 → P04` skipping over P03 — does not introduce a cycle; it short-circuits the linear chain because P04's glossary-fallback (FR-20) and SC-11's `wiki-serve.sh` self-application check depend only on P02, not on P03.
- **Demo sentence coverage**: PASS. Each phase's demo sentence names specific script invocations (`bash scripts/lifecycle/wiki-init.sh ...`, `bash tests/m032-acceptance/p01-managed-bundle-shape.sh`, etc.), HTTP status checks (`HTTP 200 at :8000`, `responds 200 within M032_DEPLOY_PROPAGATION_TIMEOUT`), JSONL field assertions (`jq '.[] | select(.event_type == "wiki-deploy-mutation")'`, `BATTERY: pass=10 skip=1 fail=0`), file-system assertions (`M032-VALIDATED marker present`, `installed-files.txt with mode: field on every entry`), or grep checks an operator can mechanically verify — not vague descriptions.
