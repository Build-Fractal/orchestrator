---
schema_version: "1.0"
type: phase-plan
phase: "P03"
milestone: "M033"
goal: "Land M033's constitution authoring + codebase ingestion + standalone-gate compliance test surface — the orchestrator-native `commands/constitution.md` + `scripts/lifecycle/constitution-author.sh` (FR-3) consuming P02's `ask_one` API; three stack starters at `templates/constitution-starters/{web-saas,cli-tool,library}.md` (FR-4) plus the operator/community starter format reference at `references/constitution-starter-format.md`; the FR-5 `scripts/verify/constitution-shape-lint.sh` lint asserting required principle headers + absence of `{{` placeholder strings; the FR-6 `scripts/verify/standalone-gate.sh constitution` handler — Principle XVI's first content-authoring compliance test, zero `speckit.*` references in the M033-shipped surface; the FR-7 `commands/ingest-codebase.md` + `scripts/lifecycle/ingest-codebase.sh` deterministic structural extraction (NOT LLM-augmented per CON-3 / NG-8) producing 5–15 seed MEMs under `<project-dir>/.orchestrator/knowledge/{architecture,conventions,decisions}/` with stable IDs; the FR-8 / MIT-005 rich-context import path emitting `<current-milestone>-CONTEXT.md` (or `_imported-context/_imported-context.md` sentinel for pre-milestone-configured projects per #Q-11) with `context_source: imported-from-existing`; the `_imported-context/` sentinel-convention reference doc plus downstream-traverser handling annotations in `validate-milestone.sh` / `build-context.sh` / `run-doctor.sh`; SC-2 + SC-3 acceptance scripts under `tests/m033-acceptance/`; three stack-fixture trees under `tests/fixtures/m033-stack-fixture-*/`; and the `m033-p03-*` phase-suite + scope-guard verifiers."
demo_sentence: "An operator runs `EDITOR=cat bash scripts/lifecycle/constitution-author.sh --stack web-saas --project-dir /tmp/m033p03-demo --yes` against a fresh project and observes (a) `templates/constitution-starters/web-saas.md` is loaded, (b) the 5–8 grilling-protocol-shaped questions are presented sequentially via P02's `ask_one`, (c) `{{...}}` placeholders are substituted in-place, (d) the draft is handed to `$EDITOR` (cat = no-op), (e) `bash scripts/verify/constitution-shape-lint.sh /tmp/m033p03-demo/.orchestrator/memory/constitution.md` exits 0, (f) `/tmp/m033p03-demo/.orchestrator/memory/constitution.md` contains the 6–8 baseline principles + `Idempotent Deploys` + `User Data Privacy` web-saas-specific principles; runs `bash scripts/verify/standalone-gate.sh constitution` against the M033 repo and observes `PASS: zero speckit references in M033-shipped constitution surface` (FR-6 / Principle XVI compliance test); runs `bash scripts/lifecycle/ingest-codebase.sh --project-dir /tmp/m033p03-demo` against a TypeScript SaaS fixture (`tests/fixtures/m033-stack-fixture-ts-saas/`) and observes 5–15 MEM files written under `/tmp/m033p03-demo/.orchestrator/knowledge/{architecture,conventions,decisions}/` with stable IDs; re-runs the same command and observes `re-ingest: <N> existing entries detected, no changes` diagnostic; against a fixture with `<fixture>/.orchestrator/DECISIONS.md` containing `DR-` entries, observes a thin context file at `<fixture>/.orchestrator/milestones/_imported-context/_imported-context.md` (sentinel path used because no active milestone is configured) with `context_source: imported-from-existing`; runs `bash scripts/dispatch/build-context.sh --profile=quick --task 'fix flaky <test-name>' --project-dir /tmp/m033p03-demo` against the post-ingest fixture and observes ≥3 of the seeded MEMs in the payload; runs `bash tests/m033-acceptance/p02-constitution-author.sh` (SC-2) and `bash tests/m033-acceptance/p03-ingest-codebase.sh` (SC-3) and observes exit 0 from each; runs `bash tools/verify/m033-p03-phase-suite.sh` and observes `SUMMARY: m033-p03-phase-suite.sh pass=N fail=0`."
risk: "high"
depends_on: ["P01", "P02"]
---

## Must-Haves

<!-- All Check commands use single-script-file shape per AD-19.
     Project-owned slug-bearing verifiers live under tools/verify/.
     Framework-owned non-slug-bearing verifiers (constitution-shape-lint.sh,
     standalone-gate.sh) live under scripts/verify/ per FR-5/FR-6 spec
     paths and the M033 Knowledge-Layer Boundary "Write-sites M033 claims".
     Verifier scripts are co-authored alongside their corresponding
     artifact within the SAME task (plan-time discipline rule 2).
     Namespacing: `m033-p03-*` prefix avoids collision with M030/M031/[M032](../../../../milestones/M032/index.md)
     and with M033/P01's `m033-p01-*` and M033/P02's `m033-p02-*` namespaces.

     Per the P01 plan-shape finding (P01-SUMMARY.md "Plan-shape finding"):
     artifact-list bullets in `## Must-Haves` MUST NOT use the bare-backtick
     shape — the auto-loop --step=V parser eval's bare-backtick bullets as
     commands. Each Truths bullet is labeled as a sentence with backticks
     embedded; each Artifacts bullet uses the `Label: path (constraints) — create`
     shape. -->

### Truths

- `commands/constitution.md` exists as a canonical command-doc per MEM012 (YAML frontmatter `description:` field; `# orchestrator:constitution` title; Prerequisites / State Check section; Core Workflow numbered sections; Output / Idempotency / Error Handling / Referenced Scripts sections). The body documents the FR-3 contract: invokes `bash scripts/lifecycle/constitution-author.sh --stack <stack> --project-dir <path> [--force] [--yes]` with the stack value validated against the closed v1 enum `web-saas|cli-tool|library`. The doc names the load-bearing tokens `Idempotent Deploys`, `Composable Default Exit Codes`, `Stable API Surface` (one per stack) so the verifier can grep them. The doc explicitly states the standalone-posture invariant from CON-3 / FR-6 — zero `speckit.*` references — and points at `bash scripts/verify/standalone-gate.sh constitution` as the mechanical compliance test.
  - Check: `bash tools/verify/m033-p03-constitution-md-shape.sh`

- `scripts/lifecycle/constitution-author.sh` exists, is executable, and implements the FR-3 contract per the spec. The driver accepts `--stack <stack>` (required, validated against `web-saas|cli-tool|library`; unknown stacks exit non-zero with the v1 enum echoed and `#Q-2` referenced per US-2 AS-4), `--project-dir <path>` (defaults to `pwd`), `--force` (overwrites existing constitution with stderr warning naming discarded edits per US-2 AS-3), `--yes` (auto-accepts grilling-shell defaults). The driver: (a) loads `templates/constitution-starters/<stack>.md`; (b) sources `scripts/lifecycle/grilling-shell.sh` and runs a 5–8 question flow via `ask_one` per FR-3 step (c) — questions cover project type, primary constraint, target user, deployment target, success metric, top constraint, top non-goal, optional 8th — recommendations derived from detected manifest type per FR-1's `--stack` detection convention; (c) substitutes `{{...}}` placeholders (`{{project_type}}`, `{{primary_constraint}}`, etc.) into the loaded starter; (d) hands the draft to `$EDITOR` (default `vi`); (e) verifies the reviewed file via `bash scripts/verify/constitution-shape-lint.sh <output>` as a post-step gate (non-zero with named missing/malformed item halts write per US-2 AS-5); (f) writes the reviewed file to `<project-dir>/.orchestrator/memory/constitution.md`; (g) writes `<project-dir>/.orchestrator/start-state/constitution-authored.complete` marker per FR-20 (P02 markers); (h) emits `constitution_authored` JSONL event per FR-22 (P02 emitter); (i) appends a one-line FR-21 dual-write Recent Changes fragment via `bash scripts/util/dual-write-runtime-md.sh append` per the P02 SSOT convention. Idempotent without `--force` — re-runs preserve the existing file byte-identical with stdout `no changes` diagnostic. Bash 3.2 compatible per MEM001.
  - Check: `bash tools/verify/m033-p03-constitution-author-sh-shape.sh`

- `templates/constitution-starters/web-saas.md`, `templates/constitution-starters/cli-tool.md`, and `templates/constitution-starters/library.md` exist per FR-4. Each contains 6–8 baseline principles common across all stacks (named by Roman-numeral `### Principle <I..VIII>` sub-headers per the FR-5 lint contract) AND 2–3 stack-specific principles named in the spec — web-saas: `Idempotent Deploys`, `User Data Privacy`; cli-tool: `Composable Default Exit Codes`, `POSIX Convention Adherence`; library: `Stable API Surface`, `Semantic Versioning Discipline`. Each starter contains documented `{{...}}` placeholders consumed by `constitution-author.sh` step (c) — at minimum `{{project_type}}`, `{{primary_constraint}}`, `{{target_user}}`. Each starter has a YAML frontmatter `schema_version: "1.0"` + `type: constitution-starter` + `stack: <name>` per MEM013 template-convention precedent.
  - Check: `bash tools/verify/m033-p03-constitution-starter-templates-shape.sh`

- `references/constitution-starter-format.md` exists per FR-4 / US-2 AS-4 and documents the starter-template file format (frontmatter schema + Roman-numeral principle-header convention + `{{...}}` placeholder vocabulary + the v1 stack list `web-saas|cli-tool|library` + the demand-driven expansion criterion per `#Q-2` — `≥2 distinct external requests for a stack post-launch`). The reference is the SSOT that future community-contributed starter authors consume; the verifier asserts the load-bearing tokens (`schema_version`, `Roman-numeral`, `{{`, `web-saas`, `cli-tool`, `library`, `#Q-2`).
  - Check: `bash tools/verify/m033-p03-constitution-starter-format-ref-shape.sh`

- `scripts/verify/constitution-shape-lint.sh` exists, is executable, and implements the FR-5 contract — given a `<path>` argument naming a constitution file, it asserts: (a) a `## Constitution Check` section header is present (load-bearing for `commands/specify.md:86,99,115` cross-references); (b) at least 6 `### Principle <I..X>` Roman-numeral sub-headers are present; (c) every principle sub-header is followed by at least one non-empty body line before the next `### Principle` or end-of-file; (d) zero literal `{{` placeholder strings remain (the verifier echoes the line numbers of any unresolved placeholders). Exit 0 on pass; exit non-zero with the named missing/malformed item to stderr on fail per US-2 AS-5. The verifier itself is bash 3.2 compatible (no `declare -A`, no process substitution) and emits `PASS:` / `FAIL:` / `SUMMARY:` lines per MEM001. Path discipline: this is a framework-owned non-slug-bearing verifier (consumed by FR-3 step (e) at runtime, NOT only at M033 plan-time) — lives under `scripts/verify/` per spec FR-5 and the Knowledge-Layer Boundary write-site list.
  - Check: `bash tools/verify/m033-p03-constitution-shape-lint-shape.sh`

- `scripts/verify/standalone-gate.sh` exists, is executable, and implements the FR-6 contract — invoked as `bash scripts/verify/standalone-gate.sh constitution`, it scans the M033-shipped constitution-authoring surface (the closed file set: `commands/constitution.md`, `scripts/lifecycle/constitution-author.sh`, `templates/constitution-starters/web-saas.md`, `templates/constitution-starters/cli-tool.md`, `templates/constitution-starters/library.md`, `references/constitution-starter-format.md`, `scripts/verify/constitution-shape-lint.sh`) for any literal `speckit` or `speckit.` reference (case-insensitive substring match via `grep -i 'speckit'`). On any match, exits non-zero with the file:line of each match echoed to stderr; on no matches, exits 0 with `PASS: zero speckit references in M033-shipped constitution surface`. The closed file set is documented inline in the script under a fenced `# >>> constitution-surface-files >>>` block (SSOT — extending the surface requires editing this block). The handler is dispatched via subcommand argument (`constitution` is v1; future subcommands like `ingest-codebase` may extend without contract drift). Path discipline: framework-owned non-slug-bearing verifier per FR-6 + Knowledge-Layer Boundary — lives under `scripts/verify/` so any downstream project that ships the framework bundle gets the gate at install time.
  - Check: `bash tools/verify/m033-p03-standalone-gate-sh-shape.sh`

- `commands/ingest-codebase.md` exists as a canonical command-doc per MEM012, documenting the FR-7 contract: invokes `bash scripts/lifecycle/ingest-codebase.sh --project-dir <path>` to scan top-level docs, package manifests, directory structure (top 2 levels per Edge Case `Codebase scan against a 100k-LOC monorepo`), test directory shape, recent git log (last 50 commits per FR-7), and prior-tooling artifacts (`.cursor/`, `.aider/`, `.claude/`, `.specify/`, `.gsd/`); produces 5–15 seed MEM entries under `<project-dir>/.orchestrator/knowledge/{architecture,conventions,decisions}/` with stable IDs derivable from source paths (re-runs detect existing seeds and emit `re-ingest` diagnostic). The doc names the deterministic-not-LLM invariant per CON-3 / NG-8 and points at `#Q-3` for the M033.5 LLM-augmentation expansion path. The doc names the FR-8 rich-context import path and the `_imported-context/` sentinel per `#Q-11`.
  - Check: `bash tools/verify/m033-p03-ingest-codebase-md-shape.sh`

- `scripts/lifecycle/ingest-codebase.sh` exists, is executable, and implements the FR-7 contract per the spec. The driver: (a) accepts `--project-dir <path>` (defaults to `pwd`), `--yes` (suppresses interactive confirmation prompts); (b) creates `<project-dir>/.orchestrator/knowledge/{architecture,conventions,decisions}/` if absent; (c) scans the closed signal set documented inline under a fenced `# >>> ingest-signal-sources >>>` block: top-level docs (`README.md`, `ARCHITECTURE.md`, `CONTRIBUTING.md`, `docs/`), package manifests (`package.json`, `pyproject.toml`, `Cargo.toml`, `go.mod`), directory structure (top 2 levels of `src/` or equivalent), test directory shape (`tests/`, `test/`, `__tests__/`, `spec/`), recent git log (`git log --oneline -50` if `.git/` present), prior-tooling artifacts (`.cursor/`, `.aider/`, `.claude/`, `.specify/`, `.gsd/`, `.gsd2/`); (d) emits 5–15 seed MEM files using the [M020](../../../../milestones/M020/index.md) `MEM###` naming convention with stable IDs derived from a deterministic hash of the source-path + signal-kind (re-runs produce the same IDs by construction); (e) detects existing seed MEMs by stable-ID match and emits `re-ingest: <N> existing entries detected, no changes` diagnostic exiting 0 (idempotent per FR-7); (f) detects rich-context source files (`<project-dir>/.orchestrator/DECISIONS.md` containing `DR-` entries OR `MILESTONE-AUDIT.md` OR populated `<project-dir>/CLAUDE.md` custom blocks) and triggers the FR-8 import path (deliverable T03 produces the rich-context branch); (g) writes `<project-dir>/.orchestrator/start-state/ingest-codebase-completed.complete` marker per FR-20; (h) emits `ingest_codebase_completed` JSONL event per FR-22; (i) appends a FR-21 dual-write Recent Changes fragment via `dual-write-runtime-md.sh append`. Bash 3.2 compatible. Deterministic structural extraction NOT LLM-augmented per CON-3 / NG-8 — the verifier asserts no model invocation.
  - Check: `bash tools/verify/m033-p03-ingest-codebase-sh-shape.sh`

- The FR-8 / MIT-005 rich-context import path is implemented as a branch inside `scripts/lifecycle/ingest-codebase.sh` (and gated by detection of `<project-dir>/.orchestrator/DECISIONS.md` with `DR-` entries OR `MILESTONE-AUDIT.md` OR populated `CLAUDE.md` custom blocks). On detection, the path: (a) resolves the emit path — when `<project-dir>/.orchestrator/config.yml` declares an active milestone, emit to `<project-dir>/.orchestrator/milestones/<current-milestone>/<current-milestone>-CONTEXT.md`; when no active milestone is configured, emit to `<project-dir>/.orchestrator/milestones/_imported-context/_imported-context.md` (the sentinel path per `#Q-11`); (b) writes a thin context file with frontmatter `context_source: imported-from-existing` + an inline reference list naming the source files (`DECISIONS.md`, `MILESTONE-AUDIT.md`, `CLAUDE.md`) — the file is intentionally thin (≤30 lines) per MEM004 pure-lib precedent (no duplicate authoring; cross-reference only); (c) for every `DR-<id>` entry detected in `<project-dir>/.orchestrator/DECISIONS.md`, surfaces it as a `<project-dir>/.orchestrator/knowledge/decisions/MEM-DR-<id>.md` cross-reference MEM (provenance-preserving, NOT duplicate authoring — the MEM body is a single-line reference to the source `DR-<id>` in the project's `DECISIONS.md`); (d) emits `imported_context_loaded` JSONL event per FR-22 (one of the 11 P02 closed-enum event types).
  - Check: `bash tools/verify/m033-p03-rich-context-import-shape.sh`

- The `_imported-context/` sentinel convention is documented in `references/imported-context-sentinel.md` per `#Q-11`. The reference: (a) names the sentinel directory path (`<project-dir>/.orchestrator/milestones/_imported-context/`) and its filename (`_imported-context.md`); (b) documents the precedence — when an active milestone is configured in `<project-dir>/.orchestrator/config.yml`, emit to the milestone-numbered path; when no active milestone is configured, emit to the sentinel path; (c) names the downstream-traverser convention — any `.orchestrator/milestones/_*` prefix is treated as a special non-milestone class by `validate-milestone.sh`, `build-context.sh`, and `run-doctor.sh` (skipped from milestone enumeration; surfaced via dedicated diagnostic when relevant); (d) names the `context_source: imported-from-existing` frontmatter field as the SSOT marker for downstream tools to detect imported-context vs natively-authored context. Annotation-only modifications to `scripts/state/derive-phase.sh` / `scripts/dispatch/build-context.sh` / `scripts/diagnostics/run-doctor.sh` (or the equivalent traverser sites — T04 will identify and gate the precise filenames at execution time) skip the `_*`-prefix sentinel from milestone enumeration; modifications are additive (no behavior change for projects without imported context). The verifier asserts the reference doc exists with the load-bearing tokens AND that the traverser sites contain the `_*`-prefix skip clause via `grep -F`.
  - Check: `bash tools/verify/m033-p03-imported-context-sentinel-shape.sh`

- Three stack fixtures exist under `tests/fixtures/m033-stack-fixture-{ts-saas,py-cli,rust-library}/`. Each fixture is a synthetic minimal project tree that exercises the FR-7 ingest signal set: TS SaaS — `package.json` with web-framework dependency + `src/` + `README.md` + `ARCHITECTURE.md` + `tests/`; Python CLI — `pyproject.toml` (no web framework) + `src/` + `README.md` + `tests/`; Rust library — `Cargo.toml` (lib crate) + `src/` + `README.md` + `tests/`. Each fixture is byte-deterministic (no timestamps, no random tokens) per the P01 PBJ-fixture precedent. Each fixture has a `README.md` oracle naming the expected MEM count (5–15) AND the expected category breakdown (≥1 architecture / ≥1 convention / ≥1 decision) so the SC-3 acceptance script can assert against ground truth. The `ts-saas` fixture additionally carries `<fixture>/.orchestrator/DECISIONS.md` with two synthetic `DR-` entries (`DR-DEMO-001`, `DR-DEMO-002`) so the FR-8 rich-context import path is exercised.
  - Check: `bash tools/verify/m033-p03-stack-fixtures-shape.sh`

- `tests/m033-acceptance/p02-constitution-author.sh` exists, is executable, and exits 0 (SC-2). The script: (a) creates a staging project under `mktemp -d`; (b) for each of the three stacks (`web-saas`, `cli-tool`, `library`), runs `EDITOR=cat bash scripts/lifecycle/constitution-author.sh --stack <stack> --project-dir <staging> --yes` against simulated stdin (recommendation-accept replies via `printf 'y\ny\ny\ny\ny\n'`) and asserts: `<staging>/.orchestrator/memory/constitution.md` exists; the file contains the stack-specific principle tokens named in FR-4 (web-saas: `Idempotent Deploys` AND `User Data Privacy`; cli-tool: `Composable Default Exit Codes` AND `POSIX Convention Adherence`; library: `Stable API Surface` AND `Semantic Versioning Discipline`); zero literal `{{` placeholder strings remain; `bash scripts/verify/constitution-shape-lint.sh <output>` exits 0; (c) for the `web-saas` stack, re-runs the command without `--force` and asserts the file is preserved byte-identical and the diagnostic `no changes` appears on stdout; (d) re-runs with `--force` and asserts the file is regenerated AND a stderr warning naming discarded edits appears; (e) runs `bash scripts/lifecycle/constitution-author.sh --stack quantum-mainframe --project-dir <staging>` and asserts non-zero exit with the v1 stack list AND `#Q-2` echoed; (f) runs `bash scripts/verify/standalone-gate.sh constitution` against the M033 repo and asserts exit 0 (proves CON-3 / Principle XVI — no `speckit.*` leakage). Cleanup mandatory.
  - Check: `bash tools/verify/m033-p03-acceptance-shape-sc2.sh`

- `tests/m033-acceptance/p03-ingest-codebase.sh` exists, is executable, and exits 0 (SC-3). The script: (a) for each of the three stack fixtures, copies the fixture into a `mktemp -d` staging project, runs `bash scripts/lifecycle/ingest-codebase.sh --project-dir <staging> --yes`, and asserts ≥5 and ≤15 MEM files exist under `<staging>/.orchestrator/knowledge/{architecture,conventions,decisions}/` with the expected category-breakdown floor (≥1 per category); (b) re-runs the same command and asserts zero new MEMs are produced AND `re-ingest: <N> existing entries detected, no changes` appears on stdout; (c) for the `ts-saas` fixture (which carries [`.orchestrator/DECISIONS.md`](../../../../decisions.md) with `DR-` entries), asserts the FR-8 rich-context import path fires — `<staging>/.orchestrator/milestones/_imported-context/_imported-context.md` exists (sentinel path because no active milestone is configured) with `context_source: imported-from-existing` in the frontmatter AND the `DR-DEMO-001` / `DR-DEMO-002` entries surface as `<staging>/.orchestrator/knowledge/decisions/MEM-DR-DEMO-001.md` and `MEM-DR-DEMO-002.md` cross-references; (d) runs `bash scripts/dispatch/build-context.sh --profile=quick --task 'fix flaky <test-name>' --project-dir <staging>` against the post-ingest fixture and asserts ≥3 of the seeded MEM IDs appear in the payload (proves the seeded graph is consumable by [M031](../../../../milestones/M031/index.md)'s universal-entry path); (e) runs `bash scripts/lifecycle/ingest-codebase.sh --project-dir <degenerate-staging>` against a fixture with no `README.md`, no manifest, only a `src/` directory and asserts ≥1 MEM is produced AND a stdout diagnostic listing what was missing appears (US-3 AS-5 — minimum viable seed). Cleanup mandatory.
  - Check: `bash tools/verify/m033-p03-acceptance-shape-sc3.sh`

- `tools/verify/m033-p03-phase-suite.sh` exists, is executable, invokes every P03 verifier in dependency order, exits 0 iff every sub-gate passes, and emits a single line `SUMMARY: m033-p03-phase-suite.sh pass=N fail=M` before exit. The suite chains, in order: `m033-p03-constitution-md-shape.sh`, `m033-p03-constitution-author-sh-shape.sh`, `m033-p03-constitution-starter-templates-shape.sh`, `m033-p03-constitution-starter-format-ref-shape.sh`, `m033-p03-constitution-shape-lint-shape.sh`, `m033-p03-standalone-gate-sh-shape.sh`, `m033-p03-ingest-codebase-md-shape.sh`, `m033-p03-ingest-codebase-sh-shape.sh`, `m033-p03-rich-context-import-shape.sh`, `m033-p03-imported-context-sentinel-shape.sh`, `m033-p03-stack-fixtures-shape.sh`, `m033-p03-acceptance-shape-sc2.sh`, `m033-p03-acceptance-shape-sc3.sh`. Thirteen sub-gates plus the suite line.
  - Check: `bash tools/verify/m033-p03-phase-suite.sh`

- The P02 cross-phase regression boundary holds — the SC-1 + SC-8 + SC-11 + SC-12 + SC-13 acceptance suites still pass against the M033 working tree after P03 land. Per the AD-15 cross-phase regression precedent baked into P02/T02 and P02/T05's verifiers, P03 deliverables MUST NOT regress the P01 or P02 surface. The phase-suite's last sub-step re-runs `tools/verify/m033-p01-phase-suite.sh` and `tools/verify/m033-p02-phase-suite.sh` and asserts both exit 0.
  - Check: `bash tools/verify/m033-p03-cross-phase-regression.sh`

- The SC-13 / scope-guard invariant holds for the P03 diff: P03 modifies/creates only files declared in this phase's "Files Likely Touched" list. None of `scripts/lifecycle/materials-intake.sh`, `scripts/lifecycle/ideation.sh`, `scripts/lifecycle/customblock-draft.sh`, `commands/materials-intake.md`, `commands/ideation.md`, `commands/customblock-draft.md` is touched (those belong to P04). None of the M032 paired-launch surfaces (`commands/wiki-init.md`, the install-bundle `project_assets:` schema, M033/P05 friendly-tester gate scripts) is touched. Modifications to `scripts/state/derive-phase.sh` / `scripts/dispatch/build-context.sh` / `scripts/diagnostics/run-doctor.sh` (or equivalent downstream traversers — T04 identifies the precise filenames) are additive `_*`-prefix skip clauses ONLY; no behavior change for projects without imported context.
  - Check: `bash tools/verify/m033-p03-scope-guard.sh`

### Artifacts

- Command doc: `commands/constitution.md` (min 60 lines, contains "orchestrator:constitution", contains "FR-3", contains "constitution-author.sh", contains "web-saas", contains "cli-tool", contains "library", contains "standalone-gate.sh", contains "Idempotent Deploys", contains "Composable Default Exit Codes", contains "Stable API Surface") — create
- Driver: `scripts/lifecycle/constitution-author.sh` (min 200 lines, contains "ask_one", contains "grilling-shell.sh", contains "--stack", contains "--force", contains "--yes", contains "templates/constitution-starters", contains "constitution-shape-lint.sh", contains "EDITOR", contains ".orchestrator/memory/constitution.md", contains "constitution_authored", contains "constitution-authored.complete", contains "dual-write-runtime-md.sh", bash 3.2 compatible) — create
- Starter: `templates/constitution-starters/web-saas.md` (min 50 lines, contains "schema_version", contains "constitution-starter", contains "stack: web-saas", contains "Constitution Check", contains "### Principle", contains "Idempotent Deploys", contains "User Data Privacy", contains "{{project_type}}", contains "{{primary_constraint}}") — create
- Starter: `templates/constitution-starters/cli-tool.md` (min 50 lines, contains "schema_version", contains "stack: cli-tool", contains "Constitution Check", contains "### Principle", contains "Composable Default Exit Codes", contains "POSIX Convention Adherence", contains "{{project_type}}", contains "{{primary_constraint}}") — create
- Starter: `templates/constitution-starters/library.md` (min 50 lines, contains "schema_version", contains "stack: library", contains "Constitution Check", contains "### Principle", contains "Stable API Surface", contains "Semantic Versioning Discipline", contains "{{project_type}}", contains "{{primary_constraint}}") — create
- Reference: `references/constitution-starter-format.md` (min 60 lines, contains "schema_version", contains "Roman-numeral", contains "Constitution Check", contains "Principle", contains "{{", contains "web-saas", contains "cli-tool", contains "library", contains "#Q-2") — create
- Lint verifier: `scripts/verify/constitution-shape-lint.sh` (min 50 lines, contains "Constitution Check", contains "Principle", contains "PASS:", contains "FAIL:", contains "SUMMARY:", contains "{{") — create
- Standalone gate: `scripts/verify/standalone-gate.sh` (min 60 lines, contains "constitution", contains "speckit", contains "constitution-surface-files", contains "PASS:", contains "FAIL:", contains "SUMMARY:") — create
- Command doc: `commands/ingest-codebase.md` (min 60 lines, contains "orchestrator:ingest-codebase", contains "FR-7", contains "FR-8", contains "ingest-codebase.sh", contains "deterministic", contains "_imported-context", contains "DR-", contains "context_source", contains "MEM-DR-", contains "#Q-3", contains "#Q-11") — create
- Driver: `scripts/lifecycle/ingest-codebase.sh` (min 250 lines, contains "ingest-signal-sources", contains "package.json", contains "pyproject.toml", contains "Cargo.toml", contains "go.mod", contains "knowledge/architecture", contains "knowledge/conventions", contains "knowledge/decisions", contains "context_source: imported-from-existing", contains "_imported-context", contains "MEM-DR-", contains "re-ingest", contains "ingest_codebase_completed", contains "imported_context_loaded", contains "ingest-codebase-completed.complete", bash 3.2 compatible) — create
- Reference: `references/imported-context-sentinel.md` (min 50 lines, contains "_imported-context", contains "context_source: imported-from-existing", contains "validate-milestone.sh", contains "build-context.sh", contains "run-doctor.sh", contains "_*", contains "#Q-11") — create
- Fixture tree: `tests/fixtures/m033-stack-fixture-ts-saas/` (contains `package.json`, `src/`, `README.md`, `ARCHITECTURE.md`, `tests/`, [`.orchestrator/DECISIONS.md`](../../../../decisions.md) with `DR-DEMO-001` and `DR-DEMO-002` entries, `README-oracle.md` naming expected MEM count + category breakdown) — create
- Fixture tree: `tests/fixtures/m033-stack-fixture-py-cli/` (contains `pyproject.toml`, `src/`, `README.md`, `tests/`, `README-oracle.md` naming expected MEM count + category breakdown) — create
- Fixture tree: `tests/fixtures/m033-stack-fixture-rust-library/` (contains `Cargo.toml`, `src/`, `README.md`, `tests/`, `README-oracle.md` naming expected MEM count + category breakdown) — create
- Acceptance: `tests/m033-acceptance/p02-constitution-author.sh` (min 100 lines, contains "SC-2", contains "FR-3", contains "FR-4", contains "FR-5", contains "FR-6", contains "web-saas", contains "cli-tool", contains "library", contains "Idempotent Deploys", contains "Composable Default Exit Codes", contains "Stable API Surface", contains "speckit", contains "no changes", contains "constitution-shape-lint.sh", contains "standalone-gate.sh") — create
- Acceptance: `tests/m033-acceptance/p03-ingest-codebase.sh` (min 100 lines, contains "SC-3", contains "FR-7", contains "FR-8", contains "ingest-codebase.sh", contains "knowledge/architecture", contains "knowledge/conventions", contains "knowledge/decisions", contains "re-ingest", contains "context_source: imported-from-existing", contains "_imported-context", contains "MEM-DR-DEMO-001", contains "build-context.sh", contains "--profile=quick") — create
- Verifier: `tools/verify/m033-p03-constitution-md-shape.sh` (min 30 lines, contains "commands/constitution.md", contains "FR-3", contains "Idempotent Deploys", contains "standalone-gate.sh") — create
- Verifier: `tools/verify/m033-p03-constitution-author-sh-shape.sh` (min 30 lines, contains "scripts/lifecycle/constitution-author.sh", contains "ask_one", contains "--stack", contains "constitution-shape-lint.sh") — create
- Verifier: `tools/verify/m033-p03-constitution-starter-templates-shape.sh` (min 30 lines, contains "templates/constitution-starters/web-saas.md", contains "templates/constitution-starters/cli-tool.md", contains "templates/constitution-starters/library.md", contains "Principle", contains "{{project_type}}") — create
- Verifier: `tools/verify/m033-p03-constitution-starter-format-ref-shape.sh` (min 25 lines, contains "references/constitution-starter-format.md", contains "Roman-numeral", contains "#Q-2") — create
- Verifier: `tools/verify/m033-p03-constitution-shape-lint-shape.sh` (min 30 lines, contains "scripts/verify/constitution-shape-lint.sh", contains "Constitution Check", contains "Principle", contains "{{") — create
- Verifier: `tools/verify/m033-p03-standalone-gate-sh-shape.sh` (min 30 lines, contains "scripts/verify/standalone-gate.sh", contains "constitution", contains "speckit", contains "constitution-surface-files") — create
- Verifier: `tools/verify/m033-p03-ingest-codebase-md-shape.sh` (min 30 lines, contains "commands/ingest-codebase.md", contains "FR-7", contains "FR-8", contains "deterministic", contains "_imported-context") — create
- Verifier: `tools/verify/m033-p03-ingest-codebase-sh-shape.sh` (min 30 lines, contains "scripts/lifecycle/ingest-codebase.sh", contains "ingest-signal-sources", contains "knowledge/architecture", contains "re-ingest") — create
- Verifier: `tools/verify/m033-p03-rich-context-import-shape.sh` (min 30 lines, contains "context_source: imported-from-existing", contains "_imported-context", contains "MEM-DR-", contains "imported_context_loaded") — create
- Verifier: `tools/verify/m033-p03-imported-context-sentinel-shape.sh` (min 30 lines, contains "references/imported-context-sentinel.md", contains "_imported-context", contains "validate-milestone.sh", contains "build-context.sh", contains "run-doctor.sh") — create
- Verifier: `tools/verify/m033-p03-stack-fixtures-shape.sh` (min 30 lines, contains "tests/fixtures/m033-stack-fixture-ts-saas", contains "tests/fixtures/m033-stack-fixture-py-cli", contains "tests/fixtures/m033-stack-fixture-rust-library", contains "DR-DEMO-001", contains "README-oracle.md") — create
- Verifier: `tools/verify/m033-p03-acceptance-shape-sc2.sh` (min 25 lines, contains "p02-constitution-author.sh", contains "SC-2") — create
- Verifier: `tools/verify/m033-p03-acceptance-shape-sc3.sh` (min 25 lines, contains "p03-ingest-codebase.sh", contains "SC-3") — create
- Verifier: `tools/verify/m033-p03-phase-suite.sh` (min 60 lines, contains "SUMMARY:", contains "m033-p03-constitution-md-shape", contains "m033-p03-constitution-author-sh-shape", contains "m033-p03-standalone-gate-sh-shape", contains "m033-p03-ingest-codebase-sh-shape", contains "m033-p03-rich-context-import-shape", contains "m033-p03-acceptance-shape-sc2", contains "m033-p03-acceptance-shape-sc3", contains "m033-p03-phase-suite") — create
- Verifier: `tools/verify/m033-p03-cross-phase-regression.sh` (min 25 lines, contains "m033-p01-phase-suite.sh", contains "m033-p02-phase-suite.sh", contains "AD-15") — create
- Verifier: `tools/verify/m033-p03-scope-guard.sh` (min 50 lines, contains "scripts/lifecycle/materials-intake.sh", contains "scripts/lifecycle/ideation.sh", contains "scripts/lifecycle/customblock-draft.sh", contains "commands/materials-intake.md", contains "commands/ideation.md", contains "commands/customblock-draft.md", contains "constitution-author.sh", contains "ingest-codebase.sh") — create

### Key Links

- `commands/constitution.md` → `scripts/lifecycle/constitution-author.sh` (command doc references the driver per MEM012 Referenced Scripts convention)
- `commands/constitution.md` → `scripts/verify/standalone-gate.sh` (command doc points at the FR-6 / Principle XVI compliance test)
- `scripts/lifecycle/constitution-author.sh` → `scripts/lifecycle/grilling-shell.sh` (driver sources P02's `ask_one` API)
- `scripts/lifecycle/constitution-author.sh` → `scripts/verify/constitution-shape-lint.sh` (driver invokes the FR-5 lint as a post-step verifier)
- `scripts/lifecycle/constitution-author.sh` → `scripts/util/jsonl-event-emitter.sh` (driver emits `constitution_authored` event per FR-22)
- `scripts/lifecycle/constitution-author.sh` → `scripts/util/start-state-markers.sh` (driver writes `constitution-authored.complete` marker per FR-20)
- `scripts/lifecycle/constitution-author.sh` → `scripts/util/dual-write-runtime-md.sh` (driver appends FR-21 dual-write Recent Changes fragment)
- `scripts/verify/standalone-gate.sh` → `commands/constitution.md` (one of the surface files the gate scans)
- `scripts/verify/standalone-gate.sh` → `templates/constitution-starters/web-saas.md` (one of the surface files)
- `commands/ingest-codebase.md` → `scripts/lifecycle/ingest-codebase.sh` (command doc references the driver)
- `scripts/lifecycle/ingest-codebase.sh` → `scripts/util/jsonl-event-emitter.sh` (driver emits `ingest_codebase_completed` and `imported_context_loaded` events)
- `scripts/lifecycle/ingest-codebase.sh` → `scripts/util/start-state-markers.sh` (driver writes `ingest-codebase-completed.complete` marker)
- `references/imported-context-sentinel.md` → `scripts/state/derive-phase.sh` (reference doc names downstream traversers requiring `_*`-prefix skip)
- `references/imported-context-sentinel.md` → `scripts/dispatch/build-context.sh` (reference doc names downstream traversers requiring `_*`-prefix skip)
- `tests/m033-acceptance/p02-constitution-author.sh` → `scripts/lifecycle/constitution-author.sh` (SC-2 exercises the driver across three stacks)
- `tests/m033-acceptance/p02-constitution-author.sh` → `scripts/verify/standalone-gate.sh` (SC-2 invokes the gate against the M033 surface)
- `tests/m033-acceptance/p03-ingest-codebase.sh` → `scripts/lifecycle/ingest-codebase.sh` (SC-3 exercises the driver across three stack fixtures)
- `tests/m033-acceptance/p03-ingest-codebase.sh` → `scripts/dispatch/build-context.sh` (SC-3 verifies seeded MEMs are consumable via M031's quick profile)
- `tools/verify/m033-p03-phase-suite.sh` → `tools/verify/m033-p03-acceptance-shape-sc2.sh` (suite chains the SC-2 wrapper)
- `tools/verify/m033-p03-phase-suite.sh` → `tools/verify/m033-p03-acceptance-shape-sc3.sh` (suite chains the SC-3 wrapper)

## Tasks

### T01: Standalone gate + constitution-shape-lint + 3 stack starters + format reference (FR-4 / FR-5 / FR-6)

See `tasks/T01-standalone-gate-and-starters-PLAN.md`.

### T02: `commands/constitution.md` + `scripts/lifecycle/constitution-author.sh` (FR-3)

See `tasks/T02-constitution-author-PLAN.md`.

### T03: `commands/ingest-codebase.md` + `scripts/lifecycle/ingest-codebase.sh` deterministic core + 3 stack fixtures (FR-7)

See `tasks/T03-ingest-codebase-core-PLAN.md`.

### T04: FR-8 / MIT-005 rich-context import path + `_imported-context/` sentinel + downstream-traverser annotations (FR-8 / #Q-11)

See `tasks/T04-rich-context-import-PLAN.md`.

### T05: SC-2 + SC-3 acceptance scripts + phase-suite + cross-phase regression + scope-guard

See `tasks/T05-acceptance-and-phase-suite-PLAN.md`.

## Task Dependencies

```
T01 ──► T02 ──┐
              ├──► T05
T03 ──► T04 ──┘
```

T01 (standalone-gate + constitution-shape-lint + 3 stack starters + format reference) is the first deliverable and has no intra-phase prerequisites — it lands the verifier surfaces and templates that T02 consumes. T02 (`constitution-author.sh` + `commands/constitution.md`) depends on T01's templates + lint verifier (the driver invokes both at runtime). T03 (`ingest-codebase` deterministic core + stack fixtures) has no intra-phase prerequisites — it is independent of the constitution chain and can run in parallel with T01/T02. T04 (FR-8 / MIT-005 rich-context import path + sentinel reference + downstream-traverser annotations) depends on T03 (extends the ingest-codebase driver in-place with the rich-context branch). T05 (SC-2 + SC-3 acceptance scripts + phase-suite + cross-phase regression + scope-guard) depends on T01 + T02 (SC-2 exercises both) + T03 + T04 (SC-3 exercises both). The phase-suite is last by construction.

## Files Likely Touched

- `commands/constitution.md` (create, T02)
- `commands/ingest-codebase.md` (create, T03)
- `scripts/lifecycle/constitution-author.sh` (create, T02)
- `scripts/lifecycle/ingest-codebase.sh` (create, T03 — extended in place by T04 for rich-context branch)
- `scripts/verify/constitution-shape-lint.sh` (create, T01)
- `scripts/verify/standalone-gate.sh` (create, T01)
- `templates/constitution-starters/web-saas.md` (create, T01)
- `templates/constitution-starters/cli-tool.md` (create, T01)
- `templates/constitution-starters/library.md` (create, T01)
- `references/constitution-starter-format.md` (create, T01)
- `references/imported-context-sentinel.md` (create, T04)
- `tests/fixtures/m033-stack-fixture-ts-saas/` (create tree, T03)
- `tests/fixtures/m033-stack-fixture-py-cli/` (create tree, T03)
- `tests/fixtures/m033-stack-fixture-rust-library/` (create tree, T03)
- `tests/m033-acceptance/p02-constitution-author.sh` (create, T05)
- `tests/m033-acceptance/p03-ingest-codebase.sh` (create, T05)
- `tools/verify/m033-p03-constitution-md-shape.sh` (create, T02)
- `tools/verify/m033-p03-constitution-author-sh-shape.sh` (create, T02)
- `tools/verify/m033-p03-constitution-starter-templates-shape.sh` (create, T01)
- `tools/verify/m033-p03-constitution-starter-format-ref-shape.sh` (create, T01)
- `tools/verify/m033-p03-constitution-shape-lint-shape.sh` (create, T01)
- `tools/verify/m033-p03-standalone-gate-sh-shape.sh` (create, T01)
- `tools/verify/m033-p03-ingest-codebase-md-shape.sh` (create, T03)
- `tools/verify/m033-p03-ingest-codebase-sh-shape.sh` (create, T03)
- `tools/verify/m033-p03-rich-context-import-shape.sh` (create, T04)
- `tools/verify/m033-p03-imported-context-sentinel-shape.sh` (create, T04)
- `tools/verify/m033-p03-stack-fixtures-shape.sh` (create, T03)
- `tools/verify/m033-p03-acceptance-shape-sc2.sh` (create, T05)
- `tools/verify/m033-p03-acceptance-shape-sc3.sh` (create, T05)
- `tools/verify/m033-p03-phase-suite.sh` (create, T05)
- `tools/verify/m033-p03-cross-phase-regression.sh` (create, T05)
- `tools/verify/m033-p03-scope-guard.sh` (create, T05)
- `scripts/state/derive-phase.sh` (additive `_*`-prefix skip annotation only — T04; no behavior change for projects without imported context)
- `scripts/dispatch/build-context.sh` (additive `_*`-prefix skip annotation only — T04; no behavior change for projects without imported context)
- `scripts/diagnostics/run-doctor.sh` (additive `_*`-prefix skip annotation only — T04 — exact target file may differ; T04 identifies precise filename at execution time)
