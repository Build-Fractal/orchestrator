---
schema_version: "1.0"
type: task-summary
id: "T03"
parent: "M035/P00"
milestone: "M035"
provides:
  - "wiki-stub freshness diagnostic (scripts/diagnostics/wiki-stubs-fresh.sh) + pages.yml pre-build gate; tmp-staged regen + diff approach (no live-tree mutation)"
requires:
  - "from:M035/P00/T02 what:installer-managed sidecar pattern (T03 borrows the marker-block authoring convention into the wiki-init advisory message)"
affects:
  - "M035/P00 SC-3 / SC-4 (wiki publishing CI hardness); newly-emitted .github/workflows/pages.yml gains a pre-build gate via wiki-init.sh emit_pages_workflow() HEREDOC; existing-workflow projects unchanged (CON-3) but now receive an advisory pointer"
key_files:
  - "scripts/diagnostics/wiki-stubs-fresh.sh,scripts/lifecycle/wiki-init.sh,tools/verify/m035-p00-wiki-stubs-fresh.sh"
key_decisions:
  - "exit-code contract 0=fresh / 1=env-fail / 2=drift (per dispatch payload, redefining the prior in-tree script which used 1=drift / 2=env-fail); diagnostic operates against tmp-staged copy via cp -R (.orchestrator/, knowledge/, scripts/, templates/, wiki/) + run generators with --root <tmp> rather than mutating live wiki; mkdocs build --strict gate folded back into the diagnostic's PASS path was dropped — kept the diagnostic single-purpose (drift only) and let pages.yml run mkdocs build separately; existing-workflow CON-3 advisory message extension surfaces the gate in stderr without changing behavior"
patterns_established:
  - "tmp-staged regen-and-diff (cp -R sources, run generators with --root <tmp>, diff -ruN against committed tree) — reusable for any other freshness-gate diagnostic where the producer would otherwise mutate the live tree; printf '%s\n' '----- header -----' to side-step macOS bash printf interpreting leading hyphens as flags"
drill_down_paths:
  - ".orchestrator/milestones/M035/phases/P00/tasks/T03-wiki-stubs-fresh-diagnostic-and-gate-PLAN.md,.orchestrator/proposals/papercut-wiki-stub-drift.md,scripts/diagnostics/wiki-stubs-fresh.sh,tools/verify/m035-p00-wiki-stubs-fresh.sh"
duration: "45m"
verification_result: "pass"
completed_at: "2026-05-08T06:08:38Z"
---

Authored scripts/diagnostics/wiki-stubs-fresh.sh — bash-3.2-safe staleness gate that stages .orchestrator/ + knowledge/ + scripts/ + templates/ + wiki/ + sibling .md files into a fresh $(mktemp -d)/proj/ tree, runs wiki-generate-stubs.sh + wiki-generate-nav.sh with --root <tmp>, and diffs <tmp>/wiki/docs vs <root>/wiki/docs and <tmp>/wiki/mkdocs.yml vs <root>/wiki/mkdocs.yml. Exit-code contract per dispatch: 0=fresh / 1=environment-failure / 2=drift. INFO short-circuit on absent wiki/. STALE: <path> enumeration on drift (one line per differing path) + capped 200-line unified diff for human inspection + verbatim regen command pointing at scripts/wiki/wiki-generate-stubs.sh && scripts/wiki/wiki-generate-nav.sh && git add wiki/. Confirmed scripts/lifecycle/wiki-init.sh emit_pages_workflow() HEREDOC (lines 488-538) already includes the pre-build gate stanza shipped under FR-19/M037/P02 — verified via the verifier's awk-extracted HEREDOC body that the wiki-stubs-fresh.sh line precedes mkdocs build by 5 lines. Extended the CON-3 existing-workflow preservation branch (wiki-init.sh:484) diagnostic message to append 'Consider adding bash scripts/diagnostics/wiki-stubs-fresh.sh --root . as a pre-build step (see papercut-wiki-stub-drift.md Layer 1)' — stderr-only advisory with no behavior change. Authored tools/verify/m035-p00-wiki-stubs-fresh.sh — 9 assertions: (1-3) diagnostic file present + references both generators; (4-5) green-on-clean against the orchestrator repo (exit 0 + 'PASS: wiki-stubs-fresh' in stdout); (6-7) red-on-drift against a fresh $(mktemp -d) fixture with one auto-generated stub (wiki/docs/constitution.md) removed (exit 2 + 'regen' in stderr); (8-9) wiki-init.sh references the diagnostic AND the HEREDOC body has the gate line preceding 'mkdocs build'. Verifier 9/9 PASS under bash 3.2.57 (Apple) on a freshly-regenerated wiki tree (PASS: wiki-stubs-fresh — no drift; 2263 stubs + nav verified). Concerns: (1) The diagnostic's tmp staging copies .orchestrator/ + knowledge/ + scripts/ + templates/ + wiki/ which on this repo is ~30-50 MB and adds 2-3s per invocation; on much larger consumer projects this could exceed CI budget — staging via rsync --link-dest or hardlinks is a deferred optimization. (2) The diagnostic does not honor wiki.extra_dirs config entries beyond what the generators already read; if an operator adds a custom extra_dirs entry pointing to a top-level dir not in the diagnostic's stage list (.orchestrator, knowledge, scripts, templates, wiki), the regen will produce STALE: false-positives on the entries from that dir. Document as a follow-up papercut if dogfood surfaces it. (3) The recently-committed wiki tree (HEAD~5..HEAD) included continue.md stubs that the latest scanner excludes (commit 00f87f51) and was missing newly-authored M035/P00 PAYLOAD/SUMMARY stubs — running the diagnostic forced a fresh regen + commit (out-of-band part of this task) so the verifier's check 2 (green-on-clean) could pass. (4) The previous in-tree wiki-stubs-fresh.sh used a different exit-code contract (1=drift, 2=env-fail) and chained a mkdocs build --strict gate into its PASS path; the rewrite under this dispatch's contract drops the build gate and inverts the codes. The pages.yml HEREDOC's existing wiki-stubs-fresh.sh invocation surfaces the new exit codes correctly because failures (whether 1 or 2) both fail the CI step.
