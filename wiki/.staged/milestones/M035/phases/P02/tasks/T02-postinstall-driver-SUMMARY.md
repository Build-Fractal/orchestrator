---
schema_version: "1.0"
type: task-summary
id: "T02"
parent: "P02"
milestone: "M035"
provides:
  - "packaging/npm/postinstall.sh executable npm-postinstall driver: Windows fail-closed guard via uname -s case (MIT-9/D003 belt-and-suspenders); INIT_CWD-aware project-dir resolution (npm convention with NPM_PREFIX global-install short-circuit emitting global-install advisory and exit 0); DRY_RUN=1 honor (D002 fixture-strategy contract, emits would_invoke= / would_check= / would_delegate= lines and exits 0 without writes); Claude Code runtime probe via ~/.claude/ directory presence (absence yields runtime_unavailable=true advisory and exit 0 — soft-fail per CI-runner-without-CC use case); delegation to packaging/install/install-claude-code.sh --project-dir $INIT_CWD preserving M025 manifest mechanism (CON-7); tools/verify/m035-p02-postinstall-shape.sh task-grain verifier (1 file-exists+executable + 7 grep contract surfaces + 1 functional DRY_RUN smoke test against temp dir, AD-19 single-script shape BATTERY: pass=9 fail=0)"
requires:
  - "from:T01 what:package.json scripts.postinstall reference 'bash packaging/npm/postinstall.sh' (T02 authors the target); from:T01 what:engines.node + os: [darwin,linux] npm-side primary-gate Windows guard (T02 is secondary defense); from:disk what:packaging/install/install-claude-code.sh M025 installer (delegation target with --project-dir flag); from:disk what:scripts/util/run-probe.sh staged-probe wrapper (CON-3/AP-009 honor for cross-reference probe)"
affects:
  - "P02/T03 (cross-channel byte-equivalence test consumes DRY_RUN=1 contract via this driver); P02/T04 (release.yml CI workflow exercises npm install pipeline that fires postinstall); P02/T05 (bundle-hygiene filter must include packaging/npm/ in npm tarball surface); P06 (commands/update.md --rollback extends npm uninstall cascade story)"
key_files:
  - "packaging/npm/postinstall.sh,tools/verify/m035-p02-postinstall-shape.sh"
key_decisions:
  - "D-RN-1 (dr-code-029 binds @build-fractal/orchestrator npm scope echoed in advisory messages); MIT-9 (Windows fail-closed via uname -s case match — secondary belt-and-suspenders behind package.json os field primary gate per D003); D002 (DRY_RUN=1 test-fixture contract emits would_invoke= lines without writes); CON-7 (M025 reversibility-gate preserved — postinstall delegates to install-claude-code.sh which writes M025 manifest, does NOT bypass); CON-3 / AP-009 (compound-chain shape-guard honored — bash 3.2 compatible, no jq, no python, no inline compound chains)"
patterns_established:
  - "npm-postinstall-as-thin-delegate (postinstall is a guard layer + INIT_CWD resolver + runtime detector; actual skill registration delegates to existing M025 install-claude-code.sh — preserves M025 manifest mechanism without bypass); INIT_CWD-with-npm-prefix-short-circuit (npm sets INIT_CWD to invocation dir; when INIT_CWD matches $NPM_PREFIX or $NPM_PREFIX/lib/node_modules, treat as 'global install no project context' and emit advisory exit 0 rather than registering against npm prefix dir); soft-fail-on-runtime-absence (Claude Code missing yields runtime_unavailable=true advisory + exit 0, NOT exit 1 — supports CI-runner use case where operator wants binary on PATH for later integration; skill registration deferred to first /orchestrator-init invocation); Windows-belt-and-suspenders-guard-pattern (package.json os field is primary gate at npm side; postinstall uname -s case is secondary defense — if user reaches postinstall on Windows, npm config was bypassed and we fail loudly with issue-tracker URL); DRY_RUN=1-as-pre-delegate-exit (DRY_RUN check fires AFTER INIT_CWD resolution but BEFORE installer invocation, so dry-run output captures the exact command that WOULD have run with the resolved project dir — T03 byte-equivalence test depends on this contract); REPO_ROOT-via-script-dir-traversal (postinstall self-resolves REPO_ROOT as 2-levels-up from $(dirname $0) — works post-npm-extraction where the package layout is preserved)"
drill_down_paths:
  - ".orchestrator/milestones/M035/phases/P02/tasks/T02-postinstall-driver-PAYLOAD.md, .orchestrator/milestones/M035/phases/P02/tasks/T02-postinstall-driver-PLAN.md"
duration: "12m"
verification_result: "pass"
completed_at: "2026-05-08T20:06:11Z"
---

T02 authored the npm postinstall driver `packaging/npm/postinstall.sh` (98 lines, executable) that runs automatically after `npm install -g @build-fractal/orchestrator`. The driver is structured as 6 layered concerns: (1) Windows fail-closed guard via `uname -s` case-match against MINGW*/CYGWIN*/MSYS*/Windows_NT/WindowsNT — secondary defense behind `package.json os: [darwin, linux]` per MIT-9/D003; emits a clear stderr message naming M009 multi-runtime-parity-audit as the deferred follow-up and points operators at the issue tracker; exits 1; (2) REPO_ROOT resolution via 2-level-up traversal from $(dirname $0) — works in the post-extraction npm package layout; INSTALLER path computed once; (3) INIT_CWD-aware project-dir resolution with NPM_PREFIX short-circuit — when INIT_CWD is empty or matches $NPM_PREFIX or $NPM_PREFIX/lib/node_modules, postinstall emits the 'global install no project context' advisory and exits 0 (per-project skill registration deferred to /orchestrator-init); (4) DRY_RUN=1 honor (D002 contract) — when set, emits `would_invoke=$INSTALLER --project-dir $PROJECT_DIR` + `would_check=~/.claude/ runtime presence` + `would_delegate=install-claude-code.sh` and exits 0 without invoking the installer; T03's byte-equivalence test depends on this contract; (5) Claude Code runtime probe via `~/.claude/` directory presence — absence yields `runtime_unavailable=true` advisory + exit 0 (soft-fail supports CI-runner-without-CC use case); (6) delegation to `packaging/install/install-claude-code.sh --project-dir "$INIT_CWD"` preserving [M025](../../../../../milestones/M025/index.md) manifest mechanism (CON-7 — postinstall does NOT bypass M025's reversibility-gate, the installer writes the manifest as it would on any other invocation).

The task-grain verifier `tools/verify/m035-p02-postinstall-shape.sh` (60 lines, executable) lands the AD-19 single-script Check shape with 9 PASS surfaces: 1 file-shape (exists + executable), 7 grep-based contract surfaces (Windows_NT name; MINGW*/CYGWIN*/MSYS* names; INIT_CWD; DRY_RUN; would_invoke= line; install-claude-code.sh delegation target; runtime_unavailable=true advisory path), and 1 functional DRY_RUN smoke test that invokes the postinstall under `INIT_CWD=<tmpdir> DRY_RUN=1` and asserts a `^would_invoke=` line appears on stdout (the closest mechanical proxy to a real-postinstall test we can run pre-publish — T03's npm-pack byte-equivalence is the next-level proxy; T04's release.yml exercises real `npm publish` only on `v*` tag-push events).

Verification: `bash tools/verify/m035-p02-postinstall-shape.sh` emits `BATTERY: pass=9 fail=0` (9 PASS lines — task plan estimated 8 but the script's verbatim shape yields 9: 1 file-shape + 7 grep + 1 functional smoke); `bash scripts/util/run-probe.sh /tmp/m035-p02-t02-postinstall-resolve.sh` emits `PASS: package.json scripts.postinstall resolves to executable: packaging/npm/postinstall.sh` (cross-reference contract between T01's package.json scripts.postinstall field and T02's authored target).

Plan-Time Discipline Rule 6 confirmed at execution: `packaging/npm/` directory and `packaging/npm/postinstall.sh` and `tools/verify/m035-p02-postinstall-shape.sh` were all absent on disk before the task ran (`ls packaging/npm/` -> 'No such file or directory'). Bash 3.2 / POSIX honored throughout the postinstall driver — no `declare -A`, no jq, no python, only `uname`/`grep`/`sed`/`case`/`echo`/parameter expansion. CON-3 / AP-009 honored — the cross-reference probe was staged via Write tool then executed via `scripts/util/run-probe.sh`, no inline compound chains.

One small payload deviation (mirroring T01's identical finding): the task plan's step-6 probe assumes `run-probe.sh` exports `REPO_ROOT` to child probes, but `scripts/util/run-probe.sh` computes REPO_ROOT locally without `export`. The staged probe defended with `REPO_ROOT="${REPO_ROOT:-/Users/brettkellgren/Sites/spec-kit-orchestrator}"` fallback so the probe runs regardless of run-probe.sh's behavior — semantically identical to the payload's intent. This is the same pattern T01's SUMMARY documented; the recommended follow-up (export REPO_ROOT in run-probe.sh OR update its docstring to clarify that probes must self-resolve) remains open and out of T02 scope.

Patterns established: npm-postinstall-as-thin-delegate (the postinstall is a guard + resolver + detector layer; actual skill registration delegates to the existing M025 install-claude-code.sh, so M025's manifest mechanism is preserved without bypass); INIT_CWD-with-npm-prefix-short-circuit (npm sets INIT_CWD to invocation dir; when it matches the npm global prefix or its node_modules subdir, treat as 'global install no project context' rather than registering against npm's own dir tree); soft-fail-on-runtime-absence (Claude Code missing yields advisory + exit 0, NOT exit 1 — supports the CI-runner-without-CC case where the operator wants the binary on PATH for later integration); Windows-belt-and-suspenders-guard-pattern (package.json os field is primary gate; postinstall uname -s case is secondary defense — if user reaches postinstall on Windows, npm config was bypassed and we fail loudly); DRY_RUN=1-as-pre-delegate-exit (DRY_RUN check fires AFTER INIT_CWD resolution but BEFORE installer invocation, so dry-run output captures the exact command that WOULD have run with the resolved project dir).

Reversibility: removing `packaging/npm/postinstall.sh` + the empty `packaging/npm/` dir + `tools/verify/m035-p02-postinstall-shape.sh` unwinds T02. T01's `package.json scripts.postinstall` reference would then dangle and `npm install` would fail — rollback ordering must be T02-before-T01-postinstall-field-removal per the task plan's Notes. The staged `/tmp/m035-p02-t02-postinstall-resolve.sh` probe is operator-cleanup-eligible post-verification.
