---
schema_version: "1.0"
type: task-summary
id: "T04"
parent: "P03"
milestone: "M035"
provides:
  - "references/installation.md extended with `## Installing via Homebrew` (consumer-facing: brew tap build-fractal/orchestrator + brew install orchestrator + per-project /orchestrator-init + brew uninstall + cross-channel byte-equivalence pointer to tests/m035-acceptance/cross-channel-byte-equivalence.sh) and `## Releasing via Homebrew` (operator-only: homebrew-publish job behavior, one-time operator setup, secrets.HOMEBREW_TAP_TOKEN provisioning under Build-Fractal/homebrew-orchestrator:contents:write scope, PAT rotation cadence + revocation, CON-6 secrets-scoped-to-tag-push compliance, GitHub App migration path); commands/update.md extended with a `## Update sources` section enumerating update_source: git | npm | homebrew dispatches (M035 P02 records the npm surface; M035 P03 records the homebrew surface; both wire into scripts/lifecycle/run-update.sh at P06); tools/verify/m035-p03-installation-doc-homebrew.sh task-grain doc-shape verifier (file-exists + 10 byte-exact pattern needles for both new sections); tools/verify/m035-p03-update-skill-doc-homebrew.sh task-grain doc-shape verifier (file-exists + 2 needles: `update_source: homebrew` + `brew upgrade`); tools/verify/m035-p03-phase-suite.sh phase-suite aggregator chaining all seven P03 per-truth verifiers (T01 formula-template-shape + render-formula-shape, T02 release-workflow-homebrew-job + release-workflow-con6-homebrew, T03 byte-equivalence-homebrew-arm, T04 installation-doc-homebrew + update-skill-doc-homebrew) and emitting canonical `BATTERY: pass=N fail=0 skip=K` line"
requires:
  - "from:P03/T01 what:tools/verify/m035-p03-formula-template-shape.sh + tools/verify/m035-p03-render-formula-shape.sh — chained by phase-suite aggregator; from:P03/T02 what:tools/verify/m035-p03-release-workflow-homebrew-job.sh + tools/verify/m035-p03-release-workflow-con6-homebrew.sh — chained; from:P03/T03 what:tools/verify/m035-p03-byte-equivalence-homebrew-arm.sh — chained (skip count surfaces upward when npm-absent); from:disk what:references/installation.md (post-P05 § Verifying integrity + § Channel-specific metadata files + § --with-<feature> Progressive Opt-In Flag Pattern + § See also already on disk; T04 appends additively at EOF); from:disk what:commands/update.md (pre-M035 interim driver, M035 P00 / M035 P05 rollback semantics, npm/homebrew unsupported-source-dispatch stubs already on disk); from:proposal what:.orchestrator/proposals/M035-packaging-distribution.md MOS-1 / MOS-2 / D008 PAT rotation / CON-6 — operator-doc content sourced from these decisions"
affects:
  - "P03 phase closure (the phase-suite aggregator is the M035-VALIDATED gate's P03 entry — closes P03 by emitting `BATTERY: pass=7 fail=0 skip=0` end-to-end on this machine); P04 curl-pipe-bash channel (will append a `## Installing via curl-pipe-bash` section adjacent to T04's homebrew section + add a `_byte-equivalence-hash.sh` curl-channel arm + a per-truth verifier the P04 phase-suite aggregator chains alongside the seven T01–T04 verifiers); P06 first-class `orchestrator:update` (wires `update_source: homebrew` from this skill's `## Update sources` section into scripts/lifecycle/run-update.sh's dispatch table — adopters discover the surface here; P06 makes it executable); operator runbook (one-time setup steps for HOMEBREW_TAP_TOKEN are now on disk under `## Releasing via Homebrew` so operators don't have to read the homebrew-publish job's YAML to provision the secret correctly)"
key_files:
  - "references/installation.md,commands/update.md,tools/verify/m035-p03-installation-doc-homebrew.sh,tools/verify/m035-p03-update-skill-doc-homebrew.sh,tools/verify/m035-p03-phase-suite.sh"
key_decisions:
  - "Insertion-point drift handled at the EOF: the plan §Steps 1 prescribed insertion `after the existing ## Verifying integrity section (which is the last section in the file post-P05)` but the file has grown since plan authoring — `## Verifying integrity` is now followed by `## Uninstall`, `## Migrating from spec-kit`, `## Verification`, and `## --with-<feature> Progressive Opt-In Flag Pattern` (with a `### See also` subsection). Per the dispatch guardrail, the two new Homebrew sections were appended at the actual EOF (after `### See also`) rather than inserted mid-file. This keeps later P05 / P03 readers' page-flow intact and avoids splitting the `## --with-<feature>` block from its `### See also` cross-references; commands/update.md adjacent-block placement: the existing skill mentioned `update_source: git|npm|homebrew` only inline at line 116 (Cross-references list) and via the M035-P05 unsupported-source-dispatch stubs (lines 73–75). No `## Update sources` section existed yet — per plan §Steps 2, a new `## Update sources` section was appended (listing git + npm + homebrew uniformly). Adjacency choice: placed immediately after the `### Unsupported source dispatches` H3 since that's the first first-class enumeration of the three sources by name; phase-suite aggregator chains EXACTLY the seven per-truth verifiers the plan names — no additions, no regressions; skip-count surfacing implemented via per-verifier BATTERY-line `skip=N` parse + aggregate sum (T03's verifier emits `skip=0` on this machine because npm IS on PATH — the npm-absent skip path is exercised on CI lanes without npm); commands/update.md note text bundles BOTH `update_source: npm` and `update_source: homebrew` into the new `## Update sources` section even though the plan only explicitly required the homebrew row — reasoning: a section listing only one of three sources reads as incomplete and the npm row's content is identical-in-shape (P06 wires the dispatch into run-update.sh, M035 P02 records the surface only); D-row not authored — the two design decisions T04 makes (EOF insertion vs mid-file; bundle npm + homebrew rows in the new `## Update sources` section) are local to T04 and do not warrant a D-row of their own. D007/D008 already bind the cross-channel + tap-publishing decisions"
patterns_established:
  - "phase-suite-aggregator-chains-per-truth-verifiers (Bash 3.2, single-script-file shape per AD-19, captures each verifier's stdout for diagnostic value when CI fails, parses per-verifier BATTERY-line skip counts and sums them upward, emits canonical `BATTERY: pass=N fail=0 skip=K` line the M035 acceptance battery in P06 greps for — pattern extends naturally to P04's curl-pipe-bash phase-suite aggregator, which adds one more verifier to the chain without changing the aggregator shape); doc-shape-verifier-via-needle-array (Bash 3.2 verifier authored as a single-script-file with for-loop over a literal needle array + `grep -qF` for fixed-string match, file-exists guard at top emitting BATTERY-pass=0-fail=1 + early exit, per-needle FAIL line surfaces missing pattern by name — pattern reusable for any future doc-surface contract gate); operator-runbook-on-disk-pattern (the `## Releasing via Homebrew` section is operator-only at the same surface as adopter-only sections — one document, multiple audiences, audience clearly labeled per section header. Adopters reading `## Installing via Homebrew` don't follow the operator section; operators reading `## Releasing via Homebrew` don't re-read the consumer section. Pattern extends to P04's curl-pipe-bash channel — `## Releasing via curl-pipe-bash` will follow the same shape); EOF-append-when-plan-insertion-point-drifts (when a plan prescribes insertion at a named anchor that's no longer the last section, append at actual EOF rather than splitting mid-document — preserves later sections' page-flow + cross-references. Surfaced explicitly in the dispatch report so the next plan-phase reader sees the insertion-point drift)"
drill_down_paths:
  - ".orchestrator/milestones/M035/phases/P03/tasks/T04-installation-doc-and-phase-suite-PLAN.md"
duration: "8m"
verification_result: "pass"
completed_at: "2026-05-09T00:00:00Z"
---

T04 authored the operator-facing Homebrew documentation in `references/installation.md` (consumer-facing `## Installing via Homebrew` + operator-only `## Releasing via Homebrew`), extended `commands/update.md` with a new `## Update sources` H2 enumerating git/npm/homebrew dispatches, shipped the two task-grain doc-shape verifiers (`m035-p03-installation-doc-homebrew.sh` + `m035-p03-update-skill-doc-homebrew.sh`), and shipped the P03 phase-suite aggregator (`m035-p03-phase-suite.sh`) chaining all seven per-truth verifiers across T01–T04.

Insertion-point drift surfaced at plan §Steps 1: the plan prescribed insertion `after the existing ## Verifying integrity section (which is the last section in the file post-P05)`, but the document has grown since the plan was authored — `## Verifying integrity` is now followed by `## Uninstall`, `## Migrating from spec-kit`, `## Verification`, and `## --with-<feature> Progressive Opt-In Flag Pattern` (with a `### See also` subsection). Per the dispatch guardrail's instruction to surface the choice, T04 appended the two new Homebrew sections at the actual EOF (after the `### See also` subsection) rather than splitting the `## --with-<feature>` block from its cross-references mid-document. This preserves the later sections' page-flow and keeps the `--with-` flag pattern intact for future P04+ readers.

`commands/update.md` placement: the existing skill mentioned `update_source: git|npm|homebrew` only inline at line 116's Cross-references list and via the M035 P05 unsupported-source-dispatch stubs (lines 73–75). No `## Update sources` H2 section existed yet — per plan §Steps 2's "if no such block exists, append a new `## Update sources` section listing all three" branch, T04 authored a fresh `## Update sources` section listing all three dispatches (git default + npm P02-recorded + homebrew P03-recorded). The section was placed adjacent to the existing `### Unsupported source dispatches` H3 since that block is the first first-class enumeration of the three sources by name — readers who reach the rollback-stub limitation now find the canonical source-dispatch enumeration immediately below it.

Phase-suite aggregator chains EXACTLY the seven per-truth verifiers the plan names — no additions, no regressions. The aggregator captures each verifier's stdout for diagnostic value when CI fails, parses per-verifier BATTERY-line `skip=N` counts via `sed -nE`, and sums them upward into the canonical `BATTERY: pass=N fail=0 skip=K` line the M035 acceptance battery in P06 will grep for. T03's byte-equivalence-homebrew-arm verifier emits `skip=0` on this machine because npm IS on PATH; the npm-absent skip path is exercised on CI lanes without npm and the aggregator's skip-sum surfaces it correctly.

D-row not authored. The two design decisions T04 makes — (a) EOF append vs mid-file insertion when the plan's anchor section drifted, (b) bundle npm + homebrew rows into the new `## Update sources` section even though only homebrew was explicitly required — are local to T04 and do not warrant a D-row of their own. D007 (cross-channel tarball-reuse architecture) and D008 (HOMEBREW_TAP_TOKEN PAT rotation cadence) already bind the load-bearing cross-channel + tap-publishing decisions for P03.

Verification: `bash tools/verify/m035-p03-installation-doc-homebrew.sh` → `BATTERY: pass=11 fail=0` (file-exists + 10 byte-exact needles: section headers, brew tap/install/uninstall, orchestrator --version, secrets.HOMEBREW_TAP_TOKEN, Build-Fractal/homebrew-orchestrator, PAT rotation cadence, CON-6). `bash tools/verify/m035-p03-update-skill-doc-homebrew.sh` → `BATTERY: pass=3 fail=0` (file-exists + 2 needles). `bash tools/verify/m035-p03-phase-suite.sh` → `BATTERY: pass=7 fail=0 skip=0` (T01: formula-template-shape 11/11 + render-formula-shape 8/8; T02: release-workflow-homebrew-job 9/9 + release-workflow-con6-homebrew 3/3; T03: byte-equivalence-homebrew-arm 9/9; T04: installation-doc-homebrew 11/11 + update-skill-doc-homebrew 3/3). End-to-end P03 contract live on disk.

Branch posture preserved: stayed on `main` the entire run, no `git checkout` / `git switch` / detached HEAD. Deliverables: `references/installation.md` modified (+96 lines, EOF append), `commands/update.md` modified (+6 lines, new `## Update sources` H2), `tools/verify/m035-p03-installation-doc-homebrew.sh` created (38 lines, executable), `tools/verify/m035-p03-update-skill-doc-homebrew.sh` created (29 lines, executable), `tools/verify/m035-p03-phase-suite.sh` created (51 lines, executable). Reversibility: removing the three new verifiers + reverting the two doc edits unwinds T04 cleanly; the phase regresses to its T03-state (homebrew arm in cross-channel-byte-equivalence test live, but no operator-runbook docs and no phase-suite aggregator).
