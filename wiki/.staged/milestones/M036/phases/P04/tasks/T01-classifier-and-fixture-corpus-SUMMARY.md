---
schema_version: "1.0"
type: task-summary
id: "T01"
parent: "P04"
milestone: "M036"
provides:
  - "tests/fixtures/m036-p04-reference-corpus/cms-rule/REF-cms-rule-fixture-01.md (cms-rule taxonomy fixture, tier 2, full FR-2 frontmatter); tests/fixtures/m036-p04-reference-corpus/cms-rule/REF-cms-rule-fixture-02.md (second cms-rule fixture, distinct content_hash); tests/fixtures/m036-p04-reference-corpus/training-material/REF-training-material-fixture-01.md (training-material taxonomy fixture, tier 2); tests/fixtures/m036-p04-reference-corpus/training-material/REF-training-material-fixture-02.md (second training-material fixture, distinct content_hash); tests/fixtures/m036-p04-reference-corpus/glossary/REF-glossary-fixture-01.md (glossary taxonomy fixture, tier 2); tests/fixtures/m036-p04-reference-corpus/regulatory-doc/REF-regulatory-doc-fixture-01.md (regulatory-doc taxonomy fixture, tier 1 per per-category default); tests/fixtures/m036-p04-reference-corpus/_negative/unknown-category/REF-blog-post-fixture.md (FR-1 taxonomy-rejection negative — category=blog-post outside the closed M036 taxonomy); tests/fixtures/m036-p04-reference-corpus/_negative/missing-source/REF-cms-rule-no-source.md (FR-2 required-field-rejection negative — source: field omitted); tests/fixtures/m036-p04-reference-corpus/_negative/tier-2-block/REF-cms-rule-blocked.md (FR-18 BLOCK-retention negative — tier_2_verdict: "BLOCK" frontmatter declared); scripts/knowledge/classify-reference.sh (~80-line pure helper lib per MEM004 — no top-level execution; sourceable; exposes classify_reference_required_fields(chunk-file) returning 0 if all six FR-2 required fields present + 1 with stderr error naming missing fields, and classify_reference_file(chunk-file) composing the FR-2 required-field check with bash-shelling-out to the existing P00 T03 tools/verify/lib/p00-validate-chunk-frontmatter.sh validator for the FR-1 taxonomy + tier-enum check; ROOT resolution via ${ORCHESTRATOR_ROOT:-$(cd "$(dirname "$0")/../.." && pwd)} for sourceable-from-anywhere support); 4 single-script-file shape verifiers under tools/verify/m036-p04-* (m036-p04-classifier-shape.sh — 7 checks: existence + executable + 5 token-presence on classify_reference_required_fields()/classify_reference_file()/p00-validate-chunk-frontmatter.sh/the FR-2 six-field list/MEM004; m036-p04-classifier-rejects-unknown.sh — sources the lib + drives classify_reference_file against the unknown-category negative fixture asserting rc=1 + stderr names "taxonomy"; m036-p04-classifier-rejects-missing-required.sh — sources the lib + drives classify_reference_required_fields against the missing-source negative fixture asserting rc=1 + stderr names "source"; m036-p04-fixture-corpus-shape.sh — 16 checks: 9 file-existence asserts + 5 frontmatter spot-checks on the cms-rule sample + 1 BLOCK-fixture tier_2_verdict declaration; AD-19 single-script-file shape, set -eu strict, grep -qF -e leading-dash-safe token-loops, ROOT resolution via ${ORCHESTRATOR_ROOT:-$(pwd)}, structured PASS:/FAIL:/SUMMARY: stdout)"
requires:
  - "P03 closed (P03-SUMMARY.md on disk); references/reference-taxonomy.md (P00 T01 — four-category SSOT); references/reference-frontmatter-contract.md (P00 T01 — six FR-2 required-field SSOT); references/reference-source-types.yaml (P00 T01 — per-category default-tier mapping); tools/verify/lib/p00-validate-chunk-frontmatter.sh (P00 T03 — taxonomy + tier-enum validator the classifier delegates to)"
affects:
  - "P04/T02 (ingest-reference.sh driver sources classify-reference.sh + walks the fixture corpus); P04/T03 (idempotency verifier + commands/ingest-reference.md consume the same corpus + classifier); P04/T04 (acceptance harness + phase-suite aggregator consume all of the above as a single battery)"
key_files:
  - "tests/fixtures/m036-p04-reference-corpus/cms-rule/REF-cms-rule-fixture-01.md,tests/fixtures/m036-p04-reference-corpus/cms-rule/REF-cms-rule-fixture-02.md,tests/fixtures/m036-p04-reference-corpus/training-material/REF-training-material-fixture-01.md,tests/fixtures/m036-p04-reference-corpus/training-material/REF-training-material-fixture-02.md,tests/fixtures/m036-p04-reference-corpus/glossary/REF-glossary-fixture-01.md,tests/fixtures/m036-p04-reference-corpus/regulatory-doc/REF-regulatory-doc-fixture-01.md,tests/fixtures/m036-p04-reference-corpus/_negative/unknown-category/REF-blog-post-fixture.md,tests/fixtures/m036-p04-reference-corpus/_negative/missing-source/REF-cms-rule-no-source.md,tests/fixtures/m036-p04-reference-corpus/_negative/tier-2-block/REF-cms-rule-blocked.md,scripts/knowledge/classify-reference.sh,tools/verify/m036-p04-classifier-shape.sh,tools/verify/m036-p04-classifier-rejects-unknown.sh,tools/verify/m036-p04-classifier-rejects-missing-required.sh,tools/verify/m036-p04-fixture-corpus-shape.sh"
key_decisions:
  - "none"
patterns_established:
  - "P04 inherits the M036 verifier conventions intact: milestone-prefixed slug (m036-p04-*), AD-19 single-script-file shape, grep -qF -e token-loop body for leading-dash safety, structured PASS:/FAIL:/SUMMARY: stdout, set -eu strict, ROOT resolution via ${ORCHESTRATOR_ROOT:-$(pwd)}; pure-lib MEM004 pattern carried into the classifier surface (classify-reference.sh has only function definitions — classify_reference_required_fields + classify_reference_file — no top-level execution; safe to source from any context including the future ingest-reference.sh driver and the _rejects-* verifiers); two-tier classifier composition pattern (FR-2 required-field presence check authored locally as a fast pre-pass + delegation to the existing P00 T03 tools/verify/lib/p00-validate-chunk-frontmatter.sh for the FR-1 taxonomy + tier-enum check — preserves single-source-of-truth for taxonomy lookup and avoids duplicating the cms-rule|training-material|glossary|regulatory-doc list in two places); _negative/<reason>/ subdirectory naming convention for ergonomically co-locating rejection-path fixtures with the positive corpus (T02's driver scans only top-level taxonomy-category subdirectories so _negative/ is silently skipped on positive-pass walks while remaining trivially addressable by per-rejection-mode verifiers); REF-* chunk frontmatter shape pre-bakes both id: and chunk_id: fields equal to REF-<category>-<cite_id> so rebuild-index.sh::fm_field can pick the chunk up unambiguously when the index extension lands later in the phase; full M036 chunk-frontmatter contract represented in fixtures (schema_version + type + milestone + id + category + chunk_id + cite_id + source + published + version + tier + content_hash + size_bytes + summary_mode + topic_tags + applies_to_field + scope_tags) so any future ingest-layer assertion has a guaranteed-shape input to test against; per-category default-tier alignment with references/reference-source-types.yaml (cms-rule/training-material/glossary at tier 2; regulatory-doc at tier 1) so the corpus exercises both tier values present in the M036 enum; tier-2-block fixture pattern — full valid frontmatter (passes FR-1 + FR-2) + an additional tier_2_verdict: "BLOCK" frontmatter field — so the FR-18 promote-vs-retain ingest invariant has a deterministic input that does not depend on running the live conversus gate (the verdict is pre-baked into the fixture so the ingest layer's BLOCK-handling path can be exercised in isolation from P03's gate machinery); shellcheck SC1090-disabled-with-comment shape for sourcing the classifier lib in verifier scripts (`. "$LIB"` after `# shellcheck disable=SC1090`) so dynamic-source warnings do not surface; per-scope tmpfile naming in /tmp/m036-p04-classifier-rejects-*-err.$$.txt — the $$ suffix isolates concurrent invocations and the trailing rm -f cleans up under set -eu without trapping; cross-task verifier-input dependency (T01's classifier-shape verifier asserts the lib exposes a "MEM004" token in its body so reviewers can trace the pattern attribution from the verifier output back to MEM004 in the knowledge graph — establishes the convention that pure-lib helpers in this milestone declare their pattern attribution in a structured comment that shape verifiers can grep for)"
drill_down_paths:
  - ".orchestrator/milestones/M036/phases/P04/tasks/T01-classifier-and-fixture-corpus-PAYLOAD.md"
duration: "25m"
verification_result: "pass"
completed_at: "2026-05-02T17:37:48Z"
---

T01 lands the foundational substrate for M036/P04 (reference-corpus ingest layer): the P04 reference-corpus fixture corpus + the classifier helper lib that the rest of P04's ingest driver consumes + four single-script-file shape verifiers gating each artifact.

**What was built**:

- **Fixture corpus** at `tests/fixtures/m036-p04-reference-corpus/` — 9 hand-authored REF chunks total. 6 valid chunks across the four M036 taxonomy categories (cms-rule × 2, training-material × 2, glossary × 1, regulatory-doc × 1) with full FR-2 frontmatter (`source`, `published`, `version`, `cite_id`, `topic_tags`, `applies_to_field`) plus the M036 chunk-frontmatter contract fields (`schema_version`, `type`, `milestone`, `id`, `category`, `chunk_id`, `tier`, `content_hash`, `size_bytes`, `summary_mode`, `scope_tags`). 3 negative-path chunks under `_negative/<reason>/` subdirectories: `_negative/unknown-category/REF-blog-post-fixture.md` (FR-1 rejection — `category: blog-post` outside the closed taxonomy); `_negative/missing-source/REF-cms-rule-no-source.md` (FR-2 rejection — `source:` field omitted); `_negative/tier-2-block/REF-cms-rule-blocked.md` (FR-18 BLOCK-retention — full valid frontmatter plus `tier_2_verdict: "BLOCK"`). The `_negative/` prefix lets T02's driver scan only top-level taxonomy-category basenames while keeping rejection-path fixtures trivially addressable by per-mode verifiers. CON-3 honored: text-only Markdown — no binaries.

- **Classifier helper** at `scripts/knowledge/classify-reference.sh` (~80 lines) — pure helper lib per MEM004 (no top-level execution; sourceable from any caller). Two functions: `classify_reference_required_fields(chunk-file)` enumerates the six FR-2 required fields and returns 1 with stderr error naming the missing field(s) on any miss; `classify_reference_file(chunk-file)` composes the FR-2 check with a bash-shelling-out to the existing P00 T03 `tools/verify/lib/p00-validate-chunk-frontmatter.sh` validator for the FR-1 taxonomy + tier-enum check. ROOT resolution via `${ORCHESTRATOR_ROOT:-$(cd "$(dirname "$0")/../.." && pwd)}` so the lib is sourceable from any working directory. Bash 3.2 / POSIX-sh per CON-2.

- **Four shape verifiers** under `tools/verify/m036-p04-*`:
  - `m036-p04-classifier-shape.sh` (7 checks): lib existence + executable bit + 5 token-presence asserts (`classify_reference_required_fields()`, `classify_reference_file()`, `p00-validate-chunk-frontmatter.sh`, the six-field FR-2 list, `MEM004` pattern attribution).
  - `m036-p04-classifier-rejects-unknown.sh`: sources the lib + drives `classify_reference_file` against the unknown-category negative fixture; asserts rc=1 + stderr names `taxonomy`.
  - `m036-p04-classifier-rejects-missing-required.sh`: sources the lib + drives `classify_reference_required_fields` against the missing-source negative fixture; asserts rc=1 + stderr names `source`.
  - `m036-p04-fixture-corpus-shape.sh` (16 checks): 9 file-existence asserts (6 valid + 3 negative) + 5 frontmatter spot-checks on the cms-rule sample + 1 BLOCK-fixture `tier_2_verdict` declaration.

All four verifiers carry the M036 verifier conventions: AD-19 single-script-file shape, `set -eu` strict, `grep -qF -e "$pat"` leading-dash-safe token-loops (carried from M036/P02 + M036/P03), ROOT resolution via `${ORCHESTRATOR_ROOT:-$(pwd)}`, structured `PASS:` / `FAIL:` / `SUMMARY:` stdout protocol per MEM001.

**Verification**:

- `bash tools/verify/m036-p04-classifier-shape.sh` → `SUMMARY: m036-p04-classifier-shape.sh fail=0` (7 PASS lines).
- `bash tools/verify/m036-p04-classifier-rejects-unknown.sh` → `SUMMARY: m036-p04-classifier-rejects-unknown.sh fail=0` (rc=1 + stderr names "taxonomy" both pass).
- `bash tools/verify/m036-p04-classifier-rejects-missing-required.sh` → `SUMMARY: m036-p04-classifier-rejects-missing-required.sh fail=0` (rc=1 + stderr names "source" both pass).
- `bash tools/verify/m036-p04-fixture-corpus-shape.sh` → `SUMMARY: m036-p04-fixture-corpus-shape.sh fail=0` (9 file-presence + 5 frontmatter + 1 BLOCK-verdict — 15 PASS lines, 0 fail).

**Patterns established**:

- Pure-lib MEM004 carried into the P04 classifier surface (function-definitions-only, no top-level I/O, sourceable from drivers + verifiers alike).
- Two-tier classifier composition: FR-2 required-field check authored locally as a fast pre-pass + delegation to the existing P00 T03 chunk-frontmatter validator for the FR-1 taxonomy + tier-enum check (preserves single-source-of-truth for taxonomy lookup; avoids duplicating the four-category list).
- `_negative/<reason>/` subdirectory naming convention for co-locating rejection-path fixtures with the positive corpus without polluting T02's driver walk (driver descends only into top-level taxonomy-category basenames).
- REF-* chunk frontmatter shape pre-bakes both `id:` and `chunk_id:` fields (each equal to `REF-<category>-<cite_id>`) so the future `rebuild-index.sh::fm_field` extension picks the chunk up unambiguously.
- Tier-2-block fixture pattern — full valid frontmatter plus `tier_2_verdict: "BLOCK"` — gives the FR-18 promote-vs-retain ingest invariant a deterministic input that does not depend on running the live conversus gate.
- MEM004 token-attribution-in-comment pattern: pure-lib helpers in this milestone declare their pattern attribution (`MEM004`) in a structured header comment that shape verifiers grep for, establishing a traceable link from verifier output back to the knowledge graph.

**Forward notes**:

- **T02** (driver `scripts/knowledge/ingest-reference.sh` + rebuild-index extension) sources `classify-reference.sh` as a function library, walks the four taxonomy-category subdirectories under `--reference-root`, calls `classify_reference_file` per chunk for FR-1 + FR-2 gating, and emits structured `CREATED:` / `REJECTED:` / `BLOCKED:` stdout per the FR-18 contract. The fixture corpus authored here is the deterministic input the T02 driver-shape + `m036-p04-driver-shape` verifiers exercise.
- **T03** (idempotency verifier + `commands/ingest-reference.md`) builds on the content-hash gate and consumes the same corpus.
- **T04** (acceptance harness + phase-suite aggregator) consumes all of the above as a single SC-driven battery.
