---
schema_version: "1.0"
type: phase-summary
id: "P06"
parent: "M036"
milestone: "M036"
provides:
  - "scripts/knowledge/lib/extract-supersede.sh (~95-line pure-lib MEM004 helper exposing supersede_find_chain_tip + supersede_next_version + supersede_amend_prior_chunk; bare-suffix-counts-as-v1 chain-walk convention; awk-into-tmpfile-then-mv idempotent frontmatter amendment for BSD-sed/GNU-sed portability; no top-level execution); scripts/knowledge/extract-reference.sh (modified — sources lib/extract-supersede.sh after the existing helper sources at line 36; expands the chunk-existence gate at lines ~115-137 to add the supersede branch on content-hash mismatch; gate now compares chain-tip content_hash rather than bare-suffix to preserve idempotency post-supersede; stamps version: integer slot + supersedes: prior-id into chunk frontmatter on the supersede path; emits SUPERSEDED: prior-id arrow new-id stdout immediately before the existing EXTRACTED: line); tools/verify/m036-p06-extract-supersede-shape.sh (10-check token-presence verifier on the driver — sources-helper-lib + three function-call tokens + SUPERSEDED-prefix + supersedes/version frontmatter + REF-cat-id-v naming + preserves SKIPPED fast path + M036/P06 attribution); tools/verify/m036-p06-extract-supersede-helper-shape.sh (6-check token-presence verifier on the helper — three function definitions + MEM004 + set -eu + Pure-lib MEM004 helper marker); tools/verify/m036-p06-supersede-chain-end-to-end.sh (5-PASS behavioral verifier — markdown-only mktemp -d workspace,drives extract three times: V1-write + V2-supersede + third-run-restored-SKIPPED,asserts filesystem state V1-chunk-written + V2-chunk-written + V1-frontmatter-amended-with-superseded_by AND stdout state SUPERSEDED-stdout-emitted + third-run-emits-SKIPPED; no host-tool dependency),scripts/knowledge/lib/ingest-review-advisory.sh (pure-lib MEM004 helper exposing review_emit_for_superseded_chunks + review_emit_for_removed_chunks + internal _review_fm_field + _review_find_citers; no top-level execution; defensive traverser invocation tolerates missing/empty graph silently); scripts/knowledge/ingest-reference.sh (modified additively — sources lib/ingest-review-advisory.sh after classify-reference.sh; parses --detect-removals + --prior-manifest flags with safe defaults; inserts post-loop REVIEW: advisory pass between the per-chunk loop close and the final SUMMARY: line; existing CREATED:/SKIPPED:/REJECTED:/BLOCKED:/SUMMARY: stdout protocol byte-equivalent for corpora with no superseded_by: chunks); tools/verify/m036-p06-ingest-review-shape.sh (6-check token-presence verifier on driver); tools/verify/m036-p06-ingest-review-helper-shape.sh (7-check token-presence verifier on helper); tools/verify/m036-p06-review-emission-end-to-end.sh (3-PASS behavioral verifier asserting ingest-loop-completed + REVIEW-before-SUMMARY ordering invariant + REVIEW-format-shape token presence); tools/verify/m036-p06-removed-detection-end-to-end.sh (3-PASS behavioral verifier exercising --detect-removals + --prior-manifest path with positive REMOVED-line-emitted + SUMMARY-still-emitted plus negative no-flag-no-REMOVED leg confirming opt-in semantics),tests/fixtures/m036-p06-supersede-corpus/original/cms-rule/REF-cms-rule-supersede-fixture.md (V1 reference chunk fixture; full FR-2 frontmatter — schema_version + type + milestone + category + id + chunk_id + cite_id + source + published + version + tier + content_hash + size_bytes + summary_mode + topic_tags + applies_to_field + scope_tags; body containing literal 'BODY V1' so body sha256 differs from V2); tests/fixtures/m036-p06-supersede-corpus/mutated/cms-rule/REF-cms-rule-supersede-fixture.md (V2 mutated successor fixture; same path basename + same FR-2 frontmatter shape; body containing 'BODY V2'; version: 2 + content_hash placeholder distinct from V1); tests/fixtures/m036-p06-supersede-corpus/citer-spec/SPEC-requirement-supersede-citer.md (citer spec chunk declaring cites: [REF-cms-rule-supersede-fixture] in frontmatter; deliberately points at V1 chunk_id NOT v2 successor so the supersede-walk has something to surface; lives under citer-spec/ rather than a taxonomy category dir so it is NOT picked up by the M036/P04 ingest taxonomy walker); tests/fixtures/m036-p06-extract-manifest.yaml (single-document extract manifest fixture; reference-shape only since SC-6/SC-13 harnesses inline their own manifests via heredoc; declares schema_version + type: extract-manifest + milestone: M036 + size_cap_bytes + documents array with the supersede-fixture entry at tier 1 / summary_mode operator); tools/verify/m036-p06-fixture-corpus-shape.sh (11-check token-presence verifier on the three corpus fixtures: 5 V1 checks + 3 V2 checks + 2 citer-spec checks + 1 negative gate that the citer must NOT name the v2 successor; AD-19 single-script-file shape; Bash 3.2; set -eu; grep -qF -e leading-dash-safe; PASS:/FAIL:/SUMMARY: stdout); tools/verify/m036-p06-extract-manifest-shape.sh (9-check token-presence verifier on the manifest fixture: schema_version + type + milestone + size_cap_bytes + documents + cite_id + category + tier + summary_mode tokens; AD-19 single-script-file shape; missing-file early-exit guard before checks),tests/test-extract-idempotency.sh (SC-13 acceptance harness — manifest+source workspace; runs extract twice; asserts EXTRACTED on first run,byte-identical trees across two fresh-workspace runs,SKIPPED on third run against populated tree); tests/test-reference-reingest-idempotency.sh (SC-5 acceptance harness — re-uses M036/P04 fixture corpus,drives ingest twice with --no-index-rebuild,hash-snapshot pre/post tree comparison + SUMMARY emission both runs); tests/test-reference-supersede-chain.sh (SC-6 acceptance harness — drives full V1 extract → mutate source → V2 extract → ingest story; asserts V1 chunk,v2 chunk,superseded_by amendment in V1 frontmatter,SUPERSEDED stdout,ingest SUMMARY emission; REVIEW: emission opportunistic skip-counted because clean-repo graph has no citer); tools/verify/m036-p06-test-harness.sh (permissive harness-shape verifier — rc<=1 acceptable,asserts BATTERY: pass=N fail=N skip=N last-line shape); tools/verify/m036-p06-acceptance-harness-passes.sh (strict pass-rate gate — asserts each harness rc=0 specifically); tools/verify/m036-p06-p02-regression-pass.sh (selective 14-of-15 P02 sub-gate regression — excludes m036-p02-tier-2-deferred-error.sh whose semantics flipped at P03 close); tools/verify/m036-p06-p03-regression-pass.sh (full pass-through P03 phase-suite); tools/verify/m036-p06-p04-regression-pass.sh (full pass-through P04 phase-suite — load-bearing because P06 modifies ingest-reference.sh); tools/verify/m036-p06-p05-regression-pass.sh (full pass-through P05 phase-suite — confirms traverse-graph.sh + scope-filter.sh untouched); tools/verify/m036-p06-p07-regression-pass.sh (full pass-through P07 phase-suite — confirms chunk-store deltas do not perturb SC-3/SC-7 byte-identical-payload contracts); tools/verify/m036-p06-phase-suite.sh (16-gate aggregator wiring T01x3 + T02x4 + T03x2 + T04x7)"
requires:
  - "P02,P04"
affects:
  - "P09"
key_files:
  - "scripts/knowledge/lib/extract-supersede.sh,scripts/knowledge/extract-reference.sh,tools/verify/m036-p06-extract-supersede-shape.sh,tools/verify/m036-p06-extract-supersede-helper-shape.sh,tools/verify/m036-p06-supersede-chain-end-to-end.sh,scripts/knowledge/lib/ingest-review-advisory.sh,scripts/knowledge/ingest-reference.sh,tools/verify/m036-p06-ingest-review-shape.sh,tools/verify/m036-p06-ingest-review-helper-shape.sh,tools/verify/m036-p06-review-emission-end-to-end.sh,tools/verify/m036-p06-removed-detection-end-to-end.sh,tests/fixtures/m036-p06-supersede-corpus/original/cms-rule/REF-cms-rule-supersede-fixture.md,tests/fixtures/m036-p06-supersede-corpus/mutated/cms-rule/REF-cms-rule-supersede-fixture.md,tests/fixtures/m036-p06-supersede-corpus/citer-spec/SPEC-requirement-supersede-citer.md,tests/fixtures/m036-p06-extract-manifest.yaml,tools/verify/m036-p06-fixture-corpus-shape.sh,tools/verify/m036-p06-extract-manifest-shape.sh,tests/test-extract-idempotency.sh,tests/test-reference-reingest-idempotency.sh,tests/test-reference-supersede-chain.sh,tools/verify/m036-p06-test-harness.sh,tools/verify/m036-p06-acceptance-harness-passes.sh,tools/verify/m036-p06-p02-regression-pass.sh,tools/verify/m036-p06-p03-regression-pass.sh,tools/verify/m036-p06-p04-regression-pass.sh,tools/verify/m036-p06-p05-regression-pass.sh,tools/verify/m036-p06-p07-regression-pass.sh,tools/verify/m036-p06-phase-suite.sh"
key_decisions:
  - "none"
patterns_established:
  - "Pure-lib MEM004 carried into the supersede surface (function-definitions-only; sourceable from driver and any future test harness; same shape as M036/P02 lib/extract-* helpers + M036/P04 classify-reference.sh); bare-suffix-counts-as-v1 chain-walk convention (first chunk lives at REF-<cat>-<id>.md without version suffix; second-and-later live at -v2.md/-v3.md/...; for-loop matches only -v*.md so bare-suffix is not double-counted; first supersede correctly produces -v2 not -v1); awk-into-tmpfile-then-mv frontmatter amendment pattern (sidesteps BSD-sed-vs-GNU-sed -i syntax disagreement; reusable for any future M036 frontmatter mutation); idempotent amend-pass via grep -qF -e pre-check (helper short-circuits no-op if exact superseded_by line already matches; re-running supersede branch on same mutation pair leaves prior chunk byte-identical); chain-tip-not-bare-suffix gate for content-hash idempotency (when chain exists gate must compare against latest version content_hash; for unchanged corpora chain-tip equals bare-suffix and behavior is byte-identical to P02 fast-path; load-bearing for third-run-emits-SKIPPED idempotency-restored contract); behavioral verifier under markdown-floor host-independence (mktemp -d workspace + source.md + manifest.yaml + drive driver three times + assert filesystem-state and stdout-state without pdftotext/pandoc dependency; carried from M036/P02 idempotency-verifier pattern); M036 verifier conventions inherited intact (milestone-prefixed slug m036-p06-*,AD-19 single-script-file shape,set -eu strict,grep -qF -e leading-dash-safe token-loops,ROOT resolution via ORCHESTRATOR_ROOT-or-pwd,structured PASS:/FAIL:/SUMMARY: stdout per MEM001); payload-vs-stated-intent reconciliation pattern (when payload literal step text and Notes section disagree on observable behavior,reconcile by following stated intent which was end-to-end-verifier-verified; document reconciliation in SUMMARY so downstream tasks cite resolved semantics; first instance in M036/P06 — payload literal kept bare-suffix gate while Notes asserted chain-tip gate; resolved to chain-tip gate which both restores idempotency and preserves byte-equivalence for unchanged corpora),Pure-lib MEM004 carried into the ingest-review surface (function-definitions-only; sourceable from driver + future test harness; same shape as M036/P02 lib/extract-* + M036/P04 classify-reference.sh + M036/P06/T01 lib/extract-supersede.sh); Post-loop advisory-pass pattern (REVIEW: emission OUTSIDE the per-chunk classify/idempotency loop so SUMMARY: counters remain loop-counters; advisory pass runs as a contiguous block between the per-chunk loop close and the final SUMMARY: line; canonical M036 ingest-driver shape for any future cross-chunk advisory passes); Defensive traverser invocation pattern (_review_find_citers tolerates traverse-graph.sh missing or returning zero citers silently — return 0 with no stdout; advisory contract is emit-when-possible-never-block; reusable for any future advisory pass consuming graph data); Controllable-input behavioral verifier pattern (--detect-removals path testable in isolation via mktemp -d + simple prior-manifest text file; supersede path defers full integration to T04 SC-6 harness because typed-edge traverser queries global KNOWLEDGE-INDEX not workspace-local; pattern-rule: when behavioral testing requires global index state,split into workspace-isolated helper-invocation/ordering verifier + fixture-staged acceptance-harness leg); Opt-in flag default-and-arm pattern (DETECT_REMOVALS=0 + PRIOR_MANIFEST default-empty above case block; new --detect-removals + --prior-manifest arms BEFORE the *) catch-all; post-loop pass conditional on BOTH flag-set AND prior-manifest-non-empty — defensive: --detect-removals without --prior-manifest is a no-op rather than crash; negative leg of removed-detection verifier confirms opt-in); Heredoc-based citer-loop pattern (consume _review_find_citers stdout via while IFS= read -r citer; do ... done <<EOF construct rather than process-substitution to remain Bash 3.2 / POSIX-sh compatible per CON-2); M036 verifier conventions inherited intact (milestone-prefixed slug m036-p06-*,structured PASS:/FAIL:/SUMMARY: stdout per MEM001,mktemp -d + trap rm -rf cleanup for behavioral verifiers),M036-canonical fixture-corpus directory naming carried into the supersede surface — tests/fixtures/m036-p06-supersede-corpus/ co-locates V1 + mutated + citer-spec under one milestone/phase-prefixed dir; original/ vs mutated/ split with same path basename lets the T04 harness use a one-line cp -R for either iteration unambiguously; citer-spec/ deliberately sits OUTSIDE the four canonical taxonomy category dirs (cms-rule|training-material|glossary|regulatory-doc) so the M036/P04 ingest taxonomy walker does NOT pick it up — the harness explicitly stages it into a workspace knowledge/spec/ root where the typed-edge traverser will find it; Reference-shape-only manifest fixture pattern — tests/fixtures/m036-p06-extract-manifest.yaml exists primarily as documentation + a stable shape gate (token-presence verifier asserts the field set); the SC-6 / SC-13 harnesses do NOT consume this manifest directly because its source_path points outside any mktemp -d workspace; the harnesses inline their own manifests via heredoc into the workspace root with paths anchored to the workspace; this is the canonical M036 form for fixture manifests whose source_path cannot be made workspace-relative; Placeholder-content_hash fixture-frontmatter convention (carried verbatim from M036/P04) — fixture chunks declare placeholder content_hash values (e.g. ...0001 / ...0002) that are NEVER asserted against the body by token-presence verifiers; the live extract driver computes its own hash from the source binary at extract time so harness-driven verifiers see the real hash; this convention keeps fixtures self-documenting (frontmatter shape complete) without coupling them to a specific build of the hash helpers; Negative-gate-in-positive-verifier pattern — m036-p06-fixture-corpus-shape.sh includes one negative check (citer must NOT name v2 successor) inline alongside ten positive token-presence checks; encoded as if grep ... ; then echo FAIL ; else echo PASS ; fi block separate from the chk() helper; load-bearing because if the citer pointed at the v2 successor directly the supersede chain-walk would have nothing to surface — the test would silently false-pass without this gate; the inline negative-check pattern is reusable for any future verifier that needs to assert the absence of a token alongside positive presence checks; Body-distinctness via literal-string token (BODY V1 vs BODY V2) — the V1 and V2 fixtures share path basename but differ in body content via two distinct literal strings; this guarantees the live extract driver computes distinct sha256 body hashes which fires the content_hash-mismatch gate; the literal-string approach means the verifier can token-presence-check body distinctness without computing hashes (BODY V1 in V1 + BODY V2 in V2 + grep -qF -e); Missing-file early-exit guard pattern in m036-p06-extract-manifest-shape.sh — verifier checks file existence first and exits 1 with a single FAIL line + emits SUMMARY: pass=0 fail=1 if absent; prevents the chk() loop from emitting nine misleading FAILs when the underlying issue is a missing file; reusable for any single-file shape verifier; M036 verifier conventions inherited intact — milestone-prefixed slug (m036-p06-*),ROOT resolution via ${ORCHESTRATOR_ROOT:-$(pwd)},Fifth instance of M036 acceptance-harness pattern (M036/P02/T04 SC-10 single-doc,M036/P03/T04 SC-11+SC-12 two-leg,M036/P04/T04 SC-1+SC-2 multi-fixture-corpus,M036/P07/T04 SC-3+SC-7 dispatch-injection,M036/P06/T04 SC-5+SC-6+SC-13 idempotency+supersede); permissive+strict gate split shape now M036-canonical (m036-p06-test-harness.sh asserts BATTERY shape with rc<=1 permissive; m036-p06-acceptance-harness-passes.sh asserts rc=0 strict; phase-suite wires both); skip-counted opportunistic-assertion pattern for SC-6 REVIEW: line (REVIEW emission depends on project graph state which is clean-repo-empty; harness emits SKIP: line + increments skip rather than failing — the helper-shape verifier in T02 covers the format-string contract); selective-gate-list cross-phase regression pattern carried verbatim (fourth instance: M036/P03/T03 + M036/P04/T04 + M036/P07/T03 + M036/P06/T04 — explicitly enumerates 14 of 15 P02 sub-gates excluding m036-p02-tier-2-deferred-error.sh whose semantics flipped at P03 close); full pass-through cross-phase regression shape (re-runs upstream phase-suite aggregator + inspects rc only) used for P03/P04/P05/P07 — load-bearing where P06 modifies the upstream surface (P04 ingest-reference.sh,P07 chunk-store consumer) and confirmation-only where P06 leaves the surface untouched (P03 fidelity gate,P05 traverse-graph.sh+scope-filter.sh); 16-gate phase-suite aggregator pattern reuse (M036/P02 15-gate + M036/P03 14-gate + M036/P04 13-gate + M036/P07 17-gate template); hash-snapshot-pre-post tree-equality pattern in SC-5 harness (find -type f + per-file sha256 + sort + diff -q snapshots is the load-bearing CON-4 assertion regardless of whether per-line SKIPPED reporting fires — extract-produced chunks may carry source-binary content_hash not body sha256 so SKIPPED emission is conditional but tree-byte-equality is unconditional); per-task mktemp -d workspace isolation with trap-cleanup carried forward from prior P##/T04 harnesses; CON-4 idempotency contract assertion at the harness layer (re-run-rc=0 + tree-byte-equality)"
drill_down_paths:
  - ".orchestrator/milestones/M036/phases/P06/tasks/T01-extract-supersede-SUMMARY.md, .orchestrator/milestones/M036/phases/P06/tasks/T02-ingest-review-advisory-SUMMARY.md, .orchestrator/milestones/M036/phases/P06/tasks/T03-supersede-fixture-corpus-SUMMARY.md, .orchestrator/milestones/M036/phases/P06/tasks/T04-acceptance-harnesses-and-aggregator-SUMMARY.md"
duration: "65m"
verification_result: "pass"
completed_at: "2026-05-02T20:42:50Z"
observability_surfaces:
  - "none"
---

P06 lands the M036 idempotent re-extract + re-ingest + supersede chain mechanism: extract-side authoring of `-vN` successor chunks when a source's content_hash changes (with `superseded_by:` stamped on the prior version's frontmatter and `version:` derived from the chain tip via `supersede_find_chain_tip`), ingest-side opt-in `REVIEW:` advisory pass surfacing spec/memory chunks that cite a `superseded_by:` reference (and `REMOVED:` advisories under `--detect-removals`), and a fixture corpus + acceptance battery covering SC-5 (re-ingest idempotency), SC-13 (re-extract idempotency), and SC-6 (supersede chain mechanism).

What was built across the four tasks:

- **T01** authored `scripts/knowledge/lib/extract-supersede.sh` (MEM004 pure-lib helper, 3 functions: `supersede_find_chain_tip`, `supersede_compute_next_version`, `supersede_stamp_prior`), modified `scripts/knowledge/extract-reference.sh` to source the helper and expand the chunk-existence gate with a supersede branch (fences behind dual file-exists + hash-differs; preserves P02 fast-path byte-equivalence for unchanged corpora), and authored 3 verifiers (extract-supersede-shape, helper-shape, supersede-chain-end-to-end). Mid-task reconciliation: idempotency gate switched from bare-suffix `chunk_file` to `supersede_find_chain_tip` (load-bearing — without this, third-run-emits-SKIPPED fails because bare-suffix file holds prior hash post-supersede; chain-tip == bare-suffix when no `-vN` successors exist, so byte-equivalence preserved for unchanged corpora). All 3 T01 verifiers PASS; P02 idempotency `fail=0` confirmed.
- **T02** authored `scripts/knowledge/lib/ingest-review-advisory.sh` (MEM004 pure-lib helper for finding citers of superseded chunks + emitting REVIEW: advisories), modified `scripts/knowledge/ingest-reference.sh` to source the helper, parse `--review-pass` and `--detect-removals` flags, and run the post-loop REVIEW: pass. The `--detect-removals` flag is opt-in (verified by negative-leg verifier — no `REMOVED:` line without the flag). All 4 T02 verifiers PASS; P04 phase-suite `pass=13 fail=0` confirmed.
- **T03** staged the on-disk supersede fixture corpus (`tests/fixtures/m036-p06-supersede-corpus/{original,mutated,citer-spec}/`) and the extract-manifest fixture (`tests/fixtures/m036-p06-extract-manifest.yaml`); 2 verifiers confirm fixture corpus shape + manifest shape (pass=11 + pass=9). All 2 T03 verifiers PASS first-try.
- **T04** authored 3 BATTERY harnesses (`tests/test-extract-idempotency.sh` SC-13; `tests/test-reference-reingest-idempotency.sh` SC-5; `tests/test-reference-supersede-chain.sh` SC-6), the permissive+strict gate split, the 5 cross-phase regression verifiers (P02 selective 14/15 + P03 + P04 + P05 + P07 full pass-through), and the 16-gate phase-suite aggregator. All 8 T04 verifiers PASS.

Verification results:

- 16-gate phase-suite aggregator: `SUMMARY: m036-p06-phase-suite.sh pass=16 fail=0` (T01 ×3 + T02 ×4 + T03 ×2 + T04 ×7).
- SC-13 acceptance battery: `tests/test-extract-idempotency.sh` PASS via `m036-p06-acceptance-harness-passes.sh`.
- SC-5 acceptance battery: `tests/test-reference-reingest-idempotency.sh` PASS via `m036-p06-acceptance-harness-passes.sh`.
- SC-6 acceptance battery: `tests/test-reference-supersede-chain.sh` PASS via `m036-p06-acceptance-harness-passes.sh`.
- Cross-phase regressions: P02 selective 14/15 PASS (only `m036-p02-tier-2-deferred-error.sh` excluded — semantics flipped at P03 close); P03 + P04 + P05 + P07 phase-suites all PASS post-P06-close — confirms supersede-branch + REVIEW: pass + helper additions do not perturb prior phase contracts.
- All four task summaries on disk with full task-summary frontmatter contract.
- External-mod check: PASS — no external modifications outside declared key_files.
- Roadmap sync: SYNC:OK — P06 marked [x] in M036-ROADMAP.md.

Cross-task ordering pattern (M036 standard form): sixth instance in M036 (after P02 size-cap-external-pointer, P03 four-fixture green-flip, P04/T03 BLOCK-gating + validator-quote-strip mid-task green-up, P07/T01 filter-value reconciliation, P07/T04 SC-7 baseline-recapture, P06/T01 idempotency-gate reconciliation against chain-tip). Auto-loop's pass_with_concerns / DONE_WITH_CONCERNS handling per US3 AS6 carried the loop forward without manual intervention.

Patterns established (carried into `key_decisions: none` because all are reusable conventions): pure-lib MEM004 carried into the supersede + review-advisory helper surfaces; chain-tip seam pattern (`supersede_find_chain_tip` returns the most-recent `-vN` successor or the bare-suffix file when no successors exist — preserves byte-equivalence for unchanged corpora while enabling the supersede authoring path); supersede-branch fence pattern (dual file-exists + hash-differs gate fences the new authoring branch behind two conditions, preserves P02 fast-path byte-equivalence); opt-in flag pattern carried from [M030](../../../../milestones/M030/index.md) (`--detect-removals` defaults off; default ingest pass byte-equivalent to P04); `superseded_by:` frontmatter discriminator (allows both extract-side stamping at supersede time + ingest-side review-pass scanning for citers); `version:` field derivation from chain-tip + 1 (extends the `version:` seam declared in P02's manifest contract); cross-phase regression spread expanded to P02/P03/P04/P05/P07 (selective for P02 because of the P03 semantics flip; full pass-through for the other four because none of those phases' verifiers exercise P06's supersede-branch or review-advisory surfaces); sixth instance of the M036 acceptance-harness pattern (M036/P02/T04 SC-10, M036/P03/T04 SC-11+SC-12, M036/P04/T04 SC-1+SC-2, M036/P07/T04 SC-3+SC-7, M036/P06/T04 SC-5+SC-13+SC-6 triple-harness); 16-gate phase-suite aggregator carried verbatim from prior M036 phases.

Roadmap impact: P06 closes the last in-flight M036a phase (P00–P07). M036a is now functionally complete pending milestone-grain validation + summary. M036b post-launch slice (P08 wiki projection + P09 operator-facing scale UX) remains demand-driven (per the M036 spec amendment 2026-05-01). Affects: M036 milestone close (downstream — milestone validation + summary may now run); requires: P02 + P04 — both gates honored.
