---
schema_version: "1.0"
type: task-summary
id: "T03"
parent: "P06"
milestone: "M036"
provides:
  - "tests/fixtures/m036-p06-supersede-corpus/original/cms-rule/REF-cms-rule-supersede-fixture.md (V1 reference chunk fixture; full FR-2 frontmatter — schema_version + type + milestone + category + id + chunk_id + cite_id + source + published + version + tier + content_hash + size_bytes + summary_mode + topic_tags + applies_to_field + scope_tags; body containing literal 'BODY V1' so body sha256 differs from V2); tests/fixtures/m036-p06-supersede-corpus/mutated/cms-rule/REF-cms-rule-supersede-fixture.md (V2 mutated successor fixture; same path basename + same FR-2 frontmatter shape; body containing 'BODY V2'; version: 2 + content_hash placeholder distinct from V1); tests/fixtures/m036-p06-supersede-corpus/citer-spec/SPEC-requirement-supersede-citer.md (citer spec chunk declaring cites: [REF-cms-rule-supersede-fixture] in frontmatter; deliberately points at V1 chunk_id NOT v2 successor so the supersede-walk has something to surface; lives under citer-spec/ rather than a taxonomy category dir so it is NOT picked up by the M036/P04 ingest taxonomy walker); tests/fixtures/m036-p06-extract-manifest.yaml (single-document extract manifest fixture; reference-shape only since SC-6/SC-13 harnesses inline their own manifests via heredoc; declares schema_version + type: extract-manifest + milestone: M036 + size_cap_bytes + documents array with the supersede-fixture entry at tier 1 / summary_mode operator); tools/verify/m036-p06-fixture-corpus-shape.sh (11-check token-presence verifier on the three corpus fixtures: 5 V1 checks + 3 V2 checks + 2 citer-spec checks + 1 negative gate that the citer must NOT name the v2 successor; AD-19 single-script-file shape; Bash 3.2; set -eu; grep -qF -e leading-dash-safe; PASS:/FAIL:/SUMMARY: stdout); tools/verify/m036-p06-extract-manifest-shape.sh (9-check token-presence verifier on the manifest fixture: schema_version + type + milestone + size_cap_bytes + documents + cite_id + category + tier + summary_mode tokens; AD-19 single-script-file shape; missing-file early-exit guard before checks)"
requires:
  - "P02 closed (extract-manifest contract + manifest field set used as template); P04 closed (reference-corpus fixture-corpus convention + REF-* chunk frontmatter shape used as template); P06/T01 closed (supersede authoring branch consumes the V1->V2 chunk pair to exercise the chain-tip walk); P06/T02 closed (REVIEW: advisory emission consumes the citer-spec chunk to exercise REVIEW emission via the typed-edge traverser)"
affects:
  - "P06/T04 (SC-6 acceptance harness tests/test-reference-supersede-chain.sh stages V1 then mutated REF chunks into a workspace reference root + stages citer-spec into a workspace knowledge/spec/ root; harness drives the live extract + ingest pipelines against the workspace; without the V1+V2 fixture pair the supersede chain-walk has no input; without the citer-spec the REVIEW: emission has no citer to surface); P06/T04 phase-suite aggregator (m036-p06-phase-suite.sh wires the two T03 token-presence shape gates as sub-gates so the fixture corpus is locked at phase-suite time)"
key_files:
  - "tests/fixtures/m036-p06-supersede-corpus/original/cms-rule/REF-cms-rule-supersede-fixture.md,tests/fixtures/m036-p06-supersede-corpus/mutated/cms-rule/REF-cms-rule-supersede-fixture.md,tests/fixtures/m036-p06-supersede-corpus/citer-spec/SPEC-requirement-supersede-citer.md,tests/fixtures/m036-p06-extract-manifest.yaml,tools/verify/m036-p06-fixture-corpus-shape.sh,tools/verify/m036-p06-extract-manifest-shape.sh"
key_decisions:
  - "none"
patterns_established:
  - "M036-canonical fixture-corpus directory naming carried into the supersede surface — tests/fixtures/m036-p06-supersede-corpus/ co-locates V1 + mutated + citer-spec under one milestone/phase-prefixed dir; original/ vs mutated/ split with same path basename lets the T04 harness use a one-line cp -R for either iteration unambiguously; citer-spec/ deliberately sits OUTSIDE the four canonical taxonomy category dirs (cms-rule|training-material|glossary|regulatory-doc) so the M036/P04 ingest taxonomy walker does NOT pick it up — the harness explicitly stages it into a workspace knowledge/spec/ root where the typed-edge traverser will find it; Reference-shape-only manifest fixture pattern — tests/fixtures/m036-p06-extract-manifest.yaml exists primarily as documentation + a stable shape gate (token-presence verifier asserts the field set); the SC-6 / SC-13 harnesses do NOT consume this manifest directly because its source_path points outside any mktemp -d workspace; the harnesses inline their own manifests via heredoc into the workspace root with paths anchored to the workspace; this is the canonical M036 form for fixture manifests whose source_path cannot be made workspace-relative; Placeholder-content_hash fixture-frontmatter convention (carried verbatim from M036/P04) — fixture chunks declare placeholder content_hash values (e.g. ...0001 / ...0002) that are NEVER asserted against the body by token-presence verifiers; the live extract driver computes its own hash from the source binary at extract time so harness-driven verifiers see the real hash; this convention keeps fixtures self-documenting (frontmatter shape complete) without coupling them to a specific build of the hash helpers; Negative-gate-in-positive-verifier pattern — m036-p06-fixture-corpus-shape.sh includes one negative check (citer must NOT name v2 successor) inline alongside ten positive token-presence checks; encoded as if grep ... ; then echo FAIL ; else echo PASS ; fi block separate from the chk() helper; load-bearing because if the citer pointed at the v2 successor directly the supersede chain-walk would have nothing to surface — the test would silently false-pass without this gate; the inline negative-check pattern is reusable for any future verifier that needs to assert the absence of a token alongside positive presence checks; Body-distinctness via literal-string token (BODY V1 vs BODY V2) — the V1 and V2 fixtures share path basename but differ in body content via two distinct literal strings; this guarantees the live extract driver computes distinct sha256 body hashes which fires the content_hash-mismatch gate; the literal-string approach means the verifier can token-presence-check body distinctness without computing hashes (BODY V1 in V1 + BODY V2 in V2 + grep -qF -e); Missing-file early-exit guard pattern in m036-p06-extract-manifest-shape.sh — verifier checks file existence first and exits 1 with a single FAIL line + emits SUMMARY: pass=0 fail=1 if absent; prevents the chk() loop from emitting nine misleading FAILs when the underlying issue is a missing file; reusable for any single-file shape verifier; M036 verifier conventions inherited intact — milestone-prefixed slug (m036-p06-*), AD-19 single-script-file shape, set -eu strict, grep -qF -e leading-dash-safe token-loops, ROOT resolution via ${ORCHESTRATOR_ROOT:-$(pwd)}, structured PASS:/FAIL:/SUMMARY: stdout per MEM001"
drill_down_paths:
  - ".orchestrator/milestones/M036/phases/P06/tasks/T03-supersede-fixture-corpus-PAYLOAD.md"
duration: "10m"
verification_result: "pass"
completed_at: "2026-05-02T20:32:19Z"
---

T03 lands the on-disk fixture corpus + single-document extract manifest fixture + two token-presence shape verifiers that gate the fixture shape. The fixtures are the inputs to the SC-6 acceptance harness in T04 (tests/test-reference-supersede-chain.sh), which copies V1 first, drives extract+ingest, then copies the mutated V2 in place, drives extract+ingest again, and asserts the supersede chain authoring branch + REVIEW: emission paths fire end-to-end. T03 is fixture-only — no driver edits, no helper-lib edits, no test-harness authoring. All work is purely creating files at the paths declared by the plan + chmod +x on the two new verifier scripts.

What was built:

- tests/fixtures/m036-p06-supersede-corpus/original/cms-rule/REF-cms-rule-supersede-fixture.md — V1 reference chunk with full FR-2 frontmatter (cms-rule category, tier 1, version 1, content_hash placeholder ...0001, topic_tags [pbj-staffing], applies_to_field [staff_count]) and a body containing the literal token BODY V1.
- tests/fixtures/m036-p06-supersede-corpus/mutated/cms-rule/REF-cms-rule-supersede-fixture.md — V2 mutated successor at the same path basename with version 2 + content_hash placeholder ...0002 and body containing BODY V2. The basename match is intentional: the T04 harness copies original/ for the first iteration then overwrites with mutated/ for the second so the live extract driver sees a single stable file path with mutated content between runs.
- tests/fixtures/m036-p06-supersede-corpus/citer-spec/SPEC-requirement-supersede-citer.md — synthetic spec chunk declaring cites: [REF-cms-rule-supersede-fixture] in frontmatter (V1 chunk_id, NOT the v2 successor). Lives under citer-spec/ rather than a taxonomy category directory so it is NOT picked up by the M036/P04 ingest taxonomy walker; the T04 harness stages it explicitly into knowledge/spec/<scope>/ within the workspace.
- tests/fixtures/m036-p06-extract-manifest.yaml — reference-shape single-document extract manifest with schema_version + type + milestone + size_cap_bytes + documents array entry covering the supersede-fixture document at tier 1 / summary_mode operator. Reference-shape only because the source_path points outside any mktemp -d workspace; SC-6/SC-13 harnesses inline their own manifests via heredoc.
- tools/verify/m036-p06-fixture-corpus-shape.sh — 11-check token-presence verifier (5 V1 + 3 V2 + 2 citer-spec + 1 inline negative gate that citer-spec must NOT name v2 successor). chmod +x.
- tools/verify/m036-p06-extract-manifest-shape.sh — 9-check token-presence verifier (schema_version + type + milestone + size_cap_bytes + documents + cite_id + category + tier + summary_mode) with missing-file early-exit guard. chmod +x.

Verification:

- bash tools/verify/m036-p06-fixture-corpus-shape.sh -> SUMMARY: m036-p06-fixture-corpus-shape.sh pass=11 fail=0 (matches expected pass=11 fail=0 from plan Notes section).
- bash tools/verify/m036-p06-extract-manifest-shape.sh -> SUMMARY: m036-p06-extract-manifest-shape.sh pass=9 fail=0 (matches expected pass=9 fail=0 from plan Notes section).

Both verifiers PASS first-try. No mid-task corrections.

Forward notes:

- T04 (acceptance harnesses + phase-suite aggregator) consumes the V1 + mutated + citer-spec triplet via the SC-6 harness tests/test-reference-supersede-chain.sh: copy original/cms-rule/REF-...md into a workspace reference-root/cms-rule/, drive extract+ingest, then overwrite with mutated/cms-rule/REF-...md, drive extract+ingest again, stage the citer-spec into the workspace knowledge/spec/<scope>/ between runs so rebuild-index registers the cites edge; assert the chain-tip file exists, the prior chunk gained superseded_by:, and a REVIEW: line surfaced naming the citer + the superseded target + the chain tip.
- T04 phase-suite aggregator (m036-p06-phase-suite.sh) wires both T03 verifiers as sub-gates so the fixture corpus shape is locked at phase-suite time.
- Placeholder content_hash convention (carried verbatim from M036/P04 fixture corpus) — fixture frontmatter declares placeholder hashes (...0001 / ...0002) that are documentation-shape only; the live extract driver computes real hashes from source bodies at extract time. The token-presence verifiers do NOT assert content_hash equality.
