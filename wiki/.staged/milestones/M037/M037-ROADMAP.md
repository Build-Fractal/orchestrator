---
schema_version: "1.0"
type: roadmap
milestone: "M037"
feature_ref: "038-wiki-team-feedback-ready"
feature_spec: "specs/038-wiki-team-feedback-ready/spec.md"
vision: "Orchestrator-managed wikis render as usable reader surfaces for non-author SMEs (P01 closed 2026-05-07), publishing-pipeline robustness lands in P02 to unblock the PBJ-team dogfood signal this week (workflow-based Pages publishing + private-repo site_url + feedback/ routing + output-noise polish), and round-3.5 polish (P03) ships gated on first PBJ feedback signal — wiki-quality noise does not contaminate the M035-launch validation loop."
tier: "C"
created_at: "2026-05-06"
updated_at: "2026-05-07"
revised: "2026-05-07 (publishing-robustness phase insertion — P01 closed 2026-05-07T00:54Z; new P02 inserted to absorb FR-18..FR-22 (US-10..US-14, F8/F10/F11/F12/F13) which folded into the spec via the 2026-05-07 amendment AFTER P01 close. F12 (FR-19+FR-20) is HIGH severity — 7-day stuck deploy in PBJ dogfood — and time-sensitive; cannot wait on round-3.5 feedback gate. Original P02 round-3.5 polish renumbered to P03; gate semantics preserved (≥3 PBJ nav-or-section-shape feedback items against P01+P02 wiki). Sources: papercut-sweep-wiki-deploy-2026-05-07.md (F8/F10/F11) + papercut-handoff-wiki-publishing-robustness-2026-05-07.md (F12/F13 with verbatim workflow YAML + regression-test scaffolds from PBJ commit e7a722e). 2026-05-06 theme-leverage amendment preserved verbatim in P03 boundary map.)"
---

## Phases

- [x] **P01**: Wiki team-feedback-ready ship-it minimum — "A non-author reader (PBJ domain SME) opens an orchestrator-managed wiki and lands on a card-grid homepage, scans a reference nav of human-readable strings (not slug-soup), reads a decisions TOC of human concepts (not codes), sees top-level sections in a sticky tab header with a 2-level TOC and an edit-this-page affordance, and the operator's `wiki:` block in `.orchestrator/config.yml` survives `orchestrator:update` byte-identical." **CLOSED 2026-05-07T00:54Z** — 6 task tranches, 9-gate phase-suite PASS, SC-1..SC-5 acceptance battery PASS (`pass=5 fail=0`). See `phases/P01/P01-SUMMARY.md`.
  - Risk: high
  - Depends: none
  - Blocked by: [M032](../../milestones/M032/index.md) closed (2026-05-05), M036a closed (2026-05-02), papercut-sweep rounds 1+2 (commits e4c3c8f7 + 90c18f07)
  - Boundary Map:
    - Produces: `templates/wiki-index-cards.md.tmpl` (new card-grid template, FR-1) | `wiki.landing_cards:` config schema (FR-2/3/4) | projected `wiki/docs/index.md` (FR-4) | `wiki-generate-stubs.sh` `version:`→`title:` projection with MIT-01 conditional-overwrite + Typeset-evaluation gate per US-2 AS-5 theme-leverage amendment (FR-5) | `wiki-generate-nav.sh` honoring stub `title:` (FR-6) | restructured [`.orchestrator/DECISIONS.md`](../../decisions.md) from 7-column markdown table to ~150 `### Title { #dr-code-nnn }` heading entries with code-chip body markup, preserving every legacy permalink (FR-7 — scope expanded per /orchestrator-plan-phase plan-time discovery: file is a markdown table not heading-shape today, operator confirmed Option 1 "restructure now") | `references/authoring-conventions.md` covering (a) DR-/BG-/AN-/MEM-/Q-### code conventions and (b) **already-enabled-but-undocumented mkdocs-material features** per theme-leverage amendment: Mermaid via superfences, snippets, admonition variants, attr_list chips, content tabs, and the new `pymdownx.details` + `pymdownx.tasklist` extensions added in FR-9 (FR-8) | `commands/dispatch.md` payload-guidance reference to convention (FR-8) | `mkdocs.yml` template additions: `navigation.tabs` + `navigation.tabs.sticky` + **`navigation.prune`** + `toc.toc_depth: 2` + **`pymdownx.details`** + **`pymdownx.tasklist` (custom_checkbox: true)** + `content.action.edit` + `content.action.view` + `edit_uri:` derived from `repo_url:` (FR-9 — three additions in **bold** folded in via theme-leverage amendment) | shared YAML-key-preservation merge primitive parameterized over (file path, managed-namespace list) consumed by FR-9 + FR-10 (resolves #Q-4) | install-template emission logic for `.orchestrator/config.yml` + `mkdocs.yml` honoring CON-3 byte-identical preservation, with DISP-1 plan-time managed-key namespace cross-reference artifact against PBJ-central live config (FR-10) | fail-closed-on-malformed-YAML behavior in install-template refresh (FR-11) | acceptance battery aggregator stub at `tests/m037-acceptance/run-acceptance-battery.sh` (SC-12 scaffold)
    - Consumes: M032 wiki tooling baseline (`scripts/wiki/`, `mkdocs.yml` template, `wiki-init.sh`, install-template plumbing) | M036a chunk corpus with `version:` + `topic_tags:` + `external_pointer:` frontmatter | papercut-sweep clean `mkdocs build --strict` baseline | this repo's [`.orchestrator/DECISIONS.md`](../../decisions.md) (heading-shape migration target) | PBJ-central `.orchestrator/config.yml` as live cross-reference target for DISP-1 namespace classification (read-only)

- [x] **P02**: Publishing-robustness paper-cut bundle — F8/F10/F11/F12/F13 from the 2026-05-07 spec amendment — "After P02 lands, an operator pushing a wiki content change to main triggers a `Deploy wiki to Pages` Actions workflow run within ~10s that completes within ~2 min, the live PBJ wiki reflects the change end-to-end without `mkdocs gh-deploy` invocation, private-repo dogfood projects render styled 404 pages (relative-asset-path mode), `wiki-deploy.sh` summarizes OUT-OF-SCOPE noise to ≤5 unique lines instead of 178, `KNOWLEDGE.md` cross-links to `.orchestrator/feedback/*.md` resolve via a `feedback/` routing arm in stub generation, and `wiki/README.md` carries the org-level-discussions-redirect recovery callout."
  - Risk: high
  - Depends: P01
  - Blocked by: none (handoff doc carries verbatim workflow YAML reference impl from PBJ-central commit `e7a722e` + verbatim regression-test scaffolds; surface area is mechanical)
  - Boundary Map:
    - Produces: `scripts/wiki/wiki-generate-stubs.sh` new routing arm `feedback:<basename>` mirroring `proposals:*` shape — inputs `.orchestrator/feedback/*.md`, outputs `wiki/docs/feedback/<basename>.md` stubs, fragment-only passthrough (`rewrite-relative-urls=false`), title from H1 with humanized-basename fallback (debug-level diagnostic on fallback), `register_child` section-index emission, idempotent removal on source-file deletion (FR-18) | install-template emission of `.github/workflows/pages.yml` with four-component shape verbatim from handoff doc / PBJ commit `e7a722e` (`actions/configure-pages@v5` + `actions/upload-pages-artifact@v3` + `actions/deploy-pages@v4`, two-job `build` + `deploy` topology, `push: branches: [main]` + `workflow_dispatch` triggers, Python 3.12 + pip-cache on `wiki/requirements.txt`, `concurrency: pages` group with `cancel-in-progress: false`, `permissions: contents:read pages:write id-token:write`) (FR-19) | `scripts/lifecycle/wiki-init.sh` `gh api -X PUT "repos/$OWNER/$REPO/pages" -f build_type=workflow` invocation with manual-fallback diagnostic on `gh` unavailable / unauthenticated; install-template MUST emit `wiki/requirements.txt` if absent (same pinned mkdocs + mkdocs-material + plugin deps as existing tooling); CON-3 preserve pre-existing `.github/workflows/pages.yml` (diagnostic only, no clobber) (FR-19) | `scripts/wiki/wiki-deploy.sh` demoted from live-deploy primitive to pre-push validation only — keep gates 1-4 (giscus-config-check + mkdocs build + link-check + giscus-smoke), drop gate 5 (`mkdocs gh-deploy --force`); replace live path with `OK: pre-deploy gates PASS. Push to main to trigger workflow deploy:` print of `git push origin main` + workflow URL, exit 0 cleanly (FR-20) | M032-shipped `docs/wiki-deploy-quickstart.md` (or sibling) updated as part of FR-20 done-definition to reflect "git push triggers deploy" — no remaining `bash scripts/wiki/wiki-deploy.sh` invocations as the live-deploy path; cross-milestone touch owned by M037 (FR-20) | `scripts/lifecycle/wiki-init.sh` repo-visibility branch via `gh api "repos/$OWNER/$REPO" --jq .visibility 2>/dev/null` with fallback to `public` on `gh` unavailable; for `private`: write empty `site_url:` (or omit the line) in rendered `wiki/mkdocs.yml` with inline comment noting private-pages relative-path rationale; for `public`: existing `site_url: "https://<owner>.github.io/<repo>/"` shape MUST NOT regress; public→private transition handled by next `orchestrator:update` re-run via P01 yaml-merge primitive (FR-21) | `scripts/wiki/wiki-deploy.sh` OUT-OF-SCOPE diagnostic-budget collapse — keep first 3-5 unique target URLs as full diagnostic lines, collapse remainder to a single summary line per target `OUT-OF-SCOPE: <N> pages -> <url> [external <descriptor>]`; `--verbose` flag disables collapsing for debugging; honors zero-OUT-OF-SCOPE fixture (no false summary line) (FR-22a) | `wiki/README.md` template (install-template-emitted) `First-deploy checklist` section with org-level-discussions-redirect callout naming both the symptom (`<Org>/<Repo>/discussions` 302s to `orgs/<Org>/discussions`) AND the recovery path (`<Org>/<Repo>/discussions/categories` direct URL); pre-existing checklist items byte-preserved; only the new callout is added (FR-22b) | `tests/m037-acceptance/p01-feedback-routing.sh` (SC-13 — three-fixture corpus: H1-derived title, humanized-basename fallback, idempotent removal) | `tests/test-wiki-init-workflow-mode.sh` (SC-14 — verbatim from handoff doc; asserts workflow YAML scaffold + no `mkdocs gh-deploy` in `wiki-deploy.sh` live path; manual end-to-end smoke evidence captured in `M037-ACCEPTANCE-EVIDENCE.md`) | `tests/test-wiki-init-private-site-url.sh` (SC-15 — verbatim from handoff doc; both visibility branches) | `tests/m037-acceptance/p01-out-of-scope-collapse.sh` (SC-16 — 178-page synthetic fixture + mixed-target diagnostic-budget rule) | `tests/m037-acceptance/p01-discussions-callout.sh` (SC-17 — README callout + M032 quickstart docs grep verifies "git push triggers deploy") | acceptance battery aggregator extension: SC-13..SC-17 added to `tests/m037-acceptance/run-acceptance-battery.sh` (target output `BATTERY: pass=10 fail=0` after P02 close; SC-6..SC-9 + SC-10 + SC-11 + SC-12 still ride P03)
    - Consumes: P01 yaml-merge primitive (`scripts/lib/yaml-merge.sh`) for `mkdocs.yml` `site_url:` mutation under operator-key preservation | P01 `scripts/lifecycle/wiki-init.sh` baseline (already extended in P01/T05 with `edit_uri:` substitution and CON-4 default-branch helper) | P01 `scripts/wiki/wiki-generate-stubs.sh` baseline (T01-T02 routing-arm pattern for `proposals:*` mirrored by FR-18) | P01 `scripts/wiki/resolve-default-branch.sh` (CON-4 helper, used by any branch-aware paths) | P01 `tests/m037-acceptance/run-acceptance-battery.sh` aggregator (target for SC-13..SC-17 extension) | M032 `scripts/wiki/wiki-deploy.sh` baseline (gate 1-4 plumbing, modified by FR-20 + FR-22a) | M032 `docs/wiki-deploy-quickstart.md` (or sibling, target of FR-20 cross-milestone update) | handoff doc verbatim workflow YAML reference impl + regression-test scaffolds ([`.orchestrator/proposals/papercut-handoff-wiki-publishing-robustness-2026-05-07.md`](../../proposals/papercut-handoff-wiki-publishing-robustness-2026-05-07.md)) | wiki-deploy paper-cut sweep canonical record ([`.orchestrator/proposals/papercut-sweep-wiki-deploy-2026-05-07.md`](../../proposals/papercut-sweep-wiki-deploy-2026-05-07.md)) | dogfood project commit `e7a722e` workflow YAML reference

- [ ] **P03**: Round-3.5 polish — tag-driven nav + GitHub source-link + knowledge card grid + 3 plugins — "After ≥3 PBJ-team nav-or-section-shape feedback items land against the P01+P02 wiki, a non-author reader navigating the reference nav sees entries grouped under deterministic config-driven buckets (QSO letters, policy manuals & FAQs, etc.) instead of a flat 70-entry list, every Tier 0/1 chunk page carries a working GitHub source-link affordance, `/knowledge/` renders as a card grid surfacing major knowledge areas, and content pages carry tag-pivot pages + last-modified timestamps." (Was P02 in the 2026-05-06 roadmap; renumbered 2026-05-07 when P02 was inserted for publishing-robustness.)
  - Risk: medium
  - Depends: P01, P02
  - Blocked by: ≥3 nav-or-section-shape feedback items filed by PBJ team against P01+P02 wiki (per #Q-6 (a) resolution; operator override available if PBJ review unexpectedly quiet)
  - Boundary Map:
    - Produces: `wiki.nav_buckets:` config schema (FR-12) | `wiki-generate-nav.sh` tag-driven subgrouping with deterministic config-declaration order + "Uncategorized" sibling bucket for tag-less chunks (FR-12) | GitHub source-link rewrite at projection time in stub generator metadata-table emission (Tier 0) + Tier 1 chunk-projection pass, with default-branch fallback to `main` per CON-4 (FR-13) | `wiki.knowledge_cards:` config schema OR scoped reuse of `wiki.landing_cards:` (final shape decided at P03 plan-phase from PBJ signal per #Q-6 (b)) (FR-14) | `/knowledge/` projection via card-grid template + power-user URL `/knowledge/_index/` for legacy auto-projected content (FR-14) | `wiki/requirements.txt` extends with version-pinned `mkdocs-redirects` + `mkdocs-git-revision-date-localized-plugin` **(only two external deps — Material's built-in `tags` plugin replaces the originally-planned external `mkdocs-tags` per theme-leverage amendment)** (FR-15) | `mkdocs.yml` template plugin entries for the three plugin capabilities — `tags` (built-in), `redirects` (external), `git-revision-date-localized` (external) (FR-15) | `topic_tags:` → `tags:` projection-time adapter preserving operator-authored `tags:` when both fields present per Edge-Case rule (FR-15) | **theme-leverage Tier 2 candidates (gated on first PBJ feedback signal per #Q-6 (a))**: Material built-in `social` plugin auto-OG-cards with `mkdocs-material[imaging]` system-deps probe in `wiki-init.sh` (FR-16) | Material built-in `meta` plugin folder-frontmatter-defaults pattern with `wiki/docs/<section>/.meta.yml` example (FR-17) | acceptance battery completion (SC-6..SC-9 + SC-10 strict-build smoke + SC-11 PBJ-update evidence + SC-12 aggregator + optional SC-9 extensions for FR-16/FR-17 if shipped)
    - Consumes: P01 card-grid template primitive (`templates/wiki-index-cards.md.tmpl`) for FR-14 reuse | P01 `wiki.landing_cards:` schema as section-scoping precedent for FR-14 final shape | P01 shared YAML-merge primitive for `wiki.nav_buckets:` + `wiki.knowledge_cards:` operator-authored-key preservation | P01 `wiki-generate-nav.sh` baseline for FR-12 modifications | P01 `wiki-generate-stubs.sh` baseline for FR-13 metadata-table-emission modifications (also extended by P02 FR-18) | P02 `wiki/requirements.txt` (P02 FR-19 emits the file if absent; P03 FR-15 extends it with two more pinned plugins) | M036a chunk corpus `topic_tags:` + `external_pointer:` frontmatter | `mkdocs.yml` `repo_url:` for FR-13 source-link rewrite

## Cross-Cutting Concerns

- **CON-3 — Operator-authored content preservation across `orchestrator:update` refreshes**: P01, P02, P03. P01 established the shared YAML-key-preservation merge primitive (consumed by FR-9 mkdocs.yml + FR-10 config.yml) and the DISP-1 plan-time managed-key namespace cross-reference artifact. P02 reuses the primitive for FR-21's `site_url:` mutation (operator-authored sibling keys preserved byte-identical). P02 FR-19 also extends CON-3 to a new managed namespace: `.github/workflows/pages.yml` is template-emitted with whole-file CON-3 preservation (pre-existing operator-authored workflow → diagnostic + no clobber, no per-key merge). P03's new schemas (`wiki.nav_buckets:` for FR-12, `wiki.knowledge_cards:` or scoped `wiki.landing_cards:` for FR-14) MUST be classified through the P01 primitive — operator-authored values for these keys survive byte-identical, orchestrator-managed defaults merge underneath. Hard contract; any violation is a P0 bug.
- **CON-2 — Projection-not-source-mutation**: P01, P03. P01 established the pattern with FR-5 (`version:` → stub `title:` at projection time, source frontmatter unchanged). P03's FR-13 (GitHub source-link rewrite) and FR-15 (`topic_tags:` → `tags:` adapter) MUST conform — both operate at projection time and preserve source chunks byte-identical. P02 is install-template/init-time work, not projection — CON-2 doesn't bind P02 directly, but FR-18's `feedback/<basename>.md` stubs are projection-time outputs and source files in `.orchestrator/feedback/` remain authoritative.
- **CON-4 — Default-branch derivation falls back to `main`**: P01 (FR-9 `edit_uri:`), P02 (any FR-19 workflow YAML branch references — currently `push: branches: [main]` is hard-coded per handoff doc reference impl; if a future operator runs on a non-main default branch, the workflow trigger mismatch is the same class of issue CON-4 addresses), P03 (FR-13 source-link rewrite). Single helper script (`scripts/wiki/resolve-default-branch.sh`) shipped in P01, consumed by P03 directly. P02's FR-19 workflow YAML inherits the `main` hard-code from the handoff reference impl — flag for operator override at install-template emit time as a P03 follow-up if PBJ feedback surfaces non-main usage.
- **CON-1 — Zero new mkdocs plugin dependencies in P01**: P01 only (no longer binding for P02/P03). P02 introduces no new mkdocs plugins (FR-19's `wiki/requirements.txt` emit uses the same pinned mkdocs + mkdocs-material + plugin deps as existing tooling — net-new toolchain dependency is zero). P03 introduces two external pip plugins per FR-15 (`mkdocs-redirects`, `mkdocs-git-revision-date-localized-plugin`); CON-1 was scoped to P01 to keep the ship-it minimum bloat-free.
- **Card-grid template primitive**: P01 establishes (`templates/wiki-index-cards.md.tmpl`, FR-1); P03 reuses for `/knowledge/` (FR-14). Template authoring cost paid once. Consuming phase (P03) MUST NOT reinvent the template — config-only customization via the `wiki.knowledge_cards:` (or scoped `wiki.landing_cards:`) schema. P02 does not consume the card-grid primitive.
- **YAML-merge primitive (`scripts/lib/yaml-merge.sh`)**: P01 established (consumed by FR-9 + FR-10). P02 consumes for FR-21 `site_url:` mutation under operator-key preservation. P03 consumes for `wiki.nav_buckets:` + `wiki.knowledge_cards:` schema introductions. The primitive is now load-bearing across three phases — any P02/P03 modification MUST extend rather than fork the primitive.
- **MIT-01/MIT-02 P0 — Conditional-overwrite escape hatch + escape-hatch fixture**: P01 only. The `version:` → `title:` projection MUST NOT overwrite stubs carrying `auto_generated: false` frontmatter; SC-2 fixture verified at P01 close. P02 FR-18's `feedback/<basename>.md` stubs are auto-generated by the new routing arm — if an operator hand-edits a feedback stub, the same `auto_generated: false` escape-hatch pattern from P01/T02 applies (read at re-generation, skip overwrite). P02 plan MUST inherit this pattern explicitly.
- **MIT-03 P0 — CON-3 back-reference**: P01 (FR-9 mkdocs.yml + FR-10 config.yml). Resolved at P01 close via the shared YAML-merge primitive. P02 FR-21 inherits the same back-reference: any `site_url:` mutation lands through the primitive, not via direct file rewrite.
- **DISP-1 plan-time gate (arbiter-ruled)**: P01 (resolved at close — 15-key cross-reference table in T06 plan). P02's FR-19 workflow YAML and `wiki/requirements.txt` introduce two new template-emit surfaces. P02 plan-phase MUST extend the managed-namespace cross-reference table: `.github/workflows/pages.yml` (whole-file managed) + `wiki/requirements.txt` (managed flat list, operator-pinned overrides survive). Operator-confirmation flagging applies for any new managed-namespace key.
- **Knowledge-Layer Boundary (M037 vs. M020/M036a)**: P01, P02, P03. All three phases are presentation-layer + tooling-layer changes only. NONE of the phases touch `knowledge/<category>/MEM*.md` content, `.orchestrator/knowledge/KNOWLEDGE-INDEX.md`, chunk frontmatter schema (read-only), edge-type catalogue, spec-chunk metrics, or tier-0/1/2 extraction adapters. P02 FR-18 reads `.orchestrator/feedback/*.md` source files but does not mutate them or the knowledge graph. Crossing the boundary requires a new spec.
- **Acceptance battery aggregator**: P01 scaffolded `tests/m037-acceptance/run-acceptance-battery.sh` covering SC-1..SC-5 (`pass=5 fail=0` at P01 close); P02 extends to SC-13..SC-17 (target `pass=10 fail=0` at P02 close); P03 extends to SC-6..SC-9 + SC-10 strict-build smoke + SC-11 PBJ-update evidence + SC-12 closure aggregator. Verification ladder applies at the milestone boundary, not per phase.
- **Cross-milestone touch (FR-20 → M032 quickstart docs)**: P02 only. M037 owns the update to M032-shipped `docs/wiki-deploy-quickstart.md` (or sibling) reflecting the new "git push triggers deploy" flow. M032 is closed (2026-05-05); the cross-milestone touch is FR-20-scoped done-definition, not a milestone reopen.

## Dependency Graph

```
            [ M032 closed ]
            [ M036a closed ]
            [ papercut-sweep R1+R2 ]
                    │
                    ▼
                  P01  ── CLOSED 2026-05-07T00:54Z
                    │
                    ▼
                  P02  ── publishing-robustness paper-cut bundle
                    │
                    │  (gated: ≥3 PBJ nav-or-section-shape feedback items
                    │         filed against P01+P02 wiki)
                    ▼
                  P03  ── round-3.5 polish (was P02 pre-2026-05-07)
```

Linear P01 → P02 → P03 with an external feedback-signal gate between P02 and P03. No internal cycles. No concurrent execution lanes.

## Execution Order

1. **P01** — closed 2026-05-07T00:54Z. No further action; serves as upstream producer for P02 (yaml-merge primitive, wiki-init.sh + wiki-generate-stubs.sh baselines, CON-4 helper, acceptance-battery aggregator) and P03 (card-grid template primitive, landing_cards schema).
2. **P02** — depends on P01 (closed). High-risk per FR-043: F12 (FR-19+FR-20) gates the entire downstream PBJ-team dogfood signal loop — a stuck legacy `pages-build-deployment` builder wedged the dogfood project for 7 days. Mitigated by handoff doc carrying verbatim workflow YAML reference impl from PBJ commit `e7a722e` + verbatim regression-test scaffolds. F13 (FR-21) is medium-high (unstyled 404 on every PBJ broken-nav click). F8/F10/F11 (FR-18 + FR-22a + FR-22b) are low-to-medium severity and ride P02 because they share `wiki-init.sh` / `wiki-deploy.sh` / install-template surface area with F12+F13 — bundling avoids two ship cycles. No internal P02 cycles; no concurrent execution lanes (single-phase ship).
3. **P03** — gated on (P02 merged) AND (≥3 PBJ nav-or-section-shape feedback items filed against the P01+P02 wiki). Operator override path available if PBJ review unexpectedly quiet. Cannot execute concurrently with P02 (P03 FR-13 source-link rewrite consumes `wiki-init.sh` baseline that P02 modifies; P03 FR-15 extends `wiki/requirements.txt` which P02 FR-19 emits if absent). Medium-risk — smaller scope, plugin additions introduce toolchain-conflict risk.

## Validation

- **No conflicting producers**: PASS. Each FR maps cleanly to exactly one phase. Card-grid template primitive (FR-1) is produced by P01 and reused (consumed) by P03 via FR-14 — reuse, not re-production. Shared YAML-merge primitive is produced by P01 (FR-9/FR-10 collapse), consumed by P02 (FR-21) and P03 (new schemas). `wiki-init.sh` is modified across P01 (T05 `edit_uri:`) + P02 (FR-19 + FR-21 visibility branch) + P03 (potentially FR-16 imaging-deps probe if shipped) — each phase modifies a distinct managed namespace within the file; per-phase ownership is non-overlapping. `wiki-deploy.sh` is modified across P01 (paper-cut commits, pre-M037-roadmap) + P02 (FR-20 demote + FR-22a OUT-OF-SCOPE collapse) — P02 wholly owns the `wiki-deploy.sh` modifications scoped to this milestone. `wiki-generate-stubs.sh` is modified across P01 (FR-5 `version:` → `title:`) + P02 (FR-18 `feedback:` routing arm) + P03 (FR-13 source-link rewrite) — three additive, non-overlapping changes.
- **All consumed items have producers**: PASS. P01 consumed from closed upstream milestones (M032, M036a, papercut-sweep) and PBJ-central live config (read-only). P02 consumes from P01 (closed — yaml-merge primitive, wiki-init.sh / wiki-generate-stubs.sh baselines, CON-4 helper, acceptance-battery aggregator) + M032-closed quickstart docs (FR-20 cross-milestone update target) + handoff doc + paper-cut sweep + dogfood commit `e7a722e` (external static refs). P03 consumes from P01 (closed) and P02 (will-be-closed at P03 entry; specifically `wiki/requirements.txt` emit + `wiki-generate-stubs.sh` extension). Every consume entry maps to a named produces entry or external static reference.
- **DAG is acyclic**: PASS. Linear P01 → P02 → P03; no cycles. Feedback gate between P02 and P03 is an external signal, not an internal edge.
- **Demo sentence coverage**: PASS. P01's demo sentence is concrete and observable (verified at close — homepage card grid + reference nav human strings + decisions TOC concepts + tabbed header + 2-level TOC + edit affordance + `orchestrator:update` survives `wiki:` block byte-identical). P02's demo sentence is concrete and observable (workflow Actions run within ~10s + completes within ~2 min + live wiki reflects change end-to-end + private-repo styled 404 + ≤5 OUT-OF-SCOPE summary lines + `KNOWLEDGE.md` feedback cross-links resolve + README discussions callout). P03's demo sentence is concrete and observable (tag-driven nav buckets + GitHub source-link affordance + `/knowledge/` card grid + tag-pivot pages + last-modified timestamps).
